# Innovation POD — the showcase, reviewed forward and backward by the 48-agent fleet

**Commit a4d1eb4 · three real phones · 65 steps, 0 failures · https://exel-ai-polling.explore-096.workers.dev/soi-session**

## 1 · The headline

The pod was asked to showcase two claims — **input in time** and **local minimum wage authorizing value** — and to mint M
as a plan accepted by the trio before the clock. Twelve Ascended Masters each fielded two independent specialists and one
Voting & Summarizer; twelve Master of Thought coordinators asked of each package the protocol's one question — *class or
instance?* Every comment below is exactly 111 words and cites the code or the run log; the synthesis is exactly 333.

Grades: **B · B- · B · B- · B- · B · B · C+ · B · B- · B- · C+**. NOT WORKING: Odin.

## 2 · The evidence the fleet read

- `docs/assessments/pod-live-run-2026-09-11/log.txt` — 65 timestamped steps on three Chromium phones over the local Realtime relay, the app's own supabase-js client and the sacred broadcast hook unmodified; screenshots `1-compose-plan` … `8-closed-receipt` per phone.
- `docs/assessments/2026-09-11_pod_time_volunteer_vs_paid.md` — time documented from the shipped clock and mint: two segments, a capped claim, three currencies, ♡ vs 웃 with no minute in both columns.
- The code: `frontend/app/soi-session/page.tsx` and `frontend/lib/pod-*.ts`; the gate `frontend/tests/pod-invariant.test.mjs` (310).

## 3 · SPIRAL — forward 1 → 10, backward 10 → 1, in every lens

**Aset** (Δ) — *forward:* Cube 1 (session/roster over the poll channel) — log `6411ms ana OK assigned seat 2` … `7218ms lead OK lead sees Ana in NGN and Bo in PHP (elections replicated)`; Member carries region/outcome (pod-roster.ts:46,:52), own-seat-only enforced. PASS. Cube 2/3 (record) — one outcome per member, own seat (page.tsx:1225; log `33413ms ana OK all three outcomes in → audit unlocked`). PASS. Cube 4 (collector/roster merge) — merge never erases an election or a filled outcome (gated). PASS. Cube 5 (gateway: platform clock) — one button, segments summed, replicated (pod-clock.ts:48; log `33201ms bo OK sees segment 2`). PASS on the clock; FAIL on the name: `witnessedHours` (page.tsx:534) is the sum of claims and `measuredHours` (:444) is what the platform witnessed — the canonical word 'witnessed hours' is bound to the wrong number, and PodFacts ships both under near-identical names. Cube 6 (AI synthesis/PodFacts) — pod_router.py:76 'M × qualified time' is the banned term; not scanned by the gate. FAIL (should-fix). Cube 7 — not touched (diff stat has no cube7/ranking file); stated as such. Cube 8 (mint/plan/tranches/ceiling/settlement) — mint is one function and M is locked with the plan (pod-baseline.ts:52, page.tsx:529). PASS on the primitives; FAIL on the carriers: receipt Plan→actual mixes clock-hours and person-hours (page.tsx:1465-1466, screenshot '0.01 h clocked … 웃 0.061 actual at 3×'); vintage stamped once at pod rate (:1442), members settled at current rate without D9 (:1476); ceiling applied to the pod sum while the words say 'each' (:592/:1434). Cube 9 (receipt/ledger) — hours printed under 웃 (page.tsx:1433) and ♡ (lexicon-data.ts:795) glyphs; the three outcomes are not on the receipt though the log says they are (script :114). FAIL. Cube 10 (replay/determinism) — gate 310 passed, 0 failed; report reproducible from fixed timestamps. PASS, with the caveat that the report fabricates per-member vintages (pod-time-report.mjs:44) and the gate pins a restated mint (pod-baseline.ts:55).  
*backward:* Cube 10→9: the receipt replays from the device ledger, which does include regionIdSel and lock (page.tsx:474) — but each phone's ledger holds ITS OWN regionIdSel, so replay is deterministic per phone and inconsistent across phones for inheriting members — FAIL (the receipt can lie about an inherited floor). 9→8: stand.earned and tranches derive from witnessedHours through mint(); vintage stamp written once (`v ?? stamp(...)`, page.tsx:1444) — PASS for 웃; the stamp records podJuris' rate only, not each contributor's, while the receipt claims "Each at their own floor" and the time document claims D9 per member — FAIL (carriers disagree on what D9 stamped). 8→7: not touched. 8→6: the backend synthesis can still describe a session without its segments/outcomes (pod_router.py:106-118 context) — FAIL (a synthesis that ignores the clock can contradict the receipt). 6→5: measure() is pure, sorted, append-tolerant (pod-clock.ts:46-58; gate 'a Start while running is ignored', 'a Stop while stopped is ignored') — PASS; the clock cannot start without a lock (page.tsx:1145 disabled={!lock}) — PASS. 5→4: clock events accepted only from a known phone (gate 'accepted only from a phone the roster knows') — PASS. 4→1: the plan (hours, m, hash) reaches every joiner in the brief (page.tsx:305, 331-332; gate 'the ACCEPTED plan reaches every phone with its hash') — PASS; the pod default locality does not — FAIL. Net: the receipt can lie upstream only through the unreplicated pod default floor and through the two-meaning "actual"; the mint, the clock and the plan lock hold.

**Asar** (Δ) — *forward:* 1 Session/roster — PASS: log "6458ms lead OK lead sees 3 in the pod", "7218ms lead OK lead sees Ana in NGN and Bo in PHP (elections replicated)"; elections/approvals own-seat-only (log 6548-7210ms). 2/3 Record — PASS for the shared written record and three own-seat outcomes (log 33345-33413ms; 6-record-three-outcomes-ana.jpg shows the three inputs). 4 Collector — PASS: outcomes and clock events reach all three phones (log 33195-33201ms "sees segment 2 (clock events replicated)"). 5 Gateway/platform clock — PASS: one button Start/Stop/Add time (log 8540/20789/21189ms), joiner Stop closes for everyone (33317ms), measure() sums segments and ignores the gap (pod-clock.ts:48-59), 5-active-two-segments-lead.jpg shows "total 0:00:24 · 0.0024..0241". 6 AI synthesis/PodFacts — FAIL in this lens: PodFacts gained witnessed_for/segments/member_outcomes (pod_router.py:47-49) but the context handed to the provider (pod_router.py:99-111) omits all three; the local buildSynthesis333 has no segment or member-outcome input (pod-synthesis.ts:33-47, 124-131), so the 8-closed-receipt synthesis reads "0.02 cross-reviewed hours settled as 0.06 웃" with no segments and no per-member outcomes, padded by POOL filler ("Classification precedes settlement…") that describes nothing this pod did. 7 Ranking — NOT TOUCHED by this diff; say so. 8 Tokens — PASS with a basis caveat: mint(witnessedHours, M) where witnessedHours is the SUM of three capped claims (page.tsx:533-535); claims of 1 h capped to 0.0067 h each (7-audit-witness-lead.jpg "counted as 0.01 h — the pod was witnessed for 0:00:24"); M locked, no picker (log 36899ms); per-member settlement in USD/NGN/PHP on the receipt (log 37222ms, page.tsx:1474-1477). 9 Receipt/ledger — PARTIAL: receipt-plan line (page.tsx:1464-1466) mixes measuredHours (pod clock, spanHours) for Δ h with stand.earned (person-hours × M) for Δ 웃 — screenshot reads "0.01 h clocked in 2 segments · Δ -1.99 h — 웃 6.000 planned · 0.061 actual"; 0.01 h × 3 ≠ 0.061. D9 line prints d9.which, which is "equal" by construction (page.tsx:606 compares vintage stamped at podJuris at :1442 against the same podJuris). 10 Replay/determinism — PASS for the code (pod-invariant 310/310, soi-pod-sim 66/66, soi-pod-flow 27/27 run here), but the time document (pod-time-report.mjs) replays a fixed scenario, not the live run's events.  
*backward:* From the receipt walking upstream, asking what could make it lie: (10) replay reproduces the stored state deterministically, so the receipt cannot drift between phones — PASS (pod-store.ts replay rule; sim 'replaying the same recorded pod reproduces the identical settlement + synthesis'). (9→8) the receipt's Settlement artefact and vintage are computed from the pod's region, not each member's (page.tsx:606, 1442) — an upstream default (Lea 'inherited' Austin) becomes the D9 rate for Ana's naira — FAIL. (8→5) stand.earned and ◬ read person-summed witnessedHours while Plan→actual hours read wall-clock measuredHours (page.tsx:534, 444, 1465) — the plan's unit fixed at COMPOSE decides which sign the receipt shows — FAIL. (6) the synthesis is fed raw claims (page.tsx:647) and no outcomes; the backend prompt ignores the clock — an upstream 1 h claim can appear in the Transcript artefact and Trio box uncapped — FAIL. (5→1) the clock events and elections are append-only and replicated over the roster channel; nothing upstream of the clock can shorten or reopen a segment — PASS. Net: replay is honest, the words on the receipt are not yet.

**Athena** (Δ) — *forward:* Cube 1 (session/roster over the poll's channel): PASS — "  6411ms  ana    OK  assigned seat 2" / "  6458ms  lead   OK  lead sees 3 in the pod"; own-seat-only enforced (pod-roster.ts:322 canEditSeat), phases only move forward (pod-roster.ts:311 PHASE_ORDER guard), the plan is part of what is approved (log 19/21/23 "approved intent, outcome AND the plan (2 h × 3)") and cannot be opened without one (page.tsx:404 canOpen). Cubes 2/3 (record, written/voice): PASS — three own-seat outcomes required before AUDIT (page.tsx:1236 disabled until members.every(outcome)); log 42-45. Cube 4 (collector/roster): PASS — joiners learn every seat from the lead's roster (pod-roster.ts:217) so `known()` (pod-roster.ts:174) accepts a clock press from either joiner; log 38 a JOINER closed segment 2 for everyone. Cube 5 (gateway: the platform clock): PASS with holes — one button Start/Stop/Add time (page.tsx:1144-1146), segments summed (pod-clock.ts:48-59), replicated (log 35-37, screenshot 5-active-two-segments-bo.jpg shows segment 2 running on Bo at 0:00:11); HOLES: strip Stop ungated (page.tsx:1567), device-clock skew unhandled (pod-clock.ts:50 sorts by each phone's own Date.now(); the run used three contexts on one machine, script:29), the event records by:"pod" not the seat (page.tsx:429). Cube 6 (AI synthesis, PodFacts): PASS — PodFacts now carries witnessed_for, segments, member_outcomes (pod_router.py diff) and the sim asserts facts keys == PodFacts (soi-pod-sim.test.mjs:285). Cube 7 (ranking): NOT TOUCHED — no change in the diff, nothing to trace. Cube 8 (tokens: mint/plan/tranches/ceiling/settlement): PASS with a should-fix — M locked with the plan and hashed (pod-baseline.ts:52-58), M picker gone at audit (log 56, band-locked), claims capped (screenshot 7-audit-witness-lead.jpg "counted as 0.01 h — the pod was witnessed for 0:00:24"), each member settles at their own floor (log 63); should-fix: plan hours compared to two different actuals (page.tsx:607 vs :1465). Cube 9 (receipt, ledger): PASS — PLAN → ACTUAL, 2 segments, ledger grammar, D9 line on the receipt (log 60-65; 8-closed-receipt-bo.jpg); but receipt assertions run on the lead phone only (script:108). Cube 10 (replay/determinism): PARTIAL — device ledger replays by revision (pod-store.ts:59, gated at pod-invariant.test.mjs:121-125) and the sim proves three-phone settlement identity (soi-pod-sim.test.mjs:257) — but the sim never drives a kind:"clock" message across the bus (grep: only comments at :66/:273), so cross-phone clock determinism is proven only by the live run, which is not in test:ci and whose npm alias still points at the old script (package.json:50 test:soi-pod-live → scripts/pod-live-run.mjs).  
*backward:* Cube 10 → 9: could the receipt lie? Yes, on a phone whose clock log diverged — supported() with startedAt===null returns the raw claim, witnessed:false (pod-clock.ts:71); the receipt on that phone shows a larger 웃 than the others; the run asserts receipt content on the lead only (script :108-113 use `L.evaluate`), so cross-phone equality of the receipt is unproven by the walkthrough (screenshots happen to agree). 9 → 8: the receipt's Δ (clock vs plan) and the ◬ (person-hours vs plan) can disagree in sign on the same page — page.tsx:1465 vs :606; time doc §4 shows Δ +1.5 h person-hours while the clock ran 1.25 h < 2 h plan. 8 → 6: PodFacts carries the clock, so the backend cannot describe a longer session — PASS. 8 → 5: the M that mints is lock.m (page.tsx:571); the lock is written at "Share QR" (page.tsx:959) with signerIdx default 0 = the lead (screenshot 7-audit-witness-lead "Locked at 2 h by Lea") — the signer is a party with a stake, because joiners are not yet named at compose; a flow-order defect (note, D11). 5 → 4: strip Stop (page.tsx:1567) can reach audit with no segments → the amber "No session was clocked" panel and uncapped claims — the gate asserts both buttons call stopAndRecord but not that both are gated. 4 → 1: a clock message from a sender the roster does not yet know is dropped (page.tsx:338-339) and never re-sent; the roster re-publish path that repairs every other field (pod-roster.ts:170-190) does not carry clockEvents. Backward trace: FAIL on the strip route and the clock-resync class; PASS elsewhere.

**Christo** (Δ) — *forward:* Cube 1 (session/roster over the poll channel) — PASS: log 2161ms "pod code issued JA9QVJ", 6411/6452ms seats 2/3 assigned, 6458ms "lead sees 3 in the pod"; own-seat-only name/locality/approval enforced (log 6464–7120ms; pod-roster.ts:236 patchAllowed, page.tsx:1045 disabled={!canEdit(i)}). Consensus gate: "Accepted by the trio" disabled until allAgreed (page.tsx:407, 1082); log 7214ms "all three approved → sync unlocked". Cube 2/3 (record, written) — PASS: 33345–33413ms three own-seat outcomes, audit unlocked only when all three are in; screenshot 6-record-ana shows own input enabled, Lea/Bo greyed. Cube 4 (collector) — PASS: elections replicated to the lead (log 7218ms "lead sees Ana in NGN and Bo in PHP"; screenshot 3-agreed-elected-lead shows Nigeria 402.739 NGN / Philippines 86.875 PHP, Lea "inherited from the pod"). Cube 5 (gateway: the platform clock) — PASS: one button (page.tsx:1145–1146 Start/Stop/Add time), pressClock broadcasts kind:"clock" (page.tsx:428–432), receiver dedupes and accepts only known phones (page.tsx:336–341); log 33195–33201ms all three "sees segment 2"; 33317ms a JOINER's Stop & record closed segment 2 for everyone; screenshot 5-active-two-segments-bo shows "segment 1 … 0:00:12 / segment 2 … running / total 0:00:24 · 0.0024..0238". Cube 6 (PodFacts) — PASS with note: pod_router.py now carries witnessed_for, segments, member_outcomes; the synthesis on the receipt shows "closed one synchronized session on a single measurable outcome". Cube 7 — NOT TOUCHED, confirmed (no ranking file in diff f2e28fe..HEAD). Cube 8 (mint/plan/tranches/settlement) — PASS on mint and settlement, FAIL on plan unit: 웃 = M × T only via mint() (page.tsx:534–535, 1476); claims capped (log 33633–33810ms; screenshot 7-audit-witness-bo "counted as 0.01 h — the pod was witnessed for 0:00:24"); each member settles in own currency, Lea labelled "inherited" (receipt 5b, page.tsx:1474–1480); M locked, no picker (log 36899ms). Plan→actual line mixes clock hours and person-hours (page.tsx:1465–1466) — FAIL. Cube 9 (receipt) — PASS with the same caveat: all three phones reach CLOSED with identical figures (log 37096–37106ms, screenshots 8-closed-receipt-lead/ana). Cube 10 (replay/determinism) — PASS: 310/310 pod-invariant assertions via npm run test:pod-invariant; soi-pod-sim 66/66; soi-pod-flow 27/27; time report reproducible from fixed timestamps.  
*backward:* From the receipt (9/10) backward: the receipt's "Plan → actual" and "Each at their own floor" lines can lie if (a) the lock they cite was never re-accepted after a "Back to edit" — Cube 8/1: page.tsx:1089 vs :311-313, joiners never receive agreed:false and the lead's podRef still says true — the gate at pod-invariant.test.mjs:534 asserts exactly that React-only pattern, so the hole is protected, not the invariant (FAIL); (b) the signer named "conflict-excluded" is the lead herself — page.tsx:197/:943/:961, screenshot 8-closed-receipt-lead.jpg Governance "Accelerator signed by LEA (conflict-excluded)"; the time document instead says "signed by Bo" (pod-time-report.mjs:26) — the two artefacts disagree on who signed (FAIL); (c) D9 for a non-default elector — Cube 8: one vintage per pod (page.tsx:1442), so Ana's NGN figure has no vintage to be "greater than" (FAIL); (d) Cube 5: a phone with an incomplete clock log (late join/new device) caps claims differently — three receipts need not agree, and soi-pod-sim's "ALL THREE PHONES FINALIZE THE SAME SETTLEMENT" (soi-pod-sim.test.mjs:256) runs with no clock events at all (FAIL as a gate, PASS in the run); (e) Cube 4/1: roster own-seat and attest-own-bit guards hold (pod-roster.ts:247, 254) — a peer cannot approve, elect or witness for another (PASS; log "6641ms lead OK approval checkbox is own seat only"); (f) Cube 1: the sync verdict witnesses readiness and does not start the clock (page.tsx:482-491; log "8394ms … clock button is on screen, not yet running") (PASS). Overall backward: FAIL on (a)-(c), which are the "three agreeing a plan" and "witnessing" half of my lens.

**Enki** (Ω) — *forward:* Cube 1 (session/roster over the poll channel): PASS — seats own-seat-only, elections replicated (log 7218ms "lead sees Ana in NGN and Bo in PHP"); edge: a lead reload restores members into React but not podRef (page.tsx:466 restores phase only), so known() at page.tsx:337 drops joiners' clock events until they re-claim. Cube 2/3 (record, written/voice): PASS — three outcomes own-seat-only, whitespace refused (page.tsx:1234 `every(m => m.outcome.trim())`), a filled outcome never erased on merge (pod-roster.ts mergeMember `outcome: incoming.outcome || local.outcome`). Cube 4 (collector/roster): PASS — RESET_PATCH leaves region and outcome untouched (pod-roster.ts:109-112). Cube 5 (gateway: the platform clock): FAIL in this lens — measure() sorts by `at` and ignores a stop before an open start (pod-clock.ts:50-53); probe: events [start@100000, stop@70000] → {running:true, stoppedAt:null}; zero-skew Playwright contexts (pod-live-run-2026-09-11.mjs:29) cannot reveal it; a running segment then freezes because nowTick ticks only in "active" (page.tsx:437-441). Dedupe by (kind,at) is fine; same-ms start/stop yields a 0 ms segment counted in "in N segments". Cube 6 (AI synthesis, PodFacts): PASS — segments, witnessed_for, member_outcomes ride to the backend (pod_router.py PodFacts +witnessed_for/segments/member_outcomes; page.tsx:670-674). Cube 7: NOT TOUCHED — say so. Cube 8 (tokens: mint/plan/tranches/ceiling/settlement): PARTIAL — mint currency-free and locked plan hash verified (pod-baseline.ts:52-63); capped claim shown ("counted as 0.01 h — the pod was witnessed for 0:00:24", 7-audit-witness-lead.jpg); NULL floor prints "no rate published" (page.tsx:1477) — no floor invented; FAIL: per-member settlement is a live lookup of members[i].region (page.tsx:598-599), the only stamp is the pod default (page.tsx:1442 `stamp(... podJuris?.rate ...)`), so Ana's naira and Bo's pesos are not vintage-locked and get no D9; settleD9 uses currency equality as the jurisdiction test (pod-rates.ts:334-341) — probe: vintage Texas 7.25 USD, current California → {rate:16, which:"current"}; Germany 13.9 EUR → Luxembourg → 15.557 "current" — cross-jurisdiction maximum inside one currency, the shopping D9 closes. Cube 9 (receipt/ledger): PARTIAL — 8-closed-receipt-lead.jpg shows "Plan → actual 2 h planned · 0.01 h clocked in 2 segments"; page.tsx:1464 compares lock.hours to measuredHours (clock) for the time Δ but to stand.earned (sum of person-hours × M) for the 웃 Δ; the time document §4 shows the consequence: plan 2 h, clock 1.25 h, "Person-hours witnessed 3.5000 h, +1.5000 h" while "◬ … delta -1.500 h"; raw float "0.020305833333333335 witnessed hours" on screen (page.tsx:1433, 7-audit-witness-lead.jpg). Cube 10 (replay/determinism): PASS for reload mid-segment — clockEvents appended on every change (page.tsx:474-477) and replayed with a live now (page.tsx:459), vintage/lock read back never re-derived (page.tsx:464,467); pod-time-report.mjs is reproducible on fixed timestamps.  
*backward:* Start at the lead's receipt (8-closed-receipt-lead.jpg: "Plan → actual 2 h planned · 0.01 h clocked in 2 segments · Δ -1.99 h — 웃 6.000 planned · 0.061 actual"; "5b · Lea → $0.15 · Ana → NGN 8.18 · Bo → ₱1.76 — D9: the pod's own floor paid the equal rate, 7.25 USD/h"). What upstream could make it lie? Cube 9→8: "0.061 actual" is 3 × (clock-capped 0.0203 웃) — the actual is person-hours while the plan is clock-hours; in this run both deltas happen to be negative, but the time document's own numbers (1.25 h clock < 2 h plan yet 10.5 웃 > 6 웃 and accel delta -1.5 h) show the two readings contradict. FAIL. Cube 8→settlement: the audit panel's "Settles at $0.44 — 웃 × 7.25 USD" (7-audit-witness-ana.jpg; page.tsx:1332-1334, settlesTo = settleInRegion(stand.payableThisYear, region)) prices Ana's naira-웃 and Bo's peso-웃 at Texas — a cross-currency sum through the pod default, which the 2026-09-11 election ruling forbids; and stand = standing(carriedIn, pod-sum) caps three persons at one 9,999. FAIL. Cube 8 D9: the per-member "D9" column in the time document is produced by pod-time-report.mjs:44 with vintage := current (trivially "equal"), not by anything the page stamps; the page holds one Vintage with podJuris rate only. The receipt copy honestly says "the pod's own floor", so the screen does not lie, but the showcase claim "each contributor … at the greater of vintage and current" is not implemented. Cube 5→4: a receipt on a phone that missed a clock frame or reloaded without storage reads a different span than the others — nothing re-syncs the clock (no clock in roster, no claim-time replay), so the three receipts are not guaranteed identical. FAIL. Cube 4→1: election own-seat, merge, reset all hold; the lead cannot move Ana's floor. PASS. Cube 10: replay reads vintage/lock back; a reopened pod cannot re-derive. PASS. Overall backward: FAIL on plan units, pod-level settlement/ceiling, and clock non-replication.

**Enlil** (Ω) — *forward:* Cube 1 (session/roster over the poll's channel): pod code JA9QVJ issued, three seats assigned, own-seat-only guards hold — log "6411ms ana OK assigned seat 2", "6464ms ana OK name input is own seat only"; pod-roster.ts exports `known()` and accepts a `clock` message only from a seated phone — PASS. Cubes 2/3 (record, written/voice): record phase takes one outcome per member, own seat only — log "33345ms lead OK outcome input is own seat only", page.tsx:1218 `pod-outcomes`; voice path untouched today — PASS. Cube 4 (collector/roster): elections replicate — "7218ms lead OK lead sees Ana in NGN and Bo in PHP (elections replicated)"; pod-roster merge never erases an election (gate line 454) — PASS. Cube 5 (gateway = the platform clock): one handler `pressClock` (page.tsx:427-433) appends and broadcasts; `measure()` (pod-clock.ts:47-58) sums segments, ignores start-while-running/stop-while-stopped; replicated — "33198ms ana OK sees segment 2", screenshot 5-active-two-segments-lead shows segment 1 0:00:12, segment 2 running, total 0:00:24 = 0.0024..0241; a joiner's Stop closes for everyone — "33317ms ana OK a JOINER pressed Stop & record" — PASS. Cube 6 (AI synthesis/PodFacts): frontend sends `witnessed_for`, `segments`, `member_outcomes` (page.tsx:672-674); Worker prompt carries them (ai-core.js:35-37 JSON.stringify(f)); FastAPI PodFacts declares them (pod_router.py:47-49) but its prompt never renders them — FAIL on the FastAPI leg (declared, dropped). Cube 7: not touched — `git diff f2e28fe..HEAD --stat` has no cube7/ranking file; say so — N/A. Cube 8 (tokens: mint/plan/tranches/ceiling/settlement): plan locked with m and yug in the hash (pod-baseline.ts:49-55), verifyBaseline fails on changed M (gate 522); M locked on audit — "36899ms lead OK M is locked on the audit screen (no picker)"; claims capped — receipt shows "Lea 0.01 h → 웃 0.020" after a 1 h claim; per-member settlement in USD/NGN/PHP via settleInRegion (page.tsx:1471-1475); D9 greater-of in settleD9 (pod-rates.ts:330-343) — PASS on mint/lock/cap/settle, but the plan's hour unit is ambiguous: accelerate() gets summed person-hours (page.tsx:607) while the receipt's time Δ uses the span (page.tsx:1465) — FAIL on plan→actual consistency. Cube 9 (receipt/ledger): receipt renders Plan → actual, 2 segments, A.B..C grammar, 5b each at own floor, D9 line — log lines 37222ms ×6 and screenshot 8-closed-receipt-lead — PASS (with the unit caveat above). Cube 10 (replay/determinism): pod-time-report.mjs regenerated to scratch and diffed identical to docs/assessments/2026-09-11_pod_time_volunteer_vs_paid.md; pod events persisted to the pod ledger and read back (page.tsx:455-467) — PASS. Overall forward: PASS with two named FAIL points (Cube 6 FastAPI prompt, Cube 8 plan unit).  
*backward:* Start at the receipt/replay and ask what upstream can make it lie. (10→9) The time document replays cleanly, but it is synthetic fixed timestamps, not the three-phone run's data; it treats plan.hours as person-hours (§4 "Person-hours witnessed 2.00 h vs 3.5000 h") while the live receipt treats the same lock.hours as wall-clock (page.tsx:1465) — two carriers of "plan vs actual" disagree on the unit: FAIL. (9→8) The receipt's trio block prints the CLAIMED `m.hours` ("1h", page.tsx:1545) three lines below 5b's counted 0.01 h — the same screen carries both the claim and the capped figure without saying which is which: should-fix. The 웃 actual (0.061) comes from witnessedHours = Σ capped claims (page.tsx:534) — a claim above the clock is capped per member (log "self-audit: claimed 1 h … will be capped"), so the mint cannot exceed 3 × M × clock; the cap holds. (8→6) Backend PodFacts now carries the clock, so its synthesis can no longer describe a longer session than witnessed; the frontend deterministic synthesis still can, since it ignores segments (pod-synthesis.ts). (6→5) A stop from any known phone lands once on every ledger (dedup on kind+at, page.tsx:339); a forged press from an unknown clientId is dropped (known guard). No CI test drives a "clock" message through onStatus across simulated phones — soi-pod-sim has zero clock coverage (grep: only the pod-code comment and the payload field mirror), so replication is proven by the live run alone, not by a gate; if the broadcast line were deleted the regex gate at pod-invariant would fail (good), but if the dedup predicate were subtly wrong (e.g. compared only `at`) every gate would still pass. (5→4→1) Roster merge never erases an elected region or a filled outcome; a reloaded lead cannot move a floor. (Run-log honesty) step 65 "receipt shows the three outcomes and M locked at 3×" is backed only by /at 3×/ (script line 114) and the receipt has no outcome text — the log overclaims. Backward verdict: FAIL on the receipt's plan-vs-actual honesty; the clock, cap, guard and lock chain otherwise hold.

**Krishna** (Ω) — *forward:* Cube 1 (session/roster over the poll's channel): PASS — hello/claim/roster/member/attest/phase unchanged; `region` and `outcome` added to Member and to the own-seat-only `member` patch (pod-roster.ts:36-52, 200-206); `mergeMember` keeps elected region and filled outcome (pod-roster.ts:149-152); run log "locality select is own seat only" ×2, "lead sees Ana in NGN and Bo in PHP (elections replicated)". Cube 2/3 (record, written/voice): PASS — per-member `outcome` own-seat-only, all three required before audit (log "outcome input is own seat only" ×3, "all three outcomes in → audit unlocked"); lead's shared record still rides `record` beside `pod`. Cube 4 (collector/roster): PASS with a gap — `podPresence` from the roster; but `regionIdSel` (pod default) is local state only (page.tsx:214, 305 brief carries intent/outcome/plan/lock, not the region), so an unelected member's "inherited" floor is per-device. Cube 5 (gateway: the platform clock): PASS on the run, FAIL on the class — `pressClock` appends locally then broadcasts `{kind:"clock"}` (page.tsx:428-432); receiver guards `known()` and dedups on kind+at (page.tsx:337-341); log "sees segment 2 (clock events replicated)" ×3 and a joiner's Stop closed segment 2 for everyone. But the clock log is not in the roster and never re-sent: a `clock` arriving before the sender's seat is known is dropped forever, a replacement phone is blind, and `supported()` then returns witnessed:false and passes the claim uncapped (pod-clock.ts:64-69). `by: "pod"` discards who pressed (page.tsx:429). Cube 6 (AI synthesis, PodFacts): PARTIAL — frontend sends witnessed_for/segments/member_outcomes (page.tsx:672-674), sim test asserts facts keys == PodFacts (soi-pod-sim.test.mjs:285), but pod_router.py:100-112 builds the context without any of the three fields — the backend model is still blind to segments and outcomes; `segments: list[dict]` is untyped. Cube 7: NOT TOUCHED — no ranking/voting code in the diff; say so. Cube 8 (tokens: mint, plan, tranches, ceiling, settlement): PASS for mint/plan/tranches (M = lock.m, page.tsx:529; lockBaseline hashes m, yug, source, pod-baseline.ts:51-58; brief.lock replicated with hash, page.tsx:330); FAIL for per-contributor D9 — one vintage per pod at podJuris (page.tsx:1442), `settleD9` called once with podJuris (page.tsx:604), per-member rows use `settleInRegion` current rate only (page.tsx:1364, 1476). min-wage.ts reuse: PASS — pod-rates.ts:24 imports US_STATE_RATES/COUNTRY_RATES, page.tsx:58 reuses detectRegion; only Cambodia is added from hi_rates with a hand-set cc. Cube 9 (receipt, ledger): PARTIAL — receipt-plan, receipt-each, ledger grammar present; vintage/D9 line only on the settling phone (Ana's receipt ends at 5b, no "6 · Stamped"). Cube 10 (replay/determinism): PASS for same-device replay (appendPod carries clockEvents, vintage, regionIdSel, lock, page.tsx:474); the pure sim never delivers a `clock` message (0 hits in soi-pod-sim/flow), so cross-phone clock determinism is proven only by the live run.  
*backward:* Cube 10 → 9: the receipt on Bo's phone reads "0.01 h clocked in 2 segments" and per-member lines in three currencies (8-closed-receipt-bo.jpg) — the ledger matches the lead's asserted body (log 37222) — PASS. 9 → 8: what could make the receipt lie? A per-member 웃 that differs by phone if `span` differs by phone (page.tsx:533 claimOf uses this phone's clockEvents) — no gate compares three phones' spans — FAIL (class named above). D9 line says "the pod's own floor paid the equal rate" — honest wording, but the showcase's stronger claim is not implemented — FAIL for the claim, PASS for honesty. 8 → 6: the deterministic synthesis reads the same members/hours; the backend AI synthesis could describe a longer session because pod_router.py never places witnessed_for/segments in the context (99-111) — FAIL. 6 → 5: supported() caps every claim to this phone's measured span (pod-clock.ts:71-76; log 33633-33810 "claimed 1 h … will be capped") — PASS. 5 → 4/1: the stop pressed by Ana (log 33317) reached Bo — proven by Bo's receipt, not by an assertion on Bo; the sim's bus carries hello/claim/roster/member/attest/phase/reset and no `clock` — the protocol's newest message is the only one without a simulated hostile/lossy case — FAIL (gate gap). 4 → 1: elections survive lead reload via mergeMember `region: incoming.region ?? local.region` (pod-roster.ts:157, gated) — PASS. min-wage.ts reuse: detectRegion imported and only suggests (page.tsx:58,220; gated), US_STATE_RATES/COUNTRY_RATES imported into JURISDICTIONS with source tags (pod-rates.ts:26,240-256) — PASS; no gate keeps min-wage.ts in sync with hi_rates.py despite the header's instruction (min-wage.ts:6-7) — note.

**Odin** (Ω) — *forward:* Cube 1 (session/roster over the poll channel): code issued, seats assigned, own-seat-only name/locality/approval — PASS on log 6411–7210ms; approval is a sticky boolean not bound to the lock hash and \"Back to edit\" clears it on one phone only — FAIL for M re-acceptance (pod-roster.ts:149, page.tsx:1089). Cube 2/3 (record, written/voice): one outcome per member, own seat, all three before audit — PASS (33345–33413ms). Cube 4 (collector/roster): elections replicated, NGN/PHP seen on the lead — PASS (7218ms); clock events are not part of the roster and are never re-sent on reconnect — FAIL on flaky networks (page.tsx:320). Cube 5 (gateway = the platform clock): Start/Stop/Add-time as one button, two segments summed, a joiner's Stop closes for everyone — PASS on the same-host relay (8540–33317ms); events stamped with each phone's Date.now() and folded by `at` — FAIL predictively under clock skew (page.tsx:429, pod-clock.ts:50). Cube 6 (AI/PodFacts): segments and three outcomes ride additively — PASS; open segment (`stop: null`) unflagged — note. Cube 7 (ranking): NOT TOUCHED by this work. Cube 8 (tokens): mint 웃 = M × T currency-free, plan locked with hash before Start, tranches and cap — PASS (3-agreed-elected, 7-audit-witness, band-locked); D9 per contributor is current-rate only and the vintage stamps the lead's jurisdiction for all — FAIL (page.tsx:606, ~1476); ceiling applied to the pod aggregate with no year — FAIL at scale/year rollover (page.tsx:534, 592); relocation keyed on currency — FAIL for CA/IN localities (pod-rates.ts:334). Cube 9 (receipt/ledger): three currencies on one receipt — PASS on all three screenshots; the three receipts differ (vintage + D9 line only on the lead's phone) — FAIL (8-closed-receipt-ana.jpg vs -lead.jpg). Cube 10 (replay/determinism): device replay via pod-store append-only — PASS (gate 310/310); replay from a joiner's device cannot reproduce the lead's receipt because the vintage never reached it — FAIL. Overall forward: FAIL.  
*backward:* From the receipt (Cube 9/10) walking back: could the receipt lie? Its "witnessed hours" is the sum of three capped claims (page.tsx:534) — each capped by `supported()` against the clock span (pod-clock.ts:74-79): PASS, a claim never exceeds the clock. But the clock span itself (Cube 5) is only as true as the event timestamps, and those are the pressing phone's wall clock with no server time and no sequence number: FAIL — a skewed or dropped Stop changes what every phone's receipt says, and nothing downstream can detect it (no monotonic ordering, no ack). The vintage (Cube 8) is written once (page.tsx:1442 `v ?? stamp(...)`) and read back never re-derived (page.tsx:464): PASS for the pod's own region; FAIL for Ana and Bo, whose NGN/PHP figure is recomputed from the CURRENT table on every render (page.tsx:1364-1365), so a year-later reopen after a statutory decrease would print a smaller number than the one they were shown — the exact thing D9 exists to prevent. The lock (Cube 8→1) is hashed and verifiable (pod-baseline.ts:66-69): PASS. The roster (Cube 4→1) merges and never erases: PASS. Local persistence (Cube 10→1): replayable but bounded to 60 revisions and localStorage only — on a phone whose storage is full or cleared the pod is gone, and no shared half exists yet (pod-store.ts:11 "A shared half over Supabase can append … later"). Backward verdict: the receipt is faithful to the phones' shared memory, but that memory can diverge silently on real networks and the receipt has no way to know.

**Pangu** (Σ) — *forward:* Cube 1 (session/roster): pod JA9QVJ issued, three seats, each member elects own locality own-seat-only — "6564ms ana OK elected Nigeria — 402.739 NGN/h", "6608ms bo OK elected Philippines — Metro Manila — 86.875 PHP/h"; pod-roster.ts:62 `member … own seat only`, :165 patchAllowed — PASS. Cube 2/3 (record, written): one outcome per member, own seat only — "33345ms…33392ms outcome input is own seat only", "33413ms ana OK all three outcomes in → audit unlocked"; 6-record-three-outcomes-*.jpg — PASS. Cube 4 (collector/roster replication): "7218ms lead OK lead sees Ana in NGN and Bo in PHP (elections replicated)"; "33195-33201ms … sees segment 2 (clock events replicated)" on all three — PASS. Cube 5 (gateway: the platform clock): one handler for every route page.tsx:428-437 (pressClock/toggleClock/stopAndRecord), inbound clock gated on known() page.tsx:338, measure() sums segments and never reopens a closed one pod-clock.ts:48-59; 5-active-two-segments-lead.jpg shows "segment 1 · 0:00:12 · segment 2 · running · total 0:00:24 · 0.0024..0241"; "33317ms ana OK a JOINER pressed Stop & record — one route" — PASS, with one note: every event carries by:"pod" (page.tsx:429), so the ledger cannot say which seat pressed. Cube 6 (AI synthesis, PodFacts): m, witnessed_for, segments, member_outcomes now ride to the backend pod_router.py:40-49 and enter the prompt at :105 "웃 SETTLED (M x hours, M={f.m})" — PASS (prompt-only; backend does not mint). Cube 7 (ranking): NOT TOUCHED by this work — stated. Cube 8 (tokens): mint is the one function pod-yug.ts:103 `hours * m`; plan = {hours, m, yug} hashed pod-baseline.ts:55-57, verified :60-63, gate asserts changing M after the lock fails (pod-invariant.test.mjs:522); tranches split through the one mint pod-baseline.ts:100-101; screenshot 8 "Plan → actual 6.000 planned → 0.061 · in 2 segments" — PASS for mint/plan/tranches. FAIL in my lens for settlement: vintage stamped once with the POD's summed hours at the POD's jurisdiction (page.tsx:1442 `stamp(witnessedHours, M, …, podJuris?.rate…)`), D9 computed only on `stand.payableThisYear` against `podJuris` (page.tsx:606); per-member lines use settleInRegion (current rate) at page.tsx:1364-1366 and :1476 — no per-contributor vintage exists, so D9's "greater of vintage and current" cannot ever be honoured for Ana or Bo. Ceiling applied to the pod sum, not per natural person (page.tsx:594 `standing(carriedIn, mint(witnessedHours, M))` where witnessedHours is the members' sum, :533). Cube 9 (receipt/ledger): 8-closed-receipt-lead.jpg shows "5b · Each at their own floor Lea 0.01 h → 웃 0.020 → $0.15 (… inherited) · Ana … NGN 8.18 (Nigeria) · Bo … ₱1.76 (Philippines — Metro Manila) — D9: the pod's own floor paid the equal rate, 7.25 USD/h"; log "37222ms lead OK receipt settles Ana in naira and Bo in pesos beside Lea in dollars"; the wording is honest about scope ("the pod's own floor") — PASS on honesty, and the receipt prints the stamp's raw float "0.020305833333333335 h" (page.tsx:1482 `{vintage.hours} h`) — cosmetic FAIL. Cube 10 (replay/determinism): `npm run test:pod-invariant` → "pod-invariant: 310 passed, 0 failed"; `test:soi-pod-sim` → "66 passed, 0 failed · bus delivered 658 messages"; `pod:time-report` regenerated to scratch and diffed against the committed document → identical — PASS for reproducibility; FAIL for what the D9 column proves: pod-time-report.mjs:44 `settleD9(yug, { rate: j?.rate, currency: j?.currency }, j)` passes the same jurisdiction as both vintage and current, so "equal rate" is a tautology, and the gate's settleD9 cases (pod-invariant.test.mjs:559-567) are unit cases, not the journey. Overall forward: PASS on input-in-time and on elected-floor settlement in local currency; FAIL on per-contributor D9 and per-person ceiling.  
*backward:* 10→9: the receipt's per-member cash (page.tsx:1476) is not in the ledger entry (page.tsx:474 persists members[].region, not a rate) — a future edit to REGION_RATES or to US_STATE_RATES changes a closed receipt for Ana and Bo; only the pod-level vintage is immune (page.tsx:464). FAIL. 9→8: `stand.earned` and `lock.yug` are compared on the receipt (page.tsx:1466) in 웃, but the time-delta beside them is in wall-clock hours; the time document (pod-time-report.mjs:163-167, §4 "Person-hours witnessed 2.00 h | 3.5000 h", "◬ … delta -1.500 h") reaches "no_time_saved" for a 1:15:00 clock against a 2 h plan — the sign of the accelerator depends on which "hours" the plan meant. FAIL. 8→6: a joiner sets the lock from any `brief.lock` carrying a string hash (page.tsx:334) and verifyBaseline (pod-baseline.ts:315) has no call site in the page — the lock is unforgeable only by convention. should-fix. 8→5: M is read from the lock (page.tsx:530) and the lock is sealed on "Share QR" (page.tsx:959) before any clock event can exist; segments cannot be shortened (pod-clock.ts:50-54; gate pod-invariant.test.mjs:25-27). PASS. 5→4: a clock press is accepted only from `known()` phones and appended once by (kind, at) (page.tsx:338-340; gate :490-494); the roster never folds the clock (pod-roster.ts:263), so a stale roster cannot rewind time. PASS. 4→1: locality is own-seat (pod-roster.ts:165-168, 247) and merge-protected (:157); outcome is own-seat and never erased (:158); seat 0 unclaimable (:197). PASS. Evidence provenance: the screenshots' app header reads SHA baa6017, but the receipt shows the "5b · Each at their own floor" line that exists only in 55a636d's diff, so the run exercised the reviewed working tree. Net backward: PASS for the clock and the mint, FAIL for the two places where a receipt can drift from what was recorded (member cash unstamped; plan unit undeclared).

**Sofia** (Σ) — *forward:* Cube 1 (session/roster): PASS — three phones seat over the poll's channel (log: "ana assigned seat 2", "bo assigned seat 3", "lead sees 3 in the pod"); own-seat rule holds for name, locality, approval, outcome (pod-roster.ts:62, log ×9 "own seat only"). Cube 2/3 (record, written/voice): PASS — shared record + one outcome per member gate audit (page.tsx:1233-1241, log "all three outcomes in → audit unlocked"). Cube 4 (collector/roster merge): PASS with note — `outcome: incoming.outcome || local.outcome` and `region: incoming.region ?? local.region` (pod-roster.ts:157-158) so a blank never erases; but a forged `clock` frame from an unknown phone is guarded only by a regex on source (pod-invariant.test.mjs:490), never delivered by the sim. Cube 5 (gateway: platform clock): PASS — one handler for button and strip (page.tsx:428-437), events replicate and dedupe (page.tsx:339-343; log "sees segment 2 (clock events replicated)" ×3, "a JOINER pressed Stop & record — one route"). Cube 6 (AI synthesis, PodFacts): PASS — segments, witnessed_for, member_outcomes ride both payloads (page.tsx:646-672; pod_router.py:47-49) — but the backend only receives them; nothing in pod_router.py reads `segments` (grep shows declaration only), so the model can still be blind to the clock. Cube 7: NOT TOUCHED — no ranking/voting code on this path; stated. Cube 8 (mint/plan/tranches/ceiling/settlement): FAIL in my lens — the mint 웃 = M × T is one and currency-free (pod-yug.ts:101), plan locked with hash before Start (pod-baseline.ts:48-55, log "M is locked on the audit screen"), tranches split correctly; BUT the plan's `hours` has no declared unit, so page.tsx:1465 measures it against the pod CLOCK and page.tsx:597/1466 against SUMMED person-hours, and the lock is signed by a member of the pod who is paid for the same hours (page.tsx:961 `members[signerIdx]`, screenshot 7-audit "Locked at 2 h by Lea") against unit.accel's "party with no stake in the payout". Cube 9 (receipt): FAIL in my lens — the same phone shows "Settled: ♡ 0.02 h witnessed → 웃 0.061" (soi.pod.x.closed, lexicon-data.ts:795) and "0.01 h clocked in 2 segments" (page.tsx:1465) one screen apart (screenshot 8-closed-receipt-ana); Ana's naira row is followed by "D9: the pod's own floor paid the equal rate, 7.25 USD/h" (page.tsx:1479) — a Lagos contributor reads a Texas rate as the D9 that paid her. Cube 10 (replay/determinism): PASS — lock, vintage, region, clockEvents are appended per revision and read back never re-derived (page.tsx:459-469, 472-476; pod-invariant 310/310, sim 66/66, flow 27/27 rerun by me).  
*backward:* Start at the receipt on Ana's phone (8-closed-receipt-ana.jpg, line 5b 'Ana 0.01 h → 웃 0.020 → NGN 8.18 (Nigeria)') and ask what upstream could make it lie. Cube 10→9: the receipt's 'D9: the pod's own floor paid the equal rate, 7.25 USD/h' reads as if D9 governed every line; Ana's NGN figure came from settleInRegion current rate with no vintage (page.tsx:1476) — the receipt overstates D9's reach: FAIL. Cube 9→8: line 3 'payable this year' is pod-summed under a per-person ceiling (page.tsx:592) — would lie above 9,999 pod-total: FAIL (dormant). Line 'Plan → actual Δ' (measuredHours) and the ◬ 'no_time_saved' reason (witnessedHours) disagree on the same receipt: FAIL. Cube 8→6: PodFacts carries segments and outcomes so the 333-word synthesis cannot invent the clock (pod_router.py:47-49; page.tsx:673-674): PASS. Cube 6→5: the span each phone feeds into its own cap is its own heard log; a missed broadcast makes Ana's clock, and therefore her cap and her 웃, differ from the lead's, with nothing to reconcile (page.tsx:337-340, no resync): FAIL as a class, PASS on the run. Cube 5→4: elections are own-seat and never overwritten by an empty merge (pod-roster.ts:157): PASS. Cube 4→2/3: outcomes own-seat, never erased (pod-roster.ts:158): PASS. Cube 3/2→1: the plan reaches joiners only via the lead's roster brief (page.tsx:318, 334) and the hash is verified (pod-baseline.ts:60); the signer is the lead by construction (page.tsx:961): PASS with note. Backward overall: FAIL — the receipt can misstate what paid Ana, what the plan measured, and whose ceiling applies, without any upstream phone having misbehaved.

**Thoth** (Σ) — *forward:* Cube 1 (session/roster): code JA9QVJ issued, 3 seats, own-seat-only name/locality/approval enforced (log 6464-7210ms) — PASS. Cube 2/3 (record): one outcome per member, all three in before audit (log 33345-33413ms) — PASS. Cube 4 (collector/roster): elections replicated ('lead sees Ana in NGN and Bo in PHP', 7218ms), Ana's Stop closed segment 2 for everyone — PASS. Cube 5 (gateway/platform clock): 4 events → 2 segments, sum 24,377 ms reproduced exactly by measure(); claims of 1 h capped to 0.00677 h (supported()) — PASS. Cube 6 (PodFacts/synthesis): m, baseline_hours, segments, member_outcomes ride to the backend (page.tsx:665-674); but accel_delta is the plan-vs-person-hours number of finding 1 — PASS on wiring, FAIL on the figure carried. Cube 7: not touched (diff stat has no cube7/ranking file) — N/A, stated. Cube 8 (mint/plan/tranches/ceiling/settlement): mint 웃 = M × T correct; tranches floor 1×/escrow correct and never summed on screen; per-member settlement in USD/NGN/PHP correct to the cent; FAIL on plan→actual basis (6 planned vs Σ-person-hours actual), on the per-person ceiling applied to the pod sum, and on D9/vintage existing only for the pod-default region. Cube 9 (receipt/ledger): A.B..C grammar correct on every figure; 'Settles at $0.44' prices three currencies' 웃 at Lea's floor — FAIL (should-fix). Cube 10 (replay): soi-pod-sim:266 replays the identical settlement; the replay is deterministic but replays the same wrong basis — PASS on determinism, FAIL on what it reproduces. Overall forward: PASS through Cube 5, FAIL from Cube 6 onward on the plan/actual and ceiling numbers.  
*backward:* Cube 10 → receipt: replayPod restores vintage only on the phone that wrote it (page.tsx:455-464), so a replay on Ana's phone yields a receipt without line 6 and without D9 — the ledger can honestly differ per phone. Cube 9 → 8: the receipt's 웃 line (stand.earned) is sound; its PLAN→ACTUAL time line reads a different quantity (measuredHours) than the 웃 line (Σ supported person-hours), so the receipt can print a Δ in hours and a Δ in 웃 that do not multiply at the stated M — the lie is on the shipped screenshot (\"0.01 h … 0.061 actual at 3×\"). Cube 8 → 5: supported() caps each member at the POD clock, so three members legitimately claim 3× the wall span — correct for person-hours, but the plan and the ♡ count use pod-minutes, so upstream the clock is fine and the lie is born where its output is compared to something in another unit. Cube 8 D9 → 1: the stamp reads podJuris (regionIdSel, lead's default), never members[i].region, so a member's election (Cube 1, proven replicated) never reaches the vintage — an upstream truth is dropped at Cube 8. Cube 6 → 1: PodFacts carries m, segments and outcomes, nothing upstream can alter the clock events (append-only, sorted fold). Cube 5 → 1: a joiner's Stop closes the segment for everyone (log 33317ms) — no path to a silent extra minute. Backward: FAIL at Cube 8 (D9 scope, plan denominator), PASS 10, 6, 5, 4, 1; Cube 7 untouched.

**Thor** (Σ) — *forward:* Cube 1 (session/roster): PASS for strangers — impostor roster/claim/phase dropped (pod-roster.ts:207, :195-197; sim test :96-110). Cube 2/3 (record, written): PASS — `record` and member `outcome` own-seat-only (log: "outcome input is own seat only" ×3), though `record` in the envelope is applied from any sender (page.tsx:342-346). Cube 4 (collector/roster): PASS for own-seat patches (pod-roster.ts:244-249) — FAIL for phase: a `member {region}` patch is honoured in `closed`. Cube 5 (gateway, the platform clock): FAIL — page.tsx:428-431 stamps `at: Date.now()` on the SENDER and `by: "pod"` (no seat attribution); page.tsx:337-340 admits any known phone's `at` verbatim with no phase/lock gate and dedups only on (kind, at); reproduced: forged early start → 8.0067 h, forged future stop → 40.003 h, `supported(40)` → capped:false, witnessed:true. Cube 6 (PodFacts): PASS as a record, FAIL as evidence — pod_router.py:47-49 accepts `segments`/`witnessed_hours` verbatim from the client, no server clock, nothing to cross-check. Cube 7: NOT TOUCHED, confirmed (no diff). Cube 8 (mint/plan/tranche/settlement): mint PASS (M = lock.m, band-locked, log "M is locked on the audit screen"); plan lock FAIL — page.tsx:331 `setLock(brief.lock)` from any envelope, no `known()` check, `verifyBaseline` never called on receive, so hours/M can be replaced after the clock; D9 FAIL — vintage stamped at settle click from per-phone `regionIdSel` (page.tsx:1442, :214, never broadcast), audit picker still live at :1329; per-member settlement :1476 at current rate, no D9, no freeze. Cube 9 (receipt/ledger): PASS on the happy path (screenshot 8-closed-receipt-lead: three currencies, two segments, PLAN→ACTUAL) — but :1539-1546 prints the raw claim "· 1h" beside "✓ witnessed" while 5b shows 0.01 h counted. Cube 10 (replay): PASS locally (appendPod, vintage read back :464) — FAIL as proof: the pod's floor and the vintage are per-phone, so three replays of one pod can carry three different stamps.  
*backward:* 10→9: the receipt on ana/bo cannot replay the D9 rate because no vintage was ever written on those phones (page.tsx:1442 runs on the presser only; 8-closed-receipt-ana.jpg lacks line 6) — FAIL. 9→8: the receipt's per-member cash is `settleInRegion(mint(claimOf(i).hours, M), localityOf(i))` read LIVE (page.tsx:1475-1477), and the vintage stamps only `podJuris?.rate` (the pod default, USD 7.25) — Ana's NGN and Bo's PHP are never stamped, so "greater of vintage and current" is unprovable for the elected members — FAIL. 8→6/5: M comes from a lock any channel peer can push (page.tsx:334) and hours come from a span any known id can pre-date (page.tsx:338-340 + pod-clock.ts:47-52: a forged start earlier than the real one opens the segment and the real start is ignored as "open") — FAIL. 5→4: `stopAndRecord` from a joiner (log 33317ms) closes the segment with THAT phone's Date.now() minus the lead's Date.now() start — span = difference of two unsynchronised clocks — FAIL in lens, PASS in the run (one machine). 4→1: seats and the pinned lead are bearer ids readable off the channel; `known()` (pod-roster.ts) is the only guard on clock events — FAIL. Replay of a stale press: dedup `(kind, at)` (page.tsx:340) makes an exact replay a no-op — PASS; the gate asserts the regex (pod-invariant.test.mjs:492) but no test sends a clock message through the sim bus (soi-pod-sim.test.mjs: "clock" appears twice, both comments).

# 4 · The twelve, on the shipped journey — 111 words each

| Master | Grade | Verdict | State | Next |
|---|:--:|---|---|---|
| **Aset** | B | PROCEED WITH CHANGE: define T once (clock span vs summed person-hours) and print each under its own name on the receipt (page.tsx:1465-1466), the audit panel (page.tsx:1433, hours never under a 웃/♡ glyph; lexicon-data.ts:795) and the time document (pod-time-report.mjs:100); stamp a D9 vintage per contributor at their elected floor instead of one pod rate (page.tsx:1442, :1476, pod-time-report.mjs:44); replicate the pod default locality in the locked brief (page.tsx:305) and lock its picker with M (page.tsx:1329); put witnessed_for/segments/member_outcomes into the Cube 6 prompt context (pod_router.py:106-118) and drop "qualified time" (pod_router.py:76); extend the BANNED scan beyond the five frontend sources; and correct the run-log line "receipt shows the three outcomes" (script :114) to what the receipt renders. | Open | Define "witnessed hours" once in pod-clock.ts (clock span) and name the summed capped claims separately (e.g. contributed hours); print each under its own name on the receipt, the audit panel and the time document, with hours never under a 웃 or ♡ glyph; stamp a per-member D9 vintage at each contributor's elected floor; then add a pod-invariant.test.mjs assertion that the receipt's Plan → actual line satisfies stand.earned === mint(printedHours, M) and that pod_router.py and pod-time-report.mjs pass the BANNED scan. |
| **Asar** | B- | PROCEED WITH CHANGE: the mint is honest (웃 = M × T, M team-accepted and hash-locked before Start, no picker on audit — log.txt step 56), but three carriers of "what happened" overclaim. (1) log.txt step 65 "receipt shows the three outcomes" is certified by /at 3×/ alone (pod-live-run-2026-09-11.mjs:114); the CLOSED block renders the pod outcome (page.tsx ~1486) but never m.outcome, and 8-closed-receipt-lead.jpg shows no per-member outcome. (2) PodFacts accepts witnessed_for/segments/member_outcomes (pod_router.py:47-49) but the context handed to the model (pod_router.py:99-111) omits all three; the local buildSynthesis333 has no segment or member-outcome input (pod-synthesis.ts:33-47); gate pod-invariant.test.mjs:540 asserts only that the page sends the keys. (3) The receipt's Plan → actual line (page.tsx:1465-1466) measures Δ h on the pod clock and Δ 웃 on summed person-hours; the raw 1 h claims appear in the Trio box (page.tsx:1544) beside the capped 0.01 h with no "capped" word; D9 vintage is stamped only at the pod's rate (page.tsx:1442) and compared against the same podJuris (page.tsx:606), so "greater of vintage and current" was never exercised and "paid the equal rate" leaks an enum; the time document (pod-time-report.mjs, invented 09:00–10:35 timestamps) never says it is not the record of the 0:00:24 live run. Cube 7 not touched. Hosted Supabase and Auth0 unproven, honestly stated by the run script. | Open | Make the record say only what happened, and gate it: (a) render the three per-member outcomes and the word "capped" (with the clock figure) in the CLOSED block, and change pod-live-run-2026-09-11.mjs:114 to assert each member's outcome text is present, not /at 3×/; (b) feed segments, capped claims and member outcomes into both the backend context (pod_router.py:99-111) and SynthesisInput (pod-synthesis.ts:33-47), and add a pod-invariant assertion that the context string and buildSynthesis333 consume them; (c) declare the plan's hour unit once and compute Δ h and Δ 웃 on that one basis (page.tsx:1465-1466); (d) stamp vintage per member at their elected locality or drop the pod-wide D9 sentence, and replace the enum "equal" with plain prose; (e) add one line to 2026-09-11_pod_time_volunteer_vs_paid.md stating it is a computed scenario and pointing at log.txt for the 0:00:24 live run. |
| **Athena** | B | PROCEED WITH CHANGE: gate stopAndRecord itself on span.segments.length > 0 (the phone strip at page.tsx:1567 is an ungated second door from ACTIVE to RECORD; the desktop button at :1148 is gated) and make pod-invariant assert the gate, not the button count (:497); then decide the unit of Baseline.hours and read the plan against ONE actual (page.tsx:1465 uses the clock span, :607 uses summed person-hours). | Open | Move the clock gate into stopAndRecord (refuse to leave ACTIVE with zero segments on every route), add the assertion to frontend/tests/pod-invariant.test.mjs so a build with an ungated route fails, and re-run the three-phone showcase with the cap check promoted from a swallowed waitForFunction (script:102) to a hard step on each phone. |
| **Christo** | B- | PROCEED WITH CHANGE: bind each seat's acceptance to the lock hash it approved and clear approvals through the roster protocol (apply/publish, not React-only setMembers at page.tsx:1089); define the accepted plan's unit so page.tsx:1465 (clock hours) and page.tsx:607/1466 (summed person-hours) read the same 2 h; stamp a D9 vintage per elected locality, not only podJuris (page.tsx:1442/606/1476); stop calling a payee-signed plan "conflict-excluded" (page.tsx:936/943/961). | Open | Make acceptance structural: each seat's approval records the lock hash it accepted (agreedTo: lock.hash), allAgreed requires every seat's agreedTo === lock.hash, Back to edit and re-open clear it via apply(patchAll(...)) + publish so joiners receive the clear; replace the grep at pod-invariant.test.mjs:534 with a reducer-driven test (three seats accept version 1, lead re-opens version 2, assert allAgreed is false on all three phones) and rerun the three-phone showcase from the committed SHA so the screenshot stamp matches. |
| **Enki** | B- | PROCEED WITH CHANGE: (1) declare the unit of a plan's hours and apply it in one place — frontend/app/soi-session/page.tsx:1464-1466 and :607 compare lock.hours to the pod clock for time but to the SUM of capped person-claims for 웃 and the accelerator (docs/assessments/2026-09-11_pod_time_volunteer_vs_paid.md §2 1:15:00 vs §4 "3.5000 h, +1.5000 h" beside "◬ delta -1.500 h"); (2) make the clock skew-proof — pod-clock.ts:50-53 sorts by `at` and drops a Stop whose timestamp precedes the open start (probe measure([start@100000, stop@70000]) → running:true), so a phone behind the starter can never close a segment for anyone, and carry the event log on the roster so a reloaded/late/dropped-frame phone converges (page.tsx:316-320 sends only pod+brief+record; :431 fire-and-forget); (3) stamp a Vintage per contributor keyed by jurisdiction id and stop settleD9 treating same currency as same jurisdiction (page.tsx:1442 stamps podJuris only; pod-rates.ts:334; probe Texas 7.25 → California → 16 "current"); (4) stop pricing the pod sum at the pod default and applying the per-person 9,999 ceiling to three people (page.tsx:591, :602); (5) round witnessedHours on screen (page.tsx:1433 raw float); (6) add a sim test that drives a `clock` message through the bus — today no test sends one. The mint itself (웃 = M × T, M from the accepted lock, hash over m and yug) is real and gated. | Open | Add a soi-pod-sim case that drives `clock` messages through the bus with skewed timestamps (stop.at < start.at), a duplicate, a dropped frame and a lead reload mid-segment, asserting all three ledgers measure the identical span — then fix pod-clock.ts measure() so a later-arriving Stop closes the open segment regardless of its `at` ordering, and pin the plan's hour unit (pod-clock vs person-hours) in one function that both the receipt time line and the 웃 line read. |
| **Enlil** | B | PROCEED WITH CHANGE: define the unit of the locked plan's hours (span or person-hours) and measure ONE actual against it in accelerate(), the receipt's Plan → actual line and pod-time-report.mjs; render witnessed_for/segments/member_outcomes in the FastAPI prompt (pod_router.py declares them, then drops them); make the live-run script assert the cap (claim-capped-*) and the three outcomes instead of /at 3×/. | Open | State the invariant on Baseline.hours (pod-baseline.ts:26) — the plan is in person-hours (or in clock span; choose one) — derive a single actualHours from it, feed that one figure to accelerate() (page.tsx:607), the receipt's Plan → actual line (page.tsx:1465) and pod-time-report.mjs, and add a pod-invariant assertion that both Δ lines on the receipt derive from the same actual; in the same commit render the three new PodFacts fields in the pod_router.py prompt with a soi-pod-sim check that they appear. |
| **Krishna** | B | PROCEED WITH CHANGE: carry the clock log and a per-seat vintage in the roster so every phone mints from the same T and settles D9 at its own floor; read witnessed_for/segments/member_outcomes in pod_router.py; add a sim case that delivers `clock` over the bus and asserts three phones hold the same span; drop or implement the per-contributor D9 claim in the receipt and the time document. | Open | Fix the class, not the instance: add `clock: ClockEvent[]` and per-seat `vintage` to the roster rebuild path (the same path that carries brief/plan/lock at page.tsx:311-323), dedupe on arrival as `lock` already does, then write the gate first — a soi-pod-sim case that delivers Start/Stop/Add over the bus with one lossy peer and one late joiner and asserts all three phones' measure().span and each member's settled 웃 are identical — and make pod_router.py:100-112 print witnessed_for, segments and member_outcomes into the context with a backend test asserting they appear. |
| **Odin** | C+ | NOT WORKING: per-contributor D9 settlement and the three-identical-receipts property — the mint 웃 = M × T with the trio-accepted M is real and gated, but the vintage lives on the settling phone only (page.tsx:211/1442, never in the session_update payload at page.tsx:320), joiners settle at the CURRENT rate via settleInRegion (page.tsx:1476), clock events are ordered by each phone's own Date.now() (page.tsx:429, pod-clock.ts:50) with no sequence or server stamp, and acceptance is a sticky OR-merged boolean (pod-roster.ts:149) not bound to the lock hash, so M can relock as v2 under v1 approvals. | Open | Make the clock and the settlement replicable and gate it: give every ClockEvent a monotonic seq plus a lead- or server-authoritative timestamp and carry the clock log and the vintage in the roster/brief so a reconnecting phone catches up; stamp a vintage per contributor at their elected jurisdiction id (not currency) and settle each member through settleD9; bind `agreed` to the lock hash so any M relock clears approvals on every phone. Add to pod-invariant.test.mjs: a skew case (joiner −30 s/+30 s Stop still closes the segment), a three-phone receipt-equality assertion, and a per-member D9 floor case across a statutory decrease. Reword the showcase header to "three emulated phones, one host, local relay". |
| **Pangu** | B | PROCEED WITH CHANGE: stamp a Vintage per contributor at settlement and route every receipt-member line through settleD9 with that member's own vintage (page.tsx:1442 stamps only podJuris; :1476 uses live settleInRegion); stop pod-time-report.mjs:44 from passing the current jurisdiction in as the vintage; declare the plan's unit (page.tsx:1465 compares lock.hours to wall-clock while :534/:594/:607 mint, ceiling and accelerate from summed person-hours); call verifyBaseline on a received lock (page.tsx:334 accepts any object with a string hash; pod-baseline.ts:60 has no call site in the page); record which seat pressed the clock (page.tsx:429 by:"pod"). The mint itself — 웃 = M × T at the trio-accepted, hashed M, capped by a three-witnessed clock — is WORKING and is the genuinely new pairing. | Open | Fix the class, not the instance: introduce a per-member Vintage (stamped once at settlement with that member's elected rate and currency, persisted in the ledger entry), route receipt-member-* and pod-time-report.mjs through settleD9 with that stamp, and add a journey assertion in soi-pod-sim that raising the NGN rate after the stamp pays Ana the higher figure while Bo and Lea are unchanged — plus a source gate that the receipt reads a per-member vintage, so deleting the fix fails 311/311. |
| **Sofia** | B- | PROCEED WITH CHANGE: declare the plan's unit and use one "actual" on every surface (page.tsx:1465 measures the 2 h plan against the pod clock; :607 and :1466 against summed person-hours; the time document reports 1:15:00 wall-clock and 3.5 person-hours against the same plan) — then stamp a vintage per member and run settleD9 per member (today one vintage at podJuris, page.tsx:1442; per-member rows settle at current rate only, :1476, under a USD D9 sentence on Ana's naira row); resync clockEvents on hello/roster and clear them on Reset (page.tsx:318-340, :502-508); bring the ~35 new signer-facing English literals under t() with the pod page inside the i18n gate. The mint 웃 = M × T from the trio-accepted, hash-locked M, one-button clock replicated to three real phones, cap-to-clock, own-seat election and own-currency settlement all WORK and the gate (310/66/27) holds them. | Open | State the plan's unit in one sentence (person-hours for the trio, or wall-clock for the pod), write it as a comment in pod-baseline.ts and on the compose panel, make measuredHours/witnessedHours resolve to that one "actual" at page.tsx:607, :1465, :1466 and in pod-time-report.mjs, and add a pod-invariant assertion that the receipt's hour delta and 웃 delta derive from the same quantity — the same fix stamps a vintage per member so settleD9 runs on Ana's NGN, not the pod's USD. |
| **Thoth** | B- | PROCEED WITH CHANGE: (1) put PLAN → ACTUAL on one declared denominator — frontend/app/soi-session/page.tsx:1465-1466 compares lock.hours (a clock-hour plan, 2 h × 3× = 6 웃) against measuredHours for Δ time but against stand.earned = mint(Σ per-member supported hours, M) (page.tsx:534-535, :592) for Δ 웃, so a pod that clocks its 2 h plan exactly with three present reads 18 웃 actual vs 6 planned and accelerate() reports −4 h; the time document §4 (scripts/pod-time-report.mjs) reads the same plan as person-hours — two carriers, two units, one undeclared plan. (2) Stamp the D9 vintage per contributor and replicate it — page.tsx:1442 writes one stamp at podJuris.rate (7.25 USD) over Σ all members' hours, page.tsx:606 runs settleD9 once on the pod sum in the pod currency, page.tsx:1476 settles Ana and Bo at the CURRENT rate with no vintage; the report's per-member "equal rate" column (pod-time-report.mjs:44) passes the same jurisdiction as both vintage and current, and the run-log step at 37222ms asserts /D9/ && /statutory wage moved/ (pod-live-run-2026-09-11.mjs:113), copy not arithmetic. (3) Apply the 9,999 ceiling per natural person, not to the pod sum — page.tsx:592 standing(carriedIn, mint(witnessedHours, M)) and :1377 hoursToCeiling(M, stand.cumulative) use a three-person sum against a per-person ceiling; page.tsx:602 settlesTo prices three currencies' 웃 at Lea's USD floor. The mint 웃 = M × T, segments, caps, A.B..C grammar, tranches, 106/114/29-37-35-13 tables and every per-member cent reproduce exactly from the log; the numbers around the mint are the ones that lie, and all are invisible at 0.061 웃. | Open | Declare the plan's unit in pod-baseline.ts (pod-hours or person-hours) and make page.tsx:1465, accelerate(), PodFacts.accel_delta and pod-time-report.mjs §4 all compare on that one denominator; then add a pod-invariant gate that a pod clocking exactly its plan with all members present yields Δ 0 h and Δ 0 웃, a per-natural-person ceiling gate (three members at 4,000 웃 → nobody capped), and a per-member D9 gate where vintage ≠ current for a non-default currency (Ana NGN) pays the greater rate. |
| **Thor** | C+ | PROCEED WITH CHANGE: bind every money-moving input at the moment it is accepted, as one class — (1) clock `at` assigned or bounded by the receiver, attributed to a seat, rejected outside `active` (page.tsx:337-340, :429 `by:"pod"`); (2) `brief.lock` accepted only from the pinned lead and only after `verifyBaseline` passes (page.tsx:334; pod-baseline.ts:60 has zero call sites); (3) a per-member vintage (locality + rate) frozen at the first clock start and carried in the roster so all three receipts stamp the same — today only the settling phone stamps (page.tsx:1442) and 8-closed-receipt-ana.jpg has no line 6. Each of the three must get a hostile-seat test on the sim bus in test:ci. | Open | Add a hostile-seat suite to frontend/tests/soi-pod-sim.test.mjs, run in test:ci, that drives real messages over the sim bus — a seated phone sending `clock` with `at` eight hours early and a future `stop`, a stranger and a seated phone pushing `brief.lock` with a wrong hash, and a `member {region}` patch in `closed` — and asserts measure() ignores the forged span, the lock stays the lead's verified hash, and the receipt's per-member vintage does not move; then make the page pass it by binding `at`, lock and locality at acceptance and broadcasting the vintage in the roster so the three receipts stamp identically. |

### Aset · fleet pod Δ · B
*Theme reinforcement & consistency — one nomenclature, 웃 = M × T everywhere, no drift between carriers*

The mint holds: 웃 = M × T lives in one function, M is locked with the plan by hash, read from the verbatim band table, and the gate proves it, 310 passed, 0 failed. The drift is in the carriers that print it. On the closed receipt, page.tsx:1465 prints time against the platform clock while the 웃 line multiplies M by the summed claims, so 0.01 h at 3× reads 0.061; T has two meanings on one line. The D9 vintage at page.tsx:1442 stamps only the pod's rate, though Ana settles in naira and Bo in pesos. pod_router.py:76 still says qualified time. Define T once, stamp each member, gate both.

*Vote:* A (B-) and B (B+) agreed on the verdict and on three findings: two meanings of "actual" on one receipt (page.tsx:1465 measuredHours vs :1477/:592 witnessedHours; confirmed in 8-closed-receipt-lead.jpg), D9 stamped once at the pod's rate with per-member "D9 equal rate" in the document produced by passing the current rate as the vintage (page.tsx:1442, pod-time-report.mjs:44 — verified), and the backend accepting witnessed_for/segments/member_outcomes without putting them in the prompt (pod_router.py:47-49 vs :100-111 — verified). They disagreed on severity: A called the receipt, glyph and D9 items blocking; B called them should-fix. The summarizer sided with B on severity because the thing under review — minting M, 웃 = M × T with the team-accepted M — is one function, locked with the plan and gated 310/310, and the live run proved per-member election, capping and two-segment summing on three phones; the defects are in printing, not minting. A's unique findings were verified and kept: hours under 웃 at page.tsx:1433 and under ♡ at lexicon-data.ts:795; "qualified time" at pod_router.py:76 outside the BANNED scan; log line 65 claiming three outcomes the receipt does not render; ceiling applied to the pod sum at :592 under "each" wording. B's unique findings were verified and kept: pod default locality absent from the brief at page.tsx:305 (run masked it because the lead chose DEFAULT_REGION_ID, log line 3) and the region picker unlocked beside a locked M at :1329. Grade settled at B, between the two, weighted toward B's reading of the mint and A's reading of the carriers.  
*MoT coordinator — class:* Class: every carrier re-derives T, D9 and locality from local state instead of the one locked/replicated definition. Members: receipt page.tsx:1465-1466; audit glyph :1433; lexicon-data.ts:795; report :100 person-hours; PodFacts witnessed_hours vs witnessed_for; pod_router.py:76 unscanned; D9 stamp :1442, settle :1476, report :44; brief :305, picker :1329; ceiling :592; log :114.

### Asar · fleet pod Δ · B-
*Synthesis & outcome validation — does the receipt and the 333-word synthesis say what actually happened*

The pod mints M honestly: 웃 equals M times T with the team-accepted M locked before Start, and log.txt step 56 proves no picker on the audit screen. The words around that mint overclaim. Log step 65 says the receipt shows the three outcomes, but pod-live-run-2026-09-11.mjs:114 asserts only the text at 3×, and the CLOSED block never renders m.outcome. PodFacts accepts segments and member_outcomes, yet pod_router.py:99-111 hands neither to the model. Plan-to-actual compares hours on the pod clock and 웃 on person-hours, so the same plan reads two ways. D9 vintage equals current by construction. The mint is sound; synthesis and receipt must state exactly what the clock witnessed, no more.

*Vote:* A and B agreed on verdict (PROCEED WITH CHANGE) and grade (B-), and on four findings: backend context ignores the new PodFacts fields, local synthesis omits segments and per-member outcomes, Plan → actual mixes pod-clock hours with person-hour 웃, and D9 vintage-equals-current is a tautology stamped at the pod's rate only. They differed in emphasis: B rated the run-log overclaim (step 65 certified by /at 3×/ alone, m.outcome never rendered in CLOSED) as blocking; A did not raise it. B added the uncapped 1 h claims beside 0.01 h with no "capped" word and the dual AI-writer race; A added that the time document is an invented-timestamp scenario carrying the live run's names and outcome texts, and that ♡ counts pod minutes while 웃 counts person-hours. I verified B's blocking finding against pod-live-run-2026-09-11.mjs:114 and the CLOSED block (only the pod-level outcome at ~page.tsx:1486 is printed) and A's pod_router.py:99-111 and pod-time-report.mjs findings, and adopted both sets. Summarizer decision: B- stands as a floor, not a courtesy — the mint, clock, cap, plan lock, per-member currencies and replay are real and gated (310/0, 66/0, 27/0); the receipt, synthesis and log are not yet truthful about outcomes, cap, basis and D9. Kept B's blocking severity because a log line certifying what the receipt does not show is the exact failure this lens exists to catch.  
*MoT coordinator — class:* CLASS: carriers of "what happened" assert more than the clock/members recorded. Members: run-script :114 (/at 3×/) → log step 65; CLOSED never renders m.outcome; Trio :1544/Transcript :626 raw 1 h, no "capped"; SynthesisInput :33-47 lacks segments/outcomes/cap; pod_router :99-111 drops the three PodFacts; gate :540 checks send not consume; Δh :1465 vs Δ웃 :1466/◬ :607 bases; D9 vintage :1442/:606 podJuris only, enum :1479; pod-time-report invented timestamps. Mint sound.

### Athena · fleet pod Δ · B
*Strategic test planning & flow mastery — the phase machine, gates, and the walkthrough coverage*

The pod mints 웃 as M times T, with M locked in the plan hash before Start (pod-baseline.ts:52-58) and no picker at audit (log 'M is locked on the audit screen'); three phones settled Ana in naira and Bo in pesos, log 65/65. But the phase machine has two doors from ACTIVE to RECORD: the desktop button refuses until a segment exists (page.tsx:1148) while the phone strip's Stop does not (page.tsx:1567), so at 375px the pod reaches audit unclocked, supported() returns the raw claim (pod-clock.ts:71), and M multiplies typed hours. Gate stopAndRecord itself; pod-invariant:497 counts buttons, not gates. Then choose one actual for the plan: page.tsx:1465 clocks span, :607 sums person-hours.

*Vote:* A and B agreed on verdict (PROCEED WITH CHANGE), grade (B), the one blocking finding (strip Stop ungated, page.tsx:1567 vs :1148 — verified), the plan-unit defect (:1465 clock span vs :607 person-hours — verified), the swallowed cap assertion (script:102 `.catch(() => {})` — verified) and lead-only receipt assertions (script:108). They differed on how to frame the clock class: A named device-clock skew (pod-clock.ts:50 sorts by each phone's Date.now(); the run's three phones shared one machine clock), B named the missing resync (clock events are page-only state the roster reducer never folds, pod-roster.ts:229, so a dropped press is never repaired). The summarizer rules these are two members of one class — the clock is page-local state ordered by device wall time with no repair path — and carries them as ONE should-fix with a sim leg (kind:"clock" over the bus, out-of-order, duplicate, skew, late-join) as the gate; the sim today has no clock message and package.json:50 still aliases the old script (verified). B's note that the lock is signed by the lead at Share QR (signerIdx default 0, before joiners are named) is kept as a note against unit.accel's no-stake signer. A's provenance note (screenshots badge baa6017 while showing 55a636d behaviour) is kept. Cube 7 untouched, stated by both. HEAD has since moved to a4d1eb4 (stale FAIL shots dropped), which retires B's last note.  
*MoT coordinator — class:* Class: "settle unwitnessed" — invariant (claim ≤ clock) has no owner. Members on the path: strip Stop page.tsx:1567 (ungated), desktop :1148 (button-gated), stopAndRecord :434 (no guard), remote phase message driving record on every phone, supported() pod-clock.ts:71 returns raw claim, Settle :1438 ignores witnessed, pod-invariant:497 counts buttons. Second class: one plan, two actuals (:607 vs :1465).

### Christo · fleet pod Δ · B-
*Consensus & user flow — three people agreeing a plan, electing places, approving, witnessing*

The three phones mint M honestly: the plan 2 h at 3× = 웃 6.000 is locked with a replay hash before Start, M is not a dial at settlement (log 36899ms), and every seat's 웃 is M × its capped hours. What they agreed to is not bound. Back to edit at page.tsx:1089 clears approvals in React only; podRef.current still says agreed:true, mergeRoster at pod-roster.ts:149 keeps approvals sticky, so a version-2 lock reopens with all three approvals nobody gave. The receipt at page.tsx:1465 reads the accepted 2 h as clock time while 웃 counts person-hours, and Ana's naira has no D9 vintage. The flow agrees; the record cannot prove it.

*Vote:* A and B agreed on: the clock-as-one-button journey, own-seat approval/election/witnessing, M locked before Start, three currencies on one receipt, and the plan-unit ambiguity (page.tsx:1465 vs :607/:1466), the payee signer wording, by:"pod" on clock events, and the baa6017 SHA stamp on 55a636d evidence. They disagreed on whether "Back to edit" is safe: A called the live path PASS (newer roster carries agreed:false verbatim, pod-roster.ts:214-222); B called it blocking (React-only setMembers, podRef.current untouched). I verified B: page.tsx:1089 never touches podRef.current, apply() at :311-313 overwrites members from step.state, re-open at :957-963 changes only phase, and mergeRoster at pod-roster.ts:149 makes agreed sticky (incoming || local) — A's cited lines only propagate a roster the lead never publishes with agreed:false. B also carried two findings A missed and I confirmed: D9 vintage stamped only for podJuris (page.tsx:1442, settleD9 :606, per-member cash at current rate :1476), and the brief at :318 carries no clockEvents while pod-roster.ts:263 ignores clock, so a late-joining or reloaded phone caps claims from a shorter log (soi-pod-sim.test.mjs:256 runs with zero clock events). The gate at pod-invariant.test.mjs:534 greps the broken pattern as proof. Summarizer sided with B on severity and grade (B-), adopted A's framing that the plan unit is the consensus defect in this lens, and kept both reviewers' note that minting M itself is WORKING on the evidence.  
*MoT coordinator — class:* Class: "what the trio agreed is not on the record." Members: agreed boolean not bound to lock.hash (page.tsx:1057), React-only clear (:1089) vs podRef/apply (:311-313) and sticky mergeRoster (pod-roster.ts:149), lock overwritten per roster (:334), plan unit clock vs person-hours (:1465/:607/:1466), D9 vintage podJuris-only (:1442/:606/:1476), payee signer wording (:936/:943/:961), brief without clockEvents (:318, pod-roster.ts:263), gate grepping the defect (pod-invariant.test.mjs:534).

### Enki · fleet pod Ω · B-
*Diversity & edge cases — odd inputs, empty outcomes, relocation, NULL floors, capped claims, reload mid-segment*

The mint holds: page.tsx:529 takes M from the accepted lock, the mint is currency-free, and the hash binds m and yug, so no phone can mint at a different multiple than the trio accepted. The edges around it do not. pod-clock.ts:50 sorts by timestamp, so a Stop from a phone running behind the starter is dropped and the segment never closes; log line 33317ms proves only one machine clock. Receipt line 1464 compares plan hours to the clock for time but to summed person-hours for tokens, so the time document prints +1.5 h and -1.5 h for one pod. Vintage is stamped once at the pod default (1442), never per contributor.

*Vote:* A and B agreed on verdict (PROCEED WITH CHANGE), grade (B-), the plan-unit contradiction, pod-default-only vintage, currency-as-jurisdiction D9, no sim test carrying a `clock` message, and that the mint/cap/either-or/lock-hash are gated. They disagreed on severity: A ranked clock skew blocking (B did not raise it) and per-member vintage blocking; B ranked plan units blocking and raised the pod-level "Settles at" line pricing the three-currency sum at the pod default plus the 9,999 ceiling applied to the pod total (A did not raise it). The summarizer re-ran A's skew probe (confirmed: stop@70000 after start@100000 leaves running:true) and re-read page.tsx:591/602 for B's pod-sum finding (confirmed). Decision: plan units and clock skew are both blocking — one contradicts the operator's "plan vs actual in time and HI token" ask on the receipt, the other loses recorded minutes against the module's own invariant on real phones the showcase never had. Per-member vintage is should-fix (the receipt copy honestly says "the pod's own floor", so the screen does not lie, but showcase claim 2 overstates); B's pod-sum settlement is should-fix; A's raw float and B's clock-not-on-roster are merged into the change list; Infinity/1e400 and zero-length segments stay notes.  
*MoT coordinator — class:* CLASS: one pod figure, three definitions. Person-sum witnessedHours vs pod-clock measuredHours vs lock.hours — members: page.tsx:535 mint, :592 standing/9,999 ceiling, :602 settleInRegion pod-default, :607-613 accelerate/split, :629/:665 synthesis+PodFacts, :1396/:1428/:1433 raw display, :1465 receipt Δ. Parallel class: pod-clock.ts:50 sort-by-`at` + :431 fire-and-forget + :316 roster-without-clock + :466 reload known().

### Enlil · fleet pod Ω · B
*Implementation & build verification — does the code do what the commit claims; gates, tsc, CI, build*

Minting M holds: the trio accepts one multiple, pod-baseline.ts hashes m with the hours, page.tsx:529 reads M only from the lock, and the receipt mints 웃 as M times capped witnessed hours, never a currency. Gates prove it: pod-invariant 310 passed, soi-pod-sim 66, soi-pod-flow 27, all in test:ci. What the plan measures against is undefined. page.tsx:607 feeds accelerate the summed person-hours; page.tsx:1465 prints the time delta against the clock span; one receipt shows 0.02 h witnessed beside 0.01 h clocked against 2 h planned. pod_router.py:47-49 accepts segments and outcomes but the prompt drops them. Log step 65 claims three outcomes shown; the script asserts only at 3×. Define the unit first.

*Vote:* A (B+) and B (B) agreed on the verdict PROCEED WITH CHANGE and on the central defect: Baseline.hours has no declared unit, so ◬ acceleration and the 웃 Δ use Σ per-member capped claims (page.tsx:534/607) while the receipt's time Δ uses the clock span (page.tsx:1465) — verified in the file; the summarizer adopts B's severity (blocking-for-the-plan-claim) and makes it the mandatory next. They disagreed on Cube 6: A found the FastAPI prompt (pod_router.py:99-111) never renders witnessed_for/segments/member_outcomes; B passed the backend and faulted only the frontend deterministic synthesis. Verified A: the context string stops at RECORDED OUTCOME; the three new fields are declared and dropped — vote A, and B's note on pod-synthesis.ts stands as a secondary. They disagreed on D9: A passed settleD9; B found it computed once at podJuris (page.tsx:604) while Ana/Bo settle via settleInRegion at the current rate only — verified B; the receipt says so honestly, so should-fix, not blocking. Only B caught that run-log step 65 'receipt shows the three outcomes' asserts merely /at 3×/ (script line 114) and the closed receipt prints no outcome text — verified, adopted. Only A caught the soft cap assertion (script line 102 .catch(() => {})) — verified, adopted. Both confirmed tsc 0, lint:gate 0, all three pod gates in test:ci, time document byte-identical on regeneration, Cube 7 untouched, HEAD a4d1eb4 pushed on both branches, Cloudflare UNVERIFIED (403). Grade settles at B: minting M is correct and gated, but the commit's plan-vs-actual and backend-not-blind claims are each half true.  
*MoT coordinator — class:* Class: "the plan's hours has no declared unit." Members: pod-baseline.ts:24/80 (accelerate delta), page.tsx:607 accelerate(witnessedHours Σ), :1393, :1442 stamp, :1465 receipt Δ vs measuredHours span, :1433 MoT line, pod-time-report.mjs:50/100. Second class: PodFacts declared-then-dropped — pod_router.py:47-49 vs prompt :99-111, pod-synthesis.ts:126-144.

### Krishna · fleet pod Ω · B
*Integration & cross-module — roster protocol, clock replication, PodFacts contract, min-wage.ts reuse*

Minting M holds: 웃 is M times T with the team-accepted M, because M comes from the locked plan (page.tsx:305 carries plan and lock in the brief) and every claim is capped to the pod clock by supported(). The run proves it on three phones: log 33317 shows a joiner's Stop closing segment 2 for everyone, and each receipt settles at its own floor. What fails is the showcase's per-contributor D9: one vintage per pod, stamped only on the settling phone at page.tsx:1442, while pod_router.py:100-112 never reads segments or outcomes. The clock rides no roster, so a late phone mints from a shorter T. Roster must carry clock and per-seat vintages.

*Vote:* A and B agreed on the verdict (PROCEED WITH CHANGE) and on all four integration gaps: (1) clockEvents ride no roster/claim rebuild path, so a late, reloaded or frame-dropped phone mints from a shorter clock and supported() un-caps it (page.tsx:337-342 known() guard, pod-clock.ts:64-76) — verified; (2) D9 is one Vintage per pod at podJuris, written only on the settling phone (page.tsx:604, 1442), per-member rows use settleInRegion current-rate only (page.tsx:1364, 1476), and the time document's D9 column feeds the current rate in as the vintage (pod-time-report.mjs:44) — verified, tautological; (3) pod_router.py:100-112 never puts witnessed_for/segments/member_outcomes in the context — verified; (4) the sim delivers zero `clock` messages (soi-pod-sim.test.mjs mentions clock only in comments at 66 and 273) — verified. They disagreed on grade: A gave B, B gave B+. B credited honesty of the receipt wording and the Worker path; A weighted the blocking per-contributor D9 claim and the missing pod-default replication (regionIdSel local only, page.tsx:305). Summarizer sides with A at B: the showcase states a per-contributor D9 that two of three receipts cannot show (Ana's has no line 6), and the newest protocol message is the only one with no bus test — a claim without its gate is the exact class the AAR forbids. B's extra notes (nowTick frozen outside active at page.tsx:439-443, Austin/Texas duplicate floor, slice(0,2) ISO guess, no min-wage/hi_rates sync gate) are adopted as notes. Cube 7 untouched — both said so; confirmed.  
*MoT coordinator — class:* Class: settlement-authoritative facts born on one phone with no roster rebuild path (brief/plan/lock/record ride rosters; these do not). Members: clockEvents (page.tsx:337-342, 428-432), vintage (1442, settling phone only), regionIdSel pod default (305), per-member D9 via settleInRegion (1365,1476), pod_router.py:100-112 dropping witnessed_for/segments/member_outcomes, tautological pod-time-report.mjs:44, sim with zero clock frames.

### Odin · fleet pod Ω · C+
*Predictive & future-proof — what breaks at scale, on real Supabase, on flaky networks, at year rollover*

The mint holds: 웃 = M × T with the trio-accepted M, hashed before Start (pod-baseline.ts:57-63), gated 310/0 today. What breaks ahead: clock events carry each phone's Date.now() (page.tsx:429) and fold by at (pod-clock.ts:50), so thirty seconds of skew drops a joiner's Stop and three phones mint three 웃; acceptance is a sticky boolean (pod-roster.ts:149) that Back-to-edit clears on one phone, so M can relock as v2 under v1 approvals; the vintage never leaves the settling phone, and log line 64 asserts the D9 receipt on the lead alone while Ana and Bo settle at current rate (page.tsx:1476). At year rollover, or any statutory decrease, D9 protects nobody but the lead.

*Vote:* A (NOT WORKING, C+) and B (PROCEED WITH CHANGE, B-) agreed on the three core failures — wall-clock ordering with no sequence (page.tsx:429, pod-clock.ts:50), D9 applied only at podJuris (page.tsx:606) while members settle at current rate (page.tsx:1365/1476), and the 9,999 ceiling on the pod sum with a hand-typed carry and no year key (page.tsx:534/592). They disagreed on severity of two items I verified: A's finding that the vintage is never broadcast (grep confirms line 320 sends { pod, ...brief, ...record } with no vintage; log line 64 asserts the D9 receipt on the lead only) is on the evidence, not merely predictive, so the showcase's second claim is overstated as shipped — I side with A on verdict form. B's finding that "three REAL phones" (script header line 1) is three Chromium contexts on one host (line 29 newContext) is correct and I adopt it as a wording defect. A's sticky-agreed finding I verified at page.tsx:958-960 (version increments, m: bandM relocked) and 1089 (local setMembers only) — real, should-fix. Summarizer decided: NOT WORKING on the D9-per-contributor and identical-receipts claims, C+ (A's grade), because a claimed property is absent on the evidence, not only under predicted load; the mint M and the operator's clock/outcomes/election asks are met and gated.  
*MoT coordinator — class:* Class: "what the receipt asserts is single-phone state, not the pod's replicated record." Members on the path: vintage local-only (page.tsx:211/1442, absent from broadcast :320); clock ordered by each phone's Date.now() (page.tsx:429, pod-clock.ts:50); clock log never in roster/brief (pod-roster.ts:263); agreed OR-merged unbound to lock hash (pod-roster.ts:149, page.tsx:959/1089); per-member settleInRegion current-rate, no D9 (page.tsx:1365/1476 vs 606); pod-summed ceiling, no year (page.tsx:534/592).

### Pangu · fleet pod Σ · B
*Cutting-edge innovation — is this genuinely the first system to pair input-in-time with local-floor value*

The pod mints M honestly: yug (웃) is M times T, M being the trio-accepted lock, hashed before Start (pod-baseline.ts:55-57), read at page.tsx:530, and the 24-second clock capped a 1 h claim to 0.01 h on three phones (log 33633ms). That pairing of witnessed time, currency-free mint and elected floor is new. But the second half fails: the vintage is stamped once at the pod's floor (page.tsx:1442) while Ana and Bo settle by live settleInRegion (page.tsx:1476), and pod-time-report.mjs:44 passes current as vintage, so D9 is a tautology. The plan's hours compare to wall-clock at :1465 yet mint from summed person-hours at :534. Stamp per contributor, declare the unit, verify the lock.

*Vote:* A and B agreed on verdict (PROCEED WITH CHANGE) and grade (B), and on the core defect: the per-contributor D9 vintage is never stamped (page.tsx:1442 stamps podJuris only; :1476 settles members at the live rate) and the time report's "equal rate" column is circular (pod-time-report.mjs:44). They differed on severity (A: blocking; B: should-fix) — I verified the lines and side with A's substance but B's framing: the receipt's own sentence ("the pod's own floor paid the equal rate") is honest, so nothing shipped lies on a phone; the document overclaims. Blocking for the document's claim, should-fix for the code. B added two findings A missed, both verified: the plan's unit is undeclared (:1465 wall-clock vs :534 person-hours, opposite ◬ verdicts for the same run) and verifyBaseline has no call site in the page (:334 accepts any string hash) — accepted. A added the ceiling applied to the pod sum rather than per natural person (:594 over witnessedHours) — verified, accepted as should-fix. Both flagged by:"pod" on clock events and the "first universal system" wording as unprovable from the evidence; the UI never claims "first", which is honest. Summarizer: grade B, unanimous, six changes ordered above by weight.  
*MoT coordinator — class:* Class: "a contributor's settlement is never recorded — only the pod's is." Members on the path: page.tsx:1442 (stamp podJuris only), :606 (D9 pod-only), :1365 and :1476 (settleInRegion live), :474 (ledger persists region, not rate), pod-time-report.mjs:43-44 (current fed as vintage), gate :585-588 (no per-member D9 assertion). Fix per-member Vintage once, all sites read it.

### Sofia · fleet pod Σ · B-
*Multi-perspective — the contributor, the lead, the auditor, the regulator, the person in Lagos vs Austin*

The mint itself holds: M×T is minted once at page.tsx:531 from the M the trio accepted and locked by hash, and three real phones proved a joiner's Stop closes the segment for everyone (log 33317ms). What fails is the unit T is measured in. The plan's 2 h is compared to the pod clock at page.tsx:1465 and to summed person-hours at page.tsx:607 and :1466, so one receipt says the pod finished early while the time document says it ran late. Ana's naira row settles at current rate only (page.tsx:1476); her D9 vintage was never stamped. Define T explicitly, stamp each member, then M means the same thing in Lagos and Austin.

*Vote:* A and B agreed on the verdict (PROCEED WITH CHANGE), on the two-unit plan defect, on per-member D9 being claimed but not implemented (:1442/:1476), and on the hardcoded-English lexicon failure. They disagreed on severity and grade: A called the unit mismatch blocking (B-), B called it should-fix (B). A found the signer-with-stake label at :961; B found three class points A missed — per-person ceiling applied to pod-summed 웃 (:592), no clock resync on hello/roster and Reset leaving clockEvents (:318-340, :502-508), and 5b minting unwitnessed claims (:1476 vs :534). Summarizer verified each cited line in the file and the log (65 OK; 33317ms joiner Stop; time doc rows 24 and 45) and sided with A on severity and grade — a receipt that contradicts itself about plan-vs-actual on the ◬ path is a measurement defect, not wording — while adopting B's Cube 5 loss/Reset class and ceiling-per-person findings as second-tier fixes. Final: B-, PROCEED WITH CHANGE.  
*MoT coordinator — class:* Class: "one plan quantity, one actual" is undefined. Members on the path: pod-baseline.ts:24 (hours unitless) → accelerate :607 (person-hours) → receipt :1465 (clock) / :1466 (person-hours) → pod-time-report.mjs:50 → per-member D9 :1442/:1476/:1477 (pod vintage, no member vintage) → ceiling :592 (pod-summed) → clock resync :318-340 / Reset :502-508.

### Thoth · fleet pod Σ · B-
*Data & analytics — every number: segments, caps, 웃, tranches, currencies, D9, the 111/333 counts*

Thoth counts: the mint is right. 웃 = M × T reproduces from the run log to the cent — 24,377 ms of segments, three members, 3× gives 0.061 웃, and $0.15, NGN 8.18, ₱1.76 all check against 7.25, 402.739 and 86.875. What lies is the denominator around the accepted M. page.tsx:1465 compares a two-hour plan against wall-clock hours for Δ time but against three-person hours for Δ 웃, so a pod clocking its plan exactly reads 18 actual against 6 planned. page.tsx:1442 stamps one USD vintage over three currencies; page.tsx:592 caps the pod sum at a per-person ceiling. Declare the plan's unit, stamp D9 per member, cap per natural person.

*Vote:* A and B agreed on verdict (PROCEED WITH CHANGE), grade (B-), and the two lead defects: the plan→actual denominator mismatch on page.tsx:1465-1466 and the pod-level-only D9 vintage at page.tsx:1442/:606/:1476. Both confirmed the mint, segments, per-member cents, A.B..C grammar and the 310+66+27 gates reproduce exactly. They disagreed on scope: A flagged the 9,999 per-natural-person ceiling applied to the pod sum (page.tsx:592, :1321, :1377) and the USD "Settles at $0.44" over three currencies (:602) as blocking/should-fix; B did not raise the ceiling basis and instead flagged the missing drawable-now currency figure (tranche 1× vs full-multiple "Settles as") and the ♡-per-pod-minute vs 웃-per-person-hour comparison in time doc §5. The summarizer verified page.tsx:592 (standing() takes Σ witnessedHours across members) and page.tsx:1476 (settleInRegion at current rate, no vintage) and sided with A on the ceiling — unit.ceiling is per natural person, so a three-member sum against it is wrong at any real scale — while carrying B's tranche-display and ♡-denominator points as should-fix. B's ranking of D9 as blocking rather than should-fix was adopted because the showcase claim names D9 per contributor. Both reviewers' notes on the backend (hi_rates 4.807 with no M, out of scope by ruling) and the 1667/1,666 same-line render (verified: (9999/6).toFixed(0) = "1667") are carried as notes.  
*MoT coordinator — class:* Class: one pod-sum (witnessedHours, page.tsx:534) fed to per-person/per-pod-clock primitives. Members: accelerate :607→receipt :1465, PodFacts.accel_delta, report §4; standing :592, carriedIn :1321, hoursToCeiling :1377; stamp :1442, settleD9 :606, settleInRegion :1476, report:44, run-log:113, gate:243; settlesTo :602; heartsFor :616. Fix: per-member record, then sum.

### Thor · fleet pod Σ · C+
*Risk & security — forged clock messages, impostor phones, jurisdiction-shopping, replay of a stale press*

Thor on the minted M: yug equals M times T holds arithmetically, but neither factor is bound. T folds clock events whose at any seated phone chose (page.tsx:337-340); a forged early start measures 8 hours and supported() certifies it witnessed. M is lock.m, adopted from any envelope carrying a brief (page.tsx:334) with verifyBaseline never called, so the team-accepted plan is replaceable after Start. The vintage is stamped only on the settling phone (page.tsx:1442); 8-closed-receipt-ana.jpg has no line 6 while lead's does, so three ledgers already disagree. Log line 64 asserted D9 on the lead alone. Cooperating phones delivered both claims; a cheating seat still chooses its own pay. Bind at acceptance.

*Vote:* A (PROCEED WITH CHANGE, B-) and B (NOT WORKING, C) agreed on the three blocking classes: sender-supplied clock `at` accepted from any known seat in any phase; `brief.lock` adopted without `verifyBaseline` or a lead check; jurisdiction re-election open after settlement with the vintage stamping only the pod floor. They disagreed on verdict and on two findings. I verified B's extra finding against the files and evidence: pod-roster.ts carries no `vintage` field, `drive()` sends only `movePhase`, and 8-closed-receipt-ana.jpg ends at 5b with no "6 · Stamped" while the lead's shows it — so the shipped evidence itself exhibits three disagreeing receipts; log line 64 ("receipt names the D9 rate that paid") was asserted on the lead only. I also confirmed A's should-fix that the Trio block prints "✓ witnessed · 1h" beside a 5b settlement of 0.01 h (visible on ana's receipt). B's signer-conflict finding (page.tsx:961, a paid member signs the accelerator) is upheld as should-fix. On verdict I side with A: the operator's two asked-for claims are delivered on three cooperating phones (log 65/65, 310/66/27 green) so the journey is not "not working"; but B's per-phone-stamp defect is real and shipped, so the grade lands between them at C+. A ranked the unverified lock blocking, B should-fix — I hold it blocking because M is the multiplier on every 웃 minted and the showcase says "locked with a hash". Cube 7 untouched, both confirmed.  
*MoT coordinator — class:* Class: money-moving inputs read live from the last message, never bound at acceptance. Members: clock `at` (page.tsx:337-340, :429 by:"pod"); brief.lock without lead check or verifyBaseline (:334, pod-baseline.ts:60 zero call sites); per-phone vintage/locality (:1442 settle-only stamp, :1476 live localityOf, pod-roster.ts member patch phase-free, no roster vintage); no hostile-seat sim gate.

# 5 · Master of Thought — synthesis (333 words)

The fleet proves the mint. Twelve packages, three pods, one arithmetic: 웃 equals M times T, where M is the multiple the trio accepted and hash-locked before Start (pod-baseline.ts:52-58, read once at page.tsx:529), and T is the clock every phone witnessed. The run log pod-live-run-2026-09-11 shows three phones: step 56 finds no picker on the audit screen, line 33317ms shows a joiner's Stop closing segment two for everyone, line 33633ms caps a one-hour claim to 0.01 h, and Thoth reproduces every cent from 24,377 ms of segments, USD 0.15, NGN 8.18, PHP 1.76 against 7.25, 402.739 and 86.875. Gates hold it: pod-invariant 310 passed, soi-pod-sim 66, soi-pod-flow 27, all inside test:ci.

The mint does not fail; its carriers drift, in four classes. First, one plan quantity has three definitions: lock.hours is unitless (pod-baseline.ts:24), the receipt measures time against the clock span (page.tsx:1465) but 웃 against summed person-hours (:607, :1466, :534), so a pod clocking its plan exactly reads 18 웃 against 6. Second, a contributor's settlement is never recorded: one vintage at podJuris (page.tsx:1442), members settled live (:1476), the report feeding current as vintage (pod-time-report.mjs:44), a per-person ceiling applied to a pod sum (:592). Third, money-moving inputs bind live, not at acceptance: clock at (page.tsx:429, pod-clock.ts:50), brief.lock without verifyBaseline (:334), sticky approvals (pod-roster.ts:149). Fourth, carriers overclaim: PodFacts dropped (pod_router.py:99-111), log step 65.

Next session runs in this order and no other. State the invariant first: one plan, one actual, one vintage per natural person, every input bound when accepted. Write it as a comment, then enumerate the whole class, fix all members together, and give each invariant a gate in test:ci that fails when the guarantee is removed: a sim case driving clock over the bus, a hostile-seat case, an assertion consuming member_outcomes not sending them. Rerun the three-phone script asserting the cap and the three outcomes instead of at 3×. Residual gaps stay the operator's: the D1 Tier-2 rule, D2 Global Agreed Standard, Cambodia's rate, Mexico FX, and backend mint without M.

## 6 · Mandatory next-session process

1. **Aset:** Define "witnessed hours" once in pod-clock.ts (clock span) and name the summed capped claims separately (e.g. contributed hours); print each under its own name on the receipt, the audit panel and the time document, with hours never under a 웃 or ♡ glyph; stamp a per-member D9 vintage at each contributor's elected floor; then add a pod-invariant.test.mjs assertion that the receipt's Plan → actual line satisfies stand.earned === mint(printedHours, M) and that pod_router.py and pod-time-report.mjs pass the BANNED scan.
2. **Asar:** Make the record say only what happened, and gate it: (a) render the three per-member outcomes and the word "capped" (with the clock figure) in the CLOSED block, and change pod-live-run-2026-09-11.mjs:114 to assert each member's outcome text is present, not /at 3×/; (b) feed segments, capped claims and member outcomes into both the backend context (pod_router.py:99-111) and SynthesisInput (pod-synthesis.ts:33-47), and add a pod-invariant assertion that the context string and buildSynthesis333 consume them; (c) declare the plan's hour unit once and compute Δ h and Δ 웃 on that one basis (page.tsx:1465-1466); (d) stamp vintage per member at their elected locality or drop the pod-wide D9 sentence, and replace the enum "equal" with plain prose; (e) add one line to 2026-09-11_pod_time_volunteer_vs_paid.md stating it is a computed scenario and pointing at log.txt for the 0:00:24 live run.
3. **Athena:** Move the clock gate into stopAndRecord (refuse to leave ACTIVE with zero segments on every route), add the assertion to frontend/tests/pod-invariant.test.mjs so a build with an ungated route fails, and re-run the three-phone showcase with the cap check promoted from a swallowed waitForFunction (script:102) to a hard step on each phone.
4. **Christo:** Make acceptance structural: each seat's approval records the lock hash it accepted (agreedTo: lock.hash), allAgreed requires every seat's agreedTo === lock.hash, Back to edit and re-open clear it via apply(patchAll(...)) + publish so joiners receive the clear; replace the grep at pod-invariant.test.mjs:534 with a reducer-driven test (three seats accept version 1, lead re-opens version 2, assert allAgreed is false on all three phones) and rerun the three-phone showcase from the committed SHA so the screenshot stamp matches.
5. **Enki:** Add a soi-pod-sim case that drives `clock` messages through the bus with skewed timestamps (stop.at < start.at), a duplicate, a dropped frame and a lead reload mid-segment, asserting all three ledgers measure the identical span — then fix pod-clock.ts measure() so a later-arriving Stop closes the open segment regardless of its `at` ordering, and pin the plan's hour unit (pod-clock vs person-hours) in one function that both the receipt time line and the 웃 line read.
6. **Enlil:** State the invariant on Baseline.hours (pod-baseline.ts:26) — the plan is in person-hours (or in clock span; choose one) — derive a single actualHours from it, feed that one figure to accelerate() (page.tsx:607), the receipt's Plan → actual line (page.tsx:1465) and pod-time-report.mjs, and add a pod-invariant assertion that both Δ lines on the receipt derive from the same actual; in the same commit render the three new PodFacts fields in the pod_router.py prompt with a soi-pod-sim check that they appear.
7. **Krishna:** Fix the class, not the instance: add `clock: ClockEvent[]` and per-seat `vintage` to the roster rebuild path (the same path that carries brief/plan/lock at page.tsx:311-323), dedupe on arrival as `lock` already does, then write the gate first — a soi-pod-sim case that delivers Start/Stop/Add over the bus with one lossy peer and one late joiner and asserts all three phones' measure().span and each member's settled 웃 are identical — and make pod_router.py:100-112 print witnessed_for, segments and member_outcomes into the context with a backend test asserting they appear.
8. **Odin:** Make the clock and the settlement replicable and gate it: give every ClockEvent a monotonic seq plus a lead- or server-authoritative timestamp and carry the clock log and the vintage in the roster/brief so a reconnecting phone catches up; stamp a vintage per contributor at their elected jurisdiction id (not currency) and settle each member through settleD9; bind `agreed` to the lock hash so any M relock clears approvals on every phone. Add to pod-invariant.test.mjs: a skew case (joiner −30 s/+30 s Stop still closes the segment), a three-phone receipt-equality assertion, and a per-member D9 floor case across a statutory decrease. Reword the showcase header to "three emulated phones, one host, local relay".
9. **Pangu:** Fix the class, not the instance: introduce a per-member Vintage (stamped once at settlement with that member's elected rate and currency, persisted in the ledger entry), route receipt-member-* and pod-time-report.mjs through settleD9 with that stamp, and add a journey assertion in soi-pod-sim that raising the NGN rate after the stamp pays Ana the higher figure while Bo and Lea are unchanged — plus a source gate that the receipt reads a per-member vintage, so deleting the fix fails 311/311.
10. **Sofia:** State the plan's unit in one sentence (person-hours for the trio, or wall-clock for the pod), write it as a comment in pod-baseline.ts and on the compose panel, make measuredHours/witnessedHours resolve to that one "actual" at page.tsx:607, :1465, :1466 and in pod-time-report.mjs, and add a pod-invariant assertion that the receipt's hour delta and 웃 delta derive from the same quantity — the same fix stamps a vintage per member so settleD9 runs on Ana's NGN, not the pod's USD.
11. **Thoth:** Declare the plan's unit in pod-baseline.ts (pod-hours or person-hours) and make page.tsx:1465, accelerate(), PodFacts.accel_delta and pod-time-report.mjs §4 all compare on that one denominator; then add a pod-invariant gate that a pod clocking exactly its plan with all members present yields Δ 0 h and Δ 0 웃, a per-natural-person ceiling gate (three members at 4,000 웃 → nobody capped), and a per-member D9 gate where vintage ≠ current for a non-default currency (Ana NGN) pays the greater rate.
12. **Thor:** Add a hostile-seat suite to frontend/tests/soi-pod-sim.test.mjs, run in test:ci, that drives real messages over the sim bus — a seated phone sending `clock` with `at` eight hours early and a future `stop`, a stranger and a seated phone pushing `brief.lock` with a wrong hash, and a `member {region}` patch in `closed` — and asserts measure() ignores the forged span, the lock stays the lead's verified hash, and the receipt's per-member vintage does not move; then make the page pass it by binding `at`, lock and locality at acceptance and broadcasting the vintage in the roster so the three receipts stamp identically.

## 7 · Residual gaps that are the operator's

- **D1** — the Tier-2 selection rule (35 jurisdictions), open since r57; the pod shows the register's recommended default as an unsettled proposal.
- **D2** — the Global Agreed Standard value (13 jurisdictions), open since r57; same.
- **Cambodia** is in both existing rate tables and absent from the operator's 103 (merged in from hi_rates.py, flagged).
- **Mexico** — 1.43 USD vs 39.380 MXN implies 27.5 MXN/USD; both figures kept, neither edited.
- **Backend mint** still `hours × 4.807` with no M and no locality — out of scope by the earlier pod-only ruling.
- **The pod join code** — silent same-code data loss, no error path for a bad code, an 8-char poll code accepted — reported, open.

---

# 8 · Fixed after the review, before the ship — the class, every member, each gated

The protocol's rule: anything the fleet marks NOT WORKING is fixed before the ship, not after. All twelve coordinators
named a *class*, and the MoT synthesis set the order — state the invariant, enumerate the class, fix every member
together, gate each. Done in one commit; the three-phone showcase was rerun on the fix: **77 steps, 0 failures**, and
its last line is the property Odin marked NOT WORKING — *the three receipts settle identically*.

| Class member (the fleet's citation) | Fix | Gate (`pod-invariant.test.mjs`) |
|---|---|---|
| One plan quantity, three meanings — receipt time by clock span, 웃 by summed claims (Aset, Enlil, Sofia, Thoth) | The plan's hours are **person-hours across the trio**, stated on the lock (`pod-baseline.ts`) and on the compose panel; the receipt compares planned vs counted person-hours and prints the pod clock separately, *under its own name* | both Δ derive from `witnessedHours`; `measuredHours` never compared to the plan |
| Hours under a coin glyph (`page.tsx` audit panel) (Aset) | `· hours {fmtABC(…)}` | the glyph form is refused |
| One vintage at the pod's rate; members settled live at current rate (Odin, Pangu, Sofia, Thoth) | **One vintage per natural person**, stamped at settlement with that member's elected floor and the accepted M; each settles through `settleD9` with *their* vintage; ceiling applied per person | stamp-per-member regex; `settleD9(Math.min(own, YUG_CEILING), v…)`; read back on reopen |
| The vintage never leaves the settling phone (Odin, Krishna) | The settlement rides the phase move (`settle: { vintage, memberVintages }`), accepted once, only from a known phone | asserted; the runner asserts three identical `receipt-each` texts |
| Sticky approval unbound to the plan (Christo, Thor, Odin) | `Member.agreedTo` = the lock hash approved; `allAgreed` requires equality with the current lock; a re-lock invalidates every approval by construction | asserted on roster, gate and checkbox |
| `brief.lock` trusted without verification (Odin, Thor) | `verifyBaseline(incoming)` before `setLock` | asserted |
| Clock ordered by each phone's wall clock (Odin, Enki, Thor) | `ClockEvent.seq` assigned by the presser; `measure()` folds by `seq`; a received press > 5 min from local time is refused | behavioural: a −30 s Stop still closes; +30 s counts; guard regex |
| A route could leave ACTIVE with nothing clocked (Athena) | `stopAndRecord` refuses with zero segments — the strip included | asserted |
| Backend accepts the clock and outcomes but does not use them (Krishna, Enlil, Asar) | `pod_router.py` renders the clock, the segments and the three outcomes in the prompt, and says 웃 = M × T | asserted on the router source |
| The receipt claimed three outcomes it did not render; the run-log line overclaimed (Asar) | Line 5a renders each member's outcome; the runner asserts each member's own text on every phone | asserted |
| "Three REAL phones" (Odin) | "three EMULATED phones on one host, local relay" | asserted |
| The time document read as a live run (Asar) | It states it is a computed scenario and names the plan's unit; the live figures are in `log.txt` | asserted |

Gate 310 → 332. Not changed, deliberately: the fleet's twelve 111-word comments and the 333-word synthesis above are
the record of what was found; the State column stays as they wrote it.
