# Sign Doc — 12 Ascended Masters × 111 words + 3 Masters of Thought × 333 words on the NEW plan items

Date: 2026-09-08 17:00–17:24 UTC · Tree: working tree at HEAD ec777d8 + uncommitted plan work (mid-application) · Run `wf_9b5f9af4-3c2`, read-only, 15/15 returned.

Scope: only the NEW items of `/root/.claude/plans/do-me-a-fsvor-pure-wirth.md` (fleet pass-2 fixes, PDF zoom, Light Codex order and symbols, text at document size). Each master verified claims in the code (file:line) and wrote exactly 111 words; each MoT verified the pod's top gaps and wrote 333 words. The distilled execution checklist is in the plan and is what ships next.

## Verdicts

| AsM | Verdict | Gaps named |
|---|---|:---:|
| Aset | ready-with-fixes | 9 |
| Asar | ready-with-fixes | 6 |
| Athena | ready-with-fixes | 10 |
| Christo | ready-with-fixes | 10 |
| Enki | ready-with-fixes | 11 |
| Enlil | ready-with-fixes | 10 |
| Krishna | ready-with-fixes | 7 |
| Odin | ready-with-fixes | 7 |
| Pangu | ready-with-fixes | 6 |
| Sofia | ready-with-fixes | 9 |
| Thoth | ready-with-fixes | 7 |
| Thor | ready-with-fixes | 5 |

| MoT-1 | READY | 13 priorities |
| MoT-2 | READY | 12 priorities |
| MoT-3 | READY | 11 priorities |

## The twelve, 111 words each

### Aset — ready-with-fixes (111 words)

I read the tree, not the plan. The words are there: Error, Check again, Zoom in, Snap to the line, Signer {n}, each with its Spanish twin, and the parity gate will pass. But three of those words reach no screen. The error step is written and never lit; the rail still maps error to nothing. Check again is a key without a button. The AI button still wears the record glyph though its cure is already imported and unused. Cyan lingers in fourteen places. The verifier now speaks sentences, names its signers, and the codex takes hyphen, underscore, bullet. Wire them; then the receipt, which drifts between Done and Verify.

Verified in the code:
- Lexicon: 16 new soi.sign.* keys added with EN + ES pairs (step.error, x.error, signer_n, verify.issue.{no_signatures,images_vs_boxes,passes_vs_signatures,rows_vs_signatures,row_malformed,row_chain,other}, check_again, zoom_in, zoom_out, zoom_reset, snap_line, zoom_hint) at frontend/lib/lexicon-data.ts:1026-1041 and frontend/lib/lexicon-translations-es-sign.ts:170-185; the ES parity gate derives from every soi.sign.* key (frontend/tests/sign-i18n.test.mjs:21-24) so it stays green
- Zoom controls carry t() aria-labels: zoom-out / zoom-reset / zoom-in / snap-line at frontend/components/sign/pdf-page-view.tsx:182-185, hint at :206 (soi.sign.zoom_hint) (plan C, applied)
- Verify issue codes become lexicon sentences with {n} substitution for row_N_chain, unknown text folded in <details>: frontend/components/sign/verify-file.tsx:11,25-31,46-48; signer names from row.name with soi.sign.signer_n fallback :32,:51; sign-verify exposes rows[].name at frontend/lib/sign-verify.ts:13-14 (plan B15, applied)
- Diagnosis row no longer prints the raw key: railOf(step) plus the failed stage via soi.sign.stage.* (EN lexicon-data.ts:995-998, ES es-sign.ts:143-146) at frontend/components/sign/sign-diag.tsx:17,46 (applied)
- Stamped date follows the Globe: dateIn(activeLocale, ...) with latn digits at frontend/components/sign/sign-flow.tsx:53-57 (helper) and :338 (plan B2 stamped date, applied)
- Glyph law written and typed: SIGN_STEP_KEYS / SignStep union, AI_GLYPH = ✦, table in the header at frontend/lib/sign-steps.ts:6-17,19-21,31-34 (plan B4, lib half applied)
- Light Codex symbols - _ • as VCVC / VRVR / VYVY, reported as BRYCVW, no green, reversal-safe at frontend/lib/light-codex.ts:25-30; unsupportedChars comment :66 (operator ask, applied); no lexicon string enumerates the supported set, so no new key is owed
- Text marks sized to the document: TXT_H 0.02 (a 16-pt line on Letter), MIN_H 0.012 at frontend/components/sign/pdf-page-view.tsx:23; stampText font follows the box height (frontend/lib/pdf-stamp.ts:134 size = clamp(dispH*0.72, 6, 24)) (operator ask, applied)

Gaps:
- **minutes** · soi.sign.step.error and soi.sign.x.error are defined (EN+ES) but never rendered: the rail memo still uses the old inline mapping (error maps to itself, lighting nothing) and lastRail is declared but never assigned or read; railOf(step) returns null for error and is unused by the rail — `frontend/components/sign/sign-flow.tsx:52,96,493-497` → In the rail useMemo use railOf(step); when it returns null light lastRail.current (set lastRail.current = r on every non-null railOf in a useEffect on step). In explain, case "error": return err || t("soi.sign.x.error"). Then step.error is reached by sign-diag only, which is fine; add an assertion in tests/sign-i18n.test.mjs that every Step literal resolves to a defined key.
- **minutes** · soi.sign.check_again is defined (EN+ES) but there is no Check again button; the waiting state shows only <Roster /> and the checking flag is never surfaced — `frontend/components/sign/sign-flow.tsx:200,702` → Beside the Roster in the waiting branch render <button data-testid="check-again" disabled={checking} onClick={() => void refreshEnvelope()}>{t("soi.sign.check_again")}</button> (44 px, border-primary tokens).
- **minutes** · Glyph law broken in the UI: the AI placement button still prints ◬ although AI_GLYPH (✦) is imported at :19 and unused, so ◬ still carries two meanings (record/chain on receipts + AI act) — `frontend/components/sign/sign-flow.tsx:626` → Replace the literal "◬ " on the ai-find button with {AI_GLYPH} ; add a one-line test in tests/sign-i18n.test.mjs asserting SIGN_STEPS.sign.glyph === "◬" and AI_GLYPH !== "◬" and that no SIGN_STEPS glyph equals AI_GLYPH.
- **minutes** · Hard-coded cyan remains in 14 places (explainer, resumed banner, roster you-badge/current signer, upload drop zone, login-now link, signers header, add-signer, sizing chip, stroke-kept notes, holders-left, and the mark box borders/resize handle), so a non-cyan theme shows two accent colours on one page (plan B3 only partly applied) — `frontend/components/sign/sign-flow.tsx:519,520,540,542,552,557,578,588,616,645,650,686; frontend/components/sign/pdf-page-view.tsx:194,198` → Swap text-cyan-400/300 -> text-primary, border-cyan-400/xx -> border-primary/xx, bg-cyan-400/xx -> bg-primary/xx (or the hue.bright/dim/faint style already used by the rail). Keep the amber text-box tint. Grep gate: grep -c cyan components/sign/*.tsx must be 0.
- **hour** · Receipt parity still drifts: the Done receipt line 2 prints name ✓/✗ per signer while the Verify receipt line 2 prints signerName · cacStamp(isoDate); line 3 matches. One document read twice gives two shapes. — `frontend/components/sign/sign-flow.tsx:711-713; frontend/components/sign/verify-file.tsx:50-53` → Extract components/sign/receipt.tsx (SignReceipt) taking rows {name, stamp?, signed} and render it from both places: line 2 = name ✓ cacStamp(signed_at|isoDate) per signer (Done has signed_at from pub.signers; Verify has isoDate from rows), line 3 unchanged.
- **minutes** · 24-hour link expiry text still follows the browser locale, not activeLocale (plan B2 second half): dateIn is called once only (stamped date) — `frontend/components/sign/sign-flow.tsx:338 (only call); the tmp-link expiry sentence near the handoff/downloads-partly block` → Route the expiry Date through dateIn(activeLocale, d, opts, true) as well; one sign-i18n test asserting an es form.
- **minutes** · New placeholder keys (soi.sign.signer_n, soi.sign.verify.issue.row_chain with {n}) are not covered by the placeholder-parity check, which pins four keys — `frontend/tests/sign-i18n.test.mjs:27` → Derive the checked set from every EN key containing {…} so Spanish must keep {n} for signer_n and verify.issue.row_chain.
- **minutes** · Light Codex symbols have no round-trip test (no light-codex test file; codex-pdf.test.mjs never encodes - _ •) — `frontend/tests/codex-pdf.test.mjs` → Add one case encoding "A-B_C•D" through the strip and decoding it back, plus an ALPHA-uniqueness assertion (no group or its reversal collides across ALPHA, NUMBERS and the frame).
- **minutes** · RTL: the handoff button row uses flex-row-reverse justify-end when mail leads, which double-flips under dir=rtl; the zoom bar and ‹ › arrows are physical (acceptable for a page control) but the mark-box resize handle is right-anchored (-right-2.5) — `frontend/components/sign/handoff.tsx:38; frontend/components/sign/pdf-page-view.tsx:198` → Order the two anchors in JSX by mailFirst instead of flex-row-reverse; keep the PDF host dir="ltr" explicitly (add dir="ltr" on the host div at pdf-page-view.tsx:189) so the canvas and its handles never mirror.

### Asar — ready-with-fixes (111 words)

The signed file now carries what the plan promised. Every text mark rides with its text. The caption prints the receipt's own UTC form, and a name the font cannot draw becomes the contact or Signer N, the same word the hidden line speaks. The helix sinks to a quarter point and stays readable from its pixels. The clear never crosses below the box, so the document's rule survives. The first signer holds the right edge and each newcomer stands to the left. One debt remains: the codex tests and the live run still expect the old space and dot, while the code writes underscore and bullet. Align them, then ship.

Verified in the code:
- A3 SoITxt:t — APPLIED. frontend/lib/pdf-stamp.ts:143 appends `:t${b64u(clean)}` (text as drawn, pdfSafe) to the SoITxt keyword; textBoxes (pdf-stamp.ts:148-159) returns TextMark with text/signerIdx/isoDate/chain via regex tolerant of colons in the ISO time and of pre-2026-09-08 keywords. Round-trip tests in tests/pdf-stamp.test.mjs (new blocks after the `s3` stampText; rotated-page block asserts `:t[A-Za-z0-9_-]+` and reads text/signerIdx/chain back).
- A1 cacStamp caption — APPLIED. frontend/lib/pdf-stamp.ts:97 `captionName(sig.name, sig.contact, idx) · cacStamp(sig.isoDate) · #hash`; cacStamp defined pdf-stamp.ts:236-239 (YYYY.MM.DD HH:MM:SS UTC). The two pairing tests in tests/pdf-stamp.test.mjs were rewritten to the `2026\.09\.07 12:00:00 UTC` form and assert the ISO string is absent from the page text. Rotated-page caption under the rule: pdf-stamp.ts:106-110 maps a 5.5-pt strip through placeOnPage/oriented.
- A2 captionName fallback — APPLIED. captionName lives in frontend/lib/codex-pdf.ts:48-52 (Latin name → contact → `Signer N`), re-exported from pdf-stamp.ts:64; codexText (codex-pdf.ts:58-69) uses the same fallback and trims the NAME to CODEX_LINE_MAX − stamp − 1, never the stamp; codexAllText (codex-pdf.ts:73-74) passes contact + rowIndex. Tests: tests/pdf-stamp.test.mjs captionName line + `Signer 2` / `wei@example.com` page-text checks; tests/codex-pdf.test.mjs long-name, non-Latin and contact cases.
- C2 initials row order — APPLIED. frontend/lib/pdf-stamp.ts:222 `x = width − INIT_SLOT.right − Σ widths[0..idx] − idx·gap` (signer 0 at the right edge, each additional signer left of the previous). tests/pdf-stamp.test.mjs new block asserts 558/518/478 on Letter, contiguity with one gap, and a wide first slot (96) pushing the second to 458. Placeholders / white-fill / SoIInitW already key by idx (pdf-stamp.ts:284-297).
- A4 helix 0.25 pt — APPLIED. frontend/lib/pdf-stamp.ts:281 embedCodexImage(..., w, 0.25); decoder reads the XObject pixels (tests/codex-pdf.test.mjs:33 asserts 2 px tall, ≥612 wide; :35 every strip decodes reverse-verified).
- Codex line 2026.09.08_08.11CST joined by • — APPLIED in code. frontend/lib/light-codex.ts:29-34 adds `-`=VCVC, `_`=VRVR, `•`=VYVY (all unused, no green, reversals CVCV/RVRV/YVYV also unused); unsupportedChars accepts them (light-codex.ts:66-74). codex-pdf.ts:66-67 writes `YYYY.MM.DD_HH.MMZZZ`, codex-pdf.ts:74 joins rows with ` • `.
- clearBox never below the box — APPLIED. frontend/lib/pdf-stamp.ts:10-21: per-/Rotate side that is the displayed bottom gets grow 0 (rot 0 → b, 90 → r, 180 → t, 270 → l).
- A6 initialsRowFrac — APPLIED. pdf-stamp.ts:224-230 (floor 0.45, cap 1); sign-layout.ts:8-12 doc; consumed in components/sign/sign-flow.tsx:45-46,390,393; tests/sign-layout.test.mjs b5 (60 % band sees ink the 45 % band misses).

Gaps:
- **minutes** · tests/codex-pdf.test.mjs still expects the OLD codex form (space between date and time, rows joined by ' . '): lines 15-18 (`ALEX SEGUIN 2026.09.07 18.02CDT`, `… . DANIEL VAIL …`), the NEW long-name/non-Latin assertions at 21-26 (`endsWith(" 2026.09.07 23.02UTC")`, `MAXIMILIAN ALEXANDER VON 2026.09.07 23.02UTC`, `SIGNER 1 2026.09.07 23.02UTC`, `ALIX.IO 2026.09.07 23.02UTC`, `SIGNER 2 …`) and line 37 (ALL strip messageForward). codexText now emits `2026.09.07_18.02CDT` and codexAllText joins with ' • ' (codex-pdf.ts:66-67,74) → test:codex-pdf fails on ~9 assertions. — `frontend/tests/codex-pdf.test.mjs` → Replace the space before HH.MM with `_` and ` . ` with ` • ` in every expected string (lines 15-18, 21-26, 37); fix the stale comment on line 14 ('space for _').
- **minutes** · scripts/sign-live-run.mjs steps at :299 and :317 match the ALL strip with `\d\d\.\d\d \d\d\.\d\d[A-Z]{2,5} \. DANIEL VAIL` — the old space + ' . ' form; both two-phone steps will go red against the new `_` and ` • ` output. — `frontend/scripts/sign-live-run.mjs` → Change both regexes to `\d\d\.\d\d_\d\d\.\d\d[A-Z]{2,5} • DANIEL VAIL` (and the same for the Light Codex page check at :317).
- **hour** · C2 has no live proof: sign-live-run.mjs:302 only renders signed-codex-p1.png; no step asserts Alex's initials at the right edge and Daniel's to the left (the plan's acceptance for C2). — `frontend/scripts/sign-live-run.mjs` → After the initialledBy/holders step (:283-284), read SoIInitW widths from the keywords, compute initialsSlot(612, top, 0, ws) and initialsSlot(612, top, 1, ws) via the lib, assert slot1.x + slot1.w + gap === slot0.x and slot0.x + slot0.w === 612 − INIT_SLOT.right; or sample the crop PNG for ink columns (right cluster = Alex, left cluster = Daniel).
- **minutes** · The contact fallback never fires in the app: sign-flow.tsx:372 calls stampSignature without `contact` or `signerIdx`, and allRows handed to codexAllText (:380-381,402) carry no `contact`. A non-Latin signer is captioned `Signer N` (N from the SoISig keyword count, pdf-stamp.ts:96) even when the roster has their e-mail; in an offline hand-off myRow ≠ meIdx can mis-number the caption against the codex row. — `frontend/components/sign/sign-flow.tsx` → Pass `{ contact: (countersign ? undefined : signers[meIdx]?.contact), signerIdx: myRow }` to stampSignature (compute myRow before the stamp) and add `contact` to this signer's entry in allRows so caption and hidden line share one rule.
- **minutes** · codex-strip.ts shim is still the import path in sign-flow.tsx:32 (plan A5 says delete if only tests import it — it is not tests that import it). — `frontend/lib/codex-strip.ts` → Point sign-flow.tsx:32 at `@/lib/codex-pdf` and delete lib/codex-strip.ts; or leave the shim and drop A5's deletion line from the plan.
- **minutes** · Light Codex page hint still advertises the old alphabet: components/light-codex-cube.tsx:187 'A–Z, digits, space, period.' — the three new symbols - _ • are accepted (light-codex.ts:29-34) but not named to the user. — `frontend/components/light-codex-cube.tsx` → Update the hint to 'A–Z, digits, space, period, hyphen, underscore, bullet' (t() key + ES).

### Athena — ready-with-fixes (111 words)

The doors changed and I checked each hinge. ship.sh now gates on test:ci, envcheck refuses SIGN_NO_AUTH, the innovation test reads the deck at SoI 2525. But the tree is red: codexText now writes an underscore and joins with a bullet, while the codex tests and two live run regexes still expect a space and a period. Commit that and test:ci fails on the first door. The colon symbol is unwritten, its ask unpersisted, the page hint unchanged. ship.sh never calls envcheck, so the refusal guards only GitHub. Pinch, double tap and nudges have no proof. Fix the assertions first, then the colon, then envcheck in ship.sh. Ship after green, not before.

Verified in the code:
- ship.sh gates on test:ci (applied, committed c66c127): frontend/scripts/ship.sh:36-37 runs `npm run --silent test:ci || fail`; test:ci at frontend/package.json:87 lists lint:gate + every Sign Doc suite (sign, sign-store, sign-verify, sign-fit, sign-layout, codex-pdf, sign-i18n, pdf-stamp, tmp-core, notify-core once — the duplicate is gone) + innovation-time + guard + embed-webc + sim-parity.
- envcheck refuses NEXT_PUBLIC_SIGN_NO_AUTH=1 (applied, c66c127): frontend/scripts/envcheck.mjs:33-36 exits 1 unless --warn; deploy.yml:64-74 runs it before Build (:80-94). Read site: app/soi-session/sign/page.tsx:28 `AUTH_OFF = !AUTH0_DOMAIN || !AUTH0_CLIENT_ID || process.env.NEXT_PUBLIC_SIGN_NO_AUTH === "1"`. The last exported bundle out/_next/static/chunks/app/soi-session/sign/page-f66b7640325c2df1.js still contains the runtime read `"1"===D.env.NEXT_PUBLIC_SIGN_NO_AUTH` (not inlined), i.e. that build was made without the flag.
- innovation-time at its real home (applied, c66c127): frontend/tests/innovation-time.test.mjs reads app/SoI-2525/page.tsx in every fs.readFile (20 replacements in the c66c127 diff); the suite prints `INNOVATION-TIME ${pass}/${pass+fail}` at :6430. The 3680/3680 figure is the plan's claim; not re-run here (read-only).
- /api/notify behind the pause switch + duplicate notify-core entry removed (c66c127 touches worker.js +1 and package.json).
- Zoom applied in tree (uncommitted): components/sign/pdf-page-view.tsx:69 re-renders at `base*zoom` via renderPage; :182-185 zoom-out / zoom-reset (shows `${Math.round(zoom*100)}%`) / zoom-in (×1.5, cap 4) / snap-line (⌖) with data-testids and t() aria-labels; :111-122 pinch + double-tap 1×↔2.5×; :132 snapSelected reuses fitAt; :138 arrow nudges 0.0025; :189 scroller `max-h-[72vh] overflow-auto` data-zoom, touchAction pan-x pan-y at z>1.
- Live proof extended with zoom/snap (uncommitted): scripts/sign-live-run.mjs:158-174 adds three steps in placeAndSign (runs for alex AND dan): zoom + → page 1.5× wide, sig-box fractions unchanged (<0.01), data-zoom '1.5'; nudge the date 9/11 px then ⌖ → bottom back on the rule within 3 px, data-fit 'underline'; zoom reset → width back to w0, label '100%'. step() count 57 → 60 (HEAD vs tree).
- Light Codex symbols - _ • applied: lib/light-codex.ts diff adds `"-": "VCVC", "_": "VRVR", "•": "VYVY"` (BRYCVW: Violet Cyan Violet Cyan · Violet Red Violet Red · Violet Yellow Violet Yellow).
- C2 initials slot reversed (applied): lib/pdf-stamp.ts:222-223 `x = width − INIT_SLOT.right − Σ widths[0..idx] − idx·gap` (signer 0 at the right edge); tests/pdf-stamp.test.mjs:73-76 cover 3 equal slots and a wide first slot.

Gaps:
- **minutes** · test:ci is RED in the tree as it stands: codexText now emits `YYYY.MM.DD_HH.MMZZZ` (lib/codex-pdf.ts:65-66, underscore) and codexAllText joins with ' • ' (lib/codex-pdf.ts:74), but tests/codex-pdf.test.mjs:17,18,21,22,24,25,26,37 still assert the old form `ALEX SEGUIN 2026.09.07 23.02UTC` and the ' . ' joiner. The plan's '26/26' predates the underscore/bullet change. First door (ship.sh:37, deploy.yml:62) fails on commit. — `frontend/tests/codex-pdf.test.mjs` → Update the eight literals to `2026.09.07_23.02UTC` / `2026.09.07_18.02CDT` and the joiner to ' • ' (line 18 and line 37); keep the 44-char and trailing-space cases. If the ':' symbol lands, change `.` between hour and minute to `:` in the same edit so the tests move once.
- **minutes** · Two live-run steps carry the stale codex regex: scripts/sign-live-run.mjs:297 (`… \d\d\.\d\d[A-Z]{2,5} \. DANIEL VAIL …` expects space + ' . ') and :313 (Light Codex page decode, same regex). Both will FAIL against ' • ' and '_'. The 60-step two-phone proof cannot reach the initials/render assertions after them. — `frontend/scripts/sign-live-run.mjs` → Change both regexes to `/^ALEX SEGUIN \d{4}\.\d\d\.\d\d_\d\d[.:]\d\d[A-Z]{2,5} • DANIEL VAIL \d{4}\.\d\d\.\d\d_\d\d[.:]\d\d[A-Z]{2,5}$/` (the [.:] class survives the ':' addition). Re-run test:soi-sign-live and record the new step count (60+) in the live-run doc.
- **minutes** · ':' Light Codex symbol (17:1x ask, plan: V B V B) is NOT applied — lib/light-codex.ts diff has only -, _, •; codex-pdf.ts:53 still says 'the full stop stands in for the colon'. The ask is not persisted either: docs/asks/ ends at 2026-09-08_1721_… (no ':' ask file). PERSIST FIRST rule. — `frontend/lib/light-codex.ts` → Write docs/asks/2026-09-08_171x_colon_symbol.md and commit alone; add `":": "VBVB"` to the SYMBOL map; switch the stamp to `${hour}:${minute}` in codexText (lib/codex-pdf.ts:65-66); update the header comment.
- **minutes** · Light Codex page hint unchanged: components/light-codex-cube.tsx:187 still reads 'A–Z, digits, space, period.' (plan: gains '- _ • :'); file is not in git status. — `frontend/components/light-codex-cube.tsx` → Change the hint string to 'A–Z, digits, space, period, - _ • :' (through t() if the page is lexicon-covered).
- **minutes** · Symbol uniqueness is proven only by a one-off script (plan text), not by a gated test: no file under tests/ imports lib/light-codex (only scripts/sign-live-run.mjs does). A future symbol that collides with a letter, digit, frame green, or a reversal passes every door. — `frontend/tests/codex-pdf.test.mjs` → Add tests/light-codex.test.mjs (or a block in codex-pdf.test.mjs): build the set of every 4-token group across ALPHA + NUMBERS + TRANSMISSION + symbols, assert size === count (no duplicates), assert no symbol group contains G, assert no symbol's reversal is a taken group; wire into test:ci in package.json:87.
- **minutes** · ship.sh never runs envcheck: scripts/ship.sh:29-43 gates tsc, test:ci, build, then wrangler deploy (:117) — the NEXT_PUBLIC_SIGN_NO_AUTH refusal (envcheck.mjs:33) and the required-vars check protect only deploy.yml:74. A local `ship.sh` with the flag in the shell (the live-run recipe exports it, sign-live-run.mjs:15) builds and promotes a login-off creator path. — `frontend/scripts/ship.sh` → After the tsc step (:34) add `step "node scripts/envcheck.mjs"; node scripts/envcheck.mjs || fail "envcheck red — not shipping"` outside the SKIP_TESTS guard so SKIP_TESTS=1 cannot bypass it.
- **minutes** · deploy.yml:76-77 still re-runs `npm run test:innovation-time` as a separate step after test:ci already ran it (:62) — the duplicate Athena pass-2 asked to delete; doubles the slowest suite on every deploy. — `.github/workflows/deploy.yml` → Delete lines 76-77 ('- name: Test / run: npm run test:innovation-time').
- **hour** · Zoom proof covers the + button, drag-at-zoom, ⌖ snap and reset only (sign-live-run.mjs:158-174). Ungated: pinch (pdf-page-view.tsx:111-115), double-tap 1×↔2.5× (:120-122), arrow nudges (:138), the − button (:182), and that a zoom > 1 does not scroll the page during a touch drag. Both phone contexts already have hasTouch:true (:208). — `frontend/scripts/sign-live-run.mjs` → Add for 'dan' only: two `p.touchscreen.tap(x,y)` within 350 ms on empty paper → data-zoom '2.5'; `host.focus()` + `keyboard.press('ArrowUp')` twice with the date selected → style.top decreased by 0.5 % and data-fit cleared; zoom-out click → '1'. Assert sig-box fractions unchanged after each.
- **hour** · What stays ungated at every door: the four live proofs (test:soi-sign-live / offline / scan / pod-live, package.json:49-52) need relay + dev server + Chromium and no workflow runs them; the Cloudflare git build path runs no suite (prebuild package.json:9 has none); verify-live.yml checks only the footer SHA, never the hosted 036 RPC. Plan lists 036 as operator-owned, but the probe is repo work. — `.github/workflows/verify-live.yml` → Add .github/workflows/sign-proofs.yml (workflow_dispatch + nightly cron): npm ci, playwright chromium, start relay + `next dev -p 3210` with the local env, run the four scripts, upload OUT as artifact. Add to verify-live.yml after the SHA match a curl to `$NEXT_PUBLIC_SUPABASE_URL/rest/v1/rpc/sign_envelope_get` failing on PGRST202.
- **minutes** · Plan verification line expects `test:soi-sign-live 57+/0`; the tree has 60 step() calls and the last recorded run doc says 55 (docs/assessments/2026-09-07_sign_doc_two_phone_live_run.md:177). After the regex fix the number should be recorded exactly, and the new screenshots '2c-zoom-alex.jpg' / '2c-zoom-dan.jpg' plus signed-codex-p1.png (Alex right, Daniel left, C2) attached. — `docs/assessments/2026-09-07_sign_doc_two_phone_live_run.md` → After the green re-run append an 'Outcome — N steps, 0 failures (2026-09-08 zoom + snap + reversed initials)' section with the three new step lines and the screenshots.

### Christo — ready-with-fixes (111 words)

The recipient of a link now signs without a login, and the roster refreshes itself while a phone waits. Both are truly in the tree. Yet the promise is half kept. The Check again button has a name in the lexicon and no place on the page. A wrong secret still reads every name on the roster. A failed save still drops the countersigner on Place while the red line speaks of saving. The error state still lights no step and says nothing. A carried file fills its rows, then demands a contact for the signer who already signed. Six fixes, each a few lines. Finish them before the fleet returns.

Verified in the code:
- B9 ?f= link without login — APPLIED (uncommitted): frontend/app/soi-session/sign/page.tsx:63 `fileLink` state, :75-79 token read + once-per-token seed guard (`wasSeeded`/`markSeeded` :53-57, sessionStorage `exel-sign-seeded:<token>`), :88 `requireLogin={!AUTH_OFF && !fileLink}`; frontend/components/sign/sign-flow.tsx:91-92 `handCarried`/`needLogin`, :140-141 authState from needLogin, :354-355 sign() login gates use needLogin; seed-once-over-draft :265-266 (`seededRef` + `readDraft()?.files.length` check). `X-File-Name` encoded frontend/lib/tmpfile.ts:5-6, decoded frontend/tmp-core.js:37-39, test rows added tests/tmp-core.test.mjs (+6).
- B16 waiting refresh — APPLIED in logic: frontend/components/sign/sign-flow.tsx:200-216 `refreshEnvelope` (getEnvelope → done / place with preplaced marks, guarded by `checking`), :217-223 effect while step==='waiting': visibilitychange + 20 s interval, only when document visible.
- B11 hand-off primary by contact kind — APPLIED: frontend/components/sign/handoff.tsx:100-104 `mailFirst = kind==='email'`, PRIMARY/SECONDARY class swap :113-119; e-mail contact → the Send-e-mail button is the filled primary, phone/unknown → text is primary. Cyan → `primary` tokens :108-109.
- B14 seeded title + rows — APPLIED: frontend/components/sign/sign-flow.tsx:269-304 effect reads codexRows + holders of file 0, title from PDF metadata else file name (strips -partly-signed-XX) :281-283, rows = recorded names + next holder's name at `nextIdx` (defaultName/defaultContact fallback) :289-297, `carriedIdx` :298; `myName`/`meIdx` follow the carried row :346-347; stamping uses meIdx :374,:380.
- B1 error step (partial) — lexicon keys `soi.sign.step.error` + `soi.sign.x.error` (+ES) frontend/lib/lexicon-data.ts:1027-1028, frontend/lib/lexicon-translations-es-sign.ts:170-171; sign-diag.tsx:132 `stage?` on DiagInput, :135 `railOf`, :144 error row names the failed phase; sign-flow.tsx:52 `railOf` helper, :95 `failStage`, :96 `lastRail`, open-failures set failStage :191,:193.
- B12b auth.isLoading guard (partial) — sign-flow.tsx:354 sign() speaks `soi.sign.err.auth_loading` when needLogin && isLoading.
- B8 removeFile clamps fileIdx + clears selection — sign-flow.tsx:309-311.
- B2 dates follow the Globe — `dateIn(activeLocale…)` sign-flow.tsx:54-58, `todayText` :345.

Gaps:
- **minutes** · B1 error step still lights no rail step and the explainer goes blank: the rail memo (sign-flow.tsx:493-497) still uses the old inline mapping (error → no `on`), `railOf` (:52) and `lastRail` (:96) are declared but never read; `explain` (:498-512) has no `case "error"`; `SignDiag` call (:543) does not pass `stage: failStage`, so the new diag row never names the phase. — `frontend/components/sign/sign-flow.tsx` → rail memo: `const r = railOf(step); if (r) lastRail.current = r; const cur = r ?? lastRail.current;` — add `case "error": return err ? "" : t("soi.sign.x.error")` (or always the x.error sentence) — pass `stage: failStage` in the SignDiag `d` prop.
- **minutes** · Failed save still sends a countersigner to PLACE while the red line says the save failed (fleet 'still true'): catch at :456-460 keeps `setStep(countersign ? "place" : "draw")` and never calls `setFailStage(stage)`. — `frontend/components/sign/sign-flow.tsx` → `setFailStage(stage); setStep("draw")` for both paths (png, initialsPng and marks are kept, so the red line, the sentence and the Sign & save button share one screen).
- **minutes** · 'Check again' button is not rendered: key `soi.sign.check_again` exists (lexicon-data.ts:1037, es-sign.ts:180) but nothing uses it — :702 renders only `<Roster />` for waiting; no data-testid, no live-run step. The creator's hand-off screen (step 'handoff', supabase mode) never flips to done either: the refresh effect (:217-223) only runs while step==='waiting'. — `frontend/components/sign/sign-flow.tsx` → Under the Roster in waiting: `<button type="button" onClick={() => void refreshEnvelope()} disabled={checking} className="mt-2 min-h-[44px] …" data-testid="check-again">↻ {checking ? t("soi.sign.loading") : t("soi.sign.check_again")}</button>`; widen the effect to `step === "waiting" || (step === "handoff" && pub?.mode !== "local")` and on status complete from handoff set signed from result files; drop `checking` from the useCallback deps (use a ref) so the interval is not re-armed on every toggle; add one live-run step (roster refresh after the other phone signs).
- **minutes** · not_party privacy not applied: :702 `(step === "waiting" || step === "not_party") && <Roster />` and :723 `step === "error" && <Roster />` still show every name + masked contact to a wrong-secret reader. — `frontend/components/sign/sign-flow.tsx` → Render Roster only when `pub && pub.party >= 0` (guard inside the Roster component) and drop `not_party` from :702.
- **minutes** · auth.isLoading: the Sign & save button (:653) is still silently disabled with `!!requireLogin && auth.isLoading` (also the old prop, not `needLogin`), so the `auth_loading` sentence at :354 is unreachable by tap. — `frontend/components/sign/sign-flow.tsx` → Use `needLogin && auth.isLoading`; either keep the button enabled and let :354 speak, or render `<span data-testid="auth-loading">{t("soi.sign.err.auth_loading")}</span>` beside the button while it hydrates.
- **minutes** · ?f= recipient still sees the creator's login hint: :557 `requireLogin && !loggedIn` shows 'log in later / log in now' on the upload step even when `fileLink` made login unnecessary (loggedIn is false because needLogin is false) — contradicts the 'no account' promise; tapping `login` (:145) redirects to Auth0. — `frontend/components/sign/sign-flow.tsx` → Change :557 to `needLogin && !loggedIn`.
- **minutes** · Seeded rows block the Signers step: the prefill (:289-297) gives recorded signers `contact: ""`, but `signersOk` (:317) demands a valid contact for EVERY row, so a ?f= / hand-carried recipient must type the first signer's contact to reach Place; and B13 is not applied — :593 still disables Next with `(multi && mode === "local")`, so on a build without Supabase the 2-row carried file cannot proceed at all (:589 amber hint stays). No 'you are signer N of N, you sign last' hint. — `frontend/components/sign/sign-flow.tsx` → `signersOk = signers.every((s, i) => s.name.trim() && (recordedRows.has(i) || contactKind(s.contact)))` (keep a `recordedRows` set from the prefill effect; render those rows read-only); drop the `(multi && mode === "local")` disable at :593 (offline hand-off exists) and keep the amber hint; add a short hint key `soi.sign.x.carried` (+ES).
- **minutes** · Hand-off: DOM order is still SMS first (`flex-row-reverse` only re-orders visually, so tab/screen-reader order contradicts the primary); when `nextContact` is empty the primary is a blank `sms:` anchor instead of Copy/QR; `window.location.href = mail` (handoff.tsx:27, sign-flow.tsx:476) still navigates the page away on desktop. — `frontend/components/sign/handoff.tsx` → Render the buttons in an array ordered by kind (email → [mail, sms, copy]; phone → [sms, mail, copy]; none → [copy, sms, mail]) instead of flex-row-reverse; open mailto via an anchor click / `window.open(mail, "_blank")`.
- **hour** · No test or live-run coverage for the lens items: tests/*.test.mjs and scripts/sign-live-run.mjs contain no `check-again`, `step.error`, `not_party`, or ?f=-without-login (auth on) step; the fleet explicitly asked for an offline-run step with auth on. — `frontend/scripts/sign-live-run.mjs` → Add to sign-offline-run / sign-live-run: open `?f=<token>` with SIGN_NO_AUTH unset and assert no `login-redirect` testid appears and `diag-auth` reads 'bypassed'; wrong-secret open asserts `roster` absent; failed-save mock asserts step chip 'Sign' is aria-current.
- **minutes** · Adjacent flow items partly owed: B3 twelve hard-coded `cyan` classes remain in sign-flow.tsx (:519-520 roster, :540 explain, :552, :557, :578, :588, :616, :645, :686) and pdf-page-view.tsx:194,:198; B4 `AI_GLYPH` is imported (:19) but the AI button (:626) still prints ◬; B2 the 24-h link expiry (:674) uses `toLocaleString()` not `dateIn(activeLocale, …, true)`. — `frontend/components/sign/sign-flow.tsx` → Replace cyan classes with `text-primary` / `border-primary/40` / `bg-primary/5`; `{AI_GLYPH} ` at :626; `dateIn(activeLocale, new Date(tmpLink.expires), { dateStyle: "medium", timeStyle: "short" }, true)` at :674.

### Enki — ready-with-fixes (111 words)

I am Enki, and I dig where the water hides. The zoom holds: pinch, double tap, minus, plus, reset, scroller, snap and arrow nudges live in the page view, and the live run proves plus, snap and reset. The upload knockout holds. The removeFile clamp holds. Three things leak. clampBox grows a short box downward off its rule; one line pinning the bottom fixes it. The text mark never reads textH, so the document size the operator asked for stops in the fitter and never reaches the page. At four times zoom the canvas passes the iOS pixel ceiling and renders blank. Cap the pixels, wire textH, pin the baseline, ship.

Verified in the code:
- ZOOM applied (uncommitted diff, frontend/components/sign/pdf-page-view.tsx): zoom state 1..4 clamped in setZoom (:72-89), page re-rendered at base×zoom via renderPage (:66-75), scroller `max-h-[72vh] overflow-auto` with touch-action pan-y / pan-x pan-y at z>1 (:189), host width = base×zoom so marks (percent fractions) stay aligned (:190,:195).
- Controls applied: 44-px − / NN% reset / + buttons with t('soi.sign.zoom_out|zoom_reset|zoom_in') (:182-184), disabled at 1 and 4; ⌖ snap button disabled without a selection (:185). Lexicon keys present EN (lib/lexicon-data.ts:1038-1042) and ES (lib/lexicon-translations-es-sign.ts:181-185).
- Pinch applied: two-touch start records distance + midpoint fraction (:111-114), move rescales about the pinch origin (:115), end clears pinch and suppresses swipe (:117). Double-tap applied: two taps <350 ms, <30 px apart, not on a mark, toggle 1× ↔ 2.5× under the finger (:120-123). Page-turn swipe disabled while zoomed (:125).
- Snap-to-line applied (:129-135): fitAt at the mark's bottom-centre then centre; sig takes the rule box, text keeps its height with bottom on the rule and x centred. Arrow nudges applied (:136-140): 0.0025 step, host tabIndex=0, preventDefault only when a mark is selected.
- Live-run steps applied (frontend/scripts/sign-live-run.mjs:158-174): zoom + → page 1.5× wide, sig fractions unchanged, data-zoom='1.5'; drag date off then ⌖ restores bottom within 3 px and data-fit='underline'; reset returns 100%.
- textH applied in lib/sign-fit.ts:9-15,72-84: label LEFT of the rule (x0−25% W to x0−2, rows cap..lineY+3) else contiguous text line above; tests/sign-fit.test.mjs:49-61 cover label, fallback, bare rule.
- Default TXT_H 0.02 and MIN_H 0.012 applied (pdf-page-view.tsx:24); addText caps a fitted text mark at TXT_H with bottom on the rule (sign-flow.tsx:333-334); date holder capped the same way (sign-flow.tsx:415-416).
- JPEG/opaque knockout applied (signature-pad.tsx:17-33): luma ≥ 235 → alpha 0 before the bbox, putImageData before the crop, plain FileReader data URL replaces signedDataURL so no helix pixels enter the ink (:104-108).

Gaps:
- **minutes** · Plan B5 NOT applied: clampBox still grows a box shorter than MIN_H DOWNWARD (h = max(h, MIN_H), y kept) so a tight fitted box or a nudged one leaves its rule; the AI box uses the same Math.max(0.02, b.h) with y kept — `frontend/components/sign/pdf-page-view.tsx:92 and frontend/components/sign/sign-flow.tsx:121` → In clampBox: if (m.h < MIN_H) { y = (m.y + m.h) − MIN_H; h = MIN_H } before the 0..1 clamp (bottom pinned); same at sign-flow.tsx:121 (y: b.y + b.h − max(0.02, b.h)). Add a clamp test asserting the bottom is unchanged.
- **hour** · Text mark sized to the document NOT wired: sign-flow never reads FitBox.textH — grep textH in sign-flow.tsx returns nothing; addText (:333) and the date holder (:415) only cap at TXT_H 0.02, so a 9-pt form and a 14-pt form both print at 16 pt — `frontend/components/sign/sign-flow.tsx:333-334,415-416` → h = f.textH ? clamp(f.textH × 1.9, 0.012, 0.03) : Math.min(f.h, TXT_H); y = f.y + f.h − h. stampText sizes the font at 0.72 × box height (lib/pdf-stamp.ts:134), so 1.9 × cap-height ≈ 1.37 em; keep the factor and add a live-run assertion that the stamped date height ≈ the 'Date:' label height ± 25 %.
- **minutes** · 4× zoom canvas exceeds mobile canvas limits: renderPage multiplies css width by dpr 2..3 (lib/pdf-render.ts:35-39); at base 343 × 4 × 3 = 4116 × ~5327 px ≈ 21.9 MP (> the 16.7 MP iOS ceiling, blank canvas) and fitAt's getImageData at that size is ~88 MB per snap/tap — `frontend/lib/pdf-render.ts:35 and frontend/components/sign/pdf-page-view.tsx:66,100-102` → Cap the render: dpr = min(dpr, 4096 / (cssWidth)) or cap total pixels at ~12 MP (scale = min(scale×dpr, sqrt(12e6 / (base.width×base.height)))); expose the effective scale on RenderedPage so fitAt still maps fractions correctly.
- **hour** · fitToUnderline pixel constants are not scale-free although the plan claims they are: dashed-rule bridging gap ≤ 3 px (sign-fit.ts:37,39), probe offsets ±4/±12 px (:49), min heights 4 px (:71,:80,:81), rule-top walk; at 4× zoom a dashed rule's gaps are 4× wider and the bridge fails, at 1× on a 343-px phone a 10-pt label is ~5 px tall — `frontend/lib/sign-fit.ts:37,39,49,71,80-81` → Derive a unit u = max(1, round(W / 600)) and scale gap (3u), probes (4u, 12u) and the 4-px minimums (4u); add a test rendering the same page at 2× width and asserting equal fractions.
- **hour** · textH label scan takes ANY ink left of the rule in a band 12 % of the page tall down to lineY+3 (sign-fit.ts:79-81): a neighbouring left rule ('Signature ___ Date ___') or two label lines return a multi-line height, and the rule's own rows push the bottom below the label baseline; the clamp (0.03) hides the overestimate as the max size instead of the document size — `frontend/lib/sign-fit.ts:79-81` → Scan the label band upward from lineY−1 like the fallback does: find the first ink row, walk up while contiguous, stop at the first blank row; exclude rows ≥ lineY (the rule itself). Add a test with a left rule and a two-line label.
- **minutes** · Snap uses the CURRENT page's canvas even when the selected mark sits on another page (marks are found by id only) — `frontend/components/sign/pdf-page-view.tsx:131-132` → Guard `if (m.page !== page) return;` in snapSelected and onKey (nudging an off-page mark is invisible).
- **hour** · Clear after a draft resume still ignored: pad onChange drops null while `resumed` is true (guard against the pad's mount-time null), so the user's Clear cannot empty png and Sign & save stays enabled (pass-2 item 77, second half) — `frontend/components/sign/sign-flow.tsx:646,649` → Make SignaturePad emit null only on a user Clear (not on mount), then `onChange={(p) => { setPng(p); if (p === null) setResumed(false); }}`; add the live-run step: reload at draw, Clear, expect Sign & save disabled.
- **minutes** · Knockout threshold gap: pixels with luma 200..234 (JPEG ringing, grey paper) are neither knocked out (≥235) nor ink (mean < 200) and stay opaque light grey — a faint slab around the stroke — `frontend/components/sign/signature-pad.tsx:23-26` → Knock out everything that is not ink (alpha 0 when mean ≥ 200, graded alpha 200→235 optional) so the two thresholds agree; add a JPEG-on-grey fixture asserting transparent corners.
- **minutes** · setZoom sets scrollLeft/Top in a requestAnimationFrame after setState; on a continuous touchmove the host width may not yet be committed when the rAF fires, so scrollLeft clamps to the old extent and the pinch origin drifts — `frontend/components/sign/pdf-page-view.tsx:72-89` → Store the pending scroll target in a ref and apply it in a useLayoutEffect keyed on zoom (after the width commit); drop the rAF.
- **minutes** · resizeSel's minimum height 0.02 disagrees with MIN_H 0.012: a 0.012-tall document-sized text mark grows to 0.02 on the first − press — `frontend/components/sign/sign-flow.tsx:343` → Use MIN_W/MIN_H imported from pdf-page-view in resizeSel instead of the literals 0.08 / 0.02.
- **hour** · Pinch and double-tap have no automated proof (only + / snap / reset are in the live run) and MIN_H/textH have no clamp test — `frontend/scripts/sign-live-run.mjs:158-174` → Add a touchscreen double-tap step (expect data-zoom='2.5' then '1') and a CDP Input.dispatchTouchEvent two-finger pinch step asserting data-zoom > 1 and sig fractions unchanged.

### Enlil — ready-with-fixes (111 words)

I build what the plan names and I count what the tree holds. The types stand: SignStep is a literal union, TextMark carries its text, VerifyReport takes initials and holders as optional fields. The unused Helvetica embed is gone. One LanguageGlobe serves every page. Five things remain owed. The codex test still asserts the old space and dot format while the library now prints underscore and bullet, so the gate is red until ten lines change. AI_GLYPH is imported yet the button still shows the record glyph. The colon symbol and the codex page hint are absent. textH is computed and never read. The codex strip shim still feeds sign flow.

Verified in the code:
- SignStep typed union + glyph law comment + AI_GLYPH export: APPLIED — frontend/lib/sign-steps.ts:18-34 (SIGN_STEP_KEYS as const, SignStep, Record<SignStep,SignStepDef>, readonly CREATOR_STEPS/COUNTERSIGN_STEPS); consumed in components/sign/sign-flow.tsx:19,52,96 (railOf returns SignStep|null, lastRail ref keeps the last real step lit on error)
- TextMark type + SoITxt :t<b64url> round-trip: APPLIED — frontend/lib/pdf-stamp.ts:143 (keyword carries :t), :148 (export interface TextMark extends StampBox {text?,signerIdx?,isoDate?,chain?}), :150-159 (regex parse, b64uDecode); tests/pdf-stamp.test.mjs asserts tm.text / tb.text+pass binding
- VerifyReport optional fields initials?/holders? + rows[].name?: APPLIED — frontend/lib/sign-verify.ts:12-16,27,42 (initialledBy/holders imported from pdf-stamp :7, Promise.all widened); pdf-stamp.ts:298 initialledBy, :321 holders exist
- Unused Helvetica embed in stampHolders REMOVED — git diff frontend/lib/pdf-stamp.ts:-311 (`const font = await doc.embedFont(...)` deleted); remaining StandardFonts uses at :93,:130,:249 are all consumed
- countSignatureImages de-dupes by PDFRef — frontend/lib/pdf-stamp.ts:174-183 (Set of ref.toString(), inline dicts keyed per page+name); PDFRef import added :8
- initialsSlot reversed (signer 0 rightmost) + initialsRowFrac — frontend/lib/pdf-stamp.ts:223,:227-230; tests/pdf-stamp.test.mjs asserts 558/518/478 and 0.45 floor / 418/612 / cap 1; sign-layout.ts:9-12 doc updated
- captionName + pdfSafe moved to lib/codex-pdf.ts:23-46 and re-exported from pdf-stamp.ts:64 (no circular import: pdf-stamp imports codex-pdf only); codexText never cuts the stamp (CODEX_LINE_MAX 44, codex-pdf.ts:49-65)
- Hidden helix 0.25 pt — frontend/lib/pdf-stamp.ts:276

Gaps:
- **minutes** · RED GATE: tests/codex-pdf.test.mjs still asserts the OLD codex format (space between date and time, ' . ' joiner) at lines 15,16,17,18,21,22,24,25,26,37 while lib/codex-pdf.ts now emits '2026.09.07_18.02CDT' and joins with ' • ' (codex-pdf.ts:57,61,65,72). test:codex-pdf (and thus test:ci / ship.sh) fails on this tree. — `frontend/tests/codex-pdf.test.mjs` → Replace the ten expected strings with the underscore form (…2026.09.07_18.02CDT) and the ' • ' joiner; keep the long-name assertion endsWith(' 2026.09.07_23.02UTC').
- **minutes** · B4 half-applied: AI_GLYPH is imported (sign-flow.tsx:19) but never used; the 'AI: find my line' button still prints ◬ (sign-flow.tsx:626), so ◬ still carries two meanings and next lint reports an unused import. — `frontend/components/sign/sign-flow.tsx` → At :626 change <span aria-hidden="true">◬ </span> to <span aria-hidden="true">{AI_GLYPH} </span>.
- **minutes** · Operator ask ':' = V B V B (Violet Black Violet Black) not in the alphabet; grep '":"' lib/light-codex.ts is empty. codexText still writes the full stop for the colon (codex-pdf.ts:52-53 comment, :57). — `frontend/lib/light-codex.ts` → Add ":": "VBVB" beside :30 (unused by ALPHA/NUMBERS/frame, reversal BVBV unused); then codexText emits `${hour}:${minute}` and the tests expect 2026.09.07_18:02CDT. Persist the 17:1x ask to docs/asks/ first.
- **minutes** · Light Codex page hint still reads 'A–Z, digits, space, period.' — the new symbols are undiscoverable. — `frontend/components/light-codex-cube.tsx` → Line 187: extend the hint to 'A–Z, digits, space, period, - _ • :' (through t() if the string is lexicon-backed).
- **hour** · 'Text marks at the document's size' half-applied: fitToUnderline returns textH but no caller reads it (grep textH → only lib/sign-fit.ts). sign-flow.tsx:235-236 caps the fitted text mark at TXT_H and :315-316 caps the date holder the same way, so the mark is a fixed 16 pt, not clamp(textH×1.9, 0.012, 0.03) as the plan states. — `frontend/components/sign/sign-flow.tsx` → In addText (:235) and the date holder (:315): const h = f.textH ? Math.min(0.03, Math.max(MIN_H, f.textH * 1.9)) : TXT_H; bottom stays on the rule (y = f.y + f.h − h). Add one sign-fit/live assertion.
- **minutes** · Dead indirection: sign-flow.tsx:24,26,44-46 import * as stampLib / layoutLib only to probe optional initialsRowFrac / initialsSlotWidths via casts, although both are real exports of lib/pdf-stamp (:223,:227). Two namespace imports and two Maybe-typed consts for a certainty. — `frontend/components/sign/sign-flow.tsx` → import { initialsRowFrac, initialsSlotWidths } from "@/lib/pdf-stamp"; delete RowFracFn/rowFracOf/slotWidthsOf and the `stampLib`/`layoutLib` namespace imports; simplify :389-393 to initialsSlotTop(bmp, { colFrac: initialsRowFrac(initialsSlotWidths(total), bmp.widthPt || 612) }).
- **minutes** · A5 condition not met: lib/codex-strip.ts (2-line re-export shim) is imported by production code — sign-flow.tsx:32 — not only by tests, so it cannot simply be deleted; the plan's 'delete if only tests import it' resolves to KEEP unless the import moves. — `frontend/lib/codex-strip.ts` → Point sign-flow.tsx:32 at "@/lib/codex-pdf" (codexAllText, codexImage are exported there), then delete the shim and any test import of it.
- **minutes** · Duplicate railOf: sign-diag.tsx (new, string→string, 'error' passthrough) and sign-flow.tsx:52 (string→SignStep|null) implement the same mapping with different results for 'error'. — `frontend/components/sign/sign-diag.tsx` → Export one railOf(step): SignStep | null from lib/sign-steps.ts and import it in both components.
- **minutes** · Misplaced doc comment: the JSDoc 'distinct SoISig images (by object reference…)' sits above `name: string; sha256: string; images…` and so documents `name`, not `images`. — `frontend/lib/sign-verify.ts` → Line 10-11: split the field line so the comment precedes `images: number` alone.
- **hour** · Scratch-copy build before push is a process step the plan names but nothing enforces: scripts/ship.sh:40-42 builds in place (no cp/rsync of the tree), and lint:gate (package.json:59) runs with --max-warnings=9999 so unused imports (AI_GLYPH) never fail the gate. — `frontend/scripts/ship.sh` → Either accept in-place build (document it) or add a `git worktree add /tmp/ship-<sha> HEAD && npm ci && npm run build` step; separately consider a strict `npx next lint --max-warnings=0 components/sign lib/sign-*.ts` line in test:ci for the Sign Doc files only.

### Krishna — ready-with-fixes (111 words)

I bind the parts and watch the joins. The alphabet grew by three groups and both decoders read them through one map, so the PNG path and the PDF path agree without a second table. The colon is still owed, with its ask unpersisted. The codex test file still expects spaces and dots, so the suite is red until it learns the underscore and the bullet. The file name crosses the header encoded and lands decoded, yet returns to the recipient mangled, because the GET never carries it back. The link recipient signs without Auth0 now. The notify route sits behind the pause. Migration 036 remains unproven on the hosted site.

Verified in the code:
- Light Codex symbols APPLIED: frontend/lib/light-codex.ts:30 adds "-":"VCVC", "_":"VRVR", "•":"VYVY" (BRYCVW: - = Violet Cyan Violet Cyan, _ = Violet Red Violet Red, • = Violet Yellow Violet Yellow). Collision proof re-run read-only over ALPHA+NUMBERS+TRANSMISSION+the proposed ':'=VBVB: 46 groups, 46 unique; reversals CVCV/RVRV/YVYV/BVBV all unused; no G in any of the four.
- Decoder integration is ONE map: GROUP_TO_CHAR is derived from ALPHA at light-codex.ts:45-47, used by parseLine (:168), readRightAligned (:217) and the Hidden Helix readRTL (:256); the PDF decoder decodeCodexPdf (lib/codex-pdf.ts:134-136) calls the same decodeImage over the raw RGB strip — so PNG decode and PDF decode both accept - _ • with no second table. unsupportedChars (light-codex.ts:67-75) keys on ALPHA too, so the Light Codex page input accepts them.
- codexText now writes the operator format: lib/codex-pdf.ts:65 `${day}_${hour}.${minute}${zone}`, codexClean, NAME trimmed to CODEX_LINE_MAX 44 minus stamp (:73-74), captionName fallback (:43-47) shared with the caption; codexAllText joins rows with ' • ' (:79-80). Hidden helix width fits: 3 signers = 138 chars × 4 px = 552 px < 612 (codexImage minWidth 612, codex-pdf.ts:80).
- X-File-Name encode/decode APPLIED: lib/tmpfile.ts:5-6 sends encodeURIComponent(name); tmp-core.js:37-39 decodeURIComponent in try/catch, plain names kept; tests/tmp-core.test.mjs:25-30 cover 'pagaré-firmado.pdf' stored decoded, ASCII-safe content-disposition on GET, plain name kept.
- ?f= recipient = offline path, no Auth0 at Sign & save APPLIED: app/soi-session/sign/page.tsx:62 fileLink state, :70-75 setFileLink(f), :62(diff) `requireLogin={!AUTH_OFF && !fileLink}`; components/sign/sign-flow.tsx:91-92 handCarried/needLogin, :354 login guard keyed on needLogin. Seed guard APPLIED: page.tsx:27-31 SEEDED_KEY per token in sessionStorage, :51-52 wasSeeded short-circuits the refetch; sign-flow.tsx seed effect (diff :163-166) seeds once per seed object and never over a restored draft.
- /api/notify behind the pause APPLIED and committed in c66c127 (unpushed): frontend/worker.js:60-61 `if (await isPaused(env)) return 503` mirroring /api/tmp at :55-56 and the money route at :50; isPaused fails open (:129-131).
- Supabase 036 RPCs vs local fallback: lib/sign-store.ts:67-83 createEnvelope falls to local on no_backend/no_migration (single signer always; multi only with localMulti); sign-flow.tsx:433 retries createEnvelope(env,{localMulti:true}) then putTempFile for the 24-h link; :435 duplicate-token re-mint (fleet Krishna item) present. Design consistent: a ?f= recipient re-enters as creator of a fresh envelope for the remaining signers, so with hosted 036 present they get a real link, without it the file travels on.
- verifySignedPdf APPLIED: lib/sign-verify.ts:12-16 rows carry name, initials/holders counts (:27, :42); pdf-stamp countSignatureImages dedupes SoISig by PDFRef (pdf-stamp.ts diff :152-154); tests/sign-verify.test.mjs:27-51 cover names/initials/holders and the shared-/Resources two-page case → images 1, ok.

Gaps:
- **minutes** · tests/codex-pdf.test.mjs is STALE against the new alphabet: lines 14-19 (and the added long-name case at :21-22 endsWith(" 2026.09.07 23.02UTC")) still expect a space before the time and rows joined by ' . ', while lib/codex-pdf.ts:65 now emits 'YYYY.MM.DD_HH.MMZZZ' and :80 joins with ' • ' — `npm run test:codex-pdf` cannot be 26/26 on this tree; the plan's claim predates the _/• change. — `frontend/tests/codex-pdf.test.mjs` → Update the five expectations to '2026.09.07_18.02CDT', '2026.01.15_08.05CST', '2026.09.07_23.02UTC', the ALL join ' • ', and the long-name endsWith('_23.02UTC'); add one round-trip assertion that decodeCodexPdf returns a strip whose messageForward contains '_' and '•' so the PDF decoder is proven on the new groups, not only the encoder.
- **minutes** · ':' symbol (operator 17:1x ask, VBVB) is not in the tree: light-codex.ts:30 has only - _ •; codexText still writes the full stop for the colon (codex-pdf.ts:65); the ask is not persisted (docs/asks/ ends at 2026-09-08_1721_…, no 17:1x colon file). — `frontend/lib/light-codex.ts` → Persist the ask to docs/asks/ and commit alone; add ":": "VBVB" beside :30 (proof above: unused, reversal BVBV unused, no G); change codex-pdf.ts:65 to `${hour}:${minute}` and update the codex-pdf tests to 'ALEX SEGUIN 2026.09.07_18:02CDT'; update the Light Codex page hint.
- **minutes** · Light Codex page hint still reads 'A–Z, digits, space, period' — it does not announce - _ • (or :). — `frontend/components/light-codex-cube.tsx:187` → Extend the hint (through the lexicon key it should already use) to 'A–Z, digits, space, period, - _ • :' with the ES string.
- **minutes** · Non-ASCII file name round-trip is only half closed: POST stores 'pagaré-firmado.pdf' decoded, but GET (tmp-core.js:28) sanitises content-disposition to 'pagar_-firmado.pdf' and getTempFile (lib/tmpfile.ts:17) reads the name from that header, so the ?f= recipient sees a mangled title and the signed-file name inherits it. The new test asserts ASCII-safety, not the name the recipient gets back. — `frontend/tmp-core.js` → On GET add header `x-file-name: encodeURIComponent(name)` (or RFC 5987 `filename*=UTF-8''…` beside the ASCII filename); in tmpfile.ts:17 prefer decodeURIComponent(res.headers.get('x-file-name')) with the content-disposition fallback; extend tests/tmp-core.test.mjs to assert the GET carries the exact decoded name.
- **minutes** · Auth0 login-at-save: the Sign & save button is still disabled while auth.isLoading (sign-flow.tsx:653 `disabled={… (!!requireLogin && auth.isLoading)}`), so the auth_loading sentence at :354 remains unreachable by tap (plan B12 'auth.isLoading: button shows the auth_loading sentence' not yet applied); :653 also tests requireLogin rather than needLogin. — `frontend/components/sign/sign-flow.tsx:653` → Drop the isLoading term from `disabled`, key on needLogin, and render t('soi.sign.err.auth_loading') as the button caption (or an aria-live line beside it) while needLogin && auth.isLoading.
- **minutes** · /api/notify pause has no test: worker.js:60-61 is the only proof; tests/notify-core.test.mjs has no paused case and worker.js is not unit-run. — `frontend/worker.js:60` → Extract the three identical isPaused guards (:50, :56, :61) into one `pausedGate(env)` helper exported from a tiny module and add a test that a paused env returns 503 for /api/notify, /api/tmp and the checkout route.
- **hour** · Hosted Supabase 036 presence remains unmeasured (fleet items 62/71 still open): probeRpc exists (sign-store.ts:45-53) but verify-live.yml has no RPC probe, so 'rpc_missing' on the live site would only be discovered by a user's first two-signer save falling to the local/file path. — `.github/workflows/verify-live.yml` → Operator-owned migration apply stays out of scope; add the read-only curl probe of sign_envelope_get to verify-live.yml and fail on PGRST202 (accept bad_token) so the deploy state line can say 036 ✓ / ✗.

### Odin — ready-with-fixes (111 words)

I gave one eye for foresight, so I read the record before the promise. The caption prints one instant, cacStamp, and the keyword keeps the ISO the readers need. The written date rides in SoITxt as text, so a court sees what was written, not only where. Names, initials and holders are on the verifier's tongue. Two pages sharing one Resources dict count one signature, proven by test. The chain arithmetic is untouched. Three doors remain open. The codex tests still expect the old space and the old dot. The flow never hands the caption its contact or index. The verifier never asks whether a text mark's pass matches its row.

Verified in the code:
- A1 cacStamp caption APPLIED: frontend/lib/pdf-stamp.ts:94-97 caption = captionName(...) · cacStamp(sig.isoDate) · #hash; SoISig/SoICodex keywords keep the ISO value (lib/pdf-stamp.ts:143, :263). Pairing tests follow it: tests/pdf-stamp.test.mjs:53-54 and :58 (Signer N caption).
- A3 SoITxt :t<b64url> APPLIED: lib/pdf-stamp.ts:141-143 appends `:t${b64u(clean)}`; textBoxes returns TextMark {text, signerIdx, isoDate, chain} via regex at lib/pdf-stamp.ts:150-160 (b64u/b64uDecode at :337-338). Round-trip tests tests/pdf-stamp.test.mjs:66-69, :90-91 (rotated page r90 with :t).
- A2 codexText never cuts the timestamp + captionName fallback APPLIED: lib/codex-pdf.ts:48-52 captionName (Latin name → contact → 'Signer N'); :55 CODEX_LINE_MAX=44; :64-80 name trimmed to 44−stamp−1, stamp cleaned, never cut; codexAllText passes contact/rowIndex (:86-87). pdfSafe/captionName re-exported from pdf-stamp (lib/pdf-stamp.ts diff 'export { pdfSafe, captionName }'). Tests tests/codex-pdf.test.mjs:20-26.
- A7 verifier names/initials/holders APPLIED: lib/sign-verify.ts:27-42 VerifyReport.rows[].name (from codexRows :n suffix, lib/pdf-stamp.ts:330-334), initials = initialledBy().length, holders = holders().length. Tests tests/sign-verify.test.mjs:26-34 ('Alex,Dan', initials 1 / holders 1). verify-file.tsx:32 signerName uses row.name, :52 prints cacStamp.
- A7 shared /Resources dedupe APPLIED: countSignatureImages dedupes by PDFRef.toString() (inline dict falls back to page+name), lib/pdf-stamp.ts countSignatureImages (diff lines 235-247). Test: tests/sign-verify.test.mjs:36-50 (two pages, one Resources dict on the Pages node, one image → images 1, boxes 1, ok).
- Chain UNCHANGED: verifySignedPdf chain logic (lib/sign-verify.ts:35-42, row_k_chain, genesis handling, closing chainHash) is untouched by the diff; only imports/report fields changed. Tamper test tests/sign-verify.test.mjs:52-56 still red on a wrong row hash.
- C2 initials slot order APPLIED: lib/pdf-stamp.ts initialsSlot x = width − right − Σwidths[0..idx] − idx·gap (diff line 267); initialsRowFrac (diff 271-274) feeds initialsSlotTop colFrac (lib/sign-layout.ts:9-12). Tests tests/pdf-stamp.test.mjs:72-79 (558/518/478 on Letter; 498/458 wide), tests/sign-layout.test.mjs:28.
- A4/A5 APPLIED: hidden helix 0.25 pt (lib/pdf-stamp.ts diff 284-286); unused Helvetica embed removed from stampHolders (diff line 294). A1 clearBox never grows below the box (diff 109-116); rotated-page caption under the rule (diff 181-187).

Gaps:
- **minutes** · codex-pdf tests still assert the OLD line form: tests/codex-pdf.test.mjs:15-18 expect 'ALEX SEGUIN 2026.09.07 18.02CDT' (space before the time) and the ALL join ' . ', but lib/codex-pdf.ts:76 now writes '2026.09.07_18.02CDT' and :87 joins with ' • '. The new tests at :20-26 also expect the space form. The suite is red as the tree stands (plan claims 26/26 from before the symbol change). — `frontend/tests/codex-pdf.test.mjs` → Update expectations at lines 15-18 and 20-26 to the underscore form and ' • ' joiner; add one round-trip case that encodes and decodes a line containing '-', '_' and '•' through codexImage/decodeCodexPdf so the new groups are proven in the PDF path, not only in ALPHA.
- **minutes** · The ':' symbol (VBVB) named in the plan ('guess we need : symbol as well. add') is NOT in the tree: no '":"' or 'VBVB' in lib/light-codex.ts; codexText still substitutes '.' for the colon (lib/codex-pdf.ts:57-59 comment, :76 `${hour}.${minute}`). The colon ask is also not persisted under docs/asks (files end at _1721). — `frontend/lib/light-codex.ts` → Persist the 17:1x colon ask to docs/asks first (commit alone). Then add '":": "VBVB"' to ALPHA beside the three new groups, change codexText to `${hour}:${minute}` and its docstring, update tests, and re-run the uniqueness script (groups + reversals, no G) so the proof covers 46 groups.
- **minutes** · Light Codex page hint not updated: components/light-codex-cube.tsx:187 still reads 'A–Z, digits, space, period.' while the alphabet now accepts - _ • (and : once added). — `frontend/components/light-codex-cube.tsx` → Change the hint to 'A–Z, digits, space, period, - _ • :' (through t() if the page is lexicon-covered).
- **hour** · The flow never hands the core the caption fallback inputs: components/sign/sign-flow.tsx:372 calls stampSignature without `contact` or `signerIdx`, and :401 codexAllText(allRows) rows carry no `contact`. The caption idx then falls back to the SoISig keyword count (lib/pdf-stamp.ts:96) while the codex line uses rowIndex=myRow; for an offline hand-off where a row is absent or reordered the visible 'Signer N' and the hidden 'SIGNER N' can disagree, and the contact path is dead code end to end. — `frontend/components/sign/sign-flow.tsx` → Compute myRow before stampSignature (move the codexRows read above line 372) and pass { contact: myContact, signerIdx: myRow }; add contact to earlier/allRows from pub?.signers / signers so codexAllText and the caption share one input. Add a sign-verify or live-run assertion that caption name text equals the decoded codex NAME for a non-Latin signer.
- **hour** · verifySignedPdf ignores the SoITxt pass binding it now decodes: lib/sign-verify.ts:28-42 only counts texts; a text mark whose :s<idx>:<iso>:<chain> does not match the pass/row it claims (a date added in a later edit, or a chain that never existed) still reads green. A court would ask 'who wrote this date and in which pass'; the file records it, the verifier does not check it. — `frontend/lib/sign-verify.ts` → For each TextMark with signerIdx: require a row with rowIndex === signerIdx and isoDate === mark.isoDate, and (when not genesis) mark.chain === passes[signerIdx].chain; push `text_<k>_pass` on mismatch and add it to ISSUE_KEYS + soi.sign.verify.issue.* (+ES). One tamper test in tests/sign-verify.test.mjs.
- **minutes** · A5 shim not resolved either way: lib/codex-strip.ts still exists and components/sign/sign-flow.tsx:32 imports codexAllText/codexImage through it (so it is not test-only and must not be deleted as the plan proposes without repointing). — `frontend/lib/codex-strip.ts` → Repoint sign-flow.tsx:32 to '@/lib/codex-pdf' and delete lib/codex-strip.ts, or leave both and strike A5 from the plan; do not delete the shim while sign-flow imports it.
- **minutes** · Two renderings of the instant remain by design: the caption and the verifier print cacStamp UTC (lib/pdf-stamp.ts:236-239) while the hidden codex line prints the signer's LOCAL time with zone (lib/codex-pdf.ts:68-76, e.g. 18.02CDT vs 23:02:39 UTC). Both derive from one ISO keyword, so the chain is unaffected, but a reader comparing the decoded Light Codex against the caption sees different clocks. — `frontend/lib/codex-pdf.ts` → Document the rule once in VerifyReport / verify-file.tsx (show 'local ZZZ = UTC' beside the row) so the two forms are explained on the receipt; no code change required for provenance.

### Pangu — ready-with-fixes (111 words)

I broke the sky open once. Today three colours broke the Light Codex open. Violet leads every new symbol; cyan, red and yellow follow; no green, no reversal collides with a taken letter. That is in the file, light codex line thirty, and it decodes. The zoom is real: pinch, double tap, snap, nudge, page fractions untouched at four times. Four things remain undone. The colon symbol is unwritten. The page hint still names four characters. The flow caps a date at a constant instead of the document's own measured height. The codex tests expect a space where the code writes an underscore. DocuSign sells trust; we print it, for free.

Verified in the code:
- Light Codex symbols APPLIED: frontend/lib/light-codex.ts:30 `"-": "VCVC", "_": "VRVR", "•": "VYVY"` (BRYCVW: - = Violet Cyan Violet Cyan, _ = Violet Red Violet Red, • = Violet Yellow Violet Yellow). Checked by hand against ALPHA (light-codex.ts:17-24), NUMBERS (:33-34) and TRANSMISSION (:36): none of VCVC/VRVR/VYVY is taken, none contains G, and the reversals CVCV/RVRV/YVYV are also unused. GROUP_TO_CHAR (light-codex.ts:47) is built from ALPHA so decode picks them up; unsupportedChars (:64-71) accepts them via hasOwnProperty on ALPHA. Correctly designed.
- codexText APPLIED to the operator's format: frontend/lib/codex-pdf.ts:65 writes `${year}.${month}.${day}_${hour}.${minute}${zone}` (underscore now real, full stop still stands in for the colon); codexAllText :73-74 joins rows with ' • '. Name trimmed to 44 − stamp − 1 (:68), timestamp never cut; captionName fallback shared with the caption (:42-46, pdf-stamp.ts:89 re-export).
- Zoom APPLIED: frontend/components/sign/pdf-page-view.tsx:37 zoom state, :76-89 setZoom clamps [1,4] and keeps the page point under the finger via scrollLeft/scrollTop, :69 re-renders the canvas at base×zoom (crisp), :108-115 pinch (two-touch distance ratio, centred on the midpoint), :118-123 double-tap 1× ↔ 2.5× under the finger, :125 zoomed swipe scrolls instead of turning the page, :130-135 ⌖ snapSelected reuses fitAt (sig takes the rule box, text keeps its height centred on the rule), :136-140 arrow nudges 0.0025 of the page, :181-185 44-px − / % / + / ⌖ buttons with t() aria-labels, :189 scroller max-h-[72vh] overflow-auto touchAction pan-x pan-y when zoom > 1, :190 host width = base×zoom. Marks are page fractions, so alignment survives zoom by construction.
- Zoom lexicon APPLIED: lib/lexicon-data.ts (+5 keys soi.sign.zoom_in|zoom_out|zoom_reset|snap_line|zoom_hint) and lib/lexicon-translations-es-sign.ts (+5 ES).
- Zoom live-run steps APPLIED: scripts/sign-live-run.mjs:158-174 — zoom-in 1.5× asserts page width ratio and sig-box fractions unchanged (data-zoom '1.5'), nudge the date off its line then ⌖ snaps its bottom back onto the rule (fit=underline), zoom-reset returns to 100 %.
- Text marks sized to the document, HALF APPLIED: lib/sign-fit.ts:48 FitBox.textH and :77-86 measure the label left of the rule, else the text line above (tests/sign-fit.test.mjs:43-55, 3 cases). TXT_H default lowered 0.035 → 0.02 at components/sign/pdf-page-view.tsx:25.
- Light Codex row order (C2) APPLIED: lib/pdf-stamp.ts:220-223 initialsSlot x = width − right − Σ widths[0..idx] − idx·gap (signer 0 at the right edge, each later signer LEFT of it); tests/pdf-stamp.test.mjs:309-317 assert 558/518/478 on Letter and the wide-first case 498/458. initialsRowFrac (:224-230) wired into sign-flow.tsx:45,389,393 so the scan band grows with the row.
- No-tax stance is on the record: docs/asks/2026-09-08_0020_fleet_ring_order_footer.md:8 'No one collects a tax on signatures. No fees. No account needed to countersign.' The ?f= offline hand-off (plan B9) and the two-signer local mode (B13) are the code that keeps it true; nothing in the sign tree references a fee.

Gaps:
- **minutes** · codex-pdf tests are stale against the new codexText/codexAllText: tests/codex-pdf.test.mjs:15-18, 20-26 and :36 still expect the OLD form 'ALEX SEGUIN 2026.09.07 18.02CDT' (space for the underscore) and the ' . ' join, while lib/codex-pdf.ts:65 now writes '2026.09.07_18.02CDT' and :74 joins with ' • '. The plan's 'codex-pdf 26/26' cannot hold on this tree — the suite is red by construction. — `frontend/tests/codex-pdf.test.mjs:15-18,20-26,36` → Update every expected string to the underscore form and the ' • ' join (e.g. 'ALEX SEGUIN 2026.09.07_18.02CDT • DANIEL VAIL 2026.09.07_18.02CDT'); keep the decode round-trip at :36 so the three new symbols are proven through extractCodexStrips/decodeCodexPdf; also refresh docs/asks/2026-09-08_0150_codex_date_format.md:6-7 which still says 'no underscore, hyphen or colon'.
- **minutes** · The colon symbol ':' (V B V B) from the 17:1x ask is NOT applied: light-codex.ts:30 has only - _ •; no VBVB anywhere in lib/ or tests/; codex-pdf.ts:65 still prints a full stop for the colon; the ask is not persisted (docs/asks has no 17:1x colon file — latest is 2026-09-08_1721). — `frontend/lib/light-codex.ts:30` → Persist the ask first (docs/asks/2026-09-08_171x_codex_colon.md, own commit), then add `":": "VBVB"` beside the three (VBVB and BVBV are unused: NUMBERS has BBBB/WBBB/VBBB, ALPHA has BBBW/WBWB — no collision), change codex-pdf.ts:65 to `${hour}:${minute}`, update the tests to '2026.09.08_08:11CST'.
- **minutes** · The Light Codex page hint still reads 'A–Z, digits, space, period' (plan says it gains '- _ • :'). — `frontend/components/light-codex-cube.tsx:187` → Change the hint to 'A–Z, digits, space, period, - _ • :' (or derive it from Object.keys(ALPHA) so it can never drift again); it is hardcoded English — route through t() with a lexicon key per the Lexicon gate.
- **hour** · textH is measured but never consumed: sign-fit.ts:48 returns FitBox.textH, yet sign-flow.tsx addText (:323-334) caps the fitted mark at `Math.min(f.h, TXT_H)` and the date holder (:415-416) does the same — the plan's `h = clamp(textH × 1.9, 0.012, 0.03)` with bottom on the rule is not in the tree (grep 'textH' in components/sign returns nothing). A document set in 9-pt or 14-pt type still gets a fixed 16-pt date. — `frontend/components/sign/sign-flow.tsx:333,415` → In addText: `const h = f.textH ? Math.min(0.03, Math.max(0.012, f.textH * 1.9)) : Math.min(f.h, TXT_H); capped = { h, y: f.y + f.h - h }`; same expression for dateH at :415; add a live-run step asserting the date box height ≈ 1.9× the measured label height.
- **hour** · Pinch has no unit or live proof: sign-live-run.mjs:158-174 only exercises the + button, snap and reset; the two-touch math (pdf-page-view.tsx:110-115) and double-tap (:118-123) — the phone gestures the operator asked for — are untested on any device, and the scroll-anchoring in setZoom (:80-86) guesses the page aspect from scrollHeight/width (ratio) which is wrong while the canvas is still re-rendering. — `frontend/components/sign/pdf-page-view.tsx:76-89,108-123` → Add a Playwright touchscreen step (hasTouch context, two dispatchTouch events with growing distance) asserting data-zoom > 1 and the sig-box fractions unchanged; use docRef's page viewport height/width for `ratio` instead of scrollHeight; send the 2c-zoom screenshot from a 375-px viewport.
- **day** · What still separates this from DocuSign/Foxit on the innovation axis: the hidden helix is a bytes-only carrier (0.25 pt at y=0, outside every printer margin — pdf-stamp.ts:275); a printed copy carries no codex, so the 'in plain sight' promise holds for the file only. Fleet item (b0ihcx3e0.txt:39) named it; nothing in the diff answers it. — `frontend/lib/pdf-stamp.ts:275` → Either state 'digital file only' in the consent sentence (lexicon key + ES) or add a print-scale carrier on the last page inside the printable margin (codex at ≥3 pt/block or a QR of codexAllText + chain). Report as owed, do not build today.

### Sofia — ready-with-fixes (111 words)

Every new key has its Spanish twin, sixteen of sixteen, and the test refuses a gap. Zoom, snap and reset carry aria labels at forty four pixels; the glyphs speak before the words do. Three codex symbols exist, but the colon the operator asked for is absent, and the codex test still expects the old period joiner, so the tree contradicts itself. The Light Codex page hint still says period only, in English, untranslated. The link expiry ignores the Globe while the stamped date obeys it. Check again has a key and no button. The AI star is imported and unused. Thirty one languages still read English by fallback, as designed.

Verified in the code:
- Spanish parity: all 16 new soi.sign.* keys (step.error, x.error, signer_n, 7 verify.issue.*, check_again, zoom_in/out/reset, snap_line, zoom_hint) added in frontend/lib/lexicon-data.ts:1026-1041 and mirrored in frontend/lib/lexicon-translations-es-sign.ts:170-185; tests/sign-i18n.test.mjs:21-24 asserts every soi.sign.* key has an ES value (gate exists).
- Zoom/snap aria-labels at 44 px: frontend/components/sign/pdf-page-view.tsx:181-186 — zoom-out/zoom-in min-h/min-w 44px with aria-label t(soi.sign.zoom_out|zoom_in), reset min-h 44 aria-label t(soi.sign.zoom_reset), snap min-h/min-w 44 aria-label + title t(soi.sign.snap_line); page ‹ › keep theirs (:179,188).
- Iconology over words: zoom bar is − / NN% / + / ⌖ glyphs, no text labels; hint sentence single key soi.sign.zoom_hint appended at pdf-page-view.tsx:206.
- Glyph law documented and typed: frontend/lib/sign-steps.ts:6-16 table, SignStep union :19-20, AI_GLYPH ✦ exported :32 so ◬ keeps one meaning.
- Stamped date follows the Globe: frontend/components/sign/sign-flow.tsx:54-56 dateIn(locale, …, numberingSystem latn with fallbacks), :88 activeLocale from useLexicon, :338 todayText uses activeLocale (was undefined locale).
- Error state in diag: frontend/components/sign/sign-diag.tsx:17 railOf + :46 t(soi.sign.step.<rail>) + stage sentence when step === error.
- Verify issue codes → keys: frontend/components/sign/verify-file.tsx (diff hunk ~:480-508) maps codes to t(soi.sign.verify.issue.*), row_N_chain regex → row_chain {n}, signerName via row.name ?? t(soi.sign.signer_n).
- Light Codex symbols - _ • applied: frontend/lib/light-codex.ts:25-30 ('-' VCVC, '_' VRVR, '•' VYVY) with the BRYCVW comment; unsupportedChars comment :66 updated; codex-pdf.ts:65-66 stamp uses '_' and codexAllText :74 joins with ' • '.

Gaps:
- **minutes** · Colon ':' symbol (operator 17:1x ask, plan says V B V B) is NOT in ALPHA; codexText still writes '_HH.MM' not 'HH:MM' so the line is not the operator's 'ALEX SEGUIN 2026.09.08_08:11CST' format — `frontend/lib/light-codex.ts:30, frontend/lib/codex-pdf.ts:65-66` → Add ":": "VBVB" beside the three symbols (prove unused + reversal BVBV unused in the one-off script), change stamp to `${hour}:${minute}` in both branches, persist the 17:1x ask to docs/asks/ first, then update tests
- **minutes** · codex-pdf tests contradict the tree: they still expect ' . ' joiner and '18.02CDT' (period), while codexAllText now joins with ' • ' and the stamp uses '_' — suite will fail; no test covers the three new symbols round-tripping through unsupportedChars/encode/decode — `frontend/tests/codex-pdf.test.mjs:18,26` → Update expected strings to '… 2026.09.07_18:02CDT • DANIEL …' and 'SIGNER 2 2026.09.07_18:02UTC'; add a case asserting unsupportedChars('- _ • :') is empty and each group has no G and no collision with ALPHA/NUMBERS/TRANSMISSION or their reversals
- **minutes** · Light Codex page hint still says 'A–Z, digits, space, period.' and the whole panel is hard-coded English (10 t() calls, one aria-label in the file) — `frontend/components/light-codex-cube.tsx:187 (also :183,:185,:194 labels/placeholder)` → Add key soi.codex.chars englishDefault 'A–Z, digits, space, period, - _ • :' (+ES 'A–Z, dígitos, espacio, punto, - _ • :'), use t() for the label/placeholder/unsupported line
- **minutes** · 24-h temp link expiry ignores the Globe: new Date(tmpLink.expires).toLocaleString() uses the browser default locale, while the stamped date obeys activeLocale (plan B2 half-done) — `frontend/components/sign/sign-flow.tsx:674` → Replace with dateIn(activeLocale, new Date(tmpLink.expires), { dateStyle: 'medium', timeStyle: 'short' }, true) (helper already at :54)
- **minutes** · 'Check again' (B16) and the error explainer (B1) have lexicon keys + ES but no UI uses them: no t('soi.sign.check_again') or t('soi.sign.x.error') anywhere in components/sign — `frontend/components/sign/sign-flow.tsx (waiting branch near :220), frontend/components/sign/sign-diag.tsx` → Render a 44 px button with ↻ glyph + t('soi.sign.check_again') calling refreshEnvelope in the waiting state; show t('soi.sign.x.error') in the diag todo when step === 'error' and no err sentence
- **minutes** · AI_GLYPH ✦ is imported but never rendered; the 'AI: find my line' button still carries whatever glyph it had (glyph law B4 not applied to the button) — `frontend/components/sign/sign-flow.tsx:19` → Use {AI_GLYPH} as the aria-hidden prefix of the soi.sign.ai.find button; grep that no ◬ remains on non-record controls
- **minutes** · Zoom reset key reads 'Fit the page' but the control shows the percentage and returns to 1× only; a screen reader hears a different action than the eye sees — `frontend/lib/lexicon-data.ts:1038, frontend/components/sign/pdf-page-view.tsx:183` → Rename englishDefault to 'Zoom 100 %' (ES 'Zoom al 100 %') or make the button actually fit-to-width; keep aria and visual meaning identical
- **hour** · 31 other languages: none of the 16 new keys exist in SIGN_TRANSLATIONS (737 keys × 32 langs), so zoom/snap aria-labels and verify issue sentences fall back to English there — acceptable by fallback chain but the aria-labels are the first thing a non-English screen-reader user hears — `frontend/lib/lexicon-translations-sign.ts` → Add at least the 4 short aria keys (zoom_in, zoom_out, zoom_reset, snap_line) to the 32-language table; the long sentences can wait
- **minutes** · 'Signer N' fallback inside the PDF caption/codex is English only (captionName) and untranslatable by design (codex alphabet A–Z); the on-screen twin soi.sign.signer_n is translated, so the page and the paper disagree for non-Latin names — `frontend/lib/codex-pdf.ts:42-45` → Document the divergence in the key's context and in soi.sign.no_fee-style footer; no code change unless a Unicode PDF font ships (operator-owned)

### Thoth — ready-with-fixes (111 words)

I measured the slots. Signer zero rests at 558, the second at 518, the third at 478, and the test proves it. The row fraction floors at 0.45 and caps at one. Zoom reads fractions from the host rectangle, so a mark keeps its place at any scale; the nudge is a quarter percent; the preview text sizes itself by 72 cqh inside a sized container. The helix is a quarter point. The stamp is never cut. Three flaws remain. The codex test still expects the old space and dot. The colon is neither persisted nor encoded. The flow ignores textH and caps every date at 0.02. Fix these before shipping.

Verified in the code:
- C2 initials slot order APPLIED and correct: frontend/lib/pdf-stamp.ts:222 x = width − right − Σ widths[0..idx] − idx·gap → 558/518/478 for [36,36,36] on Letter (612−18−36, −72−4, −108−8); wide first slot [96,36] → 498/458; test frontend/tests/pdf-stamp.test.mjs:73-77 asserts exactly these numbers and the end-to-gap chaining
- initialsRowFrac APPLIED: frontend/lib/pdf-stamp.ts:227-230 = (right + Σw + (n−1)·gap + gap)/pageWidthPt, floored 0.45, capped 1; [96×4] → 418/612 (tested pdf-stamp.test.mjs:79); consumed by the flow at components/sign/sign-flow.tsx:45,392-393 with bmp.widthPt (pdf-render.ts:8,45 supplies widthPt), initialsSlotTop colFrac param documented at lib/sign-layout.ts:9-13 and tested sign-layout.test.mjs:26-28
- MIN_H 0.012 / TXT_H 0.02 APPLIED: components/sign/pdf-page-view.tsx:25 (TXT_H 0.02 = 16 pt on Letter, was 0.035); resize keeps the bottom fixed (pdf-page-view.tsx:155 h = max(MIN_H, bottom − q.y), y = bottom − h)
- Zoom invariance APPLIED: marks are page fractions; frac() reads host.getBoundingClientRect() at the CURRENT zoom (pdf-page-view.tsx:91), host width = base × zoom (:190), re-rendered via renderPage(doc, page, base×zoom) (:69), zoom clamped [1,4] (:77), pinch (:108-115), double-tap 1 ↔ 2.5 (:122), arrow nudge step 0.0025 = ¼ % (:138), fitAt reads the live canvas so thresholds are scale-free (:98-103); live-run asserts box x/w fractions unchanged at 1.5× and 100 % on reset (scripts/sign-live-run.mjs:162-174)
- 72 cqh preview APPLIED correctly: the mark div sets containerType: "size" (pdf-page-view.tsx:194) so fontSize 72cqh (:197) resolves against the box height, not the viewport
- Helix 0.25 pt APPLIED: lib/pdf-stamp.ts:275-276 embedCodexImage(..., 0.25) with the decoder reading the XObject pixels (2 px × ≥612 px), not the drawing
- CODEX_LINE_MAX 44 with timestamp never cut APPLIED: lib/codex-pdf.ts:55,78-80 — stamp cleaned first, NAME sliced to 44 − stamp.length − 1, joined by one space; stamp '2026.09.08_08.11CDT' = 19 chars so ≤ 24 name chars survive; the caption/hidden-line share ONE captionName rule (codex-pdf.ts:48-52, pdf-stamp.ts:179 re-export, :199-200 caption)
- Light Codex symbols - _ • APPLIED: lib/light-codex.ts:29 '-'=VCVC, '_'=VRVR, '•'=VYVY; verified by hand against ALPHA/NUMBERS/TRANSMISSION: none taken, no G, reversals CVCV/RVRV/YVYV unused; unsupportedChars comment updated (:66)

Gaps:
- **minutes** · codex-pdf unit test still asserts the OLD line format (space for '_', ' . ' join) while the code now emits '_' and ' • ' — test:codex-pdf cannot be 26/26 against this tree — `frontend/tests/codex-pdf.test.mjs:14-24 vs frontend/lib/codex-pdf.ts:76-87` → Update the five expectations to 'ALEX SEGUIN 2026.09.07_18.02CDT', 'ADA LENDER 2026.01.15_08.05CST', '…_23.02UTC', join ' • ', long.endsWith(' 2026.09.07_23.02UTC'); re-run npm run test:codex-pdf before commit
- **minutes** · ':' symbol (operator 17:1x ask, plan: VBVB) is NOT in ALPHA, the stamp still writes '.' for the colon, and the ask is not persisted under docs/asks (PERSIST FIRST rule) — `frontend/lib/light-codex.ts:29; frontend/lib/codex-pdf.ts:76-77; docs/asks/` → Persist docs/asks/2026-09-08_171x_codex_colon.md and commit alone; add ":": "VBVB" (unused, no green, reversal BVBV unused — verified); change `${hour}.${minute}` → `${hour}:${minute}` in both stamp branches so the line is exactly 'ALEX SEGUIN 2026.09.08_08:11CST' (still 19 chars, ≤ 44 holds); update codex-pdf tests, light-codex-cube.tsx:187 hint to 'A–Z, digits, space, period, - _ • :'
- **hour** · Text marks are NOT sized to the document: addText caps a fitted text mark at min(f.h, TXT_H) and never reads f.textH; the plan's h = clamp(textH × 1.9, 0.012, 0.03) with bottom on the rule is unimplemented (holders' date at :415 likewise uses TXT_H) — `frontend/components/sign/sign-flow.tsx:333-334 (and :415-416)` → const h = f.textH ? Math.min(0.03, Math.max(MIN_H, f.textH * 1.9)) : Math.min(f.h, TXT_H); capped = { h, y: f.y + f.h − h }; export MIN_H from pdf-page-view; same for dateH in the holder branch; add a sign-fit→flow assertion in the live run (date box height ≈ 1.9 × label ink)
- **minutes** · clampBox grows a sub-MIN_H box DOWNWARD (h raised, y kept) so the baseline leaves the rule; plan B5 says grow upward keeping the bottom — `frontend/components/sign/pdf-page-view.tsx:92` → In clampBox compute bottom = m.y + m.h, h = clamp(m.h, MIN_H, 1), y = clamp(bottom − h, 0, 1 − h)
- **minutes** · Hidden codex line and visible caption can disagree for a non-Latin name that HAS a contact: allRows passed to codexAllText carry no `contact`, so the codex falls to 'SIGNER N' while the caption prints the contact — `frontend/components/sign/sign-flow.tsx:387-402` → Include contact (signers[rowIndex]?.contact / pub roster) in allRows so codexText's fallback matches captionName's; add a codex-pdf test with contact
- **minutes** · Row band for the slot scan is computed from default widths only (slotWidthsOf(total) with no mine/recorded aspect), so a 96-pt initials stroke with ≥4 signers can extend left of the scanned band — `frontend/components/sign/sign-flow.tsx:392-393` → Pass { idx: myRow, aspect } and the recorded SoIInitW widths into initialsSlotWidths before initialsRowFrac (same inputs stampCodexBlock uses)
- **minutes** · Plan 'C2' still names the OLD formula for initialsSlot (lib/pdf-stamp.ts:228) — stale reference only; the tree already carries the reversed formula — `/root/.claude/plans/do-me-a-fsvor-pure-wirth.md (C2)` → Mark C2 applied; cite pdf-stamp.ts:222 and pdf-stamp.test.mjs:73-79

### Thor — ready-with-fixes (111 words)

I looked for the doors a stranger can open. The build refuses the no login flag in CI, but ship.sh never runs envcheck and next build still reads a local env file, so a laptop can ship the creator login off. Close that in next.config. Missing Auth0 keys silently turn the login off too; make them required. Every API route honours the pause switch. The file link is a credential, 128 random bits, gone in a day; the holder can read the PDF, sign it with any name, and mint a new link, nothing more. Serve it as an attachment, not inline. Throttle the upload. No secret sits in any file.

Verified in the code:
- NO_AUTH refused at build — APPLIED (committed c66c127): frontend/scripts/envcheck.mjs:44-48 exits 1 when NEXT_PUBLIC_SIGN_NO_AUTH=1; .github/workflows/deploy.yml:66-74 runs envcheck before Build (deploy.yml:80-93). Correctly designed for the CI path.
- Pause switch on every API route — APPLIED (c66c127): frontend/worker.js:41-43 /api/donate (startsWith covers /api/donate/verify), :45-49 /api/ai, :51-55 /api/tmp, :56-60 /api/notify all return 503 when isPaused (worker.js:143-151). No API route bypasses the pause.
- ?f= without login — APPLIED (uncommitted): frontend/app/soi-session/sign/page.tsx:52-56 seed-once-per-token guard (sessionStorage exel-sign-seeded:<token>), :72-78 seeds only when not yet seeded, :93 requireLogin={!AUTH_OFF && !fileLink}; frontend/components/sign/sign-flow.tsx:91-92 handCarried/needLogin, :260-264 login redirect only when needLogin. What a stranger with the link can do: GET /api/tmp/<token> (frontend/tmp-core.js:22-30; 22-char base64url = 128 random bits, mintToken :17; 410 once expired/unknown), read the PDF, sign it locally with any typed name (local envelope, never the creator's Supabase envelope), and mint another 24-h link via POST /api/tmp (tmp-core.js:32-42: same-origin Origin check, %PDF- magic, 12 MB cap, no overwrite/listing). Cannot alter the creator's stored envelope. Design is consistent with the operator's 'the token is the credential' (tmp-core.js:2-7).
- X-File-Name decode — APPLIED (uncommitted): frontend/lib/tmpfile.ts:5-6 encodeURIComponent(name) on send; frontend/tmp-core.js:38-40 decodeURIComponent inside try/catch (undecodable kept as sent), .slice(0,120) applied AFTER decode; GET content-disposition sanitised to ASCII by name.replace(/[^\w.\- ]/g,'_') (tmp-core.js:29 — JS \w without /u is ASCII-only, so é → _ and quotes/CRLF are stripped); frontend/tests/tmp-core.test.mjs:25-30 lock the accented round-trip and the ASCII-safe header. Correct.
- XSS in captions/names — VERIFIED SAFE: every name/issue text is rendered as React text nodes — frontend/components/sign/verify-file.tsx:31-33 signerName/unknown, :50-51 rows and <details>; frontend/components/sign/handoff.tsx:40 <strong>{nextName || nextContact}</strong>; sms:/mailto: hrefs have a fixed scheme and encodeURIComponent'd bodies (handoff.tsx:20-21); the only innerHTML in the sign surfaces is frontend/components/light-codex-cube.tsx:107 clearing to ''. PDF side: captions go through pdfSafe (WinAnsi-only, git diff pdf-stamp.ts: import at line 11, caption at line 84); SoITxt :t<b64url> payload is regex-bounded [A-Za-z0-9_-]* on read (diff line 127). No dangerouslySetInnerHTML anywhere in components/sign.
- Secrets never in files — VERIFIED: `git diff` grep for sk_live|sk_test|eyJ…|re_…|AKIA|api_key= returns nothing; only .env.example files are tracked (git ls-files); no .env.local in frontend/; AI/Resend keys are Worker secrets (worker.js:45 comment); envcheck prints presence only (envcheck.mjs:57); sign-live-run.mjs diff carries no token/secret.

Gaps:
- **minutes** · NO_AUTH refusal is CI-only: ship.sh (the documented local ship path) never runs envcheck, and `next build` (package.json:10) reads frontend/.env.local at build time, which envcheck (process.env only) cannot see — a laptop with NEXT_PUBLIC_SIGN_NO_AUTH=1 in .env.local ships the creator login off through ship.sh's build step. — `frontend/scripts/ship.sh:37-40 (build step) + frontend/next.config.* + frontend/scripts/envcheck.mjs:44-48` → Move the refusal into next.config (throw when process.env.NEXT_PUBLIC_SIGN_NO_AUTH === '1' and the build is a deploy, e.g. CI=1 or WORKERS_CI_COMMIT_SHA set) so it fires wherever `next build` runs and after Next has loaded .env.local; also call `node scripts/envcheck.mjs` in ship.sh before `npm run build`. Add a guard test that builds with the flag and expects failure.
- **minutes** · Login silently OFF when Auth0 vars are absent: page.tsx:28 AUTH_OFF = !AUTH0_DOMAIN || !AUTH0_CLIENT_ID, while envcheck lists NEXT_PUBLIC_AUTH0_DOMAIN/CLIENT_ID as OPTIONAL (envcheck.mjs:20-24) — a CI run with those secrets unset builds green and ships a Sign Doc where anyone signs as the creator. — `frontend/scripts/envcheck.mjs:19-24 + frontend/app/soi-session/sign/page.tsx:28` → Promote NEXT_PUBLIC_AUTH0_DOMAIN and NEXT_PUBLIC_AUTH0_CLIENT_ID to REQUIRED in envcheck (or a SIGN_REQUIRED set), and on the page treat missing Auth0 config as 'login unavailable' (disable Sign & save with a sentence) rather than bypass.
- **minutes** · Stored PDF served `inline` from the site origin (tmp-core.js:29): a hand-off PDF with embedded JavaScript/openAction opens in the app's origin when the link is opened directly; the flow itself only needs the bytes via fetch (lib/tmpfile.ts:13 getTempFile), so inline rendering buys nothing. — `frontend/tmp-core.js:29` → Return `content-disposition: attachment; filename=...` plus `x-content-type-options: nosniff` and `content-security-policy: sandbox` on GET /api/tmp/<token>; update tests/tmp-core.test.mjs header assertions.
- **hour** · No rate limit on POST /api/tmp: any visitor on the site origin can mint unlimited 12 MB KV objects (tmp-core.js:32-42) — KV fill / cost abuse; the pause switch is the only brake. — `frontend/tmp-core.js:32-42 + frontend/worker.js:51-55` → Cloudflare rate-limiting rule on /api/tmp POST (e.g. 10/min per IP) or a KV/Durable-Object counter keyed by CF-Connecting-IP with a 429; document in the tmp-core header.
- **minutes** · ?f= link holder identity is unverified by design (any typed name is stamped as the signer): acceptable per the operator's 'token is the credential', but the plan should state it in docs/asks / the sign page doc-comment so a reviewer does not read it as a defect, and the verifier should show the row name it came from (row.name, now exposed at sign-verify.ts:14) as 'as typed', not 'as verified'. — `frontend/app/soi-session/sign/page.tsx:14-15 + frontend/components/sign/verify-file.tsx:33` → One sentence in the page header comment and the verify 'witnessed' line labelled 'name as recorded'; no code path change.

## The three Masters of Thought, 333 words each

### MoT-1 — pod 1 — READY (333 words)

Four masters read the same tree and returned one verdict. Ready with fixes. I verified their top gaps in the code and every one holds.

Aset checked the words. Sixteen new keys exist with Spanish twins. But three keys reach no screen. The error step is defined and the rail still maps error to nothing at line 495 while railOf sits unused at line 52. Check again has a key and no button at line 702. The AI button prints the record glyph at line 626 while AI_GLYPH is imported and never used. Cyan survives in twelve places in the flow and two in the page view.

Asar checked the file. Every promise to the PDF is kept. Text marks carry their text, captions print the receipt clock, a name the font cannot draw becomes the contact or Signer N, the first signer holds the right edge and each newcomer stands left. One debt: the codex tests at lines 17 through 37 still expect a space and a dot while the code writes an underscore and a bullet at codex line 65 and 74.

Athena checked the doors. Ship gates on test:ci, envcheck refuses the auth off flag, the innovation suite reads its real deck. The tree is red on commit for the codex reason, and the two live run regexes at lines 299 and 317 fail with it. The colon symbol is unwritten and its ask is not persisted. Ship never calls envcheck locally. The deploy workflow still runs the slowest suite twice.

Christo checked the people. A link recipient signs without a login and the roster refreshes while a phone waits. Yet a wrong secret still reads every name, a failed save still drops the countersigner on Place at line 459, the error state stays blank, and a carried file demands a contact from a signer who already signed.

Fix the tests first so the door opens, then the flow wiring, then the colon. All minutes. Ship tonight, only after green.

Priorities:
- 1. test:ci is RED: codex tests + two live-run regexes still expect 'YYYY.MM.DD HH.MM' + ' . ' while codexText writes '_' (lib/codex-pdf.ts:65-66) and codexAllText joins with ' • ' (:74). Fix tests/codex-pdf.test.mjs:14-18,21-26,37 and scripts/sign-live-run.mjs:299,317 (use [.:] class so the colon lands once). First door (ship.sh:37, deploy.yml:62) fails on commit until done. [Asar, Athena, Enlil, Krishna, Odin, Sofia, Pangu] — `frontend/tests/codex-pdf.test.mjs; frontend/scripts/sign-live-run.mjs:299,317` (minutes)
- 2. Error step wired: rail memo at sign-flow.tsx:495 still inline-maps (error lights nothing) while railOf (:52) and lastRail (:96) are declared and unused; explain (:498-512) has no case "error"; SignDiag call (:543) never passes stage: failStage; catch at :459 still sends a countersigner to PLACE (setFailStage never called). soi.sign.step.error / x.error keys exist (lexicon-data.ts:1027-1028) but reach no screen. [Aset, Christo] — `frontend/components/sign/sign-flow.tsx:52,96,459,495-512,543` (minutes)
- 3. Check again button missing: soi.sign.check_again defined (lexicon-data.ts:1037, es-sign.ts:180) but :702 renders only <Roster/>; refreshEnvelope (:201) + checking (:200) never surfaced; refresh effect (:217-223) only runs on step==='waiting', so the creator's handoff screen never flips to done. Same edit: hide Roster for not_party (:702 and :723 show every name to a wrong-secret reader). [Aset, Christo] — `frontend/components/sign/sign-flow.tsx:200-223,702,723` (minutes)
- 4. Colon symbol not applied and ask not persisted: lib/light-codex.ts:29-30 has only - _ • (no ":": "VBVB"); codex-pdf.ts:53 still says 'the full stop stands in for the colon'; docs/asks/ ends at 2026-09-08_1721 (no colon ask file). PERSIST FIRST: write the ask, commit alone, then add VBVB + switch codexText to HH:MM + header comment. Also add a gated uniqueness test (no tests/*.mjs imports lib/light-codex today) and wire into test:ci (package.json:87). [Athena, Enlil, Krishna, Sofia, Pangu] — `frontend/lib/light-codex.ts:29-30; frontend/lib/codex-pdf.ts:53,65; docs/asks/` (minutes)
- 5. Glyph law + theme tokens: AI button at sign-flow.tsx:626 prints literal ◬ while AI_GLYPH is imported at :19 and unused (◬ carries two meanings); 12 cyan classes remain in sign-flow.tsx (:519,520,540,542,552,557,578,588,616,645,650,686) and 2 in pdf-page-view.tsx (:194,198). Grep gate: grep -c cyan components/sign/*.tsx must be 0. [Aset, Christo, Enlil] — `frontend/components/sign/sign-flow.tsx:626 + 12 cyan lines; frontend/components/sign/pdf-page-view.tsx:194,198` (minutes)
- 6. Contact/signerIdx never reach the caption: stampSignature call at sign-flow.tsx:372 passes no contact or signerIdx (myRow is computed at :380 AFTER the stamp) and allRows carries no contact, so captionName's fallback is dead in the app and an offline hand-off can mis-number the caption vs the codex row. Compute myRow before the stamp, pass { contact, signerIdx: myRow }, add contact to this signer's allRows entry. [Asar, Odin] — `frontend/components/sign/sign-flow.tsx:372,380-381` (minutes)
- 7. Seeded/carried file blocks Signers step: signersOk at :316 demands contactKind for EVERY row including already-recorded signers (prefill gives contact ''), and :593 still disables Next with (multi && mode === 'local') (B13 not applied). Also :557 uses requireLogin && !loggedIn so the ?f= recipient sees 'log in now'; :653 disables Sign & save with !!requireLogin && auth.isLoading (not needLogin), making the auth_loading sentence at :354 unreachable. [Christo] — `frontend/components/sign/sign-flow.tsx:316,557,593,653` (minutes)
- 8. ship.sh never runs envcheck: scripts/ship.sh:29-43 gates tsc, test:ci, build only; the NEXT_PUBLIC_SIGN_NO_AUTH refusal (envcheck.mjs:33) protects deploy.yml:74 alone. Add envcheck after tsc, outside the SKIP_TESTS guard. deploy.yml:76-77 still re-runs test:innovation-time after test:ci already ran it; delete. [Athena] — `frontend/scripts/ship.sh:29-43; .github/workflows/deploy.yml:76-77` (minutes)
- 9. Link expiry ignores the Globe: sign-flow.tsx:674 uses new Date(tmpLink.expires).toLocaleString(); dateIn is called once only (:338). Route through dateIn(activeLocale, d, opts, true). Extend the i18n placeholder check (tests/sign-i18n.test.mjs:27 pins four keys) to every EN key containing {…} so signer_n and verify.issue.row_chain keep {n} in ES. [Aset, Christo, Sofia] — `frontend/components/sign/sign-flow.tsx:674; frontend/tests/sign-i18n.test.mjs:27` (minutes)
- 10. Light Codex page hint still 'A–Z, digits, space, period.' at components/light-codex-cube.tsx:187 (hard-coded English, not in git status); should name - _ • (and : once added) through a t() key with ES. Remove the codex-strip.ts shim by pointing sign-flow.tsx:32 at @/lib/codex-pdf (sign-flow is the only importer). [Asar, Athena, Enlil, Sofia, Pangu] — `frontend/components/light-codex-cube.tsx:187; frontend/components/sign/sign-flow.tsx:32; frontend/lib/codex-strip.ts` (minutes)
- 11. Hand-off DOM order: handoff.tsx:40 uses flex-row-reverse justify-end when mailFirst, so tab/screen-reader order contradicts the primary and double-flips under dir=rtl; order the anchors in JSX by kind instead. Add dir="ltr" on the PDF host (pdf-page-view.tsx:189) so canvas and resize handle (-right-2.5 at :198) never mirror. [Aset, Christo] — `frontend/components/sign/handoff.tsx:40; frontend/components/sign/pdf-page-view.tsx:189,198` (minutes)
- 12. Receipt parity: Done receipt line 2 (sign-flow.tsx:712) prints name ✓/✗ while Verify line 2 (verify-file.tsx:52) prints signerName · cacStamp(isoDate). Extract components/sign/receipt.tsx and render from both. [Aset] — `frontend/components/sign/sign-flow.tsx:711-713; frontend/components/sign/verify-file.tsx:50-53` (hour)
- 13. Proofs owed after green: C2 live assertion (Alex slot at right edge, slot1.x + slot1.w + gap === slot0.x) in sign-live-run.mjs after :283-284; pinch/double-tap/arrow-nudge/zoom-out steps for dan; an offline-run step opening ?f= with auth on asserting no login-redirect; wrong-secret asserts roster absent; record the new step count (tree has 60 step() calls; last doc says 55) with 2c-zoom screenshots and signed-codex-p1.png in docs/assessments/2026-09-07_sign_doc_two_phone_live_run.md. [Asar, Athena, Christo] — `frontend/scripts/sign-live-run.mjs; docs/assessments/2026-09-07_sign_doc_two_phone_live_run.md` (hour)

### MoT-2 — pod 2 — READY (333 words)

Pod two read the tree, not the plan. Enki, Enlil, Krishna and Odin returned ready with fixes, and every fix they name is real in the code.

Enki owns the zoom. Pinch, double tap, minus, plus, reset, the scroller, snap to line and arrow nudges live in the page view, and the live run proves plus, snap and reset. The JPEG knockout and the file removal clamp hold. He found three leaks. The clamp grows a short box downward off its rule. The text mark never reads textH, so the document size the operator asked for stops inside the fitter. At four times zoom the canvas passes the iOS pixel ceiling and renders blank.

Enlil owns the types and the gate. SignStep is a literal union, TextMark carries its text, VerifyReport takes initials and holders, the unused font is gone, one globe serves every page. He found the gate red. The codex test expects a space and a dot while the library prints an underscore and a bullet. The AI glyph is imported and unused. The colon symbol and the page hint are missing.

Krishna owns the joins. One alphabet map feeds the PNG decoder and the PDF decoder, the notify route sits behind the pause, the link recipient signs without Auth0. He found the file name crossing the header encoded and returning mangled because the GET never carries it back, and the save button disabled while auth loads.

Odin owns the record. The caption prints one instant, the keyword keeps the ISO, the written text rides in the file, two pages sharing one resource dictionary count once, the chain is untouched. He found the flow never hands the caption its contact or index, and the verifier never asks whether a text mark belongs to its row.

Four masters, one list. The colon and the tests first, then textH, then the clamp and the canvas cap, then the small joins. All minutes save two hours. The tree is red now and green tonight.

Priorities:
- 1. Persist the ':' ask, add ":": "VBVB" (light-codex.ts:30), codexText `${hour}:${minute}` (codex-pdf.ts:65), then bring the gate green: tests/codex-pdf.test.mjs:15-18,21-26,37 and sign-live-run.mjs:299,317 to '2026.09.07_18:02CDT' + ' • '; add one PDF round-trip case containing - _ • : [Enlil, Krishna, Odin] — `frontend/lib/light-codex.ts, frontend/lib/codex-pdf.ts, frontend/tests/codex-pdf.test.mjs, frontend/scripts/sign-live-run.mjs` (minutes)
- 2. Wire textH (operator ask): addText :333-334 and date holder :415-416 use h = f.textH ? clamp(f.textH×1.9, MIN_H, 0.03) : min(f.h, TXT_H), y = f.y + f.h − h; resizeSel :343 uses MIN_W/MIN_H; live-run asserts stamped date height ≈ 'Date:' label ±25% [Enki, Enlil] — `frontend/components/sign/sign-flow.tsx:333-334,343,415-416` (hour)
- 3. Bottom-pinned clamp: clampBox (pdf-page-view.tsx:92) if h < MIN_H { y = y + h − MIN_H; h = MIN_H } before the 0..1 clamp; AI box sign-flow.tsx:121 y: b.y + b.h − max(0.02, b.h); one clamp test asserting the bottom is unchanged [Enki] — `frontend/components/sign/pdf-page-view.tsx:92, frontend/components/sign/sign-flow.tsx:121` (minutes)
- 4. Cap the render at ~12 MP so 4× never blanks on iOS: scale·dpr ≤ √(12e6/(w·h)) in renderPage (pdf-render.ts:35-38); expose the effective scale on RenderedPage; guard snapSelected/onKey with m.page !== page (pdf-page-view.tsx:130,137) [Enki] — `frontend/lib/pdf-render.ts:35-38, frontend/components/sign/pdf-page-view.tsx:130-140` (minutes)
- 5. One input for caption and codex line: stampSignature(…, { contact: myContact, signerIdx: myRow }) at sign-flow.tsx:372 (compute myRow above it); allRows/earlier carry contact so codexAllText (:402) and captionName agree for a non-Latin signer; live-run assertion caption name === decoded codex NAME [Odin, Enlil] — `frontend/components/sign/sign-flow.tsx:372,402` (minutes)
- 6. Four one-liners: page hint light-codex-cube.tsx:187 → 'A–Z, digits, space, period, - _ • :' via a lexicon key (+ES); {AI_GLYPH} on the AI button sign-flow.tsx:626; repoint :32 to @/lib/codex-pdf and delete lib/codex-strip.ts; replace stampLib/layoutLib casts (:24,26,45-46) with direct imports of initialsRowFrac/initialsSlotWidths; one railOf exported from sign-steps.ts for sign-diag.tsx:17 and sign-flow.tsx:52 [Enlil, Krishna, Odin] — `frontend/components/light-codex-cube.tsx:187, frontend/components/sign/sign-flow.tsx:24-52,626, frontend/lib/codex-strip.ts, frontend/components/sign/sign-diag.tsx:17` (minutes)
- 7. Close the file-name round trip: tmp-core.js GET (:28) adds x-file-name: encodeURIComponent(name); tmpfile.ts:17 prefers decodeURIComponent(x-file-name) with the content-disposition fallback; tests/tmp-core.test.mjs asserts the GET returns 'pagaré-firmado.pdf' exactly [Krishna] — `frontend/tmp-core.js:28, frontend/lib/tmpfile.ts:17, frontend/tests/tmp-core.test.mjs` (minutes)
- 8. Sign & save while auth hydrates: drop auth.isLoading from disabled at sign-flow.tsx:653, key on needLogin, show t('soi.sign.err.auth_loading') as the caption while needLogin && auth.isLoading; SignaturePad emits null only on a user Clear so Clear after a draft resume empties png (:646,:649) [Krishna, Enki] — `frontend/components/sign/sign-flow.tsx:646,649,653, frontend/components/sign/signature-pad.tsx` (minutes)
- 9. Zoom hardening: pending scroll target in a ref applied in useLayoutEffect keyed on zoom instead of rAF (pdf-page-view.tsx:72-89); knockout every non-ink pixel (mean ≥ 200 → alpha 0) so the two thresholds agree (signature-pad.tsx:23-26); live-run steps for double-tap (data-zoom 2.5 then 1) and a CDP two-finger pinch [Enki] — `frontend/components/sign/pdf-page-view.tsx:72-89, frontend/components/sign/signature-pad.tsx:23-26, frontend/scripts/sign-live-run.mjs:158-174` (hour)
- 10. sign-fit scale-free: unit u = max(1, round(W/600)) scales the 3-px bridge (:37,39), ±4/±12 probes (:49) and 4-px minimums (:71,81,82); label band scanned upward from lineY−1 excluding the rule's own rows (:79-82); test at 2× width → equal fractions, test with a left rule + two-line label [Enki] — `frontend/lib/sign-fit.ts:37-82, frontend/tests/sign-fit.test.mjs` (hour)
- 11. Verifier binds each SoITxt mark to its pass: require a row with rowIndex === signerIdx and isoDate === mark.isoDate and (non-genesis) mark.chain === passes[signerIdx].chain; push text_<k>_pass, add the key to ISSUE_KEYS + soi.sign.verify.issue.* (+ES); one tamper test [Odin] — `frontend/lib/sign-verify.ts:27-42, frontend/components/sign/verify-file.tsx, frontend/tests/sign-verify.test.mjs` (hour)
- 12. Proofs and process: C2 live proof (Alex's slot at the right edge, Daniel's slot1.x + w + gap === slot0.x); pausedGate(env) helper + test that /api/notify, /api/tmp and checkout return 503 when paused; read-only RPC probe of sign_envelope_get in verify-live.yml (036 ✓/✗ on the deploy line); strict next lint --max-warnings=0 over components/sign + lib/sign-*.ts in test:ci [Krishna, Enlil, Odin] — `frontend/scripts/sign-live-run.mjs, frontend/worker.js:50-61, .github/workflows/verify-live.yml, frontend/package.json:59,87` (hour)

### MoT-3 — pod 3 — READY (333 words)

Four masters read the same tree and agree. Pangu, Sofia, Thoth and Thor each return ready with fixes, and their fixes overlap so heavily that the pod owes the plan one list.

What is applied and proven in the code. The three Light Codex symbols sit at codex line 30 as VCVC, VRVR and VYVY, with no green and no collision with any taken group. Zoom is real: pinch, double tap, the minus, percent, plus and snap buttons at 44 pixels, arrow nudges of a quarter percent, and marks kept as page fractions so alignment survives four times magnification. The initials row now grows leftward from the first signer, 558, 518 and 478 points on Letter, and the stamp test at line 73 proves it. All sixteen new lexicon keys have Spanish twins. The codex timestamp is never cut. Thor found no secret in any file, no XSS path, every API route behind the pause switch, and the hand off token is 128 random bits that die in a day.

What the tree still contradicts. The codex tests and two live run regexes expect a space and a dot joiner while the code writes an underscore and a bullet, so the suite is red by construction until five strings change. The colon symbol is neither persisted as an ask nor encoded, and the stamp still prints a full stop for it. The flow measures the document's text height in the fit and then ignores it, capping every date at a fixed 16 points. The Light Codex page hint still names four characters in hardcoded English. clampBox grows a short box downward off its rule.

Thor's door. ship.sh never runs envcheck and next build reads a local env file, so a laptop can ship the creator login off, and missing Auth0 keys turn login off silently. Close both in envcheck before the push.

Verdict. Ready to execute tonight. Land the test strings and the colon first, then textH, then Thor's build guard, then ship.

Priorities:
- 1. Codex tests + live-run regexes still expect the OLD line (space + ' . ' join) while lib/codex-pdf.ts:65 writes '_' and :74 joins ' • ' — suite red by construction. VERIFIED: tests/codex-pdf.test.mjs:15-18,21-26,37 (e.g. 'ALEX SEGUIN 2026.09.07 18.02CDT', 'A . B'); scripts/sign-live-run.mjs:299,317 (/\d\d\.\d\d[A-Z]{2,5} \. DANIEL/). Fix: update expected strings to the '_' form and ' • ' join; live-run regex class [.:] after the colon lands; add a case that unsupportedChars('- _ • :') is empty. Also refresh docs/asks/2026-09-08_0150_codex_date_format.md:6-7 ('no underscore, hyphen or colon' is now false). [Pangu, Sofia, Thoth] — `/home/user/eXeL-AI-Polling/frontend/tests/codex-pdf.test.mjs:15-18,21-26,37 + /home/user/eXeL-AI-Polling/frontend/scripts/sign-live-run.mjs:299,317` (minutes)
- 2. Colon ':' = VBVB NOT applied and the 17:1x ask NOT persisted (PERSIST FIRST). VERIFIED: lib/light-codex.ts:30 has only '-' VCVC, '_' VRVR, '•' VYVY; lib/codex-pdf.ts:65 `${hour}.${minute}` and :66 fallback likewise print a full stop; docs/asks/ latest file is 2026-09-08_1721_live_signed_pdfs_light_codex.md — no colon ask. VBVB/BVBV unused (NUMBERS BBBB/WBBB/VBBB, ALPHA BBBW/WBWB). Fix: write docs/asks/2026-09-08_171x_codex_colon.md and commit alone; add `":": "VBVB"`; change both stamp branches to `${hour}:${minute}` → 'ALEX SEGUIN 2026.09.08_08:11CST' (still 19 chars, ≤ 44 holds); then rank-1 tests. [Pangu, Sofia, Thoth] — `/home/user/eXeL-AI-Polling/frontend/lib/light-codex.ts:30 + /home/user/eXeL-AI-Polling/frontend/lib/codex-pdf.ts:65-66 + /home/user/eXeL-AI-Polling/docs/asks/` (minutes)
- 3. Text marks NOT sized to the document: FitBox.textH is measured (lib/sign-fit.ts:80-83) but never consumed. VERIFIED: sign-flow.tsx:333 `capped = { h: Math.min(f.h, TXT_H), … }`, :415 `dateH = dateFit ? Math.min(dateFit.h, TXT_H) : TXT_H`; grep 'textH' in components/sign returns nothing. Fix: `const h = f.textH ? Math.min(0.03, Math.max(MIN_H, f.textH * 1.9)) : Math.min(f.h, TXT_H); y = f.y + f.h - h` in addText and the date holder (export MIN_H from pdf-page-view.tsx:25); live-run step asserting date box ≈ 1.9× the label ink. [Pangu, Thoth] — `/home/user/eXeL-AI-Polling/frontend/components/sign/sign-flow.tsx:333-334,415-416` (hour)
- 4. NO_AUTH refusal is CI-only and Auth0 absence silently disables login. VERIFIED: scripts/ship.sh:30-40 runs tsc → test:ci → build with no envcheck call (grep 'envcheck' hits only .github/workflows/deploy.yml:11,74); next.config.js has no NO_AUTH throw; scripts/envcheck.mjs:20-24 lists NEXT_PUBLIC_AUTH0_DOMAIN/CLIENT_ID as OPTIONAL while app/soi-session/sign/page.tsx:28 `AUTH_OFF = !AUTH0_DOMAIN || !AUTH0_CLIENT_ID || NO_AUTH === '1'`. Fix: throw in next.config.js when NO_AUTH==='1' and CI/WORKERS_CI_COMMIT_SHA set; call `node scripts/envcheck.mjs` in ship.sh before build; promote the two Auth0 vars to REQUIRED; page treats missing Auth0 as 'login unavailable' (disable Sign & save), not bypass. [Thor] — `/home/user/eXeL-AI-Polling/frontend/scripts/ship.sh:30-40 + /home/user/eXeL-AI-Polling/frontend/scripts/envcheck.mjs:20-24,44-48 + /home/user/eXeL-AI-Polling/frontend/app/soi-session/sign/page.tsx:28` (minutes)
- 5. Light Codex page hint hardcoded English and stale: 'A–Z, digits, space, period.' — file untouched in this tree (not in git status). VERIFIED: components/light-codex-cube.tsx:187 (+ :183 label, :185 placeholder, :189 'Unsupported:' all hardcoded). Fix: lexicon key soi.codex.chars 'A–Z, digits, space, period, - _ • :' (+ES) or derive from Object.keys(ALPHA); route label/placeholder/unsupported through t(). [Pangu, Sofia] — `/home/user/eXeL-AI-Polling/frontend/components/light-codex-cube.tsx:183-189` (minutes)
- 6. clampBox grows a sub-MIN_H box DOWNWARD (h raised, y kept) so the baseline leaves the rule; plan B5 says bottom pinned. VERIFIED: pdf-page-view.tsx:92 clamps h to MIN_H then y to [0, 1−h] with no bottom = y+h preservation (contrast :155 resize, which does pin the bottom). Fix: bottom = m.y+m.h; h = clamp(m.h, MIN_H, 1); y = clamp(bottom − h, 0, 1 − h); same for the AI box in sign-flow. [Thoth] — `/home/user/eXeL-AI-Polling/frontend/components/sign/pdf-page-view.tsx:92` (minutes)
- 7. Hidden codex line and caption can disagree, and the slot-scan band uses default widths only. VERIFIED: sign-flow.tsx:383 allRows = {rowIndex, name, isoDate, hash} — no `contact`, so codexText's captionName fallback (codex-pdf.ts:42-46) falls to 'SIGNER N' while the caption prints the contact; :389 and :393 call slotWidthsOf(total) with no mine/recorded aspect before rowFracOf. Fix: add contact (signers[rowIndex]?.contact / pub roster) to allRows; pass { idx: myRow, aspect } + recorded SoIInitW widths into initialsSlotWidths; test with contact in codex-pdf.test.mjs. [Thoth] — `/home/user/eXeL-AI-Polling/frontend/components/sign/sign-flow.tsx:383,389-393` (minutes)
- 8. Flow leftovers with keys but no UI (B1/B2/B4/B16). VERIFIED: sign-flow.tsx:674 `new Date(tmpLink.expires).toLocaleString()` ignores activeLocale (dateIn helper at :54 exists); AI_GLYPH imported sign-flow.tsx:19 and never rendered; t('soi.sign.check_again') and t('soi.sign.x.error') (lexicon-data.ts:1028,1037) appear nowhere in components/sign; lexicon-data.ts:1040 zoom_reset englishDefault 'Fit the page' while the control shows NN% and returns to 1×. Fix: dateIn(activeLocale, …, true) for expiry; ↻ Check again button calling refreshEnvelope (data-testid check-again); x.error sentence in diag when step==='error'; {AI_GLYPH} prefix on the AI find button; rename zoom_reset to 'Zoom 100 %' (+ES). [Sofia] — `/home/user/eXeL-AI-Polling/frontend/components/sign/sign-flow.tsx:19,674 + /home/user/eXeL-AI-Polling/frontend/lib/lexicon-data.ts:1040` (minutes)
- 9. Stored PDF served inline from the site origin and no rate limit on POST /api/tmp. VERIFIED: tmp-core.js:29 `content-disposition: inline; filename=…` (no nosniff/CSP sandbox); :32-42 POST has Origin check, %PDF- magic, 12 MB cap, but no per-IP throttle. Fix: attachment + x-content-type-options: nosniff + content-security-policy: sandbox on GET (update tests/tmp-core.test.mjs header asserts); Cloudflare rate-limit rule or KV counter → 429 on POST; document in tmp-core header; label verify 'witnessed' line 'name as recorded' (verify-file.tsx:33). [Thor] — `/home/user/eXeL-AI-Polling/frontend/tmp-core.js:29,32-42` (hour)
- 10. Pinch and double-tap have no proof, and setZoom anchors on a guessed aspect. VERIFIED: sign-live-run.mjs:158-174 exercises only zoom-in button / snap / reset; pdf-page-view.tsx:80-86 `ratio = sc.scrollHeight / w0` (wrong mid re-render), :108-123 two-touch + double-tap untested. Fix: Playwright hasTouch step with two dispatchTouch events asserting data-zoom > 1 and sig-box fractions unchanged; ratio from the page viewport h/w; 2c-zoom shot at 375 px. [Pangu] — `/home/user/eXeL-AI-Polling/frontend/components/sign/pdf-page-view.tsx:80-86,108-123 + /home/user/eXeL-AI-Polling/frontend/scripts/sign-live-run.mjs:158-174` (hour)
- 11. Owed, report not build tonight: (a) hidden helix is a bytes-only carrier at 0.25 pt y=0 (pdf-stamp.ts embedCodexImage) — a printed copy carries no codex; state 'digital file only' in the consent sentence or add a print-scale carrier later (fleet b0ihcx3e0.txt:39); (b) 32-language SIGN_TRANSLATIONS lack the 4 short aria keys zoom_in/out/reset/snap_line (fallback to English); (c) 'Signer N' inside the PDF is English by design until a Unicode font ships; (d) plan C2 text still cites the OLD formula at pdf-stamp.ts:228 — tree already carries the reversed one at :222, tests :73-79. [Pangu, Sofia, Thoth] — `/home/user/eXeL-AI-Polling/frontend/lib/pdf-stamp.ts:222 + /home/user/eXeL-AI-Polling/frontend/lib/lexicon-translations-sign.ts + /root/.claude/plans/do-me-a-fsvor-pure-wirth.md (C2)` (day)
