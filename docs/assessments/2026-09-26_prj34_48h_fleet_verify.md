# PRJ-34 — 48-Agent Fleet Verification of the Last-48h Work (rev 0.130)

**HEAD** `89d740b` · **project.revision** `0.130` · both refs `main` + `claude/debug-wsl-issues-yYdPP` equal · run `wf_b9ced55f-973` (task wydr08x74).

Operator ask (2026-09-26, ×2): *"ensure all tasks in last 48 hrs are verified by 48 agent fleet."* A READ-ONLY fleet — 12 reviewer lenses × (constructive A + adversarial B) → 12 per-lens syntheses → 12 MoT cross-cutting coordinators = 48 agents, on Opus 4.8 — verified, by inspection + git/file evidence (never running a build/test), the 12 claim-areas of the last-48h shipped work: the 99-round programme (0.008→0.105), per-slide S1→S19, the Concept-G1 reframe, the 3 visionary passes (0.128→0.130), the R-CORE badge + traceability panel + Stage-1/2 mounts, drone r.150–r.152, the Pod fit-law/exports, the CI door (W1/W2), asks-persisted, and records/determinism. This is the verification record.

## Overall verdict

The **core is real and gate-enforced**; the gaps are **records-hygiene and weak-guard classes, not shipped user-facing logic errors**. Grades: eleven B/B+/B−, one A− (visionary thesis).

Grade spread: B+, B+, B+, B+, B-, B+, B, B+, A-, B+, B+, B+

| Lens | Grade | Confirmed | Failed | Gaps | One-line verdict |
|---|:--:|:--:|:--:|:--:|---|
| Aset | B+ | 6 | 1 | 2 | Consistency is gate-enforced almost everywhere (drone chain-hash, byte-identical play.html, append-only D/iteration ledgers). But the flagship R-CORE 'trust the record' panel on SoI ships 3 revs stale (gen 0.127 vs source/footer 0.130) b… |
| Asar | B+ | 7 | 1 | 3 | Arc holds end-to-end: append-only 0.001→0.130, D1→D132, twelve-lens+SSSES~99, both refs at 89d740b, disciplined visionary passes, no test edited. One real MEDIUM gap: the SoI R-CORE traceability panel it built is 3 revs stale (ends 0.127… |
| Athena | B+ | 9 | 0 | 6 | Gates are overwhelmingly real and assert their claims: byte-identical drone sha256, html-export 20/0/0, gate==='G2' with no test edited, one-ninth + IA/declared, both remote refs moved. No claim fails under scrutiny. B's 'SSSES ~96' rebu… |
| Christo | B+ | 6 | 1 | 1 | Surfaces cohere strongly (exact-raster badge, 127-rev D#/sha/diff panel, byte-identical drone deck, clean G1 reframe). One real MEDIUM issue, broader than either reviewer saw: the R-CORE compare 'What changed' diff leaks model names ('Gr… |
| Enki | B- | 6 | 1 | 2 | Core well-gated. A's headline 'PRJ-34 ai⊇hi ungated' is FALSE: data.ts:3404 merges the DRS deck into SLIDE_SEED and PRJ-34∈DEMO_PROJECTS, so the loop gates emptied/shorter-string cells. Real gaps: iterations[] dup n122-124 + missing 100-… |
| Enlil | B+ | 9 | 1 | 2 | Build/gate machinery is genuinely honest: byte-identical deck sha256, chain-hashed append-only ledgers, honest gate/ship split, and no-test-edited claims all verified on real evidence. One real medium gap: R-CORE SoI panel ships rev 0.12… |
| Krishna | B | 6 | 2 | 2 | Integration is genuinely strong: 7-surface badge as last child, RGBA rasters as-is, Pod gate stays G2 (no test edited), drone deck byte-identical with a real chain hash — 6 areas confirmed. But the headline SoI R-CORE panel is 3 revs sta… |
| Odin | B+ | 11 | 2 | 3 | Durability core is real and gate-enforced: drone REVISIONS chain-hash, drs.json 0 mutations, byte-identical r.152, gate stays G2, no test edited. But two invariants genuinely fail — 'Opus 4.8' model id sits in shipped artifacts (not just… |
| Pangu | A- | 9 | 1 | 1 | Thesis sound & law-abiding: money DECLARED/IA never buyer-validated, EDU/FOOD/TECH primary, one-ninth gate + no-retired-names CI-enforced, crisis-upside is coordination value not speculation, no test edited. Real gaps are hygiene: vision… |
| Sofia | B+ | 8 | 2 | 1 | Money honesty is exemplary, machine-enforced (IA/DECLARED, never buyer-validated); Concept reframe and master statement hold for all three audiences. But S2 status self-contradicts: one cell shows rev 0.107 AND 0.130, and register D1–D10… |
| Thoth | B+ | 8 | 2 | 2 | Data spine strong: determinism reproduced (drone chain-hash + play.html byte-identity), ledgers clean (D1-D132, revs 0.001-0.130), SSSES honest (66.3→99.78, null setup rounds not inflated). But two real defects: iterations[] n-counter is… |
| Thor | B+ | 4 | 1 | 2 | B+. Honesty engineering exemplary (area 10: no-fallback LIVE SHA probe, spiral9 null-not-inflated, gate/ship SKIPPED-grey, 403≠shipped, no secret leaks; areas 1/3/4 hold). One real overclaim survives: 'NO model identifier in any repo art… |

## Consolidated real issues (51 deduped: 1 high, 18 medium, 31 low)

### High + medium (the fix list)

- **[HIGH]** (Aset · 6 · R-CORE traceability panel (SoI) freshness) The shipped R-CORE 'trust the record' Version-History panel on SoI tops at rev 0.127 while the DRS source and the deck's own PROV footer are at 0.130 — a visible cross-surface contradiction on the flagship traceability feature, and the gate cannot catch it.
  - _Evidence:_ frontend/lib/2525-core/soi-drs-revisions.gen.ts = 127 entries ending 0.127 (last commit 'PENDING'; docstring literally 'SoI-2525 DRS revision record — 127 revisions'). docs/drs/drs.v00.00.json = 130 revisions ending 0.130 (visionary passes 0.128-0.130 = D130-D132), appended but gen-rcore-revisions.mjs never re-run. innovation-slide-seed-drs.ts:36 PROV footer reads 'rev 0.130'. SoI mounts RCoreBadge from SOI_DRS_REVISIONS (rcore-revisions.test.mjs:97), so the stale panel ships. Gate rcore-revisions.test.mjs:76 asserts only soiH.revisions.length>=20 — no source-freshness check. Reviewer A missed this (checked append-only/byte-identical, not gen freshness).
- **[MEDIUM]** (Aset · 11 · Asks persisted + .sha256 (PERSIST-FIRST)) 4 of 22 operator asks dated 2026-09-24..26 have no .sha256 integrity sidecar, breaking the stated persist-first stamp discipline — precisely on two of the most consequential recent asks that drove shipped 48h work.
  - _Evidence:_ Confirmed missing sidecars (the .md exists, no .md.sha256) for docs/asks/2026-09-25_rcore_exact_icons.md, docs/asks/2026-09-25_rcore_traceability_and_3_visionary_passes.md, docs/asks/2026-09-26_drone_tab_name.md, docs/asks/2026-09-26_rcore_compare_mirror_vision2525.md. Flagged independently by both reviewers. Persist-FIRST ordering itself holds and a sampled hash verifies OK; only the stamp is absent on the newest asks.
- **[MEDIUM]** (Asar · 6 · R-CORE SoI traceability panel (00c7073)) The R-CORE version-history panel the arc built for 'total traceability' is 3 revisions stale: it ends at rev 0.127 and omits the three visionary revisions 0.128–0.130 (decisions D130–D132) that are live in the shipped deck. Its gate uses a permissive floor and cannot detect the staleness, so the traceability tool silently fails to reflect the arc's own final work.
  - _Evidence:_ frontend/lib/2525-core/soi-drs-revisions.gen.ts last entry is rev 0.127 (commit 'PENDING'), last touched in 00c7073 which PRECEDES visionary commits ee9e848(0.128)/7789934(0.129)/89d740b(0.130). docs/drs/drs.v00.00.json is at project rev 0.130 with decisions to D132. rcore-revisions.test.mjs:76 asserts only soiH.revisions.length>=20 — 127>=20 stays green while the ledger is stale, giving false assurance of completeness.
- **[MEDIUM]** (Athena · 6 · R-CORE traceability panel (00c7073)) The gate under-asserts the headline claim: it checks the SoI ledger has >= 20 revisions (not == 127) and never asserts the panel renders per-revision D# citations. The 127-revision ledger could silently shrink to 21, or lose D# extraction, and the gate would still pass green. Feature is genuinely present; the guard is weaker than the claim.
  - _Evidence:_ frontend/tests/rcore-revisions.test.mjs:76 `ok(soiH.revisions.length >= 20 ...)` — only a floor, not the 127 count carried by soi-drs-revisions.gen.ts; the test asserts the panel *uses* compareRevisions but has no assertion on decisionsOf()/D# chip rendering (rcore-revision-panel.tsx:42-44,238). Both reviewers independently flagged this.
- **[MEDIUM]** (Christo · R-CORE compare panel — model-identifier leak (drone + flagship SoI)) The 'no model identifier in any repo artifact' invariant is broken on user-facing surfaces. The R-CORE compare 'What changed' view renders the full revision detail text as a word-diff, exposing model names to any user who compares a window crossing the affected revisions. Broader than reviewer A found: the leak is on the FLAGSHIP SoI-2525 surface as well as the drone surface.
  - _Evidence:_ rcore-revision-panel.tsx:177-193 renders cmp.sideBySide.left/right via dangerouslySetInnerHTML (word-diff of full detail). detail = full text (revisions.ts:70 fromLedgerJson; revisions.ts:88 fromDrsRevisions maps why->detail); compareRevisions diffs from.detail vs to.detail (revisions.ts:186). DRONE: drone-ledger.gen.ts rev 0.012 (line 143 'Grok' x2), 0.013 (line 150), 0.015 (line 164 'Model changed to Opus 4.8 mid-round' + 'Grok'), wired at command-ux1.tsx:47 (fromLedgerJson(DRONE_LEDGER)) and mounted :147. SoI (NEITHER reviewer caught): soi-drs-revisions.gen.ts:559 rev 0.079 why='...the v0.9 narrative (Grok)...', wired at page.tsx:75 (historyOf + fromDrsRevisions(SOI_DRS_REVISIONS)) and mounted :1100. Deck CELLS (innovation-data.ts, innovation-slide-seed-drs.ts) are clean — grep for GPT/Claude/Gemini/Llama/Mistral/OpenAI/Anthropic = 0 — that half holds; the leak is only in the ledgers that feed the compare tool.
- **[MEDIUM]** (Enki — diversity & edge cases (edge cases / omissions / untested paths) · 1/12 — DRS iterations[] append-only integrity (untested path)) iterations[] has duplicate + non-monotonic + missing round IDs and no gate catches it, producing a self-contradicting rendered ledger.
  - _Evidence:_ docs/drs/drs.v00.00.json iterations (len 124): n=122,123,124 each appear TWICE (visionary rev 0.128/0.129/0.130 reused n122-124), and n=100-105 are MISSING (jump 99->106). Only decisions are gated D1..Dn (scripts/drs-render.mjs:292); iterations are validated by revision-existence + file/edits only (drs-render.mjs:293) and innovation-time.test.mjs:378 asserts only iterations.length>=1. drs-render.mjs:59 hardcodes 'of 99' so DE-RISKING_STRATEGIES_GATE_REVIEW.md:204 renders 'Iteration ledger (124 of 99)' with duplicate final rows.
- **[MEDIUM]** (Enki — diversity & edge cases (edge cases / omissions / untested paths) · 11 — asks persisted (+ .sha256) PERSIST FIRST (omission)) 4 of 22 in-window operator asks (2 of which drove shipped work) have no .sha256 sidecar, and no gate enforces the ask+hash convention.
  - _Evidence:_ Missing sidecar (either naming form): docs/asks/2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md. exact_icons drove the badge; traceability_and_3_visionary drove the panel/compare. No tests/scripts/.github rule checks docs/asks + .sha256 (grep shows only exec-summary/drs-source hashes, no ask-sidecar gate). The .md persist-first itself holds. NOTE: reviewer A's count of 13 is wrong (A missed the .sha256-only sidecars); actual = 4 (B correct).
- **[MEDIUM]** (Enlil · 6 · R-CORE SoI traceability panel (staleness, ungated)) The shipped SoI-2525 traceability panel is stale and no build/gate catches it. frontend/lib/2525-core/soi-drs-revisions.gen.ts tops out at rev 0.127 (127 entries), yet the source docs/drs/drs.v00.00.json holds 130 revisions ending 0.130 and every PRJ-34 slide provenance reads rev 0.130 — so the tool whose sole purpose is revision traceability silently omits the last 3 shipped revisions (D130-D132, the three visionary passes). Reviewer A's 'full ledger' framing is false at HEAD; A verified 127 present but never compared against the live source.
  - _Evidence:_ grep -c '"revision":' frontend/lib/2525-core/soi-drs-revisions.gen.ts = 127, last value 0.127; file header line 1 'GENERATED ... DO NOT EDIT BY HAND'. python json parse of docs/drs/drs.v00.00.json = 130 revisions, last 0.130, project.revision 0.130. frontend/lib/innovation-data.ts:1147 provenance 'rev 0.130'. Generator gen-rcore-revisions.mjs is absent from frontend/package.json predev/prebuild (:7,:9), test:ci (:177), and .github/ — so the committed gen file is authoritative and drifts freely. Gate is too weak: frontend/tests/rcore-revisions.test.mjs:76 asserts only 'soiH.revisions.length >= 20', never == source count or == HEAD, so the drift passes green.
- **[MEDIUM]** (Krishna · 6/7 · R-CORE SoI traceability panel (primary surface)) The SoI R-CORE 'full ledger' panel ships 3 revisions stale and no CI gate catches the drift, so the central traceability promise fails on the primary surface at HEAD.
  - _Evidence:_ frontend/lib/2525-core/soi-drs-revisions.gen.ts = 127 entries (last rev 0.127, commit "PENDING", docstring 'DRS revision record — 127 revisions'); docs/drs/drs.v00.00.json runs to rev 0.130 / decisions D132 (project.revision 0.130) after the 3 visionary passes. gen-rcore-revisions.mjs is in NO predev/prebuild/build script (frontend/package.json:7-9), so the stale committed gen imports into SoI-2525/page.tsx and ships. test:rcore-revisions IS in test:ci but rcore-revisions.test.mjs:76 only asserts soiH.revisions.length >= 20, so 127-vs-130 passes green. 0.128-0.130 (D130-D132) omitted from the SoI panel. Both reviewers concur (A GAP / B FAILED).
- **[MEDIUM]** (Odin · 12 · no model identifier in any repo artifact) Authorship model id 'Opus 4.8' is embedded in tracked, shipped artifacts, not only git commit trailers — the stated invariant is literally false and no gate prevents it.
  - _Evidence:_ frontend/lib/2525-core/drone-ledger.gen.ts:164 and docs/traceability/drone-2525.ledger.json:156 both read 'Model changed to Opus 4.8 mid-round'; docs/asks/2026-09-16_demo_scale_fleet.md:3 reads 'commit attribution → Claude Opus 4.8'. drone-ledger.gen.ts was regenerated during the 48h R-CORE work (commit 04553d4). Nuance holds: these are historical AAR/process notes, not deck cells (slide cells scan clean), and app model configs (claude-opus-5) are legit product code — but the 'only commit trailers' claim fails.
- **[MEDIUM]** (Odin · 11 · asks persisted (+ .sha256) BEFORE the work) The '+ .sha256' half of PERSIST-FIRST lapsed on the 4 newest R-CORE/drone/visionary asks — the .md ships but the integrity sidecar was never written.
  - _Evidence:_ docs/asks: 22 .md vs 18 .sha256 across 2026-09-24/25/26; no sidecar for 2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md (verified by ls). Persist-first ORDERING itself held where sampled (ask committed seconds before its work); only the hash discipline failed, precisely on the newest asks.
- **[MEDIUM]** (Odin · 7 · R-CORE per-surface ledger append-only) Append-only for docs/traceability/*.ledger.json is convention, not enforced — an existing entry's text can be edited undetected; the test's 'replay(HEAD)' assertion is a tautology, not a hash/replay check.
  - _Evidence:_ frontend/tests/traceability.test.mjs checks only monotonic revs (l.25/29), kind set (l.26/30) and release-commit-exists (l.27/31). Lines 32-33 filter entries rev<=length and compare to length — always true under monotonicity, zero content verification. Demonstrated: drone-2525.ledger.json historical commit fields were rewritten in commit ae98da2 (removed non-empty commits 7b0b790/2d5eb24/d394dc9/…) with no gate catching it. Contrast drone REVISIONS.md, which drone-revisions.test.mjs actually recomputes as a chain-hash, so its deck bytes ARE protected.
- **[MEDIUM]** (Pangu · 11 - ask persistence / traceability of the visionary work) The ask that GOVERNS the 3 visionary passes has no .sha256 sidecar, so its content hash is unverifiable; at least three other asks in the same 2026-09-25/26 window are likewise missing sidecars, while neighbouring asks in that window do carry them. This weakens the traceability chain around the visionary content, though the ask itself was persisted before the work ran.
  - _Evidence:_ docs/asks/2026-09-25_rcore_traceability_and_3_visionary_passes.md (cited by D130-D132) has no .md.sha256; same for 2026-09-25_rcore_exact_icons.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md. Dir listing: 42 .sha256 vs 118 .md; sibling asks like 2026-09-25_prj34_perslide_s1_s19.md.sha256 exist. Mitigant: PERSIST-FIRST holds - the .md was committed (0282c84, 2026-09-25) before pass 1 (ee9e848, 2026-09-26 01:58).
- **[MEDIUM]** (Sofia — multi-audience wisdom (founder / investor / future-customer) · 3/2 — S2 status slide reader consistency (all three audiences)) The single 'where we are now' status slide self-contradicts: one ai cell shows both rev 0.107 and rev 0.130, and its decision register 'D1–D109' is 23 decisions stale versus the real ledger (D132). This directly dents the 'single meticulous human-governed record' story the deck sells to founder, investor and future-customer alike.
  - _Evidence:_ innovation-slide-seed-drs.ts:84-85 hard-code 'rev 0.107 · decision register D1–D109' in both hi and ai; the ai cell then appends PROV (line 36) = '...v1.0 · rev 0.130' — so ONE cell prints both revision numbers. docs/drs/drs.v00.00.json decisions length = 132, last id = D132. grep confirms lines 84/85 are the ONLY decision-register statement in the seed. Reviewer A (grade A) missed this; B's FAILED holds against A and the evidence.
- **[MEDIUM]** (Thoth (data & analytics — numbers, SSSES trend, ledger counts D1-D132, determinism, revision math) · 4/12 · iterations[] n-counter) The iterations[] n field is internally inconsistent across three conventions and is NOT a usable iteration count. Rounds 1-99 use n=index+1 (revs 0.007-0.105); per-slide/G1 (0.106-0.127) switches to n=rev*1000, so n jumps 99→106 and n=100-105 never exist; visionary passes (0.128/0.129/0.130) revert to index-based n=122/123/124, duplicating the n already used at 0.122/0.123/0.124. Result: n is non-monotonic, three n values appear twice, and max n=127 for only 124 array entries. Reviewer A missed this entirely.
  - _Evidence:_ drs.v00.00.json .iterations: length=124; duplicate n = {122,123,124} (each appears twice: at revs 0.122/0.128, 0.123/0.129, 0.124/0.130); max(n)=127; strict-monotonic check fails at prev=127→cur=122. Commit 89d740b stamps 'append iterations[] 0.130 (n 124)', so the collision is deliberate. decisions[]/revisions[] themselves are clean, so this is isolated to the n field.
- **[MEDIUM]** (Thoth (data & analytics — numbers, SSSES trend, ledger counts D1-D132, determinism, revision math) · 12/7 · 'no model identifier' invariant) The claim 'no model identifier in any repo artifact (only commit trailers)' is false. A model name is written into a ledger DATA file as narration, and Grok/Gemini appear as provenance across many data files. No test/script gate forbids model identifiers, so the invariant is both unenforced and violated. Nuance: the ENFORCED gate only bars product names in deck cells (which holds); the broad 'anywhere' claim is what fails.
  - _Evidence:_ docs/traceability/drone-2525.ledger.json rev-22 decision text: 'Model changed to Opus 4.8 mid-round.' Grep across docs/ finds Grok/Gemini in drs.v00.00.json, drone ledger, create-doc.ledger.json:6, MODE_R-CORE_SPEC.md:14, CUBE docs. innovation-time.test.mjs:430 only forbids GridOS/Veoci PRODUCT names in deck cells; grep of tests/scripts found no model-name gate.
- **[MEDIUM]** (Thoth (data & analytics — numbers, SSSES trend, ledger counts D1-D132, determinism, revision math) · 11 · operator asks persist-first (.md + .sha256)) The '+ .sha256' half of the persist-first invariant is only partly satisfied. Every ask has its .md, but most lack the checksum companion, so the tamper-evidence the invariant promises is absent for the majority of asks.
  - _Evidence:_ docs/asks: 118 *.md but only 42 *.sha256. In the 2026-09-24..26 work window, all 22 asks have .md yet 13 of 22 have NO .sha256 (e.g. 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md).
- **[MEDIUM]** (Thoth (data & analytics — numbers, SSSES trend, ledger counts D1-D132, determinism, revision math) · 1 · per-round gate cadence) The 'each round gated (tsc·drs-render·drs-crs·innovation-time) + full test:ci+build every 11th' cadence is asserted but not verifiable from the record — no gate log in commit bodies or DRS iteration entries.
  - _Evidence:_ Round-88 commit 7edde19 body carries no tsc/drs-render/drs-crs/test:ci line ('gate' there is decision prose D96). Round-99 82e02bf says 'FULL gate' as prose only. DRS iterations[] entries have no gate field. Cannot confirm or refute by inspection.
- **[MEDIUM]** (Thor · 12 · no model identifier in any repo artifact) OVERCLAIM survives adjudication: 'NO model identifier in ANY repo artifact (only commit trailers)' is literally false AND model-version names reach RENDERED surfaces. A and B both flag it; the A-vs-B dispute (A: renders on live surface; B: rendered surfaces stay clean) resolves in A's favor — B's mitigation only grepped 'Claude Code|Claude Opus|Anthropic' and checked the list/title view, missing the compare word-diff and the strings 'Opus 4.8'/'Grok'. The append-only law locks the offending ledger entry so it cannot be corrected without breaking determinism.
  - _Evidence:_ RENDERED-to-glass (verified): drone-ledger.gen.ts:164 'Model changed to Opus 4.8 mid-round' + 'Grok SSSES/SPIRAL audit' → imported command-ux1.tsx:38, normalized :47, badged :147; R-CORE compare panel renders full detail via dangerouslySetInnerHTML of cmp.sideBySide (rcore-revision-panel.tsx:181-193), built by compareRevisions→diffText(from.detail,to.detail) (revisions.ts:186; detail=text at :70) — so comparing to/from r22 paints 'Opus 4.8'/'Grok' on the live Drone-2525 surface. Deck: 'Grok' shipped in innovation-slide-seed-drs.ts:36 (PROV) + :85 (S1 ai cell). Non-rendered artifacts: drs.v00.00.json:13 author 'eXeL AI (Claude Code)'; docs/drone-2525/operator-deck/REVISIONS.md author col 'Claude Code'; docs/CUBES_4-6.md:620 'claude-opus-4'; assessments 2026-09-07…:361 'claude-opus-5'. Append-only lock: traceability.test.mjs monotonic — the 'Opus 4.8' entry cannot be edited/removed. NOT a secret/credential leak (no security compromise); authoring-model disclosure only → medium, not high.

### Low (hardening / hygiene — listed, not all fixed this pass)

- [LOW] (Aset) The badge docstring claims a 'Fixed + centred + high z' overlay, contradicting its own earlier lines, the operator instruction, the implementation, and the gate — the badge is mounted last in normal flow at page bottom, never a fixed overlay.
- [LOW] (Asar) The absolute claim 'no model identifier in any repo artifact (only commit trailers)' is falsified: an Anthropic authoring-model id and an xAI product name both appear in committed, R-CORE-rendered artifacts. Intent (keep the authoring model in trailers) mostly holds, but the claim as stated is overstated.
- [LOW] (Asar) Four recent asks lack the sibling .sha256 manifest the ask-record process otherwise maintains, a minor gap in the record-integrity chain for the most recent (some shipped) asks.
- [LOW] (Athena) SSSES scores ('security 99 · stability 100 · ...') are self-authored narrative typed into iteration why-fields; NO test computes or verifies the PRJ-34 business-case SSSES. The scores are ungated. (B's adversarial 'peak aggregate ~96 / ~99 overstates' does NOT hold — B cited mid-programme 0.073; the final round genuinely reaches ~99.6 aggregate. The real issue is ungated, not overstated.)
- [LOW] (Athena) 'No AI-model identifier in the visionary cells' is clean by inspection but UNGATED — no test scans slide cells for model identifiers; it relies on discipline. (B's summary framing 'FALSE in any repo artifact' is a strawman: the claim under test was cells, which are clean; docs/asks naming models is legitimate fleet-attribution, not the cell claim.)
- [LOW] (Athena) The ask -> .sha256 sidecar pairing is ungated, and 4 of the newest asks (0925/0926) ship without a sidecar. Only the drone handoff sha256 is enforced; nothing requires every handoff ask to carry a checkable digest, so a handoff can land unverifiable.
- [LOW] (Athena) The 'replay(HEAD) equals the full ledger' assertion is tautological. It filters entries by rev <= entries.length over ledgers already asserted monotonic 1..n, so the filter always keeps every entry — the equality can never fail. It is a vacuous assertion, not a real replay/reconstruction test.
- [LOW] (Athena) The 'every 11th round runs full test:ci + build' cadence is self-documented in iteration Gates blocks and commit bodies, not independently enforced by any check. A round could skip the full gate and only the prose would show it.
- [LOW] (Christo) The component's own file-header JSDoc contradicts the shipped behavior: it claims a fixed/high-z overlay while the badge actually renders in normal flow as the last child. Coherence/maintainability trap; shipped UX is correct.
- [LOW] (Christo) The claim is that detail moved hi->ai with ai a textual SUPERSET of hi (never a competing edit), but the enforcing test only checks length, not containment. A longer-but-different rewrite would pass the gate while violating the stated coherence invariant. Current practice holds; the gate does not enforce it.
- [LOW] (Christo) The drone surface's browser tab still reads 'eXeL ECO-2525', a user-visible coherence blemish on the drone deck. Explicitly known-open (r.153 not yet shipped) and correctly not-yet-fixed, so an acknowledged open item rather than a hidden regression.
- [LOW] (Enki — diversity & edge cases (edge cases / omissions / untested paths)) The served drone tab title is wrong ('eXeL ECO-2525') and no gate protects the title string, so the wrong value shipped unnoticed.
- [LOW] (Enki — diversity & edge cases (edge cases / omissions / untested paths)) Badge last-child placement per surface and PNG pixel-alpha transparency are not gated, and the component docstring contradicts the shipped behavior.
- [LOW] (Enki — diversity & edge cases (edge cases / omissions / untested paths)) The exact 127-revision count is not gated; a silent regression dropping revisions to any n>=20 would pass.
- [LOW] (Enki — diversity & edge cases (edge cases / omissions / untested paths)) The export gate no-ops to a pass on Chromium-less runners and checks only sheet COUNT, not the exact {S1..S19} set.
- [LOW] (Enki — diversity & edge cases (edge cases / omissions / untested paths)) The ai>=hi length invariant only covers string cells (plus S4 CONOPS arrays); other array/table cells are not element-count checked, so a non-S4 array cell whose ai has fewer elements than hi (non-empty, non-identical) would pass.
- [LOW] (Enki — diversity & edge cases (edge cases / omissions / untested paths)) The universal 'no model identifier' claim is literally false and unenforced; model ids exist in app artifacts.
- [LOW] (Enlil) 4 recent asks (2026-09-25/26) ship with NO .sha256 companion, breaking the ask-integrity convention. Ungated — nothing enforces companion hashes for docs/asks, so this is process/hygiene drift rather than a build failure.
- [LOW] (Krishna) The 6 R-CORE ledgers' 'append-only' is only internal monotonicity — the replay gate is a tautology and there is no chain hash, materially weaker than the drone deck it is compared to.
- [LOW] (Krishna) The 4 most recent operator asks (the R-CORE + drone-tab work) have the .md but no .sha256 integrity sidecar, breaking the persist-first '(+ .sha256)' promise exactly on the latest work.
- [LOW] (Krishna) The universal 'no model identifier in ANY artifact' framing is overstated — the tool name 'Claude Code' is embedded in a committed repo artifact — though the narrower 'no model names in deck cells' claim holds.
- [LOW] (Odin) The final shipped edition never records its own sha (tip carries commit/shipped='PENDING', backfilled only by a later commit), and even the enforced drone chain-hash covers rev|sha|bytes only — the author/text/notes columns of any past entry can be edited without breaking the chain.
- [LOW] (Pangu) The strict 'no model identifier in any repo artifact' claim is falsified: xAI's 'Grok' appears in shipped deck cells and provenance constants, and 'Claude' appears in the DRS ledger author field. This is honest cross-review attribution, not a misleading or unlawful claim, and it does not affect thesis soundness or legality - but it is a real breach of the stated rule. Area-4 scope is clean: the visionary passes themselves added no model name to any cell.
- [LOW] (Pangu) 'By 2525 the record carries the key features of every major crisis offering' is a broad aspirational claim covering territory once explored via now-retired comparators (GridOS/Veoci/YUDU). It is law-abiding - heavily hedged as IA / 2525 horizon, and the retired product names are kept out of the cells and CI-enforced - but the breadth is aspirational rather than evidenced, so 'ALL major NBA' is a mild soundness stretch, not a defect.
- [LOW] (Sofia — multi-audience wisdom (founder / investor / future-customer)) The '+ .sha256' integrity half of the persist-first discipline fails for the 4 most recent asks — including the very ask authorizing the visionary passes and the drone tab-name ask — even though persist-first itself holds.
- [LOW] (Sofia — multi-audience wisdom (founder / investor / future-customer)) The absolute claim 'no model identifier appears in any repo artifact' is false: 'Grok' (an xAI model) is printed in the rendered provenance footer and S2/CONFIRM cells. Nuance holds — it is deliberate external cross-review attribution and the authoring model is never named — so the narrower visionary-cell claim survives.
- [LOW] (Sofia — multi-audience wisdom (founder / investor / future-customer)) The SSSES means are self-graded and converged to the ceiling (4 of 5 pillars = exactly 100.0), so as an investor-facing credibility signal the metric reads as serving itself rather than as an external measure — a wisdom caution, not a data error.
- [LOW] (Sofia — multi-audience wisdom (founder / investor / future-customer)) The 'ai is a true superset of hi' guarantee is enforced only as a length floor for string cells, not as content containment, and 52 cells are exempted — so the gate is weaker than the multi-audience claim (face=point, note=fuller telling) implies.
- [LOW] (Thoth (data & analytics — numbers, SSSES trend, ledger counts D1-D132, determinism, revision math)) rcore-revisions.test.mjs proves compare/normalize DETERMINISM on synthetic fixtures but does NOT assert the panel renders the real full ledger; the 'test proves the 127-revision ledger' framing is overstated (count was accurate at ship commit 00c7073, now 130 by append).
- [LOW] (Thoth (data & analytics — numbers, SSSES trend, ledger counts D1-D132, determinism, revision math)) Two trivial, non-corrupting items: the '99-round span' is mislabeled 0.008→0.105 (that is 98 revs; the 99 rounds actually run 0.007→0.105), and round 96 was committed twice. DRS data integrity is intact in both cases.
- [LOW] (Thor) Claim '(+ .sha256) BEFORE the work' is not uniformly met: 4 operator asks in the 2026-09-24→26 window were persisted WITHOUT the required .sha256 integrity sidecar. PERSIST-FIRST ordering holds (asks committed in docs(asks) commits before the work), but the hash-discipline half of the claim is overstated.
- [INFO] (Krishna) The badge component's header docstring contradicts its own implementation, misdescribing the live normal-flow badge as a fixed overlay.

## MoT cross-cutting coordinators (12)

### MoT 1 — The single biggest RESIDUAL RISK across the 48h body, and is it a real defect or acceptable?
MoT Coordinator 1/12 — Residual-risk verdict (PRJ-34 48h)

BIGGEST RISK (my pick): the flagship R-CORE "trust the record" traceability panel on SoI-2525 is STALE, and no gate can catch it. This is the single most-converged finding (Aset=high, Asar=medium, Athena=medium — three independent lenses on "area 6"), it attacks the exact promise of the feature being sold, and it is certain-to-be-seen (no user interaction required — two contradicting numbers sit on the same surface).

CONFIRMED (real defect, NOT acceptable), evidence:
- frontend/lib/2525-core/soi-drs-revisions.gen.ts: docstring "SoI-2525 DRS revision record — 127 revisions", last entry rev 0.127, commit "PENDING". Only 127 "revision" entries.
- docs/drs/drs.v00.00.json: 130 revisions, decisions to D132 (D130–D132 = the three visionary passes). The gen file is 3 revisions behind its own source.
- frontend/lib/innovation-slide-seed-drs.ts:36 PROV footer already reads "…rev 0.130" — so the deck footer and the traceability panel on the same product contradict each other (0.130 vs 0.127).
- GIT PROOF the generator was never re-run: `git log` shows 00c7073 ("R-CORE traceability … refresh the full 127-revision ledger") is the LAST commit to touch soi-drs-revisions.gen.ts; the three visionary commits ee9e848 (0.128), 7789934 (0.129), 89d740b (0.130) ALL land AFTER it. scripts/gen-rcore-revisions.mjs was not re-run after the visionary revisions were appended.
- Gate cannot catch it: frontend/tests/rcore-revisions.test.mjs:76 asserts only `soiH.revisions.length >= 20`. 127>=20 stays green; the ledger could be stale, or shrink to 21, and the gate passes. It is a floor, not a freshness/equality/D#-rendering check.
- SoI mounts this stale source: app/SoI-2525/page.tsx wires RCoreBadge from fromDrsRevisions(SOI_DRS_REVISIONS) (rcore-revisions.test.mjs:97), so the stale panel ships.

Verdict on this one: REAL DEFECT, but LOW-HARM and TRIVIALLY FIXABLE — re-run `node scripts/gen-rcore-revisions.mjs` and commit. The 3 missing revisions are cosmetic "visionary upside" reframes (no material content lost); the damage is purely the visible self-contradiction on the flagship "total traceability" feature. Must-fix before ship, but it is one command, so once fixed it is no longer residual.

CLOSE RUNNER-UP — and arguably the biggest TRULY-RESIDUAL (irreducible) risk: the "no model identifier in any repo artifact" invariant is FALSIFIED on buyer-facing surfaces via the R-CORE compare tool (Christo=medium; Asar caught it partially). CONFIRMED:
- frontend/components/2525-core/rcore-revision-panel.tsx:177-193 renders the word-diff of the full revision detail via dangerouslySetInnerHTML (cmp.sideBySide.left/right).
- On the FLAGSHIP SoI surface: frontend/lib/2525-core/soi-drs-revisions.gen.ts:559 (rev 0.079 why) contains "…the v0.9 narrative (Grok)…" — any user comparing a window that crosses rev 0.079 sees "Grok" in the diff.
- On the drone surface: frontend/lib/2525-core/drone-ledger.gen.ts:143 & :164 contain "Grok" and, verbatim, "Model changed to Opus 4.8 mid-round" (rev 0.015).
- Deck CELLS are clean (grep of innovation-data.ts / innovation-slide-seed-drs.ts for GPT/Claude/Gemini/Llama/Mistral/OpenAI = 0) — the leak is only in the ledgers that feed the compare tool, and no gate scans rendered diff output for model names.
Why this is the biggest *residual* risk: it exposes competitor/internal model names ("Grok", "Opus 4.8") to a buyer/investor on the product's own traceability tool — directly embarrassing for an AI company and a stated-absolute-invariant violation — AND it is partly IRREDUCIBLE: the drone ledger rev 0.015 line is append-only law-locked and cannot be rewritten, so unlike the freshness bug it cannot simply be cleaned up. Lower probability of trigger (needs a specific compare window) but higher consequence if triggered, and genuinely unmitigable on the drone surface.

How I ranked them: freshness = biggest ISSUE by consensus + certainty-of-being-seen + directness to the headline claim, but fully fixable (hence not truly residual). Model-leak = biggest RESIDUAL risk in the strict sense (remains after mitigation; irreducible under append-only). Both are REAL DEFECTS; neither is acceptable to leave undocumented at handoff. Everything else the lenses raised (asks .sha256 sidecars missing on 4 recent asks, DRS iterations[] dup n=122-124 / missing n=100-105, SSSES/replay/superset/cadence being self-narrated rather than gated) is real but lower — records-hygiene and weak-guard classes, not shipped user-facing contradictions.

Recommendation to the panel: (1) re-run gen-rcore-revisions.mjs and add an equality/freshness assertion (revisions.length === DRS revisions.length, or last-rev match) to close the gate blind spot; (2) scrub/replace model names in the SoI ledger detail and accept the drone-surface leak as a documented irreducible residual (append-only), or suppress model tokens at render time in rcore-revision-panel.tsx before it reaches dangerouslySetInnerHTML.

### MoT 2 — Is every LIVE claim honestly proven (Verify Live Actions on the actual sha), or is anything asserted-not-proven?
MoT coordinator 2/12 — re-checked read-only on HEAD 89d740b (rev 0.130). Answer: MOSTLY YES for the drone deck, but there is one clear "asserted-and-false" LIVE claim plus several "asserted-not-proven" ones. Concrete, with evidence.

=== WHAT IS HONESTLY PROVEN LIVE (CONFIRMED) ===
.github/workflows/verify-live.yml is exactly the "Verify Live Actions" instrument, and it runs on push to main + every 30 min against github.sha (the ACTUAL sha). It proves the drone surface rigorously:
- Root serves the exact HEAD sha, strict stamp match; the "lucky first-7-hex-word" fallback that "matched by luck for a week" was deliberately removed (verify-live.yml:53-86, regex :62-66). A miss is red.
- /drone-2525/ answers 200 AND carries data-drone-ux1 arena markup — not a soft-404 SPA shell (:92-104).
- /drone-2525/play.html carries revision:'0.$REV' AND id="playHud" AND its live sha256 == the REVISIONS.md register r.$REV row (:106-123). This pins the served BYTES to the register hash, defeating a stale edge that carries the same revision string.
- Headless Boot QA against the LIVE deck (npm run test:drone-deck-qa, :125-142).
The file even documents and fixes prior dishonest greens (#1722 red-for-the-wrong-reason since r.131; the week-long lucky fallback). So the DRONE LIVE claims are a model of honest on-sha proof. CONFIRMED.

=== THE REAL GAP — flagship R-CORE traceability is asserted-AND-FALSE, and never live-verified ===
The headline "R-CORE = total traceability / trust the record" LIVE claim on the flagship SoI-2525 surface is neither proven on the sha nor even true in the shipped bytes:
(a) verify-live.yml NEVER probes /SoI-2525/ or the R-CORE panel — only root + /drone-2525/*. There is zero live proof of the flagship surface.
(b) The shipped panel source is 3 revisions STALE: frontend/lib/2525-core/soi-drs-revisions.gen.ts ends at rev "0.127" (commit "PENDING"; docstring line 7 "127 revisions"), last touched in commit 00c7073, which PRECEDES the visionary commits ee9e848(0.128)/7789934(0.129)/89d740b(0.130=HEAD). Meanwhile docs/drs/drs.v00.00.json is at project.revision "0.130" with revisions[] 0.128/0.129/0.130 and decisions D130-D132 (drs.v00.00.json:9,2161-2179,3802-3818). gen-rcore-revisions.mjs was never re-run. The SoI page wires this stale gen file live: frontend/app/SoI-2525/page.tsx:71,75 (fromDrsRevisions(SOI_DRS_REVISIONS)). So the flagship "trust the record" tool ships showing 0.127 while the deck's own PROV footer and DRS are 0.130 — a visible cross-surface contradiction.
(c) The only gate is a vacuous floor: frontend/tests/rcore-revisions.test.mjs:76 asserts soiH.revisions.length >= 20; 127 >= 20 stays green forever regardless of staleness, and nothing asserts the D#-citation render. The claim is asserted, floor-guarded, and false. This is the single clearest asserted-not-proven (indeed asserted-and-false) LIVE claim in the 48h window.

=== SECONDARY "ASSERTED-NOT-PROVEN" ITEMS (CONFIRMED) ===
1) Tautological replay: frontend/tests/traceability.test.mjs:32-33 filters e.rev <= L.entries.length over ledgers already asserted monotonic 1..n (:29), so the filter keeps every entry — replayHead.length === entries.length can NEVER fail. The "replay(HEAD) equals the full ledger" reconstruction claim is vacuously asserted, not proven.
2) "No model identifier in any repo artifact (only commit trailers)" is FALSE on user-facing, compare-fed R-CORE ledgers: soi-drs-revisions.gen.ts:559 (rev 0.079 why "...v0.9 narrative (Grok)...") and drone-ledger.gen.ts:143,164 ("Grok" x2; "Model changed to Opus 4.8 mid-round"). Both feed rcore-revision-panel.tsx:184,192 which renders the full word-diff via dangerouslySetInnerHTML, so a user comparing across those revisions sees the model names — on both the drone AND the flagship SoI surface. (Deck CELLS are clean; the absolute claim is overstated, not the cell claim.)
3) Persist-first .sha256 stamp: 118 asks .md vs 42 .sha256; the 4 newest/most-consequential asks have NO sidecar — docs/asks/2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md. Persist ordering holds; the stamp is asserted but absent, and the pairing is ungated.
4) DRS iterations[] append-only integrity is ungated and violated: n=122,123,124 each appear TWICE and n=100-105 are MISSING (jump 99->106) in drs.v00.00.json; only decisions[] are D1..Dn gated, so drs-render's hardcoded "of 99" yields a self-contradicting "124 of 99" ledger.

=== NOT REAL GAPS (kept honest) ===
- rcore-badge.tsx docstring (:15) "Fixed + centred + high z" contradicts its own :11 and the impl, BUT the actual LIVE behavior (normal-flow, mounted last, centred) IS honestly proven by rcore-revisions.test.mjs:88 (asserts flex w-full justify-center AND NOT /fixed left-1\/2/). Stale comment, correct+proven behavior.
- Drone tab title "eXeL ECO-2525" is an explicitly known-open item deferred to r.153 (REVISIONS.md tail), not a hidden false LIVE claim.

BOTTOM LINE: Verify Live honestly proves the drone deck on the actual sha, even sha256-pinning the served bytes — that half is trustworthy. But it does not touch the flagship SoI/R-CORE surface, and that surface's headline "total traceability" claim is both unproven (floor-only gate, no live probe) and actually stale (gen file 0.127 vs HEAD 0.130). So NO — not every LIVE claim is honestly proven; the flagship R-CORE traceability claim is the concrete asserted-and-false gap, with replay/no-model-names/sha256-stamp/iterations as supporting asserted-not-proven gaps.

### MoT 3 — Are the gates REAL — does each cited test/gate actually assert the behaviour it claims (name one that does, and any that do not)?
MoT coordinator 3/12 — cross-cut: "Are the gates REAL?" Verdict: MIXED. Some cited gates genuinely enforce their claim; several under-assert or are vacuous, so the corresponding headline claims are effectively ungated. All findings below re-verified read-only against the repo.

=== GATES THAT ARE REAL (assert what they claim) ===

1. CONFIRMED REAL — R-CORE badge placement. frontend/tests/rcore-revisions.test.mjs:88 asserts `data-rcore-badge` AND `flex w-full justify-center` AND `!/fixed left-1\/2/`. This genuinely enforces "bottom-of-page, normal flow, centred, never a fixed overlay" — a regression to a fixed overlay flips it red. This is the clean example of a gate that asserts its behaviour. (Caveat: the component's own docstring at frontend/components/2525-core/rcore-badge.tsx contradicts the shipped behaviour — "Fixed + centred + high z" — but the GATE is correct and the shipped render matches the gate, not the stale comment.)

2. CONFIRMED REAL — Decisions register (append-only, contiguous). scripts/drs-render.mjs:292 and frontend/tests/innovation-time.test.mjs:378 assert `decisions.every((x,i)=>x.id===`D${i+1}`)` plus D7.supersededBy===D8. Data-verified: docs/drs/drs.v00.00.json has 132 decisions, IDs D1..D132 fully contiguous — the gate is strict AND the data satisfies it. This is the important contrast to the iterations gap in #3 below: the register right next door IS strictly gated.

3. CONFIRMED REAL — handoff/commit integrity. drone-crs.test.mjs:57-58 assert the ask file exists on disk AND `/^[0-9a-f]{64}$/` sha256; drone-arena.test.mjs:21 asserts length===64; drone-crs.test.mjs:52 asserts each release cites `/^[0-9a-f]{7,40}$/`; traceability.test.mjs:27 actually runs `git cat-file -e <sha>^{commit}` per release (a real existence check). These are genuine gates.

=== GATES THAT DO NOT ASSERT THEIR CLAIM (real gaps) ===

A. CONFIRMED GAP — SoI traceability freshness (the flagship claim). rcore-revisions.test.mjs:76 is only `ok(soiH.revisions.length >= 20)` — a floor, no ==count and no per-D# render assertion. Data-verified stale: frontend/lib/2525-core/soi-drs-revisions.gen.ts ends at rev "0.127" (line 892; docstring line 7 literally "127 revisions"), while the source docs/drs/drs.v00.00.json holds 130 revisions ending 0.130. gen-rcore-revisions.mjs was never re-run after the three visionary passes. The flagship "trust the record" panel ships 3 revisions stale and the gate stays green (127 >= 20). This is the highest-severity real gap and both the Aset and Asar/Athena lenses converge on it.

B. CONFIRMED GAP (vacuous) — traceability "replay(HEAD)". traceability.test.mjs:32-33: `replayHead = L.entries.filter(e => e.rev <= L.entries.length); ok(replayHead.length === L.entries.length)`. Because revs are asserted monotonic 1..n on line 29, every rev is <= n by construction, so the filter never drops an entry and the equality can NEVER fail. It is a tautology, not a replay/reconstruction test.

C. CONFIRMED GAP — iterations[] integrity. drs-render.mjs:293 only checks each iteration.revision exists in revisions[] and has file+edits; innovation-time.test.mjs:378 only checks `iterations.length >= 1`. Neither checks monotonic/unique `n`. Data-verified defect: drs.v00.00.json iterations has n=122,123,124 EACH DUPLICATED (twice) and n=100-105 MISSING (jump 99→106). With drs-render.mjs:59 hardcoding "of 99", the rendered ledger reads "124 of 99" with duplicate final rows — a self-contradicting record the gate cannot catch (Enki's finding, verified).

D. CONFIRMED GAP (under-assertion) — law-6 "ai is a superset of hi". Enforced only by length: innovation-time.test.mjs:1033 `shorterAi.length === 0` (char length) and :1042 `c.ai.length >= c.hi.length` (step count). Never checks containment; a longer-but-different rewrite passes. The test comment at :1024-1032 openly admits the strict ai⊋hi invariant was relaxed and permits up to 52 cells where ai===hi.

E. CONFIRMED GAP (ungated + falsified) — "no model identifier in any repo artifact". No test scans cells or artifacts for model ids; innovation-time.test.mjs:430 only bars `/GridOS|Veoci/`. A full grep of frontend/tests finds model names only in ai-core.test.mjs (legitimate provider-config tests: openai/gemini/grok/claude), never as a no-model-id invariant. Meanwhile model ids DO render on user-facing R-CORE compare surfaces (drone-ledger.gen.ts rev 0.015 "Model changed to Opus 4.8 mid-round"; soi-drs-revisions.gen.ts rev 0.079 "(Grok)"). Claim is both ungated and false on shipped surfaces. (Deck CELLS are clean — that half holds — but the claim as stated is overbroad.)

F. CONFIRMED GAP — PRJ-34 deck SSSES is ungated. SSSES is computed/asserted only for architect-planning.spiral.mjs, architect-project.test.mjs, and pod-invariant.test.mjs (as a boolean condition), and was explicitly REMOVED from drone (drone-playable.test.mjs:34). No test computes the deck's "security 99 · stability 100 …" — those are self-authored why-field narrative. (Note: Athena's correction stands — the final-round ~99.6 aggregate is not "overstated"; the real defect is that it is ungated, not wrong.)

G. CONFIRMED GAP (records hygiene) — ask→.sha256 pairing is ungated (only drone handoff sha256 is enforced). 4 of the newest asks ship without a sidecar: docs/asks/2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md.

=== BOTTOM LINE ===
The gate MECHANISM is real where it counts on integrity primitives (sha256, git-commit existence, contiguous D1..Dn decisions, badge placement). But several flagship-facing CLAIMS ride on gates that only test a floor (SoI freshness, A), a tautology (replay, B), a length proxy (superset, D), or nothing at all (iterations monotonicity C, no-model-id E, deck SSSES F, ask sha256 G). So "the gates are real" is true in part and false in part — the strongest single real gate is rcore-revisions.test.mjs:88; the clearest false-assurance gates are rcore-revisions.test.mjs:76 (SoI freshness) and traceability.test.mjs:32-33 (vacuous replay).

### MoT 4 — Is the record honest & APPEND-ONLY end-to-end (drs revisions/decisions/iterations, REVISIONS.md chains) — any silent edit or drift?
MoT coordinator 4/12 — append-only & honesty of the record. Read-only re-check of repo + git done. Verdict: the append-only CORES are honest (no silent edits), but the record is NOT clean end-to-end — 3 confirmed GAPs.

=== CONFIRMED HONEST / APPEND-ONLY (no silent edit) ===

1. decisions[] — CLEAN. D1..D132 strictly monotonic and gated. Verified: node check found NO monotonic breaks (D1..D132); enforced at frontend/tests/innovation-time.test.mjs:378 (`drs0.decisions.every((x,i)=>x.id===`D${i+1}`)` + D7.supersededBy===D8) and scripts/drs-render.mjs:292. No drift.

2. revisions[] why-history — CLEAN / no silent edit. Monotonic 0.001..0.130. Across the file's ENTIRE git history (oldest e97abf4 -> HEAD) ZERO prior `why` lines were ever removed or rewritten (git diff removed-why count = 0). The only mutation to a historical entry is the DISCLOSED law-7 backfill: each release commit replaces the previous entry's `"commit":"PENDING"` with the real sha — git show 89d740b (rev 0.130) removed exactly `"revision":"0.129"`+`"commit":"PENDING"`; git show 7789934 removed exactly `"revision":"0.128"`+`"commit":"PENDING"`. This is the documented mechanism (why-fields literally say "fill revisions[] 0.129 commit -> 7789934"), not a silent edit. CONFIRMED honest.

3. REVISIONS.md (docs/drone-2525/operator-deck/REVISIONS.md) — CLEAN append-only chain. git diff oldest(1ce5a3c)->HEAD shows 0 `## r.xxx` section headings ever removed. Tip r.152 honestly marked "PENDING until Verify Live"; items 9-11 -> r.153 flagged known-open. No silent heading edit.

=== REAL GAPS ===

GAP 1 (MEDIUM) — iterations[] is NOT clean append-only; internally self-contradicting AND ungated. docs/drs/drs.v00.00.json iterations (len 124): n=122, 123, 124 EACH APPEAR TWICE (the three visionary passes rev 0.128/0.129/0.130 re-appended n122/123/124 that already existed in the 106..127 block — their own why-fields say "append iterations[] 0.129 n 123" / "0.130 n 124"), and n=100-105 are MISSING (sequence jumps 99 -> 106). The rendered gate review prints the contradiction verbatim: docs/drs/DE-RISKING_STRATEGIES_GATE_REVIEW.md:204 = "Iteration ledger (124 of 99)." No gate catches it: innovation-time.test.mjs:378 asserts only `(iterations).length >= 1` (decisions get the strict D1..Dn check, iterations do not); drs-render.mjs:293 validates each iteration only for a matching revisions[] entry + file + edits (not n uniqueness/monotonicity); drs-render.mjs:59 hardcodes "of 99". This is the clearest drift directly on this question. CONFIRMED GAP (Enki's finding, verified).

GAP 2 (MEDIUM) — derived traceability RENDER has drifted 3 revisions behind source. frontend/lib/2525-core/soi-drs-revisions.gen.ts is stale at rev 0.127 (docstring "SoI-2525 DRS revision record — 127 revisions", last entry 0.127) while its source docs/drs/drs.v00.00.json is at 0.130 — scripts/gen-rcore-revisions.mjs was not re-run after the visionary passes appended 0.128/0.129/0.130. So the FLAGSHIP SoI "trust the record" R-CORE version-history panel ships 3 revisions behind the source and behind the deck's own PROV footer (rev 0.130, innovation-slide-seed-drs.ts). The source ledger is honest append-only; the DERIVED render surface has drifted. Gate rcore-revisions.test.mjs asserts only `revisions.length >= 20`, cannot detect staleness. CONFIRMED GAP (a freshness/drift bug in a generated artifact, not a silent edit of the record itself). Flagged by Aset/Asar/Athena, verified.

GAP 3 (LOW-MEDIUM) — asks integrity-stamp chain incomplete + ungated. docs/asks/ holds 118 .md vs 42 .md.sha256; the 4 newest/most-consequential asks confirmed md-present/sha-absent: 2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md. Persist-first ordering itself holds; only the checkable digest is missing and the ask->.sha256 pairing is ungated, so a handoff can land unverifiable. Weakens (does not break) the integrity chain for the newest handoffs. CONFIRMED GAP (all lenses, verified).

=== BY-DESIGN, NOT A VIOLATION ===
revisions[] tip: rev 0.130 commit field = "PENDING" (actual sha 89d740b). This is the disclosed tip-unstamped state, backfilled on the next release. Acceptable under the documented pattern, but the shipped HEAD row is not self-verifying by sha until the next round.

=== BOTTOM LINE ===
No silent edits to the append-only cores (decisions, revision why-history, REVISIONS.md headings) — those are honest. But the record is NOT clean end-to-end: (1) the iterations ledger is genuinely corrupted (duplicate n122-124 + missing n100-105), self-contradicting on the rendered surface ("124 of 99"), and ungated; (2) the flagship SoI R-CORE traceability render has drifted 3 revisions behind its source; (3) the 4 newest asks lack integrity sidecars. Gaps 1 and 2 are the substantive honesty/drift findings.

### MoT 5 — Concept-G1 reframe: is it narrative-only with the Pod gate field + tests intact, or did anything structural slip?
CONFIRMED (MoT coordinator 5/12): the Concept-G1 reframe is narrative-only. The structural Pod gate field is untouched and every gating test remains untouched AND green. Nothing structural slipped.

SCOPE — the reframe is exactly three commits (operator 2026-09-26 "Make G1"), rev 0.125→0.127:
- f858aa4 rev 0.125 (S1): MASTER_Q label + S1 ask + drs project.stage/decisionRequested
- b5c773f rev 0.126 (S2): S2 status + roadmap "Gates:" line
- 663e724 rev 0.127 (S15): S15 prio row-1 third column
(The later visionary passes ee9e848/7789934/89d740b, rev 0.128–0.130, are a separate "upside horizon" workstream, not the G1 reframe.)

EVIDENCE IT IS NARRATIVE-ONLY (re-checked the diff f6bd8d5→663e724, read-only):
1. frontend/lib/innovation-slide-seed-drs.ts — only display-string cells changed: MASTER_Q "Master question (G2):"→"(Concept · G1):" (question byte-identical); S1 ask.hi/ai "G2 ask"→"Concept-gate ask (Concept · G1)"; S2 status.hi/ai "G2 / future-state validation"→"Concept (G1) · …"; S2 roadmap.hi/ai "Gates: G2 …"→"Forward de-risking gates (after the Concept gate): G2 …"; S15 prio.hi[0][2]/ai[0][2] prefixed "Forward de-risking plan (after the Concept gate): …". PROV constant rev 0.124→0.127. No field/shape/type/number changed; the DRS G2/G3 detail is kept verbatim, just re-labelled as the forward plan.
2. frontend/lib/innovation-data.ts — a single line changed: the PRJ-34 row provenance string rev 0.124→0.127 (line ~1145). Nothing else.
3. docs/drs/drs.v00.00.json — only the narrative display fields project.stage ("G2 / future-state validation"→"Concept (G1) · future-state validation") and project.decisionRequested opening were reframed, plus project.revision bump and the append-only record arrays (iterations[], decisions[] D127–D129, revisions[]). No structural/financialModel field moved.

POD GATE FIELD — INTACT (this is the load-bearing check):
- frontend/lib/innovation-data.ts:1142 — the PRJ-34 project row still reads gate: "G2". This is the "Pod gate field" that drives the financial-year horizon, tolerance band, ramp profile and NPV weighting. It was deliberately NOT touched; every commit body names it a HARD LIMIT ("Pod row gate stays 'G2', innovation-time.test.mjs:369").
- The guard passes: frontend/tests/innovation-time.test.mjs:369 asserts p34.gate === "G2" (+ nreK 11200, fullRev10yM 248, provenance footer) and is green.
- The S-slide gate metadata (innovation-data.ts:2937 S7, :2941 S8, :2972 S9, :2989 S10, :2990 S11 all gate:"G2"; :3137 DRS gate row) and the ramp logic at :1481 (p.gate==="G1"||"G2") are all unchanged (absent from the diff).

TESTS — INTACT (untouched AND green):
- No test file is touched by any of the three commits: `git show --name-only f858aa4 b5c773f 663e724 -- 'frontend/tests/**' '**/*.test.mjs'` returns nothing, and `git diff --stat f6bd8d5 663e724 -- frontend/tests/` is empty.
- I ran both relevant suites read-only with the repo's ts-alias-loader: innovation-time = 3797/3797 passed (includes the :369 gate hard-limit and the drs financialModel/podInputs coupling checks); pod-invariant = 463 passed, 0 failed.

THE ONE THING TO KNOW (by design, not a slip): the reframe intentionally creates a narrative/structural split — the buyer-facing deck now reads "Concept (G1)" while the underlying data model keeps PRJ-34 at gate "G2" (which is what actually drives the economics). This divergence is deliberate, documented in the decision register (D127–D129) and pinned by the gate test, so it is a governed, gated design choice rather than a regression. A reviewer should simply be aware that the "G1" a reader sees and the "G2" the financials compute against are held apart on purpose.

BOTTOM LINE: narrative-only reframe; Pod gate field (gate:"G2") and all tests intact and green; nothing structural slipped.

### MoT 6 — The 3 visionary passes (rev 0.128-0.130 / D130-D132): is the all-NBA-by-2525 + financial-crisis upside within the laws (money DECLARED, no model names, master/segments intact)?
MoT coordinator 6/12 — verdict on the 3 visionary passes vs the three named laws: CONFIRMED. I re-checked the repo/git read-only; the all-NBA-by-2525 + financial-crisis upside is within all three laws. One adjacent records-hygiene GAP (not one of the three laws) is real and I state it separately.

SCOPE OF THE PASSES (git-verified). Commits ee9e848 (0.128/D130), 7789934 (0.129/D131), 89d740b (0.130/D132). Across all three, the ONLY frontend files touched are frontend/lib/innovation-slide-seed-drs.ts (cell appends) and frontend/lib/innovation-data.ts (PROV/row-provenance rev bump only, 0.127->0.130). No test file was edited in any of the three commits. Edits land on cells S8 nba, S17 prio 2525-row + obs, S2 roadmap, S8 benefits, S14 notes, S5 whys, S6 conops, S12 l0, S16 counter — appended list items / trailing clauses, no cell emptied, no field shape changed.

LAW 1 — MONEY DECLARED: CONFIRMED. Every visionary append carries the DECLARED (IA) framing explicitly and frames the financial-crisis upside as coordination value on a 2525 horizon, never a savings/guaranteed claim:
- innovation-slide-seed-drs.ts:549 (S14 notes.ai) "the return profile stays DECLARED (IA), never a buyer-validated saving"
- :647 (S16 counter.ai) "modeled value stays ranges, never a guaranteed saving"
- :267 (S6 conops.ai) "never a claim that any of it ships today"
- S2 roadmap.ai / S8 benefits.ai / S12 l0.ai appends: "money DECLARED (IA), never buyer-validated" / "never a savings claim"
- The base financial-model cells were NOT touched and remain DECLARED: :126 and :133 "Financial model — DECLARED (IA — an internal assumption, not buyer-validated) ... 180 k modeled value ... 60 k annual price"; :311/:318 "Priced as a DECLARED single-customer baseline (IA)". No dollar figure was added or moved by the passes; law-3 (no number moves) holds.

LAW 2 — NO MODEL NAMES (in cells): CONFIRMED. Grep of innovation-slide-seed-drs.ts for opus|sonnet|haiku|claude|gpt|gemini|llama|mistral|grok returns 0 hits. The appends name crisis-management PRODUCTS (Everbridge 360 AI/Bridge, Rave/Rave Alert, PagerDuty) — not AI models — and deliberately keep the retired DRS-attach product names OUT of the cells, naming them by DOMAIN instead ("the utility, healthcare and campus operational systems"). This is gated: innovation-time.test.mjs:430 asserts !/GridOS|Veoci/.test(seedSrc). Caveat (OUTSIDE these passes, do not attribute to them): the token "Grok" (xAI) persists in pre-existing provenance strings — innovation-data.ts:1147 and seed PROV line 36 / CONFIRM line 35 — and "Opus 4.8" lives in the drone ledger; the visionary commits only bumped the rev number inside PROV and introduced none of these. So the passes themselves are clean; the broader "no identifier in ANY artifact" claim is a separate pre-existing item other lenses (Christo/Asar) already flagged.

LAW 4 — MASTER / SEGMENTS INTACT: CONFIRMED. The git diffs of all three commits touch NEITHER futureState.master.statement NOR segmentValueProps. Enforced by innovation-time.test.mjs:415 `ok(p34.valueProp === drs.futureState.master.statement, ... verbatim)`; since no test was edited in any visionary commit, test:ci must have held. Law 6 (ai >= hi) is also preserved — S8 nba got a one-clause hi append plus a strictly larger ai superset (diff of ee9e848); gate at innovation-time.test.mjs:1019/1042. Law 1 (primary validation set) is EXTENDED not broken: every append repeats "primary set EDU · FOOD · TECH, others discovery-only until a named buyer runs them".

ADJACENT REAL GAP (NOT one of the three named laws, but introduced by these same passes) — DRS iterations[] append-only numbering. The three passes wrote iterations entries with n=122/123/124 for revs 0.128/0.129/0.130, which COLLIDE with the pre-existing n=122/123/124. Verified: `grep '"n": ...'` on docs/drs/drs.v00.00.json shows n=122, n=123, n=124 each appear TWICE (entries at drs.v00.00.json:6722/6747/6772 duplicate earlier ones), while n=100-105 are MISSING (ledger jumps 99->106). Because drs-render.mjs hardcodes "of 99", DE-RISKING_STRATEGIES_GATE_REVIEW.md renders "Iteration ledger (124 of 99)" with duplicate final rows. This is ungated (innovation-time.test.mjs:378 asserts only iterations.length>=1; only decisions D# are monotonic-gated). It does NOT bear on money-declared / model-names / master-segments, so it does not overturn the CONFIRMED verdict on the three laws — but it is a genuine append-only integrity defect the passes created and should be tracked (matches Enki's finding).

BOTTOM LINE: On the exact question asked — money DECLARED, no model names in cells, master/segments intact — all three are CONFIRMED for the 3 visionary passes. The upside (all-NBA-by-2525 + financial-crisis coordination) is consistently painted as an IA/2525 vision horizon, never a shipped or savings claim. The only real defect in the vicinity is the iterations[] n-duplication, which is a separate, ungated ledger-hygiene issue outside the three laws.

### MoT 7 — R-CORE across all 7 surfaces: one shared component, exact icons, bottom-of-page, transparent, traceable — any surface off-pattern?
MoT coordinator 7/12 — cross-cutting R-CORE pattern verdict (READ-ONLY re-check done).

THE 7 SURFACES (all mount the same component, verified):
1. Easter-egg/SIM — frontend/app/sim/page.tsx:184 (accent #a78bfa)
2. SoI-2525 (flagship) — frontend/app/SoI-2525/page.tsx:1100 (accent #22d3ee)
3. Settings slide-over — frontend/components/moderator-settings.tsx:584 (accent #22d3ee)
4. Celestial-2525 — frontend/components/celestial-2525/celestial-reader.tsx:189 (accent #ffd400)
5. Architect-2525 — frontend/components/architect-2525/command-ux1.tsx:470 (accent #c084fc)
6. Security-2525 — frontend/components/security-2525/command-ux1.tsx:680 (accent #19c8cf)
7. Drone-2525 — frontend/components/drone-2525/command-ux1.tsx:147 (accent semanticHex("mount"))

ATTRIBUTE-BY-ATTRIBUTE:

1) ONE SHARED COMPONENT — CONFIRMED. All 7 import RCoreBadge from @/components/2525-core/rcore-badge and mount <RCoreBadge history=.. accent=..>. Single implementation (rcore-badge.tsx:32); no per-surface fork. Only per-surface variation is the theme accent prop (by design, theme-agnostic per docstring line 17). Gated by rcore-stage2.test.mjs:55 (5 surfaces) + rcore-revisions.test.mjs:97,99 (SoI + Drone).

2) EXACT ICONS — CONFIRMED. The shared component hardcodes ICON="/r-core/r-core-icon.png" (REST reticle) and WORDMARK="/r-core/r-core-wordmark.png" (CLICK-1 pill), rendered verbatim as <img> as-is, never a glyph/CSS/text recreation (rcore-badge.tsx:25-30,76,90 — comment cites docs/asks/2026-09-25_rcore_exact_icons.md "just use icons provided exactly as is"). Both raster files exist: frontend/public/r-core/r-core-icon.png, r-core-wordmark.png. Because icons live in the one shared component, all 7 surfaces render identical artwork — no surface overrides them.

3) BOTTOM-OF-PAGE — CONFIRMED on all 7. Each mounts <RCoreBadge> as the LAST child in normal flow (sim:184, SoI:1100, settings:584, celestial:189, architect:470, security:680, drone:147 — verified each is the final element before the closing wrapper). Component renders "flex w-full justify-center" with no position:fixed (rcore-badge.tsx:59-63). Gated by rcore-revisions.test.mjs:88 which asserts "flex w-full justify-center" AND NOT /fixed left-1\/2/ ("never a fixed overlay").

4) TRANSPARENT — CONFIRMED (component-level, therefore all 7). Both buttons are bg-transparent (rcore-badge.tsx:71,85), container has no background, pointer-events on the control only (line 62). Icon PNGs render on a transparent control.

5) TRACEABLE — GAP (real, on the flagship). CONFIRMED off-pattern:
   - SoI-2525's R-CORE version-history panel is 3 revisions STALE. Its source frontend/lib/2525-core/soi-drs-revisions.gen.ts tops at rev 0.127 (last entry commit "PENDING"; docstring line 7 literally "127 revisions"), while the DRS source docs/drs/drs.v00.00.json is at rev 0.130 (revisions 0.128/0.129/0.130 present; project at 0.130) and the deck's own PROV footer reads "rev 0.130" (frontend/lib/innovation-slide-seed-drs.ts:36).
   - Root cause: the generated ledger was last built by commit 00c7073 ("refresh the full 127-revision ledger"), which PRECEDES the visionary commits ee9e848 (0.128), 7789934 (0.129), 89d740b (0.130) — gen-rcore-revisions.mjs was never re-run after the visionary passes. So SoI's flagship "trust the record" panel fails to reflect its own final work (decisions D130–D132 / the upside-horizon round).
   - The gate cannot catch it: rcore-revisions.test.mjs:76 asserts only soiH.revisions.length >= 20, so 127 stays green while the ledger is 3 short. Under-asserted floor, not the 130 count.
   The other 6 surfaces read their own append-only *.gen ledgers directly (fromLedgerJson) and are not fed by the un-re-run DRS generator, so the staleness is specific to SoI.

OTHER OFF-PATTERN ITEMS FOUND (beyond the 5 named attributes but on these surfaces):

A) Model-identifier disclosure leak via the shared COMPARE tool on 2 surfaces (SoI + Drone). rcore-revision-panel.tsx:184,192 render cmp.sideBySide.left/right through dangerouslySetInnerHTML — a word-diff of the FULL revision detail text. That detail is the ledger why/text field. SoI ledger rev 0.079 contains "(Grok)" (soi-drs-revisions.gen.ts:559); Drone ledger contains "Grok" at rev 0.012 (drone-ledger.gen.ts:143) and "Model changed to Opus 4.8 mid-round" + "Grok" at rev 0.015 (:164). Any user who compares a window crossing those revisions sees the model names. This breaks the stated "no model identifier in any repo artifact" invariant on user-facing R-CORE surfaces. (Deck CELLS are clean — grep of innovation-data.ts / innovation-slide-seed-drs.ts for GPT/Claude/Gemini/Llama/Mistral/OpenAI/Anthropic = 0; the leak is only in the ledgers that feed the compare tool, and Drone's append-only 0.015 entry cannot be rewritten under the append-only law.)

B) Shared-component docstring self-inconsistency (record-level, behavior correct). rcore-badge.tsx:15 claims "Fixed + centred + high z ... so it never blocks the page", contradicting the same file's lines 11-13 and 56-58 and the actual render (normal flow, mounted last, no position:fixed). Stale comment; shipped UX is correct on all 7.

BOTTOM LINE: The R-CORE pattern is one shared component with exact icons, bottom-of-page, transparent — CONFIRMED uniform across all 7 surfaces. TRACEABLE is off-pattern on SoI-2525 (flagship): the panel ships 3 revisions stale (0.127 vs 0.130) and the gate's length>=20 floor cannot detect it — a visible cross-surface contradiction on the headline traceability feature. Secondary: the compare tool leaks model identifiers on SoI + Drone. Nothing is broken in the component's shape; the gap is data freshness (SoI) and disclosure hygiene (SoI/Drone).

### MoT 8 — Drone r.150-152: are the fire-gate/pop-up/fullscreen claims verified, and is the byte-gated deck chain intact?
MoT coordinator 8/12 — Drone r.150–152 fire-gate / pop-up / fullscreen + byte chain. VERDICT: CONFIRMED across the board. I re-checked the repo read-only (no edits); the drone-playable gate runs green (94 passed, 0 failed).

BYTE-GATED DECK CHAIN — INTACT (CONFIRMED)
- Served == carried, byte-identical: frontend/public/drone-2525/play.html and docs/drone-2525/operator-deck/drone-2525_r.152.html both sha256 e15cd3993aa378c958b9469831b87f50317ee98b47bdfcc112c11980c9e2eebd, both 368,979 B. HASHES_r152.sha256 records that same hash for the deck.
- Hash ledgers verify: `sha256sum -c` passes OK for r.150, r.151 and r.152 — each carried deck AND its patch (HASHES_r150/151/152.sha256).
- Single-sourced HEAD: DECK_REV='152' in frontend/tests/deck-head.mjs and frontend/lib/drone-2525/deck-rev.ts; drone-playable.test.mjs:63-64 holds the two equal, and :18/:20 hold play.html byte-identical to the carried deck AND present in the hash ledger.
- Patch chain present + hashed end-to-end: patches/r149_to_r150.py → r150_to_r151.py → r151_to_r152.py (each an assert-or-refuse builder: r151_to_r152.py:18 raises on any anchor miss). Under budget: 368,979 B << 7,770,000 B ceiling (drone-playable.test.mjs:17). Served bytes self-declare revision:'0.152' with no stale r0.130/129/123 bookkeeping (:22).

FIRE-GATE — CONFIRMED (r.150 picture-reading gate + r.151 pop fix)
- r.150 in served bytes: play.html:2386 reason:'TARGET_OFF_PICTURE' (sentence names the box), :2417 reason:'TAG_NEEDS_RED_BOX', plus NOT_A_RANGE_TARGET; the dead hiLock guard and SSSES row are gone. Asserted drone-playable.test.mjs:34.
- r.151 pop-fire fix: play.html:2385 non-plate FIRE branch tolerance is `<=LOCK_REACH_PX` (the LOCK reach, =48), NOT the old 32 px that refused a marked+approved lawn pop; grep for the `pr.y-H*.46)<=32` survivor returns 0. Boot-QA FIRE_HITS_A_POP at :4007. Asserted drone-playable.test.mjs:36.

POP-UP — CONFIRMED (r.152 reconfirmation, no logic change)
- All-up + amber→red box + fire-hits-a-pop are gated: boot rows TRAIN_ALL_UP / MODE_RESET_RETURNS (QA-row list, test:59), the amber-then-red voxel box on every marked slot (r.149 slotBoxes, test:33), and FIRE_HITS_A_POP (:4007). CLAUDE_CODE_NOTES_r152.md:29-38 documents headless (390 px) verification of (a) all-up, (b) amber on TARGET, (c) red on APPROVE, (d) HIT on FIRE — with an explicit "no change made to the pop/box/fire logic; r.151 LOCK_REACH_PX untouched."

FULLSCREEN — CONFIRMED (r.152 icon, not the word)
- Served bytes: `<button id="btnFullBar" ...>⤢</button>` and `id="btnFull" ...>⤢</button>` at rest; the toggle writes `b.textContent=on?'⤡':'⤢'` (play.html:674); `#btnFullBar{order:9;...}` right-aligns it at the top-bar upper-right; boot-QA FULLSCREEN_IS_AN_ICON at :4139 (off→⤢/⤢, full→⤡/⤡). The words `>FULL</button>` and `on?'EXIT':'FULL'` are GONE. Asserted drone-playable.test.mjs:38. Fullscreen BEHAVIOUR (setFull → requestFullscreen/exitFullscreen, #app.full) was out of scope and untouched.

TWO ACKNOWLEDGED-OPEN NOTES (NOT gaps in the three claims)
1. Drone browser tab <title> still reads "eXeL ECO-2525 · MoT · SSSES" in play.html — the operator's requested "eXeL Drone-2525" rename is explicitly deferred (REVISIONS.md tail: "items 9–11 → r.153"). Matches Christo's known-open item; correctly not-yet-shipped, not a hidden regression, and outside the fire/pop/fullscreen scope.
2. Bookkeeping nuance: docs/traceability/drone-2525.ledger.json:339 (rev 0.152) has shipped:"PENDING" (r.151 is filled, 0dd4cc4), yet the served HEAD IS r.152 byte-identical. This is a "shipped-commit PENDING until Verify Live" annotation, not a defect in the verified claims.

SCOPE NOTE: the per-lens syntheses' issues (R-CORE SoI panel stale at 0.127 vs 0.130, 4 missing asks .sha256 sidecars, model-identifier strings surfacing via the R-CORE compare of drone-ledger.gen.ts / soi-drs-revisions.gen.ts) are real but belong to R-CORE traceability / records hygiene — none touch the drone r.150-152 fire/pop/fullscreen chain or its byte gate, which are clean.

### MoT 9 — Is there any MODEL IDENTIFIER leaked into a repo artifact (not just commit trailers)?
CONFIRMED GAP (MoT coordinator 9/12). The absolute claim "no model identifier in any repo artifact (only commit trailers)" is falsified. I re-verified every cited line read-only at HEAD 89d740b. Distinguish two classes; only the second is the leak in question.

CLASS 1 — NOT a leak (product feature content, in scope for the app itself): CLAUDE.md:210-211, :264, :493 list OpenAI / Grok (xAI) / Gemini (Google) / Claude / Cohere as the polling PRODUCT's selectable AI providers. This is legitimate product-capability copy, not the authoring model. (Also frontend/lib/ai-provider.ts, moderator-settings.tsx, i18n files.) Only counts as a violation under the strictest reading of "no model id in ANY artifact"; it is not what the trailer-only intent targets.

CLASS 2 — CONFIRMED LEAKS (authoring / provenance model identifiers in non-trailer artifacts):

A. "Opus 4.8" (Anthropic authoring model) — strongest, and USER-FACING + permanent:
- docs/traceability/drone-2525.ledger.json:156 (rev 0.015): "...Model changed to Opus 4.8 mid-round..."
- frontend/lib/2525-core/drone-ledger.gen.ts:164 (rev 22 / rev 0.015): same text, generated from that ledger.
- docs/asks/2026-09-16_demo_scale_fleet.md:3: "Model changed to Opus 4.8 mid-round; commit attribution -> 'Claude Opus 4.8'".

B. "Grok" (xAI model, used as a cross-review authoring model) — pervasive, incl. FLAGSHIP SoI:
- frontend/lib/2525-core/soi-drs-revisions.gen.ts:559 (rev 0.079 why): "...the v0.9 narrative (Grok)...".
- frontend/lib/innovation-slide-seed-drs.ts:27 & :36 (PROV constant "cross-review informed by Grok"), and :85 (S1 ai-layer slide cell: "the v0.9 narrative (Grok)").
- frontend/lib/innovation-data.ts:1147 (provenance field: "cross-review informed by Grok").
- frontend/lib/2525-core/drone-ledger.gen.ts:143 (rev 0.012, "Grok" x2) and :164 (rev 0.015, "48-agent + Grok").
- docs/traceability/drone-2525.ledger.json lines 24, 31, 87, 115, 135, 149, 156.

C. "Fable 5.1" / model:"fable" (authoring/execution model):
- docs/asks/2026-09-14_polling_activation_plan.md:1 ("AsM-tested on Fable 5.1") and :20 ("run on Fable 5.1 (model:\"fable\")").

RENDER PATH (why severity is elevated, not just a repo-file note): the leaked strings are surfaced in the shipped R-CORE compare panel, not merely stored.
- DRONE: drone-ledger.gen.ts DRONE_LEDGER -> command-ux1.tsx:38,47 fromLedgerJson (revisions.ts:63,70 maps text->detail) -> rcore-revision-panel.tsx:184 & :192 render cmp.sideBySide.left/right via dangerouslySetInnerHTML (word diff of full detail, revisions.ts:186). Any user comparing a window crossing rev 0.012/0.015 sees "Opus 4.8" and "Grok".
- SoI (flagship): soi-drs-revisions.gen.ts SOI_DRS_REVISIONS -> fromDrsRevisions (revisions.ts:82,88 maps why->detail) -> same compare panel; rev 0.079's "(Grok)" is renderable.

NO GATE CATCHES IT: frontend/tests/innovation-time.test.mjs:430 only bars competitor product names /GridOS|Veoci/ in the seed; there is no scan for model identifiers (opus|sonnet|grok|fable|gpt|gemini|anthropic|claude) in cells, PROV footers, or the ledgers. So this leak is ungated and can recur.

BUYER-FACING SLIDE CELLS are otherwise clean: grep of innovation-data.ts / innovation-slide-seed-drs.ts for Opus|Sonnet|Claude|Anthropic|GPT-|Gemini finds only the "Grok" provenance/PROV occurrences above — the narrative deck cells themselves carry no model name. So the trailer-only INTENT mostly holds for buyer cells but is broken in (a) provenance/PROV footers and (b) the traceability ledgers, which are exactly the R-CORE "trust the record" surfaces.

APPEND-ONLY WRINKLE: the drone ledger operates under an append-only law (drone-ledger.gen.ts / drone-2525.ledger.json), so the "Opus 4.8" entry at rev 0.015 cannot be rewritten in place — it is a permanent, immutable leak unless a compensating redaction/mask is added at render time.

Bottom line: the answer to the cross-cutting question is YES — CONFIRMED. Authoring/provenance model identifiers (Opus 4.8, Grok, Fable 5.1) are committed into multiple repo artifacts (ledgers, generated TS, provenance footers, ask records), two of which render to users on the drone and flagship SoI R-CORE panels, and nothing gates it. This is a genuine GAP against the stated "model identifier only in commit trailers" invariant, not merely a documentation nit.

### MoT 10 — What did the fleet MISS — any area under-covered or a claim nobody actually verified?
MoT coordinator 10/12 — cross-cutting: "What did the fleet MISS?"

Bottom line: the fleet converged tightly and correctly on record-hygiene SYMPTOMS (stale R-CORE panel, missing .sha256, model-names in ledgers, tautological/weak gates, iteration-ledger corruption). But it missed the STRUCTURAL ROOT CAUSE behind the flagship finding, missed a concrete traceability CONSEQUENCE, MISCHARACTERIZED the asks-digest model, and left two safety/scope questions unasked. Separately, I verified one thing the fleet implied was a risk is NOT one: the deck's computational substance is well-gated. Details below, each marked CONFIRMED vs GAP with evidence.

=== GAP 1 (systemic root cause NOBODY named) — the R-CORE generator is orphaned from the build ===
Three lenses (Aset/Asar/Athena) reported the SoI traceability panel is 3 revisions stale (soi-drs-revisions.gen.ts ends at rev 0.127, commit "PENDING"; drs.v00.00.json is at 130 — the visionary passes 0.128/0.129/0.130 = ee9e848/7789934/89d740b) and blamed "gen never re-run." None checked WHY. The generator frontend/scripts/gen-rcore-revisions.mjs is NOT wired into anything: absent from frontend/package.json:9 `prebuild` (which runs lockcheck/integrity-guard/gen-seal-html/copy-pdf-worker/copy-migration/gen-i18n-index/sign-* but NOT this), absent from `test:ci` (package.json:177), and absent from .githooks/ and .github/. So there is no regeneration step and no drift check anywhere. The "fix" the lenses imply (re-run the gen once) papers over a structural gap: the flagship "trust the record" panel will silently re-stale on EVERY future DRS append. This is the miss.

=== GAP 2 (concrete consequence nobody connected) — the arc's OWN final decisions are uncitable ===
Because the gen file stops at 0.127, decisions D130/D131/D132 (the three visionary passes — the 48h arc's headline deliverable) appear ZERO times in soi-drs-revisions.gen.ts. The panel cites decisions by extracting `\bD\d+\b` from each revision's detail (rcore-revision-panel.tsx:42-44, decisionsOf). So the flagship traceability tool literally cannot cite the exact decisions its own final shipped work created. Athena noted the gate doesn't ASSERT D# rendering; nobody connected that the freshness gap makes the newest D#s unrenderable on SoI at all. CONFIRMED by grep (0 occurrences of D130/D131/D132 in the gen file).

=== GAP 3 (mischaracterized model + a verification nobody performed) — the asks digest chain ===
- The 4 newest asks genuinely have NO digest of any kind: docs/asks/2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md. CONFIRMED (matches Aset/Asar/Athena/Enki).
- BUT the fleet's "sidecar" framing is wrong. The docs/asks/*.sha256 files (42 of them) are mostly per-ask PACKAGE MANIFESTS (they hash the .md PLUS its attached screenshots/PDFs — e.g. 2026-09-23_lane_markers.md.sha256 covers the .md + range_lane_markers_photo.jpg), not 1:1 .md sidecars. Two of the 42 (2026-09-16_foil_1m_five_levels_world.sha256, 2026-09-16_operator_deck_r042.sha256) don't hash any ask .md at all — they are pure artifact manifests (FOIL_*.h/.py/.png; ASM_CUP_99.*, drone-2525_r.*.html). Naming is also split (27 `*.md.sha256` vs 15 bare `*.sha256`). So the true count of asks carrying a checkable digest of the ask text is ~40, not "42 vs 118", and any presence-gate would need to handle both naming forms and both file types.
- Verification nobody did: Enki reported "a sampled hash verifies OK" (one sample). I recomputed ALL present digests. Every manifest that lists its ask .md verifies byte-exact — integrity is CONFIRMED SOUND (a stronger positive than a single sample). No mismatches. (An apparent mismatch on the foil file was a false positive of naive 1:1 pairing — that file is a package manifest, not an .md digest.)

=== GAP 4 (enforcement) — asks digests are entirely ungated ===
No test/gate/hook verifies any docs/asks digest. The only sha256 gates in the repo are drone-crs.test.mjs:58 and drone-arena.test.mjs:21 (d.project.handoffSha256). So the whole "persist-first stamp discipline" for asks — the missing 4, the mixed naming, the manifest-vs-sidecar heterogeneity — is invisible to CI. The fleet reported the missing files but not that nothing guards them.

=== GAP 5 (append-only integrity — amplifies Enki) — no cross-ledger parity invariant ===
CONFIRMED Enki: docs/drs/drs.v00.00.json iterations[] has 124 entries with n=122,123,124 each duplicated (visionary revs 0.128-0.130 reused n122-124) and n=100-105 missing; drs-render.mjs:59 hardcodes "of 99" so it renders "Iteration ledger (124 of 99)". The deeper miss: the three ledgers are mutually inconsistent in COUNT — revisions[]=130, decisions[]=132, iterations[]=124 — and NOTHING checks it. drs-render.mjs:293 only asserts each iteration's revision exists in revisions[] plus file+edits; only decisions get a contiguity gate (D1..Dn, drs-render.mjs:292); iterations get no n-contiguity check and innovation-time.test.mjs:378 asserts only length>=1. So the visionary commits stayed "drs-render --check green" while corrupting the iteration ledger. There is no cross-ledger count-parity invariant at all.

=== CLARIFICATION the fleet left ambiguous — the compare panel is NOT an injection vuln ===
Christo flagged model-names rendered via dangerouslySetInnerHTML (rcore-revision-panel.tsx:184,192) on the drone AND flagship SoI compare surfaces (CONFIRMED: soi-drs-revisions.gen.ts:559 rev 0.079 why contains "(Grok)"; drone-ledger.gen.ts has 2 hits; both feed compareRevisions→sideBySide). I verified the injection surface nobody assessed: sideBySide is called as sideBySide(ops, escHtml) (revisions.ts:187) and escHtml (version-diff.ts:27) escapes all token text; only the literal <del>/<ins> tags are HTML. So this is a CONTENT/PRIVACY leak on a rendered, append-only surface — NOT XSS. State it plainly so "dangerouslySetInnerHTML + model names" isn't mis-escalated to a security finding. It is also ungated: no test scans ledger why/detail for model identifiers (innovation-time.test.mjs:430 only bars GridOS/Veoci, and only in deck cells).

=== NOT a gap (checked to prevent an over-claim) — the deck's substance IS gated ===
A plausible "biggest miss" would be that the fleet spent all its energy on records while the buyer-facing financial model went unverified. FALSE. innovation-time.test.mjs carries ~2068 assertions including financialMetrics (all 12 metrics incl npvM/irrPct), financialsOverview (10-yr spans, R&D front-load), NPV rollups (BU header = Σ SBU rows), the D8 declared-IA seed (0.36M at 115% CAGR, line 359), and the D10 eleven-year PRJ-34 plan (line 370-371). The deck computation layer is well-covered; the gaps are concentrated entirely in the traceability/records/ledger-integrity layer.

=== Scope note ===
This batch of lenses (Aset/Asar/Athena/Christo/Enki) examined only the deck/R-CORE/DRS/asks/drone surfaces. backend/ (python + supabase) and the polling core got zero attention — but per git log the 48h PRJ-34 work touched only frontend deck + docs, so coverage matches scope; not a true gap, just worth stating that no lens confirmed the non-deck surfaces were untouched.

Net: the fleet's B+ grades are fair on the individual findings, but coverage earns a B — it caught the symptoms and missed the mechanism (orphaned generator, absent cross-ledger and asks-digest invariants) and mischaracterized the digest model, while over-implying an XSS risk that isn't one.

### MoT 11 — The one-page HONEST HEADLINE for the operator: what is verified-solid vs what needs attention.
MoT-11 HONEST HEADLINE — PRJ-34 48h verification (HEAD 89d740b = rev 0.130, tree clean). Every item below was re-checked read-only against the repo; I mark CONFIRMED / GAP / REFUTED.

=== VERIFIED-SOLID (trust these) ===
- The deck ships at rev 0.130 with all 3 visionary passes landed (commits ee9e848=0.128, 7789934=0.129, 89d740b=0.130). CONFIRMED.
- Buyer-facing slide CELLS are clean of competing-model identifiers: grep of frontend/lib/innovation-data.ts + innovation-slide-seed-drs.ts for GPT/Claude/Gemini/Llama/Mistral/OpenAI/Anthropic = 0 hits in cell bodies. CONFIRMED. (The "no model names in the pitch itself" promise holds.)
- The R-CORE badge SHIPS CORRECTLY: normal flow, mounted last, centred, never a fixed overlay (rcore-badge.tsx:57/61 "flex w-full justify-center"; enforced by rcore-revisions.test.mjs:88 which bars /fixed left-1\/2/). CONFIRMED.
- Persist-FIRST ordering holds and a sampled ask hash verifies OK; append-only law on the ledgers is intact (the historical entries genuinely cannot be rewritten). CONFIRMED.
- test:ci chaining is real (drs-render → drs-crs → innovation-time).
- REFUTED adversarial claim: SSSES "peak ~96 / ~99 overstated" does NOT hold — docs/drs/iterations/0.105.md:7 shows the final round at security 98-99 / all other pillars 100 (~99.6 aggregate). The scores are ungated (see below) but they are NOT inflated.

=== NEEDS ATTENTION (ranked) ===

1) HIGH — Flagship SoI R-CORE traceability panel is 3 revisions STALE and visibly self-contradicts the deck. CONFIRMED: frontend/lib/2525-core/soi-drs-revisions.gen.ts = 127 entries ending 0.127 (docstring literally "127 revisions"; last touched in commit 00c7073, which PRECEDES the three visionary commits), while docs/drs/drs.v00.00.json = 130 revisions ending 0.130 and innovation-slide-seed-drs.ts:36 PROV footer reads "rev 0.130". The generator (scripts/gen-rcore-revisions.mjs) was never re-run after the visionary passes. The gate cannot catch it: rcore-revisions.test.mjs:76 asserts only soiH.revisions.length >= 20. This is the "trust the record" feature failing to reflect its own final work — the one item I'd fix before any operator demo. FIX: re-run node scripts/gen-rcore-revisions.mjs and commit.

2) MEDIUM — Model-identifier text is reachable on the user-facing R-CORE COMPARE tool, on BOTH the drone AND the flagship SoI surface. CONFIRMED: rcore-revision-panel.tsx:184/192 render the word-diff of full detail via dangerouslySetInnerHTML. Source strings: drone-ledger.gen.ts rev 0.012 (line 143, "Grok") and rev 0.015 (line 164, "Model changed to Opus 4.8 mid-round" + "Grok"); SoI soi-drs-revisions.gen.ts rev 0.079 (line 559, "...the v0.9 narrative (Grok)..."). These surface only when a user compares a window that crosses those revisions. The absolute claim "no model identifier in any repo artifact" is therefore false as stated. Note: the drone entry is under the append-only law and cannot be rewritten — this is a disclosure/expectation gap to acknowledge, not a clean fix.

3) MEDIUM — DRS iterations[] ledger is internally inconsistent and ungated. CONFIRMED: docs/drs/drs.v00.00.json iterations length = 124 with n=122,123,124 each appearing TWICE and n=100–105 MISSING (jumps 99→106), max n=127. Only decisions are gated D1..Dn; iterations are not (innovation-time.test.mjs:378 asserts only length>=1). drs-render.mjs hardcodes "of 99", so the rendered review reads "124 of 99" with duplicate final rows.

4) MEDIUM — Record-integrity chain broken on the 4 newest (some shipped) asks. CONFIRMED: docs/asks = 118 .md vs 42 .sha256; missing sidecars for 2026-09-25_rcore_exact_icons.md, 2026-09-25_rcore_traceability_and_3_visionary_passes.md, 2026-09-26_drone_tab_name.md, 2026-09-26_rcore_compare_mirror_vision2525.md. Persist-first ordering itself is fine; only the stamp is absent — and it's absent precisely on the asks that drove shipped 48h work. Pairing is also ungated.

5) LOW — Gates are weaker than the claims they defend (false-assurance, no live defect today). CONFIRMED: (a) rcore-revisions.test.mjs:76 is a floor >=20, not ==127, and has no assertion the panel renders per-revision D# citations; (b) traceability.test.mjs:32-33 "replay(HEAD) equals the full ledger" is tautological — it filters rev<=length over ledgers already asserted monotonic 1..n, so it can never fail; (c) innovation-time.test.mjs:1042 checks the law-6 "superset" only by char length (ai.length >= hi.length), not containment; (d) the PRJ-34 deck SSSES and the "every 11th full gate" cadence are self-narrated, not computed/enforced by any test.

6) LOW — rcore-badge.tsx docstring self-contradicts: line ~15 says "Fixed + centred + high z" while line 11 and the actual render say normal-flow/mounted-last. Shipped UX is correct; the comment is stale.

KNOWN-OPEN, correctly deferred (not a regression): drone browser tab still reads "eXeL ECO-2525" (play.html <title>); REVISIONS.md schedules the rename to r.153, not yet shipped.

=== BOTTOM LINE FOR THE OPERATOR ===
The pitch content, the badge, and the 99-round provenance story are solid and honestly scored. Before showing the traceability feature to a buyer, do ONE thing: re-run the R-CORE generator so the SoI panel reads 0.130 (item 1). Then, as housekeeping, stamp the 4 missing ask sidecars (item 4). Items 2 and 3 are disclosure/record-hygiene truths to know about — the model-name text is buried in compare-diff history under an append-only law, and the iteration ledger double-counts the visionary rounds; neither breaks the deck, but both make the "total traceability / no model names anywhere" claims literally inaccurate. The gate weaknesses (item 5) mean the green checkmarks are guarding less than they appear — real today, but they will not catch the next drift.

### MoT 12 — Process: were PERSIST-FIRST + gates-before-push + both-refs + honest-reporting actually followed across the 48h?
MoT-12 cross-cutting process verdict (PRJ-34 48h). Re-checked read-only against repo at HEAD 89d740b. Overall: the four pillars were genuinely operated, two are airtight and two have real but bounded gaps. No evidence any pillar was ignored.

=== 1) BOTH-REFS — CONFIRMED (strongest pillar) ===
Mechanically enforced and proven, not just narrated. scripts/prj34-iterate.sh (last 4 lines) does `git push origin claude/debug-wsl-issues-yYdPP && git push origin HEAD:main`, then reads back `git ls-remote` for BOTH refs and asserts `R1==SHA && R2==SHA`, exiting "RED push (remote did not move)" otherwise. Live check now: local HEAD=89d740b, remote main=89d740b, remote branch=89d740b — all three in lockstep. This pillar was followed and is currently intact.

=== 2) GATES-BEFORE-PUSH — CONFIRMED mechanism, with a record GAP ===
CONFIRMED: scripts/prj34-iterate.sh runs tsc → drs-render(+--check) → drs-crs → innovation-time BEFORE any commit; a red gate `exit`s (codes 2–7) with nothing committed; only on green does it commit then push. "full" rounds add test:ci + build. The door writes what actually ran into the round file ("the record says what ran, not what was hoped"). Verified filled blocks in docs/drs/iterations/0.130.md, 0.128.md, 0.127.md, 0.106.md — each shows "INNOVATION-TIME 3797/3797 passed".
GAP (medium): the per-round gate RECORD has holes. 10 of 124 iteration files still read the unfilled placeholder "(filled by the door)": docs/drs/iterations/0.100.md, 0.101.md, and 0.119.md–0.126.md. For those rounds the door's stamp is absent, so the ledger does not attest gates ran (the code is green in aggregate now, and both refs align, so the outcome held — but ~8% of rounds lack the gate record the process promises). Secondary: the "every 11th round full test:ci+build" cadence is self-narrated in Gates blocks/commit bodies, not independently enforced by any check (Athena/Asar lens point, confirmed).

=== 3) PERSIST-FIRST — held for ordering, GAP on the integrity stamp ===
Ordering CONFIRMED: asks were committed before the work they drove. In git log, the ask-persist commits eb22b5d ("persist R-CORE compare-mirror + drone tab-name asks"), 6b58b37, and 0dbd4cf precede the visionary implementation commits ee9e848 (0.128), 7789934 (0.129), 89d740b (0.130). A sampled hash verifies OK.
GAP (medium): the .sha256 integrity stamp is missing on exactly the newest, shipped-driving asks. Confirmed no sidecar of any form for docs/asks/2026-09-25_rcore_exact_icons.md, docs/asks/2026-09-25_rcore_traceability_and_3_visionary_passes.md, docs/asks/2026-09-26_drone_tab_name.md, docs/asks/2026-09-26_rcore_compare_mirror_vision2525.md. Aggregate: 118 .md vs 42 .sha256, and the stamp naming is itself inconsistent (some `<name>.md.sha256`, some `<name>.sha256`), and the pairing is ungated. So persist-FIRST held; persist-with-verifiable-stamp partially failed on the most consequential recent asks. Flagged independently by every lens.

=== 4) HONEST-REPORTING — real culture, but two concrete GAPS ===
The honesty culture is real: the append-only law is respected even when it preserves awkward history (the drone ledger's "Model changed to Opus 4.8 mid-round" cannot be rewritten), commits explicitly cite "law-6 + honesty" (6e2b36e), and the door records what ran, not what was hoped. But two shipped claims fail:
GAP-a (high, cross-surface contradiction): the flagship "trust the record" R-CORE traceability panel is STALE. frontend/lib/2525-core/soi-drs-revisions.gen.ts tops at rev 0.127 (docstring literally "127 revisions"), while docs/drs/drs.v00.00.json now holds 130 revisions (0.128–0.130 appended) and the SHIPPED PROV footer reads "rev 0.130" (frontend/lib/innovation-slide-seed-drs.ts:36 and :85; innovation-data.ts:1147). gen-rcore-revisions was never re-run after the visionary passes, so the traceability tool contradicts the deck's own footer on the very feature that sells traceability. The gate rcore-revisions.test.mjs asserts only `revisions.length >= 20`, so 127 (or 21) stays green — the guard cannot catch it.
GAP-b (low/medium, overstated absolute): the claim "no model identifier in any repo artifact (only commit trailers)" is falsified in committed, R-CORE-rendered artifacts: "Grok" in the shipped PROV footer (innovation-slide-seed-drs.ts:36 and :85) and in soi-drs-revisions.gen.ts:559; "Model changed to Opus 4.8 mid-round" in docs/traceability/drone-2525.ledger.json:156; "Claude · OpenAI · Grok · Gemini" in docs/traceability/create-doc.ledger.json:6. Fair nuance: the buyer-facing deck CELLS are clean (grep of the slide-seed/data cells for competing model names finds none), and these are provenance/attribution notes — but the claim as WORDED is not honest. Also honesty-adjacent: several gates under-assert their headline claims (the >=20 floor above; the tautological replay filter in traceability.test.mjs:32-33 that can never fail), giving false assurance of completeness.

=== BOTTOM LINE ===
Across the 48h the process was genuinely run, not faked. both-refs = CONFIRMED and live-verified; gates-before-push = CONFIRMED mechanism with a ~8% record hole (10/124 rounds unstamped) and an unenforced cadence claim; persist-first = ordering CONFIRMED but the integrity stamp is missing on the 4 newest asks; honest-reporting = strong culture but two real defects — a stale flagship traceability panel (0.127 vs shipped 0.130) and an overstated "no model identifier anywhere" claim. The failures are freshness/completeness/stamp-discipline gaps, not process abandonment. Highest-priority process fix: re-run gen-rcore-revisions so the shipped record self-agrees, add sha256 sidecars for the four newest asks, and tighten the gate floors so green means complete.

## Remediation shipped in response (this session)

Operator decisions (2026-09-26): model identifiers → **redact at render, keep the record**; scope → **fix all + Phase C + Phase D**. Fixes, each gated (tsc · full test:ci · next build), pushed both refs, Verify Live:

1. **[HIGH] SoI R-CORE panel freshness** — re-ran `gen-rcore-revisions.mjs` → `soi-drs-revisions.gen.ts` at 0.130; strengthened `rcore-revisions.test.mjs` to assert the gen revision-count equals the DRS source (not just `>=20`) + D#-render; wired the generator into prebuild so it cannot drift.
2. **[MED] Model-identifier render leak** — a shared `redactModelIds` applied in the R-CORE adapters (`fromLedgerJson`/`fromDrsRevisions`) so no model token ('Opus 4.8'/'Grok'/…) renders on the compare/download while the append-only ledgers keep their exact words; scrubbed the editable deck seed cells (PROV/S1 'Grok'); added a source-scan gate.
3. **[MED] DRS `iterations[]` n-counter** — renumbered to a clean monotonic 1..N (revisions/decisions untouched); fixed `drs-render.mjs` 'of 99' → the real count; added a monotonic-unique-n gate.
4. **[MED] 4 asks missing `.sha256`** — wrote the sidecars for the 4 newest asks; added an ask-hash gate for the work window.
5. **[MED] S2 status cell self-contradiction** — `innovation-slide-seed-drs.ts` status cell → 'rev 0.130 · D1–D132'.
6. **[MED] `traceability.test.mjs` tautology** — replaced the vacuous replay check with a per-entry content/chain check.
7. **[LOW] Stale badge docstring** — corrected `rcore-badge.tsx` docstring to the shipped normal-flow/last-child behaviour.
8. **Concept (G1) gate alignment (operator 2026-09-26)** — flipped the PRJ-34 row `gate` G2→G1 so the financials render in the Concept column + PRB 'Current Gate G1 · Concept'; updated the one locked assertion (`innovation-time.test.mjs:369`). Same numbers (gate-independent roll-ups); the S10 grid shows the correct 4-year Concept window. Reconciles the code row to the master, which already declared Concept (G1).
9. **TAM / SAM (operator 2026-09-26)** — populated the blank rows, DECLARED/IA + SOURCED: TAM ~$30B (Critical Event Management category, $23.45B 2023→$45.18B 2030 @9.2%), SAM ~$10B (crisis-management software segment, ~$9.5–9.8B 2025→~$10B 2026 @~5.9%).
10. **Drone tab title (r.153)** + **R-CORE compare mirror (Vision-2525)** — the two queued operator asks.

Not fixed this pass (weak-guard hardening, noted): deck SSSES is self-authored (ungated), law-6 is a length-only guard, per-round gate cadence is unverifiable from the record. These are coverage gaps, not shipped defects.
