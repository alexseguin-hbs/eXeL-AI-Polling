# Overnight review — the guided pod and the advised-members run, twelve lenses, 2026-09-12

> gotta sleep; update and spiral and assess and test overnight fixing discrepancies as you go

Workflow `wf_5e5fb9a2-9ea`, twelve reviewer lenses in parallel over commits 0088fa4 (the guide) and 7078d8a (the advised run), each asked forward 1→10 and backward 10→1, a grade A–F, every defect as a CLASS with its members, and 111 words. Persisted verbatim before any fix. The fixes that followed are listed at the end with their commits.

| Lens | Grade | Defects | Blocks |
|---|:-:|:-:|:-:|
| Aset | C | 9 | 6 |
| Asar | B | 5 | 4 |
| Athena | C | 6 | 4 |
| Christo | C | 6 | 4 |
| Enki | C | 6 | 2 |
| Enlil | C | 8 | 5 |
| Krishna | C | 8 | 2 |
| Odin | C | 7 | 4 |
| Pangu | C | 7 | 4 |
| Sofia | C | 8 | 3 |
| Thoth | C | 8 | 4 |
| Thor | C | 8 | 1 |

## Aset — consistency and theme reinforcement — grade C

**Forward 1→10.** A first-timer gets the right frame — "Step 4 of 7 · Work", seven chips with counts, a roster, one amber card — and on the happy path the button does take them to the field and outline it. But the card is not one voice: chip, sentence and button are derived by two separate switches, so Ana reads "YOUR TURN / Waiting for 2 to join / Enter your name ↓" in one box, and Bo reads "WAITING / Running — ♡ time is being witnessed" over a clock at 0:00:00 with "Start the clock" live below him. A person who arrives to join sees Step 1 of 7 and an amber arrow pointing at the lead's Intent box; and every destination the translated button points at is hardcoded English.

**Backward 10→1.** The members' typed inputs trace cleanly: Lea's 3 h × 4.807 and Ireland, Ana's Brazil plus GPS, Bo's Metro Manila, the three 1 h claims capped to the clock — all appear in log.txt and in the receipt, and the three phones carry the same synthesis. Past that the receipt claims more than happened. Its synthesis says "no time was saved" while line 1 on the same page prints Δ -2.99 h (the ◬ were withheld for unmet conditions), and asserts the awaiting lines went "to zero" while 5b still reads awaiting for Bo. "Lea 0.00 h → 웃 0.022" cannot be checked by the person holding it. The 95 green steps include predicates that accept the failure text (`≈ \$|awaiting…`) and a "333-word" check that passes 340 (164+94+82).

- **The guide card speaks with three voices at once** (blocks) — *class:* One moment, one truth: the state chip, the sentence and the action button must come from a single derivation. Today `explain` and `guide` are two independent switches over `phase`, so the card can assert three different states of the same instant.
  - members: frontend/app/soi-session/page.tsx:598-621 (`explain`) vs :629-662 (`guide`) — separate switches; :837-849 renders chip=guide.state, sentence=explain, button=guide.label · :647-648 active: a non-lead resolves to `wait` while "Start the clock" and the sticky "Stop" are both live on his phone, and soi.pod.x.active says "Running — ♡ time is being witnessed" unconditionally, before any segment exists · :643 + :443 invite: the lead's action needs `agreedTo === lock.hash`, explain (:604-606) tests only `m.agreed`, so a phone whose baseline did not verify (:361) shows "All three agreed" with no action offered to anyone · screenshots step-02-invite-ana.jpg and step-04-clock-bo.jpg
  - evidence: frontend/app/soi-session/page.tsx:837-849; docs/assessments/pod-live-run-2026-09-12-asm/step-04-clock-bo.jpg
- **The machine still speaks on the members' screens, and this page has no operator door** (blocks) — *class:* No string a pod member can reach names a vendor, a source file, a hash or the device's storage; infrastructure language goes to ?diag=1 and the console. The pod page has no ?diag=1 (grep "diag" returns nothing) and the mechanical gate covers only the sign flow, so the class is defended nowhere here.
  - members: frontend/app/soi-session/page.tsx:1122 "○ local (live sync when Supabase is reachable)" — invite phase, under the QR, on all three phones · :1492 "Source: hi_rates.py, the live settlement table the paper names." · :1533 "…, hash <8 chars>" on the accelerator card · :991 "This device would not keep a copy of the pod… free some space and reopen." · :1486 and :1489 "from the same jurisdiction's table" · gate scope: frontend/tests/sign-lexicon-voice.test.mjs:17 matches only `soi\.sign\.` keys and sign-flow.tsx; frontend/tests/pod-invariant.test.mjs:799 bans hi_rates.py only inside the UsdBeside slice — the instance, not the class
  - evidence: frontend/app/soi-session/page.tsx:1122, :1492; frontend/tests/pod-invariant.test.mjs:799
- **The guided path is translated; every place it leads is English** (blocks) — *class:* Every visible string reaches the reader through t(). The 34 new guide keys are lexicon-backed, but the controls they point at are hardcoded, so a non-English member follows a translated instruction to an untranslated field — roughly 42 hardcoded visible strings against 46 t() calls on the page.
  - members: frontend/app/soi-session/page.tsx:829 "Task • Outcome" · :862 "Your Trinity — the three leads…" · :888-890 the three-is-the-minimum note · :892 "Intent — what the pod is trying to do" (the target of soi.pod.guide.a.intent) · :906 "Measurable outcome — how we know it is done" · :917 "You — the lead" · :994 "Come back to a pod" · :1032 · :1035 · :1051 · :1107 · :1113 "Share this QR" · :1135 "The trio — join, comment & approve" · :1169 and :1468 "Settles at" · :1205 · :1262-1263 · :1351 · :1382 · :1387 "The evidence chain" · :1431 · :1448 · :1483-1492 · :1528 "Accelerator" · receipt lines :1611-1634 in their entirety
  - evidence: frontend/app/soi-session/page.tsx:892; docs/assessments/pod-live-run-2026-09-12-asm/step-02-invite-ana.jpg
- **One measured quantity, five renderings — the member cannot check the arithmetic** (blocks) — *class:* Every figure a person reads is printed at a precision that lets the next figure be derived from it. The same witnessed time is currently rounded five different ways across the carriers of one screen, and one of them rounds to zero while still minting.
  - members: frontend/app/soi-session/page.tsx:1629 receipt 5b "Lea 0.00 h → 웃 0.022" (toFixed(2)) · :1634 receipt 6 "0.0138 h" (toFixed(4); the committed receipt still shows 0.013793333333333335, fixed only after the run in eca4186) · :1612 "0.01 person-hours counted" · :1421 "counted as 0.00 h" · :1533 "Witnessed: 0.01 h" · frontend/lib/pod-synthesis.ts:135 "0.01 cross-reviewed hours settled as 0.07 웃" against the receipt's 웃 0.066 · the mono ABC forms "0.0049..2362" beside each
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (receipt lines 5b and 6); frontend/app/soi-session/page.tsx:1629
- **The synthesis and the roster assert states they did not measure** (blocks) — *class:* A panel never claims more than happened, and a sentence about why something did not happen is derived from the same reason code the panel shows. Two carriers of one fact are derived independently, so the receipt and the synthesis on the same phone disagree.
  - members: frontend/lib/pod-synthesis.ts:138 — a single else-branch prints "No time was saved against the frozen baseline" for every yaTriangle===0, while frontend/app/soi-session/page.tsx:1543 distinguishes conditions_unmet / no_time_saved / no lock; this run saved 2.99 h (receipt line 1 prints "Δ -2.99 h") and the conditions were unticked · the same paragraph restates the members' outcome ("prints a USD figure on every line… to zero") while 5b on the same receipt reads "awaiting a dated exchange-rate source" for Bo · frontend/app/soi-session/page.tsx:684 roster `case "closed": return row(true, …)` marks all three "settled" unconditionally
  - evidence: frontend/lib/pod-synthesis.ts:138; docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json
- **Assertions widened to the observed output — 95 green steps that cannot go red** (confuses) — *class:* A check must be able to fail. Three assertions on the receipt accept both the outcome and its negation, or a tolerance wide enough to swallow the miss, so "0 failures" is not evidence the pod's own measurable outcome was met.
  - members: frontend/scripts/pod-live-run-2026-09-11.mjs:193 `/≈ \$|awaiting a dated exchange-rate source/` — the failure text passes · :84 the same predicate, widened only when INP is set (without the simulated inputs it requires `≈ \$`) · :179 `total >= 333 && total <= 341` with no per-paragraph check, logging "333-word synthesis … 164 + 94 + 82 = 340" as OK against a code comment that says each paragraph is EXACTLY 111 words · docs/feedback/POD_Walkthrough_AsM_Assisted_2026.09.12.html repeats "its 333-word synthesis" to the operator · frontend/tests/pod-invariant.test.mjs:799 scoped to one slice
  - evidence: frontend/scripts/pod-live-run-2026-09-11.mjs:179,193; docs/assessments/pod-live-run-2026-09-12-asm/log.txt:71-73
- **One seat, three names on one screen** (confuses) — *class:* A person is named identically in every carrier of a screen. The roster falls back to m.role while the seat block and the approval line use their own wording, so the new who-has-done-what list does not match the thing it summarises.
  - members: frontend/app/soi-session/page.tsx:676 `firstOf(m.name) || m.role` → "Lead 2" / "Lead 3" in the roster (frontend/components/pod-roster-list.tsx) · the seat block header "MEMBER 2" · the approval checkbox "Member 2 approves the intent, outcome and plan" · the QR line ":1125 you are seat 2" · the receipt's "Lea · Ana · Bo"
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/step-02-invite-ana.jpg; frontend/app/soi-session/page.tsx:676
- **The first screen has no state for the person who came to join** (blocks) — *class:* The guide offers a turn for every role at every phase. Compose is written for the lead only, so the one card and the next-field outline point an invited member away from the join box directly above them.
  - members: frontend/app/soi-session/page.tsx:633-638 (compose branch has only the lead's four fields, then `pod-open`) · :869-887 the join-by-code form sits above it with no guide state of its own · :851 the roster is hidden on compose (`phase !== "compose"`), so nothing else tells a joiner where they are · :671-672 the amber data-next outline lands on `pod-intent`
  - evidence: frontend/app/soi-session/page.tsx:633-638
- **Copying the receipt can fail in silence** (cosmetic) — *class:* Every action a member takes reports what happened, in one sentence. The new Completed line's copy is the last act of the journey and its failure path is empty.
  - members: frontend/app/soi-session/page.tsx:689-692 `copyReceipt` — `.catch(() => {})` and `navigator.clipboard?.` short-circuits with no message; only success flips to "Copied" (:1603)
  - evidence: frontend/app/soi-session/page.tsx:691

*111 words:*

Aset's lens is consistency, and the pod's new card breaks it in the same way three times: chip, sentence and button are derived separately, so one screen says Your turn, Waiting for two to join, and Enter your name at once, and Bo reads Running while his clock shows zeroes. The DocuSign frame is right; its wiring is not. Beneath it the machine speaks — Supabase, hi_rates.py, a hash — on a page with no operator door, and forty-two English strings sit under a translated button. The receipt is green because its checks accept the failure text. Fix the derivation, one truth per moment, and the page becomes simple for anyone.

## Asar — synthesis and outcome validation — grade B

**Forward 1→10.** A stranger is told where they are and what to press: `phase-step` gives "Step 6 of 7 · Witness", the single `your-turn` card carries one sentence, one amber button, and the control it scrolls to wears the `data-next` outline (7-audit-witness-lead.jpg, 1-compose-plan-lead.jpg); the roster answers "am I waiting for someone?" by name. The derivation at page.tsx:629-662 has a target for every phase and every seat, and nothing was folded away that used to be visible — `git show 0088fa4` deletes no JSX text node, so release rule 6 holds. Two things break the promise for a real person: outside English the button is translated and the field it outlines is not (36 `t("soi.pod.*")` calls against ~39 hardcoded literals below line 830), and the screens still say Supabase, hi_rates.py, MoT, SSSES, TOK-17, D9.

**Backward 10→1.** Most of the receipt traces: 5b's per-person floors, the GPS supplement, the plan→actual delta and the three member outcomes all reach back to a keystroke in log.txt or a row in 2026-09-12_pod_asm_assist.md. Two things do not. The synthesis on all three phones states "No time was saved against the frozen baseline" while the same run's audit screen reads "2.99 h earlier than the estimate" (page.tsx:1534) and its own receipt reads "Δ -2.99 h" — pod-synthesis.ts:138 infers a cause from a zero because `SynthesisInput` never carries the reason the page already computed. And the run's "95 steps, 0 failures" is produced by presence assertions ("USA-equivalent line present", "receipt shows the ledger grammar"), so a receipt printing `0.013793333333333335 h` passed, and was only caught afterwards by eca4186 — leaving walkthrough.json and step-08/09 one commit stale against the shipped code.

- **The receipt narrates a zero as if it were its own cause** (blocks) — *class:* INVARIANT: a sentence a member reads states the reason the page computed, never a reason inferred from an amount being zero. The accelerator has three distinct reasons (conditions_unmet · no_time_saved · no lock); the synthesis collapses all three into one, and picks the false one.
  - members: frontend/lib/pod-synthesis.ts:136-138 (the `inp.yaTriangle > 0` branch) · frontend/lib/pod-synthesis.ts:36-48 (SynthesisInput carries accelDelta/yaTriangle but no reason field, so the truth cannot reach the sentence) · frontend/app/soi-session/page.tsx:1543 (the correct three-way reason, rendered only on the audit screen) · frontend/app/soi-session/page.tsx:764-777 (the call site that could pass accelRead.reason and does not) · the shipped consequence on all three phones: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json, synthesis paragraph 2, lead/ana/bo
  - evidence: frontend/lib/pod-synthesis.ts:138 vs frontend/app/soi-session/page.tsx:1534 ("2.99 h earlier than the estimate", visible in 7-audit-witness-lead.jpg)
- **One quantity, a different precision at every print site** (blocks) — *class:* INVARIANT: a number a member reads is printed by one formatter — never a raw float, and never rounded to a figure that reads as zero beside a non-zero amount derived from it. Precision is currently decided per print site, so the same hour appears as 0.013793333333333335, 0.01, 0.00 and 0.0138 on screens a person sees in one sitting. eca4186 fixed two members and gated exactly those two.
  - members: frontend/app/soi-session/page.tsx:1570 `{witnessedHours} witnessed hours` (raw float, audit screen) · :1565 `none from the {witnessedHours} h` · :749 `${witnessedHours}h cross-reviewed` in the Transcript artefact rendered at :1650 · :1613 `witnessedHours.toFixed(2)` → "0.01 person-hours counted" · :1629 `.toFixed(2)` → "Lea 0.00 h → 웃 0.022 → $0.38" · :1421 "counted as 0.00 h" · :1634 `vintage.hours.toFixed(4)` · gate frontend/tests/pod-invariant.test.mjs:802-803 pins only vintage.hours and claimOf(i).hours
  - evidence: frontend/app/soi-session/page.tsx:1570; screenshot docs/assessments/pod-live-run-2026-09-12-asm/7-audit-witness-lead.jpg ("0.013793333333333335 witnessed hours")
- **The member meets the machine — the pod page is outside every voice gate** (blocks) — *class:* INVARIANT: no screen a pod member reaches names a vendor, a source file, a settlement table or an internal register; enforced mechanically, in all 33 languages. The gate that enforces it (sign-lexicon-voice) scans only soi.sign.* keys and components/sign/sign-flow.tsx, so app/soi-session/page.tsx has no gate at all; pod-invariant substitutes a single-instance grep.
  - members: frontend/app/soi-session/page.tsx:1122 "local (live sync when Supabase is reachable)" (vendor) · :1492 "Source: hi_rates.py, the live settlement table the paper names." (source file) · :1570 "MoT clocked" · :195 "SSSES qualification passed" as a checkbox label on the audit screen · :889 "(TOK-17 · D5 anti-sybil)" · :1483-1489 "D9 —" prose · the footer "— MoT" and the "CRS list — from Vision 2525" block, both visible in 1-compose-plan-lead.jpg · gate scope: frontend/tests/pod-invariant.test.mjs:799 greps hi_rates.py only inside the UsdBeside slice; frontend/tests/sign-lexicon-voice.test.mjs:17-52 never reads this page
  - evidence: frontend/app/soi-session/page.tsx:1122 and :1492; frontend/tests/pod-invariant.test.mjs:799
- **The guide speaks 33 languages; the controls it points at speak one** (blocks) — *class:* INVARIANT: every visible string is a t() key with a lexicon entry. The new guide layer honoured it (34 soi.pod.guide.* keys) but the surface it guides did not, so in any non-English locale the amber button is translated and the outlined field beneath it is raw English — the DocuSign clarity survives only in English.
  - members: frontend/app/soi-session/page.tsx:862 "Your Trinity — the three leads…" · :889 "A pod is exactly three…" · :892 "Intent — what the pod is trying to do" · :903 and :909 placeholders · :906 "Measurable outcome — how we know it is done" · :1114 "Share this QR" · :1115 "The other two scan to join…" · :1161 placeholder "enter your name" · :1358 "what the work produced, in your words" · :1418 placeholder "hours" · :1565/:1570 the hours lines · :1593 "Settle & issue the receipt" · :1595 "Every member self-audits…" · :1601 "Settled & receipted by the pod." (~39 literal text nodes below line 830, 12 of 13 placeholders) · gate frontend/tests/pod-invariant.test.mjs:797-798 counts guide keys only and fails on no new literal
  - evidence: frontend/app/soi-session/page.tsx:892 and :1593; frontend/tests/pod-invariant.test.mjs:798
- **"0 failures" is asserted on presence, not on legibility or truth** (confuses) — *class:* INVARIANT: a run that certifies a receipt asserts what the receipt SAYS, not that a line exists. Every defect above was on screen during the 95-step run and every one of them logged OK, so the run cannot distinguish a correct receipt from a wrong one — and the committed evidence now disagrees with the shipped code.
  - members: docs/assessments/pod-live-run-2026-09-12-asm/log.txt — "USA-equivalent line present for seat 1/2/3", "receipt shows the ledger grammar", "receipt shows PLAN → ACTUAL", "receipt renders each member's OWN outcome text" (presence only) · the same log's "333-word synthesis … 164 + 94 + 82 = 340 words" logged OK against a heading that says 333 (frontend/app/soi-session/page.tsx:1663) · walkthrough.json receipt line 6 still prints 0.013793333333333335 h, and step-08-receipt-*.jpg / step-09-synthesis-*.jpg record the pre-eca4186 state · frontend/scripts/pod-live-run-2026-09-11.mjs is the harness that would carry a value assertion
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/log.txt (34573ms lines) and walkthrough.json ("receipt", line 6)

*111 words:*

Forward, the envelope model lands. Step 6 of 7, one amber sentence, one button, the roster, the outlined field — a stranger is never asked to guess, and nothing was removed to get there. Backward is where I withhold the grade. The receipt tells three phones no time was saved while its own line reads minus 2.99 hours; the page's computed reason was discarded at the door of synthesis. The audit screen prints 0.013793333333333335 witnessed hours. A member reads Supabase, hi_rates.py, MoT, SSSES. The run logged ninety-five passes because it asked whether lines existed, never whether they were legible or true. Fix the formatter, the reason, and the gate — together.

## Athena — strategic planning and flow — grade C

**Forward 1→10.** Step 1 of 7, the amber "Your turn" card, the plain-verb button and the who-has-done-what list do genuinely carry a stranger from brief to receipt: every one of the fifteen targets the guide points at exists (I checked each testid), the next field is outlined, and the phase rail names the step. But the card sits on top of an unchanged screen — the compose shot shows the join box, a TOK-17 · D5 paragraph, Domain Play, the plan, region, Seed membership, a CRS demo and a "Prototype · local state" footer all still competing under one instruction. And the guide only advises: on Ana's and Bo's phones the chip reads Waiting while pod-accept, pod-clock-toggle, pod-next and pod-settle-btn are all live and pressable, and a joiner whose relay drops (canEditSeat, !connected → lead only) sits at Waiting with a blank label and no reason, on every phase.

**Backward 10→1.** Most of the receipt traces: each figure has a member who typed it, a clock that measured it and a witness who flipped it, and the three phones settle identically. What does not trace is the receipt's own arithmetic as printed — "Lea 0.00 h → 웃 0.022 → $0.38" on line 5b beside "0.013793333333333335 h" on line 6 gives a reader two irreconcilable statements of the same hour, and the audit screen adds a third ("Witnessed: 0.01 h") and a fourth ("counted as 0.00 h"). Worse, the artefact itself is stale: walkthrough.json still carries the raw float that commit eca4186 claims to have closed, every screenshot is stamped SHA 254ef2e — a commit that predates the UI in the picture — and log.txt holds 94 lines, not the 95 claimed, none of which touch your-turn, phase-step, pod-roster or pod-copy. The run that certifies "the guided UI" never exercised the guide.

- **The float gate names two lines, so the class survives on the screen it was meant to clean** (blocks) — *class:* Invariant: no hours figure a member reads is printed at machine precision. It is enforced by asserting two specific source strings rather than by scanning every hours render, so any site the fix did not visit still prints the raw double.
  - members: frontend/app/soi-session/page.tsx:1570 ({witnessedHours} witnessed hours — raw) · :1565 (none from the {witnessedHours} h, which settle as 웃 — raw) · :1533 ({lock.hours} h) · :1612 (witnessedHours.toFixed(2) person-hours counted) · :1421 and :1505 and :1625 (toFixed(2)) · :1634 (toFixed(4)) · gate frontend/tests/pod-invariant.test.mjs:802 names only vintage.hours and claimOf(i).hours
  - evidence: frontend/app/soi-session/page.tsx:1570; docs/assessments/pod-live-run-2026-09-12-asm/7-audit-witness-bo.jpg ("0.013793333333333335 witnessed hours")
- **The same hour is printed at four precisions, and 0.00 h settles a non-zero 웃** (confuses) — *class:* Invariant: two figures on the same person's screen that derive from one measured quantity must reconcile by eye. Each site rounds independently, so a duration rounds to zero at one place while the token it produced prints non-zero at another.
  - members: page.tsx:1505 (claim 0.00 h at 4.807× = 웃 0.022) · :1625 receipt-each (Lea 0.00 h → 웃 0.022 → $0.38) · :1612 (0.01 person-hours counted) · :1634 (0.0138 h stamped) · :1381 (measuredHours.toFixed(2)) · :1533 (Witnessed: 0.01 h) · :1421 (counted as 0.00 h — the pod was witnessed for 0:00:16)
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt line 5b; frontend/app/soi-session/page.tsx:1505
- **THE SIGNER NEVER MEETS THE MACHINE is gated on the signing surface only; the pod surface is ungated and names the machine** (blocks) — *class:* Invariant: no screen a member reads names a vendor, a source file, an environment value or an internal code. The gate reads only soi.sign.* keys and sign-flow.tsx, so every other member-facing surface — the pod page, its receipt, its synthesis — is outside the fence.
  - members: page.tsx:1122 ("local (live sync when Supabase is reachable)") · :1492 ("Source: hi_rates.py, the live settlement table") · :1116 ("their login (email / OAuth)") · :889 ("TOK-17 · D5 anti-sybil") · :195 ("SSSES qualification passed") · :1733 ("CRS list — from Vision • 2525") · :1761 ("Prototype · local state") · :1533 ("hash 99268037") · frontend/app/layout.tsx:29 (commit SHA banner on every page) · the member text and synthesis carrying docs/asks/2026-09-12_fx_to_usd.psv, tests/pod-invariant.test.mjs, FX_TO_USD, usdEquivalent (walkthrough.json) · gate scope frontend/tests/sign-lexicon-voice.test.mjs:9,17,23
  - evidence: frontend/app/soi-session/page.tsx:1122 and :1492; docs/assessments/pod-live-run-2026-09-12-asm/1-compose-plan-lead.jpg
- **The guide advertises a turn that no control enforces** (confuses) — *class:* Invariant: if the card says it is not your turn, the control it is protecting is not actionable on your phone. The guide derives whose turn it is from isLead/canEdit, but every shared button's disabled predicate ignores the seat entirely, so Waiting and pressable coexist.
  - members: page.tsx:1588 pod-settle-btn (guide: isLead; button: !allWitnessed||!allSelfAudited) · :1215 pod-accept (guide: isLead; button: !allAgreed) · :1277 pod-clock-toggle (guide: isLead; no seat check) · :1280 pod-stop (guide: non-lead; any seat) · :1366 pod-next (any seat) · guide derivation :629-662
  - evidence: frontend/app/soi-session/page.tsx:1588 vs :657; docs/assessments/pod-live-run-2026-09-12-asm/7-audit-witness-bo.jpg (chip WAITING above an enabled "Settle & issue the receipt")
- **"Waiting" is also what a dead end looks like — the guide has no state for cannot-act** (blocks) — *class:* Invariant: every phase gives the person on the phone either an action or a reason. guide's wait branch returns an empty label and no button, and me = -1 (no editable seat) collapses into the same wait, so a joiner before seat assignment and any joiner whose relay is down read the identical blank card at every phase.
  - members: page.tsx:631 (wait has label "" and target null) · :640 invite me<0 → wait · :645 sync · :648 active · :652 record · :657 audit · :633-638 compose has no me<0 branch, so a code-entry visitor is told "Write the intent" while the join box above is never a guide target (no pod-join testid) · frontend/lib/pod-roster.ts:343 (!connected → only the lead may edit any seat)
  - evidence: frontend/app/soi-session/page.tsx:631,640; frontend/lib/pod-roster.ts:343
- **The evidence bundle is not regenerated when the code that prints it changes, and does not exercise what it certifies** (blocks) — *class:* Invariant: an assessment folder shown to the operator must be produced by the commit it is filed under and must assert the change it is filed for. Nothing re-runs or freshness-checks the walkthrough after a fix, and nothing requires the run to touch the new surface.
  - members: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt line 6 still "0.013793333333333335 h" after eca4186 fixed it · all 47 screenshots stamped SHA 254ef2e, a commit predating the Step-N-of-7 UI they show · log.txt is 94 lines against "95 steps" claimed in commit 7078d8a and in the brief · zero log steps assert your-turn, your-turn-action, phase-step, pod-roster, data-next or pod-copy
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (receipt, line 6) vs commit eca4186; log.txt (94 lines, no guide assertions)

*111 words:*

The card is the right idea: one step, one sentence, one button, and the roster reads at a glance. But the guide only advises; every control it reserves for the lead stays pressable on the other two phones, so "Waiting" is a suggestion, not a turn. Below the card the screen is unchanged: a commit hash, Supabase, hi_rates.py, TOK-17, SSSES, and hours printed four ways: 0.00 h settling 0.022 웃 beside 0.013793333333333335. The float gate named two lines instead of the class, and two survive. The run that certifies this never clicked the new card, and the receipt on the record predates its fix. Nothing was removed; the folding rule held.

## Christo — consensus and user flow — grade C

**Forward 1→10.** A first-timer is genuinely carried: "Step 4 of 7 · Work" over seven chips, one amber "Your turn" card with a single plain-language action, an outlined next field, and a roster that names who still owes what — the DocuSign spine is really there, and the three phones reached a receipt with no dead end (log.txt, 95 steps, 0 failures). It breaks on the two things the card promises. First, the card's "Waiting" is advisory only: Bo's phone reads WAITING while "Settle & issue the receipt" sits live beneath it (7-audit-witness-bo.jpg), Ana's reads WAITING beside a live "Start the clock" (4-active-ready-ana.jpg), and log.txt:139 records a joiner ending the work phase for all three. Second, everything the card points at is untranslated hardcoded English (53 `t()` calls against ~79 hardcoded strings on the same page), so outside English the guide speaks and the controls do not.

**Backward 10→1.** Most of the receipt traces cleanly: the plan (3 h × 4.807), Ireland as pod default, Washington/Brazil+GPS/Metro Manila elections, three 1 h self-audits, the cap to the clock and the identical three-phone synthesis all match `2026-09-12_pod_asm_assist.md` line for line, and the 340-word synthesis is inside the sanctioned 333–341 band. Three things do not trace. The receipt prints `0.013793333333333335 h` on line 6 and `Lea 0.00 h → 웃 0.022 → $0.38` on 5b — a figure no person typed and a zero that contradicts the money beside it. All 48 screenshots stamp `SHA: 254ef2e`, a commit that predates 0088fa4 and therefore cannot have rendered the guide card they show, while log.txt and walkthrough.json carry no SHA or date at all. And `pod-live-run-2026-09-11.mjs:119` prints the literal "(2 h × 3)" as an observation, contradicting line 2 of its own log. No UI was removed — the settle prose and evidence chain are folded under `details-settle`/`details-evidence` with their testids intact; rule 6 holds.

- **The guide says "Waiting" while the same screen leaves the step's irreversible control live for that person** (blocks) — *class:* Every control the guide withholds from a person must be disabled for that person. Today the guide's who-acts logic (isLead / me) lives only in the card; not one target button consults it, so "Waiting" is decoration and any of the three phones can advance or end the pod at any time.
  - members: frontend/app/soi-session/page.tsx:1214-1220 pod-accept (disabled={!allAgreed} only; guide reserves it for isLead) · :1276-1279 pod-clock-toggle (disabled={!lock}; guide reserves it for isLead) · :1573-1591 pod-settle-btn (disabled={!allWitnessed || !allSelfAudited}; guide reserves it for isLead) · :1365-1368 pod-next (no seat gate) · :1280-1283 pod-stop, which the guide actively hands to non-leads at :648 as their one action, making the terminal action the joiner's prompt. Screens: 3-agreed-elected-bo.jpg (Bo WAITING, "Accepted by the trio" live), 4-active-ready-ana.jpg (Ana WAITING, "Start the clock" live), 7-audit-witness-bo.jpg (Bo WAITING, "Settle & issue the receipt" live).
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/log.txt:139 "a JOINER pressed Stop & record — one route, segment 2 closed for everyone"; frontend/app/soi-session/page.tsx:1573
- **The pod surface names the machine — the signer-voice gate was never extended past the sign flow, and its one pod fix was scoped to a slice** (blocks) — *class:* No screen a member reads may name a vendor, a source file, a backend or an internal spec identifier. The mechanical gate (frontend/tests/sign-lexicon-voice.test.mjs, in test:ci) reads only soi.sign.* keys and components/sign/sign-flow.tsx; there is no soi.pod.* / soi-session equivalent, so the pod page is defended by taste alone — and the one fix that was made names a single call site.
  - members: Visible strings in frontend/app/soi-session/page.tsx: :1122 "local (live sync when Supabase is reachable)" (vendor) · :1492 "Source: hi_rates.py, the live settlement table the paper names." (source file, inside the Details fold) · :1685-1686 "Written by Cube 6 (Gemini/OpenAI) from the recorded outcome." and "…once the AI backend is online (Semi-Automated → Autonomous)" (two vendors, a cube number, a backend) · :889 "(TOK-17 · D5 anti-sybil)" · :1387 "clock-in → cross-review (TOK-17)" · :1644 "one ledger record, read four ways (TOK-26)" · :1483/:1489 "D9 —" · :1533 "hash {lock.hash}" · :1733 "CRS list — from Vision • 2525" · the accelerator condition "SSSES qualification passed" (visible in 7-audit-witness-bo.jpg). The gate: frontend/tests/pod-invariant.test.mjs:799 asserts hi_rates.py is absent only within page.slice(indexOf('function UsdBeside'), indexOf('const WHITE_PAPER')) — the instance, not the class.
  - evidence: frontend/app/soi-session/page.tsx:1122, :1492, :1685; frontend/tests/pod-invariant.test.mjs:799
- **Hours a person reads are printed raw, or rounded to a figure that contradicts the money beside it** (blocks) — *class:* Every hours figure on a member-facing surface is formatted exactly once, at a precision that can never contradict the currency printed next to it. Commit eca4186 rounded two call sites and gated those two by name; the same expression survives at six others, and its mirror image (over-rounding to 0.00 h beside a non-zero payment) is not covered at all.
  - members: frontend/app/soi-session/page.tsx:746 receipt transcript `${parseFloat(m.hours) || 0}h` · :749 `${witnessedHours}h` and `${accelDelta}h` · :1264 `{lock.hours} h` · :1565 `{witnessedHours} h` · :1570 `{witnessedHours} witnessed hours` · :1697 `${parseFloat(m.hours)}h`. The over-rounded twin: receipt line 5b prints "Lea 0.00 h → 웃 0.022 → $0.38", "Ana 0.00 h → 웃 0.022 → R$0.16", "Bo 0.00 h → 웃 0.022 → ₱1.92", and each audit card reads "counted as 0.00 h". Gate covering only two members: frontend/tests/pod-invariant.test.mjs:802.
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt line 6 "0.013793333333333335 h"; screens step-08-receipt-bo.jpg and 7-audit-witness-bo.jpg ("0.013793333333333335 witnessed hours")
- **The guide translates; everything it points at does not** (blocks) — *class:* Every visible string reaches the reader through t(). The DocuSign work added 34 soi.pod.guide.* keys and left the surrounding page in hardcoded English, so in any of the 32 non-English languages the card says "Paso 4 de 7 · Su turno" and the field, the button and the receipt it points at stay English. 53 t() calls against ~79 hardcoded visible text nodes on one page.
  - members: frontend/app/soi-session/page.tsx:862 "Your Trinity — the three leads…" · :889 the three-is-the-minimum paragraph · :892 "Intent — what the pod is trying to do" · :906 "Measurable outcome — how we know it is done" · :917-918 · :924 placeholder="Your name" · :991 the lose-the-pod warning · :994 "Come back to a pod" · :1032 · :1035 · :1051 · :1107 · :1113 "Share this QR" · :1135 "The trio — join, comment & approve" · :1205 · :1219 "Accepted by the trio — go to synchronized start" · :1222 · :1278/:1282 the clock and record buttons · :1330 · :1351 · :1382 · :1387 · :1448 · :1478 · :1516-1517 · :1526-1533 · :1551 · :1591 "Settle & issue the receipt" · :1593 · :1606 · :1611 · :1618-1644 the receipt's own six lines · :1663 · :1691 · :1709 · :1733 · :1761. No gate: pod-invariant.test.mjs:797 counts guide keys only; soi-i18n-contract.test.mjs covers DEFAULT_SOI keys, not soi.pod.*.
  - evidence: frontend/app/soi-session/page.tsx:892, :1219, :1591; frontend/tests/pod-invariant.test.mjs:797
- **The Completed screen's only action can fail without saying so** (confuses) — *class:* A finished artefact must never be discarded, and the person must always have a route off the phone that visibly fails when it fails (AAR 2026-09-09). The pod's last screen offers exactly one route, and it is written so that both of its failure modes are silent.
  - members: frontend/app/soi-session/page.tsx:689-692 copyReceipt — `void navigator.clipboard?.writeText(text).then(…).catch(() => {})`: the optional chain returns undefined where the API is absent (non-secure context, older iOS in-app browsers) and the catch swallows a denied permission, so the button neither flips to "Copied" nor reports anything. There is no second route on that screen — no share, no download, no selectable plain-text block — and no assertion in frontend/tests/pod-invariant.test.mjs:792 beyond the existence of the handler. Same single-route shape at the settled screen for all three seats.
  - evidence: frontend/app/soi-session/page.tsx:691
- **The run's own evidence cannot be tied to the build that produced it, and one log line is a literal** (confuses) — *class:* Every artefact of a run carries the build identity and reports only what it observed. Here the stamp is stale on all screenshots, absent from the two text artefacts, and one step string is a constant printed as an observation.
  - members: All 48 .jpg files in docs/assessments/pod-live-run-2026-09-12-asm/ render the header "SHA: 254ef2e" (frontend/app/layout.tsx:29 reads NEXT_PUBLIC_GIT_SHA baked at server start, next.config.js:35) — 254ef2e precedes 0088fa4 and cannot contain the guide card the same images show. log.txt and walkthrough.json carry no SHA, no commit and no date. frontend/scripts/pod-live-run-2026-09-11.mjs:119 prints "approved intent, outcome AND the plan (2 h × 3)" as a literal on every run, contradicting log.txt line 2 ("plan set: 3 h at 4.807×") in the same file; the same script's line 159 hardcodes "the 1 h claim".
  - evidence: frontend/scripts/pod-live-run-2026-09-11.mjs:119; docs/assessments/pod-live-run-2026-09-12-asm/log.txt:2 vs :27

*111 words:*

The envelope lands. Step 4 of 7, one amber card, a roster, an outlined next field: three phones followed it to a receipt without a single dead end, and nothing was removed, only folded. My lens is consensus, and consensus is where it breaks. Bo's card reads Waiting while Settle and issue the receipt sits live beneath it; Ana is told to wait beside Start the clock; the log records a joiner ending the work for everyone. A card that says wait must not leave the door open. Then the receipt prints 0.013793333333333335 hours and 0.00 h earning $0.38. Gate the pod's voice the way the signer's voice is already gated.

## Enki — diversity and edge cases — grade C

**Forward 1→10.** On the happy path a newcomer is genuinely served: `pod-phase-rail.tsx:31` gives "Step N of 7 · Brief", the amber card at `page.tsx:837` gives one sentence and one button, `pod-roster-list.tsx` gives who has done what, and `data-next` outlines the field being asked for. It breaks at the first choice the guide does not know about — on the compose screenshot (`1-compose-plan-lead.jpg`) the card already reads "Your turn · Ready — share the QR & open the pod" while the multiple, the pod place, the signer, the Domain Play list and the brainstorm radio sit below it undecided and unmarked, because the compose branch at `page.tsx:633-638` tests only intent, outcome, name and hours. And a first-timer who is not reading English meets the entire guide in English: all 34 `soi.pod.guide.*` keys exist only as `englishDefault`, while 45 sibling `soi.pod.*` keys are translated in all 32 language files.

**Backward 10→1.** Most of the receipt does trace: the log's 95 steps each name a person and a control, every screenshot exists per seat per step, and the capped-claim, GPS-supplement and per-person-floor lines each have a step behind them. Three things do not. `walkthrough.json` line 6 still reads `0.013793333333333335 h`, the exact float commit eca4186 says it fixed — the source was regexed, the artefact of record was never reissued, so the shipped receipt and the shipped screen now disagree. Line 5b reads "Lea 0.00 h → 웃 0.022 → $0.38": a person is paid for zero hours because `page.tsx:1629` rounds hours to two places at the print site alone. And the heading "333-word synthesis" (`page.tsx:1663`) sits beside its own count of 340, with 164/94/82 per paragraph, logged OK three times.

- **The pod screen names the machine; the voice gate only guards the sign screen** (blocks) — *class:* INVARIANT: no string a pod member can reach names a file, a vendor, a store, an internal service or a register code. It is enforced for `soi.sign.*` and `sign-flow.tsx` only, so the entire pod surface is ungated and is currently in breach.
  - members: frontend/app/soi-session/page.tsx:1492 ("Source: hi_rates.py, the live settlement table the paper names.") · :1122 ("○ local (live sync when Supabase is reachable)") · :1686 and :1685 ("Cube 6 (Gemini/OpenAI) writes these tiers once the AI backend is online (Semi-Automated → Autonomous)") · :889 ("TOK-17 · D5 anti-sybil") · :1387 ("(TOK-17)") · :1644 ("(TOK-26)") · :1483 and :1489 ("D9 —") · frontend/lib/pod-rates.ts:934 (the `fx` branch prints `u.source` verbatim into UsdBeside, unbounded free text). The two gates that should catch these: frontend/tests/pod-invariant.test.mjs:799 scans only the 10-line slice between `UsdBeside` (page.tsx:132) and `WHITE_PAPER` (page.tsx:142), so it passes while :1492 ships; frontend/tests/sign-lexicon-voice.test.mjs:18 matches only keys beginning `soi.sign.`.
  - evidence: frontend/app/soi-session/page.tsx:1492 · frontend/tests/pod-invariant.test.mjs:799
- **The 34 new guide strings are English-only in a 33-language product** (blocks) — *class:* INVARIANT: a new visible string is not shipped until it exists in every language, not merely in lexicon-data.ts. The gate counts English defaults, so an untranslated release passes.
  - members: All 34 `soi.pod.guide.*` keys, frontend/lib/lexicon-data.ts:803-836 — every one has `englishDefault` and no translation. Verified 0 occurrences of `soi.pod.guide.` in each of the 32 files under frontend/lib/i18n-sign/ (ar, bn, cs, da, de, el, fi, fr, he, hi, id, it, ja, ko, ms, ne, nl, no, pa, pl, pt, ro, ru, sv, sw, th, tl, tr, uk, vi, zh) and 0 in lexicon-translations.ts, lexicon-translations-sign.ts, lexicon-translations-soi-r228.ts, lexicon-translations-es-sign.ts — while 45 sibling `soi.pod.*` keys ARE present in every one of those 32 files. The gate at frontend/tests/pod-invariant.test.mjs:797-798 asserts only `guideKeys >= 34` against lexicon-data.ts.
  - evidence: frontend/tests/pod-invariant.test.mjs:797 · frontend/lib/i18n-sign/de.ts (45 soi.pod.* keys, 0 soi.pod.guide.*)
- **"Your turn — Ready" is shown above controls the guide has never heard of** (confuses) — *class:* INVARIANT: every control that changes what the receipt says is either a step in the guide's chain or is settled before the card can read Ready. Today the chain is a hand-listed 15 testids, so any control outside that list is unmarked, unnamed and invisible to the roster.
  - members: Controls with testids that are never a guide `target` and never receive `data-next`: `pod-band` (the multiple, page.tsx ~1015) · the pod place select and each seat's LocalityElect (page.tsx:718 `localityOf`; this election decides the currency and the amount each person is paid, and all three members used it on the run) · `baseline-signer` (page.tsx:1022) · `rung-select` (page.tsx:1559, the ♡ ladder) · `member-gps-fix-` · the Domain Play list and the "Brainstorm a task not on this list" radio (page.tsx:960) · the record-method picker. The compose branch (page.tsx:633-638) returns `turn("soi.pod.guide.a.open")` as soon as intent, outcome, name and hours are non-empty. Separately, `wait` (page.tsx:631) returns `label: ""` and `target: null`, so a joiner who arrives at a full pod (page.tsx:640, `me < 0`) gets a "Waiting" chip with no instruction and no button.
  - evidence: frontend/app/soi-session/page.tsx:633 · docs/assessments/pod-live-run-2026-09-12-asm/1-compose-plan-lead.jpg
- **Figures on the receipt contradict each other, and the rounding fix created a new pair** (confuses) — *class:* INVARIANT: no two numbers a person reads on the same line may disagree. Rounding was applied per print site (eca4186) rather than to the reading as a whole, so each site is individually well-formed and the line is not.
  - members: walkthrough.json line 5b: "Lea 0.00 h → 웃 0.022 → $0.38", "Ana 0.00 h → 웃 0.022 → R$0.16", "Bo 0.00 h → 웃 0.022 → ₱1.92" — zero hours, three payments; from page.tsx:1629 `.toFixed(2)`. walkthrough.json line 6 still "0.013793333333333335 h" although page.tsx:1634 now prints `.toFixed(4)` — the artefact of record was never reissued after eca4186, so the commit message and the committed receipt disagree. walkthrough.json "Plan → actual 3 person-hours planned · 0.01 person-hours counted · Δ -2.99 h" beside "3 h × 4.807 = 웃 14.421", from the unrounded `{parseFloat(baselineHrs)}` at page.tsx:1028. Unformatted table floats still reach the screen at page.tsx:1483 `{d9.rate}` and page.tsx:1634 `{vintage.rate}`. The gate (pod-invariant.test.mjs:802) tests two literal source patterns, not the property.
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (receipt, lines 5b and 6) · frontend/app/soi-session/page.tsx:1629
- **"333-word synthesis" is disproved by the count printed beside it** (confuses) — *class:* INVARIANT: a heading never states a quantity the same screen contradicts (the panel-never-claims-more rule, applied to counts rather than to states).
  - members: frontend/app/soi-session/page.tsx:1663 heading "333-word synthesis — 3 paragraphs" against :1664 which prints the live total (340 on this run) and :1676 which prints each paragraph's own count (164, 94, 82). The comment at page.tsx:762 promises "each paragraph is EXACTLY 111 words" while frontend/lib/pod-synthesis.ts:13 states the opposite ("need NOT be exactly 111 words per paragraph"). The run recorded the contradiction as a pass three times, log.txt at 34301/34431/34558 ms: "333-word synthesis on this phone: 164 + 94 + 82 = 340 words". Nothing in pod-invariant.test.mjs gates the heading against the count.
  - evidence: frontend/app/soi-session/page.tsx:1663 · docs/assessments/pod-live-run-2026-09-12-asm/log.txt:34301ms
- **The advised run varied nothing, so it is not evidence about a first-time member** (confuses) — *class:* INVARIANT (my own lens): a run offered as proof that a UI is followable must contain at least one member who is not the author's twin — one unfamiliar language, one refusal, one wrong turn. This run contains none, so 95/95 green measures the script, not the screen.
  - members: All three seats reused the previous showcase's seats; my Bosnia advice (the hardest lawful seat, an entity rate with no national figure) was overruled at docs/assessments/2026-09-12_pod_asm_assist.md, Bo section, Thor: "tonight varies one thing" — and then nothing varied. All three claimed exactly 1 h (log.txt 29358/29451/29558 ms); all three approved; all three witnessed 2 of 2; 95 steps, 0 failures, 0 refusals, 0 back-navigation. Every phone ran the English UI although one seat elected Brazil and one the Philippines and the product ships 33 languages. The typed intent and outcomes are engineers' text and print verbatim on the member-facing receipt: "docs/asks/2026-09-12_fx_to_usd.psv", "tests/pod-invariant.test.mjs" (walkthrough.json synthesis.lead paragraph 1), "the generator, FX_TO_USD and usdEquivalent to receipt line 5b" (walkthrough.json outcomes[2]). Uncovered paths that the guide code shows can be reached: the full pod (page.tsx:640), a member who never elects a place and is settled as "inherited" (page.tsx:718-720), a withheld witness, a claim below the clock, a member who leaves mid-clock. NOTE — no UI removal found: the evidence chain and the settlement prose are folded under `details-evidence` and `details-settle`, not deleted, and the only string replaced (page.tsx:135, the hi_rates USD-mirror parenthetical) was substituted, not dropped.
  - evidence: docs/assessments/2026-09-12_pod_asm_assist.md (Bo · Thor) · docs/assessments/pod-live-run-2026-09-12-asm/log.txt

*111 words:*

Diversity is the lens, and this run has none. Three seats, three identical hours, ninety-five green steps, one language, and the same two places as yesterday: my advice to elect the hardest lawful seat was overruled so tonight would vary one thing, and nothing varied. A first-timer is served well on the happy path; the step line, the card, the outlined field are gains. The card says Ready above six undecided controls, the election that decides what a person is paid is never pointed at, and every non-English phone reads the guide in English. Backward, the receipt still prints zero hours beside a dollar figure, and claims 333 words over 340.

## Enlil — implementation and build verification — grade C

**Forward 1→10.** Steps 1–3, 5 and 7 land: the chip says Your turn, the sentence names one action, the amber button scrolls to and outlines the real control, and at witnessing it names the person still owed — a first-timer can follow those. Step 4, the screen where the work actually happens, contradicts itself: Ana's card reads WAITING beside "Running — ♡ time is being witnessed" while the clock shows 0:00:00, the roster shows all three ✓ ready with nobody pending, and two enabled buttons sit below with no next-field mark. The guide speaks 33 languages; every control it points at speaks only English, and a claimed hour comes back as "counted as 0.00 h".

**Backward 10→1.** Most of it traces: the plan (3 h × 4.807), the three elected places, the 1 h claims capped to a 0:00:16 clock, and the per-seat figures all reconcile between the assist doc, the log, the screenshots and receipt lines 3–6. What does not: 22 of 60 step() calls take the ok=true default, so a third of a "0 failures" record asserts nothing — one prints "the plan (2 h × 3)" against a locked 3 h × 4.807. The receipt's own claim fails: the synthesis asserts the measurable outcome ("psv grows from 1 row to 29… the 60 to zero") while line 5b shows Bo awaiting and the named file holds one row. Nothing on the record touches the guide it was run to certify.

- **The guide card's state is not derived from the state of the controls beneath it** (blocks) — *class:* INVARIANT: on every phase and every seat, the card's chip, sentence and button agree with which controls are enabled, and a WAITING card names someone who is actually pending. Today the chip is derived per-seat, the sentence per-phase, and the controls per-permission — three sources, so they can disagree on one screen.
  - members: frontend/app/soi-session/page.tsx:646-648 (active: a non-lead with 0 segments returns wait, while pod-clock-toggle at :1276 is disabled only on !lock and pod-stop at :1280 only on 0 segments — both live for that seat) · frontend/lib/lexicon-data.ts:790 soi.pod.x.active is static, so it reads "Running — ♡ time is being witnessed" when span.segments.length===0 and the clock prints 0:00:00, contradicting page.tsx:1258 ("All three are ready. Start the clock to begin the work.") one block below · page.tsx:681 roster at active reports all three "✓ ready" under a WAITING chip — nobody to wait for · lexicon-data.ts:791 soi.pod.x.record, same static shape at step 5 · page.tsx:627 `me = members.findIndex((_,i)=>canEdit(i))` — on one phone canEdit is true for every seat, so me is always 0 and isLead always true: seats 2 and 3 can never be given a turn on that device
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/4-active-ready-ana.jpg (WAITING · "Running…" · 0:00:00 · Start the clock + Record the outcome both live); frontend/app/soi-session/page.tsx:646
- **A quantity that is not zero is printed to a member as 0.00, beside the tokens it minted** (confuses) — *class:* INVARIANT: no figure a member reads renders a non-zero amount as zero. eca4186 fixed the sibling symptom (raw float on line 6) by rounding; rounding is the other half of the same class and was not enumerated.
  - members: frontend/app/soi-session/page.tsx:1630 receipt 5b `(v ? v.hours : claimOf(i).hours).toFixed(2)` → "Lea 0.00 h → 웃 0.022" · page.tsx:1611 `witnessedHours.toFixed(2)` → "0.01 person-hours counted" against a 0:00:16 clock · the audit line "counted as 0.00 h — the pod was witnessed for 0:00:16" (screenshot step-07-audit-ana.jpg) · page.tsx:590 closed explain `.replace("{h}", witnessedHours.toFixed(2))` · walkthrough.json receipt line 5b, all three seats read 0.00 h
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (receipt line 5b); frontend/app/soi-session/page.tsx:1630
- **THE SIGNER NEVER MEETS THE MACHINE is gated on one surface; the pod names files, vendors and tables** (blocks) — *class:* INVARIANT: no string a member can reach names a file, a vendor, a table, a build or an environment. The mechanical gate reads only soi.sign.* keys and components/sign/sign-flow.tsx; the pod has no equivalent, and pod-invariant's one attempt checks a ten-line window.
  - members: frontend/tests/sign-lexicon-voice.test.mjs:17,23 (scope is soi.sign.* + sign-flow.tsx only — soi.pod.* unchecked in all 33 languages) · frontend/tests/pod-invariant.test.mjs:799 asserts !hi_rates.py only between `function UsdBeside` (page.tsx:132) and `const WHITE_PAPER` (page.tsx:142) — ten lines — while page.tsx:1492 renders "Source: hi_rates.py, the live settlement table the paper names." · page.tsx:1122 "○ local (live sync when Supabase is reachable)" · page.tsx:1686 "Cube 6 (Gemini/OpenAI) writes these tiers once the AI backend is online" · page.tsx:135 "a second floor from the platform's own US-dollar table" (also the one visible string 0088fa4 replaced rather than folded — a label change nobody asked for, release rule 6) · page.tsx:934 and :1115 "OAuth login" / "(email / OAuth)" · page.tsx:960 "Domain Play (wires into the Level-3 substrate)" · page.tsx:909 "baseline HAL" · the issued receipt itself carries docs/asks/…psv, tests/pod-invariant.test.mjs, FX_TO_USD, usdEquivalent
  - evidence: frontend/app/soi-session/page.tsx:1492 vs frontend/tests/pod-invariant.test.mjs:799
- **A third of the run record prints OK without asserting anything, and one such line states a figure the receipt contradicts** (blocks) — *class:* INVARIANT: every line on a run record is an assertion whose failure stops the run. `step(who, what, ok = true)` makes the unchecked case the default, so narration and proof are indistinguishable on the log a person reads.
  - members: frontend/scripts/pod-live-run-2026-09-11.mjs:47 (ok = true default) · 22 of 60 step() call sites pass no condition · :119 `step(who, 'approved intent, outcome AND the plan (2 h × 3)')` — the locked plan is 3 h × 4.807 (log.txt line 2; receipt "3 person-hours planned"; step-03-seat-ana.jpg "3 h × 4.807") · :75 plan label unasserted · :179 the line is labelled "333-word synthesis" while printing and passing 340 (tolerance 333–341 lives only in a comment)
  - evidence: frontend/scripts/pod-live-run-2026-09-11.mjs:47,119; docs/assessments/pod-live-run-2026-09-12-asm/log.txt:29
- **The run that certifies the DocuSign change never exercises the DocuSign change** (blocks) — *class:* INVARIANT: the run that certifies a release drives the release on a phone. Everything gated for commit 0088fa4 is a source-shape regex — it proves the strings exist in the file, never that a person can follow them.
  - members: docs/assessments/pod-live-run-2026-09-12-asm/log.txt — 95 steps, zero occurrences of your-turn, phase-step, pod-roster, data-next, pod-copy, completed, "Step N of 7" · frontend/scripts/pod-live-run-2026-09-11.mjs — none of those testids appear; the script clicks the underlying controls directly, so the guide could be absent and the run would still read 0 failures · frontend/tests/pod-invariant.test.mjs:778-799 — all 22 new assertions are regexes over page.tsx / pod-phase-rail.tsx / pod-roster-list.tsx source · no assertion anywhere that a WAITING card corresponds to a pending person, or that the guide's target is enabled for that seat
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/log.txt (95 lines, no guide reference); frontend/tests/pod-invariant.test.mjs:781
- **The closed screen asserts the outcome was achieved; nothing on the path checks it, and the same receipt refutes it** (blocks) — *class:* INVARIANT: the panel never claims more than happened. The pod can settle, stamp and synthesise with no gate asking whether the measurable outcome was met, and the synthesis grammar states the target as accomplished.
  - members: frontend/lib/pod-synthesis.ts:126 — "…and closed one synchronized session on a single measurable outcome: ${inp.outcome}", the target restated as fact · walkthrough.json synthesis ¶1 asserts "…psv grows from 1 row to 29… taking the 60 non-USD settling places… to zero" while the same file's receipt line 5b prints Bo "awaiting a dated exchange-rate source" and docs/asks/2026-09-12_fx_to_usd.psv holds 2 lines (one data row) · frontend/app/soi-session/page.tsx:653-657 the audit phase gates hours + witnesses only; no outcome-met condition guards pod-settle-btn · frontend/app/soi-session/page.tsx:138-140 fmtGps documents "two decimals of a degree" and ships toFixed(4) — a second invariant asserted in a comment and contradicted by the record ("GPS -23.5505, -46.6333")
  - evidence: frontend/lib/pod-synthesis.ts:126; docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (synthesis ¶1 vs receipt line 5b)
- **"Copy the receipt" copies less than the receipt on screen** (confuses) — *class:* INVARIANT: what a member carries away is the whole record they were shown. The copy is scoped to one testid, so anything rendered outside it is silently dropped and one line is pre-truncated.
  - members: frontend/app/soi-session/page.tsx:690 copyReceipt reads only `[data-testid="receipt-3"]`.textContent plus the three synthesis paragraphs · page.tsx:1609 line 1 is already cut at 80 chars with "…" inside receipt-3 (visible as "named so…" in walkthrough.json) · page.tsx:1637-1639 Intent, Outcome and the full Recorded text sit outside receipt-3 and are never copied · page.tsx:1645-1652 the four artefacts (transcript, portfolio, governance, settlement) are likewise excluded · frontend/tests/pod-invariant.test.mjs:792 asserts only that copyReceipt exists, never that its text equals the receipt
  - evidence: frontend/app/soi-session/page.tsx:690,1609
- **The simple layer is English-only, and so is every control it points at** (confuses) — *class:* INVARIANT (Lexicon gate): every user-facing string goes through t(), and a new key ships to the 33 languages. The 34 new guide keys exist only as englishDefault, and the phase blocks the guide points into were never brought through t() at all — so "super simple" is legible in one language.
  - members: frontend/lib/lexicon-data.ts:804-837 — soi.pod.guide.* appears in no translation file (grep -rl "soi.pod.guide" frontend/lib → lexicon-data.ts only), while its neighbour soi.pod.earns.* is translated · hardcoded strings on the member's path in frontend/app/soi-session/page.tsx: :862 "Your Trinity — the three leads who gather the pod's feedback" (also mislabels members as leads), :889, :892, :906, :917-918, :923, :929, :934, :960, :1231, :1241 "started ✓"/"tap to start", :1246 "Reset", :1258, :1264-1267, :1277-1283 the clock and stop button labels, :1326, :1606, :1609-1634 every receipt label ("Plan → actual", "Drawn & held", "Each at their own floor", "Stamped") · frontend/tests/pod-invariant.test.mjs:798 counts keys (>=34) but nothing forbids a hardcoded string on the page
  - evidence: frontend/lib/lexicon-data.ts:804; frontend/app/soi-session/page.tsx:1277-1283

*111 words:*

Enlil, on build verification. The scaffolding is real: Step 4 of 7, one card, a roster, an amber next-field mark. But the run that certified it never touched it — ninety-five steps, not one naming your-turn, the roster, or the copy. Twenty-two of sixty step calls default to OK and assert nothing, and one of them prints a plan the receipt contradicts. On Ana's screen the card says WAITING beside 'Running', over a clock at zero and two live buttons. The receipt declares an outcome the same page refutes, and hi_rates.py reaches a member because the gate reads ten lines. Good bones; the gates verify text, not journeys for a person.

## Krishna — integration across modules — grade C

**Forward 1→10.** On three connected phones a newcomer is genuinely carried: Step N of 7, one amber "Your turn" card whose button scrolls to and outlines the exact control, a who-has-done-what list, and a Completed line — the 95-step run walks it without a stall, and every target the guide names exists (page.tsx:627-662 against the testids at 902, 923, 1009, 1161, 1189, 1237, 1277, 1335, 1358, 1418, 1434, 1588, 1603). It breaks the moment the pod is not three phones: `me` is the first seat this phone may edit, and offline `canEditSeat` gives the lead all three, so the guide finishes seat one and then shows "Waiting" with no button through invite, sync, record and audit while the only person present still has the other two seats to fill. Even when it works the card speaks twice at once — chip "YOUR TURN", sentence "Waiting for 2 to join", button "Enter your name" (2b-joined-ana.jpg) — because `explain` is pod-scoped and `guide.label` person-scoped and nothing reconciles them. And the guide is the only translated layer: every field and button it points at is hardcoded English, so the simplification stops at the English reader.

**Backward 10→1.** Most of the receipt traces: the plan, the two segments, the per-seat floors, the D9 line and each member's own outcome text all come from what the phones typed, and the log derives its labels from the INPUTS file. Three things do not. The issued receipt still prints "0.013793333333333335 h" on line 6 and the Portfolio artefact still builds `${witnessedHours}h` raw (page.tsx:749) — eca4186 fixed one printer, not the class, and the gate greps two exact substrings. The run's "0 failures" over-reads itself: `step()` defaults `ok = true`, so 32 of 60 call sites assert nothing, a screenshot that failed to capture logs OK, and two labels hardcode values the run never used ("the plan (2 h × 3)" against a plan of 3 h × 4.807). Worst for the signer rule, what the members typed — `docs/asks/2026-09-12_fx_to_usd.psv`, `tests/pod-invariant.test.mjs`, `FX_TO_USD`, `usdEquivalent` — traces perfectly and is printed on every phone.

- **The guide is bound to one seat, so it stalls wherever one person holds more than one** (blocks) — *class:* The whole guide is derived from `me = members.findIndex(canEdit)` — the FIRST seat this phone may edit — and every phase branch reads `members[me]` only. `canEditSeat` (lib/pod-roster.ts:343) returns true for ALL seats to the lead when the relay is not connected, so `me` is permanently 0 and the guide can never name the work left on seats 2 and 3. The invariant that is not guaranteed: whenever this phone can still act, the card offers an action.
  - members: frontend/app/soi-session/page.tsx:627 (`me`); :641-643 (invite — checks members[me] only, then Waiting until allAgreed); :645 (sync — own Ready only); :650-652 (record — own outcome only); :654-657 (audit — own hours, and `j` is computed only for reviewer `me`); :648 (active — non-lead waits with no action until a segment exists). Ungated: frontend/tests/pod-invariant.test.mjs:783-788 asserts the targets exist, never that a person who can act is given one.
  - evidence: frontend/app/soi-session/page.tsx:627,641-657 with frontend/lib/pod-roster.ts:343
- **Machine words reach the member's screen — the voice gate only guards soi.sign.* lexicon values** (blocks) — *class:* The mechanical defence of THE SIGNER NEVER MEETS THE MACHINE scans lexicon VALUES under the `soi.sign.` prefix. It never scans the pod page's JSX literals, the text members type that the receipt echoes, or the synthesis template's output — so on the pod every one of those three carriers is unguarded, and one hit is literally a word in the gate's own BANNED list.
  - members: frontend/app/soi-session/page.tsx:1691-1693 ("Written by Cube 6 (Gemini/OpenAI)…", "once the AI backend is online (Semi-Automated → Autonomous)" — vendor names + "backend"); frontend/app/layout.tsx:29 (SHA / build-date strip above every pod screen); page.tsx:889 ("TOK-17 · D5 anti-sybil"); :1387 ("TOK-17"); :1644 ("TOK-26"); :135 ("a second floor from the platform's own US-dollar table"); :1062 ("dataset … verified … scope"); :1570 ("MoT clocked"); echoed member text and the synthesis: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt line 5a and synthesis ¶1 print "docs/asks/2026-09-12_fx_to_usd.psv", "tests/pod-invariant.test.mjs", "FX_TO_USD", "usdEquivalent", "receipt line 5b". Gate scope: frontend/tests/sign-lexicon-voice.test.mjs:9,19,51.
  - evidence: screenshot 2b-joined-ana.jpg (the approval screen shows the .psv path and the test filename); frontend/app/soi-session/page.tsx:1691
- **Raw floating-point numbers still reach the screen — the rounding fix named one call site** (confuses) — *class:* Numbers are formatted at each printer rather than at one boundary, so every new printer starts unrounded. eca4186 rounded the stamped hours and the per-claim rows and gated exactly those two expressions; every other hours figure still interpolates the raw float.
  - members: frontend/app/soi-session/page.tsx:749 (Portfolio artefact `${witnessedHours}h` — also enters the copied text); :746 (Transcript `${parseFloat(m.hours) || 0}h`); :1565 (`{witnessedHours} h`); :1570 (`{witnessedHours} witnessed hours`); :1264 and :1533 (`{lock.hours}`); :1697 (`${parseFloat(m.hours)}h`). Gate: frontend/tests/pod-invariant.test.mjs:802 forbids two literal substrings only. Related precision mismatch on one line: :1640 prints hours at 2 dp beside 웃 at 3 dp, so the issued receipt reads "Lea 0.00 h → 웃 0.022 → $0.38".
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt line 6: "0.013793333333333335 h"; log.txt final line "Lea 0.00 h → 웃 0.022 → $0.38"
- **"Copy the receipt" copies part of the receipt** (confuses) — *class:* The copy is anchored to a single testid (`receipt-3`) instead of to the receipt region, so anything rendered as a sibling of that list is silently outside the copy — and the same anchoring means any future receipt block is dropped by default.
  - members: frontend/app/soi-session/page.tsx:690 (copyReceipt reads `receipt-3` + three synthesis paragraphs). Excluded: :1636-1638 (Intent, Outcome, Recorded) and :1642-1660 (Transcript · Portfolio · Governance · Settlement). Duplicate control on the same screen: the guide's amber button :845 and the Completed row's button :1603 both read "Copy the receipt".
  - evidence: frontend/app/soi-session/page.tsx:690 vs :1608 (`receipt-3` opens) and :1636
- **The guide card states two different things at once** (confuses) — *class:* `explain` (pod-scoped, what the pod is waiting for) and `guide.label`/`guide.state` (person-scoped, what YOU press) are derived in two independent switch statements and rendered inside one card with one chip. Nothing forces them to agree, so in every phase where the pod is waiting on someone else while you still have an action, the card reads as a contradiction.
  - members: frontend/app/soi-session/page.tsx:837-849 (the card composes both); invite :600-607 vs :639-643; sync :609 vs :645; record :611 vs :650-652; audit :613-618 vs :653-657.
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/2b-joined-ana.jpg — chip "YOUR TURN", sentence "Waiting for 2 to join — share the code or QR.", button "Enter your name ↓"
- **An "OK" line in the run log is not evidence that anything was checked** (confuses) — *class:* `step(who, what, ok = true)` prints OK when no assertion is passed, so narration and verification are indistinguishable in the 95-line receipt; and two labels carry literal values instead of reading the INPUTS file, so the log can assert a figure the app never held.
  - members: frontend/scripts/pod-live-run-2026-09-11.mjs:47 (`ok = true` default) — 32 of 60 `step(` call sites pass no condition; :54 (a crop that fails to capture is logged with ok=true); :119 ("approved intent, outcome AND the plan (2 h × 3)" while log.txt line 2 records the plan as 3 h × 4.807); :159 ("the 1 h claim" hardcoded rather than IN.seats[who].hours). Same family: the closed screen header says "333-word synthesis" beside a counter reading 340 (page.tsx:1671,1674).
  - evidence: frontend/scripts/pod-live-run-2026-09-11.mjs:47,119 against docs/assessments/pod-live-run-2026-09-12-asm/log.txt lines 2 and 27
- **The guide speaks 33 languages; every control it points at speaks one** (confuses) — *class:* The 34 new strings went through t(); the fields, placeholders, aria-labels, fold summaries and buttons the guide names are hardcoded English literals in the JSX, so the simplification layer translates and the thing to press does not. `soi.pod.*` also has zero rows in lexicon-translations.ts, so even the keyed half falls back to English everywhere.
  - members: frontend/app/soi-session/page.tsx:829 ("Task • Outcome"); :862; :888-889; :891; :906; :991; :994; :1009 (aria-label "planned hours", placeholder "hours"); :1032; :1035 (fold summary "Why these four, and every place"); :1107; :1113; :1135; :1161 (placeholder "enter your name"); :1205; :1219; :1263; :1351; :1358 (placeholder); :1368 ("Next — witness the hours"); :1385 and :1465 (t("…details") + an untranslated English suffix); :1387; :1418 (placeholder "hours"); :1551; :1590 ("Settle & issue the receipt"); :1593; :1606; :1644; :1671; :1691-1693; :1709. Coverage: 0 occurrences of "soi.pod" in frontend/lib/lexicon-translations.ts.
  - evidence: frontend/app/soi-session/page.tsx:1368 and :1590 (guide button keyed, target button not); grep -c soi.pod frontend/lib/lexicon-translations.ts → 0
- **No UI was removed — one string was reworded without an ask** (cosmetic) — *class:* Checked against release rule 6. The explainer line was moved into the card (testid pod-explain kept), the evidence chain and settlement prose were folded under Details with their testids intact (details-evidence, details-settle, pod-settle, pod-settle-d9), and the header pill now renders the phase's label key. The one substantive text change is a rewording, not a fold: the USD mirror branch's provenance clause.
  - members: frontend/app/soi-session/page.tsx:135 — `" (${u.source})"` became `" (a second floor from the platform's own US-dollar table, not a conversion)"` in 0088fa4; the named source the member used to read is gone from that branch (the fx branch still prints "rate of {asOf}, {source}"). No other visible element was deleted in 0088fa4 or 7078d8a.
  - evidence: git show 0088fa4 -- frontend/app/soi-session/page.tsx, line 54 of the diff

*111 words:*

Integration is where this lives. The guide layer is clean and derived, never duplicated — but it is bolted to one seat. me is the first editable seat, so a lead on a dropped relay, who may edit all three, is told to do seat one and then shown Waiting through four steps he must finish alone. The same seam runs the other way: the guide speaks in keys, the controls it points at speak hardcoded English, and the receipt it copies is missing the intent. Downstream, the members' own file paths and a vendor's name reach the phone. Fix seams at the class, not the screens. Nothing was removed tonight.

## Odin — foresight and future-proofing — grade C

**Forward 1→10.** On three connected phones the DocuSign move lands: "Step N of 7 · Brief", one amber "Your turn" card, one plain-language action, an outlined next field, a roster of who has done what, and a green Completed with a copy button — a stranger on the compose screen (1-compose-plan-lead.jpg) can act without reading anything else. It breaks the moment the configuration changes: the guide's actor is a single index `me = members.findIndex(canEdit)` (page.tsx:627), so offline — where `canEditSeat` grants the lead every seat (lib/pod-roster.ts:343) — the card shows "Waiting" with no button in invite, sync, record and audit, to the only person who can act; and a joiner still typing a code is told to "Write the intent". The chrome above the card also names a git SHA and build env vars on every screen, which is the machine the member was never supposed to meet.

**Backward 10→1.** Most of the receipt traces: the code, the seats, the two clock segments, the per-person floors and the identical settlement across three phones are all in log.txt and re-derivable. The load-bearing sentence does not. The synthesis states the typed outcome in the past tense (lib/pod-synthesis.ts:126) — "docs/asks/2026-09-12_fx_to_usd.psv grows from 1 row to 29 … taking the 60 … to zero" — while that file holds one row, FX_TO_USD holds one row, and line 5b on the same screen still reads "awaiting a dated exchange-rate source"; nothing checked, and the panel claims more than happened. The receipt of record also still prints 0.013793333333333335 h, the defect eca4186 says is gated, because the run was never re-issued; and the log's own assertions accept either outcome ("a figure by a traceable route, or the words for what is missing"; "340 words" logged as the 333-word synthesis), so 95 OK lines are not evidence.

- **The machine-vocabulary law is gated only on soi.sign.* lexicon values; every literal a member actually reads is ungated, and four of them name the machine** (blocks) — *class:* INVARIANT: no string rendered on a screen a member or signer can reach names a vendor, a source file, a build artefact or an environment variable. The gate (tests/sign-lexicon-voice.test.mjs) reads only englishDefault values of keys prefixed soi.sign., so hardcoded JSX literals and every soi.pod.* surface are outside it — the rule is enforced on the lexicon, not on the screen.
  - members: frontend/app/layout.tsx:29 ("SHA: <git sha> | Date | Time" from NEXT_PUBLIC_GIT_SHA/BUILD_DATE/BUILD_TIME — shared chrome above EVERY route, the pod and the signing flow included, visible top-of-frame in every screenshot in docs/assessments/pod-live-run-2026-09-12-asm/); frontend/app/soi-session/page.tsx:1122 ("○ local (live sync when Supabase is reachable)"); :1492 ("Source: hi_rates.py, the live settlement table the paper names."); :1062 (renders j.source verbatim — values "hi_rates.py" and "dataset-2026-09-12", lib/pod-rates.ts:734,794); :135 (fx branch still interpolates ${u.source} from FX_TO_USD rows, so any future row's text prints unfiltered — the mirror branch was fixed, this twin was not); frontend/tests/pod-invariant.test.mjs:799 (the guard slices only page.tsx between 'function UsdBeside' and 'const WHITE_PAPER', ~10 lines, while the live offender sits 1350 lines below it)
  - evidence: frontend/app/layout.tsx:29 · frontend/app/soi-session/page.tsx:1122,1492,1062,135 · frontend/tests/sign-lexicon-voice.test.mjs:9,17-19 · screenshot 1-compose-plan-lead.jpg (top bar)
- **The guide's actor is one seat index, so it stalls in every configuration except three connected phones** (blocks) — *class:* INVARIANT: at every step, the phone that CAN act is told what to do. The guide derives a single `me` (first seat this phone may edit) and then gates on members[me] or `isLead && all…`, so any configuration where one phone holds several seats, or no seat yet, produces a "Waiting" card with no action for the only person able to proceed. Not a compose-screen bug — a bug in how the guide models who is holding the phone.
  - members: frontend/app/soi-session/page.tsx:627 (me = findIndex(canEdit)); :633-638 (compose ignores isJoiner — a member who arrived to type a code is told "Write the intent"); :641-643 (invite: lead offline has named/agreed seat 0, seats 2-3 unreachable → wait); :645 (sync: same); :650-652 (record: own outcome in, others' unreachable → wait); :654-657 (audit: witness targets only reviewer index me, so seats 1-2's attestations are never offered → allWitnessed never true → wait forever); :675-687 (roster marks "you" on one line only); root cause frontend/lib/pod-roster.ts:343 canEditSeat returns true for EVERY seat when !connected, and :347 canWitnessAs likewise
  - evidence: frontend/app/soi-session/page.tsx:627,641-657 · frontend/lib/pod-roster.ts:343,347 · log.txt:7 ("live channel subscribed (relay)") — the offline path the code documents as supported was never run
- **The receipt asserts the typed outcome as accomplished fact and is contradicted on its own screen** (blocks) — *class:* INVARIANT: the panel never claims more than happened. buildSynthesis333 re-emits free-typed intent and outcome in the past tense ("set out to … and closed one synchronized session on a single measurable outcome: <outcome>") with no link to anything that verifies it, so whatever a member types becomes the document of record's finding. Every free-typed field that reaches a settled artefact is a member of this class, not just the outcome.
  - members: frontend/lib/pod-synthesis.ts:126 (past-tense restatement of inp.intent and inp.outcome); walkthrough.json synthesis paragraph 1 on all three phones ("docs/asks/2026-09-12_fx_to_usd.psv grows from 1 row to 29 … taking the 60 non-USD settling places … to zero, with tests/pod-invariant.test.mjs still green") against docs/asks/2026-09-12_fx_to_usd.psv (2 lines: header + 1 row) and frontend/lib/pod-rates.ts:611-613 (FX_TO_USD = 1 identity row); walkthrough.json receipt line 5b on the same screen still printing "awaiting a dated exchange-rate source for this currency" for Bo; frontend/app/soi-session/page.tsx:1620-1621 (5a echoes each member's outcome verbatim) and :1636-1638 (Intent / Outcome / Recorded reprinted unqualified)
  - evidence: frontend/lib/pod-synthesis.ts:126 · docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (synthesis[*].paragraphs[0], receipt 5b) · docs/asks/2026-09-12_fx_to_usd.psv
- **95 green steps, but the assertions accept either outcome** (blocks) — *class:* INVARIANT: a step logged OK must be false for at least one reachable state. Several checks in the run pass on both branches, or pass while reporting a number that contradicts their own label, so "0 failures" carries no information about the guided UI. The class is the assertion shape, not any one line.
  - members: log.txt:74-76 ("333-word synthesis on this phone: 164 + 94 + 82 = 340 words" logged OK, against frontend/lib/pod-synthesis.ts:49 TOTAL_TARGET = 333 and page.tsx:1663 label "333-word synthesis"); log.txt:4 and :81-83 ("a figure by a traceable route, OR the words for what is missing" — the awaiting text satisfies it, which is exactly the state the pod set out to remove); log.txt:59-61 ("claimed 1 h (above the clock — will be capped)" logged OK); log.txt:2 vs :2255 (plan 3 h against 0.01 h actual, no step asserts the plan was meaningful). Only log.txt:95 (three receipts settle identically) can fail.
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/log.txt:4,59-61,74-76,81-83 · frontend/lib/pod-synthesis.ts:49
- **One quantity, three precisions — the receipt prints 0.00 h beside 웃 0.022 and $0.38** (confuses) — *class:* INVARIANT: the same hours figure reads the same everywhere a person sees it, and never rounds to a value its neighbours contradict. eca4186 fixed formatting per call site by regex instead of introducing one formatter, so the receipt now carries the same quantity at three precisions and the 2-dp sites collapse a real settlement to zero.
  - members: frontend/app/soi-session/page.tsx:1629 (5b, toFixed(2) → "0.00 h → 웃 0.022 → $0.38"); :1612 (plan→actual, toFixed(2) → "0.01 person-hours counted"); :1634 (stamped, toFixed(4) → "0.0138 h"); :1505 (member card toFixed(2)); :1381 (measured toFixed(2)); :1421 (capped claim toFixed(2)); gate frontend/tests/pod-invariant.test.mjs:802-803 pins two literal expressions rather than requiring a single formatter
  - evidence: frontend/app/soi-session/page.tsx:1629,1612,1634 · walkthrough.json receipt ("5b · Each at their own floor Lea 0.00 h → 웃 0.022 → $0.38") · log.txt:95
- **"Copy the receipt" copies less than the receipt** (confuses) — *class:* INVARIANT: a control that names an artefact hands over that whole artefact. copyReceipt reads one element's textContent plus the synthesis, so everything the settled screen shows outside that element is silently dropped — including the block literally titled "The receipt".
  - members: frontend/app/soi-session/page.tsx:690 (copyReceipt reads only [data-testid="receipt-3"] + the three synthesis paragraphs); excluded: :1636-1638 (Intent / Outcome / Recorded), :1644 ("The receipt — one ledger record, read four ways (TOK-26)": transcript, portfolio, governance, settlement), :1691 ("Trio — accepted, witnessed & documented"), :1385/:1465 (the folded evidence chain and settlement prose). Nothing in frontend/tests/pod-invariant.test.mjs:792 checks what is copied, only that copyReceipt exists.
  - evidence: frontend/app/soi-session/page.tsx:690,1644 · frontend/tests/pod-invariant.test.mjs:792
- **The new guidance layer is English-only, and half of its own strings never reach the lexicon** (confuses) — *class:* INVARIANT: every visible string goes through t() and resolves in all 33 languages. The gate counts keys (>= 34) rather than checking that the strings on screen are keys or that any translation exists, so a t() call concatenated with a hardcoded English tail passes, and a key with zero translations passes.
  - members: frontend/tests/pod-invariant.test.mjs:797-798 (counts soi.pod.guide.* keys only); frontend/lib/lexicon-data.ts:803-836 (34 new keys, zero occurrences of soi.pod.guide in any lib/lexicon-translations*.ts or lib/i18n-*); frontend/app/soi-session/page.tsx:1385 and :1465 (t("soi.pod.guide.details") + hardcoded " — the evidence chain" / " — how this settles", both added by 0088fa4); untranslated literals on the same member surface: :862, :889 ("TOK-17 · D5 anti-sybil"), :892, :906, :917, :991, :1032, :1035, :1330, :1351, :1382, :1387, :1593, :1606, :1611, :1644
  - evidence: frontend/tests/pod-invariant.test.mjs:797-798 · frontend/lib/lexicon-data.ts:803-836 · frontend/app/soi-session/page.tsx:1385,1465

*111 words:*

Foresight asks what breaks later. Tonight's pod reads beautifully on three connected phones because three connected phones are the only configuration that exists. Unplug the relay and the same guide card says Waiting to the one person who can act; arrive to join and it tells you to write an intent you were never asked for. The receipt narrates an outcome the repository never produced, prints zero hours beside a dollar figure, and a git SHA sits above every screen a signer reads. The ninety-five green steps assert presence, not truth. Fold the explanations, keep them; but gate the invariant, not the instance, or tomorrow's phone finds the fourth dead end.

## Pangu — innovation — grade C

**Forward 1→10.** At the lead's seat the page is genuinely followable now: "Step 1 of 7 · Brief", one amber card naming the single action, a button that scrolls to and outlines the real control, a roster of who has done what, and a Completed line with a copy — nothing was removed to get there, only folded, so release rule 6 holds. It breaks for the other two seats. A member who opens the page to type a pod code is told "Your turn — Write the intent" and pointed at the lead's textarea, because the compose branch has no joiner case and `me` falls through to seat 0; and at Work the card reads WAITING while an enabled "Start the clock", an enabled sticky "Stop", and the prose "All three are ready. Start the clock to begin the work." sit on the same 375px screen. The instruction and the button are not the same authority, so the person who has never seen this page is told to wait by one half of it and to act by the other.

**Backward 10→1.** Most of the receipt traces: the elections, the two segments, the cap, the per-person floors and the identical three-phone settlement are all read off the page by the run, and the awaiting-a-rate line honestly refuses to invent a number. Two things do not. Line 5b prints "Lea 0.00 h → 웃 0.022 → $0.38" — a payment for zero hours, because each site rounds at its own precision with no rule that a nonzero figure may never print as zero; and line 6 of the committed walkthrough still carries 0.013793333333333335 h, the exact defect eca4186 says it fixed, because the artefact was never re-issued. Worse for this session's own claim: the 95-step run and its 48 screenshots touch none of the DocuSign work — no step, no crop, no JSON field names the guide card, the roster, the step line, the next-field mark or the copy — yet both commits report "0 failures on the guided UI", and one step caption asserts "(2 h × 3)" as a literal when the plan set two lines above was 3 h at 4.807×.

- **The guide tells a member to wait while the same screen offers them the button** (blocks) — *class:* Whose-turn is derived from role in the guide but from content in the DOM: the card's state and the control's `disabled` are computed by two different rules, so WAITING and an enabled control coexist (and a guided action can be pressed by someone the guide never addressed).
  - members: frontend/app/soi-session/page.tsx:647-648 (active: non-lead → wait) vs :1276-1279 pod-clock-toggle `disabled={!lock}` only, and the sticky Stop; :643 guide gates pod-accept on isLead vs :1215 `disabled={!allAgreed}` only; :657 guide gates pod-settle-btn on isLead vs :1588 (no role gate); :652 pod-next vs :1366 (content gate only); :651 all three members guided to the one shared field pod-record :1335 (no seat gate)
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/4-active-ready-bo.jpg — "WAITING · Running…" above an enabled "Start the clock" and "All three are ready. Start the clock to begin the work."; frontend/app/soi-session/page.tsx:647
- **The person who joins by code is guided as if they were the lead** (blocks) — *class:* The guide has no branch for a member who has not yet been seated: `me` resolves to the first editable seat (0) for any arrival, and the compose case never asks whether this phone is joining, so two of three people read the lead's instruction. The join control is not even in the guide's target vocabulary.
  - members: frontend/app/soi-session/page.tsx:627 (`me = members.findIndex(canEdit)`); :633-638 (case "compose", no isJoiner/joinCode branch); :869-887 the join form has no data-testid and no guide target; frontend/tests/pod-invariant.test.mjs:783 target list contains no join control
  - evidence: frontend/app/soi-session/page.tsx:633; docs/assessments/pod-live-run-2026-09-12-asm/1-compose-plan-lead.jpg (join box above, guide points past it)
- **The receipt pays money for zero hours** (blocks) — *class:* Every hours figure is rounded at whatever precision its own print site chose, and no rule forbids a nonzero quantity printing as 0.00 beside a nonzero settlement — so the receipt no longer reconciles with itself. The same quantity appears at 2, 4 and full precision on one page.
  - members: frontend/app/soi-session/page.tsx:1629 receipt-member `.toFixed(2)` ("Lea 0.00 h → 웃 0.022 → $0.38"); :1505 settle-member `.toFixed(2)`; :1421 claim-capped `.toFixed(2)`; :1612 receipt-plan `witnessedHours.toFixed(2)` ("0.01 person-hours counted"); :1533 "Witnessed: 0.00 h"; :618 the closed sentence `{h}=witnessedHours.toFixed(2)`; :1634 vintage `.toFixed(4)` — a third precision for the same hours
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json line 5b
- **The run offered as proof of the DocuSign change never looks at it** (blocks) — *class:* The receipt asserts a property the run did not measure: not one of the 95 steps, 48 screenshots or walkthrough fields exercises the guide card, the roster, the step line, the next-field outline, the Completed line or the copy button — the new work is defended only by source-regex in pod-invariant.
  - members: docs/assessments/pod-live-run-2026-09-12-asm/log.txt (0 of 94 lines match your-turn/roster/Step/data-next/copy); frontend/scripts/pod-live-run-2026-09-11.mjs:86,93,101,123,130,138,147,163,167,173 (every crop targets a control, never the instruction); walkthrough.json keys code/names/synthesis/receipt/outcomes; commit 0088fa4 and 7078d8a messages ("on the guided UI")
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/log.txt; docs/assessments/pod-live-run-2026-09-12-asm/step-06-outcome-bo.jpg (a 5 KB crop of a truncated input, no step, no instruction)
- **A fix shipped without re-issuing the receipt that showed the defect** (confuses) — *class:* The artefact that demonstrated a defect is left standing as the record after the code changed, and the runner that would refresh it is forked by date rather than extended — so the only receipt a reader has contradicts the shipped page.
  - members: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json line 6 ("0.013793333333333335 h") after eca4186; log.txt and all 48 screenshots predate eca4186; frontend/package.json:51 `pod:showcase` → scripts/pod-live-run-2026-09-11.mjs while frontend/scripts/pod-live-run.mjs sits unused beside it
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json line 6 vs commit eca4186
- **The pod's screens sit outside the machine-voice gate** (confuses) — *class:* THE SIGNER NEVER MEETS THE MACHINE is enforced mechanically only for soi.sign.* in lib/i18n-sign; nothing reads the pod page, so vendor names, source files and register codes reach the screens all three members read and copy.
  - members: frontend/tests/sign-lexicon-voice.test.mjs:36 (scope is lib/i18n-sign only); frontend/app/soi-session/page.tsx:1122 "local (live sync when Supabase is reachable)"; :1492 "Source: hi_rates.py, the live settlement table the paper names."; :889 "(TOK-17 · D5 anti-sybil)"; :1387 "clock-in → cross-review (TOK-17)"; :1483,1489,1631 "D9 —"; frontend/app/layout.tsx:29 the SHA banner over every pod screen (confirm with the operator before touching); and the receipt echoing typed paths — walkthrough.json 5a "FX_TO_USD and usdEquivalent to receipt line 5b"
  - evidence: frontend/app/soi-session/page.tsx:1122 and :1492
- **The instruction is translatable; the control it names is not** (confuses) — *class:* New guide strings went through t() (34 keys) but every destination the guide points at is hardcoded English, so a non-English member reads a translated instruction naming a button that does not exist in their language — the guided path is broken exactly where it is followed.
  - members: frontend/lib/lexicon-data.ts:819-836 (guide keys) vs frontend/app/soi-session/page.tsx:892 "Intent — what the pod is trying to do"; :906 "Measurable outcome — how we know it is done"; :1114 "Share this QR"; :1278 "Start the clock"/"Stop the clock"/"Add time"; :1282 "Stop & record the outcome"; :1591 "Settle & issue the receipt"; :1606 "Settled & receipted by the pod."; :862 "Your Trinity — …"; :889 the pod-of-three note; :1609-1634 the whole receipt
  - evidence: frontend/app/soi-session/page.tsx:1278 vs frontend/lib/lexicon-data.ts:827

*111 words:*

Innovation lens. The envelope idea is the right one, and one card that says whose turn it is, derived from state the page already holds, is the cheapest guidance this pod could have bought. But an instruction only innovates when it is the same authority as the button. Here the card says Waiting while Start the clock sits enabled two inches below, and a member who arrives by code is told to write the lead's intent. The receipt pays money for zero hours. Ninety-five green steps touch none of it. Make the guide the gate — derive enablement from it, and let the run assert what a person reads on screen.

## Sofia — multi-perspective analysis — grade C

**Forward 1→10.** On the lead's phone the envelope model lands: "Step 4 of 7 · Work", one amber sentence, one button that scrolls and focuses the real control, a who-has-done-what list, folded Details, Completed with copy — and every target the guide names exists (all eighteen testids resolve), so no button points at nothing. From the other two seats it thins: at ACTIVE, Ana reads the chip WAITING over the sentence "Running — ♡ time is being witnessed" while the clock beside it reads 0:00:00, no actor is named, no field is outlined, and two enabled buttons (Start the clock, Record the outcome — 1276 gates on `lock`, not on the seat) sit unmarked, so the guide's model of who may act is not the page's. Every waiting branch (648, 652, 657, 640) leaves the label empty rather than saying "waiting for Lea". A first-timer in one of the 33 languages meets a translated guide wrapped in an English page — the labels around it (892, 906, 1135, 1606, 1691) never went through t().

**Backward 10→1.** The provenance is genuinely clean: the assist doc's advised inputs (3 h × 4.807, Ireland default, Brazil, Metro Manila, Washington, 1 h claims, Ana's GPS fix) appear verbatim in log.txt and again in walkthrough.json's receipt and outcomes, the synthesis is identical on three phones, and nothing in the UI was removed by 0088fa4 — the prose was folded and the pill relabelled, as permitted. Where it breaks is arithmetic a reader can redo: the receipt says "Lea 0.00 h → 웃 0.022 → $0.38", line 4's 0.014 + 0.053 does not make line 3's 0.066, the synthesis says 0.07 웃 where the receipt says 0.066, and the heading "333-word synthesis" stands beside its own counter reading 340 — which the run logged as OK. Two fixes tonight were written to the instance, not the class: the hi_rates.py gate checks one slice of the file while line 1492 still prints it, and the hours gate pins two literals while 1565 and 1570 still interpolate a raw float that a screenshot shows on screen. One reconciliation (Christo) refuses positions — "Sofia's eight currencies", "her São Paulo fix" — that are not in the drafts as recorded.

- **Receipt figures cannot be re-derived at the precision they are printed** (blocks) — *class:* Every figure a person reads is printed at a precision at which the relation it asserts still holds — hours × M = 웃, floor + held = settles, and one quantity reads the same in every carrier.
  - members: frontend/app/soi-session/page.tsx:1505 ("0.00 h at 4.807× = 웃 0.022"); :1421 ("counted as 0.00 h" while 웃 settle); :1618 (0.014 + 0.053 vs :1614 0.066); :1612 (2 dp) vs :1634 (4 dp) for the same hours; :749 receipt.portfolio; frontend/lib/pod-synthesis.ts:67 num() → "0.01 cross-reviewed hours settled as 0.07 웃"
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json — receipt line "5b · Each at their own floor Lea 0.00 h → 웃 0.022 → $0.38"; step-08-receipt-lead.jpg
- **Raw floating-point hours survive the commit that claims to have removed them** (blocks) — *class:* No unformatted number reaches a member's screen; the gate must state the invariant, not name the two lines that failed.
  - members: frontend/app/soi-session/page.tsx:1570 ({witnessedHours} witnessed hours); :1565 ("none from the {witnessedHours} h"); :749 (`${witnessedHours}h cross-reviewed`); gate frontend/tests/pod-invariant.test.mjs:802 pins only the literals {vintage.hours} and {claimOf(i).hours} — 426 assertions pass with the above live
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/7-audit-witness-bo.jpg — "0.013793333333333335 witnessed hours"; frontend/app/soi-session/page.tsx:1570
- **The machine speaks on screens the three members read** (blocks) — *class:* No screen a pod member can reach names a file, a vendor, a build, an internal cube, framework or acronym; it speaks about their work and their next action (SIGNER NEVER MEETS THE MACHINE).
  - members: frontend/app/soi-session/page.tsx:1492 ("Source: hi_rates.py, the live settlement table"); :1122 ("live sync when Supabase is reachable"); :1686 and :1685 ("Cube 6 (Gemini/OpenAI) writes these tiers once the AI backend is online (Semi-Automated → Autonomous)"); :195 ("SSSES qualification passed"); :1570 ("MoT clocked"); :1733 ("CRS list — from Vision • 2525"); :934 and :1115 ("OAuth login"); :135 ("the platform's own US-dollar table"); frontend/app/layout.tsx:29 ("SHA: <git sha>" banner on every page, operator ruling needed before touching); scope gap: frontend/tests/sign-lexicon-voice.test.mjs:9-19 tests soi.sign.* keys only, so the pod page is ungated
  - evidence: frontend/app/soi-session/page.tsx:1492; step-09-synthesis-lead.jpg (footnote naming Cube 6 / Gemini / OpenAI / AI backend)
- **The WAITING card names no actor and can contradict the screen behind it** (confuses) — *class:* Every guide state states what is true now and who must act next; the guide's model of who may act matches what the page actually enables.
  - members: frontend/app/soi-session/page.tsx:648 (active, non-lead, no segments → wait with empty label); frontend/lib/lexicon-data.ts:790 soi.pod.x.active ("Running — ♡ time is being witnessed") rendered while the clock shows 0:00:00; page.tsx:652 (record wait names nobody); :657 (a non-lead reads the lead's "settle & issue the receipt"); :640 (unseated joiner); :1276 (pod-clock-toggle enabled for every seat, gated only on `lock`, while that seat is told it is waiting)
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/4-active-ready-ana.jpg — WAITING + "Running…" above a 0:00:00 clock and two enabled buttons
- **The panel claims 333 words beside its own counter reading 340** (confuses) — *class:* The panel never claims more than happened — a stated figure agrees with the figure the same screen computes.
  - members: frontend/app/soi-session/page.tsx:1663 (heading "333-word synthesis") vs :1665 (counter = 340); :762 comment ("each paragraph is EXACTLY 111 words") vs frontend/lib/pod-synthesis.ts:12-13 ("need NOT be exactly 111"); docs/assessments/pod-live-run-2026-09-12-asm/log.txt ("333-word synthesis on this phone: 164 + 94 + 82 = 340 words" logged OK)
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/step-09-synthesis-lead.jpg
- **Visible strings on the guided page still bypass t()** (confuses) — *class:* Every user-facing string on a member's path resolves through t() with a lexicon key, and a gate proves it for this page — not only for the guide strings added this session.
  - members: frontend/app/soi-session/page.tsx:135 (new in 0088fa4, hardcoded); and ~33 more on the same path: :862, :892, :906, :1032, :1107, :1135, :1263, :1351, :1387, :1489, :1528, :1551, :1593, :1606, :1691, :1733 plus placeholders :903, :909, :1161, :1358, :1425; gate gap: frontend/tests/pod-invariant.test.mjs:798 counts keys only, frontend/tests/soi-i18n-contract.test.mjs covers soi.* framework keys only
  - evidence: frontend/app/soi-session/page.tsx:135
- **"Copy the receipt" copies part of the receipt** (confuses) — *class:* What the copy button hands a member is the whole document they are looking at, spaced as they read it.
  - members: frontend/app/soi-session/page.tsx:690 copyReceipt (receipt-3 <ol> + the three synthesis paragraphs only) — omits Intent :1637, Outcome :1638, Recorded :1639 and the entire four-artefact block titled "The receipt" :1642-1655; textContent concatenation drops separators between the figure spans (e.g. "웃 0.0660.0238..2507earned")
  - evidence: frontend/app/soi-session/page.tsx:690; docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (receipt string ends at line 6)
- **A reconciled voice cites drafts the record does not contain** (confuses) — *class:* Every claim in the assessment traces to something recorded in the same file; a reconciler may only refuse a position that a draft actually holds.
  - members: docs/assessments/2026-09-12_pod_asm_assist.md — Christo: "I refused Sofia's eight currencies" (Sofia's 111 words propose no currency count) and "Her São Paulo fix supplements" (the São Paulo fix is Odin's); docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json binds no screenshot to any of the 95 log steps, so the filenames are the only trace
  - evidence: docs/assessments/2026-09-12_pod_asm_assist.md (Christo paragraph vs Sofia and Odin paragraphs)

*111 words:*

Sofia's lens is many seats at once. From the lead's phone the envelope works: Step 4 of 7, one amber action, a roster, Completed and copy. From Ana's phone at the same moment the card says WAITING and Running while the clock reads zero and two enabled buttons sit unmarked — the guide models permission the page does not enforce. The receipt then asks her to believe 0.00 hours became $0.38, under a heading claiming 333 words beside a counter reading 340. Both fixes tonight were written to the instance: one slice, two literals. The class survives, green. Fold the machine words, name the actor, print figures a person can check.

## Thoth — data and analytics — grade C

**Forward 1→10.** The envelope shape genuinely arrives: `PodPhaseRail` prints "Step 4 of 7 · Work", one amber card names the single action, `your-turn-action` scrolls and focuses the real control, `data-next` outlines it, and `PodRosterList` shows who has done what — a stranger on a 375px phone can find the next tap on every phase, and `canOpen` (page.tsx:440) matches the guide's compose chain exactly, so there is no dead end. What breaks the promise is that the card's sentence is derived from `phase` alone while its chip and button are derived from real state: on `4-active-ready-lead.jpg` a first-time member reads "YOUR TURN · Running — ♡ time is being witnessed. Anyone can stop it for everyone." directly above a 0:00:00 clock and a "Start the clock" button. The same member then reads "counted as 0.00 h" (step-07-audit-lead.jpg) and a receipt line settling that 0.00 h at $0.38, and the fields the guide points at ("Write the intent", "Enter your name") still carry English-only placeholders under a translated instruction.

**Backward 10→1.** The receipt does not trace. Its stated measurable outcome — the exchange-rate file grows from 1 row to 29 and the 60 "awaiting" lines go to zero — is narrated as accomplished in synthesis ¶1 on all three phones, while line 5b of the same receipt still prints "awaiting a dated exchange-rate source" for Bo, `docs/asks/2026-09-12_fx_to_usd.psv` holds one data row, and `FX_TO_USD` (pod-rates.ts:611) holds one entry; nothing gates the closed screen against its own claim. The figures do not reconcile either: the same person's hours print as 0.00 h (5b), 0.01 (plan line), 0.0138 (line 6) and 0:00:16, and the "333-word synthesis" heading sits beside its own count of 340 with no paragraph at 111. Worst for provenance, the run cited as the evidence for the DocuSign change never touches the guide — no `your-turn`, `data-next`, `pod-roster` or `phase-step` appears in `pod-live-run-2026-09-11.mjs` — and the single assertion that would have failed was widened, only for INPUTS runs, in the same commit that reports 95 steps and 0 failures.

- **The guide's sentence is computed from the phase, not from what is true, so the one line a stranger reads can contradict the card it sits in** (blocks) — *class:* INVARIANT: every word on the guide card is derived from the same state tuple as its chip and its button — the card never asserts a condition the page can disprove. Today `explain` switches on `phase` alone while `guide` switches on phase AND state, so the two halves of one card disagree.
  - members: frontend/app/soi-session/page.tsx:610 (case "active" returns soi.pod.x.active "Running — ♡ time is being witnessed" whenever phase===active, including before span.running, beside guide.a.clock_start at :647); page.tsx:611 (case "record" returns the imperative "Record the outcome — write it, speak it…" to a seat whose outcome is already in and whose guide is `wait`, :649-652); page.tsx:609 (case "sync" returns "Tap Start — waiting for {who}" to a person who already tapped Start, :645); page.tsx:600 (compose_ready "Ready — share the QR & open the pod" is phase-derived, shown to any seat); the gate at frontend/tests/pod-invariant.test.mjs:782 asserts only that the sentence IS pod-explain, never that it agrees with guide.state
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/4-active-ready-lead.jpg (and -ana, -bo): "YOUR TURN / Running — ♡ time is being witnessed. Anyone can stop it for everyone." above a 0:00:00 clock and a "Start the clock" button; frontend/app/soi-session/page.tsx:610
- **One quantity, four precisions on one receipt: hours round to 0.00 h while the same line pays $0.38** (blocks) — *class:* INVARIANT: a quantity a person reads has ONE printed form everywhere it appears, at a precision that cannot render a non-zero figure as zero. Today each call site picks its own toFixed, so witnessed hours print as 0.00, 0.01, 0.0138 and 0:00:16 on the same screens, and the gate pins only the raw-float instance found last night.
  - members: frontend/app/soi-session/page.tsx:1381 (measuredHours.toFixed(2) → "0.00 h"); :1421 (claim-capped-i "counted as 0.00 h — the pod was witnessed for 0:00:16"); :1505 ("0.00 h at 4.807× = 웃 0.022"); :1533 (witnessedHours.toFixed(2)); :1534 (accel-delta toFixed(2)); :1612 (receipt-plan "0.01 person-hours counted"); :1629 (receipt-each "Lea 0.00 h → 웃 0.022 → $0.38"); :1634 (receipt-vintage toFixed(4) "0.0138 h" — the SAME hours as :1629); frontend/lib/lexicon-data.ts:795 soi.pod.x.closed {h} (toFixed(2) at page.tsx:617); fmtABC mantissa strings ("0.0049..2362") beside all of them; frontend/tests/pod-invariant.test.mjs:801-803 gates only vintage/claim raw floats, and by pinning toFixed(4) at :1634 it locks the disagreement in
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/step-07-audit-lead.jpg ("counted as 0.00 h — the pod was witnessed for 0:00:16"); walkthrough.json receipt line 5b "Lea 0.00 h → 웃 0.022 → $0.38" vs line 6 "0.013793333333333335 h"
- **The receipt and the synthesis print a typed outcome as accomplished fact, and this one is contradicted by the receipt's own next line** (blocks) — *class:* INVARIANT: the closed screen never states as done anything the pod did not evidence — a typed goal is quoted as a goal, never narrated as achieved. Today every closing artefact restates the `outcome` field in the past tense with no check against the run's own output.
  - members: frontend/lib/pod-synthesis.ts via buildSynthesis333 (page.tsx:764) — ¶1 renders the outcome text as what the session achieved ("tonight's closed receipt prints a USD figure on every line, taking the 60 … to zero"); page.tsx:747-749 portfolio artefact ("Contributed to \"{outcome}\""); page.tsx:1638 "Outcome:" line; page.tsx:743 transcript artefact; page.tsx:750-753 governance artefact; no assertion in frontend/tests/pod-invariant.test.mjs ties any closing claim to observed state
  - evidence: walkthrough.json: synthesis ¶1 claims the awaiting lines went to zero while receipt line 5b prints "awaiting a dated exchange-rate source" for Bo; docs/asks/2026-09-12_fx_to_usd.psv holds 1 data row; frontend/lib/pod-rates.ts:611-613 FX_TO_USD holds 1 entry
- **The run cited as proof of the DocuSign change never exercises the guide, and its one failing check was widened in the same commit** (blocks) — *class:* INVARIANT: the artefact offered as evidence for a change must touch the thing the change added, and no assertion may be relaxed in the commit that reports the pass. Neither holds: the harness drives the underlying controls only, and the USA-equivalent predicate was broadened for exactly the configuration this run used.
  - members: frontend/scripts/pod-live-run-2026-09-11.mjs — contains no `your-turn`, `your-turn-action`, `data-next`, `pod-roster` or `phase-step` selector anywhere in its 203 lines; its crops are seat cards (seatCard(), :58) not the guide card; :84 the assertion becomes `new RegExp(INP ? '≈ \\$|awaiting a dated exchange-rate source' : '≈ \\$')`, i.e. weaker only when INPUTS is set — the same run whose log records it OK; log.txt lines 4 and 90-92 record the widened predicate as a pass; commit 7078d8a's message reports "95 steps, 0 failures on the guided UI"
  - evidence: frontend/scripts/pod-live-run-2026-09-11.mjs:84 (diff in commit 7078d8a); docs/assessments/pod-live-run-2026-09-12-asm/log.txt:4
- **A member's screen still names a vendor, a source file and internal spec numbers; the signer-voice gate covers a ten-line slice of this page and nothing else** (confuses) — *class:* INVARIANT: no string a pod member can read names a vendor, a file, a table or an internal identifier — and that rule is enforced over EVERY member-facing string, not one function. The gate added with this change slices the page from `function UsdBeside` to `const WHITE_PAPER` (ten lines), and sign-lexicon-voice.test.mjs reads only the i18n directory, so the rest of the page is ungoverned.
  - members: frontend/app/soi-session/page.tsx:1122 ("○ local (live sync when Supabase is reachable)" — vendor, on the invite screen every joiner sees); :1492 ("Source: hi_rates.py, the live settlement table the paper names."); :889 ("(TOK-17 · D5 anti-sybil)"); :1387 ("— clock-in → cross-review (TOK-17)"); :1644 ("one ledger record, read four ways (TOK-26)"); :1483 and :1489 ("D9 — pays…"); the gate frontend/tests/pod-invariant.test.mjs:799 (slice(indexOf('function UsdBeside'), indexOf('const WHITE_PAPER')) = page.tsx:132-142 only); frontend/tests/sign-lexicon-voice.test.mjs:36 (reads a lexicon dir, never app/soi-session). Related removal in the same commit: page.tsx:134 dropped `(${u.source})` from the hi_rates route while the fx route still prints `(rate of ${u.asOf}, ${u.source})` — provenance now shown on one branch of one line and hidden on the other, decided per call site rather than by one rule
  - evidence: frontend/app/soi-session/page.tsx:1122 and :1492; frontend/tests/pod-invariant.test.mjs:799
- **The guide is translated; the control it points at is not, so a non-English member reads an instruction over an English field** (confuses) — *class:* INVARIANT: every string on a member's path goes through t(). The 34 new soi.pod.guide.* keys satisfy the gate's count, but the gate counts only guide keys, so the labels and placeholders the guide sends people to remain hardcoded English and the fallback chain never reaches them.
  - members: Placeholders at frontend/app/soi-session/page.tsx:903, :909, :924, :929, :1009, :1156, :1161, :1196, :1305, :1358 (these are exactly the pod-intent, pod-outcome, pod-name, baseline-hours, member-name-i and member-outcome-i controls the guide targets); labels/headings at :829, :862, :889, :892, :906, :917, :991, :994, :1032, :1035, :1107, :1135, :1351, :1387, :1551, :1593, :1606, :1663; new hardcoded string added by this change at :134 (" (a second floor from the platform's own US-dollar table, not a conversion)"), which then prints verbatim on the receipt; frontend/lib/pod-rates.ts:935 USD_MISSING; the gate frontend/tests/pod-invariant.test.mjs:797-798 counts soi.pod.guide.* keys only
  - evidence: frontend/app/soi-session/page.tsx:1161 placeholder="enter your name" beside guide key soi.pod.guide.a.name (lexicon-data.ts:821); walkthrough.json receipt 5b carries the untranslated "(a second floor from the platform's own US-dollar table, not a conversion)"
- **"333-word synthesis" is a claim the same block disproves, and nothing gates it** (confuses) — *class:* INVARIANT: a named count on screen is the count that is printed, or the label states the real figure. The heading, the architecture comment and the CRS spec line all assert 333 words in three 111-word paragraphs while the rendered counter shows 340 (164/94/82) and no gate checks either number.
  - members: frontend/app/soi-session/page.tsx:1663 (heading "333-word synthesis") beside :1665 (the live 340 count) and :1677 (per-paragraph counts); page.tsx:20 (phase comment); page.tsx:161 (CRS spec text "auto-writes a 333-word (3×111) summary", member-readable in the CRS list); frontend/lib/pod-synthesis.ts buildSynthesis333; docs/assessments/pod-live-run-2026-09-12-asm/log.txt lines 76-78 record "333-word synthesis … = 340 words" as OK on three phones; frontend/tests/pod-invariant.test.mjs has no 111/333 assertion
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json counts [164,94,82] total 340; frontend/app/soi-session/page.tsx:1663
- **"Copy the receipt" copies a receipt whose record line is truncated at 80 characters** (confuses) — *class:* INVARIANT: the artefact a person carries away says everything the screen says. The copy reads receipt-3's textContent, and receipt-3's line 1 clips the shared record at 80 chars with an ellipsis, while the full text lives outside that element.
  - members: frontend/app/soi-session/page.tsx:690 (copyReceipt reads only [data-testid="receipt-3"] plus the three synthesis paragraphs); :1611 (recordValue.slice(0, 80) + "…"); :1639 (the full "Recorded ({recordMethod}): {recordValue}" line sits OUTSIDE receipt-3 and is never copied); :1637-1638 (Intent and Outcome likewise outside the copied element)
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt line 1: "Recorded written — Dated exchange-rate file drafted — currency, units per USD, as-of date, named so…"

*111 words:*

Data lens. The envelope shape lands: Step 4 of 7, one card, one button, a roster a stranger reads in a glance. Then the numbers betray it. One quantity prints four ways on one receipt (0.00 h, 0.01, 0.0138, 0:00:16), and 0.00 h earns $0.38. The synthesis is called 333 words and counts 340. The receipt narrates an outcome its own line contradicts and the repository does not hold: the exchange-rate file still has one row. The run cited as proof never touches the guide, and its one failing assertion was widened in the same commit. Simple to follow is not yet true to check. Fix each class, not tonight's instance.

## Thor — risk and security — grade C

**Forward 1→10.** A first-timer does get real help: "Step 2 of 7 · Invite", a seven-chip rail with counts, a who-has-done-what roster, an amber next-field outline, and a Completed line with copy — the DocuSign shape is genuinely there, and nothing was removed, only folded. But the card contradicts itself at the moment it matters: on the invite screenshot the chip reads YOUR TURN, the sentence reads "Waiting for 2 to join — share the code or QR", and the button reads "Approve the plan ↓", because `explain` is derived per phase (page.tsx:598-621) while `guide` is derived per seat (page.tsx:629-662). In ACTIVE the lead's guide loops forever on the clock and never points at "Record the outcome" — the run itself had a joiner press it (log.txt 28502ms). And every step is topped by a strip reading "SHA: 254ef2e", with Supabase, OAuth, hi_rates.py, CRS and MoT scattered on the same screens, which is exactly the machine a member was promised she would never meet.

**Backward 10→1.** Most of the receipt does trace: each outcome line is the member's own typed words, the segments and the cap are the platform's own clock, and the three phones settle identically. Where it breaks is arithmetic presentation, not provenance: one quantity reaches the reader as "0.01 person-hours counted" (line 1), "0.00 h" per person (5b) and "0.013793333333333335 h" stamped (line 6), so a person reading her own receipt cannot reconcile zero hours with 웃 0.022 — and the eca4186 fix rounded two printing sites while `{witnessedHours}` still prints raw at page.tsx:1565, :1570 and in the transcript artefact at :749. The narration around the receipt also fails to trace: log.txt asserts "approved intent, outcome AND the plan (2 h × 3)" from a hardcoded harness string (pod-live-run-2026-09-11.mjs:119) while the plan actually set was 3 h × 4.807, the panel labels a "333-word synthesis" whose own counts total 340, and all 45 screenshots are stamped with 254ef2e — the commit *before* the guide they depict.

- **The signer-voice gate covers the signing flow only; the pod, where money settles, is ungated and violates** (blocks) — *class:* INVARIANT: no screen a member can reach names a vendor, a source file, a build or an internal identifier. It is enforced by frontend/tests/sign-lexicon-voice.test.mjs, whose scope is soi.sign.* keys, components/sign/sign-flow.tsx and lib/i18n-sign/ only (test lines 9-40). Every other member-facing surface — the pod included — is outside the gate, so the class is defended on one member and unenforced on the rest.
  - members: frontend/app/layout.tsx:29 ("SHA: 254ef2e | Date | Time" strip above EVERY route, visible in 2-invite-lead.jpg and 8-closed-receipt-lead.jpg) · frontend/app/soi-session/page.tsx:1122 ("○ local (live sync when Supabase is reachable)") · :934 ("Your email imports from your OAuth login") · :1115 ("their info imports from their login (email / OAuth)") · :1492 ("Source: hi_rates.py, the live settlement table the paper names") · :889 ("TOK-17 · D5 anti-sybil") · footer "MoT" and "CRS list — from Vision 2525" (both jpgs) · and the gate itself, frontend/tests/sign-lexicon-voice.test.mjs:9-40, which never reads soi-session/page.tsx or layout.tsx
  - evidence: frontend/app/soi-session/page.tsx:1122, frontend/app/layout.tsx:29, docs/assessments/pod-live-run-2026-09-12-asm/2-invite-lead.jpg
- **One quantity, three renderings on the same receipt; the rounding fix reached 2 of ≥6 printing sites** (confuses) — *class:* INVARIANT: an hours figure a person reads is printed once, at one precision, and every printing of the same quantity agrees. eca4186 patched the two sites named in the previous run's log and left the rest, and the gate it added (pod-invariant.test.mjs:802) asserts only those two literal patterns — a symptom patch with a symptom-shaped gate.
  - members: RAW, unformatted: frontend/app/soi-session/page.tsx:1565 ("none from the {witnessedHours} h") · :1570 ("{witnessedHours} witnessed hours") · :749-750 (transcript artefact `${witnessedHours}h cross-reviewed`, `${accelDelta}h ahead`). DISAGREEING precisions of the same figure: :1612 toFixed(2) → "0.01 person-hours counted" · :1505 toFixed(2) → "Lea 0.00 h → 웃 0.022" · :1634 toFixed(4) → "0.0138 h" · :618 toFixed(2) in the closed sentence · :1421 and :1533 toFixed(2). GATE: frontend/tests/pod-invariant.test.mjs:802-803 covers only {vintage.hours} and {claimOf(i).hours}.
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json receipt lines 1, 5b, 6 ("0.01 person-hours counted", "Lea 0.00 h → 웃 0.022", "0.013793333333333335 h")
- **The guide card's chip, sentence and button describe three different things** (confuses) — *class:* INVARIANT: the chip (state), the sentence and the button on the one 'Your turn' card must all describe the same next act, for every phase and every seat. The chip and the button are derived per seat from `guide` (page.tsx:629-662); the sentence is `explain`, derived per phase for the pod as a whole (page.tsx:598-621). Wherever the pod's state and the reader's state differ, the card disagrees with itself — this is structural, not a wording slip.
  - members: invite (page.tsx:601-608 vs :639-643) — proven on screen: chip YOUR TURN + "Waiting for 2 to join" + "Approve the plan ↓" · sync (:609 vs :644-645, sentence names who has not started while your button says Start) · active (:610 vs :646-648) · record (:611 vs :649-652) · audit (:612-617 vs :653-658) · and the `waiting` branch (:631), which renders a phase sentence with no action and no chip explanation of whose turn it is
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/2-invite-lead.jpg; frontend/app/soi-session/page.tsx:598-621 vs 629-662
- **In ACTIVE the guide never points the lead at the control that ends the phase** (confuses) — *class:* INVARIANT: on every phase, for every seat, the guide's target is a control that can advance THIS person's step. The active branch returns pod-clock-toggle unconditionally for the lead, so once the clock is stopped the card reads "Add time" indefinitely and never offers pod-stop ("Record the outcome"); only non-leads are ever pointed there. The same class covers seats the guide leaves with no instruction at all.
  - members: frontend/app/soi-session/page.tsx:646-648 (isLead → always pod-clock-toggle; pod-stop unreachable for seat 1) · :640 (invite, me<0 → silent wait, no instruction for a person who scanned the QR before a seat exists) · :645 (sync, me<0 → same) · :657 (audit, non-lead after all witnessing → silent wait with no 'nothing left to do' statement)
  - evidence: frontend/app/soi-session/page.tsx:646-648; docs/assessments/pod-live-run-2026-09-12-asm/log.txt 28502ms "a JOINER pressed Stop & record"
- **New visible strings shipped outside t(), and no gate asserts the pod page has none** (confuses) — *class:* INVARIANT: every user-facing string on a cube surface goes through t() with a lexicon key (CLAUDE.md Language Lexicon checklist). pod-invariant.test.mjs:797-798 only counts that ≥34 soi.pod.guide.* keys exist; it never asserts the page renders no hardcoded English, so new literals pass the gate that was written for exactly this change.
  - members: NEW in 0088fa4/eca4186: frontend/app/soi-session/page.tsx:1385 (`{t("soi.pod.guide.details")} — the evidence chain`) · :1465 (`… — how this settles`) · :133 ("(a second floor from the platform's own US-dollar table, not a conversion)"). PRE-EXISTING on the same reader path (~36 literals): :889, :892, :906, :915, :924, :929, :934, :1114-1115, :1256, :1262-1264, :1268, :1276-1282, :1358, :1418, :1425, :1478-1489, :1565, :1570, :1663, :1686
  - evidence: frontend/app/soi-session/page.tsx:1385 and 1465; frontend/tests/pod-invariant.test.mjs:797-798
- **The evidence folder is stamped with a SHA that is not the build it shows** (confuses) — *class:* INVARIANT: every artefact issued as proof of a change identifies the commit it was produced from. The banner reads process.env.NEXT_PUBLIC_GIT_SHA, which the showcase harness never refreshes, so all 45 screenshots carry the parent commit — the operator cannot tell which build he is looking at, which is the same failure class as reporting a push as a ship (AAR 2026-07-29).
  - members: frontend/app/layout.tsx:29 (unset/stale NEXT_PUBLIC_GIT_SHA) · every jpg in docs/assessments/pod-live-run-2026-09-12-asm/ (45 files) stamped 254ef2e, the commit BEFORE 0088fa4 which introduced the guide those screenshots depict · the same banner will stamp any future showcase run identically
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/2-invite-lead.jpg top strip "SHA: 254ef2e" vs git log 0088fa4/7078d8a
- **The run log asserts a plan that was not the plan** (confuses) — *class:* INVARIANT: every line the harness writes to the record is read from the run's own inputs, never from a literal frozen when the harness was written. Three narration strings still hardcode the old 2 h × 3 plan while the advised inputs set 3 h × 4.807, so the receipt of the run contradicts the run.
  - members: frontend/scripts/pod-live-run-2026-09-11.mjs:119 ("approved intent, outcome AND the plan (2 h × 3)", printed three times in log.txt at 9252/9713/9783ms) · frontend/scripts/build-fleet48-review.mjs:89 ("1-compose-plan — … (2 h × 3 = 6 웃)") · frontend/scripts/build-pod-walkthrough.mjs:24 (the non-ASSIST fallback still narrates hours 2, multiple 3×, New York)
  - evidence: docs/assessments/pod-live-run-2026-09-12-asm/log.txt 2255ms ("plan set: 3 h at 4.807×") vs 9252ms ("the plan (2 h × 3)")
- **A panel claims a count the same panel contradicts** (cosmetic) — *class:* INVARIANT: the panel never claims more than happened (SIGNER rule 5). The closed screen labels the block "333-word synthesis — 3 paragraphs" while printing counts that sum to 340, and the harness records that mismatch as a pass.
  - members: frontend/app/soi-session/page.tsx:1663 (the "333-word synthesis" label) · :20 and :161 and :760 (the same 333/111 claim in the spec text a member can read) · docs/assessments/pod-live-run-2026-09-12-asm/walkthrough.json (counts [164,94,82], total 340, on all three seats) · log.txt 34301/34431/34558ms, each logged OK
  - evidence: frontend/app/soi-session/page.tsx:1663; docs/assessments/pod-live-run-2026-09-12-asm/log.txt 34301ms

*111 words:*

Risk lens. The guided pod is genuinely easier to walk, and nothing was removed, folded only. But the invariant that protects a person, no screen naming the machine, is gated on the signing flow alone; the pod is outside it and shows Supabase, OAuth, hi_rates.py, CRS, MoT and a commit SHA above every step. The rounding fix landed on two members of a six-member class, so the receipt still reads 0.00 h, 0.01 h and 0.013793333333333335 h for one quantity. The card says Your turn while its sentence says Waiting. The lead loops. Screenshots carry the parent commit's SHA. Gate the class, on this surface, before another pod settles real money.
