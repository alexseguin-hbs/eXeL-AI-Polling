# The 48-AsM fleet review of Drone-2525 at r.133 → r.134 (operator 2026-09-19: "then test entire drone-2525 as 38 ASM team. 48 AsM fleet")

Method: 12 lenses (the twelve AsM names, one testing super-power each) × (reviewer A on the SERVED r.133 · reviewer B, adversarial,
on the r.134 CANDIDATE built from A's findings · one synthesis per lens) + 12 MoT coordinators over cross-cutting questions = 48
read-only agents at the configured model and effort (never overridden). Each report is written verbatim under the session
scratchpad (`fleet48/<LENS>-<ROLE>.md`) and summarised here; every claim below cites the served source. Evidence of record:
`docs/assessments/2026-09-19_r132_asm_team_test.md` (38 seats, D1–D22), `docs/drone-2525/operator-deck/REVISIONS.md`.

## Reviewer A — twelve lenses on the served r.133

| lens | grade | what held (gated) | the class NOT held | first owed |
|---|---|---|---|---|
| Aset · consistency | C | one score writer; peer tally from the same row; one refusal reason on every surface; one empty-range sentence; bytes = one fact | phDes/board on the frame while the score is on the event; tallies the strip shows with no row (training lapse, RESET); the record names the device, never the actor; rangeRelease never repaints; eight spellings of one revision; link/voice report the last write | one event-time repaint for the whole strip |
| Asar · outcome truth | C | mark→approve→hit/miss on the hashed record; refusals as rows; QUAL lapse and table rows; peer rows reduce; SAVE/EXPORT/pack never move the hash | state that changes the score without a row (RESET, mode, lane, training lapse, bulk TIME lapses, SPENT); the author not in the hashed row; presence a last-written value; the record carries no view; the sidecar says ASM_CUP_99 for every play | one canonical row for every score-changing state |
| Athena · the stranger | C | the two-step by pills; the score at 390; nothing lapses behind the intro; one empty-range sentence; TARGET aims | every toast under the intro (z 6 vs 40); silent training lapses; "WAIT APPROVE" names a person who is not there; SELECT offers what PRACTICE refuses; one 1.4 s toast slot 250 px from the eye; 11 CONTINUE taps of jargon | the sentence above the intro |
| Christo · two-phone room | C | the room, the two-humans gate, one hash (8/8 here); the wire obeys the button rules; the approver tallies range hits | the unaided join is impossible (RED code shown nowhere; the gate reads it out of JS); no liveness; the approver's outcome mode-dependent (no qual tally, no lastShot); seats see SIDs not people; rtt is two clocks | show the other seat's code with one sentence |
| Enki · edge cases | C | lane 42; the wrap at 320; one round per exposure through RESET/MAP/sky; a lapse counts in MAP | dock FIRE under the R-HEAD stick; a scored HIT from MAP with the strip frozen; VOICE ON after the recognizer died; RESET off the record; frame-time clocks pause in a hidden tab; a sky tap marks the cone | one safe-area layout law + a disjointness gate |
| Enlil · gates | C | all gates green and fail-closed; owned manifest; bytes hashed; the lifts evaluate the deck's own functions | ASM_TICKED_LIVE is a regex; 12 boot rows are `fn.toString()` tests and ~20 call the function under test; drone-team-e2e was green on r.132 with D3 live (reads state, not the picture); **the two-phone gate ran in no workflow while the register said "a Deploy step"**; layout proved by CSS strings in one viewport | loop-driven rows; the e2e reads the approver's DOM; wire it |
| Krishna · integration | B− | one byte chain from carried file to live probe; one HEAD constant; the register chain refuses a rowless file; the lifts read HEAD | the domain JSON two editions behind, green; append-only hashed for three columns of one file; the live probe identifies a revision by a string two byte-sets share; two replayHash functions; sourcing drift; gates that run nowhere | one HEAD across the whole record, gated |
| Odin · future-proof | C | 1v1 one hash; the hash sorts by orderKey; the host is the one sequencer; match size locked; per-lane order deterministic | no canonical range clock (each device raises plates from its own dt); the ledger forks on a dropped channel; the reducer applies in arrival order while the hash sorts; identity columns make the same play a different hash; one pc/dc per device; seats unaddressed | an EXPOSE row from the host |
| Pangu · the AI member | C | ticked from the loop; fires only on red, once, through fireN; sees the lane's plates from the pit; OFF default | no actor identity (signs as the first turret in the loop); the record never names the AI; one stale `live` list, 43 marking turrets; in a LIVE room its mark is un-approvable here and "two humans" there; the FIRE half has no gate; **A's D18 correction: MAP hides the AI, it does not stop it** | ASM@device on every row |
| Sofia · words | D | three sentences a stranger needs; one empty-range sentence; the band in words; the other seat named | record vocabulary spoken (HI-2, PEER, SOLO, NET); machine ids and SIDs on the strip; two vocabularies for one fact (TABLE I / TI PRONE SUP); no operator door (the DATA panel is the machine's); infrastructure failures named by cause; D9 fixed as four instances with no gate | a word gate + `?diag=1` |
| Thoth · numbers | C | rounds/hits/lapses/tables measured and gated; fps measured; the manifest owned; bytes chained; the hash compared | SSSES is a label dressed as a measurement (sec = red box exists; suc = constants; scale = 70+15·peers); the panel repainted by side effect (pack()); one figure two denominators (98/99 vs 99/100); rtt from a foreign clock; high-water marks read as live; counters that count the buffer; six revision strings | retire SSSES as a displayed score until measured |
| Thor · the fire gate | C | every input funnels into fireN's red check; the AI only on red; LIVE two-humans; authority scoped to the exposure; slot and lane must match | the approval is a bit, not a live decision (survives scene change, mode, reset, a lost approver, time); a second weaker approval primitive (the CH5 popup, #btnYes rebinds); the door commit fires with no record and never consumes the box; no path checks the target is alive; inputs ungated by focus/phase/view; the reducer trusts an embedded row's author | one consumer for authority |

Twelve grades: C ×10, B− ×1, D ×1. Convergent verdict: **the engine, the fire gate's red check, the determinism of the range and
the byte chain are real and gated; the authority bit, the record's authorship, the link's liveness, the stranger's sentences and
the numbers on the panel were not.**

## What r.134 folded from the twelve A reports (each edit is a comment in `patches/r133_to_r134.py` naming the lens)
Authority has one consumer (Thor) · RESET/mode rows that travel and confirm (Asar, Enki, s33) · heartbeat + lost approver said and
released (Christo, Thor, s35) · VOICE outcome, OFF, negation first, no lone "f" (Enki, Thor, s36) · MAP keeps the strip (Enki, s28) ·
ASM@device on DESIGNATED and HIT/MISS rows, HI OVER AI, no dead plate, one shot per tick (Pangu, Asar, s38) · the CH5 popup deleted;
the door tag on the record (Thor) · dead refs refused (Thor) · keys ignore text fields and the intro; forged rows refused at the wire
(Thor) · toast above the intro; "NOW PRESS APPROVE"; training lapses said and rowed (Athena, Asar) · the host shows the other team's
code (Christo, Sofia) · the approver's qual tables and lastShot from the peer's rows; a peer lapse releases (Christo, Aset) · one
revision string on the panel (Thoth, Aset) · the two-phone gate wired into Deploy and reading the approver's picture (Enlil) ·
`deck-consistency` gate + domain JSON caught up + Verify Live hashes the body + patch paths relative (Krishna).

NOT folded in r.134 (named for r.135, with the lens): the canonical range clock and the fork-on-drop ledger (Odin) · re-reduce from
the sorted log (Odin, Asar) · the word gate and `?diag=1` (Sofia) · SSSES retired until measured; rtt as SKEW (Thoth) · wall-clock
tables with a HIDDEN row; the dock FIRE under the stick; SAVE as one download (Enki, Athena) · loop-driven ASM rows and measured-pixel
layout rows at three viewports (Enlil) · patch replay in CI; two replayHash projections reconciled (Krishna) · seat addressing and
one pc/dc per peer (Odin).
