# The 48-AsM fleet review of Drone-2525 at r.133 → r.134 (operator 2026-09-19: "then test entire drone-2525 as 38 ASM team. 48 AsM fleet")

Method: 12 lenses (the twelve AsM names, one testing super-power each) × (reviewer A on the SERVED r.133 · reviewer B, adversarial,
on the r.134 CANDIDATE built from A's findings · one synthesis per lens) + 12 MoT coordinators over cross-cutting questions = 48
read-only agents at the configured model and effort (never overridden). Each report is written verbatim under the session
scratchpad (`fleet48/<LENS>-<ROLE>.md`) and summarised here; every claim below cites the served source. Evidence of record:
`docs/assessments/2026-09-19_r132_asm_team_test.md` (38 seats, D1–D22), `docs/drone-2525/operator-deck/REVISIONS.md`.

## Reviewer A — twelve lenses on the served r.133

| lens | grade | what held (gated) | the class NOT held | first owed |
|---|---|---|---|---|
| Aset · consistency | C | one score writer; peer tally from the same row; one refusal reason on every surface; one empty-range sentence; bytes = one fact | phDes/board on the frame while the score is on the event; tallies the strip shows with no row (training lapse, RESET); the record names the device, never the actor; rangeRelease never repaints; eight spellings of one revision; link/voice report the last write | one event-time repaint for the whole strip |
| Asar · outcome truth | C | mark→approve→hit/miss on the hashed record; refusals as rows; QUAL lapse and table rows; peer rows reduce; SAVE/EXPORT/pack never move the hash | state that changes the score without a row (RESET, mode, lane, training lapse, bulk TIME lapses, SPENT); the author not in the hashed row; presence a last-written value; the record carries no view; the sidecar says ASM_CUP_99 for every play | one canonical row for every score-changing state |
| Athena · the stranger | C | the two-step by pills; the score at 390; nothing lapses behind the intro; one empty-range sentence; TARGET aims | every toast under the intro (z 6 vs 40); silent training lapses; "WAIT APPROVE" names a person who is not there; SELECT offers what PRACTICE refuses; one 1.4 s toast slot 250 px from the eye; 11 CONTINUE taps of jargon | the sentence above the intro |
| Christo · two-phone room | C | the room, the two-humans gate, one hash (8/8 here); the wire obeys the button rules; the approver tallies range hits | the unaided join is impossible (RED code shown nowhere; the gate reads it out of JS); no liveness; the approver's outcome mode-dependent (no qual tally, no lastShot); seats see SIDs not people; rtt is two clocks | show the other seat's code with one sentence |
| Enki · edge cases | C | lane 42; the wrap at 320; one round per exposure through RESET/MAP/sky; a lapse counts in MAP | dock FIRE under the R-HEAD stick; a scored HIT from MAP with the strip frozen; VOICE ON after the recognizer died; RESET off the record; frame-time clocks pause in a hidden tab; a sky tap marks the cone | one safe-area layout law + a disjointness gate |
| Enlil · gates | C | all gates green and fail-closed; owned manifest; bytes hashed; the lifts evaluate the deck's own functions | ASM_TICKED_LIVE is a regex; 12 boot rows are `fn.toString()` tests and ~20 call the function under test; drone-team-e2e was green on r.132 with D3 live (reads state, not the picture); **the two-phone gate ran in no workflow while the register said "a Deploy step"**; layout proved by CSS strings in one viewport | loop-driven rows; the e2e reads the approver's DOM; wire it |
| Krishna · integration | B− | one byte chain from carried file to live probe; one HEAD constant; the register chain refuses a rowless file; the lifts read HEAD | the domain JSON two editions behind, green; append-only hashed for three columns of one file; the live probe identifies a revision by a string two byte-sets share; two replayHash functions; sourcing drift; gates that run nowhere | one HEAD across the whole record, gated |
| Odin · future-proof | C | 1v1 one hash; the hash sorts by orderKey; the host is the one sequencer; match size locked; per-lane order deterministic | no canonical range clock (each device raises plates from its own dt); the ledger forks on a dropped channel; the reducer applies in arrival order while the hash sorts; identity columns make the same play a different hash; one pc/dc per device; seats unaddressed | an EXPOSE row from the host |
| Pangu · the AI member | C | ticked from the loop; fires only on red, once, through fireN; sees the lane's plates from the pit; OFF default | no actor identity (signs as the first turret in the loop); the record never names the AI; one stale `live` list, 43 marking turrets; in a LIVE room its mark is un-approvable here and "two humans" there; the FIRE half has no gate; **A's D18 correction: MAP hides the AI, it does not stop it** | ASM@device on every row |
| Sofia · words | D | three sentences a stranger needs; one empty-range sentence; the band in words; the other seat named | record vocabulary spoken (HI-2, PEER, SOLO, NET); machine ids and SIDs on the strip; two vocabularies for one fact (TABLE I / TI PRONE SUP); no operator door (the DATA panel is the machine's); infrastructure failures named by cause; D9 fixed as four instances with no gate | a word gate + `?diag=1` |
| Thoth · numbers | C | rounds/hits/lapses/tables measured and gated; fps measured; the manifest owned; bytes chained; the hash compared | SSSES is a label dressed as a measurement (sec = red box exists; suc = constants; scale = 70+15·peers); the panel repainted by side effect (pack()); one figure two denominators (98/99 vs 99/100); rtt from a foreign clock; high-water marks read as live; counters that count the buffer; six revision strings | retire SSSES as a displayed score until measured |
| Thor · the fire gate | C | every input funnels into fireN's red check; the AI only on red; LIVE two-humans; authority scoped to the exposure; slot and lane must match | the approval is a bit, not a live decision (survives scene change, mode, reset, a lost approver, time); a second weaker approval primitive (the CH5 popup, #btnYes rebinds); the door commit fires with no record and never consumes the box; no path checks the target is alive; inputs ungated by focus/phase/view; the reducer trusts an embedded row's author | one consumer for authority |

Twelve grades: C ×10, B− ×1, D ×1. Convergent verdict: **the engine, the fire gate's red check, the determinism of the range and
the byte chain are real and gated; the authority bit, the record's authorship, the link's liveness, the stranger's sentences and
the numbers on the panel were not.**

## What r.134 folded from the twelve A reports (each edit is a comment in `patches/r133_to_r134.py` naming the lens)
Authority has one consumer (Thor) · RESET/mode rows that travel and confirm (Asar, Enki, s33) · heartbeat + lost approver said and
released (Christo, Thor, s35) · VOICE outcome, OFF, negation first, no lone "f" (Enki, Thor, s36) · MAP keeps the strip (Enki, s28) ·
ASM@device on DESIGNATED and HIT/MISS rows, HI OVER AI, no dead plate, one shot per tick (Pangu, Asar, s38) · the CH5 popup deleted;
the door tag on the record (Thor) · dead refs refused (Thor) · keys ignore text fields and the intro; forged rows refused at the wire
(Thor) · toast above the intro; "NOW PRESS APPROVE"; training lapses said and rowed (Athena, Asar) · the host shows the other team's
code (Christo, Sofia) · the approver's qual tables and lastShot from the peer's rows; a peer lapse releases (Christo, Aset) · one
revision string on the panel (Thoth, Aset) · the two-phone gate wired into Deploy and reading the approver's picture (Enlil) ·
`deck-consistency` gate + domain JSON caught up + Verify Live hashes the body + patch paths relative (Krishna).

NOT folded in r.134 (named for r.135, with the lens): the canonical range clock and the fork-on-drop ledger (Odin) · re-reduce from
the sorted log (Odin, Asar) · the word gate and `?diag=1` (Sofia) · SSSES retired until measured; rtt as SKEW (Thoth) · wall-clock
tables with a HIDDEN row; the dock FIRE under the stick; SAVE as one download (Enki, Athena) · loop-driven ASM rows and measured-pixel
layout rows at three viewports (Enlil) · patch replay in CI; two replayHash projections reconciled (Krishna) · seat addressing and
one pc/dc per peer (Odin).

## Headline at r.136 (2026-09-19 night, autonomous)

**Fleet: 48/48 delivered** — 12 lenses × reviewer A (served r.133) + reviewer B (r.134 candidate, adversarial) + one synthesiser each
(A + B + the r.135 candidate), plus 12 MoT coordinators. Folds: A → r.134, B → r.135, the syntheses → r.136 (the r.135 regression three of
them found independently: an AI mark in a room approved by the other phone was fireable by nobody).

**True and gated (three):** the AI teammate marks from the game loop and its mark and shot are the AI's on the record (QA goes red when the
loop call is cut) · two phones end on one hash through a lapse, a drop and a withdrawn approval (drone-team-e2e 9/9, now in Deploy) · nothing
marks, approves or fires before a round; the approver's seat never fires the marker's box; an expired target scores nothing.

**Still not true (three):** a stranger cannot join from the two screens alone (the six digits do not pick the team; the refusal is sent before
the wire exists) · a shot can still land with no approver present (fireN checks no liveness; a silent drop keeps the red box 12 s against a
4 s exposure) · each phone keeps its own range clock and forks its ledger on a channel drop; the strip still prints plate ids.

**Grades at r.135 (syntheses):** Aset B− · Asar C+ · Athena B− · Christo B− · Enki B− · Enlil B− · Krishna B+ · Odin C+ · Pangu B ·
Sofia C− · Thoth C · Thor B−.

**Decisions only the operator can make (MoT 10):** T13 or the trinity palette · the AsM Cup fixture's revision (0.039 vs r.042) · the
RED team code private or on the host's card · HI OVER AI in a room (two humans, or one human + AI) · wall-clock or frame-time QUAL tables ·
"one approve, N rounds" at CH1–4 as doctrine or defect · SAVE as one download or four · the DATA panel behind `?diag=1`.

**Two phones, one WiFi — the operator's script:** MoT 7 below (S0–S14), with the numbers the sheet must carry.

## Reviewer B — twelve lenses, adversarial, on the r.134 candidate (840cf55c…)

Each B reviewer received A's report, the r.134 patch and the candidate bytes, and was told to break the fold. Grades are the reviewers' own first lines, verbatim. What r.135 closed is recorded in the register (`REVISIONS.md` r.135) and in the syntheses below.

| Lens | Grade r.134 | Reviewer B's own headline |
|---|---|---|
| Aset | C+ | the record is now one thing (actor on the mark and the shot, lapse and reset as rows, voice one state on three surfaces), but every fix that touched a writer stopped at the writer: the phase strip, the board, the COM/CIL panel and the peer's record still each … |
| Asar | C | RESET, the training lapse and the AI's mark and shot are finally rows, but `decide()` drops every fact it is handed from the hashed row, the APPROVE row lies about who marked, and the fold made the two-seat record diverge on the first lapse.… |
| Athena | C+ | the two silent stalls are gone (refusals above the intro, every lapse said and on the record), but the first sentence is fixed on one pill only, the SELECT screens are exactly r.133, and the new lapse sentence sends him to a press that refuses for 1.5 s.… |
| Christo | C+ | the host now shows the digits and the approver's strip counts the shot, but liveness is a flag that flaps back on the next message, the withdrawn approval still fires on the shooter's phone, and the two records diverge at the first lapse.… |
| Enki | C+ | VOICE outcome/OFF and RESET-on-the-record are closed for real, MAP and blank-sky are instance fixes, the dock/stick and the frame-time clock are untouched, and the fold killed the HUD tail (stream ladder, bloom, MoT stamp, comm text) and the desk keyboard afte… |
| Enlil | C+ | the one gate that was green-while-dead now reads the approver's DOM and provably fails on r.132, CI wiring and a HEAD-consistency gate exist on disk, and the manifest owns 110 rows; but A's first owed item (a loop-driven AI row) was not done, three new regex r… |
| Krishna | B+ | the chain now reaches the domain JSON, the ledger, the carried patch and the live body's sha, and test:ci fails on a missing link; but it links names rather than commits and bytes, the patch is hashed not replayed, the append landed as a whole-file rewrite, an… |
| Odin | C | nothing this lens owns was touched, the document now says so plainly (REVISIONS.md:237-244), and the 1v1 gate still passes; but the liveness fold added a fork-on-drop member that fires on every drop, made the per-device clock class live in the default mode, an… |
| Pangu | B− | The AI member now has a name on its mark and its shot and the tick is one-shot and dead-safe, but every decision row, toast and wire `how` still say a human did it, and in QUAL·40 and LIVE rooms the AI's trigger pull is booked a… |
| Sofia | D+ | the three empty-hand refusals and the voice failure now name the next press and the box no longer survives a channel change, but all six of A's classes are open (no plateWord, no door, no gate) and the fold put four new record words on the player's screen (ASM… |
| Thoth | C− | The three labels A named were relabelled or halved (BOOT closed, UPDATES h3 only, FIXTURE now says HISTORICAL) and one path now decays on a heartbeat, but every number A called a label is still a label, rtt is still a foreign c… |
| Thor | C | every click-reachable instance A named is closed and each closure carries a row, but the class is not: the approver's seat fires on the marker's approval, WAITING fires, a door tags with no box, an expired UAV scores, a string prefix defeats two-humans, and a … |

## The twelve syntheses — one package per lens over A + B + the r.135 candidate (882287ec…)

Each synthesiser read A, B and the r.135 patch, re-verified B's "BROKE" claims against the r.135 bytes, and wrote STATE AT r.135 / GRADE / THE ONE SENTENCE / OWED r.136. The full reports are in the session scratchpad; the grade, the sentence and the owed list are carried here verbatim.

### Aset
- GRADE at r.135: B- — every fact the record carries is now ONE fact on both seats (lapse, reset with its content, the actor, the approval), the panel is alive again, and the lost link is said on four surfaces the same tick; what still says two things is the phase strip for one frame after FIRE/RESET, the DATA pack (blu 200 beside rangeHit 0), the AI's shot on the strip and in the log, the revision in six spellings, and a TAB beacon that re-inflates a DROPPED peer count.
- THE ONE SENTENCE: r.135 made the record say one thing per event on both phones and brought the panel back to life, but the strip, the score pack, the revision and the peer count still have one surface each that reports a different fact — the class is half-closed at the writers, not at the readers.
- OWED r.136:
  1. hudPhase() as the tail of every state writer — fireN (after 2322/2328 lastShot, with lastShot.by=state._shooter), rangeRelease (749) and the peer RESET path (1965) — gate: a QA row that fires, releases and receives a peer RESET and reads phDes+phScore+board on the SAME tick, plus a HUD_TAIL_ALIVE row (one phys() step → cilTxt≠"—" and calibrate invoked).
  2. One score per session — rangeReset()/resetRangePlates() clear blu/score/hits (or the DATA pack stops carrying metrics.blu/hits in range mode) and a QUAL hit emits no SCORE row — gate: "after a hit and a RESET, pack().qual and metricsOf() agree, on both seats, and both hashes moved"; RESET_ON_RECORD run with a red box held (asserts HOLD+RESET, not e0+1).
  3. One name per fact — revLabel() for the six spellings (263, 476, 4003, 4024 banner, 4120 fixture) with drone-playable forbidding any r?\.?0?\.1\d\d ≠ HEAD in served bytes (register narrative excepted); tableName() for "TABLE I"/"TI PRONE SUP" (2257, 2286); peers only rises on DIRECT (1472 under the path==='DIRECT' branch) and paintComm() also writes qaNet — gate: linkLost → TAB HELLO → pathTxt still "DROPPED · 1p" and all five link surfaces read from state.com in one QA row.
  HARDWARE-GATED:
  - Two real phones: the joiner's LAPSED/tables after the host's lapse row over real WebRTC (the per-device clock is now the host's by design — lapseIsMine at 757 — but only a real second device proves the row arrives before the joiner's own exposure ends); DROPPED · 1p on both phones when one locks its screen, and whether a real TAB/PRESENCE beacon re-inflates it to 2p.
  - The one-frame phDes lag after FIRE/RESET at a real 30 fps phone (headless 60 fps hides it).
  - VOICE not-allowed on a real iOS/Android permission prompt (SpeechRecognition never reached in headless).
  - Whether hudTail's CIL/HIL stats and the bloom class hold at a real device's fps (probe saw 60/96, calibrate 9×/1.2 s).

### Asar
- GRADE at r.135: **C+** — RESET, mode, lane-release, every lapse and the AI's own mark/shot are hashed rows that say who and why, and the lapse divergence is closed on the wire; but the same divergence still fires on QUAL rows and door tags, a SPENT and a TARGET DOWN are still a nothing and a `MISS`, a HIT can still land 150 ms after its approver is gone, and the new marker's-seat rule makes an AI mark in a room unfireable by anyone.
- THE ONE SENTENCE: the record is finally honest about what a human or the AI did on ONE phone; put two phones on a QUAL table or a door and the two records disagree, and an AI mark in a room now ends in a red box no seat is allowed to fire.
- OWED r.136:
  1. One writer, one wire, for EVERY host row — `ev()` on the sequencer host calls `sessionBroadcastCommit` (or `qualAdvanceTable` and `commit` go through `netEvent`), one seat writes QUAL/SIM-ACTION per lane, the other reduces; gate in team-e2e: QUAL TIME-out on both seats + one door tag → exactly one `QUAL` and one `SIM-ACTION` row on each seat, `hh===hj`, MATCH.
  2. The marker's-seat rule knows the AI: `NOT_THE_MARKERS_SEAT` compares the seat that owns the mark (`ASM@<sid>` → `<sid>`), never the raw `by`; gate: room, host AsM SPOT, joiner approves, host fires → `HIT role:AI`, no REJECT; joiner fires → REJECT; plus `APPROVER_SEAT_NEVER_FIRES` extended with an `ASM@` marker.
  3. Refusals and presence on the record — SPENT and TARGET DOWN as `REJECT` rows (never a `MISS` verb; TARGET DOWN must not set `_eng` on the peer), TIME-out as N `LAPSE` rows, `fireN` refusing with a row when the box is PEER-approved and `lastPeerAt` is older than two beacons, a peer RESET obeying the host's live-table guard, sidecar `fixture` from real state; gate: close the approver between APPROVE and FIRE → REJECT row, no HIT, MATCH after reconnect; replay `state.events` alone → `qualR/qualH/rangeLapsed` equal `pack().qual` after a TIME-out.
  HARDWARE-GATED (only two real phones / a Pi / a human can prove):
  - Whether a real radio drop reaches `dc close` at all, and how long DIRECT · 2p · red reads on a phone during the 12 s before `SILENT 12 S` — the headless close never fired `dc close`; only silence caught it.
  - That two phones' `sessionSeq`/`logicalClock` still agree after real clock skew and a SNAPSHOT reconnect — the hash match after an in-flight reconnect is unproven headless.
  - That a human reading the pack trusts `LAPSED 19` beside zero LAPSE rows, a `MISS DOWN` that was a refusal, and a red box both phones say the other must fire — a reader test, not a browser test.
  - That SAVE yields all four files on iOS/Android and that `fixture:'ASM_CUP_99'` on a live-play sidecar is noticed as false.

### Athena
- GRADE at r.135: B- — every sentence the stranger reads on the range now names his next tap and every lapse is said, on the record, and truthful about the gap; the three SELECT screens and the eleven-slide intro are exactly r.133, so he still stalls twice before the first shot.
- THE ONE SENTENCE: Once on the range the stranger is now told what to press at every step (AMBER → NOW PRESS APPROVE → NOW PRESS FIRE → HIT / LAPSED · NEXT IN 1.5 S), but the road to the range — eleven jargon slides on 32 px buttons, ENTER WAITING ROOM above PRACTICE, and CH1/3/5 and QUAD/VTOL that PRACTICE silently cannot honour — is untouched since r.133.
- OWED r.136:
  1. SELECT screens honour the solo path (:183-200): PRACTICE first and primary; CH1/3/5 and QUAD/VTOL marked "ROOM ONLY" and greyed when PRACTICE is the exit; `#plat` follows what `goRange` actually seats. Gate: a QA row that PRACTICE from any highlighted CH/craft lands on the seat the bar shows, and that no enabled intro button leads to a refusal.
  2. The refusal is drawn beside the pressed button on the intro (or `#toast` rises above the intro's content at 390), and `TOAST_ABOVE_INTRO` (:3944) reads `elementFromPoint` at the toast's centre, not the z number. Gate: that rewritten row.
  3. CIN :4351 in the range's words ("TARGET paints amber. APPROVE turns it red — you, alone, in PRACTICE; the other seat in a room. Then FIRE.") and `#intro button` ≥ 44 px at 390. Gate: a source row that no CIN line contains "Second HI" and the intro button height row.
  (Residual, same class as 5: a one-tick queue in `toast()` so "MIST" and the PRACTICE pair are not lost — gate: a toast-log row that two sentences in one tick both display.)
  HARDWARE-GATED
  Whether a 12 px sentence at y 738 is read by an eye on the pip at y 487 during a 3 s exposure; whether a thumb makes TARGET-APPROVE-FIRE inside that exposure at 50 m; whether the intro refusal at y 724 is ever seen by an eye on a button at y 206; the 32 px intro buttons under a real finger; Safari 100dvh for the pill row at y 656; a real double-tap-to-fire on glass.

### Christo
- GRADE at r.135: B− — the room now survives its first lapse, its first drop and a withdrawn approval with one record and one truth about who is present, but a stranger still cannot join with what the two screens say, the refusal never reaches him, and the approver is told to press the button that refuses him.
- THE ONE SENTENCE: Two phones now agree on who is in the room, who approved, and who lapsed — the walk that still breaks is the first one, the stranger typing six digits, and r.135's fix for it runs before the wire exists.
- OWED r.136:
  1. The join code selects the team and the refusal reaches the joiner BEFORE the wire: carry the team in the OFFER/ANSWER wrap so a typed RED code seats RED without a tap, and fold the mismatch into the joiner's own ANSWER screen (the host cannot send it) — gate: `drone-team-e2e` reads the six digits from `#wrOtherCode` (never `state`), joins with no team tap, and a second pass with the wrong digits asserts one sentence on the JOINER's `#lcTxt`.
  2. `fireN` refuses a PEER red when `state.com.peers<2` or `lastPeerAt` is older than one exposure (4 s), and the one FIRE sentence on the approver's phone says "THE OTHER SEAT FIRES" instead of "NOW PRESS FIRE" — gate: an e2e step that mutes one seat's dc.send WITHOUT closing it and asserts the other seat's FIRE is refused within the exposure; an in-file row asserting the approver's approve toast never contains FIRE.
  3. The host re-sends what the vanished seat missed: on "THE OTHER SEAT IS BACK", the host runs `snapSend` unconditionally (not only under `syncProof.reconnectSeen`) and the lobby snapshot carries `rangeMode` — gate: e2e drops the joiner for 15 s across one LAPSE, restores it, and asserts MATCH and equal LAPSED counters on both strips.
  HARDWARE-GATED: two real phones on WiFi with no internet (the 1.5 s offer window vs the STUN cap); airplane mode between APPROVE and FIRE on the approver's phone (no close frame — the 12 s watchdog is the only demoter, and the exposure is 4 s); whether a real WebRTC session survives a 15 s radio gap without ICE restart; a human hearing "THE OTHER SEAT IS GONE" and knowing what to do next; a stranger's own hands doing the join using only the two screens.

### Enki
- GRADE at r.135: **B−** — VOICE outcome, the whole-utterance fire word, RESET/MODE with their tallies on the record, the HUD tail, the replay bar and the select guard are closed for real; the dock FIRE under the stick, a scored HIT from the MAP, the frame-time QUAL clock and the three unaimed mark entrances are exactly where A found them on r.133.
- THE ONE SENTENCE: r.135 fixed what it was told and left the two things a thumb finds first — the dock's FIRE button still lives under the R-HEAD stick on every phone, and a shooter can still be approved and score a HIT while looking at the map.
- OWED r.136:
  1. One bottom-stack law (safe-area-aware) for `#joyR`, `.dock`, `#face`, `#toast`, `body.replay #replayBar` and a `BOTTOM_STACK_DISJOINT` gate that measures `getBoundingClientRect` pairwise at 320/390 portrait, turret, dock open — the dock's FIRE and the MAP lane pips must be tappable (gate: `BOTTOM_STACK_DISJOINT`, behaviour, read from the DOM).
  2. `fireN` gains "a shot needs the picture": `REJECT NOT_IN_PICTURE` while `viewMode==='map'` (and behind `#side`/the intro), and `designate`/`approveDesig` say `LEAVE THE MAP FIRST`; gate `MAP_NO_FIRE` as a behaviour row (mark + approve in MAP, fireN, assert no HIT and a REJECT row).
  3. The record hears the human's no and every mark looks at what it marked: the spoken HOLD is `decide('HOLD',id,{reason:'VOICE_HOLD',said})`; `no-speech` re-listens while `state.voice`; `pointerup` 3678, voice 3557–3559 and the map tap 3663 call `aimUnitAt` like `targetN` 2162 (gates: `VOICE_HOLD_IS_A_ROW`, `EVERY_MARK_AIMS` — after each entrance `pipOff(desig.ref)` < the pip floor).
  (Then: wall-clock the QUAL table with a HIDDEN row on `visibilitychange` — `CLOCK_IS_WALL`; the peer's RESET sentence carries the row's `qualR`; the mode-change sentence survives the second `goRange()`.)
  HARDWARE-GATED (only two real phones / a Pi / a human can prove):
  - Screen lock, incoming call and app-switch on iOS/Android zero rAF (headless only throttles to ~6×): the true size of the free QUAL pause and whether the data channel survives it.
  - The real `safe-area-inset-bottom` (iPhone ≈34 px) shifts the dock/joyR/toast/replay-bar stack; all geometry above is at inset 0.
  - `webkitSpeechRecognition` on iOS Safari / Android Chrome: whether `no-speech` fires every ~8 s of silence on a live range, whether `onerror` fires at all with the mic muted by hardware, and whether the whole-utterance rule survives the recognizer's own punctuation ("Fire." / "fire one").
  - The native `<select>` picker on a phone: whether a key or a stick move after a pick reaches the page at all (the OS modal, not the deck's guard, ate the key here).
  - A 30 fps phone / Pi at `HAL PI`: whether the frame-dt table runs 1.5× slow against a stopwatch.
  - Thumb reach at 320: whether a thumb on the R-HEAD stick (440–512) also brushes the APPROVE pill (top 380) or the dock's FIRE (471–509) — the two are 31 px apart.

### Enlil
- GRADE at r.135: B- — the deck finally has a gate that proves its headline (the AI marked BY THE LOOP, and it goes red when the loop call is removed) and every piece of wiring is committed and on main, but one of the three advertised "behaviour rows" passed on the previous row's leftovers, no gate measures a pixel, no checker holds the record's gate sentences to package.json/deploy.yml, and the red-artefact-commit practice was repeated and recorded as acceptable.
- THE ONE SENTENCE: r.135 proves the AI member is alive from the frame loop — I cut the loop call and watched the row go red — but MAP_KEEPS_THE_STRIP was renamed a behaviour row and still cannot fail (it passed on a stale strip from the row before it), so the register's "three regex rows became behaviour rows" is two-thirds true.
- OWED r.136:
  1. Rows that fail for their claim: MAP_KEEPS_THE_STRIP clears `#phDes` after designate and before `phys(0.001)` and asserts the strip names `q.id`; LOST_APPROVER_IS_SAID asserts `#toast` contains 'THE OTHER SEAT IS GONE'; a row changes `#rngMode` with `qualR>0` and asserts the ask — gate: drone-deck-qa replays the 2590 and 1574 mutations and expects red (a mutation gate beside the row).
  2. The record's gate sentences get a checker: deck-consistency parses package.json + deploy.yml and holds every gate NOTES/REVISIONS call "Repo"/"Deploy" to a real script, with `test:asm-play-ai` added to both, ASM_MARKED_BY_THE_LOOP's note `/frame loop itself marked/` pinned in drone-deck-qa.mjs:41, and a `fn.toString()` budget (≤13, each paired with a behaviour row id) in deck-qa-manifest — gate: deck-consistency in test:ci.
  3. Measured pixels: drone-deck-qa at 320×568 / 390×844 / 1440×900 asserting `#playHud.scrollWidth<=clientWidth`, reticle∩`#fAppr`=∅, LOCK∩footer=∅, `#side` left ≥ stage right; team-e2e closes the joiner context and asserts the host's DROPPED strip and the red box back to amber — gate: drone-deck-qa + drone-team-e2e in Deploy.
  HARDWARE-GATED: two real phones on a real WiFi/ICE path (both e2e runs are two contexts in one Chromium; STUN unreachable here); a real microphone permission prompt (VOICE not-allowed / NO MIC); a Raspberry Pi for HAL PI (budgetMs never throttles in Chromium); a human reading the 320 px strip and thumbing a 6×6 px 300 m box; the live site — `cd frontend && npm run status`: SHA c4d2560 | committed ✓ | pushed ✓ (origin/main = c4d2560) | cloudflare UNVERIFIED (HTTP 403 from the sandbox) — https://exel-ai-polling.explore-096.workers.dev/drone-2525/play.html.

### Krishna
- GRADE at r.135: B+ (held, not raised) — every link I can compute agrees (register, README, JSON 0.020, ledger 30, HASHES, patch replay, 12/12 headless, 9/9 two-context), the append was an append and the commit cited is the artefact's, but all three items B owed this lens for r.135 are untouched: the gates still link names, the patch is still never executed, the matrix/DRN-13.03/lexicon/deny-list still describe r.131 or r.128.
- THE ONE SENTENCE: r.135 is one head everywhere a script can check it — but the record is coherent by discipline this edition, not by gate, and what is on the remote is still r.134 (2020c02 unpushed, the fold staged).
- OWED r.136:
  1. deck-consistency compares EVIDENCE, not prose: JSON release.commit == ledger release.commit, both ∈ the register row's "shipped in" for DECK_REV; ledger `sha256 xxxxxxxx` prefix == register sha; the JSON held canonical (`JSON.stringify(JSON.parse(f), null, 2)+'\n' === f`); 0.018's 449de5f corrected by an APPENDED `correction` entry — gate: test:deck-consistency (test:ci, spiral9).
  2. Replay the patch chain in CI: drone-revisions runs `python3 patches/r{N-1}_to_r{N}.py` on the carried predecessor into a temp dir for every carried patch and holds output sha == the row's sha and patch sha == HASHES_rN:2; add test:drone-deck-qa + test:drone-team-e2e to test:ci behind a Chromium-presence check that FAILS, never skips — gate: test:drone-revisions + test:ci.
  3. Close the stale carriers by rule, not by edit: drone-playable:22's deny-list → "every r0.NNN / r.NNN in the deck equals DECK_REV or sits inside a HISTORICAL FIXTURE / dated UPDATES label"; DRN-13.03's metric computed from the REVISIONS.md row count (8) in drone-crs; crs-matrix DRN rows held to `change == v00.00 r<project.revision>`; lexicon `drone.play_deck` englishDefault carries no revision (the code supplies it) — gates: test:drone-playable, test:drone-crs, test:lexicon-coverage.
  HARDWARE-GATED: whether Cloudflare's edge serves the 882287ec… bytes (sandbox curl → HTTP 000; only the runner's Verify Live can compute the live sha — and it will read r.134 until 2020c02 + the fold are pushed); two real phones over STUN/TURN ending on one hash (headless proves two contexts on one host, 2da99b4b == 2da99b4b, with no NAT between them); the PLAY label reading r.135 in a non-English store once a translation of drone.play_deck exists (today all 33 fall back to the English default, which lexicon-coverage exempts).

### Odin
- GRADE at r.135: C+ — the three r.134 members this lens could name in one line (duplicate LAPSE, dead reconnect, AI-mark bypass) are closed and the 1v1 gate holds, but none of the six future-proof classes moved, and the joiner still forks its ledger on every channel drop — a regression r.134 added and r.135 carries, now under a working "IS BACK" toast that cannot repair it.
- THE ONE SENTENCE: r.135 is an honest 1v1 — every phone still keeps its own range clock, forks its ledger the moment its channel closes, and cannot say which seat fired (both phones are T06) — so 3v3 remains a label held true only by the refusal at :2971, and the first thing owed is the fork, because it fires in the 1v1 that is served today.
- OWED r.136:
  1. Never mint on a JOIN: `sessionPrepareLocal` :1183 on role JOIN with the channel down keeps the row pending (or refuses "NOT ON THE RECORD · RECONNECT"); `linkLost` writes a pending HOLD; SNAPSHOT :1517 merges (keeps `committed:false`, resubmits) and pending rows expire or re-mint on the next host. GATE: `drone-team-e2e` drop-mid-red → HOLD → reconnect → ONE hash on both; QA `LOST_APPROVER_IS_SAID` re-run with `role:'JOIN'` asserting `committed:false`.
  2. The range clock on the record: the host emits `EXPOSE {lane,k,base,sec}` when a plate rises; a joiner advances a lane only from rows (or `{k,phase,t}` in LOBBY_LAUNCH + SNAPSHOT); LAPSE/HIT/DESIGNATED reducers (:1963, :2179) refuse a row whose id is not the lane's current exposure. GATE: two-context e2e in QUAL·40 asserts the same `base` up on both at APPROVE and `R.k` equal on both after 20 s.
  3. A pure fold and a seat on the row: `applyWorld` becomes a fold over `events.filter(committed).sort(orderKey)` on every commit/snapshot (:1990, :1518); `ev` :2020 carries `lane`+`unit`; `laneClaims` :2922 is read at launch. GATE: two rows delivered out of order → world === hash; QA asserts `lane`/`unit` on a HIT row and distinct `unit` on the two seats of the team e2e. (Per-peer `lastSeen`/`peers[]`/one pc-dc per joiner stay before `match>1` is ever allowed past :2971.)
  HARDWARE-GATED: two real phones on one WiFi, with a real drop and rejoin, are the only proof of the fork (P1) and the return path (:1471) as the code reads — the harness fakes `readyState`; real clock skew and frame-rate difference between two phones are the only proof of how far the two range clocks drift (P4c ran both in one process); a third phone is the only proof that its answer overwrites the second's channel (:3291/:3327); a Pi/EDGE relay host is unproven in any harness; LIVE at the edge is UNVERIFIED from this sandbox.

### Pangu
- GRADE at r.135: B. The AI member is one actor on the mark, the shot, the decision, the strip and the other phone's toast, fires once per red box, never in QUAL·40 and never alone in a room — but the harness still cannot see it shoot, the joiner's record and the SIM-ACTION row still say a human did it, and MAP still hides a firing AI.
- THE ONE SENTENCE: r.135 gave the AI a name and a leash (one shot per red box, never a qualification round, never one human in a room), but the only machine that plays the AI seat still fires with its own finger and reports the AI's three hits as three misses — the AI's shot is proven by an in-file row, not yet by the replay the record quotes.
- OWED r.136:
  1. The harness measures the AI as the AI — asm-play `ai` seat waits for a HIT/MISS row `by ASM@` instead of clicking `#fFire`, :14 reads "1 SPOT · 2 FIRE · 3 OFF", REVISIONS's "AI FIRE 3/3" restated from the JSON; gate: an asm-play run with asmPress 2 on the candidate reports `hits` equal to the strip's HIT (3/3, 0 refusals).
  2. One record of one approval in a room, on the record — the joiner's APPROVE row carries the AI (`designatedBy:ASM@host, how:'PEER HI OVER AI'`, viaAI from the row, never as a passport), the slot's `how` and :2205's msg follow, and the rule is a D-number decision row ("r.129 stands; HI OVER AI is the solo form"); gate: LIVE_TWO_HUMANS extended with an ASM@ mark (own device HELD, other seat → PEER HI OVER AI on both records, identical `stableCanon`).
  3. The rest of the identity class in one pass — SIM-ACTION row `role:AI` (decide passes `by:_shooter`), replay log names ASM@ not T1, APPROVE `samePerson/sameDevice:false` for an AI mark, the approve toast says "THE AI FIRES NEXT" when asmFire and "YOU APPROVED THE OTHER SEAT'S MARK" for a peer mark, the AI's MISS booked as `MISS · SEAT AIM` or refused when the seat is off-plate, D18 appended to the assessment and asmTick either stopped in MAP or MAP_KEEPS_THE_STRIP asserting the AI keeps marking; gate: a row that runs mark+approve+shot with FIRE on and finds no `T1`, no `samePerson:true` and no `role:HI` on any ASM@ row.
  HARDWARE-GATED: two real phones both with SPOT on (ASM@host and ASM@joiner marking one lane — which mark takes the single desig slot, and whether the joiner's strip says THE OTHER SEAT'S AI on the first frame); whether the joiner's approve reaches the host before the host AI's next tick under real data-channel latency (the shot is one tick after approve, ~50 ms); whether a phone in MAP view hears or feels the AI fire; whether a Pi seat at 60/96 fps keeps `asmFired` across a dropped frame; whether a human reading "YOU APPROVED THE AI'S MARK · NOW PRESS FIRE" presses FIRE into the AI's shot and reads NO RED BOX as a miss.

### Sofia
- GRADE at r.135: C- — the session id is off every player surface, the AI is named as the AI, one amber sentence and one approve sentence exist in the player's words and the lost-seat sentence is now true, but the strip, board and lapse toasts still print "C-150R-L18", the machine panel is still the player's panel with new machine words on it ("skew", "D3"), LISTENING still lies for 100 ms, and with no word gate the release added a new over-claim ("YOUR TARGET LAPSED") on the most-pressed button.
- THE ONE SENTENCE: r.135 taught the deck to say "THE AI" and "THE OTHER SEAT" instead of a session id, but a player still reads C-150R-L18 on the strip, cnn3/WEBCRYPTO/skew on the panel and "YOUR TARGET LAPSED" after their own hit — until a word gate exists every fold will keep adding one machine word for each it removes.
- OWED r.136:
  1. `frontend/tests/drone-words.test.mjs` in test:ci (B's two-stage spec: static extraction of toast/textContent/fillText/option/button strings outside a `?diag=1` block + runtime read of #phDes/#phScore/#board/#feed/#replayBar over asm-play's solo/ai/team seats), banning HI-2|PEER|SOLO|NET|cnn|WEBRTC|WEBCRYPTO|IDB|WORKER|SEGMENTS|pip|skew|WIRE|MIST|EDGE|SPIRAL|SSSES|MoT|HAL|ASM@|C-\d{2,3}[LRC]?(-L\d\d)?|[a-z0-9]{6,9} — gate: the test itself; r.136 fails CI on "CH0 D3", "0ms skew" and "LAPSED · C-200L" until they are folded.
  2. Route the strip (2663-2665), the board, both lapse toasts (758/760), 2361/2398/2360/2346 through plateWord/seatWord; collapse TWO HUMANS to 2200's sentence at 2227, lapse to ONE sentence at 758=760=844, and make 2303 say "YOUR TARGET LAPSED" only when state.lastLapse.id === the player's own desig id (else "NO RED BOX · MARK A TARGET FIRST"); delete 2305-2312 — gates: QA rows ONE_SENTENCE_PER_REFUSAL (toast set per refusal class has size 1) and LAPSE_IS_YOURS_ONLY_IF_YOU_MARKED_IT, plus the static stage of item 1.
  3. The operator door: `?diag=1` gates the aside from "LINK · 7.77 MB" down (MoT LEVELS, cil/hil/sil/com/cmp/cal/lcCrypto/seqTxt/pathTxt readouts), the motLvl/halPick/diffLvl pickers and their 3486/3488 toasts, room notes 234/244, canvas 1813, Spiral gate 3450-3471; DATA becomes score · SAVE · EXPORT · REPLAY · CONTROLS; "LISTENING" moves to `r.onstart`; "WIRE"→"EXPORT SAVED TO THIS PHONE", "SAVED 333"→"SAVED" — gates: QA row DIAG_DOOR_CLOSED (without ?diag=1 no element containing cnn|WEBCRYPTO|IDB|skew is in the DOM) and the runtime stage of item 1 asserting LISTENING is never captured before state.voice===true.
  HARDWARE-GATED: whether "MARKED BY THE OTHER SEAT'S AI" / "THE OTHER SEAT IS GONE" / "THE OTHER SEAT WITHDREW · YOUR RED BOX IS AMBER AGAIN" arrive on the second phone inside the 12 s heartbeat and read as one sentence at 320 px; whether LISTENING→VOICE OFF is two toasts or one flash on a real iPhone/Android (headless Chromium has no recognizer — only the lie is provable here); whether "50 M" / "150 M RIGHT" alone finds the plate in daylight on a laser-tag range with a human at the far end; the canvas "· SPIRAL v1" stamp and MAP legend at real DPR.

### Thoth
- GRADE at r.135: C. The boot lie, the overclaiming loss sentence, the path-blind heartbeat and the un-revivable DROPPED are closed by real behaviour, and one strip now says "skew"; but SSSES is the same label with the same unfailable row, rtt's sign is wrong on four of five surfaces and the revived frame tail put "-1981ms" back on the COM stat, qaOps grew a third denominator, peers/sync still climb on a relay beacon, and six revision strings stand where one should.
- THE ONE SENTENCE: r.135 fixed what the strip said about a lost link, but every number the DATA panel offers as the system's quality — SSSES/AVG, the signed "rtt", OPS, SAMPLES, the six revisions — is still a label, a stale snapshot or a foreign clock, and the Aset fix that revived the frame tail quietly re-opened the negative-millisecond stat that r.135 claims to have relabelled.
- OWED r.136:
  1. rtt — one writer, one sign, one word: delete the l.2677 frame writer (or make it the paintComm string), apply `Math.abs`+"skew" (or an echoed same-clock HELLO-ACK) to the sample row l.2095, CSV l.3427 and COMM l.3442; gate: a deck-qa row `NO_NEGATIVE_MS` that injects a foreign t0, lets one frame run, and reads comTxt/pathTxt/last sample/COMM for a "-" (`tests/deck-qa-manifest.mjs`, in drone-deck-qa).
  2. D16 — peers/sync/scale follow the path: gate l.1472/l.1476 on `path==='DIRECT'||state.com.path!=='DROPPED'`, decay TAB/LOCAL on the same 12 s (l.1575), write qaNet on loss, SCALE after a loss = the never-linked value; gate: a deck-qa row `DROPPED_NEVER_2P` (relay HELLO after linkLost → peers 1, ALONE, SCALE 70) plus the existing LOST_APPROVER_IS_SAID.
  3. SSSES + OPS + revisions as honest numbers: compute the SSSES row AFTER the l.3963 restore (so the QA's 11 HOLDs cannot move it), print sec/suc/scale "DECLARED" and eff "UNMEASURED" while null, make the row assert measured inputs (fps>0, peers from lastPeerAt) so it can go red; print qaOps "113/114 · 2 pending" until both deferred rows land and make ASM_MARKED_BY_THE_LOOP red (not green) when "not exercised"; derive updBox head and qaRev from BUILD.revision (kill "r.128 …" and the doubled "r."); gate: extend the drone-deck-qa rev check to assert exactly ONE `r.1\d\d` string on the panel and that `SSSES` is absent from EXPECTED_RED only if its inputs are measured.
  HARDWARE-GATED: a real round trip (two phones over WebRTC, echo-based, one clock) and what the 12 s heartbeat sees when a phone sleeps vs locks vs backgrounds Safari; whether "DROPPED · 2p" ever appears on a real second phone that keeps a BroadcastChannel sibling tab; SCALE/EFF (fps vs HAL PI budget) on a Pi; four simultaneous downloads and the 4000→3000 trim against Android/iOS storage — every SCALE/EFF/OPS number above is desktop-Chromium only.

### Thor
- GRADE at r.135: B- — every click-reachable member B named is closed with a row (WAITING, the approver's seat, the door, the expired target, the passport, the CH5 re-approve), but the new seat rule was cut one token too wide and silently disarmed the AI member in every room, and the peer's ordinary release still never reaches the shooter.
- THE ONE SENTENCE: r.135 makes the fire gate refuse the right people, but it now also refuses the AI's own approved mark in a room, and a partner who walks away from an approval (scene change) still leaves a live red box on your phone.
- OWED r.136:
  1. Fix the seat rule: in fireN 2301 the marker's seat is `SID || 'ASM@'+SID` (never `data.by` from a row); gate: behaviour row AI_MARK_FIRES_IN_A_ROOM + a `seat:'ai'`-in-a-room harness run that ends with a HIT row by ASM@host.
  2. Reduce every peer HOLD that releases (AUTHORITY_RELEASED, TARGET_DOWN, TARGET_GONE, LANE CHANGE, PEER RESET) at 1964, and call releaseAuthority from lobbyEnterLive (2933) and the drone expiry (2533); gate: behaviour rows PEER_RELEASE_TAKES_MY_BOX (apply a peer HOLD AUTHORITY_RELEASED ⇒ my red is amber, FIRE refused) and NO_RED_INTO_LIVE (red in WAITING ⇒ amber after START).
  3. One entry to commit(): mark()'s watch branch (2342) moves below the red-box check, btnYes (3496) and Enter (3538) call a gated `commitPending()` that refuses without `desig.phase==='red'` for that id, and the 2306-2313 corpse goes; gate: behaviour row WATCH_TAG_NEEDS_RED_BOX (mode watch, pending set, btnYes ⇒ no TAG row, score unchanged) and a source gate that `commit(` has exactly one caller.
  HARDWARE-GATED
  Two real phones: the approver's radio dropping between APPROVE and FIRE on real ICE (linkLost timing at 1570 vs the 12 s silence at 1576), and a TAB-mode room losing a phone (1571 returns; nothing headless reaches it). A real mic: "don't approve" / "the fire alarm" through SpeechRecognition (unreachable headless; the row is a regex). A Pi/edge clock: any approval TTL, once one exists. A human ruling: whether "one approve, N rounds" at CH1-4 (2330 keeps red after a miss) is doctrine or defect, and whether the AI member's mark should ever be fireable by the approving seat rather than the marking one.

## The twelve MoT coordinators — one cross-cutting question each, verbatim

### MoT 1

```
MOT-1 · WHAT r.136 FOLDS FIRST, IN ORDER (12 syntheses; Enki from A/B — no synthesis). Verified on served play.html sha 882287ec… = docs r.135, HEAD c4d2560 = origin/main; patch docs/drone-2525/operator-deck/patches/r134_to_r135.py. Line numbers are r.135.
ORDER RULE: (a) before (b) before (c) — a regression is served to every player today and was green at 115/116 because no row covers it; a false sentence on the record must be corrected by APPEND before r.136 claims anything (CLAUDE.md: measured number, never typed); then defects by blast radius (every turret phone > every room drop > every room shot > first join > every timed table > every word).

(a) REGRESSIONS r.135 INTRODUCED
1. An AI mark in a LIVE room is unfireable by any seat — fireN 2301 `desig.by!==SID`, but the AI marks as 'ASM@'+SID (2460), so host and joiner both get REJECT NOT_THE_MARKERS_SEAT; asmTick's own trigger (2464) dies on the same line (Thor "R.135 BROKE" S2/T4; Asar B3 probe; Pangu). One-token fix (`SID || 'ASM@'+SID`) + row AI_MARK_FIRES_IN_A_ROOM — no r.135 row covers it, which is why the QA is green.
2. The approver is told to press the button that refuses him — 2238 toasts "APPROVED BY THE OTHER SEAT · NOW PRESS FIRE" on the APPROVER's phone; 2301 answers "THE SEAT THAT MARKED IT FIRES · YOU APPROVED" (Christo NEW, observed on the joiner; Pangu). Same edit: "THE OTHER SEAT FIRES".
3. The negative-millisecond COM stat is back — patch:17 re-called `hudTail(u,S)` from phys (the Aset-B fix), reviving 2677 `rtt.toFixed(0)+'ms p'` at frame rate over the 4394 abs+"skew" writer: "-1981ms p2" 59 frames in 60 (Thoth probe). Delete the 2677 writer; row NO_NEGATIVE_MS.
4. A new over-claim on the most-pressed button — 2303 says "YOUR TARGET LAPSED · MARK THE NEXT" whenever ANY plate lapsed in 6 s (757 sets lastLapse for every unmarked lapse): said to a player who marked nothing and to one who just HIT, while the row says REJECT NO_RED_BOX (Sofia rule 5; Asar "new small lie"). Condition on lastLapse.id === the player's own desig id.
5. The r.134 divergence class reappears one member over — LAPSE moved to netEvent (758) but the host's `ev('QUAL')` (818) and the door tag's SIM-ACTION (2346 → decide, which sends only HOLD/REJECT/RESET, 2170) never leave the host (sessionPrepareLocal 1183-1195 commits with no broadcast): QUAL TIME-out → host 2 rows, joiner 1, DIVERGED forever; door tag → score 350 vs 0 (Asar team3 probe, "A MISSED"). One writer, one wire for EVERY host row; team-e2e gate on a TIME-out + one tag → one row each seat, hh===hj.

(b) CLAIMS ON THE RECORD THAT ARE FALSE TODAY (correct by append, then fix)
6. REVISIONS:257 / NOTES §6 "skew, not rtt, on the panel" — comTxt is signed at frame rate (item 3) and the sample row 2095, CSV rtt 3427 (46 negative of 359 rows) and COMM section 3442 are untouched: 1 surface of 5 holds (Thoth).
7. REVISIONS:258 / NOTES "VOICE_NEGATION_HOLDS, MAP_KEEPS_THE_STRIP, ASM_NEVER_MARKS_DEAD are behaviour rows" — VOICE_NEGATION_HOLDS is still `vs.indexOf` (3935, verified); MAP_KEEPS_THE_STRIP stayed GREEN with hudPhase() cut from the map branch (2590) because it reads the previous row's stale strip (Enlil mutation; Thor B4; Aset). One of three is true.
8. REVISIONS:263 / NOTES "AI FIRE 3/3 (the AI's shots on the record)" — asm-play.mjs:40 clicks #fFire for the `ai` seat; the harness JSON says hits 0, misses 3 (each FIRE read NO RED BOX after the AI shot). The 3/3 was read from the strip, not the harness (Pangu A5). A typed number.
9. REVISIONS:262 / NOTES §6 "the join refusal travels" — 3341 linkSend runs while the host applies the ANSWER, before any data channel exists, and throws into `catch(_){}`; the joiner stays "ANSWER READY · HOST MUST VERIFY CODE" forever, 0 events, no toast (Christo A1c probe).
10. REVISIONS:253 "only DIRECT traffic feeds the heartbeat" — the path holds, but 1472 `peers=Math.max(2,…)` and 1476 sync=MATCH run on ANY HELLO on ANY path: one TAB beacon after loss → "DROPPED · 2p" MATCH SCALE 100 on one strip (Thoth, Aset item 6, Odin P2c).
11. REVISIONS:260-261 "116 rows, 115/116" — ASM_MARKED_BY_THE_LOOP self-passes `ok:true 'not exercised'` when phase≠SETUP (3995); qaOps has three denominators (113/114 → 114/115 → 115/116 by 2.8 s) and the log row keeps the first (Thoth, Enlil residue). SSSES row 3973 still cannot fail; suc=90 is the QA's own 11 HOLDs (Thoth).
12. Repo carriers still describing r.128/r.131: lexicon `drone.play_deck` "PLAY · r.128" (lexicon-data.ts:2980); crs-matrix "r0.016" ×5; DRN-13.03 "4 entries r.128..r.131" against 8 register rows; drone-playable:22 deny-list; six revision spellings on one page (r0.135 · r.0.135 · 0.135 · r.128 · r.132 · r0.103) (Krishna 1/3/6, Aset 5, Thoth). deck-consistency holds prose regexes, never JSON.commit==ledger.commit; patch hashed, never run (Krishna a/b).

(c) LARGEST PLAYER-FACING DEFECTS (blast radius order)
13. The dock's FIRE is under the R-HEAD stick on every turret phone — `#app.turret #joyR` bottom 56px+safe z8 (CSS 70) vs `.dock` bottom 8px+safe z6 (CSS 80), unchanged by the patch (0 hits): elementFromPoint(FIRE centre)=joyR at 320 and 390, a tap pans and fires nothing, silently; 15 of 42 MAP lane pips untappable at 320 (Enki A1/B1 measured; register admits "still open"). One bottom-stack law + a getBoundingClientRect disjointness gate.
14. The joiner forks its ledger on every channel drop — sequencerUsesHost (1164) is false once dc closes, so sessionPrepareLocal commits the row locally as host (P1: HOLD PEER_LOST committed:true sessionSeq 101); "THE OTHER SEAT IS BACK" fires but snapSend needs syncProof.reconnectSeen (1476-1481) → DIVERGED with nothing to repair (Odin 2, Christo A2). Fires in the 1v1 served today.
15. A shot lands with no approver present — fireN never reads lastPeerAt (2296-2312, verified); a silent drop (airplane mode, no close frame) leaves the red box up to the 12 s watchdog (1575) > any 4 s exposure (Asar probe: HIT 150 ms after the approver closed; Christo probe135-drop); and the peer's ordinary release (HOLD AUTHORITY_RELEASED · SCENE CHANGE) is not reduced at 1964 — joiner walks to CH1, host still fires a HIT (Thor S4b).
16. The stranger cannot join from the two screens — typed RED digits without tapping RED = TEAM DENIED (lobbyNetJoin 3068 binds to the lit button), and the refusal never arrives (item 9); ENTER WAITING ROOM sits above PRACTICE, CH1/3/5 and QUAD not greyed though PRACTICE cannot honour them, refusal at y 724 for a button at y 206, 32 px intro buttons, 11 jargon slides (Christo A1, Athena 4-6 — untouched since r.133).
17. Every timed table runs on clamped frame dt — no visibilitychange/pagehide (grep 0), `dt=min(.05,…)`: a hidden tab pauses a QUAL table for free, a 30 fps phone plays it 1.5× slow, nothing on the record (Enki A5/B5; register admits open).
18. Machine words on the player's surfaces with no word gate — strip 2663-2665, board and both lapse toasts 758/760 print "C-150R-L18"; panel prints cnn3 / WEBCRYPTO / "0ms skew" / "CH0 D3" (new in r.135); `?diag=1` = 0 hits; LISTENING toasted before onstart (3566); TWO HUMANS still two sentences (2200 vs 2227); "nothing marked" is six sentences (Sofia). Each fold adds one machine word per word removed until drone-words.test.mjs exists.
19. A score without authority — watch-mode mark() sets `pending` (2342) before the red-box check (2343); btnYes 3496 / Enter 3538 → commit → TAGGED +250 and a TAG row with no red box ever (Thor item 2, probed). Plus the `if(false)` corpse 2306-2313 still ships.
20. The DATA pack says two things about one session — RESET clears qual but not blu/score/hits (pack().metrics.blu=200 beside rangeHit=0), a QUAL hit still emits a SCORE row (Aset 2); MAP still hides a firing AI (spawn 2585 before the map return 2586; Pangu A6); host and joiner both report unit T06 for lane 5 — two phones, one turret id (Odin 6).
HARDWARE-GATED (not for r.136's headless gate): real ICE drop timing vs the 12 s watchdog; safe-area-inset-bottom ≈34 px on the dock/stick overlap; SpeechRecognition on iOS/Android; screen-lock rAF; Cloudflare LIVE (403 from the sandbox — `cd frontend && npm run status`, UNVERIFIED).
```

### MoT 2

```
MOT-2 — GATES IN THE SERVED r.135 THAT ARE GREEN-WHILE-DEAD OR CANNOT FAIL FOR THEIR CLAIM (play.html 882287ec, 116 rows; lines are the served file; every claim verified by reading the row)
A. fn.toString() rows — 13/116 (Enlil-SYNTH "Regex roster 13/116"; verified 3730-3735, 3737, 3741, 3742, 3923, 3928, 3935, 3955). Class: they go red only when the SOURCE TEXT changes; the call site can be removed, commented or made unreachable while the string survives. Enlil proved it: `/* asmTick(dt); */` in spawn kept ASM_TICKED_LIVE (3923) green while the loop row went red. What must break / the behaviour row that replaces each:
1. ROOM_READY_GATE 3730 — red only if the toast literal is edited. Replace: joiner not READY, click #wrLaunch → no LOBBY_LAUNCH row, #toast /WAIT BOTH READY/.
2. ROOM_READY_AUTH 3731 — same. Replace: pre-auth joiner presses #wrReady → lobby.ready stays false, #toast names the auth.
3. ROOM_HOST_MATCH 3732 — same. Replace: role JOIN, lobbySetMatch(3) → matchSize unchanged, the refusal toasted.
4. ROOM_CODE_LOCK 3733 — same. Replace: after auth, lobbyRotateCodes() → teamCodes identical before/after, refusal toasted.
5. ROOM_CANON_LAUNCH 3734 — red only if the verb literal leaves BOTH functions. Replace: lobbyLaunch() → last event verb LOBBY_LAUNCH, and applyWorld of that row alone sets phase LIVE.
6. ROOM_NO_FORCE_SOLO 3735 — a NEGATIVE regex: red only if someone types "forceSolo"; any other solo path passes. Replace: lobbyLaunch() with one member → phase not LIVE, refusal row.
7. ROOM_HASH_DATA 3737 — replace: two events equal but for data → two replayHash values.
8. SLOTS_USE_LANE 3741 — replace: state.lane=5 → every slots() id is a lane-5 plate.
9. PEER_DESIG_PLATES 3742 — half is already behaviour (PEER_DESIG_TO_SLOT 3905 calls peerDesig directly); the commIn half is only a regex. Replace: commIn({verb:'DESIGNATED',id:'C-100L-L01',peerId:'peer-A'}) → tgtSlot[2] holds that plate by peer-A.
10. ASM_TICKED_LIVE 3923 — superseded by ASM_MARKED_BY_THE_LOOP 3995 (the loop row that Enlil watched go red). Retire; two rows for one claim is how the dead one hides.
11. EMPTY_RANGE_ONE_SENTENCE 3928 — proves one entrance of three (Enlil 5). Replace: no plate up → targetN(1), click #fTgt, voice 'target' → three toasts, all === noLockMsg().
12. VOICE_NEGATION_HOLDS 3935 — indexOf order over voiceToggle source; the regexes at 3556-3560 are never run (Thor 5, Aset 17, Enlil 6, Christo A6). Replace: feed the onresult handler {transcript:"don't approve"} on an amber box → amber stays, #toast VOICE HOLD, no APPROVE row; "the fire alarm" on a red box → no shot row.
13. HOLD_ROWS_TRAVEL 3955 — replace: linkMute off, stub linkSend, decide('HOLD',…) → exactly one stub call whose event.verb is HOLD (one decision, one row).
B. Rows that pass on a previous row's leftovers:
14. MAP_KEEPS_THE_STRIP 3936 asserts `/AMBER/` on #phDes after phys(0.001) in map — designate() never calls hudPhase (callers are only 1574, 2590, 2657), so the DOM it reads is the strip LOST_APPROVER_IS_SAID 3933 left through linkLost→hudPhase (plate C-100L). Enlil deleted hudPhase() from 2590 → still 115/116, the row green with the previous row's plate in its note. Replace: blank #phDes after designate, run phys in map, assert the strip names q.id AND the phase; then approve → /RED/ (Enki-B 29).
15. RESET_ON_RECORD 3931 asserts events.length===e0+1 — true only because the QA holds no box; a real RESET with a box writes HOLD+RESET (4430-4431; Aset probe: 2 rows). It cannot notice the RESET row losing its why/mode/qualR data. Replace: red box up, qual40, qualR>0 → HOLD AUTHORITY_RELEASED then RESET with data {why,mode,qualR}; plus a row that changes #rngMode with qualR>0 and asserts the select reverts and the ask is toasted (4434-4436 — nothing exercises it, Enlil B).
C. Rows that self-pass or cannot fail:
16. ASM_MARKED_BY_THE_LOOP 3995 pushes `ok:true, note:'not exercised here'` when lobby.phase≠SETUP (Enlil residue, Thoth "passes by absence"); drone-deck-qa.mjs:41 pins notes for four range rows, not this one, so a not-exercised green counts as a pass in CI. Fix: ok:false + note NOT EXERCISED, or pin `/frame loop itself marked/` in drone-deck-qa.
17. SSSES 3973 `S&&S.avg>0` — avg ≥ (95+40)/2 = 67 by construction (2561-2563); red only if ssses() throws (Thoth). Replace: compute after the restore at 3976 and assert measured inputs (fps>0, peers from lastPeerAt, holds not the QA's own 11).
18. F03_ONE_LY 3720 and TEAM_CODE_POLICY 3729 are `push(id,true,…)` literals — never red. Replace: F03 → count of LY units === 1; TEAM_CODE_POLICY → the AES key derivation does not consume the 6 digits (or delete the row; a policy is not a gate).
19. LOST_APPROVER_IS_SAID 3933 hand-sets path='DIRECT' and asserts state.com.path/peers/desig/hiApproved only — the sentence at 1574 is asserted by nothing and a TAB-mode loss is never exercised (Thor 16, Enlil B). Replace: assert #toast /THE OTHER SEAT IS GONE/ and the strip /DROPPED · 1p/, and reach linkLost through lastPeerAt aging past 12 s (1576), not by calling it.
20. TOAST_ABOVE_INTRO 3944 compares two z-index numbers, not the picture (Athena 1). Replace: elementFromPoint at the toast's centre returns #toast while #intro is up.
21. HUD_SCORE_WRAPS 3918 reads computed-style strings; no pixel is measured at any width (Enlil 4). Replace: at 320 and 390 with HIT/MISS/LAPSED and the QUAL clock populated, #playHud.scrollWidth<=clientWidth and the score line inside the viewport.
22. roundOpen() 2295 returns true on `!!state.qaArmed` — a production gate with a test backdoor; every fire-gate row that forgets to un-arm (EXPIRED_TARGET_SCORES_NOTHING 3950, AI_PASSPORT 3952, CH5_MISS 3954 do not) runs with the round open by decree. Replace the backdoor with the rows setting lobby.phase='PRACTICE' explicitly, and delete the qaArmed clause.
D. e2e assertions that read state, not the picture:
23. drone-team-e2e.mjs: of 9 asserts, the joiner's amber (`tJ.desig`), both-red (`aH.desig`/`aJ.desig`), LIVE (`sH.lobby`), rev and the hash/MATCH read `state`; only DIRECT/AUTH, the self-approve toast, the host's DOWN and the joiner's HIT 1 read the DOM — the class D3 belonged to. Replace: J's #phDes names the host's plate as AMBER after the mark; both #phDes /RED/ after approval; close ctxJ → host strip /DROPPED/ and the red box amber (Enlil owed 3).
24. drone-team-e2e.mjs:32-35 lifts the RED code from `state.lobby.teamCodes`, never from #wrOtherCode — the host's card can break and 9/9 stays green (Christo A1a). Replace: read the six digits from #wrOtherCode's text.
25. asm-play.mjs `ai` seat still clicks #fFire (line 40) and reads NO RED BOX after the AI's own shot; REVISIONS r.135 "AI FIRE 3/3" is the strip's HIT 3 beside JSON hits 0 (Pangu A5), and the seat is in no package.json script or workflow (Enlil 1; deploy.yml:71-72 = deck-qa + team-e2e). Replace: for the `ai` seat, assert a HIT/MISS row with data.by /^ASM@/ and no #fFire click; add `test:asm-play-ai` to package.json and deploy.yml.
26. drone-deck-qa.mjs:30 waits on a COUNT (`state.qa.total >= n`), then :33 holds the set equal to the manifest — the count is harmless only because the manifest moved in the same fold (Krishna 6); test:ci carries neither browser gate (package.json:170) — a green test:ci proves nothing about the 116 rows.
E. fn.toString budget rule for deck-qa-manifest.mjs (a static check in deck-consistency, which is in test:ci):
27. `export const SOURCE_ROWS = { ROOM_READY_GATE:'ROOM_READY_GATE_BEHAVES', …13 entries… }` — every row whose body contains `.toString()` is listed here, paired with the id of the behaviour row that proves the same claim; `export const SOURCE_ROW_BUDGET = 13` (the r.135 measured count).
28. Gate: parse toolSelfTest (3700-3986) and the deferred rows; count push() rows whose expression contains `.toString()` or `indexOf(` over a function source → fail if count > SOURCE_ROW_BUDGET, if any such id is absent from SOURCE_ROWS, if a paired behaviour id is absent from DECK_QA_ROWS, if any `push('X',true,` literal exists, or if a row's note matches /not exercised/ while ok is true.
29. Ratchet: the budget may only go DOWN; a new source row is refused unless its paired behaviour row lands in the same edition, and retiring a source row (ASM_TICKED_LIVE first) decrements the budget in the same commit — deck-consistency then holds manifest count == actual count, so a row cannot be renamed "behaviour" without changing what it reads (the MAP_KEEPS_THE_STRIP class).
```

### MoT 3

```
MoT-3 · THE SSSES HONEST POSTURE (r.135 as served: frontend/public/drone-2525/play.html, byte-identical to the r.135 candidate per Thoth-SYNTH:3)

PER PILLAR, AS r.135 COMPUTES IT (play.html):
1. SEC = `state.desig.phase==='red'?100:95` (l.2561). A label: it reads whether a red box exists at the instant, not any security property. At the QA it is 100 because the QA's own red box is standing at l.3932 before the restore; a fresh player reads 95. NOT MEASURED. (Thoth-SYNTH:6, Thoth-A:21)
2. STAB = `crash?max(40,100-crash*20):100` (l.2568), crash = `window 'error'` count (l.1122). One measured counter under a declared scale; 100 is reached by absence (no error yet), never by a test. Register: "crash 0 (window.error count, this session)"; the pillar itself NOT MEASURED.
3. SCALE = `min(100,70+peers*15)` (l.2569). peers is a high-water flag capped at 2 (`Math.max` l.1402/l.1472, path-blind at l.1472 so a relay HELLO after a loss reads "DROPPED · 2p" and SCALE 100), and the QA forces `peers=2` at l.3932. No load, no N. NOT MEASURED. (Thoth-SYNTH:10, Odin-A:18)
4. EFF = `fps>0 ? round(40+min(60,fps/spec().fps*60)) : null` (l.2570); fps is measured once a second in `loop` (l.3685). The ONLY measured pillar — but null at boot (the QA row runs before the first 1 s window), printed as "" (`setT('sE',D.eff)` l.2578; probe sE "" at QA, "77" at +1.3 s), and `fpsStatus:'UNMEASURED'` (l.2571) is printed nowhere. Divisor `spec().fps` is the budget label, not a device reading (same at cilTxt l.2674). Measured on desktop headless Chromium 390×844 only; no phone or Pi number exists (Thoth-SYNTH:6, :31 HARDWARE-GATED).
5. SUC = `max(40,min(100,70+(fires?20:0)+(refuses?10:0)-(holds>8?10:0)))` (l.2562), from `state.decisions` kinds. "Succinctness" (code quality, SSSES.md) has nothing to do with decision counts. At the QA the inputs are the QA's OWN synthetic decisions (11 HOLD · 5 FIRE · 6 REJECT → 90; it read 93 at r.133 and 90 at r.134 because the test suite grew past holds>8, not because the product changed) — `const S=ssses()` at l.3972 runs BEFORE `state.decisions=evSave.decisions` at l.3987. After the restore a fresh player reads 70. NOT MEASURED. (Thoth-SYNTH:6/:17, Thoth-B:9/:27)
6. AVG = mean of 4 parts at QA time (eff null) and 5 parts in play (l.2575) — two scales in one cell; steps 90→82 within ~1 s of boot with no marker (probe sA 84 at publish, 82 at +1.3 s); `metricsLive.ssses.avg` 90 frozen beside `pack().ssses.avg` 86 in one session.json (l.3989 vs l.3394). (Thoth-SYNTH:6-7)

WHAT THE REGISTER SHOULD REPORT TODAY (report the measured number, never a typed one — CLAUDE.md):
7. SEC not measured · STAB not measured (crash counter 0 this session; no failure injected) · SCALE not measured (peers is a 0/1/2 flag; 1v1 only, one pc/dc) · EFF measured fps 60 on headless Chromium 390×844 against budget label 96 → 77; UNMEASURED on any phone or Pi · SUC not measured · AVG: do not report (mixed 4/5-part scale, boot value is the QA's own decisions).
8. The register already says so in prose: REVISIONS.md:82 "SSSES Scalability/Efficiency stay unmeasured until it exists", :243 and :287 "SSSES pillars other than efficiency are labels"; CLAUDE_CODE_NOTES_r135.md:40 owes "SSSES retired until each pillar is measured"; the r.133 fleet review carried the same verdict (docs/assessments/2026-09-19_r133_fleet48_review.md:23, :41). No lens disputes it.
9. Typed numbers still on the record, to be read as historical, not current: CRS_DRONE-2525.md:1017 "SSSES 93" (0.005) and :1018 "lifted to 96" (0.006); drone-2525.v00.00.json:49/:56 the same. 93 is the constant (100+100+70+100)/4 whenever crash=0, peers=0 (Thoth-A:21). drone-2525.v00.00.json:1796 / CRS_DRONE-2525.md:586 STATE the honest contract ("every SSSES pillar computed from evidence produced in the same run") that the served deck does not meet.

WHAT MUST NEVER BE DISPLAYED TO A PLAYER (THE SIGNER NEVER MEETS THE MACHINE; Sofia's word gate bans SSSES|SPIRAL|MoT — Sofia-SYNTH:26, Sofia-A:24):
10. Any SSSES number or word on a reachable surface. Today every one is reachable: the DATA button (l.269) opens `#side` (l.388) for any player and `?diag=1` does not exist (grep 0; Sofia-A:18; REVISIONS.md:243). Surfaces: the SEC/STAB/SCALE/EFF/SUC/AVG rows (l.468-474); the SPIRAL toast "SPIRAL v1 SSSES 90 H …" (l.3470) and its log row (l.3471); the canvas stamp "· SPIRAL v1" (l.1813); the qaOut tail "SSSES 90" (l.4010); the browser-tab title "eXeL ECO-2525 · MoT · SSSES" (l.7); the level list "IX QUALIFY SSSES" (l.619).
11. Specifically never: a blank EFF cell (null printed as ""), an AVG that steps 90→82 with no cause shown, a SCALE that rises on a relay beacon after the link is lost, and any pillar value whose inputs are the QA's own synthetic decisions. If a number is shown to the operator at all, sec/suc/scale carry "DECLARED" and eff carries "UNMEASURED" while null (Thoth-SYNTH:28).

THE SMALLEST CHANGE THAT LETS THE SSSES ROW GO RED:
12. Today `push('SSSES', S&&S.avg>0, …)` (l.3973) cannot fail: the floors give avg ≥ (95+40+70+40)/4 = 61 > 0 (Thoth-A:21). It is a row that asserts a constant is positive.
13. Smallest change (one predicate + one move): compute `S` AFTER the restore at l.3976-3987 and assert measured inputs — `push('SSSES', S.device.fpsStatus==='MEASURED' && S.device.eff!=null && (S.device.peers<=1 || state.com.lastPeerAt>0), JSON.stringify(S))`. At boot fps is not yet measured, so either (a) the row lands in the existing 1.5 s deferred slot beside DRAW_COMPLETES (l.4001 pattern), or (b) `'SSSES'` is added to EXPECTED_RED (tests/deck-qa-manifest.mjs:5) until it is — drone-deck-qa.mjs:37-38 then fails loudly if the exemption goes stale or a red row is unexpected. Gate stays in Deploy as it is (DRN-11.11, CRS_DRONE-2525.md:69). Thoth-SYNTH:28 asks exactly this: "SSSES is absent from EXPECTED_RED only if its inputs are measured".
14. FIX THE CLASS: the row is one member; the class is "every DATA-panel quality number is a label or a stale snapshot" (SSSES/AVG, signed rtt on 4 of 5 surfaces, qaOps with three denominators, SAMPLES = buffer length, six revision strings — Thoth-SYNTH:23). The SSSES predicate above is the first gate of that class, not its close.
```

### MoT 4

```
MoT-4 · THE TWO-PHONE MATCH CLAIM — what "one hash on both phones" proves at r.135, what it does not, the sentence, the order
WHAT IT PROVES (verified on the r.135 bytes, sha 882287ec…, play.html line numbers)
1. The gate is one walk: host TARGET → joiner APPROVE → host FIRE → HIT, then `replayHash()` equal on both AND `state.sync==='MATCH'` on both (`frontend/scripts/drone-team-e2e.mjs:5,41-56,70`). Two isolated Chromium contexts, one machine, real WebRTC, no NAT, no drop, no lapse, no QUAL table, no door tag, no mode change in the walk (Krishna: 2da99b4b==2da99b4b; Odin 1v1: 537c5307; Enlil: ba952bb6).
2. The hash is: committed rows, sorted by orderKey, one line each of eventId|orderKey|sessionSeq|logicalClock|peerId|seq|…|result|data, folded by `fnv1a64` — a 32-bit, 8-hex FNV despite its name (play.html:2038-2049, :4126; Asar). MATCH therefore means: on this walk the joiner holds exactly the host sequencer's committed rows, and its own pending rows came back committed. Peer-agreement only — REVISIONS.md:241-243 already says "by design".
3. Closed by probe at r.135, inside that same peer-agreement: one training LAPSE → one row from the host, 708099fd==708099fd (Christo B3; Asar dc5bd469); a withdrawn approval demotes the shooter's red (Christo); a DROPPED path is not revived by a relay HELLO and "THE OTHER SEAT IS BACK" fires on a DIRECT one (Odin B3, :1401/:1471).
WHAT IT DOES NOT PROVE
4. The range clock. `rangeTick` advances every lane from local `dt` (:761-778); no EXPOSE row; the reducer has no exposure guard (:1963). Plates are not rows, so two phones can hash MATCH while one shows a plate up that the other shows down (REVISIONS.md:240,286; NOTES_r135 owed 1; Odin P4c: the joiner's `R.k` ran to 7 on its own clock; `approveDesig` :2218 still says "per device").
5. Identity columns. `eventId=SID+'-E'+seq` with SID random per load (:568, :2023); logicalClock bumped on EVERY envelope (`commEnv` :1376-1378); orderKey carries peerId. The same play is a different hash every load — MATCH is not replayability, and two `replayHash` projections (deck :2038 vs `lib/drone-2525/decisions.ts:136`) are held to one stream by no gate (Krishna 4).
6. Host-only rows. On the host `sessionPrepareLocal` commits without a broadcast (:1183-1195; `sessionBroadcastCommit` reached only from `commIn`, :1204). `ev('QUAL')` (:818) and the door's `decide('SIM-ACTION')` (:2346) never travel: QUAL TIME-out with both seats on the table → host 2 rows, joiner 1, DIVERGED for good; door tag → host SIM-ACTION+TAG, joiner TAG, score 350 vs 0 (Asar "A MISSED", team3/door probes). MATCH holds only on the paths that happen to go through `netEvent`.
7. The fork on a channel drop. `sequencerUsesHost` needs `dc.readyState==='open'` (:1164-1168); with the channel down the joiner commits and MINTS its own sessionSeq (:1193-1195): Odin P1 on r.135 — `HOLD PEER_LOST committed:true sessionSeq:101 hostId:HOSTX`. SNAPSHOT replaces wholesale (:1517-1519); on "IS BACK" nothing re-syncs (`snapSend` only under `reconnectSeen`, :1476-1479); the next hash is DIVERGED with nothing to repair (Odin B2, Christo A2). The r.134 defect ships again under a working toast.
8. Arrival order. `appendCanonicalEvent` pushes and applies in arrival order (:1990-2016) while the hash sorts (:2038); SNAPSHOT re-applies in array order (:1518). Equal hashes, possibly unequal worlds (Odin; REVISIONS.md:241).
9. The red box through a silent drop. Heartbeat every 4 s, loss declared at 12 s of silence, DIRECT only (:1575); `fireN` (:2296-2312) never reads `lastPeerAt` (Asar: `/lastPeerAt/.test(fireN)` → false). Exposure is 4 s, so on an airplane-mode drop (no close frame) the PEER red stands the whole plate and the shot lands `HIT hiApproved:true` with no approver present (Christo probe135-drop: red stood 3 s until the plate lapsed; Asar: HIT 150 ms after the approver closed, the HOLD arriving 12 s later to no one). A relay HELLO still writes `peers=2` and `sync=MATCH` on any path (:1472/:1476) — "DROPPED · 2p · MATCH" is reachable (Thoth D16).
10. Seats. No lane/unit on any row; both phones report `unit:'T06'` for one lane (Odin team135) — the record cannot say which seat fired.
11. Scale. One `pc`/`dc` (:3265; `ondatachannel` :3291 overwrites; one answer :3327); `peers` a scalar (:1472 `Math.max(2,…)`); liveness one `lastPeerAt`; `commIn` never compares the sender to `evt.peerId` (:1396; Thor 6). 3v3 exists only as the refusal `MULTI-PEER TRANSPORT NOT QUALIFIED` (:2971).
12. Hardware. Every number above is two contexts on one host; the harness fakes `readyState`; the headless close never fired `dc close` (Asar); the edge is UNVERIFIED from this sandbox and remote main was r.134 at review time (Krishna).
THE HONEST SENTENCE (register and operator)
13. "At r.135, MATCH means that after one mark → approve → fire between two browser contexts on one machine, both hold the same committed rows in the same order and hash them to the same 8 hex digits. It does not mean the two phones saw the same range, that the record survives a dropped channel, that every host row reached the other seat, that a red box was live when it was fired, or that the record says which seat fired. Two real phones have not run it; 3v3 is refused, not qualified."
ORDERED PREREQUISITES BEFORE "3v3 NETWORKED" MAY BE CLAIMED (each with its gate; none is optional)
14. Close the fork: a JOIN with its channel down never mints — pending row or refusal; SNAPSHOT merges and resubmits; gate: team-e2e drop-mid-red → HOLD → reconnect → ONE hash on both; `LOST_APPROVER_IS_SAID` re-run as `role:'JOIN'` asserting `committed:false` (Odin 1).
15. Every host row on the wire: `ev()` on the sequencer host broadcasts; one writer for QUAL and SIM-ACTION/TAG; gate: QUAL TIME-out on both seats + one door tag → exactly one QUAL and one SIM-ACTION row on each seat, MATCH (Asar 1).
16. One range clock: the host emits EXPOSE {lane,k,base,sec}; a joiner raises plates only from rows (and `{k,phase,t}` in LAUNCH + SNAPSHOT); reducers refuse a row for a plate that is not the lane's current exposure; gate: same `base` up on both at APPROVE and equal `R.k` after 20 s (Odin 2; NOTES_r135 owed 1).
17. Re-reduce from the sorted log: `applyWorld` as a fold over `events.filter(committed).sort(orderKey)` on every commit and snapshot; gate: two rows delivered out of order → world === hash (Odin 3; NOTES_r135 owed 2).
18. Liveness at the trigger: `fireN` refuses a PEER red when `peers<2` or `lastPeerAt` is older than one exposure (4 s); the heartbeat shorter than an exposure; peers/sync/SCALE follow the path and TAB/LOCAL decay too; gate: mute one seat's `dc.send` without closing it → the other seat's FIRE refused inside the exposure; deck-qa `DROPPED_NEVER_2P` (Christo 2, Thoth 2).
19. A seat on the row: `lane` + `unit` on every row, distinct units on the two seats, `laneClaims` read at launch; gate in team-e2e (Odin 3).
20. Per-peer transport: `peers[]` with per-peer `lastSeen`, one pc/dc per joiner, `commIn` matching sender to `evt.peerId`, member removal; only then lift :2971 (Odin, Thor 6; NOTES_r135 owed 3).
21. Two real phones over WiFi with NAT, a real radio drop and rejoin across one LAPSE, ending MATCH with equal LAPSED counters on both strips, on the SHA Verify Live reports as LIVE (Christo 3; Krishna; hardware-gated in every lens).
22. Only after 14-21: a third phone, proving its answer does not overwrite the second's channel (:3291/:3327) — the first 3v3 claim is that gate's green run, reported as a measured hash, never a typed one.
```

### MoT 5

```
MOT-5 · THE FIRE-GATE INVARIANT AND WHERE r.135 DOES NOT YET HOLD IT
Verified on the served frontend/public/drone-2525/play.html (sha256 882287ec…b97b == REVISIONS r.135 row, BUILD.revision '0.135' at :568). Line numbers are the served file.

THE INVARIANT (operator's terms, one sentence):
A shot leaves this seat only while the round is open, at a target still standing on THIS device, out of a red box that a named human present in the room put there now, pulled by the seat that owns the mark — the marker's own hand or that seat's own AI — once per exposure of that target, and every mark, approval, refusal, release and shot is a decision row on the record and nothing else moves the box or the score.

WHERE THE SERVED r.135 DOES NOT ENFORCE IT (ordered by cost to the player):
1. THE r.135 REGRESSION — the marker's seat forgets its own AI (Thor S, Asar S, Christo S). fireN :2301 refuses when `desig.how` matches /^PEER/ and `desig.by!==SID` in LIVE. The AI marks as `'ASM@'+SID` (:2461); the other seat's approval sets how='PEER HI OVER AI' (:2199-2200); so on the host by='ASM@host'≠SID → REJECT NOT_THE_MARKERS_SEAT, and on the joiner (how 'PEER', by=host) the same. asmTick's own trigger (:2464-2465) goes through fireN and dies on :2301. An AsM SPOT mark in any LIVE room is a red box no seat can pull. No QA row covers it — the in-file row set has APPROVER_SEAT_NEVER_FIRES (human only) and no AI_MARK_FIRES_IN_A_ROOM (grep of the row names). Same class, other face (Christo S): approveDesig :2238 tells the APPROVER "… NOW PRESS FIRE", the button :2301 then refuses.
2. "PRESENT NOW" is not read at the trigger (Christo S A2, Asar S). fireN :2296-2303 never reads `state.com.peers` or `lastPeerAt`; a silent drop (no close frame) leaves a PEER red standing until the 12 s watchdog (:1576) while an exposure is 4 s — a HIT landed 150 ms after the approver's context was closed (Asar team probe). linkLost :1571 also returns before saying anything on a TAB-mode room.
3. "NAMED HUMAN": the local approver is the label 'HI-2' (:2219 `who||'HI-2'`), so approvedBy on state, row and wire (:2236-2237) is a role string, not a seat; only the wire path (:1496 `m.by||pay.by||m.peerId`) names a seat, and it checks membership only when `live` (:2197-2198).
4. THE PARTNER'S ORDINARY RELEASE NEVER REACHES THE SHOOTER (Thor S 1, probed S4b). The reducer :1964 demotes red only on HOLD APPROVER_LOST|PEER_LOST; the approver's HOLD AUTHORITY_RELEASED (scene change :4290, lane change :4466) lands and does nothing — the shooter stays red and fires a HIT.
5. "LIVE TARGET ON THIS DEVICE" is judged on the marker's device only (Thor S 4). peerDesig :2179-2190 and wireApprove :2192-2205 read neither `up` nor `lifePct`; approveDesig :2218 exempts how==='PEER'; the local-row reducer :1982 builds a placeholder ref `{id,x:0,y:1,z:0}` with no `up`, invisible to the :2302 gate. The range clock is per device (REVISIONS r.135 "still open").
6. A UAV that expires while red releases nothing (:2533 `d.up=false`, no releaseAuthority — the pop at :2514 has it); the box stays red until FIRE and :2302 is what saves it.
7. A RED BOX BUILT IN WAITING CROSSES INTO LIVE (Thor S 1, probed G4a): wireApprove's member check is under `if(live…)` (:2197-2198); lobbyEnterLive :2930-2938 does not call releaseAuthority; roundOpen :2295/:2297 refuses the shot in WAITING but the red survives START.
8. "ONCE PER EXPOSURE" holds only in QUAL·40. At CH0-4 a miss keeps the box red (:2326 `else state.desig.phase='red'`), fireN checks `phase` only (:2303) and `hiApproved` only at CH5 (:2304) → one approval, N rounds; the SPENT guard :2268 is qual40-only and writes no row (Asar S). The AI's once-per-box (`asmFired` :2464) does hold.
9. THINGS OUTSIDE A DECISION ROW still move the box or the score (Asar S, Thor S 2/3): rangeRelease :749 drops a red/amber box with no row when a plate falls (:776) or lapses (:778) — on a joiner the lapse is not even a row (lapseIsMine :755 suppresses netEvent); the door commit :2346 is `decide('SIM-ACTION')`, which the wire never carries (:2170 sends HOLD/REJECT/RESET only) → joiner holds TAG only, score 350 vs 0, DIVERGED; TARGET DOWN is a MISS verb (:2267) and SPENT is no row (:2268); mark()'s watch branch :2342 sets `pending` BEFORE the red-box check :2343, and btnYes :3496 / Enter :3538 call `commit()` ungated → TAGGED +250 and a TAG row with no red box ever (Thor probe G5d-watch); the CH5 corpse `if(false){…}` :2306-2313 still stands.
10. commIn :1396 never compares `evt.peerId` with the envelope's sender (Thor S 6) — harmless for two seats in LIVE, open for 3+ seats and for WAITING.
11. The record's actor is split on the AI's shot: HIT/DESIGNATED say role:AI by:ASM@ (:2167, :2464 `_shooter`), the same shot's SIM-ACTION at :2322 is role:HI (Asar S) — "the marker's seat (its own AI)" is not one name across the rows.

GATES OWED FOR THIS SENTENCE (none exist in the served file): AI_MARK_FIRES_IN_A_ROOM (LIVE, by 'ASM@'+SID, how 'PEER HI OVER AI', approvedBy a member ⇒ HIT row by ASM@, no REJECT; joiner FIRE ⇒ REJECT); PEER_RELEASE_TAKES_MY_BOX; NO_RED_INTO_LIVE; WATCH_TAG_NEEDS_RED_BOX + `commit(` has one caller; a FIRE refused with a row when lastPeerAt is older than one exposure; the approver's approve toast never contains FIRE.
```

### MoT 6

```
MOT-6 · THE STRANGER'S 60 SECONDS AT r.135 (phone 390×844, cold load → first HIT). Sources: Athena-SYNTH (probe athena-s-probe.mjs, QA 114/114, 0 errors), Sofia-SYNTH (sofia-s-135.log, toast() hooked), play.html r.135 lines; bar = "a stranger understanding in 60 s" (docs/asks/2026-09-16_demo_scale_fleet.md §Demo; r132 test D9 :83).
Tap count measured from source: CONTINUE ×11 (CIN.length 11, :4347-4358) + NEXT + NEXT + PRACTICE + TARGET + APPROVE + FIRE = 17 taps; SKIP TO SELECT route = 7. Athena S: "he still stalls twice before the first shot" (intro, SELECT). Wall-clock 60 s itself is HARDWARE-GATED (headless taps in 150 ms; Athena A/S).

ORDERED PATH — every screen and sentence, marked
1. sc0 "DRONE-2525" / "STANDALONE HTML · CIVIC REHEARSAL" / "A wireframe point. Then the Capitol." / CONTINUE / SKIP TO SELECT / "PLAY → RECORD → REPLAY → COMPARE → QUALIFY → IMPROVE → SHARE" (:163-169) — STALLS: "STANDALONE HTML" is a machine word on the first screen; two exits, nothing says which one plays now; no line says "no real weapons" (Sofia §4/§6 class D9).
2. CIN 1 "Today we protect the Capital of Texas." HELPS · 2 "Not automation alone. People. Rehearsal. Evidence." NEUTRAL · 3 "Look. TARGET paints amber. Seeing is not deciding." HELPS (names the pill) (:4348-4350).
3. CIN 4 "Second HI turns amber red. AI may spot. HI authorizes." (:4351) — LIES on the solo path: the stranger alone presses APPROVE himself; "HI", "AI may spot" name a person and a rule not on his screen (Athena S §3 OPEN; Sofia §1).
4. CIN 5 "FIRE in the sim. Center 100 to 0. Off-center minus 33." — HELPS (FIRE) then STALLS (scoring math); CIN 6 "…That is R-CORE." STALLS; CIN 7 "CH0 range. CH5 same gate under time. Principle does not bend." STALLS (:4352-4354; Athena S §6 "jargon in the first 60 s", UNTOUCHED since r.133).
5. CIN 8 "Turret sensor. Quad hover. VTOL picket. FOIL wing." NEUTRAL (sets up a choice PRACTICE will not honour, see 8) · 9 CREW · 10 CITIES NEUTRAL · 11 "Humanity authorizes. BEGIN REHEARSAL." HELPS (:4355-4358). All 11 on 32 px buttons (:136; probe introBtnH [32,32,32]) — STALLS under a finger.
6. sc1 SELECT CITY: CAPITAL · LIVE (on) / AUSTIN · NEXT / ATLANTIS · ACCORDS / "Only CAPITAL plays in 1.1. Others qualify later." / NEXT (:172-179) — STALLS: a screen with one legal answer; AUSTIN → "RESERVED · CAPITAL ONLY IN 1.1" (:4323) drawn at y≈724 for a button at y≈120 (toast bottom:92px :77; Athena B "520 px from the finger").
7. sc2 SELECT CRAFT: TURRET · SENSOR / QUAD · HOVER (on) / VTOL FOIL / NEXT (:181-189) — LIES: any pick → goRange forces a turret (:4472) while the bar keeps "▢ QUAD"/"▶ VTOL FOIL" (`#plat`; Athena S §4; Sofia bars).
8. sc3 SELECT RANGE: CH0 TRAIN (on) / CH1 LAWN / CH3 MIX / CH5 NET · FOILS; ENTER WAITING ROOM (first, y 168); PRACTICE · NO ROOM · CH0 ONLY (second, y 206); "This glass is 1.1. EXPORT carries 5.5 for the engines." (:191-201) — STALLS: the primary button is the room; PRACTICE reads as a lesser mode; fine print is machine words. LIES: CH1/3/5 pickable, then PRACTICE answers "PRACTICE ALONE IS CH0 ONLY · PICK CH0 TRAIN, OR ENTER THE WAITING ROOM FOR CH5" (:2949) at y 724, off the eye (Athena S §4 UNTOUCHED, probe solohint []).
9. (branch, first button) sc4 "WAITING ROOM · R-CORE" / "FORM TEAM → AUTHENTICATE → READY → START" / ROLE LOCAL / PATH OFFLINE / ROSTER 1 / 2 / HOST ROOM / READY / "…Light-4 permutation seed. They are not the AES-GCM traffic key." / "DIRECT LINK · MANUAL SIGNALING" / "Current HTML transport is one DIRECT WebRTC peer…" (:204-247) — STALLS (dead end alone: 1412 px page, START/BACK at y 1240 below the fold, READY → "READY HOLD · WAIT DIRECT + TEAM AUTH", no "alone → PRACTICE" line; Athena A/B) and breaks THE SIGNER NEVER MEETS THE MACHINE (AES-GCM, WebRTC, signaling).
10. PRACTICE tap → one tick emits "RANGE · L21 DOWN · TRAINING · RESET" (:4492) then "CH0 PRACTICE · NO ROOM" (:2957); single-slot toast() (:2075) shows only the last — STALLS: names no next tap, "NO ROOM" reads as a fault (Athena S §5 UNTOUCHED; Sofia probe practiceCH0).
11. Range at 390: bar 1 "eXeL | r0.135 | REC | CONTROLS | VOICE | MAP | DATA | MORE"; bar 2 "SCEN · RANGE 50-300 M | ▢ QUAD | LADY BIRD LAKE | MoT 1.1 | L21 DOWN | TRAINING · RESET | RESET | CH0 TRAIN | DIFF 3 | HAL AUTO" (Sofia bars; Athena A "145 px of settings above the picture that no range shot needs") — STALLS; "▢ QUAD" LIES (seat is a turret).
12. Strip "CH0 D3 | TARGET FIRST | TRAINING · RESET · HIT 0 · MISS 0 · LAPSED 0" (:353, :2654, :2666, :2258) — "TARGET FIRST" HELPS; "D3" STALLS (new machine token at r.135, Sofia §2); "RESET" reads as a command (Athena B). Pills TARGET · APPROVE · FIRE at y 656 (:366-368) HELPS.
13. While he reads 11-12 the first 3 s plate lapses: "LAPSED · C-50 · NEXT TARGET IN 1.5 S" (:758, plateBase not plateWord) and LAPSED 1 on the strip — STALLS (a plate id in the first sentence he did not cause; Sofia §2/§3; Athena A "idle 14 s → LAPSED 2").
14. TARGET (pill / key 1 / plate tap) → "AMBER · 50 M CENTRE · NOW PRESS APPROVE" (:1875, one sentence on all three paths, probe tgtPill/tgtKey1/canvasTap) HELPS; in the gap → "NO TARGET UP · NEXT IN 1.2 S" (:1876) HELPS. Strip "AMBER T1 C-50-L21" (:2664) STALLS — two vocabularies for one fact (Sofia §3).
15. APPROVE → "RED BOX · 50 M CENTRE · APPROVED BY YOU · NOW PRESS FIRE" (:2238) HELPS; strip "RED T1 C-50-L21" STALLS. APPROVE with nothing → "NO AMBER · MARK A TARGET FIRST" (:2217) HELPS.
16. FIRE → "HIT · IN THE AIMING CIRCLE" (:2290, bandWord :2262) HELPS; strip "LAST T1 C-50-L21 DOWN · TRAINING · RESET · HIT 1 · MISS 0 · LAPSED 1" HELPS (score) / STALLS (plate id). Sentence 12 px at y 738, 250 px below the pip at y 487 (Athena S §5) — readability HARDWARE-GATED.
17. Branches on the way: FIRE on amber → "AMBER · PRESS APPROVE FIRST" HELPS; FIRE after his red lapsed → "YOUR TARGET LAPSED · MARK THE NEXT" HELPS here but LIES when he marked nothing or just hit (:2303 keys on any lapse in 6 s; Sofia §5 probe fireNothing/secondPull).

MINIMAL CHANGES THAT MAKE THE PATH STALL-FREE (words + screens, with replacement text)
A. sc0 (:163-169): "STANDALONE HTML · CIVIC REHEARSAL" → "CIVIC REHEARSAL · LASER TAG · NO REAL WEAPONS"; SKIP TO SELECT → "PLAY NOW · ALONE" calling practiceNoRoom() with CH0 (cold load → 1 tap → range = 4 taps to first HIT); CONTINUE stays for the story. (Fixes 1, and 6-9 for the stranger who plays now.)
B. CIN (:4351-4354): slide 4 → "TARGET paints amber. APPROVE turns it red — you, alone, in PRACTICE; the other seat in a room. Then FIRE." (Athena S owed 3); slide 5 → "FIRE in the sim. Centre scores 100. Off-centre scores less."; slide 6 → "Replay who saw, who marked, who approved. Every shot is on the record."; slide 7 → "CH0 is the range. CH5 is the same rule under time."; `#intro button` min-height 44px (:136).
C. sc1-sc3 honour the solo path (:172-201): AUSTIN/ATLANTIS `disabled` + "· LATER", fine → "Only CAPITAL plays today."; sc2 fine "PRACTICE seats a turret. Craft plays in a room." and `#plat` follows what goRange seats (:4472); sc3 PRACTICE first and primary, labelled "PRACTICE · ALONE · CH0 TRAIN", ENTER WAITING ROOM second, labelled "PLAY WITH OTHERS · WAITING ROOM", CH1/3/5 `disabled` + "· ROOM ONLY" while PRACTICE is the exit; delete "This glass is 1.1. EXPORT carries 5.5 for the engines." (Athena S owed 1) — the :2949/:4323 refusals become unreachable, so the far-from-finger toast (Athena S owed 2) needs no move.
D. sc4 (:204-247): first line under h1 "ALONE? GO BACK AND PRESS PRACTICE." with BACK beside it above the fold; both roomnotes (AES-GCM, WebRTC, signaling) and "MANUAL SIGNALING" behind `?diag=1` (Sofia owed 3; SIGNER NEVER MEETS THE MACHINE).
E. PRACTICE landing (:4492 + :2957): one sentence, "PRACTICE · A TARGET POPS FOR 3 S · PRESS TARGET, APPROVE, FIRE"; a one-tick queue in toast() (:2075) so no sentence is lost (Athena S residual §5).
F. Phone bar 2 (Sofia bars): collapse to "L21 · TRAINING" with SCEN/water/MoT/DIFF/HAL/`#plat` under MORE; strip (:2654) "CH0 D3" → "CH0"; RANGE_MODE_NAME bounce (:741, :323) "TRAINING · RESET" → "TRAINING · TARGETS RETURN".
G. Route strip :2663-2665, board, lapse :758/:760 through plateWord → "AMBER · 50 M CENTRE", "RED · 50 M CENTRE", "LAST · 50 M CENTRE · DOWN", "LAPSED · 50 M · NEXT TARGET IN 1.5 S"; :2303 says "YOUR TARGET LAPSED · MARK THE NEXT" only when state.lastLapse.id === his own desig id, else "NO RED BOX · MARK A TARGET FIRST" (Sofia owed 2).
GATES (FIX THE CLASS): `frontend/tests/drone-words.test.mjs` in test:ci banning HI-2|PEER|SOLO|NET|cnn|WEBRTC|WEBCRYPTO|IDB|WORKER|skew|R-CORE|AES|HTML|C-\d{2,3}[LRC]?(-L\d\d)?|D\d on every player string outside ?diag=1 (Sofia owed 1); QA rows: no enabled intro button leads to a refusal, PRACTICE from any highlighted craft/CH lands on the seat the bar shows, no CIN line contains "Second HI", `#intro button` ≥ 44 px, TOAST_ABOVE_INTRO by elementFromPoint (:3944), LAPSE_IS_YOURS_ONLY_IF_YOU_MARKED_IT (Athena S owed 1-3).
```

### MoT 7

```
MOT-7 · WHAT ONLY HARDWARE CAN PROVE + THE OPERATOR'S TWO-PHONE SCRIPT · r.135 (sha 882287ec…; line numbers = frontend/public/drone-2525/play.html r.135; lenses = fleet48/*-SYNTH.md, Enki from Enki-A:30-35 / Enki-B:34 — no Enki synthesis exists)
A. HARDWARE-GATED, DE-DUPLICATED (15 classes from 12 lenses; every headless proof of these is two contexts in ONE Chromium, no NAT, faked readyState — Odin, Enlil)
H1 A real radio drop: does airplane mode / screen lock reach `dc close` (linkLost ≤300 ms, :1277/:1293) or only the 12 s silence watchdog (:1575, 4 s poll)? During that window the shooter's PEER red stands against a 3–8 s exposure (:737) and a HIT lands with no approver — Christo, Asar, Thor, Odin; Thoth adds sleep vs lock vs backgrounded Safari.
H2 The return: "THE OTHER SEAT IS BACK" (:1471) on a real rejoin; whether WebRTC survives a 15 s gap with no ICE restart; missed rows are never re-sent (snapSend only under reconnectSeen :1479); the joiner mints a sessionSeq while down (Odin P1) → DIVERGED forever — Christo, Odin, Asar.
H3 "DROPPED · 2p": whether a real second phone's sibling tab / TAB beacon re-inflates peers after a drop (:1472 is path-blind) — Aset, Thoth.
H4 Two range clocks: real clock skew + fps difference between phones — how far `rangeTick` (:761-778) drifts; whether the host's LAPSE row (:755-758) reaches the joiner before the joiner's own exposure ends (:1963); sessionSeq/logicalClock after a SNAPSHOT reconnect — Odin, Aset, Asar.
H5 A real round trip on one clock: comTxt "-1981ms" at frame rate (:2677) vs "ms skew" (:4392/:4394) on a real pair — Thoth.
H6 The unaided join with real hands over a LAN with no internet: six digits read off the host's card (:3049) do not select the team (:3068); the mismatch refusal runs before any dc exists (:3341); the 1.5 s host-candidate window (:3310) vs STUN (:3288) unreachable; two phones over STUN/TURN ending on one hash — Christo, Krishna.
H7 The AI seat in a room: both phones SPOT on (which mark takes the single desig slot); the joiner's approve reaching the host before the AI's next tick (~50 ms); an AI mark in a LIVE room is unfireable by either seat (:2301, Thor R.135 BROKE 1) — Pangu, Thor.
H8 A real microphone: iOS/Android permission prompt, not-allowed, no-speech cadence, "don't approve", "the fire alarm"; LISTENING→VOICE OFF as two toasts or one flash — Aset, Enlil, Enki, Sofia, Thor.
H9 Real frame rate / Pi: the one-frame phDes lag after FIRE/RESET at 30 fps; hudTail CIL/HIL and bloom at device fps; HAL PI budgetMs never throttles; the frame-dt QUAL clock vs a stopwatch (1.5× slow?); asmFired across a dropped frame — Aset, Enki, Enlil, Thoth, Pangu.
H10 Real glass: thumb on the R-HEAD stick brushing APPROVE (320 px: reticle y379, pill top y380); TARGET→APPROVE→FIRE inside a 3 s exposure at 50 m; a 12 px sentence at y738 read with the eye on the pip at y487; the intro refusal at y724 for a button at y206; 32 px intro buttons; safe-area-inset-bottom ≈34 px; Safari 100dvh at the pill row; double-tap-to-fire; a 6×6 px 300 m box; the 320 px strip; canvas/MAP legend at real DPR — Athena, Enki, Enlil, Sofia.
H11 Screen lock / incoming call / app-switch zeroing rAF on iOS/Android (the free QUAL pause; whether the data channel survives it) — Enki.
H12 SAVE = four downloads on iOS/Android (how many the phone keeps); the 4000→3000 sample trim against phone storage; `fixture:'ASM_CUP_99'` on a live-play sidecar noticed as false — Asar, Thoth.
H13 A human reading: "THE OTHER SEAT IS GONE" and knowing the next tap; "NOW PRESS FIRE" on the approver's phone (:2238) sending a finger into the refusal at :2301; "LAPSED 19" beside zero LAPSE rows; "MISS DOWN" that was a refusal; "YOUR TARGET LAPSED" after one's own hit (:2303) — Asar, Christo, Pangu, Sofia.
H14 LIVE at the edge: whether Cloudflare serves 882287ec… (sandbox curl 403/000; only the runner's Verify Live can compute it) — Enlil, Krishna, Odin. H15 A THIRD phone's answer overwriting the second's channel (:3291/:3327) — Odin; out of tomorrow's two-phone scope.
B. THE SCRIPT (two real phones, one WiFi; A = HOST/BLU, B = JOIN/RED; CH0 left highlighted so the strip shows HIT/MISS/LAPSED, :2257; mode left at TRAINING · RESET, :644/:741; stopwatch in hand; write every number you read, never a typed one)
S0 Both phones open https://exel-ai-polling.explore-096.workers.dev/drone-2525/play.html, DATA panel: the revision must read 0.135 (H14). r.134 or older = STALE — stop, the day proves nothing about r.135. (Six spellings of the revision on one panel are KNOWN — Aset 5, Thoth; note, do not stop.)
S1 A: ENTER WAITING ROOM (:199) → HOST ROOM (:224) → CREATE HOST OFFER (:240). Must say: MY TEAM 6 = six digits; OTHER TEAM 6 = "NNNNNN · GIVE THESE 6 DIGITS TO THE OTHER SEAT" (:3049); AUTH UNPAIRED; LC status "CODE READY · SHARE OUT OF BAND" (:3142); the launch button "START · WAIT DIRECT + AUTH" (:3054/:2972); a blob in the DIRECT LINK box. DEFECT: OTHER TEAM 6 reads PRIVATE on the host; no blob.
S2 Carry the blob A→B out of band (AirDrop / paste — the deck has no QR, :4245 is clipboard only; H6). Record the seconds it takes a stranger's hands.
S3 B: ENTER WAITING ROOM → TAP RED TEAM (the digits do not pick the team, :3068 — skipping the tap yields TEAM DENIED on A and B hangs) → type A's OTHER TEAM 6 into "ENTER YOUR TEAM'S 6 DIGITS" (:233) → paste the blob → JOIN OFFER / CREATE ANSWER (:241). Must say: "ANSWER READY · HOST MUST VERIFY CODE" (:3321), toast "WIFI ANSWER · ROOM <room>". DEFECT: "ENTER TEAM 6" with six digits typed; "JOIN FAIL"/"JOIN CRYPTO FAIL"; the answer never appearing (the 1.5 s window :3310 on a LAN with no STUN, H6 — record the seconds).
S4 Carry the answer B→A; A: HOST APPLY ANSWER (:242). Must say A: "PAIR AUTHORIZED · AES-GCM READY" (:3362), toast "ANSWER APPLIED · CODE AUTH", AUTH AUTHORIZED, PATH "DIRECT · 2p · Nms skew" (:4392); B: "PAIR AUTHORIZED · LC…" (:1466), AUTH AUTHORIZED, DIRECT · 2p. DEFECT: PATH OFFLINE/TAB on either phone after 10 s; "TEAM CODE MISMATCH" on A with B stuck on "ANSWER READY · HOST MUST VERIFY CODE" forever (KNOWN — Christo A1c: the refusal cannot travel before the dc; record it, restart from S1); a signed "-NNNNms" on the COM stat (KNOWN, Thoth H5 — record the number).
S5 READY on both (:225). B before S4 would read "READY HOLD · WAIT DIRECT + TEAM AUTH" (:2980) — after S4 that sentence is a DEFECT. A's button must move "START · WAIT BOTH READY" → "START REHEARSAL" (:3054).
S6 A: START REHEARSAL. Both: toast "ROOM LIVE · 1v1 · BLU" / "… · RED" (:2944), intro gone, strip "TRAINING · RESET · HIT 0 · MISS 0 · LAPSED 0" (:2257), phDes "TARGET FIRST" (:2666), sync MATCH. DEFECT: LAPSED > 0 at START (RANGE_IDLE_BEFORE_START); the two units carrying one id ("T06" on both) is KNOWN (Odin) — record, do not stop.
S7 MARK: A presses TARGET on a standing plate. A must say "AMBER · 100 M CENTRE · WAIT FOR THE OTHER SEAT TO APPROVE" (:1875), strip "AMBER T1 <plate>" (the plate id on the strip is KNOWN, Sofia 2). B within 2 s: strip "AMBER T1 <plate> · MARKED BY THE OTHER SEAT" (:2664, seatWord :2260), head turned to it; toast "NET AMBER T1 <id>" is KNOWN machine words (:2190). Check: A pressing APPROVE on its own mark → "TWO HUMANS · A SECOND PERSON MUST APPROVE · THE OTHER SEAT PRESSES APPROVE" (:2227), no red. DEFECT: B shows no amber in 2 s; B still "TARGET FIRST"; A goes red alone.
S8 APPROVE: B presses APPROVE. B must say "RED BOX · 100 M CENTRE · APPROVED BY THE OTHER SEAT · NOW PRESS FIRE" (:2238 — the "NOW PRESS FIRE" is a KNOWN wrong sentence, Christo/Pangu; H13); B then presses FIRE once: must be refused "THE SEAT THAT MARKED IT FIRES · YOU APPROVED" (:2301), no HIT anywhere. A within 2 s: toast "RED · PEER HI-2 · <id>" (:2205, KNOWN words), strip "RED T1 <plate>" red (:2663). DEFECT: A amber after 2 s; B's FIRE scores; A's FIRE says "AMBER · THE OTHER SEAT MUST APPROVE" (:2303) after B approved.
S9 FIRE: A presses FIRE inside the exposure (3 s at 50 m … 8 s at 300 m, :737). A: "HIT · IN THE AIMING CIRCLE" or "MISS · …" (:2289), strip "TRAINING · RESET · HIT 1 · MISS 0 · LAPSED 0" on the same tick, phDes "LAST T1 <plate> DOWN" (one frame late is KNOWN, Aset 1/H9). B within 2 s: "THE OTHER SEAT HIT · 100 M CENTRE" (:1959), strip HIT 1, its red box gone, MATCH; both DATA panels show the same hash — write both. DEFECT: B HIT 0; B's red box standing on the downed plate; DIVERGED; the two hashes differ.
S10 LAPSE: mark nothing; let one exposure end. A (the room's lapse clock, :755): "LAPSED · 100 M · NEXT TARGET IN 1.5 S" (:758), strip LAPSED 1. B: LAPSED 1 from A's row, no second row (:1963), MATCH; write whether B's plate was still standing when its LAPSED ticked (H4). Then A presses FIRE within 6 s with nothing marked → "YOUR TARGET LAPSED · MARK THE NEXT" (:2303; an over-claim when A marked nothing is KNOWN, Sofia 5). DEFECT: B LAPSED 0 or 2; DIVERGED after the lapse; a second "LAPSED · …" toast on B; A LAPSED ≠ B LAPSED.
S11 AIRPLANE MODE (H1, the shot no harness reaches): A marks, B approves (A red, PEER). B switches airplane mode ON at once; stopwatch starts; A presses FIRE at 2 s and again at 6 s. A must say, at T_drop: "THE OTHER SEAT IS GONE · YOU ARE ALONE · YOUR RED BOX IS AMBER AGAIN" (:1574), strip amber, PATH "DROPPED · 1p · …" (:4392), sync ALONE; every FIRE after T_drop → "AMBER · THE OTHER SEAT MUST APPROVE" (:2303). Write T_drop: ≤1 s means dc close reached (:1277); 12–16 s means only the watchdog caught it (:1575). Any FIRE that lands "HIT" before T_drop is the member of the class that only this test can show — write the second it landed and the plate. B (its own dc closed): "THE OTHER SEAT IS GONE · YOU ARE ALONE" and DROPPED · 1p, its box amber. DEFECT: A red > 16 s; A's strip "DIRECT · 2p · MATCH" at 20 s (the r.134 48 s bug); "DROPPED · 2p" on either phone at any time (H3); B showing no sentence at all.
S12 RETURN (H2): B airplane OFF, screen on, stopwatch restarts. A must say "THE OTHER SEAT IS BACK" (:1471) and PATH DIRECT · 2p within T_back — ONLY if the channel survived; if it never comes in 30 s the channel died and r.135 has no rejoin (KNOWN, Christo A2) — B re-joins from S2, write T_back or "NO REJOIN". Then S7–S9 once more: HIT 2 on both and MATCH. DEFECT: A says IS BACK while B's PATH stays DROPPED (or the reverse); sync DIVERGED after the return that never returns to MATCH (Odin's fork: B minted a HOLD row while down — write both hashes); "IS BACK" with A and B LAPSED counters unequal (no re-sync, snapSend :1479); A's strip DIRECT · 2p with no sentence.
S13 SAVE on both phones (H12): write how many of the four files each phone kept; the sidecar's `fixture:'ASM_CUP_99'` on live play is KNOWN false (Asar). VOICE on one phone (H8): the permission prompt, then "don't approve" on an amber box — no red; "LISTENING" vs "VOICE OFF" as one flash or two toasts — write which.
S14 What tomorrow's sheet must carry, measured: revision string at S0; seconds for S2/S3 (H6); T_drop and the plate/second of any HIT landed before it (H1); T_back or NO REJOIN (H2); the two hashes at S9, S10 and after S12 (H2/H4); HIT/MISS/LAPSED on both strips at S9, S10, S12; any "DROPPED · 2p" (H3); any "-NNNNms" (H5); how many SAVE files (H12). A result outside the "must say" of its step is a defect of r.135; the KNOWN items above are already on the record (REVISIONS.md r.135 "Still open, honest") and are recorded, not re-reported.
```

### MoT 8

```
# MOT-8 · The record's authorship law (served r.135, read-only; play.html = frontend/public/drone-2525/play.html)

THE LAW: every row carries the actor who caused it and the seat that owns it; the AI is an actor, never a passport. `actor` is who caused the row (`<sid>`, `ASM@<sid>`, or `CLOCK` / `LINK` / `HOST` when no one pressed); `seat` is the room member that owns the fact (the marker's seat for a box, the shooter's seat for a shot, the lane's seat for a table); a reducer judges by the envelope's sender and the member list, never by a row's `by` (Thor S6, gate `AI_PASSPORT_NEVER_FROM_A_ROW`, REVISIONS.md:258-259). `role` is derived from `actor` and from nothing else.

WHAT r.135 WRITES ON EVERY ROW (`ev()` play.html:2020-2030), and why it is not yet the law:
1. `peerId:SID` is the WRITING DEVICE (2026) — never a person, never a seat: no `team`, `lane` or `unit` key on any row (Odin S11 "an actor, not a seat"; both phones report `unit:'T06'`).
2. `role` is `'AI'` iff `data.by` starts `ASM@`, else `state.pilot||'HI'` (2024) — and `state.pilot` is a MODE flag: `'HI'` in every mode (2433-2438), `'AI'` in `watch` (2439: a human's rows say AI), `'HOLD'` after RELINQUISH (4269). A clock-caused LAPSE/QUAL and a link-caused HOLD say `role:HI`.
3. `decide()` stamps `actor:state._shooter||SID` on the DECISION (2167) but passes only `extra` into the row (2170) — so the decision names the AI where the hashed row says HI (Pangu A2; Asar S8 "one shot, two authors"). `_shooter` is set only around `fireN` (2464).
4. The host commits a submitted row as sent (`sessionCommitEvent(pay.event)` 1422-1426) and `commIn` never compares `evt.peerId` to the envelope's sender (1396 rejects only `===SID` or empty) — open for 3+ seats and WAITING (Thor S6).

PER VERB — what the row says, who it is true for (H = human, AI = ASM@, O = the other seat), and the exact field set r.136 must write (all rows: `actor`, `seat`, `role` derived, plus):
5. DESIGNATED (1875) `data:{slot, by:src||SID}`. H: `by`=device, true as a device. AI: `by:'ASM@'+SID` (2460), `role:AI` — true (Pangu S1). O: reducer 1977-1979 books it as the PEER's mark, `viaAI` a strip fact only — true as a rule, but the joiner's record no longer says the AI marked (Asar S8 room PARTIAL). r.136: `{actor, seat:markerSeat, slot, viaAI}`.
6. APPROVE (2236 local; 2204 wire) `data:{hiApproved, designatedBy, approvedBy, samePerson, sameDevice, transport, how}`. H solo: `approvedBy:'HI-2'` is a label, not a seat. AI mark approved on its own device: `samePerson:true, sameDevice:true` is FALSE — two actors (Pangu A2). O: `approvedBy:<peer sid>, how:'PEER HI OVER AI'` — true (Odin B5); in a room the joiner's row names the AI nowhere (Asar S8). AI never approves (no path) — true. r.136: `{actor:approverSid, seat:approverSeat, designatedBy, markerSeat, how, samePerson:actor===designatedBy, sameDevice:seat===markerSeat}`; `'HI-2'` retired.
7. HIT / MISS (2276 → `netEvent` 2033) `data:{by:_shooter}` ONLY when the AI fires, else `data:null`. AI: `role:AI by:ASM@` — true. H: the shooter is only the device; the peer reducer reads `row.data.by` → undefined, `lastShot.by` empty (1959, 1962; Aset 3). r.136: `{actor, seat, band, slot, approvedBy}` on every shot.
8. LAPSE (758 training, 759 QUAL) `netEvent`, no data — nobody named, `role:HI` though the CLOCK acted; in a room only the host's clock writes it (755) and `peerId:host` reads as the host acting on the joiner's plate. r.136: `{actor:'CLOCK', seat:laneSeat, lane, exposure}`.
9. SIM-ACTION (2329 via `decide`) `data:{pts, direct}` — no `by`: the AI's kill says `role:HI` (Pangu A2; 2329 passes no `by`). TAG (2346) = SIM-ACTION `{tag,pts}` + `netEvent('TAG')` — the approving human is named in `log()` only. r.136: `decide` passes `by:_shooter`; `{actor, seat, pts, direct, tag}`.
10. SCORE (2329) result `'BLU n · RED n'`, `by:ASM@` when the AI (Asar: `SCORE role:AI`) — a tally has no actor; it borrows the shot's. r.136: `{cause:<HIT eventId>, blu, red}`, `role:'SYSTEM'`.
11. HOLD (`decide` at 2196/2200/2225/1573/2305/4425) `data` differs per site: `wireApprove` `by` is the CLAIMED approver from the envelope (unverified, item 4); `linkLost` (1573) and `releaseAuthority` (4425 `{reason:'AUTHORITY_RELEASED', why}`) have no actor — the LINK, scene or reset acted, `role:HI`. r.136: `{actor:sid|'ASM@sid'|'LINK'|'CLOCK'|'PEER:<sid>', seat, reason, designatedBy, approvedBy, cause}`.
12. REJECT (1396, 2182, 2297, 2300-2304, 2343) `data:{reason, by?/from?/slot?}` — the refused actor is the device; an AI refusal (WAITING room, QUAL·40 at 2463 is silent, no row) carries `actor` in the decision only. `ROW_AUTHOR_MISMATCH` (1396) `from` = envelope sender — the only row that names the sender. r.136: `{actor, seat, reason, from:envelopeSender}`.
13. RESET (4430) `{why, mode, lane, qualR, qualH}` — no `by`; human only, the AI never resets — true; peer reducer 1965 applies it to all. r.136: add `{actor, seat}`.
14. QUAL (818) `ev('QUAL','T'+table, reason+' h/r')`, no data — `id` is the TABLE; the CLOCK (TIME) or the 40th round acted; no lane, no shooter seat, `role:HI`. r.136: `{actor:'CLOCK'|shooterSid, seat, lane, table, reason, hits, rounds}`.
15. LOBBY_STATE (2790/2821/2834, host) `id=room`, snapshot — actor host, true. LOBBY_READY (2994) `{peerId, team, ready, craft, challenge, lane}` — the ONLY row that names a seat (team+lane), actor = that seat, true. LOBBY_AUTH (3355, host writes ABOUT the joiner) `peerId:host, data.peerId:joiner` — actor and subject split across two fields with no name for the split. LOBBY_LAUNCH (3001) snapshot, host — true. The AI never writes a lobby row — true. r.136: `LOBBY_AUTH {actor:host, subjectSeat}`; `LOBBY_STATE.seats[]` is the seat→peerId map every later `seat` resolves against.
16. The one place a passport was still read AS a seat: `fireN` 2301 `desig.by!==SID` → `NOT_THE_MARKERS_SEAT`; `ASM@host ≠ host`, so the AI's mark is unfireable in every room (Thor REGRESSION, Asar S3). r.136: `seatOf(actor)=actor.replace(/^ASM@/,'')` — actor and seat are two fields precisely so this cannot recur.

r.136 ROW SHAPE (one shape, every verb): `eventId, seq, logicalClock, sessionSeq, t, challenge, diff, verb, id, result, peerId` (writer device) · `actor` (`<sid>` | `ASM@<sid>` | `CLOCK` | `LINK` | `HOST`) · `role` computed from `actor` only (`HI` | `AI` | `SYSTEM`; `state.pilot` never read) · `seat` (team+lane of the owning member; `SOLO` outside a room) · `cause` (the eventId this row answers: SCORE, HOLD, REJECT) · `data` per verb above. GATES: a source test that every `ev(`/`decide(`/`netEvent(`/`lobbyRecord(` call supplies `actor`+`seat`; a replay test that `role` recomputed from `actor` equals the stored `role` on every row of the solo/ai/team fixtures; `SEQ_SUBMIT` commits only when `pay.event.peerId===m.sid`, else `REJECT ROW_AUTHOR_MISMATCH {from}`.
```

### MoT 9

```
MoT-9 · 3v3 READINESS — ordered prerequisites before `state.lobby.match>1` may pass play.html:2971 (r.135, sha 882287ec…, verified in the served file)

STANDING: 3v3/9v9 are labels (play.html:216/:295) held true only by the refusal `MULTI-PEER TRANSPORT NOT QUALIFIED` at :2971 (Odin :10, :23). REVISIONS.md:246 "3v3/9v9 stand on a single-peer link"; :286-288 and NOTES_r135:37-39 list the same owed items. The 1v1 that ships today is honest only on the happy path: team e2e 9/9, one hash on both (Odin :19, Christo :1, Krishna :3) — and `drone-team-e2e.mjs` has no drop step at all (only `b.close()` at :71).

ORDERED PREREQUISITES (each: fact at r.135 → gate that must exist before the next is started):
0. The gates must run where a red is seen. `drone-deck-qa` + `drone-team-e2e` run only in deploy.yml:71-72; `npm run test:ci` carries neither (Krishna :11). GATE: both in test:ci behind a Chromium check that FAILS, never skips (Krishna :27). Until this, every gate below is a local script, not a rule.
1. Never mint on a JOIN. `sessionPrepareLocal` :1183 + `sequencerUsesHost` :1164 mint on every drop: P1 on r.135 → `HOLD PEER_LOST committed:true sessionSeq:101` on the joiner; SNAPSHOT :1517-1519 replaces wholesale, pending rows leave `events` (P5), no expiry (P10); the "IS BACK" toast (:1471) then announces a return the forked hash cannot repair (Odin :7, :16). First because it fires in the served 1v1 (Odin :23). GATE: team e2e drop-mid-red → HOLD pending → reconnect → ONE hash on both; QA `LOST_APPROVER_IS_SAID` (:3932) re-run with `role:'JOIN'` asserting `committed:false` (Odin :26).
2. The canonical range clock on the record. Instance closed (`lapseIsMine` :755, one LAPSE row per room, P4c; Christo B3 hashes 708099fd==708099fd), class open: `rangeTick` :761-778 still advances every lane from local `dt` (joiner ran `R.k` to 7 on its own clock), no EXPOSE row, no `{k,phase,t}` in `snapSend` :1561 or the launch snapshot :2922, reducer :1963 has no exposure guard, `lobbyLaunchSnapshot` :2912-2928 carries no `rangeMode` (Odin :6, Christo :6). GATE: two-context e2e in QUAL·40 asserts the same `base` up on both at APPROVE and `R.k` equal on both after 20 s (Odin :27); a pre-START mode difference reconciled at START (Christo :6).
3. A pure fold over the sorted log, and a seat on every row. `appendCanonicalEvent` :1990-2016 pushes then applies in arrival order; SNAPSHOT :1518 re-applies in array order; the hash sorts by orderKey (Odin :8, REVISIONS.md:241). `ev` :2020-2027 carries `peerId`/`role`/`actor` — no `lane`, no `unit`; `laneClaims` :2922 is the single occurrence (written, never read — verified: 1 hit); host AND joiner both report `unit:'T06'` for lane 5 (team135.json final/host + final/join — verified) (Odin :11). GATE: two rows delivered out of order → world === hash; QA asserts `lane`+`unit` on a HIT row and distinct `unit` on the two seats of the team e2e (Odin :28).
4. The join code selects the team, and the refusal reaches the joiner before the wire. `lobbyNetJoin` :3068 binds the digits to the lit button (typed RED digits without tapping RED → TEAM DENIED); the r.135 "refusal travels" at :3341 runs while applying the ANSWER, before any dc exists → `linkSend` throws into `catch(_){}`, joiner stuck on "ANSWER READY · HOST MUST VERIFY CODE" forever; the e2e lifts RED from `state.lobby.teamCodes` (drone-team-e2e.mjs:32-35), never from `#wrOtherCode` (Christo :4, :18). GATE: e2e reads the six digits from `#wrOtherCode`, joins with no team tap; a second pass with wrong digits asserts one sentence on the JOINER's `#lcTxt` (Christo :18). With 6 seats, the code is the only thing that can seat a stranger.
5. Liveness on the shot and re-sync on return (still 1v1-scoped). `fireN` :2296-2312 has no liveness member: on a SILENT drop the shooter's red stands until the 12 s watchdog (:1575), longer than any 4 s exposure (probe135-drop: red stood 3 s until lapse); the approver is toasted "NOW PRESS FIRE" (:2238) by the seat :2300 refuses; on "IS BACK" the host does not `snapSend` (only under `syncProof.reconnectSeen` :1479-1481) so the rows the vanished seat missed are never re-sent (Christo :5, :11, :19-20). GATE: e2e mutes one seat's dc.send WITHOUT closing it → other seat's FIRE refused within the exposure; e2e drops the joiner 15 s across one LAPSE, restores → MATCH + equal LAPSED on both strips; in-file row: the approver's toast never contains FIRE (Christo :19-20).
6. One pc/dc per peer with per-peer liveness — the item that actually lifts :2971. One `pc`/`dc` (:3265); `ondatachannel` :3291 overwrites; `linkWifiApplyAnswer` :3327 applies one answer; three HELLO sids → `peers:2`, no `peers[]`; `peers=Math.max(2,…)` :1472; `linkLost` `peers=1` :1571; liveness is one scalar per room (`lastPeerAt` :1401/:1471, one 12 s test :1575); grep `peers[]|lastSeen` → 0 hits (Odin :10, :28). GATE: a third context joins and NEITHER earlier channel is overwritten; per-peer `lastSeen` demotes exactly the seat that went silent; `lobbyCanLaunch` :2971 lifts only when `peers[].length === 2*match` all DIRECT+AUTH.
Order rationale: 1 before 2-3 because it corrupts the 1v1 served today (Odin :23); 2 before 3 because a fold over rows can only agree if the rows carry the exposure; 4-5 are 1v1 walks a stranger hits before any third phone exists (Christo :15); 6 last because every earlier gate is written for two contexts and must be re-run at N once the transport is real (Odin :28 parenthesis; NOTES_r135:39).

SMALLEST HONEST DEMO THE OPERATOR SHOULD SEE FIRST — 1v1 WITH A REAL DROP AND RETURN (two real phones, one WiFi; Odin :30, Christo :22):
host + joiner, QUAL·40 (or TRAINING), lane 5 → joiner marks, host approves → joiner phone goes to airplane mode mid-red → host strip "THE OTHER SEAT IS GONE · YOU ARE ALONE", red demoted to amber, FIRE refused (Christo :5, :10 — works today on a CLOSE frame; on a silent drop only after 12 s, item 5) → joiner returns → "THE OTHER SEAT IS BACK" (:1471, works today) → BOTH strips MATCH, one hash, equal LAPSED counters, the joiner's HOLD row pending/expired, not committed. Today this demo FAILS at the last step: the joiner's ledger forked at the drop (sessionSeq 101, Odin :7) and the host never re-sends (Christo :5) → DIVERGED with nothing to repair (Odin :16). Passing it is items 1 + 5 alone, and it is the same walk `drone-team-e2e` must gain as a drop step (it has none, :71). Only after that walk is green on two phones does a third phone (item 6) mean anything.

HARDWARE-GATED (cannot be proven from this sandbox): the fork and return path on real phones (harness fakes `readyState`, Odin :30); real clock skew between two range clocks (P4c ran both in one process); a third phone's answer overwriting the second's channel (:3291/:3327); WebRTC surviving a 15 s radio gap without ICE restart (Christo :22); LIVE at the edge UNVERIFIED — remote main is r.134 (300c534), 2020c02 unpushed (Krishna :3, :23).
```

### MoT 10

```
MOT-10 · THE DECISIONS ONLY THE OPERATOR CAN MAKE (r.135 candidate 882287ec…; play.html lines are r.135 = frontend/public/drone-2525/play.html; working tree HEAD 543a86c, 25 dirty paths — LIVE UNVERIFIED, sandbox 403 per Enlil-SYNTH:81)

1. T13 OR THE TRINITY PALETTE? Raised by the deck README §Divergences item 1 (README.md:198-203, r.050 fold), not by a fleet48 lens; r130 fleet review:267 counted a 14th DOM colour. Fact: play.html:566 `T13` (13 muted instrument hexes) + :567 `OK` set, and `add()`/`addR()` throw on any hex outside it (:917, :966); the repo's law is `frontend/lib/trinity-palette.ts:5-19` (13 saturated spectrum hues) enforced by `tests/vector-law.test.mjs:41,47`, which also draws Security/Architect/Manta/Celestial. Two 13-colour laws, one gate each, no hue shared.
   (a) Keep T13 in the deck — cost: the same Capitol wireframe ships in two palettes across the domain plays; the seat semantics (HI #5BA8FF, AI #C084FC, SI #E8D5B0, LOCK, TAG) stay deck-only and vector-law never covers the deck.
   (b) Adopt the trinity spectrum — cost: every `add(…,T13.X,…)` line (~30, :970-1002), the bullseye in T13.SI (REVISIONS.md:33) and the r.013 md5 `7dfe3c58` (README.md:190) change; saturated #FF0000/#FFFF00 on the dark instrument ground is a new readability question at 320 px, and the deck's HI/AI/SI meaning must be re-mapped onto hues named for other things.
2. ASM CUP FIXTURE — 0.039 OR r.042? Raised by README.md:219 item 8 and `lib/drone-2525/asm-cup.ts:14-16` ("the operator's to confirm"); flagged, not gated, at `tests/asm-cup.test.mjs:31` and CRS_DRONE-2525.md:875. Fact: ASM_CUP_99.json:4 declares `"revision": "0.039"`; the deck it shipped with is r.042 (README.md:194: decide/asmTick/challengeSpec were ADDED r.035→r.042); the bytes are sha-pinned and read-only by contract; Asar-SYNTH:11 adds that every live-play sidecar still writes `fixture:'ASM_CUP_99',untouched:true` (:3431) and the panel labels a different fixture "HISTORICAL FIXTURE (r0.103)" (:4120).
   (a) 0.039 is the reference — cost: the baseline (6v6 BLU 3–1, 3v3 BLU 5–0) was produced by a deck without `decide`/`asmTick`, so no r.042+ deck can be expected to reproduce it; "regression fixture" means bytes only, never behaviour.
   (b) r.042 is the reference — cost: the JSON's own field is then wrong and cannot be edited (pinned); a corrected fixture is a NEW file + hash + sidecar note, and the 0.103 panel label needs the same ruling.
3. PRIVATE RED CODE, OR THE HOST'S CARD SHOWS IT? Raised by Christo A (r132 assessment:120 s23 "ROOM_PRIVATE_CODES, operator decision"; REVISIONS.md:199). Fact: r.134 already shows it — :3049 prints "<6 digits> · GIVE THESE 6 DIGITS TO THE OTHER SEAT" on the host's card — while the room note :480 still says "opponent secret stays PRIVATE", the joiner's card :231 still reads PRIVATE, and QA `ROOM_PRIVATE_CODES` (:3736) still passes because it checks only that `#wrBluCode/#wrRedCode` are absent, not `#wrOtherCode`. The record and the gate say one thing, the card another. Christo-SYNTH:31: the digits still do not select the team (:3068), the mismatch refusal is sent before any channel exists (:3341, never arrives), and the e2e lifts the code from state (drone-team-e2e.mjs:32-35), never from the card.
   (a) Private — cost: an unaided cross-team join is impossible by design (Christo-A:13; the host mints both codes so "secret from the host" protects nothing); revert :3049, keep the gate.
   (b) The card shows it — cost: retire the "opponent secret" note (:480) and rewrite `ROOM_PRIVATE_CODES` to assert the card; then the code must seat the team and the refusal must reach the joiner (Christo owed 1) before the walk is real.
4. HI OVER AI IN A ROOM — one human + the AI, or r.129's two humans (r.135 chose two)? Raised by Pangu (A:17, B:14), Asar B, Thor B. Fact: r.129 rule REVISIONS.md:35-36 ("in a LIVE room the red box needs two separate humans, decided by seated identity"); r.134 exempted the AI mark (REVISIONS.md:214 "never held for two humans") → Pangu-B:14 probed one human + SPOT = a whole shot in LIVE; r.135 withdrew it (:2225-2228 comment "r.129's two-humans rule stands; HI OVER AI is the solo form", REVISIONS.md:266-267) — in prose only, no D-number row (Pangu-SYNTH:7). Consequence today: Thor-SYNTH:79 / Asar-SYNTH:50 — with :2301 `desig.by!==SID` the AI's mark (`by:'ASM@'+SID`) is REJECT `NOT_THE_MARKERS_SEAT` on BOTH seats: an AsM SPOT mark in a room is a red box no one may fire (no QA row covers it; 115/116 green).
   (a) Two humans (r.129/r.135) — cost: in a room the AI is a spotter for the other seat only, its own seat is HELD `TWO_HUMANS`, and until Thor owed 1 (`SID || 'ASM@'+SID`) lands nothing fires; the joiner's record still says `PEER HI-2` where the host's says `PEER HI OVER AI` (Pangu-SYNTH:7).
   (b) One human + AI (r.134) — cost: "two humans" becomes attribution, not people (Pangu-A:17); a LIVE room is halved silently; the r.129 rule must be rewritten as a decision row and `LIVE_TWO_HUMANS` (:3825) extended either way.
5. WALL-CLOCK OR FRAME-TIME QUAL TABLES? Raised by Enki (A:20, SYNTH:10) and Odin (SYNTH:36). Fact: `loop` clamps `dt=Math.min(.05,…)` (:3684); table seconds are `state.clock-qualT0` (:836); no `visibilitychange`/`pagehide` handler exists; measured 4.21 s in a background tab advanced the table 0.667 s; a 30 fps phone plays a 40-round table ~1.5× slower than the sheet (Enki-A:20); each phone raises plates from its own dt (Odin-SYNTH:36), so two seats' clocks drift. Toast lifetime is the one wall-clock exception (:4234 per Enki-A).
   (a) Wall-clock + a HIDDEN/PAUSED row (`CLOCK_IS_WALL`) — cost: a locked screen or incoming call now LAPSES exposures on the record (rounds lost to the phone, not the shooter); QA rows that step `phys(0.001)` synthetically (:3936, :3940) need rework; rooms still need Odin's host EXPOSE row for one clock.
   (b) Frame time (as shipped) — cost: a timed table pauses for free, indefinitely, unrecorded; "142 S LEFT" is not seconds on a slow phone; the QUAL sheet's times are not comparable across devices.
6. "ONE APPROVE, N ROUNDS" AT CH1–4 — DOCTRINE OR DEFECT? Raised by Thor (A:14, B:5, SYNTH:31 "a human ruling"). Fact: :2330 after a MISS keeps `phase='red'` at CH1–4 and sets `hiApproved=false`, but `fireN` reads `hiApproved` only at CH5 (:2305) and the red check at :2303 reads phase alone — so one approval covers every pull of the exposure at CH1–4; CH5 goes back to amber (:2330, r.135). The code comment claims doctrine ("r.131: red holds for THIS exposure") while the r.134 register says "a red box whose approval was spent asks for a fresh APPROVE" (REVISIONS.md:207-208) — record and code disagree.
   (a) Doctrine — cost: the approver's decision is a bit for the exposure at CH1–4 (Thor's class 1); a second pull after a miss needs no human; write it as a D-row and a QA row.
   (b) Defect — every pull re-approved like CH5 — cost: TARGET→APPROVE→FIRE twice inside a 3–8 s exposure on a thumb (Athena hardware-gated:27); solo shooters press APPROVE per round; the CH5 path becomes the only path.
7. SAVE AS FOUR DOWNLOADS? Raised by Athena (r132 assessment:163 s29 "phones drop three"), Enki-A:18 (D22), Asar-SYNTH:37 (hardware-gated: all four on iOS/Android). Fact: `saveAll` calls `download()` four times (:3425 session.json, :3429 samples.csv, :3430 train.json if `state.cmp`, :3431 sidecar.json) from one tap; NOTES_r135:41 owes "SAVE as one download"; the sidecar name is a repo contract (`asm-cup.ts:36 SIDECAR_NAME`, README.md:152).
   (a) One download — cost: a single bundle either embeds the CSV in JSON (the samples CSV stops being a CSV) or needs a zip, and the deck is one self-contained file under a 7.77 MB budget; `asm-cup.ts` and the sidecar-beside-the-fixture rule must read the new shape.
   (b) Four (as shipped) — cost: on phones the browser keeps one file per gesture — three of four are dropped silently and "SAVED 333" (:3432) claims all four (Sofia-SYNTH:9, rule 5 over-claim).
8. DOES THE DATA PANEL GO BEHIND ?diag=1? Raised by Sofia (A:18, SYNTH:8, owed 3); REVISIONS.md:244 named it for r.135, :287 still owes it; `grep -c diag` = 0. Fact: a player's DATA opens "MoT LEVELS · cnn3 · WORKER+IDB · WEBCRYPTO + WEBRTC · ECDH/HKDF/AES-GCM · 0ms skew · CH0 D3" (Sofia-SYNTH:8; r.135 ADDED "skew" and "D3" to the player's surface); CLAUDE.md "THE SIGNER NEVER MEETS THE MACHINE" §3 already names `?diag=1` as the operator's door.
   (a) Behind ?diag=1 — cost: every probe and QA row that reads `#cilTxt/#hilTxt/#cmpTxt/#pathTxt` (Aset-SYNTH:14, Thoth-SYNTH) must open the door first; the MoT/HAL/DIFF pickers leave the player's reach; a word gate (Sofia owed 1) is what keeps it closed.
   (b) Keep it on the player's panel — cost: the deck violates the CLAUDE.md rule on every phone, and each fold adds a machine word for one it removes (Sofia-SYNTH:23) with nothing to fail the build.
```

### MoT 11

```
# MoT-11 — process lessons of r.133 → r.135 for CLAUDE.md's AAR style (facts, ordered, cited)

1. THE ARTEFACT-ALONE COMMIT IS RED AT ITS OWN SHA. f2557ac (r.134) and 2020c02 (r.135) each carry play.html at the new revision while `frontend/tests/deck-head.mjs` still says the old one (`git show f2557ac:…deck-head.mjs` → DECK_REV '133'; `2020c02` → '134'; verified here) — so `test:drone-playable` (package.json:170 item 149) and `test:deck-consistency` (item 159), both in test:ci, fail at that commit (Enlil B:26; Enlil SYNTH:21 "CLASS REPEATED"; Krishna SYNTH:19 "one non-bisectable red commit per edition"). REVISIONS.md:283-284 records it as acceptable ("the ship commit is the one that must be green"). CLAUDE.md's "commit the artefact alone" and its "zero errors before pushing" are in tension and nobody wrote which wins (Krishna SYNTH:19).
   RULE: the artefact commit is PERSIST FIRST, so it may precede the fold — but it must never be a SHA test:ci rejects; the wiring that makes it green (deck-head, manifest, register row) lands in the SAME commit or the artefact commit is made on a branch and squashed with its ship.
   CHECK: a pre-push hook (or deploy.yml) runs `npm run test:ci` on EVERY commit in the push range (`git rev-list origin/main..HEAD`), not only HEAD; a red intermediate refuses the push.

2. THE INSTANCE WAS FIXED, THE CLASS WAS LEFT — the B reviewers' "INSTANCE ONLY" roster on the r.134 candidate:
   a. Fire gate: keydown ignores inputs/intro (3512) but `fireN`/`approveDesig` never read `rangeArmed()` — a WAITING room behind the intro fired to `HIT` by DOM click (Thor B:9, probe S7).
   b. Authority: `releaseAuthority` called from chLvl/RESET/dead target, no consumer for a lane change, a peer's ordinary release, an expiring UAV (Thor B:5; Thor SYNTH:5 still open for the peer's release at r.135).
   c. Liveness: `linkLost` + 12 s watchdog exist, `commRecv` rewrites DIRECT on every inbound message — the instance flaps back in the same second (Christo B:7; Thoth B:13).
   d. The record: training lapse became a row via `ev()` not `netEvent` — both phones wrote it, hashes 91d2d501 ≠ 0cf3aa8f from the first lapsed plate in the default mode (Christo B:15; Asar B:7; Aset B:7).
   e. Words: one of three amber-fire sites rewritten (Sofia B:5); only the TARGET pill's sentence, `designate()` still "WAIT APPROVE" (Athena B:6); D9 fixed as instances, no word gate (Sofia B:10).
   f. Revision strings: BOOT and the UPDATES h3 fixed, six spellings still on the panel (Aset B:10; Thoth B:15).
   g. Reducer trust: `peerId` checked, `data.by` now a second trusted field — a joiner forged `ASM@` (Thor B:10; r.135 `AI_PASSPORT_NEVER_FROM_A_ROW`).
   h. Harness/gates: team-e2e reads the approver's DOM but test:ci still carries neither browser gate (Krishna B:11); patch hashed, never replayed (Krishna B:7).
   RULE: before fixing a member, enumerate the class by grep (every call site of the primitive: fireN, approveDesig, wireApprove, peerDesig, commit) and write the invariant as one sentence at the top of the patch; a fix that names one site is refused at review.
   CHECK: a class-inventory gate — for each invariant, a test that asserts the guard function is the ONLY path (grep count of the raw operation outside the guard = 0), e.g. `releaseAuthority` the sole writer of `desig=null` (Thor's "one consumer" made mechanical), `netEvent` the sole writer of score-changing rows.

3. A FOLD ADDED A REGRESSION AND THE GATE STAYED GREEN.
   a. The HUD split left `hudTail()` defined, never called — seven panel stats "—", `calibrate()` 0×, bloom dead, every frame (Aset B:14; Enki B:20); 109/110 green because no row read those stats. r.135 re-called it (play.html:2657) — still no QA row asserts it (Aset SYNTH:14 "fixed, not gated"), and the revived tail re-opened the signed "-1981ms" COM stat r.135 claims to have relabelled (Thoth SYNTH:7).
   b. The r.135 marker's-seat rule (play.html:2301) matches `/^PEER/` ∧ `by!==SID` — the AI marks as `ASM@SID`, the other seat's approval sets `PEER HI OVER AI`, so `AsM SPOT` in every LIVE room is a red box no seat can pull, `asmTick`'s own trigger dies on the same line; 115/116 green because no row covers an AI mark in a room (Thor SYNTH:19, probed S2/T4). One-token fix: seat = `SID || 'ASM@'+SID`.
   c. `MAP_KEEPS_THE_STRIP` (3936) stayed green with `hudPhase()` deleted from the map branch — it read the PREVIOUS row's plate (Enlil SYNTH:17).
   RULE: every function that a fold splits or renames gets a caller-count assertion, and every new refusal gets a positive-path row (the thing it must still allow) beside the negative one.
   CHECK: mutation gate in drone-deck-qa — serve a copy with the new call commented out (Enlil's method, SYNTH:6) and require the row to go RED; rows assert the plate id they set, never a word the last row left; a `fn.toString()` regex budget (≤13, each paired with a behaviour row) in deck-qa-manifest (Enlil SYNTH:29).

4. CLAIMS ON THE RECORD THAT OVERCLAIMED. (a) REVISIONS r.132 "a Deploy step": `git log -S"drone-team-e2e" -- .github` empty, wired only in r.134 deploy.yml:71-72 (Enlil A:15; corrected REVISIONS r.134). (b) r.134 "one revision string": r0.103, "r.128 WAITING ROOM", "r.0.134" still on the panel — six spellings (Thoth B:15; Krishna B:21d; corrected r.135). (c) r.134 "RESET and a mode change … ask before discarding": `rangeResetOnRecord` asked only for why==='BUTTON' (4399); MODE (4404) discarded with no ask (Enlil B:33; corrected r.135; r.135 fixed it at 4434-4436 but NO row exercises a mode change with qualR>0 — Enlil SYNTH:15). (d) r.135 "AI FIRE 3/3 (the AI's shots on the record)" reads the strip; the harness JSON says hits 0 misses 3 (Pangu SYNTH:8).
   RULE: a sentence in REVISIONS/NOTES that names a gate, a workflow or a count is a measured claim — write the command and its output beside it, never the intent; corrections are appended, never edited (r.134/r.135 did this right).
   CHECK: deck-consistency parses package.json + deploy.yml and holds every "Deploy"/"test:ci"/"Repo:" sentence in NOTES/REVISIONS to a real script (Enlil B:42; Enlil SYNTH:29); `drone-playable` holds an allow-list of ONE revision string (a `revLabel()`), not a deny-list of three (Aset B:10; Krishna B:8); a replay/count quoted in the register is restated from the harness JSON, not the strip (Pangu SYNTH:24).

5. THE HARNESS READ ITS OWN CLICK, NOT THE RECORD. `asm-play.mjs:40` clicks `#fFire` for the `ai` seat; with AsM FIRE on the AI hit 3/3 (strip HIT 3) and the harness reported `hits 0, misses 3` because the human's click read `NO RED BOX` after the AI's shot; `:14` still documents the press order wrong ("1 = AsM FIRE; 2 = OFF" vs SPOT/FIRE/OFF since r.133) (Pangu B:8; Pangu SYNTH:8, 21). The same shape in r.132: team-e2e green with D3 live because it read `state`, not the approver's picture (Enlil A:3).
   RULE: a harness proves an actor by the actor's rows (`HIT by ASM@…`) and the player's picture (DOM), never by its own input's return value; "the only machine that plays the seat" is itself under review each fold.
   CHECK: the `ai` seat waits for a HIT/MISS row `by ASM@` and asserts `hits` equals the strip's HIT (Pangu SYNTH:24); `test:asm-play-ai` added to package.json and deploy.yml (Enlil SYNTH:29) so the harness runs where the claim is made; team-e2e keeps its DOM read (drone-team-e2e.mjs:69).

6. CROSS-CUTTING, FOR THE AAR: three consecutive editions each shipped with a correction of the previous one's register (r.134: 3, r.135: 3) — the corrections were honest and appended, but every one was a claim a checker could have refused before it was written; the r.135 residue (hudTail ungated, the AI disarmed in rooms, the harness 0/3, red artefact commits) is the same class on the next revision. LIVE is UNVERIFIED from the sandbox (Enlil SYNTH:15).
```

### MoT 12

```
MOT-12 · THE ONE-PAGE HEADLINE (r.135, 2026-09-19 night; every line cites the lens synthesis or the file:line that proves it)
WHAT HE ASKED (docs/asks/2026-09-19_usability_asm_team_test.md, verbatim, hashed): "test out all UI/Ux for usability and laser tag targets as solo, team of HI member, or AI member. then test entire drone-2525 as 38 ASM team. 48 AsM fleet" — autonomous, while he was on break.
WHAT SHIPPED (REVISIONS.md:175-289; three editions, each an exact-anchor patch on the one before; deck and served play.html byte-identical, sha 882287ec… at r.135, re-verified by sha256sum):
- r.133 (38-seat team test, waves 1-2 = 26 seats): the AI teammate had never been switched on in real play (its test called it by hand) — now it marks from the game loop and fires only on a box a human turned red; the score was cut off the phone screen — now it wraps; the second phone finally sees the hit it approved.
- r.134 (wave 3 = 12 seats + the 48-fleet's first readers): a red box now dies with its reason (scene change, reset, lost partner, dead target) and every reset / mode change / lapse is a hashed row; the link notices a lost phone in 12 s; VOICE says when it stopped listening; the host's card shows the other team's six digits.
- r.135 (the fleet's adversarial second readers): three r.134 breakages closed (dead side-panel stats, two phones writing one lapse twice, a dropped link that revived itself); nothing marks / approves / fires before PRACTICE or START; the AI is named as the AI on record and strip; no session id or plate id reaches the player. QA 116 rows, 115/116 headless (SYNC_DIRECT by design).
FLEET GRADES AT r.135 (each lens's own synthesis; Enki wrote no synthesis — his B report graded r.134):
- Aset (consistency) B-: the record says one thing per event on both phones; the strip, the score pack, the revision (six spellings) and the peer count each still have one surface that disagrees (Aset:21).
- Asar (outcome truth) C+: honest on one phone; two phones on a QUAL table or a door still diverge, and an AI mark in a room is unfireable (Asar:24).
- Athena (the stranger) B-: on the range every sentence names the next tap; the 11 jargon intro slides and the SELECT screens are untouched since r.133 (Athena:16).
- Christo (two-phone room) B-: the room survives a lapse, a drop and a withdrawn approval; the unaided join still fails and the refusal never reaches the joiner (Christo:13).
- Enki (edge cases) C+ at r.134, no r.135 verdict: VOICE and RESET closed; dock FIRE under the stick, frame-time clocks and MAP fire untouched (Enki-B:26).
- Enlil (gates) B-: the AI-from-the-loop row goes red when the loop call is cut (proven by mutation); MAP_KEEPS_THE_STRIP still cannot fail; no gate measures a pixel (Enlil:23).
- Krishna (integration) B+: every link a script can check agrees; coherent by discipline, not by gate — the patch is hashed but never run in CI (Krishna:21).
- Odin (future-proof) C+: an honest 1v1; each phone keeps its own range clock and forks its ledger on every channel drop (Odin:21).
- Pangu (the AI member) B: one actor on mark, shot, decision and strip; the harness still fires with its own finger and reports the AI's 3 hits as 3 misses (Pangu:19).
- Sofia (words) C-: session ids gone, but "C-150R-L18" on the strip, cnn3/skew on the panel, and a new over-claim "YOUR TARGET LAPSED" after your own hit (Sofia:21).
- Thoth (numbers) C: the lost-link strip is fixed; SSSES is still a label, rtt's sign is wrong on 4 of 5 surfaces, six revision strings (Thoth:21).
- Thor (fire gate) B-: refuses the right people, but the new seat rule disarmed the AI in every room; a partner's scene change leaves your red box live (Thor:21).
TRUE AND GATED (three):
1. The AI teammate is alive from the game loop and its mark and shot are on the record as the AI's — QA ASM_MARKED_BY_THE_LOOP goes red when the loop call is removed (Enlil:6; Pangu:4; play.html:2504, :3995).
2. Two phones end on one hash through a lapse, a drop and a withdrawn approval — drone-team-e2e 9/0 with equal hashes, now in Deploy (Christo:1,:10; Asar:14; Odin:19; deploy.yml:71-72).
3. Nothing marks, approves or fires before a round; the approver's seat never fires the marker's box; an expired target scores nothing — probed by DOM clicks, five QA rows (Thor:9; NOTES_r135 Gates).
STILL NOT TRUE (three):
1. A stranger cannot join from the two screens alone: the six digits do not pick the team, and the mismatch refusal is sent before the wire exists, so the joiner waits forever (Christo:4).
2. A shot can still land with no approver present: fireN never checks liveness; a silent drop (airplane mode) keeps the red box up 12 s against a 4 s exposure (Christo:5; Asar:9).
3. Each phone still runs its own range clock, forks its ledger when its channel drops, and a partner's ordinary release never reaches the shooter (Odin:6-7; Thor:5); the strip still prints plate ids and the panel machine words (Sofia:6,:8).
THE r.135 REGRESSION (found independently by Thor, Asar and Christo): play.html:2301 refuses FIRE when the mark's author is not this seat — but the AI signs as 'ASM@<seat>', so in a LIVE room an AI mark approved by the other phone can be fired by NOBODY ("THE SEAT THAT MARKED IT FIRES · YOU APPROVED" on both phones) while the approver's own toast says "NOW PRESS FIRE" (:2238). No QA row covers it — that is why 115/116 is green (Thor:19; Asar:16; Christo:11). Smaller, same edition: reviving hudTail put the signed "-1981ms" back on the COM stat at frame rate, over the "skew" writer (:2677 vs :4394; Thoth:7).
TOMORROW, TWO PHONES, ONE WIFI (in this order):
1. Host makes a room; on the other phone type the six digits from the host's card AND tap the team button (the digits alone will not seat you — Christo:4). Landing in TEAM DENIED with no sentence is finding 1, live.
2. Human play: mark on one phone, APPROVE on the other, the marker fires — both strips must read HIT 1 and MATCH. Then AsM SPOT on the host, let it mark, the other phone APPROVE, host FIRE — the refusal you get is the r.135 regression, live.
3. APPROVE on phone B, airplane-mode B, FIRE on A inside 4 s — a HIT here is finding 2. Bring B back: expect "THE OTHER SEAT IS BACK" and MATCH on both.
4. Read the strip out loud: any "C-150R-L18", "D3" or "skew" you had to decode goes to Sofia's word gate (r.136 item 1).
REPO: SHA c4d2560 | committed ✓ (tree carries two untracked r.136 drafts, not mine) | pushed ✓ origin/main = c4d2560 | cloudflare UNVERIFIED (HTTP 403 from the sandbox) | https://exel-ai-polling.explore-096.workers.dev/drone-2525/play.html — check: `curl -s <that url> | sha256sum` must start 882287ec.
```

