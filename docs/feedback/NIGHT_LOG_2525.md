# NIGHT LOG · SoI-2525 / Vision-2525 — autonomous passes
> Operator asleep. One high-value item per pass, R-CORE verified before code, SSSES stated, every new
> behaviour locked and every lock mutation-tested. Append only — never rewritten.

| # | Shipped | SSSES | Locks | SHA |
|--:|---------|-------|------:|-----|
| 1 | **Cube 23 · De-Risk Gateway + Primitive #13 Risk Register** — the first Level-3 substrate cube BUILT rather than declared. `lib/2525-core/derisk.ts`: Pilot→Refine→Qualify→Adopt ladder (Principle #3, skip is unexpressible), 66%/30-day quorum, human-signer gate (Principle #1), append-only risk ledger with a drift-detecting hash for Cube 25 quote-lock (Primitive #15). Pure, deterministic, domain-neutral. | Stability +30 · Security +15 · Succinctness +10 | 3476 → 3504 (+28) | (this commit) |
| 2 | **Z-1a · Foundations for the S1 template one-pager.** `SLIDE_SLOT_ASPECT` scalar → per-code map (a second `big` chart would have letterboxed the PDF while the screen self-corrected); drift lock now iterates every entry AND names a charted slide with no seed; `compact` finally gets the label lanes X-6b skipped — the one mode the operator actually photographed. | Stability +25 · Security 0 | 3504 → 3508 (+4) | (this commit) |
| 3 | **Z-1b · The S1×33 gate — the instrument, built before the change it measures.** Operator: "Test for this is S1 works on all 33 projects. as you go." The existing sweep already visited every project for the header check; it now also audits S1's panels there — overflow and empty-panel-body, the two failures that make an exec sheet unusable. Baseline recorded on today's S1: **33/33, overflow 0, one distinct panel count (3)**. | Stability +15 | 3508 (gate is a script, not a lock) | (this commit) |
