# Operator ask — 2026-09-11 · one project per pod

## Verbatim

> one can only select one project

With a screenshot of the live compose screen (`b6010397-image.png`): "Topic — open by default; tag up to 3 Domain Plays
if you like · **3/3 selected**" — Open topic, Architect-2525 and Security-2525 all selected, Manta-2525 greyed out.

## What it establishes

A pod works on **one** project. This follows from the earlier ruling that every task gets a plan with its M, accepted by
the trio before Start: a plan is for one task in one project; three tagged projects has no single plan to lock. The
multi-select ("tag up to 3") predates that ruling and contradicts it.

Also on the same screenshot, noticed and fixed with it: **`&plus;` renders as literal text** on the "New Project" line —
an HTML entity inside a JSX string.

## Provenance
Received 2026-09-11 with the screenshot. Persisted before code (PERSIST FIRST, AAR 2026-08-28).
