# Operator ask — 2026-09-08 ~00:30 UTC (verbatim, persisted before analysis)

> configure Supabase
> [pasted: the Sign Doc page on build bc4a179 — "Creating the document: Supabase is not configured on this build…"; diag: Storage Supabase (shared); Sign backend: migration 036 is NOT applied; Login signed in · explore@exel-ai.com; sentence: apply supabase/migrations/036_sign_envelopes.sql]

Context: the operator's phone still shows build bc4a179 (the live site is at 63f1711, whose panel carries
Copy migration 036 SQL). Applying 036 needs the project's SQL editor or a service-role/DB credential.
