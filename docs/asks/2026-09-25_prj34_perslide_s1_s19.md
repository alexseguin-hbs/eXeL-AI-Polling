# PRJ-34 · per-slide pass S1 → S19 (operator 2026-09-25)

## Verbatim ask
> work S1, the. individually through S19

## Clarification (operator, AskUserQuestion 2026-09-25)
- **What the per-slide pass does:** "12-lens AsM review + refine" — for each slide S1 → S19 in turn, run all twelve
  AsM lenses focused on that ONE slide, apply the refinements it warrants (face text, notes, hierarchy), and ship
  per slide with a written per-slide review.
- **Timing:** "After 99 finishes" — let the running automated rounds reach round 99 first, then start the per-slide
  pass from the final revision (avoids edit collisions on the shared seed/master files).

## Reading of record
The deck is the PRJ-34 / CrisisCommand Future State Innovation-Pod deck, **S1 through S19** (19 slides;
`frontend/lib/innovation-slide-seed-drs.ts`, `hi` = slide face, `ai` = speaker-note superset; masters in
`docs/drs/drs.v00.00.json`). The automated 99-iteration programme worked the deck by THEME/class across slides;
this pass works each SLIDE individually — one revision per slide, twelve lenses focused on that slide, refine that
slide, gated commit + push both refs, exactly the ITERATION_BRIEF format scoped to one slide.

## Execution
Per-slide workflow: `…/workflows/scripts/prj34-perslide-s1-s19.js` (twelve-lens-per-slide agent → door
`scripts/prj34-iterate.sh` for gates/commit/push). Launched after round 99 lands, with
`startRevNum = (last landed rev numeric)+1`, `prevSha0`/`prevRev0` = the final landed round, a far-future deadline.
Each slide = its own revision (S1 → 0.106 … S19 → 0.124 if 99 ends at 0.105); full CI on the last slide and every
11th revision. Written per-slide review = each revision's `docs/drs/iterations/0.NNN.md` (twelve lenses + Spiral
test + AAR, all scoped to that slide).
