# Operator ask — 2026-09-17 · Drone-2525 r.093 → r.102 arc ("take elements for execution")

The operator uploaded a chain of Grok deck revisions with iterative review, HEAD now **r.102**:

- `drone-2525_v.00.00_r.093.html` (sha256 `b5323864…e6b5`) — self-test in tool; 9 gaps found.
- `drone-2525_v.00.00_r.094.html` (sha256 `ae53c9e4…cf00`) — WebRTC carries the room; `appendCanonicalEvent`;
  Blob byte sizing; BT honest; SIM vs human plays separated. 4 gaps remain.
- `drone-2525_v.00.00_r.097.html` (sha256 `e809ea98…d9e0`).
- `drone-2525_v.00.00_r.098.html` (sha256 `2a72c17a…f86d`).
- `drone-2525_v.00.00_r.102.html` (sha256 `9de2a582…6c56`) — independent-trial snapshot/restore per trial.
- `ALL_NIGHT_SIM.json` → carried as `ALL_NIGHT_SIM_r101.json` (sha256 `65f3fa3f…8822`) — rev 0.101 batch,
  2,016 trials (8×252) in wallMs 952 (accelerated, NOT 8 hours), seed 2525, top CH5 NET 690, hash f5a282d5.

Operator instruction, verbatim: *"see latest from Grok for learnings; hoping you can take elements for
execution."*

## THE ONE LAW (Grok's convergence, adopt for our port's P2)
**ONE EVENT → ONE REDUCER → ONE WORLD → ONE HASH.** A canonical event is created once, transmitted as that
exact row, and applied to world state (target life, up/down, effects, BLU/RED score) only through a single
deterministic reducer `applyWorld(state, row)` — never local mutation on the sender plus log replication.

## Qualification (the two-phone MATCH gate)
Device A TARGET → Device B amber → B APPROVE → both red → A sim action → both worlds + hashes match →
disconnect → snapshot → hash verified → reconnect → next event → hashes still match.

## Still open at r.102 (do NOT inherit)
1. Real two-peer COMM/SYNC QA gate (F04_NO_DIRECT proves the opposite of connectivity).
2. One decision → one canonical event (r.095/098/102 still double-count SIM-ACTION: `decide()` + `netEvent()`).
3. Unmeasured device metrics = null, never 100 (offline batch device fps 0 → expected fps → eff 100 is wrong).
4. A successful SIM-ACTION path in the batch (r.102 had holds:1 refuses:1 fires:0 — authority-gate only).
5. Exported evidence stats: uniqueTrialHashes, hashHistogram, mismatchCount, digest-over-all-hashes.
6. Concurrent ordering: `orderKey` exists but `replayHash` does not sort by it — needed before 3v3/9v9.
7. Endurance ≠ depth: a real 5-hour continuous run with wall-clock checkpoints, heap/FPS, crash count.

## Decision (operator 2026-09-17)
Build **P1 (the 60-second one-phone demo on-ramp)** next; this networking/determinism arc is captured for P2
and built AFTER the demo qualifies. Learnings persisted here and in the plan (`ROUND 12 — r.093→r.102 ARC`).

Files carried into `docs/drone-2525/operator-deck/`; hashes in `HASHES_r093_r102.sha256`.
