# Operator ask — platform roster + scenes (2026-09-17)

Persisted verbatim. This DIVERGES from the deck's units table on purpose; the platform gate now checks the
operator-recorded roster (domain JSON), not the deck.

"remove manta 99-66; keep ark sail and mini. if MASS-AI IS SELECTED, you are R2D2 looking ground robot on two
wheels, if ark or mini are selected you are on lake travis going around buys patrolling"

Decision: REMOVE M99 (Manta 99-66). KEEP M66 (MINI) + ARK (Ark Sail) → scene Lake Travis, patrol around buoys.
KEEP R2 → relabel MASS-AI → an R2D2-style two-wheel ground robot. Air/turret (T1/D1Q/D1/D1F) → Capitol lawn.
One gimbal, one TARGET→amber→APPROVE→red→FIRE two-step across every scene; a scene changes only the world and
the motion model (air flight / two-wheel drive / surface-with-buoys), never the fire gate.
