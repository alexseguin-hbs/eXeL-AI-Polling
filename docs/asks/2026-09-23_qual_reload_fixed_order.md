# Operator ask — 2026-09-23 (sixth) — reload on QUAL by hand; the same target order as the actual test

Persisted verbatim before analysis (CLAUDE.md: persist first).

> way better on sizing; key is we reload on Qual and have same order as actual test so user memorizes order

## Read as
1. In QUAL · 40 the shooter reloads by hand at each position change (four magazines of ten, one per phase), as on the real range —
   the tower's rest no longer loads the next magazine for him; an empty magazine refuses FIRE until RELOAD is pressed; four magazines only.
2. The engagement order — which silhouette comes up when, left / centre / right included — is the same every time and on every lane, so
   a shooter can memorise it the way soldiers memorise the real table.
