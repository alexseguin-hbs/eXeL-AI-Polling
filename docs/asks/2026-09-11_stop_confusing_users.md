# Operator ask — 2026-09-11 · "Stop confusing USERS unnecessary UI/UX"

## Verbatim

> Stop confusing USERS unnecessary UI/UX

With a screenshot of the live compose screen (`f05b43d3-image.png`): the "Where these hours are anchored — 웃 = M × T"
panel with a paragraph about the mint, a paragraph explaining `N.mmmm..ssss` and `0.0068..1751`, a "All 106 places · 29
with a published rate · 114 rows across 33 languages" disclosure, then a second panel "Estimate, before the work" with
three more sentences, then the button.

## What it establishes

The compose screen speaks to the person opening a pod about THEIR next action — the same law as "the signer never meets
the machine": no doctrine, no arithmetic lessons, no register counts on the path. One panel, the controls a person must
set, one line that shows what those settings mean, and the button. The explanations live in the documents and behind a
single collapsed line, not inline.

## Provenance
Received 2026-09-11 with the screenshot. Persisted before code (PERSIST FIRST, AAR 2026-08-28).
