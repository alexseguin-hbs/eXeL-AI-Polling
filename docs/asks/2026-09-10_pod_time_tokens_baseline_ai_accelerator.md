# Operator ask — 2026-09-10 morning (persisted verbatim before any analysis)

> also ensure the POD session works to document time SI token or HI token or task that has baseline target with AI
> accelerator added.  Ensure one can come back to the same. simulate completion with AsM and AI summary using ApI into
> open AI or others via settings and use all the learnings from sign doc field

Read as five requirements:
1. A POD session documents TIME against a unit of work — an SI token, an HI token, or a task.
2. That unit carries a BASELINE TARGET, with an AI ACCELERATOR applied against it.
3. A person can COME BACK to the same pod session and continue it.
4. Completion can be SIMULATED with the 12 Ascended Masters plus an AI-written summary, through the provider chosen in
   Settings (OpenAI, Gemini, Grok, Claude).
5. Every learning from the Sign Doc work applies: state the invariant first, fix the class not the instance, the user never
   meets the machine, every invariant gets a gate in the build, reproduce the operator's environment, and the AsM review the
   journey before the ship.
