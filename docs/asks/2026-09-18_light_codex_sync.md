# Operator ask — 2026-09-18 · Light Codex as the SYNC crypto-comms layer (P2)

Persist-first capture of the operator's directive, verbatim, before any analysis. Irreproducible design input.

## Verbatim
> use light codex as inter-network communication between HI and AI agents, with ability for 2 humans to allow
> interoperations using 6 digit crypto key (for anyone in same instance), to allow for Crypto level
> communications using Light Codex between nodes (similar to how SKL is used for Link 16 protocols). take
> inspiration from Light codex and Atlantis Accords Level 1-7 packaged HTML layer of security for transmitting
> updates and info for SYNC

(Sent alongside a re-share of `drone-2525_v.00.00_r.102.html` and `ALL_NIGHT_SIM.json`, already carried in
`docs/drone-2525/operator-deck/` at commit 90882b3; the r.093→r.102 reducer-law learnings are in the plan.)

## What it asks, decomposed (to be grounded against the repo before design)
1. **Light Codex = the inter-node transport/encoding** for HI↔AI agent communication (COMM/LINK/SYNC in the
   R-CORE loop). SYNC envelopes ride Light Codex between nodes.
2. **A 6-digit crypto key** lets **two humans in the same instance** interoperate — the shared secret that
   authorises a peering. Echoes the existing crew-link CODE / room code, now with a crypto meaning.
3. **SKL-for-Link-16 analogy**: the key is *loaded* into the nodes the way a Simple Key Loader distributes
   crypto keys/loads for a Link-16 net — a keying step distinct from the message traffic it then protects.
4. **Atlantis Accords Level 1–7 packaged-HTML security layer**: a tiered (L1–L7) security wrapper, delivered
   as a self-contained HTML layer, for transmitting SYNC updates and info. Take inspiration from the existing
   Atlantis Accords (Cube 16) tiering.

## Where this lands
This is the **P2 SYNC/transport** layer (the r.093→r.102 arc): ONE EVENT → ONE REDUCER → ONE WORLD → ONE HASH,
now with a Light-Codex-encoded, 6-digit-keyed, L1–L7-tiered SYNC channel between nodes. NOT a P1 item.
Sequencing (finish P1 edge sticks first vs pivot to P2 now) is the operator's call — asked before executing.

TODO on grounding: locate "Light Codex" and "Atlantis Accords Level 1-7" in the repo and cite the real files.
