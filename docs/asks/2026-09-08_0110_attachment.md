# Operator ask — 2026-09-08 ~01:10 UTC (verbatim, persisted before analysis)

> [screenshot: the phone's mail composer opened by "Send by e-mail" on the offline hand-off — the script says "upload the partly-signed PDF attached to this message" but nothing is attached]
> Should have attachment if emailed or texted

mailto: and sms: links cannot attach files; the phone's share sheet (Web Share with files) can — that is the
mechanism, with download + composer as the fallback.
