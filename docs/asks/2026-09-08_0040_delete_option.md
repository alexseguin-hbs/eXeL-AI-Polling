# Operator ask — 2026-09-08 ~00:40 UTC (verbatim, persisted before analysis)

> [screenshot: Addendum A with a stray date box mid-page, the toolbar showing "Sizing: date", − / +, the date field and a bare ✕]
> ensure delete option exists if accidental date or signature is olaced

Context: a ✕ existed at the end of the toolbar when a mark is selected (remove-mark). Wanted: unmistakable — on the
box itself and labelled.
