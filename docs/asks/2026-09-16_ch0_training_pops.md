# Operator ask — CH0 training rung (2026-09-16)

Persisted verbatim.

"Have AsM add level 0 for training with pop up targets"

Decision: CH0 TRAINING — a rung below CH1: stationary turret, pop-up targets only (deck pops, life 4+CH), the
two-step fire gate unchanged (nothing fires without APPROVE), auto-assigned to a first-time player before CH1;
at CH0 the turret pre-aims the bull ring so a first-timer sees a target at once (r.073 lock order). The twelve
AsM lenses run it as the first rung of the ladder.
