# Operator ask — 2026-09-08 ~00:50 UTC (verbatim, persisted before analysis)

> [screenshot: the operator's real 3-page Promissory Note signed on the earlier build — the box overlapping the schedule table on page 3, typed "AS" initials at the bottom-right]
> Initial must be physical Initial.  Allow for spot bottom right, must not cover text.  Then ensure there are 3 placeholders so Danny can sign after me and initial and it goes ton standard location for 2 signatories

Read as: (1) initials are DRAWN by the signer, not typed; (2) their spot at the bottom-right of every page is chosen
so it never covers page text; (3) after the first signer, the file carries three placeholders for the second —
signature (on the other party's line), date (on that party's Date line), initial (next slot bottom-right) — and the
second signer's marks land there.
