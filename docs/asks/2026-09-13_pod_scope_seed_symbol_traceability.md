# Operator instructions — 2026-09-13 · scope back to the Innovation Pod · Seed symbol before Vision-2525 · per-section traceability

## Verbatim

> we should be only updating Innovation Pod

> also, once done use Seed symbol and add to entire Vision -2525 before word Seed. Do this for all languages; symbol is
> not word and doesn't count against word count

> from here on out we will have Vision-2525 traceability for each section of website so you dont do stupid ship. Create
> for POD session, create Doc, Sign Doc. Same version control with revisions and comparisons will be possible, just for
> this only

> https://exel-ai-polling.explore-096.workers.dev/soi-session/

## What they establish

1. **Scope = the Innovation Pod only** (the SoI Pod at /soi-session, soi.pod.*). Revert the whole-app translation reach
   (everything outside soi.pod.*), keep the pod fully translated. Decided in plan mode: revert everything outside
   soi.pod.*; Innovation Pod = /soi-session only.

2. **Seed symbol before "Vision-2525"** — prepend the Seed (of Life) symbol before every occurrence of the Vision-2525
   name, in all 33 languages. The symbol is not a word and must not count against any word count (the 333-word synthesis,
   the 111-word comments, any gate). [To confirm on execution: the exact symbol/glyph the operator means.]

3. **Vision-2525 traceability per website section** — a per-section append-only ledger with version control, revisions
   and comparisons (the existing Vision 2525 living-document / replay mechanism, docs/VISION2525_DOCUMENT_SPEC.md),
   created for THREE sections only for now: POD session, Create Doc, Sign Doc. Purpose: traceability so scope errors
   ("stupid ship") are caught. [To confirm on execution: what each section's ledger tracks and where it lives.]

## Provenance
Received 2026-09-13. Persisted before any code (PERSIST FIRST, AAR 2026-08-28).

## Clarification (2026-09-13, after the revert)

> if only only Ux language lexicons, yes we need to be in all 33 languages for all UX

The distinction, corrected: **"only updating Innovation Pod" applies to FEATURES / behaviour / new UI** — do not build
into other apps. **UX language lexicons are different: ALL UX must be in all 33 languages** across the whole app. So the
whole-app translation pass I reverted was actually wanted. Restoring it (undo commit ba819d4); the global lexicon-coverage
gate and the per-language i18n-app store come back. My earlier revert was the miss.
