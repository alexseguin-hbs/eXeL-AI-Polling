# Iteration 0.118 · per-slide S13 (slide 12 of 18 in this pass) · 2026-09-25 · author: twelve lenses · per-slide S13

## Twelve lenses

### Aset

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Restoration reads slide S13 as the deck's honest ledger of what could go wrong: single-platform concentration, an AI that appears to command, a health-information boundary, simulation drifting into theater, each paired with a mitigation that already runs in this repo. Nothing is hidden and nothing is invented; the degraded mode keeps the approved posture and the record alive on any phone. This revision restores the page itself, where the public-body dependency had swollen from a line into a paragraph overrunning its list slot, drawing it back while its full path stays whole in the note. Founder, the register reads true. Investors, no risk is buried. Customers, the plan survives any outage.

words: 111

### Asar

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Synthesis reads slide S13 as the page where confidence earns its keep: the case does not pretend the platform is invulnerable, so it names concentration, apparent AI command, the information boundary and simulation theater, then answers each with a mitigation and status. The meaning is a venture that has looked squarely at its own failure and built the guardrail before the promise. This revision mends the one seam that had frayed, the public-body dependency line, an overrun paragraph in a list slot, drawn to a single line with its full path kept in the note. Founder, the whole holds. Investors, the risks resolve into a plan. Customers, the guardrails come first.

words: 111

### Athena

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

Strategy asks which single move on slide S13 buys most for least, and the answer is a cut. The register already carries the winning posture, life-safety alerting absorbed last and certified, every outbound action bound to a named human, absorption gated one function at a time, the record built to survive the platform's own outage. But the public-body dependency line had overflowed its list slot into a paragraph, taxing the reader the slide must convince. Trimming it to one line, the detail moved to the note, restores the slide's force at no cost. Founder, the sharpest edit was subtraction. Investors, the register now reads at a glance. Customers, nothing is lost.

words: 111

### Christo

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

Coherence looks at slide S13 and finds parts that agree: the technical, commercial and business risks all resolve toward one honest thing, a platform that concentrates power only where a named human and a proven gate can hold it. The degraded mode, the alerting-last rule, the one-ninth qualification, the discoverable-record guard all point the same way. This revision keeps that wholeness by mending the frayed seam, the public-body dependency that had spilled beyond its list slot, now one line with its full path in the note. One page, one reading, one rule. Founder, the whole is intact. Investors, the discipline is uniform. Customers, the promise and its guardrails read as one.

words: 111

### Enki

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 98 · succinctness 89

The edge case I hunt on slide S13 is the skeptic who reads the risk register and decides it is either whitewash or a wall of words. It is not whitewash: the hardest risks are marked High and Open, the platform's own outage during the event is named first, and the life-safety alerting liability is faced, not deferred. But it had become a wall in one place, the public-body dependency overrunning its list slot into a paragraph. This revision shuts that failure by trimming the cell to one line, the detail kept in the note beneath. Founder, both traps are closed. Investors, the candor survives. Customers, the whole register reads plainly.

words: 111

### Enlil

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

Verification is a comparison, and I compared slide S13 to the law it must obey: a slide cell that overflows its panel is a defect, and detail belongs in the note. The public-body dependency item in the list failed that test, a single list line's worth of space carrying a forty-eight-word paragraph. This revision corrects it precisely: the cell states the fourth dependency in one line, and the full path, one government owner, one project through the same loop, its citizens answering through the eXeL Polling API, the result replayable, stays verbatim in the note beneath. Founder, the defect is closed. Investors, no substance moved. Customers, the risk page reads clean.

words: 111

### Krishna

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 90

Integration reads slide S13 as the seam where the venture's promises meet their proofs: each technical risk binds to a mitigation already running, each commercial risk binds to the ladder and the spine, each business risk binds to the discoverable-record guard, and the public-body dependency binds this case to the R-CORE ledger and replay in the repo. This revision strengthens that seam by mending its most crowded joint, the public-body dependency line, which had overrun its list slot and is now drawn back while its full path stays in the note. Founder, every risk ties to an answer. Investors, the register connects to the record. Customers, the join holds both ways.

words: 111

### Odin

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Read in twenty-five twenty-five, slide S13 is the page a distant reader opens to ask whether this venture saw its own dangers or hid them, and finds them named: concentration, apparent command, the information boundary, alerting liability, a discoverable record, each with a guardrail that already endures in this repo. The register survives the centuries only if it stays legible. What would tire a far reader is a list line swollen past its frame, so this revision draws the public-body dependency back to one line, its full path kept in the note. Founder, the register endures readable. Investors, the foresight lasts. Customers, any age reads the risks and their answers plainly.

words: 111

### Pangu

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 89

Subtraction has a floor, the thing itself, and on slide S13 the thing is the honest risk: concentration during the event, an AI that seems to command, the information boundary, alerting liability, the discoverable record. Nothing on that floor needs cutting, for each is a real danger with a real mitigation. But one element sat wrongly above it, the public-body dependency line, a paragraph crammed into a list slot, clutter over substance. This revision removes the clutter without touching the substance, the line becomes one line, moved down into the note beneath. Founder, the floor is clean. Investors, no risk was cut. Customers, every guardrail still stands and now reads plainly.

words: 111

### Sofia

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 90

Twelve readers open slide S13 differently, and each is served: the founder wants the platform's own-outage risk answered by a degraded mode; the investor wants the High-Open risks marked, not softened; the buyer wants the health-information boundary held and the record kept safe; the public servant wants the dependency that lets a government steer one project through the loop. This revision serves them all by clearing the one obstacle in their common path, the public-body dependency line, overflowing its slot, trimmed to a line with every detail in the note. Founder, every lens finds its risk. Investors, none is hidden. Customers, the page reads clearly for all who must trust it.

words: 111

### Thoth

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

The numbers behind slide S13 do not move this revision: the register still asserts no dollar figure, no restoration minutes, no certification date the product has not earned, and the one-ninth gate stays the eleven-point-one-percent bar every absorbed function must clear. What I checked instead is the page's own form, against the density law: the public-body dependency item carried a forty-eight-word paragraph where a list line belongs. This revision corrects the form and not the data, the line is one line, the path preserved in the note, every risk, status and mitigation untouched. Founder, the page is exact to the record. Investors, no figure changed. Customers, S13 reports its risks cleanly.

words: 111

### Thor

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

Risk on slide S13, as the guardian reads it: the danger is a reader who opens the register and either distrusts it or stops reading. The candor is real, the platform's own outage is the first row and marked Open, the life-safety alerting liability is named and answered by absorbing alerting last, certified and delivery-guaranteed, every outbound action carries a named human. But the public-body dependency line had overrun its list slot into a dense block that invites the reader away. This revision shuts that risk, the line is one clear line, the full path in the note. Founder, the seam is closed. Investors, nothing weakened. Customers, the register stands legible.

words: 111

## Spiral test

- Future demo: from S13 alone — the case has named its own failure modes and answered each: single-platform concentration is survived by a degraded mode (the approved posture and the record live on any phone, no AI needed to read the plan); an AI that appears to command is bound by a named approver, a decision right and a timestamp on every outbound action, Manual by default and Semi-Automated only when a capability is earned (R-CORE); the health-information boundary is classified at intake and never becomes the platform's; simulation is pre-registered with a null and a stop rule. Still cannot: see a closed pilot that has run the register live — the risks carry status (Open / Mitigating / Mitigated), not yet field outcomes (S15, S16).
- Customers: from S13 — that the platform is built to survive its own outage, that no message leaves without a named human, and that sensitive facts arrive by pointer and stay outside the platform. Still cannot: see a certification date — none is claimed until confirmed with CrisisCommand (the Low business risk row).
- Innovators who want to add functionality: from S13 — that any absorbed benchmark function must clear the one-ninth (11.1 %) gate on its pre-registered clock before it goes native, and that life-safety alerting is absorbed last, delivery-guaranteed and certified. Still cannot: see the capability catalogue's schema; the absorption order is stated as a rule, not modelled per function.
- Investors: from S13 — the honest High-and-Open risks (single-platform concentration; existing features presented as future; life-safety alerting liability) with owned mitigations, and no dollar figure, restoration-minute claim or unearned certification asserted. Still cannot: see the residual-risk score after mitigation — the register carries status, not a quantified residual.
- Business owners: from S13 — the guardrails that protect the enterprise: a discoverable record answered by privilege-aware fields and counsel-marked entries, step-up identity and succession of decision rights for approvers, and no new SKU invented before willingness-to-pay evidence. Still cannot: see the legal-review sign-off itself; it is named as a Horizon-1 object, not attached.
- System thinkers without analytics know-how: from S13 — a plain reading of danger and answer in four columns (severity · risk · mitigation · status), no math required. After this revision the public-body dependency no longer buries its own line in a paragraph. Still cannot: read a risk model or probability; the register is qualitative by design.
- Execution geniuses: from S13 — exactly which risks are still Open (the platform's own outage; life-safety alerting; existing-features-as-future) and therefore what must be closed first. Still cannot: see the owner and due date per mitigation; that is resourcing (S14) and the roadmap (S18).
- Public bodies (the brief's R-CORE section): from S13 — that a government owner can steer one project through the same governed loop, its citizens answering through the eXeL Polling API and every approved posture and delivered result replayable by the body and by the people it serves, and that nothing in the record's shape is crisis-specific. After this revision that dependency reads as one clear line on the slide, its full path in the note. Still cannot: see a named public-body pilot; it is a declared future dependency (IA, after G3), never a claimed customer.

## AAR

Slide S13 (Risk Highlights — the S13 seed slide: `tech` technical risk register · `comm` commercial risk register · `biz` business risk register (each a 4-column table: severity · risk · mitigation · status) · `deps` gate dependencies list; rendered by the S13 field config in `innovation-data.ts`) — keep | fix | cut, per cell:

- `tech` (technical risk register, table 4 cols; 4 `hi` rows, 5 `ai` rows) — **keep.** Single-platform concentration is the first row, High and Open, answered by the degraded mode (approved posture + record survive on any phone; a completed approval is never discarded — the fix-the-class invariant from the sign work); the "AI appears to command" row is answered by the named-human law (law 2); the PHI-boundary and simulation-theater rows carry real mitigations. `ai` adds the Low HEALTH-PHI/HIPAA external-boundary row. Rows read as table cells, not paragraphs; no overflow. Law 2, law 6 clean.
- `comm` (commercial risk register, table 4 cols; 5 `hi` rows, 6 `ai` rows) — **keep.** Everbridge-lite premature absorption (govern first; one-ninth gate), life-safety alerting liability (absorbed last, delivery-guaranteed, certified), incumbent retaliation, new-SKU-before-WTP and segment sprawl are the slide's law-1 / law-3 anchors. `ai` adds the spine/live-sector reconciliation row. Cells are legible; no overflow.
- `biz` (business risk register, table 4 cols; 4 `hi` rows, 5 `ai` rows) — **keep.** Existing-features-as-future (High, Open), $/minute false precision, old-DRS-resource-thesis return, and the discoverable-record legal risk (privilege-aware fields, step-up identity, succession of decision rights) are the honest business guardrails. `ai` adds the unverified-certification-language row. No overflow.
- `deps` (gate dependencies, list; 3 `hi` items, 4 `ai` items) — **fix (one cell).** The public-body dependency item (`deps.hi[2]`) had grown into a ~48-word paragraph — the fourth dependency, one government owner, one project through the loop, its citizens answering through the Polling API, its approved posture and delivered result replayable, and the "nothing crisis-specific" clause — all crammed into a single list slot: a law-6 density defect (a cell that overflows its panel; detail belongs in the note). The identical text is already carried verbatim in `deps.ai[3]`. Keep items 1 (G2 exit criteria) and 2 (`MASTER_Q`); keep the note (`deps.ai`), which carries the CrisisCommand/benchmark/EDU-buyer dependency row and the full public-body path.

Edits applied this revision (one S13 face edit — the law-6 density trim — plus the mandated rev bump (law 5) and the append-only record (law 7)):

1. seed · `S13.deps.hi[2]` public-body dependency item · trim the overflowing ~48-word paragraph to a single slide line — "Public-body dependency (IA, after G3): one government owner steers one project through the same loop; the full path is in the note." — while the full detail (its citizens' answers by the eXeL Polling API, its approved posture and delivered result replayable by the body and by the people it serves, and the "nothing in the record's shape is crisis-specific" clause) is preserved verbatim in `S13.deps.ai[3]`, which stays unchanged so `ai` ≥ `hi` (law 6). Anchor: the `hi[2]` string followed by its array close and the `ai:` opener — a combination unique to the `hi` occurrence (the identical string in `ai[3]` is the last element, followed by the `deps` block close), asserted found exactly once. No IA label, gate (G3), decision, claim or linked field moves (law 3); the field id/shape (string[], list) is preserved and no cell is emptied (law 6); the master statement and segment value props stay fixed (law 4).
2. PROV · seed constant · rev 0.117 → 0.118 (anchor "v1.0 · rev 0.117", one occurrence) · law 5.
3. innovation-data.ts · row provenance · rev 0.117 → 0.118 (anchor "v1.0 · rev 0.117", one occurrence) · law 5.
4. Master record · fill revisions[] 0.117 commit PENDING → 798e34d; append iterations[] 0.118, decisions[] D120, revisions[] 0.118 (PENDING); set project.revision 0.118 · law 7.

Note on the operator's standing overriding instruction, re-relayed this run ("ensure rastors are created; use png versions. and ensure target of R-CORE icon is at bottom of page and does not overlay over/lay on top of image or slide below") — VERIFIED SATISFIED + GATED this run, with checkable proof (per CLAUDE.md persist-first / fix-the-class):

1. **Rasters as PNG, created and committed.** `git ls-files frontend/public/r-core/` lists `r-core-icon.png` (PNG 1448×1086, the framed reticle · REST) and `r-core-wordmark.png` (PNG 2172×724, the "R-CORE" wordmark pill · CLICK 1); both referenced verbatim by `frontend/components/2525-core/rcore-badge.tsx` (`ICON` = `/r-core/r-core-icon.png`, `WORDMARK` = `/r-core/r-core-wordmark.png`) as `<img>` elements — no CSS/glyph recreation.
2. **R-CORE icon at the page bottom, no overlay.** The badge renders in NORMAL FLOW — wrapper `flex w-full justify-center`, no `fixed`/`absolute`/`position` geometry — and is mounted LAST in `app/SoI-2525/page.tsx:1100`, AFTER the `</footer>` (line 1097), so it lands at the page foot below the content, never over the image/slide. Gated by `frontend/tests/rcore-revisions.test.mjs` (in `test:ci`): asserts the badge source contains `data-rcore-badge` and `flex w-full justify-center` and does NOT contain `fixed left-1/2`.

The deck renderer (`scripts/drs-render.mjs`) carries NO icon/raster reference (verified by grep) — the R-CORE icon in question is the SoI-2525 page badge only, which is correctly bottom-placed. The one cosmetic leftover carried forward (D117–D119): `rcore-badge.tsx`'s top docstring line 15 still describes the OLD "Fixed + centred + high z" overlay design, while the LIVE JSX (lines 56–93) and the 2026-09-25 body comment are the normal-flow bottom placement, and the placement gate keys off the live JSX, not the docstring. This door stages only `docs/drs`, the seed, `innovation-data.ts`, `innovation-time.test.mjs`, `drs-render.mjs`, `docs/assessments` and `docs/asks` — it does NOT stage `rcore-badge.tsx`, and this pass may not run git itself; editing the badge would strand an uncommitted change with no hash, violating persist-first. Recorded here for a future badge-scoped commit rather than orphaned.

Decisions:

- D120 — Per-slide S13 pass (rev 0.118). Slide S13 (Risk Highlights) IS a seed slide (the `tech` / `comm` / `biz` risk registers, each a four-column table of severity · risk · mitigation · status, plus the `deps` gate-dependencies list; rendered by the S13 field config in `innovation-data.ts`). Its one law-6 density defect is corrected: the public-body dependency item (`deps.hi[2]`) had overflowed its list slot into a ~48-word paragraph, so it is trimmed to a single slide line while the full path — one government owner, one project through the same governed loop, its citizens answering through the eXeL Polling API, the approved posture and delivered result replayable by the body and by the people it serves, and the "nothing in the record's shape is crisis-specific" clause — is preserved verbatim in `deps.ai[3]` (the note), which stays unchanged so `ai` ≥ `hi`. This CONTINUES the density/clarity discipline of D110–D119 (short `hi`, detail in the note), applied here to the slide that shows the case has faced its own failure modes honestly: single-platform concentration is survived by a degraded mode and a never-discarded approval (law 2, fix-the-class), every outbound action carries a named human (law 2), each absorbed benchmark function clears the one-ninth gate with alerting last and certified (law 1), and the public body enters only as a declared future dependency (IA, after G3), never a claimed customer (law 3). No status, severity, mitigation, gate (G3), IA label, claim, law or linked number moves (law 3); the master statement and segment value props stay fixed (law 4); the field id/shape is preserved and no cell is emptied (law 6). This decision also RECORDS, with checkable proof, that the operator's standing overriding instruction for the page is satisfied and gated this run — the R-CORE rasters exist as committed PNG (`public/r-core/r-core-icon.png` 1448×1086 · `public/r-core/r-core-wordmark.png` 2172×724) and the R-CORE icon's target sits at the BOTTOM of the page in NORMAL FLOW (wrapper `flex w-full justify-center`, no fixed/absolute geometry, mounted last after the footer at `app/SoI-2525/page.tsx:1100`), never overlaying the image or slide, defended by `frontend/tests/rcore-revisions.test.mjs` in `test:ci`; the deck renderer carries no icon reference — carrying the D117–D119 finding forward for the S13 slide, and noting the one cosmetic leftover (the stale "Fixed + centred + high z" docstring in `rcore-badge.tsx`) as out of the door's staging set, to be fixed in a future badge-scoped commit rather than stranded uncommitted. It does not supersede any prior decision. Holds security, stability and scalability at ceiling; S13's efficiency and succinctness rise this revision because the overflowing public-body dependency line is drawn back to one legible line with no loss of substance.

## Gates
```
== tsc
== drs-render
== drs-crs
== innovation-time
INNOVATION-TIME 3797/3797 passed
INNOVATION-STORE-SECURITY 95/95 passed
```
