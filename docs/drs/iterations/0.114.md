# Iteration 0.114 · per-slide S9 (slide 8 of 18 in this pass) · 2026-09-25 · author: twelve lenses · per-slide S9

## Twelve lenses

### Aset

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 91

Restoration reads slide S9 as the deck's promise made countable: each high-priority user story carried from POC through the five maturity marks, tied to a CRS number so the commitment endures beyond the people who first made it. A record lasts only when every story keeps its own thread, from need to release. This revision honours the founder's own instruction for the page: the R-CORE mark that reopens this history is a true raster, a PNG, and it rests at the foot of the page, never laid over the slide it serves. Founder, the stories hold their thread. Investors, nothing derived shifts. Customers, the trace from need to release reads whole.

words: 111

### Asar

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 91

Synthesis reads slide S9 as the page where intention becomes commitment: the user stories, each mapped across POC, Alpha and the three MVP releases, each bound to a CRS number and to the customer need it answers. Its meaning is that binding, and the meaning holds because the table is derived from the brief, deterministic, the same stories out for the same project in. This revision resolves the founder's ask here: the R-CORE icon that reopens the record is the supplied PNG raster, and it sits at the page foot in normal flow, below the content, never over it. Founder, the synthesis stands. Investors, nothing moves. Customers, the commitment reads plainly.

words: 111

### Athena

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 92

Strategy asks which single move on S9 buys most for least, and this revision the answer is not a new claim but a guarantee kept: the founder asked that the R-CORE mark be a raster and sit at the bottom of the page, off the slide. That is now proven true and committed, so the affordance that reopens the version history cannot break or cover the story it explains. The stories themselves earn their place: each traced by CRS number to a release and a need. Founder, the cheapest high-value move was to secure the mark, done. Investors, no derived figure shifts. Customers, the table reads clean under an unobtrusive mark.

words: 111

### Christo

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 92

Coherence looks at slide S9 and finds a page already whole: the user stories, the five maturity marks, the CRS traceability, the customer need each story answers. Each part points at one thing, the disciplined path from a stated need to a shipped release. This revision keeps that coherence by settling the founder's request in its favour: the R-CORE mark is a genuine PNG raster and rests at the page foot, in the normal flow beneath the content, never a fixed overlay on the slide. One page, one honest reading, one mark that waits below. Founder, the whole holds. Investors, nothing underneath shifts. Customers, need to release reads as one line.

words: 111

### Enki

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

The edge case I hunt on slide S9 is the reader whose page fails to draw the mark, or whose mark lands on top of the story table and hides a row. This revision closes both: the R-CORE artwork is a raster committed to the repository as a PNG, so it renders, and it is mounted last in normal flow at the page bottom, so it cannot cover the slide above it. I walked S9 whole and the rest held: the stories, the maturity marks, the CRS trace all derive cleanly from the brief. Founder, the two failure paths are shut. Investors, no figure changes. Customers, no row is ever hidden.

words: 111

### Enlil

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 99 · succinctness 92

Verification is a comparison, and I compared slide S9 to what the founder asked and to what the repository now holds. The claim: the R-CORE rasters exist as PNG and the mark sits at the page bottom, not over the slide. The record: both PNG files are present and committed, and the badge renders in normal flow, mounted last, with no fixed or absolute placement. The build is sound and true. The user stories themselves tie out, each carried across the maturity marks to a CRS number and a need. Founder, the ask is met and proven. Investors, no derived number moves. Customers, the trace and the mark both stand exact.

words: 111

### Krishna

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

Integration reads slide S9 as the seam where a customer's need joins a shipped release: each story mapped across POC through MVP3 and bound to a CRS number, the same traceability the R-CORE decision record runs on. This revision strengthens that seam at the founder's word: the R-CORE mark, the affordance that opens the version history joining this page to its whole past, is a real PNG raster resting at the page foot, never over the slide. The join is clean in both directions, story to release and page to record. Founder, the whole seam holds. Investors, every derived field ties out. Customers, the path from need to release stays connected.

words: 111

### Odin

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 91

Read in twenty five twenty five, slide S9 is the page a reader opens to see which needs became commitments and when: the user stories, their maturity across five releases, the CRS number that lets any future hand trace them. The stories read plainly across the centuries. What would tire a distant reader is a mark that fails to load or a mark that covers the table; this revision spends its effort making the R-CORE mark a durable PNG raster at the page foot, off the slide, so the affordance survives every age. Founder, the mark is built to last. Investors, nothing derived shifts. Customers, whatever century opens this reads clearly.

words: 111

### Pangu

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Subtraction has a floor, the thing itself, and on slide S9 the thing is the disciplined list: user stories, their release maturity, their CRS trace, the need each answers. Nothing on the face needs cutting; the table is derived and lean. The one thing that could sit wrongly above that floor is the R-CORE mark laid over the slide, and this revision removes that risk by fixing the mark in normal flow at the page bottom, drawn from a PNG raster, so it adds an affordance without clutter over the page. Founder, the floor is clean and the mark is off it. Investors, nothing modeled breaks. Customers, the list stands clear.

words: 111

### Sofia

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 91

Twelve readers open slide S9 differently, and each is served: the product lead wants the stories and their release maturity; the auditor the CRS trace; the buyer the need each story answers; the founder the R-CORE mark that reopens the version history. This revision serves that last reader on his own terms: the mark is the supplied PNG raster and it sits at the page foot in normal flow, never over the slide any other reader is studying. So one reader's own affordance never costs another reader a single row. Founder, every reader finds their line. Investors, no derived figure changes. Customers, the story-to-need table reads without a mark crowding it.

words: 111

### Thoth

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 99 · succinctness 92

The figures behind slide S9 do not move this revision: the stories are derived deterministically from the brief, and the CRS numbers are sequential per project, the same in and the same out. What I checked instead is the founder's presentational ask, and I checked it against files, not memory: the R-CORE rasters are present as PNG, sized and committed, and the badge lays out at the page bottom in normal flow with no overlay geometry. The correction here is not to data but to certainty, the ask made verifiable. Founder, the page is exact and the mark is proven. Investors, the derived record holds. Customers, S9 reports its stories clearly.

words: 111

### Thor

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 99 · succinctness 92

Risk on slide S9, as the guardian reads it: the danger is a mark that will not render, breaking trust at the affordance that proves this deck keeps its history, or a mark that covers the table and hides a commitment. Either would let a skeptic doubt the whole record. This revision shuts both: the R-CORE mark is a committed PNG raster, so it renders, and it sits in normal flow at the page foot, so it cannot overlay the slide. The stories, maturity marks and CRS trace are sound. Founder, the seam a skeptic opens is closed. Investors, no dollar, figure, gate, claim or law shifts. Customers, nothing is hidden.

words: 111

## Spiral test

- Future demo: from S9 alone — the disciplined path from need to release: each high-priority user story carried across POC · Alpha · MVP1 · MVP2 · MVP3, bound to a sequential CRS number and to the customer need it answers, all derived deterministically from PRJ-34's own brief (same project in, same stories out). After this revision the R-CORE mark that reopens the version-history-and-compare tool is proven a real PNG raster at the page foot, in normal flow, never over the slide. Still cannot: see the full CRS-##.IN.<TEAM>.### traceability id on the face — the discipline selector drives it behind the CRS # column.
- Customers: from S9 — which of their stated needs this project commits to, and in which release each lands; the CRS number is the handle they can quote back. Still cannot: read the acceptance detail behind each story — that lives in the traceability matrix the CRS # points to.
- Innovators who want to add functionality: from S9 — the story-to-release-to-CRS structure to build against, and the R-CORE mark at the page foot (a PNG raster, unobtrusive) that opens the version history so a new contributor sees how the record has moved. Still cannot: see the object model itself; that is S8 · S14.
- Investors: from S9 — that the roadmap is not a wish list but a traced set of commitments, each need mapped to a release and a CRS number, deterministic and auditable. Still cannot: see the priced value of each story — that resolves on S3 · S8 · S14 (every figure DECLARED, IA).
- Business owners: from S9 — the concrete commitments they are buying, need by need, release by release, with a traceability handle for each. Still cannot: see delivery dates on the face; the maturity marks name the release, not the calendar (gate dates are on S2).
- System thinkers without analytics know-how: from S9 — a plain table: a need, a story, when it ships, its CRS number; and a small mark at the page bottom that, when tapped, shows how this page has changed over time. Still cannot: read the derivation logic; it is deterministic but the engine is not on the face.
- Execution geniuses: from S9 — where execution attaches: each story's maturity mark says which release owns it, and the CRS number is the thread to the requirement behind it. Still cannot: see resourcing head-count against each story; that is S14.
- Public bodies (the brief's R-CORE section): from S9 — the traceability spine itself: a citizen's or stakeholder's need becomes a numbered, versioned commitment whose history is one tap away at the R-CORE mark, replayable by the body that owns it (S12 · S16 · S17). Still cannot: see a public body's own stamped clocks; none is named on this page.

## AAR

Slide S9 — keep | fix | cut, per cell:

- stories (the one S9 field · the high-priority user-stories table) — **keep (no seed cell exists; content is DERIVED, not authored).** S9 is NOT a seed slide: the seed (`frontend/lib/innovation-slide-seed-drs.ts`) defines S1–S8 and S11–S18 and has no S9 / S10 key. S9's single field `stories` is `linked: true` (`frontend/lib/innovation-data.ts` line 2973, cols = `STORY_COLS`) and its rows are generated deterministically by `storiesOf(p)` (line 3514) from PRJ-34's own brief (needs / outcomes / solution), with a sequential per-project CRS number in the CRS # column. There is no PRJ-34-specific hand-authored S9 text to trim, and editing the shared `storiesOf` engine or the generic S9 hint / projectHint would change every one of the 33 projects, which the scope law forbids. The table is lean, deterministic and traceable — no density defect on the derived face, so nothing is trimmed this revision.

The subject of this revision is the OPERATOR'S OWN OVERRIDING INSTRUCTION for this page, verified against the repository (not memory), since S9 carries no editable seed cell:

1. **"ensure rastors are created; use png versions."** VERIFIED SATISFIED: the two R-CORE raster artworks exist as PNG and are tracked + committed — `frontend/public/r-core/r-core-icon.png` (PNG, 1448×1086, the framed reticle · REST) and `frontend/public/r-core/r-core-wordmark.png` (PNG, 2172×724, the "R-CORE" wordmark pill · CLICK 1); both are also present in the built `out/r-core/`. The badge references them verbatim as PNG (`rcore-badge.tsx`: `ICON = "/r-core/r-core-icon.png"`, `WORDMARK = "/r-core/r-core-wordmark.png"`), per the operator's "just use icons provided exactly as is". No CSS / glyph / text recreation.
2. **"ensure target of R-CORE icon is at bottom of page and does not overlay over/lay on top of image or slide below."** VERIFIED SATISFIED: the R-CORE badge renders in NORMAL FLOW at the page foot — its wrapper is `flex w-full justify-center` with `pointer-events: auto` and NO `fixed` / `absolute` / `position` geometry, and it is mounted LAST in the Innovation 2525 surface (`app/SoI-2525/page.tsx` line 1100, after the `<footer>` closes the content), so it lands at the page bottom below the content, never as an overlay. Present mode and the maximized views are separate `fixed inset-0 z-[60]` overlays that render ABOVE the normal-flow badge, so the badge cannot cover a slide. (Note: the stale header line in `rcore-badge.tsx` describing "Fixed + centred + high z" is a leftover from the earlier overlay design; the live implementation and the 2026-09-25 body comment are the normal-flow bottom placement the operator asked for. `app/SoI-2525/page.tsx` and `rcore-badge.tsx` are outside this pass's editable set, so the assurance is recorded, not re-edited.)

Edits applied this revision (no S9 face edit is possible — S9 has no seed cell; the operator's raster / R-CORE assurance is verified and recorded; plus the mandated rev bump (law 5) and the append-only record (law 7)):

1. PROV · seed constant · rev 0.113 → 0.114 (anchor "v1.0 · rev 0.113", one occurrence) · law 5
2. innovation-data.ts · row provenance · rev 0.113 → 0.114 (anchor "v1.0 · rev 0.113", one occurrence) · law 5
3. Master record · fill revisions[] 0.113 commit PENDING → 3fcfc6c; append iterations[] 0.114, decisions[] D116, revisions[] 0.114 (PENDING); set project.revision 0.114 · law 7

(Per-slide constraint: this revision's subject is slide S9, whose content is derived and carries no editable seed cell, so no other slide face is edited. Anchor mechanics: `v1.0 · rev 0.113` occurs once in the seed (PROV) and once in innovation-data.ts (the row). No number, claim, gate, law or linked field moves (law 3): PRJ-34's digital inputs — nreK 11200, fullRev10yM 248 — and every derived field (S1 chart · S2 / S3 profile · S8 charts · S10 grid · the S9 stories table) are untouched, and the master statement (vprop) and the segment value props stay fixed under law 4.)

Decisions:

- D116 — Per-slide S9 pass (rev 0.114). Slide S9 (User Stories — Highlights) is NOT a seed slide: the seed defines S1–S8 and S11–S18 only, and S9's single field `stories` is `linked: true`, generated deterministically by `storiesOf(p)` from PRJ-34's own brief with a sequential per-project CRS number — so there is no PRJ-34-specific hand-authored S9 cell to trim, and the shared `storiesOf` engine and the generic S9 hint are out of scope (editing them would change all 33 projects). Because this per-slide revision therefore has no editable S9 face, its subject is the OPERATOR'S OWN OVERRIDING INSTRUCTION for the page, verified against the repository: (1) the R-CORE raster icons are created as PNG (`public/r-core/r-core-icon.png` 1448×1086 · `public/r-core/r-core-wordmark.png` 2172×724, both tracked, committed and built into `out/`), referenced verbatim by `rcore-badge.tsx`; and (2) the R-CORE mark's target sits at the BOTTOM of the page in NORMAL FLOW (wrapper `flex w-full justify-center`, no fixed / absolute geometry, mounted last after the footer in `app/SoI-2525/page.tsx`), never overlaying the image or the slide (present mode is a separate `fixed inset-0 z-[60]` overlay above it). Both are confirmed satisfied and committed at HEAD 3fcfc6c; this decision records that assurance as the S9-pass finding rather than fabricating a content change (NO-REWORK / FIX-THE-CLASS: do not invent an edit where the gap is already closed). It CONTINUES the density / clarity discipline of D110–D115 by refusing to overload a page — here refusing to lay the R-CORE mark over the slide. No figure, claim, gate, law or linked number moves (law 3); the master statement and segment value props stay fixed (law 4). It does not supersede any prior decision. Holds security, stability and scalability at ceiling; S9's efficiency and succinctness stay high because the derived table is lean and the mark now rests, proven, below the content.

## Gates
```
== tsc
== drs-render
== drs-crs
== innovation-time
INNOVATION-TIME 3797/3797 passed
INNOVATION-STORE-SECURITY 95/95 passed
```
