# Iteration 0.126 · per-slide S2 (slide 4 of 5 in this pass) · Concept (G1) reframe round · 2026-09-26 · author: twelve lenses · per-slide S2 · G1 reframe

## Twelve lenses

### Aset

SSSES (business case): security 96 · stability 98 · scalability 97 · efficiency 95 · succinctness 92

Restoration reads slide S2, the project overview, and asks whether its enduring truth survives. The status line named the current stage wrong: it opened "G2 / future-state validation" as though a validation gate were the moment, when the venture stands at Concept. This revision restores that truth, so the status now reads "Concept (G1) · future-state validation", and the roadmap gates line keeps its G2 and G3 measurement detail while naming them the forward de-risking gates that run after Concept. Nothing enduring was lost: the four horizons, the one-ninth gate, the top risks all remain. Founder, the overview now tells the reader the honest stage before it tells them the plan.

words: 111

### Asar

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 96 · succinctness 91

Synthesis reads slide S2 as the page that gathers the whole venture into one status: where it stands, the roadmap of horizons, and the top risks. This revision synthesises the reframe the operator asked for: the overview reads as a Concept roadmap-gate artifact, not a validation-gate ask. The status line, which opened "G2", now names the Concept gate; the roadmap keeps the DRS G2 measurement design and G3 A/B detail, presented as the forward de-risking gates that follow Concept. The horizons, the one-ninth absorption gate and the risks are untouched. Founder, the overview and the executive summary now agree: the venture is now at Concept, and the plan is what follows.

words: 111

### Athena

SSSES (business case): security 97 · stability 99 · scalability 98 · efficiency 96 · succinctness 92

Strategy asks which single move on slide S2 buys most for least, and the answer is the status line. A reader who saw "G2 / future-state validation" as the current stage read the venture as further along than it stands, and priced that mismatch. Renaming the stage to Concept, while keeping the G2 measurement gate and G3 A/B as the forward de-risking gates on the roadmap, aligns the overview with where the work truly sits. The strongest board position is always the honest one. Founder, the overview now sets the right expectation: the venture is at the Concept gate, and the de-risking gates run after the concept is approved, not before.

words: 111

### Christo

SSSES (business case): security 96 · stability 98 · scalability 97 · efficiency 95 · succinctness 92

Coherence looks at slide S2 and asks whether every part agrees with S1. Before this revision one part disagreed: the status line said "G2" while the executive summary now placed the venture at Concept. This revision restores wholeness by making the status and the roadmap say one thing: the venture stands at the Concept gate, and the forward de-risking gates keep their names and their detail, correctly placed after concept approval. Founder, the overview and the summary agree again. Investors, the stage on S2 matches the ask on S1. Customers, the one core principle is unchanged, a named human holds command over every outbound action, on this slide and every other.

words: 111

### Enki

SSSES (business case): security 95 · stability 97 · scalability 97 · efficiency 94 · succinctness 90

The edge case I hunt on slide S2 is the sceptic who catches the overview claiming a stage it has not earned. Before this revision the sceptic had a real catch: the status said "G2 / future-state validation" while the summary said Concept, and a mismatch between two adjacent slides makes every horizon suspect. This revision closes the trap by naming the true gate, Concept, and keeping the G2 and G3 gates as the forward de-risking plan they are. The trap I refused this round was reframing the horizon ladder or the risk table, which carry no current-gate label. Founder, the sceptic finds an overview whose stage survives a hostile read.

words: 111

### Enlil

SSSES (business case): security 96 · stability 99 · scalability 98 · efficiency 96 · succinctness 91

Verification is a comparison, and I compared slide S2 to the laws it must obey. First, the reframe scope: the status line and the roadmap gates line are the current-gate references on S2; drs.json project.stage and decisionRequested are already Concept from the S1 pass, so no master field moves again. Second, the hard limit: the Pod row's gate field stays "G2", untouched, as innovation-time.test.mjs requires. Third, laws 3 and 4: no dollar, no linked number, no horizon, no master statement moves. Founder, every check passed this round; only the two S2 seed strings that carry the current-gate reference changed. Investors, nothing outside the reframe, and no test at all, was disturbed.

words: 111

### Krishna

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 95 · succinctness 91

Integration reads slide S2 as the seam between the executive summary and the detail slides. The reframe strengthens that seam: the status line and the S1 ask now carry one stage, Concept, so the two slides never disagree; the roadmap's G2 and G3 gates bind forward to the validation slides (S11 · S16). This revision re-cuts the seam once, on S2's own two strings, and leaves the horizons and risks that bind to S3, S13 and S16 as they were. Founder, S1 and S2 tell one story. Investors, the stage in the overview and the ask on the summary are the same claim. Customers, the loop and the principle are unchanged.

words: 111

### Odin

SSSES (business case): security 96 · stability 98 · scalability 97 · efficiency 95 · succinctness 91

Read in twenty-five twenty-five, slide S2 is the page a distant reader opens to learn what stage this venture had reached. Foresight is telling that reader the truth about the moment. Before this revision the status claimed a validation stage while standing at Concept, a small dishonesty that would confuse a distant reader centuries on. This revision names the true gate, Concept, and keeps the de-risking gates as the forward path on the roadmap. The overview now survives the centuries because it never pretends to a stage it had not reached. Founder, the far reader learns the honest stage. Investors, the overview will still read true when the outcome is known.

words: 111

### Pangu

SSSES (business case): security 95 · stability 97 · scalability 97 · efficiency 94 · succinctness 90

Subtraction has a floor, the thing itself, and on slide S2 the thing is the one honest stage. It does not add a claim or cut a fact; it corrects a label so the status names its true gate. The primordial move is to break open the false framing, the status opening "G2 / future-state validation", and let the true one stand, Concept, with the validation gates named forward on the roadmap. Nothing load-bearing was removed: the horizons, the one-ninth gate, the roadmap detail and the five top risks all remain. Founder, the floor is clean and the stage is true. Investors, no horizon was padded and no risk was hidden.

words: 111

### Sofia

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 96 · succinctness 92

Twelve readers open slide S2 differently, and the reframe serves them all. The founder wanted the overview's stage to match the summary; it now does. The investor wanted to know what stage the venture is at; it is Concept, not a validation gate. The system thinker wanted the forward path legible; the G2 measurement gate and G3 A/B are named as the forward de-risking gates on the roadmap. The customer wanted the core principle unchanged; a named human still holds command. Founder, every lens now reads one consistent stage. Investors, the overview states the stage that fits the moment. Customers, nothing about how the platform is governed changed on this page.

words: 111

### Thoth

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 96 · succinctness 92

The numbers behind slide S2 do not move this revision, and that is the finding. The horizons 2027, 2030 and 2525, the one-ninth (11.1 %) gate, the top-risk table all stand exactly as the record declares. What changed is two labels, not any quantity: the status line now says Concept where it said G2, and the roadmap's G2 and G3 gates are named as the forward de-risking plan. I checked: no dollar, no linked number, no horizon date was touched, and none of it was. The stale "rev 0.107" printed in the status is left as the record has carried it. Founder, every figure on the overview is as it was.

words: 111

### Thor

SSSES (business case): security 97 · stability 99 · scalability 98 · efficiency 96 · succinctness 92

Risk on slide S2, as the guardian reads it: the overview is where a diligence team checks stage against ask, so a false stage claim here is exposure. Before this revision the status said "G2" while the summary said Concept, and that gap between two adjacent slides is the kind diligence teams exploit. This revision closes it by naming the true gate and keeping the validation gates as the forward de-risking plan, without touching the Pod row's structural gate field a test guards. Founder, the overview no longer over-claims its stage. Investors, the stage survives diligence because it matches the record. The core control, a named human in command, is unmoved.

words: 111

## Spiral test

- Future demo: from S2 alone — the venture is at the Concept (G1) gate; the roadmap runs govern first (2027) → the critical-event surface absorbed under the one-ninth (11.1 %) gate, alerting last (2030) → Enterprise Decision Graph → Recursive Readiness (2525), and the forward de-risking gates (G2 measurement design, G3 instrumented A/B) run after Concept. Still cannot: see the G2 tabletop clocks or the G3 A/B deltas stamped — those are empty until the de-risking work runs (S11 · S16).
- Customers: from S2 — the top risks that govern how the one platform is built (govern before absorbing, alerting last and certified, degraded mode where a completed approval is never discarded, a named human on every outbound action). Still cannot: see the customer's own data terms, which enter through S14 and legal review.
- Innovators who want to add functionality: from S2 — the roadmap horizons and the one-ninth gate any absorbed function must clear before it goes native. Still cannot: see the per-object interface shape; that is on S6.
- Investors: from S2 — the venture's true stage (Concept, matching S1) and the forward de-risking gates named on the roadmap; the risks are stated plainly, not buried. Still cannot: see buyer-validated value — the sizing waits for the G2/G3 de-risking work (S11 · S16).
- Business owners: from S2 — a plain reading that this is a concept being advanced, not a late-stage validation, and the top risks that would stop it. Still cannot: see the resourcing detail, which is S14.
- System thinkers without analytics know-how: from S2 — the stage, the roadmap of horizons and the top risks in plain terms, no math required. Still cannot: read a probability or dollar per horizon; those are DECLARED and sized later.
- Execution geniuses: from S2 — the ordered roadmap (govern → absorb by the one-ninth gate → decision graph → recursive readiness) and the forward de-risking gates as the sequence to execute against. Still cannot: see owners and dates per horizon; those are assigned as each gate is passed.
- Public bodies (the brief's R-CORE section): from S2 — the same governed roadmap and named-human control a public project would inherit, and the risk discipline that governs it. Still cannot: see a public-body-specific step; it enters after G3 (D12) through the same loop.

## AAR

Concept (G1) reframe note (operator 2026-09-26 "Make G1", this whole round): the deck must READ as a CONCEPT (G1) roadmap-gate artifact — the project sits at the G1 Concept gate of the ladder (G1 Concept → G2 Plan → G3 Develop → G4 Qualify → G5 Launch → G6 Maximize), the ask is to pass Concept and advance to Plan, and the deck's existing "G2" wording is the De-Risking-Strategies VALIDATION gate (G2 measurement design, G3 instrumented A/B), kept as the FORWARD de-risking plan that runs after Concept. **Slide S2 (the project overview) IS a home of the current-gate framing**: its `status` cell opened "G2 / future-state validation" (the current-stage label), and its `roadmap` cell prints the DRS "Gates: G2 … · G3 …" line. Note S1/S2 are the home of `project.stage` / `decisionRequested`, but the S1 pass (rev 0.125) already reframed both to Concept (G1) — they are byte-checked as already-Concept this round and NOT re-edited (append-only, no duplicate edit). So this S2 pass applies the reframe to the two S2 seed strings only. The HARD LIMITS are honoured: the Pod row's `gate` field in innovation-data.ts stays "G2" (asserted by innovation-time.test.mjs:369), no test is edited, and the roadmap G1–G6 ladder labels are not changed — only the CURRENT-gate framing.

No-rework finding (R-CORE law): grep-verified that the current-gate references on S2's path are exactly two seed strings — S2.status.hi/ai opening "G2 / future-state validation" (2 occurrences) and S2.roadmap.hi/ai "Gates: G2 validates the measurement design … · G3 runs the instrumented A/B." (2 occurrences). drs.json project.stage / decisionRequested are already Concept (S1 pass, rev 0.125); the two "G2 / future-state validation" strings remaining in drs.json are in decisions[] and iterations[] — the append-only record, which law 7 forbids editing. No parallel implementation is forked.

Slide S2 (project overview — status · roadmap · toprisks) — keep | fix | cut, per cell:

- `status` (`hi` + `ai`) — **fix.** Both opened "G2 / future-state validation", naming a forward validation gate as the current stage. Reframed to "Concept (G1) · future-state validation" (gate label only; the customer-facing product, the internal method, the edition, the provenance and the append-only-register note are all unchanged). Field shapes unchanged (both strings); `ai` stays ≥ `hi`.
- `roadmap` (`hi` + `ai`) — **fix.** The "Gates: G2 validates the measurement design … · G3 runs the instrumented A/B." line named the DRS validation gates without placing them relative to the Concept gate. Reframed to "Forward de-risking gates (after the Concept gate): G2 validates the measurement design … · G3 runs the instrumented A/B." — the DRS G2/G3 detail is kept verbatim, now presented as the forward de-risking plan the operator asked for. The four horizons and the validation-sequence line (ai) are unchanged. Field shapes unchanged (both string[]); `ai` stays ≥ `hi`.
- `roadmap` horizons + validation-sequence (ai) — **keep.** The 2027/2030/2525 horizons and the one-ninth gate are the absorption ladder, not the current roadmap gate; no current-gate label. No edit.
- `toprisks` (`hi` + `ai`) — **keep.** The five risks and their mitigations carry no current-gate reference; law-3 claims (govern first, alerting last, one-ninth gate, named human) unchanged. No edit.

Edits applied this revision (≤9; the Concept (G1) reframe edits plus the mandated law-5 provenance bump and law-7 append-only record; no dollar / linked number / horizon / master statement / segment value prop moves — laws 3–4; no cell emptied, no field shape changed, ai ≥ hi — law 6):

1. seed · S2 `status.hi` · "G2 / future-state validation · customer-facing product CrisisCommand.ai …" → "Concept (G1) · future-state validation · customer-facing product CrisisCommand.ai …" (anchor on the hi-unique "ONE PLATFORM · rev 0.107" tail) · G1 reframe (status stage label → Concept).
2. seed · S2 `status.ai` · same "G2 / future-state validation" → "Concept (G1) · future-state validation" (anchor on the ai-unique "ONE PLATFORM (operator 2026-09-24 + the twelve-lens review)" tail) · G1 reframe.
3. seed · S2 `roadmap.hi` + `roadmap.ai` · "Gates: G2 validates the measurement design (interviews, NBA teardown, prototype, one pre-registered EDU tabletop) · G3 runs the instrumented A/B." → "Forward de-risking gates (after the Concept gate): G2 validates the measurement design (interviews, NBA teardown, prototype, one pre-registered EDU tabletop) · G3 runs the instrumented A/B." (both occurrences, count-asserted 2) · G1 reframe (DRS G2/G3 kept as the forward de-risking plan).
4. seed · `PROV` constant · rev 0.125 → 0.126 (anchor once) · law 5.
5. `innovation-data.ts` · row `provenance` · rev 0.125 → 0.126 (anchor once) · law 5. (The row's `gate` field stays "G2" — HARD LIMIT, innovation-time.test.mjs:369.)
6. `drs.v00.00.json` · `project.revision` · 0.125 → 0.126 · law 7 (renderer refuses a master whose project.revision is not the last revisions[] entry).
7. `drs.v00.00.json` · append-only record · fill revisions[] 0.125 commit PENDING → f858aa4; append iterations[] 0.126, decisions[] D128, revisions[] 0.126 (kind release, commit PENDING) · law 7. (project.stage / decisionRequested already Concept from rev 0.125 — not re-edited.)

Decisions:

- D128 — Per-slide S2 pass (rev 0.126), Concept (G1) reframe round (operator 2026-09-26 "Make G1"), slide 4 of 5 in this pass. Slide S2 is the project overview of the founder-facing Innovation-Pod deck (innovation-slide-seed-drs.ts S2: status · roadmap · toprisks). Like S1 (D127), S2 IS a home of the current-gate framing, so this pass applies the reframe to its two current-gate seed strings: (a) the S2 `status` cell (hi + ai) opening "G2 / future-state validation" — the current-stage label — becomes "Concept (G1) · future-state validation" (gate label only; the customer-facing product, internal method, edition, provenance and register note unchanged); (b) the S2 `roadmap` gates line (hi + ai) "Gates: G2 validates the measurement design … · G3 runs the instrumented A/B." becomes "Forward de-risking gates (after the Concept gate): G2 validates the measurement design … · G3 runs the instrumented A/B." — the DRS G2/G3 detail kept verbatim, now presented as the forward de-risking plan that runs after Concept, as the operator directed. drs.v00.00.json project.stage and decisionRequested are NOT re-edited: the S1 pass (D127, rev 0.125) already reframed both to Concept (G1), and this round byte-verifies them as already-Concept (append-only, no duplicate edit). HARD LIMITS honoured: the Pod row's `gate` field in innovation-data.ts stays "G2" (asserted by innovation-time.test.mjs:369, drives the financial-year horizon); no test edited; the roadmap G1–G6 ladder labels unchanged — only the current-gate framing. NO-REWORK (R-CORE law): the reframe touches only the two S2 strings carrying the current-gate reference; the horizons, the one-ninth gate and the top-risk table are left as-is; no parallel implementation is forked. Laws held: no horizon, the one-ninth (11.1 %) figure, timing label, claim, law or linked financialModel number moves (law 3); the master statement (futureState.master.statement === valueProp) and the three segment value props stay fixed (law 4); no cell emptied and no field shape changed, ai ≥ hi (law 6); the append-only record grows only (law 7). Mandatory law-5 provenance bump (PROV constant + row provenance, rev 0.125 → 0.126) and law-7 record (fill revisions[] 0.125 commit → f858aa4; append iterations[] 0.126, decisions[] D128, revisions[] 0.126 PENDING; set project.revision 0.126). It does not supersede any prior decision — it completes on S2 the current-gate reframe that D127 applied on S1, on the same reframe-round mandate.

## Gates

(filled by the door)
