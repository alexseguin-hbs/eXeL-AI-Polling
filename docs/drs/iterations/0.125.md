# Iteration 0.125 · per-slide S1 (slide 3 of 5 in this pass) · Concept (G1) reframe round · 2026-09-26 · author: twelve lenses · per-slide S1 · G1 reframe

## Twelve lenses

### Aset

SSSES (business case): security 96 · stability 98 · scalability 97 · efficiency 95 · succinctness 92

Restoration reads slide S1, the executive summary, as the page a far reader opens first, and finds the enduring truth intact: one human-governed platform, the master statement printed once, the operating loop, the absorption ladder. What this revision restores is honesty about where the venture stands. The ask no longer says "G2 ask" as though a validation gate were the request; it says the true thing, that this is the Concept gate and the ask is to approve the concept and advance to Plan, with the de-risking G2 and G3 gates named as the forward plan. Founder, the summary now tells the reader which gate they are being asked to pass.

words: 111

### Asar

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 96 · succinctness 91

Synthesis reads slide S1 as the page where every other slide is gathered into one reading: value proposition, benchmark, market table, strategy and the ask. This revision synthesises the reframe operator asked for on the twenty-sixth: the deck now reads as a Concept roadmap-gate artifact rather than a validation-gate ask. The master question, a shared constant, carries its new label once and every carrier updates together; the S1 ask and the project header say Concept, advance to Plan; the DRS G2 and G3 detail survives as the forward de-risking plan. Founder, the whole case still resolves to one page, and that page now names the correct gate for the correct decision.

words: 111

### Athena

SSSES (business case): security 97 · stability 99 · scalability 98 · efficiency 96 · succinctness 92

Strategy asks which single move on slide S1 buys most for least, and the answer is the reframe of the ask. A reader who saw "G2 ask" on the summary read the venture as further along than it is, and an investor priced that mismatch. Renaming the current ask to the Concept gate, while keeping the G2 measurement gate and G3 A/B as the forward plan, aligns what is requested with where the work stands. The strongest board position is the honest one. Founder, the ask now sets the right expectation: approve the concept, advance to Plan, and let the de-risking gates run after, not before, the decision you are making.

words: 111

### Christo

SSSES (business case): security 96 · stability 98 · scalability 97 · efficiency 95 · succinctness 92

Coherence looks at slide S1 and asks whether every part agrees. Before this revision one part disagreed: the summary asked for a "G2" decision while the ladder placed the venture at Concept. This revision restores wholeness by making the ask, the master question and the header all say one thing: the request is to pass Concept and advance to Plan. The forward de-risking gates keep their names and their detail, now correctly placed after the concept is approved. Founder, the parts agree again. Investors, the ask matches the stage. Customers, the one core principle is unchanged, a named human holds command over every machine action, on this slide and every other.

words: 111

### Enki

SSSES (business case): security 95 · stability 97 · scalability 97 · efficiency 94 · succinctness 90

The edge case I hunt on slide S1 is the sceptic who catches the venture claiming a stage it has not earned. Before this revision the sceptic had a real catch: the ask said "G2" while the ladder said Concept, and a mismatch like that makes every other number suspect. This revision closes the trap by naming the true gate, Concept, and keeping the G2 and G3 validation gates as the forward plan they are. The trap I refused this round was over-reaching the reframe into cells that carry no gate label. Founder, the sceptic finds an ask that matches the stage, and a summary whose honesty survives a hostile read.

words: 111

### Enlil

SSSES (business case): security 96 · stability 99 · scalability 98 · efficiency 96 · succinctness 91

Verification is a comparison, and I compared slide S1 to the laws it must obey. First, the reframe scope: drs.json is touched only for project.stage and decisionRequested, and S1 is their home, so it is in bounds; the Pod row's gate field stays "G2", untouched, as the hard limit and innovation-time.test.mjs require. Second, one master: the master question is a shared constant, so relabelling it Concept updates every use consistently, which is correct. Third, laws 3 and 4: no dollar, no linked number, no master statement or segment value prop moves. Founder, every check passed; the only fields changed are those the reframe names. Investors, nothing outside the reframe was disturbed.

words: 111

### Krishna

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 95 · succinctness 91

Integration reads slide S1 as the seam where the deck meets its first reader. The reframe strengthens that seam: the master question, printed on S1 and reused on S16, now carries one label, so the two slides never disagree about the gate; the S1 ask and the project header say Concept together; the DRS G2 and G3 gates bind forward to the validation slides where they live. This revision re-cuts the seam once, at the shared constant, and propagates. Founder, S1 and S16 still tell one story. Investors, the ask on the summary and the stage in the header are the same claim. Customers, the loop and the principle are unchanged.

words: 111

### Odin

SSSES (business case): security 96 · stability 98 · scalability 97 · efficiency 95 · succinctness 91

Read in twenty-five twenty-five, slide S1 is the page a distant reader opens to learn what this venture asked for. Foresight is telling that reader the truth about the moment. Before this revision the summary asked for a validation-gate decision while standing at Concept, a small dishonesty that would confuse a reader centuries on. This revision names the true gate, Concept, and advance to Plan, and keeps the de-risking gates as the forward path. The page now survives the centuries because it never pretends to a stage it had not reached. Founder, the far reader learns the honest ask. Investors, the summary will still read true when the outcome is known.

words: 111

### Pangu

SSSES (business case): security 95 · stability 97 · scalability 97 · efficiency 94 · succinctness 90

Subtraction has a floor, the thing itself, and on slide S1 the thing is the one honest ask. It does not add a claim or cut a fact; it corrects a label so the ask names its true gate. The primordial move is to break open the false framing, "G2 ask", and let the true one stand, the Concept gate, advance to Plan, with the validation gates named forward. Nothing load-bearing was removed: the master statement, the loop, the ladder, the market table all remain. Founder, the floor is clean and the ask is true. Investors, no number was padded and no claim was inflated to make the summary look advanced.

words: 111

### Sofia

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 96 · succinctness 92

Twelve readers open slide S1 differently, and the reframe serves them all. The founder wanted the ask to match the stage; it now does. The investor wanted to know exactly what decision is requested; it is the Concept gate, advance to Plan, not a validation gate. The system thinker wanted the forward path legible; the G2 measurement gate and G3 A/B are named as what runs after concept approval. The customer wanted the core principle unchanged; a named human holds command. Founder, every lens now reads one consistent ask. Investors, the summary requests the decision that fits the moment. Customers, nothing about how the platform is governed changed on this page.

words: 111

### Thoth

SSSES (business case): security 96 · stability 98 · scalability 98 · efficiency 96 · succinctness 92

The numbers behind slide S1 do not move this revision, and that is the finding. The single-customer baseline, the ramp, the ten-year figures, the one-ninth percentage, all stand as the financial model declares. What changed is a set of labels, not a set of quantities: the ask, the master question and the project header now say Concept where they said G2, and the de-risking G2 and G3 gates are named as the forward plan. I checked: no dollar and no linked number was touched, and none was. Founder, every figure on the summary is as it was. Investors, the reframe is a truth about stage, not a change to the model.

words: 111

### Thor

SSSES (business case): security 97 · stability 99 · scalability 98 · efficiency 96 · succinctness 92

Risk on slide S1, as the guardian reads it: the summary is the first thing an investor or hostile reader sees, so a false stage claim here is the whole deck's exposure. Before this revision the ask said "G2" while the ladder said Concept, and that gap is the kind a diligence team exploits. This revision closes it by naming the true gate and keeping the validation gates as the forward plan, without touching the Pod row's structural gate field a test guards. Founder, the summary no longer over-claims its stage. Investors, the ask survives diligence because it matches the record. The core control, a named human in command, is unmoved.

words: 111

## Spiral test

- Future demo: from S1 alone — the venture is at the Concept (G1) gate of the roadmap ladder (G1 Concept → G2 Plan → G3 Develop → G4 Qualify → G5 Launch → G6 Maximize) and is asking to pass Concept and advance to Plan by approving the concept and its de-risking plan; the plan then runs the DRS validation gates (G2 measurement design, G3 instrumented A/B). Still cannot: see the G2 tabletop clocks or the G3 A/B deltas stamped — those are the forward de-risking work, empty until run (S11 · S16).
- Customers: from S1 — one human-governed platform where leaders see facts, decide with named authority, act, hold every stakeholder to one approved posture, prove it and learn; a named human holds command over every machine action. Still cannot: see the customer's own data-handling terms, which enter through the S14 objects and legal review.
- Innovators who want to add functionality: from S1 — the loop and the ten leadership deltas anything they add must serve, and the one-ninth gate any absorbed function must clear. Still cannot: see the per-object interface shape; that is on S6.
- Investors: from S1 — the exact decision requested (pass Concept, advance to Plan) now matches the stage in the header, and the forward de-risking gates are named; the DECLARED single-customer baseline anchors the case. Still cannot: see buyer-validated value — S1 is DECLARED (law 3), the sizing waits for the G2/G3 de-risking work.
- Business owners: from S1 — a plain reading that the ask is to approve a concept and advance it, not to fund a late-stage validation, and that a human stays in command. Still cannot: see the resourcing detail, which is S14.
- System thinkers without analytics know-how: from S1 — the operating loop in one line and the ask in plain terms, no math required. Still cannot: read a probability or dollar per delta; those are DECLARED and sized later.
- Execution geniuses: from S1 — the ordered ask (approve concept → advance to Plan → run the de-risking G2/G3 gates) as the sequence to execute against. Still cannot: see owners and dates per step; those are assigned as the gate is passed.
- Public bodies (the brief's R-CORE section): from S1 — the same one platform and named-human-in-command control a public project would inherit, opened through the eXeL Polling API. Still cannot: see a public-body-specific ask; it enters after G3 (D12) through the same loop.

## AAR

Concept (G1) reframe note (operator 2026-09-26 "Make G1", this whole round): the deck must READ as a CONCEPT (G1) roadmap-gate artifact — the project sits at the G1 Concept gate of the ladder (G1 Concept → G2 Plan → G3 Develop → G4 Qualify → G5 Launch → G6 Maximize), the ask is to pass Concept and advance to Plan, and the deck's existing "G2" wording is the De-Risking-Strategies VALIDATION gate (G2 measurement design, G3 instrumented A/B), kept as the FORWARD de-risking plan that runs after Concept. **Slide S1 (the executive summary) IS a home of the current-gate framing**: its `ask` cell opened "G2 ask", the shared `MASTER_Q` constant it prints carried the label "Master question (G2)", and S1 is a home of `project.stage` / `decisionRequested` (with S2). So this S1 pass DOES apply the reframe (unlike the S19 pass, which carried no gate reference). The HARD LIMITS are honoured: the Pod row's `gate` field in innovation-data.ts stays "G2" (asserted by innovation-time.test.mjs:369), no test is edited, and the roadmap G1–G6 ladder labels are not changed — only the CURRENT-gate framing.

No-rework finding (R-CORE law): grep-verified that the "G2 ask" / "Master question (G2)" / project.stage "G2 / future-state validation" strings are the current-gate references on S1's path (`innovation-slide-seed-drs.ts` S1.ask.hi/ai + the `MASTER_Q` const; `drs.v00.00.json` project.stage / decisionRequested). MASTER_Q is a shared constant (used in S1.ask.ai and S16) — relabelling it once updates every use consistently (the reframe explicitly names this as correct, one master). No parallel implementation is forked.

Slide S1 (executive summary — oneline · segment · market · strategy · ask) — keep | fix | cut, per cell:

- `oneline` (operating loop + absorption ladder 2027/2030/2525) — **keep.** Carries no current-gate "G2" label; the 2027/2030/2525 horizons are the absorption ladder, not the roadmap gate ladder. No edit.
- `segment` (benchmark + three NBAs) — **keep.** No gate reference. No edit.
- `market` (three-segment table + S17 market model) — **keep.** No gate reference; law-3 linked table unchanged. No edit.
- `strategy` (ONE-platform thesis + ten deltas) — **keep.** References the one-ninth absorption gate (2030), not the current roadmap gate; no "current ask" label. No edit.
- `ask` (`hi` + `ai`) — **fix.** Both opened "G2 ask", framing a forward validation gate as the current request. Reframed to the Concept-gate ask (pass Concept, advance to Plan), keeping the DRS G2 measurement gate and G3 A/B as the forward de-risking plan and keeping all eight approval items, the NBA definition and the master question. Field shapes unchanged (both strings); `ai` stays ≥ `hi`.
- `MASTER_Q` const (printed on S1.ask.ai and S16) — **fix.** Label "Master question (G2)" → "Master question (Concept · G1)" (gate label only; the question is byte-identical otherwise). One master, every use updates together.

Edits applied this revision (≤9; the Concept (G1) reframe edits plus the mandated law-5 provenance bump and law-7 append-only record; no dollar / linked number / master statement / segment value prop moves — laws 3–4; no cell emptied, no field shape changed — law 6):

1. seed · `MASTER_Q` const · `Master question (G2):` → `Master question (Concept · G1):` (anchor once) · G1 reframe, one master (updates S1.ask.ai + S16 consistently).
2. seed · S1 `ask.hi` · reframed "G2 ask: …" → "Concept-gate ask (Concept · G1): approve the concept and its de-risking plan and advance to Plan. The de-risking plan then runs the three segment teardowns …, at the forward DRS G2 measurement gate; G3 A/B … after the G2 measurement design is credible." · G1 reframe (current ask = Concept; G2/G3 kept as forward plan).
3. seed · S1 `ask.ai` · reframed opening "G2 ask (G2 · G3 are the two De-Risking Strategies validation gates …): approve 1 EDU …" → "Concept-gate ask (Concept · G1 — the current ask is to pass the Concept gate and advance to Plan …; G2 · G3 are the two forward … gates that run after Concept …): approve the concept and its de-risking plan, whose steps are 1 EDU …" · G1 reframe (rest of the eight items + MASTER_Q + G2 exit unchanged).
4. `drs.v00.00.json` · `project.stage` · "G2 / future-state validation" → "Concept (G1) · future-state validation" · G1 reframe (S1/S2 is its home; regenerated by drs-render, --check stays green).
5. `drs.v00.00.json` · `project.decisionRequested` · reframed opening "G2 ask (v1.0) — approve:" → "Concept-gate ask (Concept · G1, v1.0) — pass the Concept gate and advance to Plan by approving the concept and its de-risking plan, which then runs the forward DRS validation gates (G2 measurement design, G3 A/B) — approve:" (the eight approval items and the master question kept) · G1 reframe.
6. `drs.v00.00.json` · `project.revision` · 0.124 → 0.125 · law 7 (renderer refuses a master whose project.revision is not the last revisions[] entry).
7. seed · `PROV` constant · rev 0.124 → 0.125 (anchor once) · law 5.
8. `innovation-data.ts` · row `provenance` · rev 0.124 → 0.125 (anchor once) · law 5. (The row's `gate` field stays "G2" — HARD LIMIT, innovation-time.test.mjs:369.)
9. `drs.v00.00.json` · append-only record · fill revisions[] 0.124 commit PENDING → f6bd8d5; append iterations[] 0.125, decisions[] D127, revisions[] 0.125 (kind release, commit PENDING) · law 7.

Decisions:

- D127 — Per-slide S1 pass (rev 0.125), Concept (G1) reframe round (operator 2026-09-26 "Make G1"), slide 3 of 5 in this pass. Slide S1 is the executive summary of the founder-facing Innovation-Pod deck (innovation-slide-seed-drs.ts S1: oneline · segment · market · strategy · ask). Unlike the per-slide S18 (D125) and S19 (D126) passes, which found no current-gate reference and reduced to law-6 optimization, S1 IS a home of the current-gate framing, so this pass applies the reframe: (a) the S1 `ask` cell (hi + ai) is reframed from "G2 ask" to the Concept-gate ask — pass the Concept (G1) gate and advance to Plan by approving the concept and its de-risking plan — while KEEPING the DRS G2 measurement gate and G3 A/B as the FORWARD de-risking plan, all eight approval items, the NBA definition and the master question; (b) the shared `MASTER_Q` constant's label "Master question (G2)" becomes "Master question (Concept · G1)" (gate label only, question byte-identical), which — one master — updates its use on S1.ask.ai and S16 consistently, as the reframe explicitly names correct; (c) drs.v00.00.json project.stage "G2 / future-state validation" → "Concept (G1) · future-state validation" and decisionRequested's "G2 ask (v1.0) — approve:" opening → a Concept-gate framing (S1/S2 is their home; regenerated by drs-render, --check green). HARD LIMITS honoured: the Pod row's `gate` field in innovation-data.ts stays "G2" (asserted by innovation-time.test.mjs:369, drives the financial-year horizon); no test edited; the roadmap G1–G6 ladder labels unchanged — only the current-gate framing. NO-REWORK (R-CORE law): the reframe touches only the strings that carry the current-gate reference; no parallel implementation is forked. Laws held: no horizon, the one-ninth (11.1 %) figure, timing label, claim, law or linked financialModel number moves (law 3); the master statement (futureState.master.statement === valueProp) and the three segment value props stay fixed (law 4); no cell emptied and no field shape changed, ai ≥ hi (law 6); the append-only record grows only (law 7). Mandatory law-5 provenance bump (PROV constant + row provenance, rev 0.124 → 0.125) and law-7 record (fill revisions[] 0.124 commit → f6bd8d5; append iterations[] 0.125, decisions[] D127, revisions[] 0.125 PENDING; set project.revision 0.125). It does not supersede any prior decision — it completes on S1 the current-gate reframe that D125 (S18) and D126 (S19) recorded as not-applicable on those slides.

## Gates

(filled by the door)
