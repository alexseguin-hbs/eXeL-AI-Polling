# Iteration 0.123 · per-slide S18 (slide 1 of 5 in this pass) · Concept (G1) reframe round · 2026-09-26 · author: twelve lenses · per-slide S18 · G1 reframe

## Twelve lenses

### Aset

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 89

Restoration reads slide S18 as the deck's promise honoured at the very end: a governance capability retires only after its successor keeps the governed record intact, and nothing life-critical is switched off without a named successor channel. The whole history, decisions, plan versions, evidence and outcome-linked replay, leaves in one open export that is the customer's, never the vendor's. For a public body that export is its citizens' record, opened through the eXeL Polling API so the people who answered can replay what was decided and why. This revision draws the decision-register line back to its core claim, register and ledger travel with the record. Founder, memory outlives the vendor here.

words: 111

### Asar

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 89

Synthesis reads slide S18 as the page where the whole venture hands itself back: at end-of-life the platform does not hoard, it returns, exporting the decision history, the plan versions, the evidence and the outcome-linked replay to the customer in an open format, absorbed functions retired in the reverse order of absorption. The loop that began at the first crisis closes cleanly, the last event's replay carried into the successor so the next crisis never restarts from a blank page. This revision resolves the one crowded seam, the decision-register line trimmed to its core claim, its elaboration and a stale count moved off the face. Founder, the whole record returns intact.

words: 111

### Athena

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Strategy asks which single move on slide S18 buys most for least, and again the answer is a cut. The page already carries the discipline that matters at exit: retire only behind a successor that keeps the record, export everything in an open format, hand the citizens' record back through the Polling API. But the decision-register line dragged a trailing elaboration and a stale range, D1 to D107, while the register itself has run far past it. Trimming the line to its core claim, the elaboration kept in the note and the drifting number dropped, restores the panel and removes a defect that would have aged badly. Founder, subtraction won again.

words: 111

### Christo

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Coherence looks at slide S18 and finds every part agreeing on one honest thing: at the end the record belongs to the customer, and the vendor leaves without taking the institution's memory with it. The retirement rule, the open export, the reverse-order phase-out with a named successor channel, and the citizens' record opened through the Polling API all point the same way, toward transparency and accountability that outlive the vendor. This revision keeps that wholeness by mending a frayed edge, the decision-register line, which carried a trailing elaboration and a stale count that no longer matched the growing register. Founder, the whole holds. Investors, the exit is clean. Customers, memory stays.

words: 111

### Enki

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 97 · succinctness 88

The edge case I hunt on slide S18 is the buyer who fears lock-in, who reads an end-of-life plan and suspects the vendor keeps the keys. The page closes that fear: every absorbed function's data leaves with the customer in the same open export, partnered rails hand back cleanly, and decision rights and approval history export so authority never reverts to memory. The trap I found this round was a smaller one, a stale count on the decision-register line, D1 to D107, that a reader could check against the ledger and catch drifting. This revision shuts it, the line states its core claim, the number dropped. Founder, no trap survives here.

words: 111

### Enlil

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 98 · succinctness 91

Verification is a comparison, and I compared slide S18 to two laws it must obey. First, density: a cell longer than it needs is a defect and detail belongs in the note. Second, honesty: no number on the deck may quietly go stale. The decision-register line failed both, carrying a trailing elaboration its siblings do not and a pinned range, D1 to D107, while the register has grown past D124. This revision corrects both, the line states its core claim, the elaboration preserved verbatim in the note so the note stays longer than the slide, and the drifting count removed rather than re-pinned. Founder, the defect is closed. Investors, nothing moved.

words: 111

### Krishna

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 89

Integration reads slide S18 as the seam where the venture's whole record meets its own departure: each exit clause binds a capability to the successor that must keep its record, each open export binds the customer to data that stays theirs, the citizens' record binds a public body to the people it serves through the Polling API. This revision strengthens that seam by mending its one loose joint, the decision-register line, which trailed an elaboration and a stale range while the register itself kept growing. Drawn back to its core claim, the elaboration kept in the note, the join holds. Founder, every clause ties to its successor. Investors, the exit holds.

words: 111

### Odin

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 89

Read in twenty-five twenty-five, slide S18 is the page a distant reader opens to ask whether this venture knew how to leave without erasing what it learned, and finds that it did: the record is exported in an open format, the citizens' record returned through the Polling API, the last event's replay carried into the successor so no crisis restarts from a blank page. The page survives the centuries only if it stays legible and never lies, so a decision-register line carrying a count that drifts with every round would betray a far reader. This revision drops the count and trims the line. Founder, the exit endures readable. Investors, foresight lasts.

words: 111

### Pangu

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 88

Subtraction has a floor, the thing itself, and on slide S18 the thing is the clean return: retire behind a successor, export everything open, hand the citizens' record back, carry the last replay forward. Nothing on that floor needs cutting, for each is real and load-bearing at exit. But one element sat wrongly above it, the decision-register line, a trailing elaboration and a stale count crowding a claim that stands on its own. This revision removes the clutter without touching the substance, the line states that the register and the ledger travel with the record, the rest moved to the note. Founder, the floor is clean. Investors, no clause cut here.

words: 111

### Sofia

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Twelve readers open slide S18 differently, and each is served at the end: the founder wants proof the venture leaves without lock-in; the investor wants an orderly wind-down that protects the asset's value; the buyer wants every byte of the record handed back in an open format; the public servant wants the citizens' record returned through the Polling API so accountability outlives the vendor. This revision serves them all by clearing the one obstacle in their common path, the decision-register line, which carried a trailing elaboration and a stale range now trimmed to its core claim. Founder, every lens finds its own exit. Investors, none of it inflated at all here.

words: 111

### Thoth

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 98 · succinctness 91

The numbers behind slide S18 barely move this revision, and that is the point: the retirement rule, the open export, the reverse-order phase-out and the citizens' record all stay exactly as they stood. What I checked is a number that should not have stood, the decision-register line's pinned range, D1 to D107, a count that drifts every round as the register grows and that a diligent reader could catch out of date. Rather than re-pin a moving figure, this revision names the register without a range, so the line can never go stale, and moves its elaboration to the note. Founder, the whole page is exact. Investors, nothing counts wrong now.

words: 111

### Thor

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 91

Risk on slide S18, as the guardian reads it: the danger at end-of-life is a buyer who suspects the vendor keeps the record, or a reader who catches a stale number and doubts the rest. The rigor is real: nothing life-critical is switched off without a named successor channel, every absorbed function's data leaves in the open export, the citizens' record returns through the Polling API. But the decision-register line pinned a range, D1 to D107, that had drifted past the growing register. This revision shuts that risk down, the line states its core claim, the count dropped, the elaboration kept in the note. Founder, the seam is fully closed here.

words: 111

## Spiral test

- Future demo: from S18 alone — the venture knows how to leave: a governance capability retires only after its successor keeps the governed record intact, nothing life-critical is switched off without a named successor channel, and the whole record (decisions, plan versions, evidence, outcome-linked replay) exports to the customer in an open format. Still cannot: see a retirement dated or priced — S18 is a DECLARED template slide (not in v0.9), direction not a commitment (law 3).
- Customers: from S18 — that the record is theirs, never the vendor's: every absorbed function's data leaves in the same open export, partnered rails (Rave, PagerDuty, quality / recall, legal) hand back cleanly, and decision rights and approval history export so authority never reverts to memory. Still cannot: see the export's file format named — the slide says "open format", the schema is the S14 Horizon-1 objects (referenced, not itemised on this face).
- Innovators who want to add functionality: from S18 — that anything they add must, at end-of-life, leave with the customer in the open export and hand back cleanly, so no extension can become a lock-in. Still cannot: see the export API contract; the note names the record, not a per-field egress schema.
- Investors: from S18 — that the wind-down protects asset value: an orderly reverse-order phase-out, no life-critical channel dropped without a named successor, and a complete open export, so a retirement never destroys the institutional memory the platform built. Still cannot: see a cost or duration for end-of-life — the phase-out ceiling (≤ 3 yrs, S18 risk row in innovation-data.ts) lives in the gate-review matrix, not on this narrative face; the horizons are IA direction (law 3).
- Business owners: from S18 — that Organizational Context and institutional memory are handed over intact, the decision register and the iteration ledger travel with the record, and end-of-life is communicated to leadership, crisis team and stakeholders through the approved posture. After this revision the decision-register line reads its core claim, no trailing elaboration or stale count on the face.
- System thinkers without analytics know-how: from S18 — a plain reading that when the tool goes away the record does not, that nothing critical is switched off without a named human channel to take it, and that the export is open so it can be read by whatever comes next. Still cannot: read a probability or a dollar; the exit is described in plain obligations, deliberately without numbers on the slide.
- Execution geniuses: from S18 — the exit runbook by horizon (EOL-120 draft plan → EOL-90 align & execute → EOL-60 final deliverables → EOL-0 handover), what each stage must deliver, and that absorbed functions and partnered rails retire in the reverse order of absorption. Still cannot: see an owner and date per stage; that resourcing lives in S14 and the roadmap.
- Public bodies (the brief's R-CORE section): from S18 — that at end-of-life the export is the citizens' record, opened through the eXeL Polling API so the people who answered can replay what was decided, by whom and why, and transparency and accountability outlive the vendor (R-CORE already runs in this repo). Still cannot: see a public-body wind-down run; it enters as a declared future dependency, after G3.

## AAR

Concept (G1) reframe note (operator 2026-09-26 "Make G1", this whole round): the deck must READ as a CONCEPT (G1) roadmap-gate artifact — the project sits at the G1 Concept gate of the ladder (G1 Concept → G2 Plan → G3 Develop → G4 Qualify → G5 Launch → G6 Maximize), the ask is to pass Concept and advance to Plan, and the deck's existing "G2" wording is the De-Risking-Strategies VALIDATION gate (G2 measurement design, G3 instrumented A/B), kept as the FORWARD de-risking plan that runs after Concept. **Slide S18 carries NO narrative gate/stage reference in any of its cells** (grep of the S18 seed block for `G2 · G1 · Concept · gate · stage · ask` returns nothing; the `gate: "G7"` on S18 in `innovation-data.ts` is the structural AMTS-matrix position of the End-of-Life slide, not a narrative cell, and the reframe HARD-LIMITS leave the ladder labels and structural gates untouched). S18 is not the home of `project.stage` / `decisionRequested` (that is S1 / S2), so per the reframe those drs.json fields are NOT touched this round. Therefore, per the reframe's own rule — "if your slide carries no gate/stage reference, just do the normal law-6 optimization" — this S18 pass is a normal law-6 density optimization.

Slide S18 (End-of-Life — a DECLARED template slide, not in v0.9, under the ONE-PLATFORM boundary: `e120` EOL-120 draft plan (list; 3 `hi` / 4 `ai`) · `e90` EOL-90 align & execute (list; 2 `hi` / 3 `ai`) · `e60` EOL-60 final deliverables (list; 2 `hi` / 3 `ai`) · `e0` EOL-0 handover (list; 3 `hi` / 4 `ai`, the last `ai` row is PROV); rendered by the S18 field config in `innovation-data.ts`, all four panels `kind: "list"`) — keep | fix | cut, per cell:

- `e120` (EOL-120 draft plan, list; 3 `hi` / 4 `ai`) — **keep.** `hi[0]` already carries the optimized reference pattern (retire only behind a successor that keeps "the Horizon-1 objects itemised on S14" — short on the face, `ai[0]` enumerates decision rights / approval state / action ownership / evidence / replay); `hi[1]` the open export; `hi[2]` the citizens'-record line (kept whole — the citizen must be present on the page, brief R-CORE section). `ai` adds the absorbed-function / partnered-rails handback row so `ai` ≥ `hi`. No overflow, law 1 / law 2 clean.
- `e90` (EOL-90 align & execute, list; 2 `hi` / 3 `ai`) — **keep.** `hi[1]` carries the reverse-order phase-out with the safety caveat "nothing life-critical is switched off without a named successor channel" — a law-2 named-human-on-every-consequence statement that is load-bearing on the face and is NOT trimmed. `ai` adds the decision-rights/approval-history export row so `ai` ≥ `hi`.
- `e60` (EOL-60 final deliverables, list; 2 `hi` / 3 `ai`) — **keep.** Final replay package + last approved current-plan state archived; escalation watchpoints and stakeholder posture closed with a named successor channel; `ai` adds the legal-review / no-PHI-system-of-record note. Both `hi` cells read cleanly, no overflow.
- `e0` (EOL-0 handover, list; 3 `hi` / 4 `ai`) — **fix (one cell).** `hi[2]` reads "The decision register (D1–D107) and the iteration ledger travel with the record — every key decision, its revision and its author, replayable." — two defects: (1) a trailing elaboration ("— every key decision, its revision and its author, replayable") that belongs in the note (law 6 density, keep `hi` short, detail in `ai`), and (2) a pinned count "(D1–D107)" that has drifted stale — the master decision register is now at D124 (D125 this revision), so the printed range is out of date and would drift further every round. Fix: trim `hi[2]` to its core claim "The decision register and the iteration ledger travel with the record." (elaboration to the note, stale range removed rather than re-pinned to a moving figure); update the note `ai[2]` to keep the elaboration (so `ai` ≥ `hi`) while also dropping the same stale range. Keep `hi[0]` (end-of-life communication through the approved posture) and `hi[1]` (last event's replay carried into the successor). Field id/shape (string[], 3 `hi` / 4 `ai` rows, `ai[3]` = PROV) preserved; no cell emptied (law 6); master statement and segment value props untouched (law 4); no dollar or linked number moves (law 3 — the D-range is a narrative reference to the ledger, not a financialModel figure).

Edits applied this revision (one S18 face-cell change across its two carriers `hi`/`ai` — the law-6 density trim + stale-count removal — plus the mandated rev bump (law 5) and the append-only record (law 7)):

1. seed · `S18.e0.hi[2]` · trim the decision-register line to its core claim — "The decision register (D1–D107) and the iteration ledger travel with the record — every key decision, its revision and its author, replayable." → "The decision register and the iteration ledger travel with the record." — moving the trailing elaboration to the note and removing the drift-prone stale range (law 6 density; law 2/3 honesty — a printed count that no longer matches the growing register is removed, not re-pinned). Anchor: the `hi[2]` line immediately followed by `],\n        ai: [` (binds the twice-occurring cell string to the `hi[2]` occurrence only); asserted found exactly once.
2. seed · `S18.e0.ai[2]` (the note) · drop the same stale range while keeping the elaboration — "The decision register (D1–D107) and the iteration ledger travel with the record — every key decision, its revision and its author, replayable." → "The decision register and the iteration ledger travel with the record — every key decision, its revision and its author, replayable." — so the note stays a superset of `hi[2]` (`ai` ≥ `hi`) and carries no stale number. Anchor: the `ai[2]` line immediately followed by `,\n          PROV,` (binds to the `ai[2]` occurrence only); asserted found exactly once.
3. seed · `PROV` constant · rev 0.122 → 0.123 (anchor "v1.0 · rev 0.122", one occurrence) · law 5.
4. `innovation-data.ts` · row `provenance` · rev 0.122 → 0.123 (anchor "v1.0 · rev 0.122", one occurrence) · law 5.
5. Master record (`docs/drs/drs.v00.00.json`) · fill `revisions[]` 0.122 commit PENDING → c157569; append `iterations[]` 0.123, `decisions[]` D125, `revisions[]` 0.123 (kind release, commit PENDING); set `project.revision` 0.123 · law 7. (`project.stage` / `decisionRequested` NOT touched — S18 is not their home; the G1 reframe of those fields belongs to the S1 / S2 pass.)

Decisions:

- D125 — Per-slide S18 pass (rev 0.123), Concept (G1) reframe round. Slide S18 (End-of-Life) is a DECLARED template slide (not in v0.9), governed under the ONE-PLATFORM boundary: it states how CrisisCommand.ai leaves — a governance-state capability retires only after its successor keeps the governed record (the Horizon-1 objects itemised on S14) intact; the whole record (decision history, plan versions, evidence, outcome-linked replay) exports to the customer in an open format; every absorbed function's data leaves and partnered rails (Rave, PagerDuty, quality / recall, legal) hand back cleanly, so the record is the customer's and never the vendor's; the reverse-order phase-out never switches off anything life-critical without a named successor channel (law 2); and for a public body the export is its citizens' record, opened through the eXeL Polling API so the people who answered can replay what was decided, by whom and why — transparency and accountability outlive the vendor (R-CORE already runs in this repo). Concept (G1) reframe (operator 2026-09-26 "Make G1"): S18 carries NO narrative gate/stage reference in any cell — a grep of the S18 seed block for G2 / G1 / Concept / gate / stage / ask returns nothing, and the gate: "G7" on S18 in innovation-data.ts is the structural AMTS-matrix position of the End-of-Life slide, not a narrative cell (the reframe HARD-LIMITS leave the ladder labels G1 Concept … G6 and the structural gates untouched); S18 is not the home of project.stage / decisionRequested (that is S1 / S2), so those drs.json fields are NOT touched this round. Therefore, per the reframe's own rule, this S18 pass is a normal law-6 optimization. Its one defect is corrected in the e0 EOL-0 handover panel: the decision-register line (e0.hi[2]) carried a trailing elaboration that belongs in the note AND a pinned count "(D1–D107)" that has drifted stale (the register is now at D124, D125 this revision), so the line is trimmed to its core claim "The decision register and the iteration ledger travel with the record.", the elaboration preserved verbatim in the note (e0.ai[2], so ai ≥ hi), and the stale range removed from both carriers rather than re-pinned to a moving figure — a durable fix that can never go stale. This CONTINUES the density/clarity discipline of D110–D124 (short hi, detail in the note) and ADDS the honesty rule that no narrative reference to the append-only ledger may print a count that drifts out of date. No horizon, gate, the one-ninth (11.1 %) figure, timing label, claim, law or linked financialModel number moves (law 3); the master statement and segment value props stay fixed (law 4); the field id/shape (string[], 3 hi / 4 ai, ai[3] = PROV) is preserved and no cell is emptied (law 6). It does not supersede any prior decision. Holds security, stability and scalability at ceiling; S18's efficiency and succinctness rise this revision because the overloaded decision-register cell is drawn back to one legible, non-drifting claim with no loss of substance.

## Gates

(filled by the door)
