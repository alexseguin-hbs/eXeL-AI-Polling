# Iteration 0.120 · per-slide S15 (slide 14 of 18 in this pass) · 2026-09-25 · author: twelve lenses · per-slide S15

## Twelve lenses

### Aset

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 88

Restoration reads slide S15 as the deck's honest confession that nothing is proven yet and its disciplined promise of how proof will arrive: every voice-of-customer row still stands at zero customers, every disposition marked pursue-hypothesized, the measurement design named per segment against Rave, Everbridge and PagerDuty, and outcome-linked replay set to record. No price is claimed before a buyer validates baseline, delta, weight, attribution and willingness. This revision restores the page itself, where the EDU learning cell had swollen past its siblings with a trailing caveat, drawn back while its full status stays in the note. Founder, the honesty reads true. Investors, nothing is inflated. Customers, proof always precedes the price.

words: 111

### Asar

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 88

Synthesis reads slide S15 as the page where the venture's readiness to be measured meets its refusal to price ahead of evidence: the pre-launch voices are all at zero, each learning tied to a segment clock and a benchmark comparator, the priorities ordered by the absorption ladder, and the impact rule binding contribution to validated willingness minus CrisisCommand's own cost-to-serve. The whole is a case that designed its test before claiming a result. This revision mends the one frayed seam, the EDU learning cell that ran past its column with a status caveat, drawn to a line, its note below. Founder, the whole holds. Investors, evidence leads. Customers, price follows proof.

words: 111

### Athena

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 89

Strategy asks which single move on slide S15 buys most for least, and again the answer is a cut. The page already carries the discipline: every voice at zero and honestly labelled hypothesized, the learnings measured per segment against the named benchmark, the priorities ordered by qualification, and no price until willingness-to-pay clears cost-to-serve. But the EDU learning cell had overrun its column with a trailing caveat its sibling rows do not carry, taxing the reader it must convince. Trimming it to the metric alone, the caveat moved to the note, restores the column at no cost. Founder, subtraction won again. Investors, it reads at a glance. Customers, nothing was lost.

words: 111

### Christo

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 89

Coherence looks at slide S15 and finds parts that agree: the zeroed voices, the per-segment learnings, the qualification-ordered priorities and the price-after-evidence rule all resolve toward one honest thing, a venture that will not name a result it has not measured or a price a buyer has not confirmed. The clocks, the comparators and the modeled-value equation point the same way. This revision keeps that wholeness by mending the frayed seam, the EDU learning cell that had spilled past its column with a caveat, now the metric alone, its status in the note. Founder, the whole is intact. Investors, the discipline is uniform. Customers, honesty and pricing read as one page.

words: 111

### Enki

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 97 · succinctness 87

The edge case I hunt on slide S15 is the skeptic who reads a page of zeros and decides it has nothing, or a crowded cell and decides it hides something. It has neither: the zeros are honest, every voice pre-registered against a named comparator, and the price rule refuses a number until a buyer validates baseline, delta, weight, attribution and willingness. But one cell had become uneven, the EDU learning overrunning its column with a trailing caveat. This revision shuts that failure: the cell is trimmed to its metric, the status in the note. Founder, both traps are closed. Investors, the rigor survives. Customers, the page reads plainly for anyone.

words: 111

### Enlil

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Verification is a comparison, and I compared slide S15 to the law it must obey: a slide cell that overflows its panel is a defect, and detail belongs in the note. The EDU row's learning cell failed that test, carrying a trailing status caveat, measurement design proven and value not yet, that its FOOD and TECH siblings do not, breaking the column's even width. This revision corrects it: the cell now states the metric alone, minutes to approved objectives and the count of consequential revisions, and the caveat stays verbatim in the note. Founder, the defect is closed. Investors, no substance moved at all. Customers, the page reads clean and complete.

words: 111

### Krishna

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 88

Integration reads slide S15 as the seam where the venture's measurement meets its money: each pre-launch voice binds to a segment clock, each clock to a named comparator, each learning to the disposition that will follow, and the impact rule binds validated willingness to CrisisCommand's own cost-to-serve to yield contribution. This revision strengthens that seam by mending its most crowded joint, the EDU learning cell, which had overrun its column with a status caveat and is drawn back while its full status stays in the note. Founder, every measurement ties to a decision. Investors, the model connects the test to a price. Customers, the join holds both ways, evidence to price.

words: 111

### Odin

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 88

Read in twenty-five twenty-five, slide S15 is the page a distant reader opens to ask whether this venture knew, before it launched, what it would measure and how price would follow proof, and finds both: voices pre-registered at zero against named comparators, learnings tied to segment clocks, price withheld until a buyer validates every input. The page survives the centuries only if it stays legible. What would tire a far reader is a table cell swollen past its neighbours, so this revision draws the EDU learning cell back to its metric, the caveat kept below. Founder, the test design endures readable. Investors, the foresight lasts. Customers, any age reads it plainly.

words: 111

### Pangu

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 97 · succinctness 87

Subtraction has a floor, the thing itself, and on slide S15 the thing is the honest pre-launch state and the price-after-evidence rule: the voices at zero, learnings measured per segment, priorities ordered by qualification, contribution as validated willingness minus the cost-to-serve. Nothing on that floor needs cutting, for each is real and load-bearing. But one element sat wrongly above it, the EDU learning cell, a status caveat crammed into a metric column, clutter over substance. This revision removes the clutter without touching the substance, the cell becomes its metric, the caveat moved to the note. Founder, the floor is clean. Investors, no measurement was cut. Customers, every single voice still counts.

words: 111

### Sofia

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 89

Twelve readers open slide S15 differently, and each is served: the founder wants the pre-launch test designed before launch; the investor wants price withheld until willingness-to-pay clears cost-to-serve; the buyer wants learnings measured against the tool they already run; the public servant wants the outcome-linked replay that makes a decision answerable. This revision serves them all by clearing the one obstacle in their common path, the EDU learning cell, overflowing its column with a caveat, now trimmed to its metric with the status in the note. Founder, every lens finds its own row. Investors, none is inflated. Customers, the page reads clearly for all who must trust what will be tested.

words: 111

### Thoth

SSSES (business case): security 98 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

The numbers behind slide S15 do not move this revision: the voices stay at zero customers, the dispositions stay pursue-hypothesized, the one-ninth gate stays the bar each absorbed function must clear, and the modeled-value equation stays a method, time value plus discrete avoided cost plus risk-adjusted outcome, shown as ranges, never a guaranteed saving. What I checked is the page's own form, against the density law: the EDU learning cell carried a status caveat where a metric belongs, breaking the column. This revision corrects the form, the cell is its metric, the caveat stays in the note. Founder, the page is exact. Investors, no figure changed. Customers, S15 now reports cleanly.

words: 111

### Thor

SSSES (business case): security 99 · stability 100 · scalability 100 · efficiency 98 · succinctness 90

Risk on slide S15, as the guardian reads it: the danger is a reader who sees zeros and distrusts an empty case, or an uneven cell and suspects concealment. The rigor is real: every voice is pre-registered against a named comparator, the price rule refuses a number until a buyer validates baseline, delta, weight, attribution and willingness, and replay binds every decision to a record. But the EDU learning cell had overrun its column with a trailing caveat that invites doubt. This revision shuts that risk down, the cell is its metric, the caveat kept in the note. Founder, the seam is closed. Investors, nothing weakened. Customers, the page stands legible.

words: 111

## Spiral test

- Future demo: from S15 alone — the venture designed its pre-launch test before launch: every voice-of-customer row stands at zero customers and is labelled "Pursue — hypothesized" (nothing validated yet), each learning measured per segment against the named comparator (EDU vs Rave, FOOD vs Everbridge 360 AI / Bridge, TECH vs PagerDuty), with outcome-linked replay and time-to-coherence measurement set to record. Still cannot: see a single filled result — every row is at zero, by design (that is the G2 tabletop and the G3 A/B, S16).
- Customers: from S15 — that no price is set until a buyer validates baseline, delta, economic weight, attribution and willingness to pay, and that contribution = validated WTP − CrisisCommand-specific CTS, with modeled value shown as ranges, never guaranteed savings. Still cannot: see the price itself — none is printed until WTP clears CTS (law 3, declared).
- Innovators who want to add functionality: from S15 — the development priorities in absorption order (H1 Governed Leadership State → H2 absorb the benchmark's surface by qualification, the one-ninth (11.1 %) gate, alerting last → H3 Enterprise Decision Graph → H4 Recursive Readiness, R-CORE-informed), and that segment-specific workflows enter only where segment evidence proves them, never universal. Still cannot: see the per-function capability schema; the absorption order is a rule, not a per-function model.
- Investors: from S15 — that the case has designed the evidence it will gather (per-segment learnings, WTP, CTS) and refuses to price ahead of it, and that contribution is defined as validated WTP minus CrisisCommand-specific CTS. Still cannot: see a validated WTP or a measured CTS — both are named as evidence to gather at G3, not asserted (law 3).
- Business owners: from S15 — that the disposition on every pre-launch voice is "Pursue — hypothesized" (an honest zero) and that old DRS prices are explicitly not evidence; the commercial form is decided by customer WTP + CTS. Still cannot: see a segment-by-segment WTP or CTS figure; they are the quantities to measure, not yet measured.
- System thinkers without analytics know-how: from S15 — a plain reading that nothing is proven yet, that each learning is measured against the tool the buyer already runs, and that price waits on evidence. After this revision the EDU learning cell reads as one clean metric, no trailing caveat crowding the column. Still cannot: read a probability or a dollar; the modeled-value equation is named as a method, deliberately without numbers on the slide.
- Execution geniuses: from S15 — exactly which learnings the pre-launch test must capture per segment (minutes to approved objectives / posture, the count of consequential / conflicting approved versions, outcome-linked replay, time-to-coherence Δ with attribution confidence) and the priority order to build them. Still cannot: see the owner and due date per priority; that resourcing detail lives in the note and the roadmap (S16 / S18).
- Public bodies (the brief's R-CORE section): from S15 — that the same pre-registered, comparator-anchored measurement and the outcome-linked replay that qualify a crisis function apply, unchanged in shape, to proving a public posture before adoption — the body's people asked through the eXeL Polling API, the result replayable by the body and by the citizens who answered. Still cannot: see a public-body voice or figure; the pre-launch voices are CrisisCommand's own, and the public-body path enters as a declared future dependency (S13 / S16, IA, after G3, D12).

## AAR

Slide S15 (BETA Feedback — pre-launch VOCs: the S15 seed slide: `voc` the pre-launch voice-of-customer register (4-column table: # customers · differentiator · VOC learnings · Pivot / Pursue / Pass; 4 `hi` rows, 5 `ai` rows) · `prio` the development-priorities table (3-column: priority · feature enhancement · method + timing; 4 `hi` rows, 5 `ai` rows) · `impact` the impact-to-business-case / pricing longtext; rendered by the S15 field config in `innovation-data.ts`) — keep | fix | cut, per cell:

- `voc` (pre-launch VOC register, table 4 cols; 4 `hi` rows, 5 `ai` rows) — **fix (one cell).** The EDU row's VOC-learnings cell (`voc.hi[0][2]`) carried a trailing status caveat — "— measurement design proven, value not yet" — that its FOOD and TECH sibling rows do not, breaking the learnings column's even width: a law-6 density defect (a cell that overflows relative to its column; the caveat belongs in the note). The caveat is preserved verbatim in `voc.ai[0][2]` (the note), which stays unchanged so `ai` ≥ `hi`. Keep the other rows: the FOOD, TECH and outcome-linked-replay rows each state their segment learning cleanly, and `ai` adds the time-to-coherence measurement row; every disposition is honestly "Pursue — hypothesized" at zero customers (law 2, law 3), and no comparator name moves.

- `prio` (development priorities, table 3 cols; 4 `hi` rows, 5 `ai` rows) — **keep.** The four horizons in absorption order (H1 Governed Leadership State → H2 absorb the benchmark's surface by qualification, the one-ninth (11.1 %) gate, alerting last → H3 Enterprise Decision Graph → H4 Recursive Readiness, R-CORE-informed) each state their method + timing cleanly; `ai` adds the segment-specific-workflows priority (only where segment evidence proves them, never universal). No cell overflows; the H2 one-ninth-gate parenthetical is dense but stays one table cell and its fuller path lives on S14 / S16 — borderline, left in place this revision to keep the single-edit focus on the clearer defect. Law 1, law 6 clean.

- `impact` (impact to business case / pricing, longtext) — **keep.** States the law-3 discipline: no price until a buyer validates baseline, delta, economic weight, attribution and willingness; contribution = validated WTP − CrisisCommand-specific CTS; old DRS prices are not evidence. `ai` adds the Modeled Value equation (Time Value + Discrete Avoided Cost + Risk-Adjusted Outcome Value, ranges, never guaranteed savings) so `ai` ≥ `hi`. A longtext panel accommodates its length; no overflow. Law 2, law 3 clean.

Edits applied this revision (one S15 face edit — the law-6 density trim — plus the mandated rev bump (law 5) and the append-only record (law 7)):

1. seed · `S15.voc.hi[0][2]` EDU VOC-learnings cell · trim the trailing status caveat that overflows the learnings column — "Minutes event → approved objectives; # consequential revisions — measurement design proven, value not yet" → "Minutes event → approved objectives; # consequential revisions" — so the EDU cell matches the even width of its FOOD / TECH sibling rows, while the caveat ("— measurement design proven, value not yet") is preserved verbatim in `S15.voc.ai[0][2]` (the note), which stays unchanged so `ai` ≥ `hi` (law 6). Anchor: the `S15: {` → `voc:` → `hi:` → EDU-row block (the caveat substring alone occurs twice — in `hi` and `ai` — so the anchor includes the unique `S15: {` / `voc:` / `hi:` prefix to bind the `hi` occurrence only), asserted found exactly once. No comparator name (Rave / Everbridge 360 AI / Bridge / PagerDuty), disposition ("Pursue — hypothesized"), gate, IA label, claim or linked number moves (law 3); the field id/shape (string[][], 4 cols × 4 `hi` rows) is preserved and no cell is emptied (law 6); the master statement and segment value props stay fixed (law 4).

2. PROV · seed constant · rev 0.119 → 0.120 (anchor "v1.0 · rev 0.119", one occurrence) · law 5.

3. innovation-data.ts · row provenance · rev 0.119 → 0.120 (anchor "v1.0 · rev 0.119", one occurrence) · law 5.

4. Master record · fill revisions[] 0.119 commit PENDING → de1b9e9; append iterations[] 0.120, decisions[] D122, revisions[] 0.120 (PENDING); set project.revision 0.120 · law 7.

Note on the operator's standing overriding instruction, re-relayed this run ("ensure rastors are created; use png versions. and ensure target of R-CORE icon is at bottom of page and does not overlay over/lay on top of image or slide below") — RE-VERIFIED SATISFIED + GATED this run, with checkable proof:

1. **Rasters as PNG, created and committed.** `git ls-files frontend/public/r-core/` lists `r-core-icon.png` (PNG 1448×1086, 8-bit RGBA — the framed reticle · REST) and `r-core-wordmark.png` (PNG 2172×724, 8-bit RGBA — the "R-CORE" wordmark pill · CLICK 1); both are RGBA (transparent, no black-box edges) and referenced verbatim by `frontend/components/2525-core/rcore-badge.tsx` (`ICON` = `/r-core/r-core-icon.png` line 28, `WORDMARK` = `/r-core/r-core-wordmark.png` line 29) as `<img>` elements — no CSS/glyph recreation.

2. **R-CORE icon at the page bottom, no overlay.** The badge renders in NORMAL FLOW — wrapper `data-rcore-badge` `flex w-full justify-center` (line 61), no `fixed`/`absolute`/`position` geometry — and is mounted LAST in `app/SoI-2525/page.tsx:1100`, AFTER the `</footer>` (line 1097) and the content, so it lands at the page foot below the content, never over the image/slide. Gated by `frontend/tests/rcore-revisions.test.mjs` (in `test:ci`), which asserts the badge source contains `data-rcore-badge` and `flex w-full justify-center` and does NOT contain a fixed-overlay class.

The deck renderer (`scripts/drs-render.mjs`) carries NO icon/raster reference — the R-CORE icon in question is the SoI-2525 page badge only, which is correctly bottom-placed. This per-slide door stages only `docs/drs`, the seed, `innovation-data.ts`, `innovation-time.test.mjs`, `drs-render.mjs`, `docs/assessments` and `docs/asks` — it does not touch the badge component or the page, both already correct and gated; no orphaned or uncommitted change is created here (persist-first).

Decisions:

- D122 — Per-slide S15 pass (rev 0.120). Slide S15 (BETA Feedback — pre-launch VOCs) IS a seed slide (the `voc` pre-launch voice-of-customer register, a four-column table of # customers · differentiator · VOC learnings · Pivot / Pursue / Pass; the `prio` development-priorities table, a three-column table of priority · feature enhancement · method + timing; and the `impact` impact-to-business-case / pricing longtext; rendered by the S15 field config in `innovation-data.ts`). Its one law-6 density defect is corrected: the EDU row's VOC-learnings cell (`voc.hi[0][2]`) carried a trailing status caveat — "— measurement design proven, value not yet" — that its FOOD and TECH sibling rows do not, breaking the learnings column's even width, so it is trimmed to the metric alone — "Minutes event → approved objectives; # consequential revisions" — while the caveat is preserved verbatim in `voc.ai[0][2]` (the note), which stays unchanged so `ai` ≥ `hi`. This CONTINUES the density/clarity discipline of D110–D121 (short `hi`, detail in the note), applied here to the slide that shows the case designed its own pre-launch test before claiming any result and refuses to price ahead of evidence: every voice-of-customer row stands honestly at zero customers and disposition "Pursue — hypothesized" (law 2), each learning is measured per segment against the named comparator (EDU vs Rave, FOOD vs Everbridge 360 AI / Bridge, TECH vs PagerDuty), no price is set until a buyer validates baseline, delta, economic weight, attribution and willingness to pay, contribution = validated WTP − CrisisCommand-specific CTS, and modeled value is shown as ranges, never a guaranteed saving (law 2, law 3), with development priorities ordered by the absorption ladder under the one-ninth gate, alerting last (law 1). No comparator name, disposition, gate, IA label, claim, law or linked number moves (law 3); the master statement and segment value props stay fixed (law 4); the field id/shape is preserved and no cell is emptied (law 6). This decision also RE-RECORDS, with checkable proof, that the operator's standing overriding instruction for the page is satisfied and gated this run — the R-CORE rasters exist as committed 8-bit RGBA PNG (`public/r-core/r-core-icon.png` 1448×1086 · `public/r-core/r-core-wordmark.png` 2172×724) and the R-CORE icon's target sits at the BOTTOM of the page in NORMAL FLOW (wrapper `flex w-full justify-center`, no fixed/absolute geometry, mounted last after the footer at `app/SoI-2525/page.tsx:1100`), never overlaying the image or slide, defended by `frontend/tests/rcore-revisions.test.mjs` in `test:ci`; the deck renderer carries no icon reference. It does not supersede any prior decision. Holds security, stability and scalability at ceiling; S15's efficiency and succinctness rise this revision because the overflowing EDU learning cell is drawn back to one legible metric with no loss of substance.

## Gates

(filled by the door)
