# Prompt — ECO-2525 r.042

Attach `drone-2525_v.00.00_r.042.html`. Do not overwrite `ASM_CUP_99.*` (seed 2525 regression fixture).

Doctrine: PLAY → RECORD → REPLAY → COMPARE → QUALIFY → IMPROVE → SHARE
R-CORE coordinates. HI is authority. Civic sim only — Capitol lawn is a virtual arena, not a live-fire planner.

Fixed from r.040 review:
1. 42 turrets are created AFTER `const units`.
2. QUAD = D1Q · VTOL = D1 · FOIL = D1F. applyMode('foil') labels D1F only.
3. Gameplay truth is CH1–CH5 + DIFF 1–5. TG selector hidden.
4. Every shot writes a decision record: designated, hiApproved, authorityLevel, actor, decisionId. CH5 rejects without HI APPROVE.
5. Feed lines: time | actor | verb | id | result.
6. Cup baseline stays: 6v6 BLU 3–1 · 3v3 BLU 5–0 · pairs 1–3 RED · pairs 4–6 BLU.

Compare next sims on designation rate, HI holds, auth failures, handoffs, FPS, replay hash — not winners only.
