<!-- GENERATED by scripts/drone-render.mjs from docs/drone-2525/drone-2525.v00.00.json — do not hand-edit. -->
# CRS · Drone-2525

**Version 00.00 · revision 0.035** · Vision • 2525 Level-3 Domain Play on WIREFRAME-CORE

> Simulation for play and critique only. Bounding estimates, not design evidence. Not flight authorisation and not a weapons system.

Handoff: `docs/asks/2026-09-19_range_popups.md` `sha256 c42cebc50e13f9e086256bd58d11357219b902edb4cd25d4665507ddec6c069d`

Id law (docs/vision-2525/CRS-GOVERNANCE-PLAN.md): `DRN-##.##`, two digits never letters; parents are permanent; sub-ids append only; a changed requirement is **superseded**, never rewritten.

## The ladder — one step per Vision • 2525 section

| CRS | Requirement | § | R-CORE | Phase | Status | Verified by |
|---|---|---|---|---|---|---|
| `DRN-01` | Arena frame and tiles | VII UCRS | Manual | pilot | implemented | `tests/drone-capitol-tiles.test.mjs` |
| `DRN-02` | Canonical wire model and one-step export | VIII Rehearsal | Manual | pilot | implemented | `tests/wire-export.test.mjs` |
| `DRN-03` | Edge primitives | XII Flywheel | Manual | pilot | implemented | `tests/wire-core.test.mjs · tests/vector-law.test.mjs` |
| `DRN-04` | One gimbal for turret and airframe | IV LINK | Manual | pilot | implemented | `tests/drone-gimbal.test.mjs` |
| `DRN-05` | Capture and shoot with line of sight | V EDGE | Manual | pilot | implemented | `tests/drone-game.test.mjs` |
| `DRN-06` | Score and replay | VI SYNC / Replay | Manual | pilot | implemented | `tests/drone-game.test.mjs` |
| `DRN-07` | Command surface, route and unlock | I Humanity First | Manual | pilot | implemented | `next build · tests/vector-law.test.mjs` |
| `DRN-08` | Flight and energy, bounded | IX Trust | Manual | pilot | implemented | `tests/drone-flight.test.mjs` |
| `DRN-09` | Two operators, two devices | III COMM | Manual | pilot | implemented | `tests/drone-link.test.mjs` |
| `DRN-10` | AI roles under human approval | X Governance | Manual | pilot | implemented | `tests/drone-game.test.mjs` |
| `DRN-11` | Vector law, the 13, and adaptive fidelity | XI SSSES | Manual | pilot | implemented | `tests/drone-fidelity.test.mjs · tests/vector-law.test.mjs` |
| `DRN-12` | Every word in 33 languages | XIII Translation | Manual | pilot | implemented | `tests/lexicon-coverage.test.mjs · tests/drone-i18n.test.mjs` |
| `DRN-13` | Versioned, revisioned, comparable | II R-CORE | Manual | pilot | implemented | `tests/traceability.test.mjs · tests/drone-crs.test.mjs` |
| `DRN-11.01` | The MoT ladder: five compute bands by five resolution steps | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-11.04` | Demand and capability are separate, and never mixed | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-05.03` | Multi-sensor fusion, shed from the top and never blind | V EDGE | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-11.05` | Hold 1080p30, and name every step down from it | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-11.06` | Self-test: the rung this machine actually holds, in 6 to 15 minutes | XI SSSES | Manual | pilot | implemented | `tests/self-cal-runner.test.mjs` |
| `DRN-11.07` | The robot may pick its own compute class | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-10.01` | Shared Intent: a key group, switched on or off | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-10.02` | Key members only, by invitation | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-10.03` | The group advises; a named person decides | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-10.04` | Volunteer input, recognised on the ladder that already exists | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-09.01` | A seat may only send what that seat controls | III COMM | Manual | pilot | implemented | `tests/drone-link.test.mjs` |
| `DRN-09.02` | Three paths carry every word between the two devices | III COMM | Manual | pilot | implemented | `scripts/drone-crew-e2e.mjs` |
| `DRN-11.08` | Measured, not assumed, on a slowed processor | XI SSSES | Manual | pilot | implemented | `scripts/drone-perf.mjs` |
| `DRN-13.01` | Nine runs, or it is not determinism | II R-CORE | Manual | pilot | implemented | `scripts/drone-spiral9.mjs` |
| `DRN-09.03` | Forty-two aircraft, twenty-one against twenty-one | III COMM | Manual | pilot | implemented | `tests/drone-swarm.test.mjs · scripts/drone-perf.mjs` |
| `DRN-11.09` | One budget, two consumers: the aircraft are the world | XI SSSES | Manual | pilot | implemented | `tests/drone-swarm.test.mjs` |
| `DRN-10.05` | Five authority levels, and silence is never consent on any of them | X Governance | Manual | pilot | implemented | `tests/drone-authority.test.mjs` |
| `DRN-13.02` | A score that can fall | II R-CORE | Manual | pilot | implemented | `scripts/drone-spiral9.mjs` |
| `DRN-05.04` | The beam, the shield, and the time it takes | V EDGE | Manual | pilot | implemented | `frontend/tests/drone-laser.test.mjs` |
| `DRN-05.05` | Nothing is destroyed in the air | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-laser.test.mjs` |
| `DRN-05.06` | Lighting inside the vector law | XI SSSES | Manual | pilot | implemented | `frontend/tests/drone-laser.test.mjs` |
| `DRN-08.01` | One aircraft, two sizes | VIII Rehearsal | Manual | pilot | implemented | `frontend/tests/drone-scale.test.mjs` |
| `DRN-10.06` | The same game at every N | VIII Rehearsal | Manual | pilot | implemented | `frontend/tests/drone-contest.test.mjs` |
| `DRN-10.07` | The person is the bottleneck, and it is measured | X Governance | Manual | pilot | implemented | `frontend/scripts/drone-ladder.mjs` |
| `DRN-12.01` | From open play to a real floor | XII Flywheel | Manual | pilot | implemented | `frontend/tests/drone-winners-circle.test.mjs` |
| `DRN-12.02` | The last gate is not about skill | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-winners-circle.test.mjs` |
| `DRN-11.10` | The aircraft is the FOIL | IX Trust | Manual | pilot | implemented | `frontend/tests/drone-naming.test.mjs` |
| `DRN-07.01` | The address the game answers at | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-route.test.mjs` |
| `DRN-04.01` | One seat geometry for every vehicle | IV LINK | Manual | pilot | implemented | `frontend/tests/seat-view.test.mjs` |
| `DRN-05.07` | Amber, then red — no shot without a second authority | V EDGE | Manual | pilot | implemented | `frontend/tests/drone-slots.test.mjs · frontend/tests/drone-tap.test.mjs` |
| `DRN-06.01` | The decision record and the replay hash | VI SYNC / Replay | Manual | pilot | implemented | `frontend/tests/drone-decisions.test.mjs` |
| `DRN-07.02` | The operator deck: keys, sticks, touch and voice | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-controls.test.mjs · controls-schema · stick-sets · drone-tap` |
| `DRN-10.08` | Compare on what he asked to compare on | X Governance | Manual | pilot | implemented | `frontend/tests/drone-contest.test.mjs · frontend/scripts/drone-ladder.mjs` |
| `DRN-10.09` | CH1–CH5 and DIFF 1–5, from the deck | X Governance | Manual | pilot | implemented | `frontend/tests/drone-challenge.test.mjs` |
| `DRN-06.02` | The AsM Cup as a regression fixture | VI SYNC / Replay | Manual | pilot | implemented | `frontend/tests/asm-cup.test.mjs` |
| `DRN-09.04` | The second person, over the link | III COMM | Manual | pilot | implemented | `frontend/tests/drone-link.test.mjs` |
| `DRN-08.02` | The platform identities, from the deck | VIII Rehearsal | Manual | pilot | implemented | `frontend/tests/drone-platforms.test.mjs` |
| `DRN-05.08` | The range: per-lane pop-ups at 50–300 m, three modes | VIII Rehearsal | Manual | pilot | implemented | `frontend/tests/range-2525.test.mjs · frontend/scripts/drone-deck-qa.mjs (RANGE_POP_SCHEDULE, MODE_RESET_RETURNS, MODE_DOWN_STAYS, MODE_QUAL_TIMED, ONE_ROUND_PER_EXPOSURE, TABLE_III_EXPOSES_ONLY_SCORABLE)` |
| `DRN-05.09` | A hit is scale-true; a target that is down is not a target | V EDGE | Manual | pilot | implemented | `frontend/scripts/drone-deck-qa.mjs (RANGE_HIT_*, LAPSE_RELEASES_THE_BOX) · frontend/tests/drone-playable.test.mjs` |
| `DRN-04.02` | One forward basis; one aim from the eye | IV LINK | Manual | pilot | implemented | `frontend/tests/range-2525.test.mjs · frontend/scripts/drone-deck-qa.mjs (PROJ_FWD_AGREES, PAN_RIGHT_MOVES_WORLD_LEFT, FWD_AT_YAW90, PIT_SEES_300_RIGHT_250_LEFT)` |
| `DRN-11.11` | Render honesty: exceptions recorded, must-hit first, the deck's own QA in CI | XI SSSES | Manual | pilot | implemented | `frontend/scripts/drone-deck-qa.mjs · frontend/tests/deck-qa-manifest.mjs` |
| `DRN-13.03` | Every operator-deck revision documented, chain-hashed, gated | II R-CORE | Manual | pilot | implemented | `frontend/tests/drone-revisions.test.mjs · frontend/tests/drone-playable.test.mjs` |

## Every row in full

### DRN-01 — Arena frame and tiles

**Statement.** The arena is an ENU frame on the Texas Capitol origin, and every building, tree, road and contour it draws is either carried from the repo, hand-authored and badged, or read from a map extract.

| field | value |
|---|---|
| Input / Output | `DRN-01.IN` → `DRN-01.OUT` |
| Vision • 2525 | § VII — UCRS |
| U-WF trace | `U-WF-01` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | tile gate green; every element carries a source |
| DTM target | hand-authored block renders |
| Stretch | OSM extract replaces it with zero code change |
| Verification | `tests/drone-capitol-tiles.test.mjs` |
| Status | **implemented** |

### DRN-02 — Canonical wire model and one-step export

**Statement.** One model feeds the browser and the four engine targets, and a change is visible in all of them or none.

| field | value |
|---|---|
| Input / Output | `DRN-02.IN` → `DRN-02.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-02` · `U-WF-05` · `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | lossless round trip on the FOIL wireframe |
| DTM target | py/obj/cs/cpp from one model |
| Stretch | every domain exports through it |
| Verification | `tests/wire-export.test.mjs` |
| Status | **implemented** |

### DRN-03 — Edge primitives

**Statement.** Buildings, trees, roads and contours draw as edges only, with door and window openings in their real wall.

| field | value |
|---|---|
| Input / Output | `DRN-03.IN` → `DRN-03.OUT` |
| Vision • 2525 | § XII — Flywheel |
| U-WF trace | `U-WF-02` · `U-WF-10` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no fill on any drawn element |
| DTM target | Capitol block renders as edges |
| Stretch | shared with Architect and Security |
| Verification | `tests/wire-core.test.mjs · tests/vector-law.test.mjs` |
| Status | **implemented** |

### DRN-04 — One gimbal for turret and airframe

**Statement.** The same mount and gimbal record drives a fixed turret and a drone, so nothing learned on the turret is thrown away on the airframe.

| field | value |
|---|---|
| Input / Output | `DRN-04.IN` → `DRN-04.OUT` |
| Vision • 2525 | § IV — LINK |
| U-WF trace | `U-WF-02` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | identical frustum model from both mounts |
| DTM target | turret slews and frames a door |
| Stretch | airframe mount reuses it unchanged |
| Verification | `tests/drone-gimbal.test.mjs` |
| Status | **implemented** |

### DRN-05 — Capture and shoot with line of sight

**Statement.** A capture stores the sensor's extracted edges, and a shot only counts when the ray actually reaches the door.

| field | value |
|---|---|
| Input / Output | `DRN-05.IN` → `DRN-05.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | occluded shots are refused |
| DTM target | tag a door by capture then shot |
| Stretch | live camera behind the same interface |
| Verification | `tests/drone-game.test.mjs` |
| Status | **implemented** |

### DRN-06 — Score and replay

**Statement.** A completed capture or shot is written to the replay log before the score moves, so nothing that happened can be lost.

| field | value |
|---|---|
| Input / Output | `DRN-06.IN` → `DRN-06.OUT` |
| Vision • 2525 | § VI — SYNC / Replay |
| U-WF trace | `U-WF-06` · `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | replay reproduces the score and the hash |
| DTM target | a round replays identically |
| Stretch | portable .drn2525 file |
| Verification | `tests/drone-game.test.mjs` |
| Status | **implemented** |

### DRN-07 — Command surface, route and unlock

**Statement.** The domain has its own route and its card is enterable, drawn under the vector law at any viewport down to 375 px.

| field | value |
|---|---|
| Input / Output | `DRN-07.IN` → `DRN-07.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | route builds; card enters |
| DTM target | arena visible on a phone |
| Stretch | two devices in one session |
| Verification | `next build · tests/vector-law.test.mjs` |
| Status | **implemented** |

### DRN-08 — Flight and energy, bounded

**Statement.** The flight model is a bounding estimate that says so on every run, and its published numbers reproduce from the source constants.

| field | value |
|---|---|
| Input / Output | `DRN-08.IN` → `DRN-08.OUT` |
| Vision • 2525 | § IX — Trust |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | --reproduce reports zero drift |
| DTM target | hover and cruise endurance |
| Stretch | VTOL transition envelope |
| Verification | `tests/drone-flight.test.mjs` |
| Status | **implemented** |

### DRN-09 — Two operators, two devices

**Statement.** The pilot is the only sender of flight state and the targeteer the only sender of gimbal commands.

| field | value |
|---|---|
| Input / Output | `DRN-09.IN` → `DRN-09.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | role authority refuses a wrong sender |
| DTM target | two phones share one round |
| Stretch | hosted realtime |
| Verification | `tests/drone-link.test.mjs` |
| Status | **implemented** |

### DRN-10 — AI roles under human approval

**Statement.** An AI-initiated shot never fires without a human approval event.

| field | value |
|---|---|
| Input / Output | `DRN-10.IN` → `DRN-10.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no shot without an approve event |
| DTM target | AI proposes, human approves |
| Stretch | both AI seats with humans watching |
| Verification | `tests/drone-game.test.mjs` |
| Status | **implemented** |

### DRN-11 — Vector law, the 13, and adaptive fidelity

**Statement.** Everything drawn is edges in one of the 13 colours, and resolution and frame rate rise only with compute left over after the Edge sensor's CNN reserve.

| field | value |
|---|---|
| Input / Output | `DRN-11.IN` → `DRN-11.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` · `U-WF-09` · `U-WF-11` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | model hash identical at every tier; LOW holds a Pi budget |
| DTM target | four tiers, one truth |
| Stretch | accelerator profile reaches ULTRA |
| Verification | `tests/drone-fidelity.test.mjs · tests/vector-law.test.mjs` |
| Status | **implemented** |

### DRN-12 — Every word in 33 languages

**Statement.** No user-facing string is English in the source; every one is a lexicon key present in all 33 languages.

| field | value |
|---|---|
| Input / Output | `DRN-12.IN` → `DRN-12.OUT` |
| Vision • 2525 | § XIII — Translation |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | lexicon coverage green with no pending keys |
| DTM target | keys staged and listed |
| Stretch | filled in all 32 non-English |
| Verification | `tests/lexicon-coverage.test.mjs · tests/drone-i18n.test.mjs` |
| Status | **implemented** |

### DRN-13 — Versioned, revisioned, comparable

**Statement.** Every edition of this specification appends a revision that cites its commit, and any two revisions can be replayed and compared.

| field | value |
|---|---|
| Input / Output | `DRN-13.IN` → `DRN-13.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | ledger validates; render --check clean |
| DTM target | Version 00.00 revision 0.001 |
| Stretch | CRS rows compare in /crs |
| Verification | `tests/traceability.test.mjs · tests/drone-crs.test.mjs` |
| Status | **implemented** |

### DRN-11.01 — The MoT ladder: five compute bands by five resolution steps

**Statement.** Detail and sensing are asked for on a 25-rung ladder, 1.1 through 5.5, where the band is the edge-compute class and the step is the resolution; rung 1.1 is the 1983 vector arcade and is the fastest picture the machine can make.

| field | value |
|---|---|
| Input / Output | `DRN-11.01.IN` → `DRN-11.01.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` · `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | ladder monotonic in both axes; 1.1 asks for more frames than 5.5 |
| DTM target | 25 rungs selectable |
| Stretch | a robot picks its own rung unattended |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-11.04 — Demand and capability are separate, and never mixed

**Statement.** What the picture asks for (the rung) and what the machine can deliver (the HAL class and the measured frame rate) are different quantities held in different modules, and only calibration is allowed to reconcile them.

| field | value |
|---|---|
| Input / Output | `DRN-11.04.IN` → `DRN-11.04.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a richer rung never asks for a faster picture |
| DTM target | the old inverted four-tier table is gone |
| Stretch | the same split serves every 2525 domain |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-05.03 — Multi-sensor fusion, shed from the top and never blind

**Statement.** Sensing runs EO, IR, ACOUSTIC, MAG and CHEM, one more module per compute band; when the frame will not hold, modules are shed from the top of that ladder and EO is never shed, because a unit that cannot see is not degraded but blind.

| field | value |
|---|---|
| Input / Output | `DRN-05.03.IN` → `DRN-05.03.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | EO survives every degradation path |
| DTM target | five modules declared per band |
| Stretch | a real sensor replaces the simulated one behind the same interface |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-11.05 — Hold 1080p30, and name every step down from it

**Statement.** The unit tries to hold a 1920x1080 30 Hz reference stream; when it cannot it sheds a sensor module, then resolution, then frame rate, in that order, and every step is named on screen rather than taken silently.

| field | value |
|---|---|
| Input / Output | `DRN-11.05.IN` → `DRN-11.05.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | degradation order fixed; climb needs sustained headroom |
| DTM target | stream ladder live in the HUD |
| Stretch | the same ladder drives a real encoder |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-11.06 — Self-test: the rung this machine actually holds, in 6 to 15 minutes

**Statement.** Pointed at a new board or a new sensor, the unit walks the ladder from 1.1 upward under real drawing load, measures a sustained frame rate at each rung, and reports the highest rung it holds at the reference stream, inside a declared 6 to 15 minute budget.

| field | value |
|---|---|
| Input / Output | `DRN-11.06.IN` → `DRN-11.06.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-06` · `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | dwell derived from the budget; sweep stops early after two failures |
| DTM target | a saved result file naming the ceiling |
| Stretch | run headless on the robot itself |
| Verification | `tests/self-cal-runner.test.mjs` |
| Status | **implemented** |

### DRN-11.07 — The robot may pick its own compute class

**Statement.** Three compute classes are declared with their frame budgets and sensor costs, and AUTO lets the unit choose its own class from the frame rate it measures rather than from a label someone typed.

| field | value |
|---|---|
| Input / Output | `DRN-11.07.IN` → `DRN-11.07.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | AUTO thresholds derived from the budgets themselves |
| DTM target | PI / EDGE-SOC / ACCELERATOR selectable |
| Stretch | a fourth class added without touching a caller |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-10.01 — Shared Intent: a key group, switched on or off

**Statement.** A closed group of named people may be switched on to weigh in on decisions; off is the normal state and the round behaves exactly as it does without them.

| field | value |
|---|---|
| Input / Output | `DRN-10.01.IN` → `DRN-10.01.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no call can open while it is off; switching off closes any open call |
| DTM target | the switch and a closed roster |
| Stretch | the group is a real polling session on Cube 1 |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-10.02 — Key members only, by invitation

**Statement.** Reaching the group's call needs a code issued to named people; an empty roster produces no invitation rather than an open link, and the right code in the wrong hands is refused.

| field | value |
|---|---|
| Input / Output | `DRN-10.02.IN` → `DRN-10.02.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | both halves must hold: on the roster, and a live code |
| DTM target | a code and a scannable square |
| Stretch | delivery through the operator's own channel |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-10.03 — The group advises; a named person decides

**Statement.** Consensus is shown beside the decision a person is about to make and can never make it: no function in the consensus engine can retire a shot request or open the authority gate, and a unanimous group changes nothing about what may fire.

| field | value |
|---|---|
| Input / Output | `DRN-10.03.IN` → `DRN-10.03.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the two modules do not import each other; unanimity does not open the gate |
| DTM target | advice beside the question |
| Stretch | the same rule wherever a crowd meets an authority |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-10.04 — Volunteer input, recognised on the ladder that already exists

**Statement.** Answering is voluntary and silence counts as nothing; a contribution is recognised as noted, and as adopted only after the decision went the way it argued, using the three fixed heart values the pod ladder already declares rather than a fourth invented here.

| field | value |
|---|---|
| Input / Output | `DRN-10.04.IN` → `DRN-10.04.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no value outside the declared ladder; silence earns nothing |
| DTM target | a readable record of what was valued |
| Stretch | entries reach the Cube 8 append-only ledger |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-09.01 — A seat may only send what that seat controls

**Statement.** The pilot flies and cannot aim, photograph or shoot; the targeteer aims, photographs and shoots and cannot fly; a message whose payload does not belong to the seat that sent it is refused on the way in as well as on the way out.

| field | value |
|---|---|
| Input / Output | `DRN-09.01.IN` → `DRN-09.01.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the other seat's controls are absent, not disabled |
| DTM target | two seats that cannot do each other's job |
| Stretch | the same rule for a third seat |
| Verification | `tests/drone-link.test.mjs` |
| Status | **implemented** |

### DRN-09.02 — Three paths carry every word between the two devices

**Statement.** A crew message goes out on another tab, on shared storage and through a cloud endpoint at once; any one arriving is the message delivered and the duplicates the others produce are dropped without being counted as errors.

| field | value |
|---|---|
| Input / Output | `DRN-09.02.IN` → `DRN-09.02.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | two separate browser profiles exchange flight and aim |
| DTM target | a phone and a computer flying one aircraft |
| Stretch | a fourth path when one is provisioned |
| Verification | `scripts/drone-crew-e2e.mjs` |
| Status | **implemented** |

### DRN-11.08 — Measured, not assumed, on a slowed processor

**Statement.** Performance is reported from real painted frames on a processor slowed ten times, because a desktop pins every case at the refresh rate and hides any change; the heaviest case is the one quoted.

| field | value |
|---|---|
| Input / Output | `DRN-11.08.IN` → `DRN-11.08.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 48.0 to 57.3 fps at level 5.5, ten times slowed |
| DTM target | a before and after that can be compared |
| Stretch | run on the real board |
| Verification | `scripts/drone-perf.mjs` |
| Status | **implemented** |

### DRN-13.01 — Nine runs, or it is not determinism

**Statement.** Every gate runs nine times and every reproducible artefact — the model hash, the schedule, a flown path, a crew code, the rung verdicts — must be byte-identical across all nine, and the spiral is asserted in both directions rather than asserted about.

| field | value |
|---|---|
| Input / Output | `DRN-13.01.IN` → `DRN-13.01.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 1,395 checks per run, nine runs, zero drift, ten spiral checks |
| DTM target | a report a reviewer can re-run |
| Stretch | the same harness for every domain |
| Verification | `scripts/drone-spiral9.mjs` |
| Status | **implemented** |

### DRN-09.03 — Forty-two aircraft, twenty-one against twenty-one

**Statement.** An engagement puts 42 aircraft in the air as two opposing sides of 21, held in flat columns so a tick allocates nothing, and drawn as an overlay in the arena's own camera batched into a handful of paths.

| field | value |
|---|---|
| Input / Output | `DRN-09.03.IN` → `DRN-09.03.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-02` · `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a tick allocates nothing; 42 aircraft at rung 2.3 hold the 30 Hz reference on a processor slowed ten times |
| DTM target | 42 aircraft flying |
| Stretch | the same columns carry a thousand |
| Verification | `tests/drone-swarm.test.mjs · scripts/drone-perf.mjs` |
| Status | **implemented** |

### DRN-11.09 — One budget, two consumers: the aircraft are the world

**Statement.** In an engagement the swarm takes its share of the rung's segment budget first and the Capitol block takes what is left, so at a poor rung the world thins rather than the aircraft vanishing; and every aircraft is drawn at the smallest size before any is given detail.

| field | value |
|---|---|
| Input / Output | `DRN-11.09.IN` → `DRN-11.09.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | all 42 drawn at every one of the 25 rungs |
| DTM target | 42 drawn at rung 1.1 with the world at 112 segments |
| Stretch | the same rule wherever a budget is shared |
| Verification | `tests/drone-swarm.test.mjs` |
| Status | **implemented** |

### DRN-10.05 — Five authority levels, and silence is never consent on any of them

**Statement.** Autonomy rides on the band that already exists: level 1 a named person per shot, level 5 the key group's live lean as a standing mandate it can end at once; at every level a named human decision exists before any machine shot, and what scales is the breadth of one decision rather than whether one happened.

| field | value |
|---|---|
| Input / Output | `DRN-10.05.IN` → `DRN-10.05.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a refusal from all five levels when nothing has been decided |
| DTM target | five levels selectable |
| Stretch | the ladder serves any domain with a machine and an authority |
| Verification | `tests/drone-authority.test.mjs` |
| Status | **implemented** |

### DRN-13.02 — A score that can fall

**Statement.** Every SSSES pillar is computed from evidence produced in the same run — authority checks, nine-run drift, measured frames against the declared reference, whether the projection memo demonstrably holds, and duplication and dead exports counted from the source — and a performance file measured at a different commit is refused rather than trusted.

| field | value |
|---|---|
| Input / Output | `DRN-13.02.IN` → `DRN-13.02.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no pillar is a literal; the score fell before it rose |
| DTM target | a score a reviewer can re-derive |
| Stretch | the same scorer for every domain |
| Verification | `scripts/drone-spiral9.mjs` |
| Status | **implemented** |

### DRN-05.04 — The beam, the shield, and the time it takes

**Statement.** Engagement is a fluence model, not hit points: irradiance at the target falls with beam spread and atmospheric loss, so the same emitter needs under a second in close and seconds at reach, and a fine-track lock means a broken beam loses its progress. The shield absorbs first and nothing reaches the airframe until it fails.

| field | value |
|---|---|
| Input / Output | `DRN-05.04.IN` → `DRN-05.04.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-09` · `U-WF-11` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the published dwell table is what the model computes, at every range |
| DTM target | a player learns to close and to hold, without being told |
| Stretch | a real emitter profile behind the same interface |
| Verification | `frontend/tests/drone-laser.test.mjs` |
| Status | **implemented** |

### DRN-05.05 — Nothing is destroyed in the air

**Statement.** A defeated aircraft is DISABLED and flown down under control to a safe landing. There is no reachable state called destroyed, and a beam on an aircraft already coming down changes nothing about its descent.

| field | value |
|---|---|
| Input / Output | `DRN-05.05.IN` → `DRN-05.05.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | every state a beam can reach is one of five, and none of them is destroyed |
| DTM target | real machines over a real floor with people underneath |
| Stretch | a physical descent check before every match |
| Verification | `frontend/tests/drone-laser.test.mjs` |
| Status | **implemented** |

### DRN-05.06 — Lighting inside the vector law

**Statement.** The engagement reads from stroke colour and weight alone — tracking, absorbing, the moment the shield fails, burning, coming down — plus a shield ring drawn as a declared n-gon that shrinks as the shield is spent. No glow, no gradient, no bloom.

| field | value |
|---|---|
| Input / Output | `DRN-05.06.IN` → `DRN-05.06.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-11` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | every stage names a palette role and a declared stroke weight |
| DTM target | forty-two engagements readable as shapes |
| Stretch | the same language on every 2525 surface |
| Verification | `frontend/tests/drone-laser.test.mjs` |
| Status | **implemented** |

### DRN-08.01 — One aircraft, two sizes

**Statement.** The mini foil scales to the full one by geometric similarity, with every derived quantity following the exponent it actually has: area as the square, mass as the cube, stall as the square root. The geometry, the proportions and every rule about materials or people are unchanged by the switch.

| field | value |
|---|---|
| Input / Output | `DRN-08.01.IN` → `DRN-08.01.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-01` · `U-WF-04` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | stall at the full size is the square root of the length ratio times the mini's, recomputed rather than asserted |
| DTM target | a demo at the Capitol at the size it will really be |
| Stretch | a third size needing one line and no arithmetic |
| Verification | `frontend/tests/drone-scale.test.mjs` |
| Status | **implemented** |

### DRN-10.06 — The same game at every N

**Statement.** The engagement runs at 1v1, 2v2 and every rung to 21v21 — 42 aircraft, 84 seats — from one model, so what breaks can be found on the way up rather than only at full strength. Each rung is measured for beam waste, contention, decision load and seats.

| field | value |
|---|---|
| Input / Output | `DRN-10.06.IN` → `DRN-10.06.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-06` · `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | twenty-one rungs, three seeds each, twelve computed review lenses, all passing |
| DTM target | a complexity found at the rung where it appears |
| Stretch | the ladder run against a live crew |
| Verification | `frontend/tests/drone-contest.test.mjs` |
| Status | **implemented** |

### DRN-10.07 — The person is the bottleneck, and it is measured

**Statement.** The invariant that a machine shot needs a named human decision does not bend with N, so the decision load is measured at every rung and compared against what the people present can physically answer. Levels one to three put the decision with one person; four and five put it with a quorum of the key group, which is the only thing that raises a throughput ceiling.

| field | value |
|---|---|
| Input / Output | `DRN-10.07.IN` → `DRN-10.07.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no rung at the top authority level asks for more decisions than its approvers can answer |
| DTM target | an argument for the authority ladder from measurement, not assertion |
| Stretch | the same measurement against real approvers |
| Verification | `frontend/scripts/drone-ladder.mjs` |
| Status | **implemented** |

### DRN-12.01 — From open play to a real floor

**Statement.** Progression runs open play to the winners' circle to a tournament to a physical match. The cut is one ninth, the R-CORE recursion gate, floored and published with its cut line. The score is dominated by accuracy and by whether decisions were answered rather than by kills.

| field | value |
|---|---|
| Input / Output | `DRN-12.01.IN` → `DRN-12.01.OUT` |
| Vision • 2525 | § XII — Flywheel |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | one ninth holds at every field size from nine to three hundred and ninety-nine |
| DTM target | the eleven per cent who want the real thing find it |
| Stretch | a season run end to end |
| Verification | `frontend/tests/drone-winners-circle.test.mjs` |
| Status | **implemented** |

### DRN-12.02 — The last gate is not about skill

**Statement.** A physical match requires a NAMED safety officer, both crews present, the arena clear of anyone not in the match, and every machine's descent checked. None of those is a score, a perfect record opens none of them, and a refusal names the condition that is missing without discarding the entry.

| field | value |
|---|---|
| Input / Output | `DRN-12.02.IN` → `DRN-12.02.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a champion with a perfect record still cannot clear an arena |
| DTM target | nobody is hurt by a tournament |
| Stretch | a signed floor clearance on the record |
| Verification | `frontend/tests/drone-winners-circle.test.mjs` |
| Status | **implemented** |

### DRN-11.10 — The aircraft is the FOIL

**Statement.** This surface calls the aircraft the FOIL and uses no other name for it — not in a label, a lexicon value, a CRS row, a component, a shipped document or an export. The geometry provenance path is the single declared exception, because a derivation that cannot name its source cannot be verified.

| field | value |
|---|---|
| Input / Output | `DRN-11.10.IN` → `DRN-11.10.OUT` |
| Vision • 2525 | § IX — Trust |
| U-WF trace | `U-WF-05` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | forty-four files scanned, the token refused outside two declared provenance fields |
| DTM target | a name we chose, used everywhere |
| Stretch | the provenance carried under a neutral path |
| Verification | `frontend/tests/drone-naming.test.mjs` |
| Status | **implemented** |

### DRN-07.01 — The address the game answers at

**Statement.** The game is served at /drone-2525 as well as at /main/Drone-2525. Both routes render the one component, their code is identical, and both are emitted to the static export — a route that compiles but does not emit is a 404 in production and green in CI.

| field | value |
|---|---|
| Input / Output | `DRN-07.01.IN` → `DRN-07.01.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | both pages exist, import the same component, and appear in out/ |
| DTM target | a link that works when it is handed to somebody |
| Stretch | the live site checked from outside the sandbox |
| Verification | `frontend/tests/drone-route.test.mjs` |
| Status | **implemented** |

### DRN-04.01 — One seat geometry for every vehicle

**Statement.** The pilot's eye is at +0.18 L forward and +0.12 L up; the targeteer's at −0.15 L (aft) and −0.04 L, as fractions of the nose-to-tail length; a turret's separation is exactly zero. The same geometry on rotors, on the wing and through a transition, and the same on every 2525 vehicle — read from the controls schema, never retyped.

| field | value |
|---|---|
| Input / Output | `DRN-04.01.IN` → `DRN-04.01.OUT` |
| Vision • 2525 | § IV — LINK |
| U-WF trace | `U-WF-02` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 28.5 cm between the eyes on the 0.7777 m foil in every mode; turret 0; no second geometry in the source |
| DTM target | a crew's pictures agree the same way through a transition |
| Stretch | a Manta or a droid inherits its parallax from its own length |
| Verification | `frontend/tests/seat-view.test.mjs` |
| Status | **implemented** |

### DRN-05.07 — Amber, then red — no shot without a second authority

**Statement.** A designation (button, key, tap, or spoken 'target') puts a door in a slot as AMBER and cannot fire. Only APPROVE — a second person over the link, or the same person acting explicitly as HI-2 — turns it RED, and the record says which of those it was. FIRE, a double-tap and a spoken 'fire' share one path and are refused with NO_RED_BOX or AMBER_NO_APPROVE; every refusal is recorded as a decision, never merely toasted.

| field | value |
|---|---|
| Input / Output | `DRN-05.07.IN` → `DRN-05.07.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | approve() is the only writer of red; a double-tap on a fresh mark is a refusal; two-step and two-person told apart |
| DTM target | a first mark can never be a shot |
| Stretch | the second authority arrives over the crew link |
| Verification | `frontend/tests/drone-slots.test.mjs · frontend/tests/drone-tap.test.mjs` |
| Status | **implemented** |

### DRN-06.01 — The decision record and the replay hash

**Statement.** Every designate, approve, refusal, hold and shot is a decision with a sequence id, a named actor, designated, hiApproved, authorityLevel, challenge, diff and the ledger's revision. The feed line is time | actor | verb | id | result. The replay hash is FNV-1a 64 over exactly r.043's projection — no wall-clock, no ISO, no FPS, no SID — and free-form notes sit beside the stream and are never hashed.

| field | value |
|---|---|
| Input / Output | `DRN-06.01.IN` → `DRN-06.01.OUT` |
| Vision • 2525 | § VI — SYNC / Replay |
| U-WF trace | `U-WF-06` · `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a note carrying 'fps 58.3 sid 9f3a' does not move the hash; a different game time does not either; the same round replays to the same hash |
| DTM target | two runs can say WHICH row they disagreed at |
| Stretch | the sidecar beside the immutable Cup fixture |
| Verification | `frontend/tests/drone-decisions.test.mjs` |
| Status | **implemented** |

### DRN-07.02 — The operator deck: keys, sticks, touch and voice

**Statement.** EXEL-2525-CONTROLS-1 is the one map on every 2525 vehicle: L stick BODY (W A S D), R stick HEAD (arrows), Q E turn, U J climb, T next door, 1 2 3 the slots, F fire, C capture. Keys write the refs a thumb does; arrows and the R stick are one gimbal rate inside the declared limits; a tap marks, a double-tap fires only red, the aiming seat's drag looks; voice is offered, never required; calibration is persisted so a resting thumb reads zero; the keymap prints. Typing never flies.

| field | value |
|---|---|
| Input / Output | `DRN-07.02.IN` → `DRN-07.02.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-09` · `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | schema held field for field against r.050; arrows do not move the airframe; a 6% resting thumb reads zero; no gesture reaches a shot without canFire |
| DTM target | a person flies and aims with what is in front of them |
| Stretch | the same deck on the turret, the Manta, the Ark and the droid |
| Verification | `frontend/tests/drone-controls.test.mjs · controls-schema · stick-sets · drone-tap` |
| Status | **implemented** |

### DRN-10.08 — Compare on what he asked to compare on

**Statement.** Every rung of the ladder reports designation rate, HI holds, auth failures, handoffs, simulation speed and a replay hash beside the three it already measured — not winners only. HI holds read zero in the contest and say so, because the contest models a person as a latency and never as a refusal; the holds that are real live in the decision record. Simulation speed is a wall-clock throughput and is never inside the hash.

| field | value |
|---|---|
| Input / Output | `DRN-10.08.IN` → `DRN-10.08.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the same seed replays to the same hash at every rung; level 3 refuses far fewer fire-ticks than level 1 |
| DTM target | a rung can be compared with yesterday's |
| Stretch | the Cup fixture replayed against these six |
| Verification | `frontend/tests/drone-contest.test.mjs · frontend/scripts/drone-ladder.mjs` |
| Status | **implemented** |

### DRN-10.09 — CH1–CH5 and DIFF 1–5, from the deck

**Statement.** Five challenges × five difficulties, the deck's challengeSpec verbatim: quota 4+CH×2+DIFF doors in play, window scaled by rate max(10, 56−CH×6−DIFF×4), concurrent doors 1→3, moving from CH3, NET at CH5. Chosen from two dropdowns in the top bar, never a hidden selector. At CH5 NET a red box must come from a second person; self-approval is refused CH5_NO_APPROVE and recorded as a HOLD. TG is not resurrected.

| field | value |
|---|---|
| Input / Output | `DRN-10.09.IN` → `DRN-10.09.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 25 CH×DIFF cells equal to r.050's function; harder is never more time; CH5 self-approval refused and on the record |
| DTM target | a player feels the rung, and CH5 needs two people |
| Stretch | moving targets from the swarm at CH3+ |
| Verification | `frontend/tests/drone-challenge.test.mjs` |
| Status | **implemented** |

### DRN-06.02 — The AsM Cup as a regression fixture

**Statement.** ASM_CUP_99.json and ASM_ROSTER.csv are read-only by contract: their bytes are pinned by sha256 and a gate refuses if either moves. The published baseline — seed 2525, 6v6 BLU 3–1, 3v3 BLU 5–0, pairs 1–3 RED and 4–6 BLU at every level — is recomputed from the fixture's numbers, not restated. A new run writes a sidecar beside the fixture, never at it. The fixture's declared revision (0.039) is reported and flagged, not gated.

| field | value |
|---|---|
| Input / Output | `DRN-06.02.IN` → `DRN-06.02.OUT` |
| Vision • 2525 | § VI — SYNC / Replay |
| U-WF trace | `U-WF-06` · `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | two hashes pinned; four baseline sentences recomputed; roster = the twelve; sidecar path ≠ fixture path; the module imports no filesystem |
| DTM target | a later build is judged against his published result |
| Stretch | the contest replays the Cup at seed 2525 and compares his six |
| Verification | `frontend/tests/asm-cup.test.mjs` |
| Status | **implemented** |

### DRN-09.04 — The second person, over the link

**Statement.** The targeteer's amber mark goes out on the gimbal word; the pilot's device shows it with an APPROVE button; the approval crosses the link as its own message, authored by seat, applied once by sequence on the device that holds the mark, in the sender's name — so the box reads two-person. A seat cannot approve its own mark through the link: its echo is dropped and the holder refuses a sender equal to the designator.

| field | value |
|---|---|
| Input / Output | `DRN-09.04.IN` → `DRN-09.04.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | approve authored by either seat, needs door + slot; duplicate lands once; own echo dropped; holder refuses self |
| DTM target | two phones make one red box, with two names on it |
| Stretch | the same message on the Manta and the droid |
| Verification | `frontend/tests/drone-link.test.mjs` |
| Status | **implemented** |

### DRN-08.02 — The platform identities, from the deck

**Statement.** The eight PLATFORM identities are data lifted from the deck — id, label, kind and flight mode — and a gate compares them to r.050's own units table and dropdown. A platform dropdown sits beside the mode row. D1Q QUAD·HOVER has no wing and refuses the transition by name; D1 VTOL flies both; D1F FOIL takes the wing the moment the transition is legal. Platforms that do not exist on this arena are listed and dated, never hidden; the round never forks a second airframe model per platform.

| field | value |
|---|---|
| Input / Output | `DRN-08.02.IN` → `DRN-08.02.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-05` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 8 ids, labels, kinds and 3 flight modes equal to r.050; D1Q wing refused; D1F auto-transitions; one airframe model |
| DTM target | the same platform names on the deck and in the app |
| Stretch | Manta, Ark and the droid on their own arenas |
| Verification | `frontend/tests/drone-platforms.test.mjs` |
| Status | **implemented** |

### DRN-05.08 — The range: per-lane pop-ups at 50–300 m, three modes

**Statement.** At CH0 the turret sits on one of 42 lanes and the ten silhouettes of the 9127 Alt-C sheet (50 F · 100 F ×3 · 150 E ×2 · 200 E ×2 · 250 E · 300 E) stand DOWN until their exposure: one plate per lane at a time, in a seeded per-lane order, for 3 s at 50 m rising 1 s per 50 m to 8 s at 300 m, 1.5 s between exposures. TRAINING·RESET raises a downed target on its next scheduled exposure; TRAINING·DOWN keeps it down until RESET; QUAL·40 runs tables I/II/III of 20/10/10 exposures under clocks that cover them (142/75/75 s), one round per exposure, a lapse is an unfired MISS, a table exposes only what it can score, 23/30/36.

| field | value |
|---|---|
| Input / Output | `DRN-05.08.IN` → `DRN-05.08.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 10 silhouettes; exposures 3..8 s by distance; 42 independent lane orders; one round per exposure; 40/40 attainable |
| DTM target | a first-timer sees a target rise, its caption and its seconds, on a phone |
| Stretch | a canonical range clock shared between two phones (r.132) |
| Verification | `frontend/tests/range-2525.test.mjs · frontend/scripts/drone-deck-qa.mjs (RANGE_POP_SCHEDULE, MODE_RESET_RETURNS, MODE_DOWN_STAYS, MODE_QUAL_TIMED, ONE_ROUND_PER_EXPOSURE, TABLE_III_EXPOSES_ONLY_SCORABLE)` |
| Status | **implemented** |

### DRN-05.09 — A hit is scale-true; a target that is down is not a target

**Statement.** A silhouette is hit when the pip sits inside its projected outline (CIRCLE inside the aiming circle, else SILHOUETTE), with an angular floor of 3 mrad (min 3 px) so the standard is the same at every zoom and screen size. A plate that is down, falling, or not the lane's current exposure cannot be hit and is never resurrected; when a target goes down (hit or lapsed) its amber/red box goes with it; a miss keeps red for that exposure only and never re-arms the CH5 second authority.

| field | value |
|---|---|
| Input / Output | `DRN-05.09.IN` → `DRN-05.09.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | RANGE_HIT_50 / RANGE_HIT_300 aimed hits; RANGE_MISS_300_OFF20; LAPSE_RELEASES_THE_BOX; no simDirect in any range row |
| DTM target | the pip on the silhouette is the whole rule |
| Stretch | an outline test (dome / shoulders) instead of the bounding rectangle |
| Verification | `frontend/scripts/drone-deck-qa.mjs (RANGE_HIT_*, LAPSE_RELEASES_THE_BOX) · frontend/tests/drone-playable.test.mjs` |
| Status | **implemented** |

### DRN-04.02 — One forward basis; one aim from the eye

**Statement.** The camera vector, the projector, the body motion, the lock cone, every aim and every box read ONE basis (fwdOf/rightOf/yawTo, derived from the projector) and ONE world position (worldOf); every aim is computed from the camera eye to the centre of mass (aimUnitAt). Before r.130 the camera vector and the projector disagreed by 180°: the range pit looked away from its plates and left-stick forward flew the airframe away from what the screen showed.

| field | value |
|---|---|
| Input / Output | `DRN-04.02.IN` → `DRN-04.02.OUT` |
| Vision • 2525 | § IV — LINK |
| U-WF trace | `U-WF-04` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | camera forward lands at screen centre at every yaw/tilt; a fixed point slides left when the stick pans right; the body closes on the point on screen at yaw 90° |
| DTM target | forward is what you see |
| Stretch | the same basis lifted into lib/wire-core for every 2525 surface |
| Verification | `frontend/tests/range-2525.test.mjs · frontend/scripts/drone-deck-qa.mjs (PROJ_FWD_AGREES, PAN_RIGHT_MOVES_WORLD_LEFT, FWD_AT_YAW90, PIT_SEES_300_RIGHT_250_LEFT)` |
| Status | **implemented** |

### DRN-11.11 — Render honesty: exceptions recorded, must-hit first, the deck's own QA in CI

**Statement.** A render exception is recorded (state.drawErr), never swallowed; a frame stamps that it ran to its last line and a deferred QA row reads it. What you must hit takes the segment budget before the world on every channel. The deck's boot QA (84 rows) runs in headless Chromium in CI against a checked-in row manifest with set equality and a tight red-list; QA snapshots and restores the record and leaves no designation on the first screen.

| field | value |
|---|---|
| Input / Output | `DRN-11.11.IN` → `DRN-11.11.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | DRAW_COMPLETES; QA_LEAVES_NO_TRACE; deck-qa manifest equality; only SYNC_DIRECT red on one device |
| DTM target | a green board that cannot lie about the picture |
| Stretch | a completeness assertion that the must-hit set survived the budget |
| Verification | `frontend/scripts/drone-deck-qa.mjs · frontend/tests/deck-qa-manifest.mjs` |
| Status | **implemented** |

### DRN-13.03 — Every operator-deck revision documented, chain-hashed, gated

**Statement.** docs/drone-2525/operator-deck/REVISIONS.md carries one entry per carried revision from r.128 — bytes, sha256, shipping commit, what changed, corrections of earlier claims as NEW lines — chained by sha256(prev|rev|sha|bytes); a gate holds every carried file to its row, README HEAD to the last row, and the chain intact; ONE tests/deck-head.mjs names HEAD for every deck-reading gate; the patchers that built each revision are carried beside it.

| field | value |
|---|---|
| Input / Output | `DRN-13.03.IN` → `DRN-13.03.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 4 entries r.128..r.131; chain intact; HEAD constant equal across gates and the PLAY button |
| DTM target | a reviewer can check every revision by hash tomorrow |
| Stretch | a chain that also covers the narrative and the shipping commit, anchored in git |
| Verification | `frontend/tests/drone-revisions.test.mjs · frontend/tests/drone-playable.test.mjs` |
| Status | **implemented** |

## Revisions (append only)

| revision | date | kind | commit | why |
|---|---|---|---|---|
| 0.001 | 2026-09-15 | ask | `7b0b790` | Drone-2525 1st Pass persisted verbatim; the CRS ladder opens at Version 00.00. |
| 0.002 | 2026-09-15 | decision | `ea91e01` | The MoT ladder replaces the four-tier fidelity table: 5 compute bands x 5 resolution steps, 1.1 the arcade rung and the fastest. Demand and capability are separated. Multi-sensor fusion EO->CHEM sheds from the top and never sheds EO. The unit holds 1080p30 and names every step down. A self-test walks the ladder in 6 to 15 minutes and reports the rung this machine actually holds. |
| 0.003 | 2026-09-15 | release | `a3cb92f` | The flying modes. A fixed-wing VTOL hovers on rotors and cruises on a wing, with the stall speed as the number the transition turns on. A machine may hold the pilot's seat, the camera operator's seat, or both — and a machine that is aiming must ask a named person before any shot, a gate with no bypass. All four of the operator's modes are unlocked. |
| 0.004 | 2026-09-15 | release | `a1a9ff7` | Shared Intent, as a placeholder: a key group switched on or off, invited by a code and a scannable square, answering a real-time call on the decision the round already has. The group advises and a named person still decides. Recognition uses the three fixed heart values the pod ladder already declares, rather than a fourth invented for this feature. |
| 0.005 | 2026-09-15 | release | `dffaa79` | Two people on two devices: one flies, one works the camera, and neither can do the other's job. Optimised against a measurement on a processor slowed ten times rather than against a desktop that hides every change. Nine runs of every gate with zero drift, ten spiral checks, SSSES 93. |
| 0.006 | 2026-09-15 | release | `c7c40a5` | Forty-two aircraft, 21 against 21, inside the ladder's budget at every rung. A five-level authority ladder on the band that already exists, with the invariant unbent on every rung. And an SSSES scorer rewritten to measure rather than assert, which dropped the score before the work lifted it to 96. |
| 0.007 | 2026-09-16 | decision | `` | The fixed-wing is declared at the 1.111 m foil, proportional, from the early Security-2525 drawing — and the drawing's axes are declared with it, because the shipped extent had span, depth and nose-to-tail under each other's names and every consumer inherited the permutation. |
| 0.008 | 2026-09-16 | decision | `` | The wing is derived from the drawing instead of asserted beside it. wingAreaM2 was 0.9 m2 against a real planform of 0.3092 m2, so the round had been flying a wing three times the size of the one on screen. Stall moves 8.43 -> 14.38 m/s, and the quad's acceleration is now derived from the stall rather than typed, so the transition stays reachable on any airframe. |
| 0.009 | 2026-09-16 | decision | `` | The aircraft is the FOIL on this surface and nowhere says its other name, with one declared exception for the provenance path. The game answers at /drone-2525 as well as /main/Drone-2525. |
| 0.010 | 2026-09-16 | decision | `` | Lasers, shields and time on target. A fluence model rather than hit points, so range is the thing that matters: 0.79 s to disable at 100 m, 5.15 s at 400 m, 13.67 s at 700 m. A defeated aircraft is disabled and flown down under control at 1.6 m/s; there is no state called destroyed. |
| 0.011 | 2026-09-16 | decision | `` | Ten CRS rows for the overnight work: the beam and shield rules, the safe descent, the lighting inside the vector law, the two sizes and the square-cube law between them, the N ladder and what it measured about the person, the winners' circle and its one-ninth cut, the floor that must be cleared by name, the FOIL naming rule, and the address the game answers at. |
| 0.012 | 2026-09-16 | decision | `` | Operator deck r.050 adopted. The keyboard (EXEL-2525-CONTROLS-1) through the ref bus; the R stick is the HEAD; one centred look-stick in the turret modes; stick calibration persisted; tap, double-tap and look-drag on the arena; the T-box; voice on r.042's grammar; T1 T2 T3 with AMBER then RED and a fire that refuses NO_RED_BOX and AMBER_NO_APPROVE and records both; the decision record with r.043's replay hash; ONE seat geometry for every vehicle (28.5 cm on the foil, turret 0) replacing the two we had — a correction on the record; and the ladder measuring his six: designation rate, HI holds, auth failures, handoffs, sim speed, replay hash. |
| 0.013 | 2026-09-16 | decision | `` | CH1–CH5 × DIFF 1–5 from the deck's challengeSpec, verbatim (25 cells checked against r.050's own function): quota bounds the doors in play, rate shortens the window, concurrent grows 1→3; at CH5 NET the red box must come from a second PERSON, and a device approving its own mark is refused CH5_NO_APPROVE and recorded as a HOLD. The AsM Cup (99 runs, seed 2525) is the regression fixture, read-only by contract: its bytes are pinned by hash and the published baseline — 6v6 BLU 3–1, 3v3 BLU 5–0, pairs 1–3 RED, 4–6 BLU — is recomputed from its numbers; the fixture's declared revision 0.039 is reported, not gated. APPROVE crosses the crew link: the targeteer's amber goes out on the gimbal word, the pilot approves it by name, and the mark turns red two-person on the device that holds it. Verify Live now probes /drone-2525/ itself. |
| 0.014 | 2026-09-16 | decision | `` | PLATFORM identities from the deck, as data: T1 TURRET · D1Q QUAD·HOVER · D1 VTOL · D1F FOIL · M99 MANTA 99-66 · M66 MINI 66-33 · ARK SAIL 33 · R2 MASS DROID — ids, labels, kinds and flight modes lifted from r.050's units table and checked against it by a gate. The four that exist on this arena are live; the sea, sail and droid platforms are dated, not hidden. D1Q has no wing and says so; D1F goes to the wing the moment it can; D1 is the VTOL as before. Also: the red box and its decision id now mirror to the approving device (eXeL AI gate 3). |
| 0.015 | 2026-09-17 | decision | `` | Round 12 P0: operator deck HEAD moved r.050 -> r.075 and the control-schema transcription is re-pinned to it (controls.ts revision 0.075; the controls-schema gate now reads drone-2525_r.075.html, resolving BUILD when it lifts window.CONTROLS). The CONTROLS contract is byte-identical r.050 -> r.075 apart from the revision field, so this is a pin, not a behaviour change. window.CONTROLS.vehicle still names manta-99-66 (the control-map contract, which craft share the same map) -- distinct from the PLAYABLE platform roster, where the operator removed Manta (a later P3 revision). r.051-r.075 + the SSSES/SPIRAL-99 audit are carried read-only. |
| 0.016 | 2026-09-19 | release | `9870031` | The range with turrets actually works (operator ask 2026-09-19, persisted with the 9127 sheet). Operator deck HEAD r.130 then r.131: one forward basis (the camera vector and the projector were 180 degrees apart), per-lane pop-ups at 50-300 m with FM 3-22.9 exposure times, TRAINING RESET / TRAINING DOWN / QUAL 40, a scale-true angular hit rule, one round per exposure, the box goes down with its target, the deck's own QA in CI, and an append-only chain-hashed revision register. Five DRN rows added: the range (DRN-05), the hit rule (DRN-05), one basis (DRN-04), render honesty (DRN-11), the register (DRN-13). A 24-lens fleet review of r.130 is recorded in docs/assessments/2026-09-19_r130_fleet_review.md. |
| 0.017 | 2026-09-19 | release | `3d653bc` | Operator deck HEAD r.132: the wire path obeys the same rules as the buttons (peer marks land in slots with their author, a peer approval needs a seated human other than the marker, a snapshot never hands over red, HOLD/REJECT rows travel); two isolated browser contexts over WebRTC end on ONE replay hash (frontend/scripts/drone-team-e2e.mjs). Entered late (Krishna A, fleet 48): the JSON sat two editions behind the register. |
| 0.018 | 2026-09-19 | release | `449de5f` | Operator deck HEAD r.133: the 38-AsM team-test fold. The AI member was dead code in live play (asmTick never called from the loop) and fires only on a red box; the score strip wraps; the approver's device tallies HIT/MISS and takes the box down from the same row; one sentence for an empty range; no lapse before the round starts; TARGET aims as well as marks. |
| 0.019 | 2026-09-19 | release | `f2557ac` | Operator deck HEAD r.134: the fold of team-test wave 3 and the 48-lens fleet review. RESET and mode change are canonical rows that travel and ask before discarding a live table; the link has a heartbeat and a lost approver is said (its approval does not outlive it); VOICE reports the outcome and negation is a HOLD; MAP keeps the strip; the AI member is an actor on the record (ASM@device) and never marks a dead plate; authority has one consumer (scene change, reset, lost approver release it with a row); the CH5 popup is gone; a door tag is on the record; dead refs are refused; keys ignore text fields and the intro; a training lapse is said and is a row; refusals are drawn above the intro; the host shows the other team's six digits. |
| 0.020 | 2026-09-19 | release | `2020c02` | Operator deck HEAD r.135: the fold of the fleet's second (adversarial) reviewers. Three r.134 regressions closed (the uncalled frame tail; a training-lapse row from both phones that diverged the records — one lapse clock per room; a DROPPED link undone by the next message). The fire gate's remaining members: the round must be open, the approver's seat never fires the marker's box, a door tag needs a red box, an expired target scores nothing, a row's data.by is never an AI passport, CH5 re-approves after a miss, a refused wire approval is a HOLD row, a received APPROVER_LOST demotes the shooter's red. The record carries the decision's content and the actor; AI rows say AI; one name for a human's approval of the AI's mark; the AI never fires a qualification round. No session id or plate id reaches the player. Three regex QA rows became behaviour rows; a loop-driven AI row. 116 rows, 115/116 headless. |
| 0.021 | 2026-09-19 | release | `543a86c` | Operator deck HEAD r.136: the fold of the 48-agent fleet's synthesis. The r.135 regression (an AI mark in a room refused for every seat: the marker's-seat rule was cut one token too wide) is closed — the marker's seat is the human or its own AI, and the approver is told the other seat fires; every peer release demotes the shooter's box; no red box crosses from the waiting room into LIVE; an expired drone takes its box; two QA rows now assert what they name (MAP strip names the plate; the lost-approver sentence). 117 rows, 116/117 headless. |
| 0.022 | 2026-09-23 | release | `c0e57ea` | Operator deck HEAD r.137: the range the operator described. TRAINING · RESET / DOWN stand every target (a hit one comes back after 3 s, or stays down; no exposure clock, nothing lapses). QUAL · 40 is the Army IWQ Table VI 40-round day fire he supplied: 18 engagements in 4 phases, singles/doubles/triples/quads up TOGETHER for 5/8/12/16 s, 3 s between engagements, 9 s between phases, one round per silhouette, an unengaged target is an unfired miss, 40 targets (50×6 100×7 150×8 200×8 250×6 300×5), the 50 R standing shot first; engagements 15–18 DECLARED. C-50L added (11 silhouettes). r.130's by-distance exposures and 20/10/10 tables superseded on the record. QA 124 rows, 123/124 headless. |
| 0.023 | 2026-09-23 | release | `c5995ce` | Operator deck HEAD r.138: true-scale silhouettes (F 0.495×0.508 m, E 0.495×1.016 m; a 300 m E is 1.6 px at 1×, the 3 mrad pip floor keeps it hittable), labels justified by side and stacked so none overlaps, the zoom law (turret 3× in QUAL · 40, 30× in training; every craft 30× optical; one clamp), magazine + reload (10 rounds in QUAL as four magazines with the phase rest reloading on the record, 30 elsewhere; an empty magazine refuses FIRE; RELOAD is a canonical row; counter + zoom at the top of the picture), full screen (bars, strip and dock hide; sticks, TARGET · APPROVE · FIRE and RELOAD stay), layout() toggles classes instead of rewriting them. QA 132 rows, 131/132 headless. |
| 0.024 | 2026-09-23 | release | `0f71961` | Operator deck HEAD r.139: landscape and portrait, like Mission Planning. A phone is a phone in both orientations (device class by the short side; r.138 classed a landscape phone as a desk). FULL fills the whole screen in every orientation — r.138's grid let the stage slide into an auto row at 36 % height, corrected on the record; FULL beats the desk layout. Portrait stacks the magazine line and the strip; landscape sets them side by side. Rotation re-lays out (orientationchange, screen.orientation). The deck-QA gate runs the boot QA in both orientations in CI. QA 136 rows, 135/136 headless in each. |
| 0.025 | 2026-09-23 | release | `9d45dae` | Operator deck HEAD r.140: the dark green plates and the 25 m Alt-C sheet. The F-type silhouette is 26 × 21 in (NSN 6920-00-071-4589; r.138's 19.5 × 20 corrected on the record), E 19.5 × 40 in. TARGETS picker: POP-UPS · 50–300 M or ALT-C SHEET · 25 M — a 17 × 22 in sheet at 25 m carrying the ten silhouettes scaled by 25/range (measured: the same height on screen as the real target at its range); one geometry through qWorld()/plateDims(); paper never falls — a hit is a hole, DOWN scores out, QUAL · 40 runs the same program on the sheet; the sheet travels with RESET. QA 140 rows, 139/140 in each orientation. |
| 0.026 | 2026-09-23 | release | `1e8b2d4` | Operator deck HEAD r.141: life-size at 12 inches. One focal length at 1× — the eye distance in CSS px (12 in × 152.3 px/in = 1827.6 px, iPhone 12 Pro Max) — so the picture subtends the angles the world does; the deck's typed 38° half-angle (a 76° field) is corrected on the record: a 50 m F is now 24 px wide at 1×, a 300 m E 6 px; the stick rate follows the real half-angle; the 50 L / 50 R stand at ±4.6 m, the edges of a portrait phone at 50 m. QA 142 rows, 141/142 in each orientation. |
| 0.027 | 2026-09-23 | release | `662f8e3` | Operator deck HEAD r.142: reload by hand on QUAL, the same order as the actual test. The engagement program names its silhouettes (left / centre / right) and is the same on every lane, every time — the r.137 per-lane seed retired by operator decision so a shooter can memorise the order; ten targets a phase = one magazine; the tower's rest says PRESS RELOAD and loads nothing; an empty magazine refuses FIRE; four magazines as issued (a fifth refused NO_MAGAZINE). QA 143 rows, 142/143 in each orientation. |
| 0.028 | 2026-09-23 | release | `a9c6360` | Operator deck HEAD r.143: the green horizon line removed; the bullseye is white and turns red when the pip sits on a target (a plate's outline + 8 px; other targets within 28 px), the silhouette's bracket red with it — aim, never authority, the amber→red designation box untouched; LOCK is the thing in the crosshair (with every target up it had named the nearer plate beside the pip). QA 146 rows, 145/146 in each orientation. |
| 0.029 | 2026-09-23 | release | `b2b64dc` | Operator deck HEAD r.144: the targets on each of the 42 lanes — every lane carries the same eleven silhouettes (42 × 11 = 462 plates, gated by id and range, standing in training); the neighbouring lanes (±1) draw every silhouette, dim, as on a real range; marking and firing stay on the shooter's lane. QA 148 rows, 147/148 in each orientation. |
| 0.030 | 2026-09-23 | release | `8531339` | Operator deck HEAD r.145: lane markers — white numbered boards on posts at each lane's left edge at 100, 200 and 300 m (the last lane both edges), 129 boards each with its lane number, in the wire idiom, drawn with the silhouettes for the shooter's lane and its neighbours; one pure laneMarkers(lane) for painter, HUD and QA. QA 149 rows, 148/149 in each orientation. |
| 0.031 | 2026-09-23 | release | `5466050` | Operator deck HEAD r.146: the boot QA's deferred rows (DRAW_COMPLETES, ASM_MARKED_BY_THE_LOOP) decide on their evidence — a completed frame or a render exception; the loop's mark — up to a declared ceiling (20 s / 5 s), not on a fixed 1.5 s / 700 ms timer that judged a cold CI runner 'no frame completed' for ever (Deploy #950). Proven under a 30x CPU throttle. QA 149 rows, 148/149 in each orientation. |
| 0.032 | 2026-09-23 | release | `7a7a2c7` | Operator deck HEAD r.147: LOCK, T1 · T2 · T3 and key 1 are the targets nearest the bullseye on the picture within LOCK_REACH_PX (48 px, declared), never the nearest in metres and never off the picture — one rule for plates, rings, buoys, pops, aircraft, foils and doors, on the range and the Capital (TARGET / APPROVE / FIRE had kept resetting to the 50 m left target). QA 152 rows, 151/152 in each orientation. |
| 0.033 | 2026-09-23 | release | `8ccab36` | Operator deck HEAD r.148: the eye outranks the mark — the 48-agent fleet's fold on r.147 (whose rule ran only on an empty board): a mark is held as LOCK only while the bullseye is on it; one door for TARGET / key N / voice that keeps a mark's phase and author and releases my own unfinished mark on the record; T1-T3 obey the reach on every craft; the AI member spots through the bullseye, never moves the seated head and waits for the HIT to be read; a FIRE with no box names NONE; the board under the strip; APPROVE's author from the authenticated envelope; a peer HIT needs an APPROVE on this record. QA 161 rows, 160/161 in each orientation. |
| 0.034 | 2026-09-23 | release | `6b585bf` | Operator deck HEAD r.149: a box only around the marked target — no edge markers unless targeted; amber on TARGET, red on APPROVE; the 3D wire box (voxel) around every marked slot in its phase colour (slotBoxes); the three range modes unchanged and re-stated (RESET pops back up, DOWN stays down, QUAL rises the engagement together and a hit one stays down for its window). QA 163 rows, 162/163 in each orientation. |
| 0.035 | 2026-09-24 | release | `ec241fa` | Operator deck r.150 — the fire gate reads the picture: a round or a tag is spent only on a red box the bullseye is on (FIRE says where, costs nothing); no tag without a red box; no approval on a dead plate; the approver looks at a peer's mark from any seat, never for an AI mark; the AI re-arms on reload and reads the HIT for 1.4 s; a forged red never shoots the bull; the deck's own SSSES row gone. Items 1–4 of the fleet's order (docs/assessments/2026-09-23_r147_fleet48_review.md). Corrections on the record for five earlier QA rows that fired off the picture. |

