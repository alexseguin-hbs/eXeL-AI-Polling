<!-- GENERATED by scripts/drone-render.mjs from docs/drone-2525/drone-2525.v00.00.json — do not hand-edit. -->
# CRS · Drone-2525

**Version 00.00 · revision 0.011** · Vision • 2525 Level-3 Domain Play on WIREFRAME-CORE

> Simulation for play and critique only. Bounding estimates, not design evidence. Not flight authorisation and not a weapons system.

Handoff: `docs/asks/2026-09-15_drone_2525_first_pass.md` `sha256 0d3987649112c0222a87795167eb1aa85b68df698c55b76dd929bab86233d1e2`

Id law (docs/vision-2525/CRS-GOVERNANCE-PLAN.md): `DRN-##.##`, two digits never letters; parents are permanent; sub-ids append only; a changed requirement is **superseded**, never rewritten.

## The ladder — one step per Vision • 2525 section

| CRS | Requirement | § | R-CORE | Phase | Status | Verified by |
|---|---|---|---|---|---|---|
| `DRN-01` | Arena frame and tiles | VII UCRS | Manual | pilot | implemented | `tests/drone-capitol-tiles.test.mjs` |
| `DRN-02` | Canonical wire model and one-step export | VIII Rehearsal | Manual | pilot | implemented | `tests/wire-export.test.mjs` |
| `DRN-03` | Edge primitives | XII Flywheel | Manual | pilot | implemented | `tests/wire-core.test.mjs · tests/vector-law.test.mjs` |
| `DRN-04` | One gimbal for turret and airframe | IV LINK | Manual | pilot | implemented | `tests/drone-gimbal.test.mjs` |
| `DRN-05` | Capture and shoot with line of sight | V EDGE | Manual | pilot | implemented | `tests/drone-game.test.mjs` |
| `DRN-06` | Score and replay | VI SYNC / Replay | Manual | pilot | implemented | `tests/drone-game.test.mjs` |
| `DRN-07` | Command surface, route and unlock | I Humanity First | Manual | pilot | implemented | `next build · tests/vector-law.test.mjs` |
| `DRN-08` | Flight and energy, bounded | IX Trust | Manual | pilot | implemented | `tests/drone-flight.test.mjs` |
| `DRN-09` | Two operators, two devices | III COMM | Manual | pilot | implemented | `tests/drone-link.test.mjs` |
| `DRN-10` | AI roles under human approval | X Governance | Manual | pilot | implemented | `tests/drone-game.test.mjs` |
| `DRN-11` | Vector law, the 13, and adaptive fidelity | XI SSSES | Manual | pilot | implemented | `tests/drone-fidelity.test.mjs · tests/vector-law.test.mjs` |
| `DRN-12` | Every word in 33 languages | XIII Translation | Manual | pilot | implemented | `tests/lexicon-coverage.test.mjs · tests/drone-i18n.test.mjs` |
| `DRN-13` | Versioned, revisioned, comparable | II R-CORE | Manual | pilot | implemented | `tests/traceability.test.mjs · tests/drone-crs.test.mjs` |
| `DRN-11.01` | The MoT ladder: five compute bands by five resolution steps | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-11.04` | Demand and capability are separate, and never mixed | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-05.03` | Multi-sensor fusion, shed from the top and never blind | V EDGE | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-11.05` | Hold 1080p30, and name every step down from it | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-11.06` | Self-test: the rung this machine actually holds, in 6 to 15 minutes | XI SSSES | Manual | pilot | implemented | `tests/self-cal-runner.test.mjs` |
| `DRN-11.07` | The robot may pick its own compute class | XI SSSES | Manual | pilot | implemented | `tests/mot-ladder.test.mjs` |
| `DRN-10.01` | Shared Intent: a key group, switched on or off | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-10.02` | Key members only, by invitation | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-10.03` | The group advises; a named person decides | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-10.04` | Volunteer input, recognised on the ladder that already exists | X Governance | Manual | pilot | implemented | `tests/si-pod.test.mjs` |
| `DRN-09.01` | A seat may only send what that seat controls | III COMM | Manual | pilot | implemented | `tests/drone-link.test.mjs` |
| `DRN-09.02` | Three paths carry every word between the two devices | III COMM | Manual | pilot | implemented | `scripts/drone-crew-e2e.mjs` |
| `DRN-11.08` | Measured, not assumed, on a slowed processor | XI SSSES | Manual | pilot | implemented | `scripts/drone-perf.mjs` |
| `DRN-13.01` | Nine runs, or it is not determinism | II R-CORE | Manual | pilot | implemented | `scripts/drone-spiral9.mjs` |
| `DRN-09.03` | Forty-two aircraft, twenty-one against twenty-one | III COMM | Manual | pilot | implemented | `tests/drone-swarm.test.mjs · scripts/drone-perf.mjs` |
| `DRN-11.09` | One budget, two consumers: the aircraft are the world | XI SSSES | Manual | pilot | implemented | `tests/drone-swarm.test.mjs` |
| `DRN-10.05` | Five authority levels, and silence is never consent on any of them | X Governance | Manual | pilot | implemented | `tests/drone-authority.test.mjs` |
| `DRN-13.02` | A score that can fall | II R-CORE | Manual | pilot | implemented | `scripts/drone-spiral9.mjs` |
| `DRN-05.04` | The beam, the shield, and the time it takes | V EDGE | Manual | pilot | implemented | `frontend/tests/drone-laser.test.mjs` |
| `DRN-05.05` | Nothing is destroyed in the air | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-laser.test.mjs` |
| `DRN-05.06` | Lighting inside the vector law | XI SSSES | Manual | pilot | implemented | `frontend/tests/drone-laser.test.mjs` |
| `DRN-08.01` | One aircraft, two sizes | VIII Rehearsal | Manual | pilot | implemented | `frontend/tests/drone-scale.test.mjs` |
| `DRN-10.06` | The same game at every N | VIII Rehearsal | Manual | pilot | implemented | `frontend/tests/drone-contest.test.mjs` |
| `DRN-10.07` | The person is the bottleneck, and it is measured | X Governance | Manual | pilot | implemented | `frontend/scripts/drone-ladder.mjs` |
| `DRN-12.01` | From open play to a real floor | XII Flywheel | Manual | pilot | implemented | `frontend/tests/drone-winners-circle.test.mjs` |
| `DRN-12.02` | The last gate is not about skill | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-winners-circle.test.mjs` |
| `DRN-11.10` | The aircraft is the FOIL | IX Trust | Manual | pilot | implemented | `frontend/tests/drone-naming.test.mjs` |
| `DRN-07.01` | The address the game answers at | I Humanity First | Manual | pilot | implemented | `frontend/tests/drone-route.test.mjs` |

## Every row in full

### DRN-01 — Arena frame and tiles

**Statement.** The arena is an ENU frame on the Texas Capitol origin, and every building, tree, road and contour it draws is either carried from the repo, hand-authored and badged, or read from a map extract.

| field | value |
|---|---|
| Input / Output | `DRN-01.IN` → `DRN-01.OUT` |
| Vision • 2525 | § VII — UCRS |
| U-WF trace | `U-WF-01` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | tile gate green; every element carries a source |
| DTM target | hand-authored block renders |
| Stretch | OSM extract replaces it with zero code change |
| Verification | `tests/drone-capitol-tiles.test.mjs` |
| Status | **implemented** |

### DRN-02 — Canonical wire model and one-step export

**Statement.** One model feeds the browser and the four engine targets, and a change is visible in all of them or none.

| field | value |
|---|---|
| Input / Output | `DRN-02.IN` → `DRN-02.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-02` · `U-WF-05` · `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | lossless round trip on the FOIL wireframe |
| DTM target | py/obj/cs/cpp from one model |
| Stretch | every domain exports through it |
| Verification | `tests/wire-export.test.mjs` |
| Status | **implemented** |

### DRN-03 — Edge primitives

**Statement.** Buildings, trees, roads and contours draw as edges only, with door and window openings in their real wall.

| field | value |
|---|---|
| Input / Output | `DRN-03.IN` → `DRN-03.OUT` |
| Vision • 2525 | § XII — Flywheel |
| U-WF trace | `U-WF-02` · `U-WF-10` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no fill on any drawn element |
| DTM target | Capitol block renders as edges |
| Stretch | shared with Architect and Security |
| Verification | `tests/wire-core.test.mjs · tests/vector-law.test.mjs` |
| Status | **implemented** |

### DRN-04 — One gimbal for turret and airframe

**Statement.** The same mount and gimbal record drives a fixed turret and a drone, so nothing learned on the turret is thrown away on the airframe.

| field | value |
|---|---|
| Input / Output | `DRN-04.IN` → `DRN-04.OUT` |
| Vision • 2525 | § IV — LINK |
| U-WF trace | `U-WF-02` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | identical frustum model from both mounts |
| DTM target | turret slews and frames a door |
| Stretch | airframe mount reuses it unchanged |
| Verification | `tests/drone-gimbal.test.mjs` |
| Status | **implemented** |

### DRN-05 — Capture and shoot with line of sight

**Statement.** A capture stores the sensor's extracted edges, and a shot only counts when the ray actually reaches the door.

| field | value |
|---|---|
| Input / Output | `DRN-05.IN` → `DRN-05.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | occluded shots are refused |
| DTM target | tag a door by capture then shot |
| Stretch | live camera behind the same interface |
| Verification | `tests/drone-game.test.mjs` |
| Status | **implemented** |

### DRN-06 — Score and replay

**Statement.** A completed capture or shot is written to the replay log before the score moves, so nothing that happened can be lost.

| field | value |
|---|---|
| Input / Output | `DRN-06.IN` → `DRN-06.OUT` |
| Vision • 2525 | § VI — SYNC / Replay |
| U-WF trace | `U-WF-06` · `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | replay reproduces the score and the hash |
| DTM target | a round replays identically |
| Stretch | portable .drn2525 file |
| Verification | `tests/drone-game.test.mjs` |
| Status | **implemented** |

### DRN-07 — Command surface, route and unlock

**Statement.** The domain has its own route and its card is enterable, drawn under the vector law at any viewport down to 375 px.

| field | value |
|---|---|
| Input / Output | `DRN-07.IN` → `DRN-07.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | route builds; card enters |
| DTM target | arena visible on a phone |
| Stretch | two devices in one session |
| Verification | `next build · tests/vector-law.test.mjs` |
| Status | **implemented** |

### DRN-08 — Flight and energy, bounded

**Statement.** The flight model is a bounding estimate that says so on every run, and its published numbers reproduce from the source constants.

| field | value |
|---|---|
| Input / Output | `DRN-08.IN` → `DRN-08.OUT` |
| Vision • 2525 | § IX — Trust |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | --reproduce reports zero drift |
| DTM target | hover and cruise endurance |
| Stretch | VTOL transition envelope |
| Verification | `tests/drone-flight.test.mjs` |
| Status | **implemented** |

### DRN-09 — Two operators, two devices

**Statement.** The pilot is the only sender of flight state and the targeteer the only sender of gimbal commands.

| field | value |
|---|---|
| Input / Output | `DRN-09.IN` → `DRN-09.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | role authority refuses a wrong sender |
| DTM target | two phones share one round |
| Stretch | hosted realtime |
| Verification | `tests/drone-link.test.mjs` |
| Status | **implemented** |

### DRN-10 — AI roles under human approval

**Statement.** An AI-initiated shot never fires without a human approval event.

| field | value |
|---|---|
| Input / Output | `DRN-10.IN` → `DRN-10.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no shot without an approve event |
| DTM target | AI proposes, human approves |
| Stretch | both AI seats with humans watching |
| Verification | `tests/drone-game.test.mjs` |
| Status | **implemented** |

### DRN-11 — Vector law, the 13, and adaptive fidelity

**Statement.** Everything drawn is edges in one of the 13 colours, and resolution and frame rate rise only with compute left over after the Edge sensor's CNN reserve.

| field | value |
|---|---|
| Input / Output | `DRN-11.IN` → `DRN-11.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` · `U-WF-09` · `U-WF-11` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | model hash identical at every tier; LOW holds a Pi budget |
| DTM target | four tiers, one truth |
| Stretch | accelerator profile reaches ULTRA |
| Verification | `tests/drone-fidelity.test.mjs · tests/vector-law.test.mjs` |
| Status | **implemented** |

### DRN-12 — Every word in 33 languages

**Statement.** No user-facing string is English in the source; every one is a lexicon key present in all 33 languages.

| field | value |
|---|---|
| Input / Output | `DRN-12.IN` → `DRN-12.OUT` |
| Vision • 2525 | § XIII — Translation |
| U-WF trace | — |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | lexicon coverage green with no pending keys |
| DTM target | keys staged and listed |
| Stretch | filled in all 32 non-English |
| Verification | `tests/lexicon-coverage.test.mjs · tests/drone-i18n.test.mjs` |
| Status | **implemented** |

### DRN-13 — Versioned, revisioned, comparable

**Statement.** Every edition of this specification appends a revision that cites its commit, and any two revisions can be replayed and compared.

| field | value |
|---|---|
| Input / Output | `DRN-13.IN` → `DRN-13.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | ledger validates; render --check clean |
| DTM target | Version 00.00 revision 0.001 |
| Stretch | CRS rows compare in /crs |
| Verification | `tests/traceability.test.mjs · tests/drone-crs.test.mjs` |
| Status | **implemented** |

### DRN-11.01 — The MoT ladder: five compute bands by five resolution steps

**Statement.** Detail and sensing are asked for on a 25-rung ladder, 1.1 through 5.5, where the band is the edge-compute class and the step is the resolution; rung 1.1 is the 1983 vector arcade and is the fastest picture the machine can make.

| field | value |
|---|---|
| Input / Output | `DRN-11.01.IN` → `DRN-11.01.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` · `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | ladder monotonic in both axes; 1.1 asks for more frames than 5.5 |
| DTM target | 25 rungs selectable |
| Stretch | a robot picks its own rung unattended |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-11.04 — Demand and capability are separate, and never mixed

**Statement.** What the picture asks for (the rung) and what the machine can deliver (the HAL class and the measured frame rate) are different quantities held in different modules, and only calibration is allowed to reconcile them.

| field | value |
|---|---|
| Input / Output | `DRN-11.04.IN` → `DRN-11.04.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a richer rung never asks for a faster picture |
| DTM target | the old inverted four-tier table is gone |
| Stretch | the same split serves every 2525 domain |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-05.03 — Multi-sensor fusion, shed from the top and never blind

**Statement.** Sensing runs EO, IR, ACOUSTIC, MAG and CHEM, one more module per compute band; when the frame will not hold, modules are shed from the top of that ladder and EO is never shed, because a unit that cannot see is not degraded but blind.

| field | value |
|---|---|
| Input / Output | `DRN-05.03.IN` → `DRN-05.03.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | EO survives every degradation path |
| DTM target | five modules declared per band |
| Stretch | a real sensor replaces the simulated one behind the same interface |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-11.05 — Hold 1080p30, and name every step down from it

**Statement.** The unit tries to hold a 1920x1080 30 Hz reference stream; when it cannot it sheds a sensor module, then resolution, then frame rate, in that order, and every step is named on screen rather than taken silently.

| field | value |
|---|---|
| Input / Output | `DRN-11.05.IN` → `DRN-11.05.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | degradation order fixed; climb needs sustained headroom |
| DTM target | stream ladder live in the HUD |
| Stretch | the same ladder drives a real encoder |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-11.06 — Self-test: the rung this machine actually holds, in 6 to 15 minutes

**Statement.** Pointed at a new board or a new sensor, the unit walks the ladder from 1.1 upward under real drawing load, measures a sustained frame rate at each rung, and reports the highest rung it holds at the reference stream, inside a declared 6 to 15 minute budget.

| field | value |
|---|---|
| Input / Output | `DRN-11.06.IN` → `DRN-11.06.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-06` · `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | dwell derived from the budget; sweep stops early after two failures |
| DTM target | a saved result file naming the ceiling |
| Stretch | run headless on the robot itself |
| Verification | `tests/self-cal-runner.test.mjs` |
| Status | **implemented** |

### DRN-11.07 — The robot may pick its own compute class

**Statement.** Three compute classes are declared with their frame budgets and sensor costs, and AUTO lets the unit choose its own class from the frame rate it measures rather than from a label someone typed.

| field | value |
|---|---|
| Input / Output | `DRN-11.07.IN` → `DRN-11.07.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | AUTO thresholds derived from the budgets themselves |
| DTM target | PI / EDGE-SOC / ACCELERATOR selectable |
| Stretch | a fourth class added without touching a caller |
| Verification | `tests/mot-ladder.test.mjs` |
| Status | **implemented** |

### DRN-10.01 — Shared Intent: a key group, switched on or off

**Statement.** A closed group of named people may be switched on to weigh in on decisions; off is the normal state and the round behaves exactly as it does without them.

| field | value |
|---|---|
| Input / Output | `DRN-10.01.IN` → `DRN-10.01.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no call can open while it is off; switching off closes any open call |
| DTM target | the switch and a closed roster |
| Stretch | the group is a real polling session on Cube 1 |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-10.02 — Key members only, by invitation

**Statement.** Reaching the group's call needs a code issued to named people; an empty roster produces no invitation rather than an open link, and the right code in the wrong hands is refused.

| field | value |
|---|---|
| Input / Output | `DRN-10.02.IN` → `DRN-10.02.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | both halves must hold: on the roster, and a live code |
| DTM target | a code and a scannable square |
| Stretch | delivery through the operator's own channel |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-10.03 — The group advises; a named person decides

**Statement.** Consensus is shown beside the decision a person is about to make and can never make it: no function in the consensus engine can retire a shot request or open the authority gate, and a unanimous group changes nothing about what may fire.

| field | value |
|---|---|
| Input / Output | `DRN-10.03.IN` → `DRN-10.03.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the two modules do not import each other; unanimity does not open the gate |
| DTM target | advice beside the question |
| Stretch | the same rule wherever a crowd meets an authority |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-10.04 — Volunteer input, recognised on the ladder that already exists

**Statement.** Answering is voluntary and silence counts as nothing; a contribution is recognised as noted, and as adopted only after the decision went the way it argued, using the three fixed heart values the pod ladder already declares rather than a fourth invented here.

| field | value |
|---|---|
| Input / Output | `DRN-10.04.IN` → `DRN-10.04.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no value outside the declared ladder; silence earns nothing |
| DTM target | a readable record of what was valued |
| Stretch | entries reach the Cube 8 append-only ledger |
| Verification | `tests/si-pod.test.mjs` |
| Status | **implemented** |

### DRN-09.01 — A seat may only send what that seat controls

**Statement.** The pilot flies and cannot aim, photograph or shoot; the targeteer aims, photographs and shoots and cannot fly; a message whose payload does not belong to the seat that sent it is refused on the way in as well as on the way out.

| field | value |
|---|---|
| Input / Output | `DRN-09.01.IN` → `DRN-09.01.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the other seat's controls are absent, not disabled |
| DTM target | two seats that cannot do each other's job |
| Stretch | the same rule for a third seat |
| Verification | `tests/drone-link.test.mjs` |
| Status | **implemented** |

### DRN-09.02 — Three paths carry every word between the two devices

**Statement.** A crew message goes out on another tab, on shared storage and through a cloud endpoint at once; any one arriving is the message delivered and the duplicates the others produce are dropped without being counted as errors.

| field | value |
|---|---|
| Input / Output | `DRN-09.02.IN` → `DRN-09.02.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | two separate browser profiles exchange flight and aim |
| DTM target | a phone and a computer flying one aircraft |
| Stretch | a fourth path when one is provisioned |
| Verification | `scripts/drone-crew-e2e.mjs` |
| Status | **implemented** |

### DRN-11.08 — Measured, not assumed, on a slowed processor

**Statement.** Performance is reported from real painted frames on a processor slowed ten times, because a desktop pins every case at the refresh rate and hides any change; the heaviest case is the one quoted.

| field | value |
|---|---|
| Input / Output | `DRN-11.08.IN` → `DRN-11.08.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 48.0 to 57.3 fps at level 5.5, ten times slowed |
| DTM target | a before and after that can be compared |
| Stretch | run on the real board |
| Verification | `scripts/drone-perf.mjs` |
| Status | **implemented** |

### DRN-13.01 — Nine runs, or it is not determinism

**Statement.** Every gate runs nine times and every reproducible artefact — the model hash, the schedule, a flown path, a crew code, the rung verdicts — must be byte-identical across all nine, and the spiral is asserted in both directions rather than asserted about.

| field | value |
|---|---|
| Input / Output | `DRN-13.01.IN` → `DRN-13.01.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | 1,395 checks per run, nine runs, zero drift, ten spiral checks |
| DTM target | a report a reviewer can re-run |
| Stretch | the same harness for every domain |
| Verification | `scripts/drone-spiral9.mjs` |
| Status | **implemented** |

### DRN-09.03 — Forty-two aircraft, twenty-one against twenty-one

**Statement.** An engagement puts 42 aircraft in the air as two opposing sides of 21, held in flat columns so a tick allocates nothing, and drawn as an overlay in the arena's own camera batched into a handful of paths.

| field | value |
|---|---|
| Input / Output | `DRN-09.03.IN` → `DRN-09.03.OUT` |
| Vision • 2525 | § III — COMM |
| U-WF trace | `U-WF-02` · `U-WF-07` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a tick allocates nothing; 42 aircraft at rung 2.3 hold the 30 Hz reference on a processor slowed ten times |
| DTM target | 42 aircraft flying |
| Stretch | the same columns carry a thousand |
| Verification | `tests/drone-swarm.test.mjs · scripts/drone-perf.mjs` |
| Status | **implemented** |

### DRN-11.09 — One budget, two consumers: the aircraft are the world

**Statement.** In an engagement the swarm takes its share of the rung's segment budget first and the Capitol block takes what is left, so at a poor rung the world thins rather than the aircraft vanishing; and every aircraft is drawn at the smallest size before any is given detail.

| field | value |
|---|---|
| Input / Output | `DRN-11.09.IN` → `DRN-11.09.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | all 42 drawn at every one of the 25 rungs |
| DTM target | 42 drawn at rung 1.1 with the world at 112 segments |
| Stretch | the same rule wherever a budget is shared |
| Verification | `tests/drone-swarm.test.mjs` |
| Status | **implemented** |

### DRN-10.05 — Five authority levels, and silence is never consent on any of them

**Statement.** Autonomy rides on the band that already exists: level 1 a named person per shot, level 5 the key group's live lean as a standing mandate it can end at once; at every level a named human decision exists before any machine shot, and what scales is the breadth of one decision rather than whether one happened.

| field | value |
|---|---|
| Input / Output | `DRN-10.05.IN` → `DRN-10.05.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a refusal from all five levels when nothing has been decided |
| DTM target | five levels selectable |
| Stretch | the ladder serves any domain with a machine and an authority |
| Verification | `tests/drone-authority.test.mjs` |
| Status | **implemented** |

### DRN-13.02 — A score that can fall

**Statement.** Every SSSES pillar is computed from evidence produced in the same run — authority checks, nine-run drift, measured frames against the declared reference, whether the projection memo demonstrably holds, and duplication and dead exports counted from the source — and a performance file measured at a different commit is refused rather than trusted.

| field | value |
|---|---|
| Input / Output | `DRN-13.02.IN` → `DRN-13.02.OUT` |
| Vision • 2525 | § II — R-CORE |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no pillar is a literal; the score fell before it rose |
| DTM target | a score a reviewer can re-derive |
| Stretch | the same scorer for every domain |
| Verification | `scripts/drone-spiral9.mjs` |
| Status | **implemented** |

### DRN-05.04 — The beam, the shield, and the time it takes

**Statement.** Engagement is a fluence model, not hit points: irradiance at the target falls with beam spread and atmospheric loss, so the same emitter needs under a second in close and seconds at reach, and a fine-track lock means a broken beam loses its progress. The shield absorbs first and nothing reaches the airframe until it fails.

| field | value |
|---|---|
| Input / Output | `DRN-05.04.IN` → `DRN-05.04.OUT` |
| Vision • 2525 | § V — EDGE |
| U-WF trace | `U-WF-09` · `U-WF-11` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | the published dwell table is what the model computes, at every range |
| DTM target | a player learns to close and to hold, without being told |
| Stretch | a real emitter profile behind the same interface |
| Verification | `frontend/tests/drone-laser.test.mjs` |
| Status | **implemented** |

### DRN-05.05 — Nothing is destroyed in the air

**Statement.** A defeated aircraft is DISABLED and flown down under control to a safe landing. There is no reachable state called destroyed, and a beam on an aircraft already coming down changes nothing about its descent.

| field | value |
|---|---|
| Input / Output | `DRN-05.05.IN` → `DRN-05.05.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-06` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | every state a beam can reach is one of five, and none of them is destroyed |
| DTM target | real machines over a real floor with people underneath |
| Stretch | a physical descent check before every match |
| Verification | `frontend/tests/drone-laser.test.mjs` |
| Status | **implemented** |

### DRN-05.06 — Lighting inside the vector law

**Statement.** The engagement reads from stroke colour and weight alone — tracking, absorbing, the moment the shield fails, burning, coming down — plus a shield ring drawn as a declared n-gon that shrinks as the shield is spent. No glow, no gradient, no bloom.

| field | value |
|---|---|
| Input / Output | `DRN-05.06.IN` → `DRN-05.06.OUT` |
| Vision • 2525 | § XI — SSSES |
| U-WF trace | `U-WF-11` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | every stage names a palette role and a declared stroke weight |
| DTM target | forty-two engagements readable as shapes |
| Stretch | the same language on every 2525 surface |
| Verification | `frontend/tests/drone-laser.test.mjs` |
| Status | **implemented** |

### DRN-08.01 — One aircraft, two sizes

**Statement.** The mini foil scales to the full one by geometric similarity, with every derived quantity following the exponent it actually has: area as the square, mass as the cube, stall as the square root. The geometry, the proportions and every rule about materials or people are unchanged by the switch.

| field | value |
|---|---|
| Input / Output | `DRN-08.01.IN` → `DRN-08.01.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-01` · `U-WF-04` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | stall at the full size is the square root of the length ratio times the mini's, recomputed rather than asserted |
| DTM target | a demo at the Capitol at the size it will really be |
| Stretch | a third size needing one line and no arithmetic |
| Verification | `frontend/tests/drone-scale.test.mjs` |
| Status | **implemented** |

### DRN-10.06 — The same game at every N

**Statement.** The engagement runs at 1v1, 2v2 and every rung to 21v21 — 42 aircraft, 84 seats — from one model, so what breaks can be found on the way up rather than only at full strength. Each rung is measured for beam waste, contention, decision load and seats.

| field | value |
|---|---|
| Input / Output | `DRN-10.06.IN` → `DRN-10.06.OUT` |
| Vision • 2525 | § VIII — Rehearsal |
| U-WF trace | `U-WF-06` · `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | twenty-one rungs, three seeds each, twelve computed review lenses, all passing |
| DTM target | a complexity found at the rung where it appears |
| Stretch | the ladder run against a live crew |
| Verification | `frontend/tests/drone-contest.test.mjs` |
| Status | **implemented** |

### DRN-10.07 — The person is the bottleneck, and it is measured

**Statement.** The invariant that a machine shot needs a named human decision does not bend with N, so the decision load is measured at every rung and compared against what the people present can physically answer. Levels one to three put the decision with one person; four and five put it with a quorum of the key group, which is the only thing that raises a throughput ceiling.

| field | value |
|---|---|
| Input / Output | `DRN-10.07.IN` → `DRN-10.07.OUT` |
| Vision • 2525 | § X — Governance |
| U-WF trace | `U-WF-09` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | no rung at the top authority level asks for more decisions than its approvers can answer |
| DTM target | an argument for the authority ladder from measurement, not assertion |
| Stretch | the same measurement against real approvers |
| Verification | `frontend/scripts/drone-ladder.mjs` |
| Status | **implemented** |

### DRN-12.01 — From open play to a real floor

**Statement.** Progression runs open play to the winners' circle to a tournament to a physical match. The cut is one ninth, the R-CORE recursion gate, floored and published with its cut line. The score is dominated by accuracy and by whether decisions were answered rather than by kills.

| field | value |
|---|---|
| Input / Output | `DRN-12.01.IN` → `DRN-12.01.OUT` |
| Vision • 2525 | § XII — Flywheel |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | one ninth holds at every field size from nine to three hundred and ninety-nine |
| DTM target | the eleven per cent who want the real thing find it |
| Stretch | a season run end to end |
| Verification | `frontend/tests/drone-winners-circle.test.mjs` |
| Status | **implemented** |

### DRN-12.02 — The last gate is not about skill

**Statement.** A physical match requires a NAMED safety officer, both crews present, the arena clear of anyone not in the match, and every machine's descent checked. None of those is a score, a perfect record opens none of them, and a refusal names the condition that is missing without discarding the entry.

| field | value |
|---|---|
| Input / Output | `DRN-12.02.IN` → `DRN-12.02.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-08` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | a champion with a perfect record still cannot clear an arena |
| DTM target | nobody is hurt by a tournament |
| Stretch | a signed floor clearance on the record |
| Verification | `frontend/tests/drone-winners-circle.test.mjs` |
| Status | **implemented** |

### DRN-11.10 — The aircraft is the FOIL

**Statement.** This surface calls the aircraft the FOIL and uses no other name for it — not in a label, a lexicon value, a CRS row, a component, a shipped document or an export. The geometry provenance path is the single declared exception, because a derivation that cannot name its source cannot be verified.

| field | value |
|---|---|
| Input / Output | `DRN-11.10.IN` → `DRN-11.10.OUT` |
| Vision • 2525 | § IX — Trust |
| U-WF trace | `U-WF-05` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | forty-four files scanned, the token refused outside two declared provenance fields |
| DTM target | a name we chose, used everywhere |
| Stretch | the provenance carried under a neutral path |
| Verification | `frontend/tests/drone-naming.test.mjs` |
| Status | **implemented** |

### DRN-07.01 — The address the game answers at

**Statement.** The game is served at /drone-2525 as well as at /main/Drone-2525. Both routes render the one component, their code is identical, and both are emitted to the static export — a route that compiles but does not emit is a 404 in production and green in CI.

| field | value |
|---|---|
| Input / Output | `DRN-07.01.IN` → `DRN-07.01.OUT` |
| Vision • 2525 | § I — Humanity First |
| U-WF trace | `U-WF-12` |
| R-CORE mode | Manual |
| De-risk phase | pilot |
| Declared metric M | both pages exist, import the same component, and appear in out/ |
| DTM target | a link that works when it is handed to somebody |
| Stretch | the live site checked from outside the sandbox |
| Verification | `frontend/tests/drone-route.test.mjs` |
| Status | **implemented** |

## Revisions (append only)

| revision | date | kind | commit | why |
|---|---|---|---|---|
| 0.001 | 2026-09-15 | ask | `7b0b790` | Drone-2525 1st Pass persisted verbatim; the CRS ladder opens at Version 00.00. |
| 0.002 | 2026-09-15 | decision | `ea91e01` | The MoT ladder replaces the four-tier fidelity table: 5 compute bands x 5 resolution steps, 1.1 the arcade rung and the fastest. Demand and capability are separated. Multi-sensor fusion EO->CHEM sheds from the top and never sheds EO. The unit holds 1080p30 and names every step down. A self-test walks the ladder in 6 to 15 minutes and reports the rung this machine actually holds. |
| 0.003 | 2026-09-15 | release | `a3cb92f` | The flying modes. A fixed-wing VTOL hovers on rotors and cruises on a wing, with the stall speed as the number the transition turns on. A machine may hold the pilot's seat, the camera operator's seat, or both — and a machine that is aiming must ask a named person before any shot, a gate with no bypass. All four of the operator's modes are unlocked. |
| 0.004 | 2026-09-15 | release | `a1a9ff7` | Shared Intent, as a placeholder: a key group switched on or off, invited by a code and a scannable square, answering a real-time call on the decision the round already has. The group advises and a named person still decides. Recognition uses the three fixed heart values the pod ladder already declares, rather than a fourth invented for this feature. |
| 0.005 | 2026-09-15 | release | `dffaa79` | Two people on two devices: one flies, one works the camera, and neither can do the other's job. Optimised against a measurement on a processor slowed ten times rather than against a desktop that hides every change. Nine runs of every gate with zero drift, ten spiral checks, SSSES 93. |
| 0.006 | 2026-09-15 | release | `c7c40a5` | Forty-two aircraft, 21 against 21, inside the ladder's budget at every rung. A five-level authority ladder on the band that already exists, with the invariant unbent on every rung. And an SSSES scorer rewritten to measure rather than assert, which dropped the score before the work lifted it to 96. |
| 0.007 | 2026-09-16 | decision | `` | The fixed-wing is declared at the 1.111 m foil, proportional, from the early Security-2525 drawing — and the drawing's axes are declared with it, because the shipped extent had span, depth and nose-to-tail under each other's names and every consumer inherited the permutation. |
| 0.008 | 2026-09-16 | decision | `` | The wing is derived from the drawing instead of asserted beside it. wingAreaM2 was 0.9 m2 against a real planform of 0.3092 m2, so the round had been flying a wing three times the size of the one on screen. Stall moves 8.43 -> 14.38 m/s, and the quad's acceleration is now derived from the stall rather than typed, so the transition stays reachable on any airframe. |
| 0.009 | 2026-09-16 | decision | `` | The aircraft is the FOIL on this surface and nowhere says its other name, with one declared exception for the provenance path. The game answers at /drone-2525 as well as /main/Drone-2525. |
| 0.010 | 2026-09-16 | decision | `` | Lasers, shields and time on target. A fluence model rather than hit points, so range is the thing that matters: 0.79 s to disable at 100 m, 5.15 s at 400 m, 13.67 s at 700 m. A defeated aircraft is disabled and flown down under control at 1.6 m/s; there is no state called destroyed. |
| 0.011 | 2026-09-16 | decision | `` | Ten CRS rows for the overnight work: the beam and shield rules, the safe descent, the lighting inside the vector law, the two sizes and the square-cube law between them, the N ladder and what it measured about the person, the winners' circle and its one-ninth cut, the floor that must be cleared by name, the FOIL naming rule, and the address the game answers at. |

