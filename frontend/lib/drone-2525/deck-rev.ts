// The served operator-deck revision, for the shell's PLAY button. ONE source with tests/deck-head.mjs (the gate
// drone-playable holds the two equal), so the button can never say r.128 while the bytes are r.131 (fleet lens 12B).
export const DECK_REV = "135";
