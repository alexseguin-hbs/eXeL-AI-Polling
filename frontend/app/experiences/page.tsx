// Canonical Experiences route — /experiences (the short, shareable URL).
import { ExperiencesLanding } from "@/components/experiences/experiences-landing";

export default function Page() {
  return <ExperiencesLanding basePath="/experiences" />;
}
