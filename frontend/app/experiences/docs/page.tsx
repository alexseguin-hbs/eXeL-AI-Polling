// Canonical portfolio route — /experiences/docs (opened by the Experiences QR).
import { ExperiencesPortfolio } from "@/components/experiences/experiences-portfolio";

export default function Page() {
  return <ExperiencesPortfolio basePath="/experiences" />;
}
